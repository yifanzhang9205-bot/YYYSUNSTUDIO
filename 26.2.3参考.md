1. 状态同步的“幽灵” (Race Conditions)
在 1.3 和 1.4 中，你使用了 textNodeStore 和 useTextNodeActions。

坑点：当用户快速切换模式或频繁修改 Prompt 时，API 请求（Gemini/Coze）可能还在进行中。如果没有完善的请求取消机制（如 AbortController），旧请求的返回可能会覆盖新请求的结果，导致 UI 显示错误。

建议：在 textNodeStore 中为每个节点增加一个 pendingRequestId 标识。

2. IndexedDB 与内存泄漏
在 4.2 任务中提到使用 Blob URL。

坑点：URL.createObjectURL() 创建的链接除非页面关闭，否则不会自动释放。如果用户频繁上传图片或切换模式，会导致浏览器内存持续上升。

建议：在图片更新或组件销毁时，务必调用 URL.revokeObjectURL(oldUrl)。

3. “自动创建节点”的递归风险
任务 5.2 提到进入文生图模式时“自动创建输出节点”。

坑点：如果逻辑写在 useEffect 或模式切换的同步逻辑里，容易触发“创建节点 -> 触发状态更新 -> 又触发创建”的死循环。

建议：确保这个逻辑是单次触发的（Action 驱动），并且在创建前检查该节点是否已经存在对应的 outputNodeId。

4. 节点高度的“弹性伸缩”
坑点：1.2 设置了固定高度 280px。但 3.1（大面积文本输入）和 4.1（图片预览 + 提示词编辑）的内容量差异巨大。如果高度固定，UI 会非常拥挤；如果高度自适应，Canvas 的布局计算可能会受影响。

建议：在 TextNode 主组件中使用 min-height，并确保 Reactflow（或你使用的画布库）能监听到节点尺寸变化并更新连接线位置。