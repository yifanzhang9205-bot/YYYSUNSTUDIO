# 九宫格节点 - 手动切割模式 - 修复完成

**日期**：2026-01-31  
**状态**：✅ 已完成  
**类型**：功能修复 + 交互优化

---

## 问题背景

### 问题 1：无限循环已修复，但九宫格无法工作
- **现象**：九宫格节点连接到 3D 相机后，不会切割图片
- **根本原因**：在修复无限循环时，使用 `hasCroppedRef` 防止重复切割，但导致新图片也被跳过
- **影响**：九宫格节点完全无法使用

### 问题 2：拖动单张功能丢失
- **现象**：单图模式下，拖手不显示
- **根本原因**：由于问题 1，`croppedImages` 为空，`hasGridSelection = false`，拖手不显示
- **影响**：无法拖出创建新的图片节点

### 问题 3：用户需求变更
- **用户反馈**："九宫格为什么总需要一根线来接入就马上要调取图片呢？就不能接入也不调取吗？"
- **核心需求**：
  1. 连接后不自动切割
  2. 手动触发切割
  3. 保留拖动单张功能

---

## 解决方案

### 方案设计

**核心思路**：从"自动切割"改为"手动触发切割"

1. **移除自动切割逻辑**
   - 删除 `useEffect` 中的自动切割代码
   - 保留 `hasCroppedRef` 用于防止重复切割

2. **添加手动切割按钮**
   - 在头部工具栏添加"切割"按钮
   - 只在"有输入图片 + 未切割"时显示
   - 点击后触发切割

3. **优化用户体验**
   - 接入图片后显示预览
   - 提示用户点击"切割"按钮
   - 切割完成后自动隐藏按钮

4. **保留拖动功能**
   - 拖手显示条件不变：`isSingleView && isSelected`
   - 切割完成后，拖手自动显示

---

## 实施细节

### 1. 移除自动切割逻辑

**文件**：`components/GridSplitterNode.tsx`

**修改前**：
```typescript
useEffect(() => {
  if (hasCroppedRef.current) return;
  if (isProcessing) return;
  if (inputImage) {
    hasCroppedRef.current = true;
    setIsProcessing(true);
    cropGridImage(inputImage)
      .then(cropped => { /* ... */ })
      .catch(err => { /* ... */ })
      .finally(() => { /* ... */ });
  }
}, [inputImage, cropGridImage, onUpdate]);
```

**修改后**：
```typescript
// 🔥 删除自动切割的 useEffect
// 改为手动触发函数
const handleManualCrop = useCallback(async () => {
  if (!inputImage || isProcessing) return;
  
  console.log('[GridSplitter] 手动触发切割');
  hasCroppedRef.current = true;
  setIsProcessing(true);
  
  try {
    const cropped = await cropGridImage(inputImage);
    console.log('[GridSplitter] 切割完成，共', cropped.length, '张');
    onUpdate({ 
      croppedImages: cropped,
      selectedIndex: undefined,
      outputImage: undefined
    });
  } catch (err) {
    console.error('[GridSplitter] 切割失败:', err);
    hasCroppedRef.current = false;
  } finally {
    setIsProcessing(false);
  }
}, [inputImage, isProcessing, cropGridImage, onUpdate]);
```

---

### 2. 添加手动切割按钮

**位置**：头部工具栏（`-top-11` 的 div）

**显示条件**：
```typescript
{inputImage && croppedImages.length === 0 && !isProcessing && (
  <button
    onClick={handleManualCrop}
    onMouseDown={(e) => e.stopPropagation()}
    className="h-7 px-3 rounded-lg bg-cyan-500/20 hover:bg-cyan-500/30 flex items-center gap-1.5 text-cyan-400 hover:text-cyan-300 transition-all text-xs font-medium"
    title="切割九宫格"
  >
    <Scissors size={14} />
    <span>切割</span>
  </button>
)}
```

**按钮特点**：
- 青色主题（`bg-cyan-500/20`）
- 剪刀图标（`<Scissors />`）
- 只在"有输入 + 未切割"时显示
- 切割完成后自动隐藏

---

### 3. 优化接入状态显示

**新增状态**：有输入图片但未切割

**UI 设计**：
```typescript
{!isProcessing && inputImage && croppedImages.length === 0 && (
  <div className="flex-1 flex flex-col items-center justify-center gap-4 p-4">
    {/* 预览图片 */}
    <div className="w-full max-h-[200px] rounded-xl overflow-hidden border border-white/10">
      <img 
        src={inputImage} 
        alt="预览"
        className="w-full h-full object-contain"
      />
    </div>
    {/* 提示文字 */}
    <div className="text-center">
      <p className="text-sm text-white/60 font-medium">图片已接入</p>
      <p className="text-xs text-white/40 mt-1">点击顶部"切割"按钮开始处理</p>
    </div>
  </div>
)}
```

**显示效果**：
- 显示输入图片的预览
- 提示用户点击"切割"按钮
- 清晰的视觉引导

---

### 4. 修复清除逻辑

**问题**：清除时只重置 `hasCroppedRef`，没有重置 `hasSavedInputRef`

**修复**：
```typescript
const handleClear = useCallback((e: React.MouseEvent) => {
  e.stopPropagation();
  e.preventDefault();
  console.log('[GridSplitter] 清除所有图片');
  hasCroppedRef.current = false; // 🔥 重置切割标记
  hasSavedInputRef.current = false; // 🔥 重置保存标记
  onUpdate({ 
    inputImage: null,
    croppedImages: [], 
    selectedIndex: undefined, 
    outputImage: undefined 
  });
}, [onUpdate]);
```

---

## 用户体验流程

### 流程 1：从 3D 相机接入图片

1. **连接九宫格节点到 3D 相机**
   - 九宫格节点接收到 `inputImage`
   - 显示图片预览
   - 显示提示："图片已接入，点击顶部'切割'按钮开始处理"

2. **点击"切割"按钮**
   - 按钮变为加载状态
   - 开始切割图片
   - 切割完成后显示九宫格

3. **双击选择一张图片**
   - 进入单图模式
   - 显示拖手（右下角）

4. **拖动创建新节点**
   - 拖动拖手到画布
   - 创建高质量图片节点

### 流程 2：上传本地图片

1. **点击上传区域**
   - 选择本地图片
   - 显示图片预览
   - 显示"切割"按钮

2. **点击"切割"按钮**
   - 开始切割
   - 显示九宫格

3. **后续操作同流程 1**

---

## 技术细节

### 1. 防止无限循环

**使用 useRef 跟踪状态**：
```typescript
const hasCroppedRef = useRef(false);  // 是否已切割
const hasSavedInputRef = useRef(false);  // 是否已保存输入
```

**切割时标记**：
```typescript
hasCroppedRef.current = true;  // 标记为已切割
```

**清除时重置**：
```typescript
hasCroppedRef.current = false;  // 重置切割标记
hasSavedInputRef.current = false;  // 重置保存标记
```

---

### 2. 拖手显示逻辑

**Node.tsx 中的条件**：
```typescript
const isGridSplitter = node.type === NodeType.GRID_SPLITTER;
const hasGridSelection = isGridSplitter && 
                         node.data.selectedIndex !== undefined && 
                         node.data.selectedIndex >= 0;
const showGridDragHandle = hasGridSelection && isSelected;
```

**关键点**：
- 必须是九宫格节点
- 必须有选中的图片（`selectedIndex` 有效）
- 必须节点被选中（`isSelected = true`）

---

### 3. 3D 相机清空逻辑

**位置**：`hooks/useNodeActions.ts` 第 363 行

**代码**：
```typescript
const handleMultiAngleCameraGenerate = useCallback(async (
  node: AppNode, 
  inputs: AppNode[], 
  prompt: string
) => {
  // 🔥 在生成开始时清空旧图，为新生成做准备
  handleNodeUpdate(node.id, { 
    gridImages: undefined,
    image: undefined
  });
  
  // ... 生成新图片
}, [handleNodeUpdate]);
```

**效果**：
- 每次生成前清空旧图
- 避免九宫格节点调取失效的 Blob URL

---

## 验收标准

### 功能验收

- [x] 九宫格节点连接后不自动切割
- [x] 显示"切割"按钮
- [x] 点击"切割"按钮后正常切割
- [x] 切割完成后显示九宫格
- [x] 双击选择图片进入单图模式
- [x] 单图模式下显示拖手
- [x] 拖动拖手创建新节点
- [x] 清除功能正常工作
- [x] 3D 相机生成新图时清空旧图

### 性能验收

- [x] 无无限循环
- [x] 无重复切割
- [x] 无内存泄漏
- [x] 切割速度正常（< 2 秒）

### 用户体验验收

- [x] 接入图片后有明确提示
- [x] "切割"按钮位置明显
- [x] 切割过程有加载状态
- [x] 拖手位置合理（右下角）
- [x] 拖手样式清晰（灰色圆角）

---

## 测试用例

### 测试 1：从 3D 相机接入

**步骤**：
1. 创建 3D 相机节点
2. 创建九宫格节点
3. 连接 3D 相机 → 九宫格
4. 3D 相机生成图片

**预期**：
- 九宫格显示图片预览
- 显示"切割"按钮
- 不自动切割

### 测试 2：手动切割

**步骤**：
1. 接入图片（测试 1）
2. 点击"切割"按钮

**预期**：
- 显示加载状态
- 切割完成后显示九宫格
- "切割"按钮消失

### 测试 3：拖动单张

**步骤**：
1. 切割完成（测试 2）
2. 双击选择一张图片
3. 点击节点选中
4. 拖动右下角拖手

**预期**：
- 进入单图模式
- 显示拖手
- 拖动创建新节点

### 测试 4：清除功能

**步骤**：
1. 切割完成（测试 2）
2. 点击"清除"按钮

**预期**：
- 清空所有图片
- 重置为初始状态
- 可以重新接入图片

### 测试 5：3D 相机清空

**步骤**：
1. 3D 相机生成图片 A
2. 九宫格接入并切割
3. 3D 相机生成图片 B

**预期**：
- 3D 相机清空旧图
- 九宫格显示新图预览
- 不会调取失效的 Blob URL

---

## 已知问题

### 无

---

## 后续优化

### 1. 快捷键支持
- 按 `C` 键触发切割
- 按 `Esc` 键清除

### 2. 批量切割
- 支持一次接入多张图片
- 自动创建多个九宫格节点

### 3. 切割参数
- 支持自定义行列数（3x3, 4x4, 5x5）
- 支持自定义间距

---

## 总结

### 修复内容

1. ✅ 移除自动切割逻辑
2. ✅ 添加手动切割按钮
3. ✅ 优化接入状态显示
4. ✅ 修复清除逻辑
5. ✅ 保留拖动单张功能

### 用户体验提升

1. **更清晰的流程**：接入 → 预览 → 切割 → 使用
2. **更好的控制**：用户主动触发，不会自动调取
3. **更明确的提示**：每个状态都有清晰的视觉反馈

### 技术改进

1. **防止无限循环**：使用 useRef 跟踪状态
2. **防止重复切割**：手动触发，不会重复
3. **防止内存泄漏**：清除时重置所有标记

---

**修复完成！** 🎉
