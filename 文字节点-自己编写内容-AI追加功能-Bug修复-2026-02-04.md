# 文字节点"自己编写内容"模块 - Bug 修复完成

**日期**: 2026-02-04  
**状态**: ✅ 已完成

---

## 修复的问题

### 问题 1: `require is not defined` 错误

**错误信息**:
```
Uncaught ReferenceError: require is not defined
    at Node.tsx:918:38
```

**问题原因**:
在 Node.tsx 的 JSX 中直接使用了 `useTextNodeStore.getState()`，这在某些情况下会导致模块加载问题。

**解决方案**:
创建了独立的 `AIInputBarWrapper` 组件，将 Hook 调用移到组件内部：

```tsx
// AIInputBarWrapper - 包装 AIInputBar，避免在 JSX 中使用 Hook
const AIInputBarWrapper: React.FC<{
  node: AppNode;
  nodeWidth: number;
  nodeHeight: number;
  isWorking: boolean;
  onUpdate: (id: string, data: Partial<AppNode['data']>, size?: { width?: number, height?: number }, title?: string) => void;
}> = ({ node, nodeWidth, nodeHeight, isWorking, onUpdate }) => {
  // 使用 Hook 获取节点数据
  const nodeData = useTextNodeStore(state => state.getNode(node.id));
  const isManualMode = nodeData?.mode === 'manual';
  
  return (
    <AIInputBar
      nodeId={node.id}
      nodeX={node.x}
      nodeY={node.y}
      nodeWidth={nodeWidth}
      nodeHeight={nodeHeight}
      isVisible={true}
      onSend={(nodeId, prompt, model) => {
        console.log('[Node] AI 输入:', { nodeId, prompt, model });
        
        if (isManualMode) {
          // MANUAL 模式：追加内容到文本框
          const currentPrompt = nodeData?.prompt || '';
          const newPrompt = currentPrompt 
            ? `${currentPrompt}\n\n${prompt}`
            : prompt;
          useTextNodeStore.getState().updatePrompt(nodeId, newPrompt);
        } else {
          // 其他模式：更新节点的 prompt 和 model
          onUpdate(nodeId, { prompt, model });
        }
      }}
      placeholder={isManualMode ? "输入你的灵感，AI 会帮你生成内容..." : "描述你想要的内容，AI 会帮你实现..."}
      isGenerating={isWorking}
    />
  );
};
```

**修改文件**:
- `components/Node.tsx`

---

### 问题 2: 文字节点周围出现虚线

**问题描述**:
文字节点周围出现蓝色虚线，不固定位置，偶尔会看见。

**问题原因**:
这是 **Helper Lines（辅助线）** 功能，来自之前实施的 UI 改版（React Flow 风格）。辅助线的检测逻辑有 Bug，导致在非拖动状态下也会显示。

**解决方案**:
在 `useDrag.ts` 的 `detectHelperLines` 函数中添加拖动状态检查：

```typescript
const detectHelperLines = useCallback((
  draggingNode: AppNode,
  allNodes: Map<string, AppNode>,
  threshold: number = 5
): HelperLine[] => {
  // 🔥 修复：如果不在拖动状态，直接返回空数组
  if (!isDragging) return [];
  
  // ... 其他检测逻辑
}, [isDragging]); // 🔥 添加 isDragging 依赖
```

**修改文件**:
- `hooks/useDrag.ts`

---

## 辅助线功能说明

### 什么是 Helper Lines？

Helper Lines（辅助线）是一个对齐辅助功能，类似于 Figma、Sketch 等设计工具中的智能参考线。

**功能**:
- 拖动节点时，自动检测与其他节点的对齐关系
- 显示蓝色虚线，提示用户可以对齐的位置
- 支持左对齐、右对齐、中心对齐、顶对齐、底对齐、垂直居中对齐

**触发条件**:
- 只在拖动节点时显示
- 当拖动的节点与其他节点的边缘或中心距离小于 5px 时，显示辅助线
- 松手后自动消失

**视觉效果**:
- 蓝色虚线（`stroke="#3b82f6"`）
- 虚线样式（`strokeDasharray="4 4"`）
- 不可交互（`pointer-events-none`）

---

## 测试步骤

### 测试 1: require 错误修复

1. 清除浏览器缓存（Ctrl+Shift+Delete 或 Ctrl+F5）
2. 刷新页面
3. 创建一个文字节点
4. 检查控制台，确认没有 `require is not defined` 错误

### 测试 2: 辅助线功能

1. 创建多个节点
2. 拖动其中一个节点
3. 当节点接近其他节点时，应该看到蓝色虚线
4. 松手后，虚线应该立即消失
5. 静止状态下，不应该看到任何虚线

### 测试 3: AI 追加功能

1. 创建一个文字节点，选择"自己编写内容"模式
2. 在文本框中输入一些内容（例如："这是第一段"）
3. 在下方 AI 对话框中输入内容（例如："继续写第二段"）
4. 点击发送
5. 确认 AI 生成的内容追加到文本框，格式为：
   ```
   这是第一段
   
   [AI 生成的内容]
   ```

---

## 相关文档

- `UI改版-阶段2-功能3-Helper-Lines-完成-2026-02-01.md` - 辅助线功能实施文档
- `文字节点-自己编写内容-AI追加功能-2026-02-04.md` - AI 追加功能实施文档
- `hooks/useDrag.ts` - 拖拽逻辑和辅助线检测
- `components/Node.tsx` - 节点组件和 AI 输入框集成

---

## 总结

✅ **问题 1 已修复**: `require is not defined` 错误已解决，通过创建独立组件避免在 JSX 中使用 Hook  
✅ **问题 2 已修复**: 辅助线只在拖动时显示，静止状态下不会出现虚线  
✅ **功能正常**: AI 追加功能正常工作，内容正确追加到文本框

**下一步**:
- 测试浏览器缓存清除后的效果
- 验证所有文字节点模式的 AI 输入功能
- 确认辅助线在所有节点类型上都正常工作
