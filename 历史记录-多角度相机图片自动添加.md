# 历史记录 - 批量下载和持久化修复

## 问题描述

用户反馈了两个问题：

### 问题 1：批量下载不方便
- 每次下载都要单独保存，浏览器弹出多次保存对话框
- 无法一次性下载所有选中的图片

### 问题 2：清除失效
- 下载并清除后，刷新页面，已清除的记录又出现了
- 说明 `assetHistory` 没有正确保存到 localStorage

## 解决方案

### 修复 1：ZIP 批量下载

**使用 JSZip 打包下载多张图片：**

1. **单张图片：** 直接下载（不打包）
2. **多张图片：** 打包成 ZIP 文件，一次性下载

**优势：**
- 只弹出一次保存对话框
- 所有图片打包在一个 ZIP 文件中
- 文件名自动编号，避免冲突
- ZIP 文件名包含日期：`历史记录-2025-01-24.zip`

### 修复 2：立即保存到 localStorage

**问题原因：**
- 原来依赖 `useEffect` 自动保存，有延迟
- 刷新页面时，可能还没保存完成

**解决方法：**
- 删除历史记录后，**立即调用 `saveToStorage`**
- 不等待 `useEffect` 触发
- 确保数据持久化

## 修改内容

### 文件：`App.tsx`

```typescript
const downloadSelectedImagesAndClear = useCallback(async (selectedIds: Set<string>) => {
    // ... 收集要下载的图片 ...
    
    if (assetsToDownload.length === 1) {
        // 单张图片：直接下载
        const asset = assetsToDownload[0];
        const a = document.createElement('a');
        a.href = asset.src;
        a.download = `${asset.title || 'image'}.png`;
        a.click();
    } else {
        // 多张图片：打包成 ZIP 下载
        const JSZip = (await import('jszip')).default;
        const zip = new JSZip();
        
        // 将所有图片添加到 ZIP
        for (let i = 0; i < assetsToDownload.length; i++) {
            const asset = assetsToDownload[i];
            const response = await fetch(asset.src);
            const blob = await response.blob();
            const filename = `${i + 1}-${asset.title || 'image'}.png`;
            zip.file(filename, blob);
        }
        
        // 生成并下载 ZIP 文件
        const zipBlob = await zip.generateAsync({ type: 'blob' });
        const a = document.createElement('a');
        a.href = URL.createObjectURL(zipBlob);
        a.download = `历史记录-${new Date().toISOString().slice(0, 10)}.zip`;
        a.click();
    }
    
    // 从历史记录中删除
    const newAssetHistory = assetHistory.filter(a => !selectedIds.has(a.id));
    setAssetHistory(newAssetHistory);
    
    // ✅ 立即保存到 localStorage（不等待 useEffect）
    await saveToStorage('assets', newAssetHistory);
    
    // 清除 Blob URL
    for (const asset of assetsToDownload) {
        if (asset.src.startsWith('blob:')) {
            URL.revokeObjectURL(asset.src);
        }
    }
}, [assetHistory]);
```

### 依赖安装

```bash
npm install jszip
```

## 工作流程

### 下载单张图片

1. 用户勾选 1 张图片
2. 点击"下载并清除"
3. 浏览器弹出保存对话框（1 次）
4. 保存为 PNG 文件
5. 从历史记录中删除
6. 立即保存到 localStorage ✅
7. 释放内存

### 下载多张图片

1. 用户勾选多张图片（或全选）
2. 点击"下载并清除"
3. 后台打包成 ZIP 文件
4. 浏览器弹出保存对话框（1 次）✅
5. 保存为 `历史记录-2025-01-24.zip`
6. 从历史记录中删除
7. 立即保存到 localStorage ✅
8. 释放内存

### ZIP 文件结构

```
历史记录-2025-01-24.zip
├── 1-多角度相机 - 角度 1.png
├── 2-多角度相机 - 角度 2.png
├── 3-多角度相机 - 角度 3.png
├── 4-多角度相机 - 角度 4.png
├── 5-多角度相机 - 角度 5.png
├── 6-多角度相机 - 角度 6.png
├── 7-多角度相机 - 角度 7.png
├── 8-多角度相机 - 角度 8.png
└── 9-多角度相机 - 角度 9.png
```

## 测试步骤

### 测试批量下载

1. 生成一个多角度相机的九宫格（9 张图片）
2. 打开历史记录面板
3. 点击"全选"按钮
4. 点击"下载并清除"
5. **应该只弹出 1 次保存对话框** ✅
6. 保存的是一个 ZIP 文件
7. 解压 ZIP，里面有 9 张图片

### 测试持久化

1. 生成一些图片到历史记录
2. 勾选部分图片
3. 点击"下载并清除"
4. **刷新页面（F5）** ✅
5. 历史记录中应该没有已清除的图片
6. 只剩下未下载的图片

### 测试单张下载

1. 生成 1 张图片到历史记录
2. 勾选这张图片
3. 点击"下载并清除"
4. **直接下载 PNG 文件（不打包）** ✅
5. 历史记录清空

## 技术细节

### JSZip 使用

```typescript
// 动态导入（减少初始加载体积）
const JSZip = (await import('jszip')).default;
const zip = new JSZip();

// 添加文件
zip.file('filename.png', blob);

// 生成 ZIP
const zipBlob = await zip.generateAsync({ type: 'blob' });

// 下载
const url = URL.createObjectURL(zipBlob);
// ... 触发下载 ...
URL.revokeObjectURL(url); // 清理
```

### 立即保存

```typescript
// 更新状态
setAssetHistory(newAssetHistory);

// 立即保存（不等待 useEffect）
await saveToStorage('assets', newAssetHistory);
```

### 文件名处理

```typescript
// 使用序号避免文件名冲突
const filename = `${i + 1}-${asset.title || 'image'}.png`;

// ZIP 文件名包含日期
const zipFilename = `历史记录-${new Date().toISOString().slice(0, 10)}.zip`;
// 例如：历史记录-2025-01-24.zip
```

## 完成状态

✅ 多张图片打包成 ZIP 下载（只弹出 1 次保存对话框）
✅ 单张图片直接下载（不打包）
✅ 删除后立即保存到 localStorage（刷新不会恢复）
✅ 清除 Blob URL 释放内存
✅ 安装 jszip 依赖
✅ 构建成功，无错误

---

**修改时间：** 2025-01-24
**修改文件：** `App.tsx`, `package.json`
**新增依赖：** jszip
**测试状态：** 待用户测试
