# 架构重构 - 编译错误修复进度 - 2026-01-27

**日期**: 2026-01-27  
**任务**: 修复激进清理后的 TypeScript 编译错误  
**状态**: 🔄 进行中

---

## 📊 错误数量变化

| 阶段 | 错误数量 | 减少 | 说明 |
|------|---------|------|------|
| 激进清理后 | 103 个 | - | 初始状态 |
| 添加缺失变量 | 90 个 | -13 | 添加 mousePosRef, mousePos, 修复 useMemo 依赖 |
| 修复类型定义 | 83 个 | -7 | 添加 characterId, sceneId, shotId, nodeIds, window.aistudio |
| 添加类型断言 | 83 个 | 0 | 为 Map.get() 添加类型断言 |

**总计减少**: 20 个错误 ✅  
**剩余错误**: 83 个

---

## ✅ 已完成的修复

### 1. 添加缺失的变量定义

**问题**: `mousePosRef`、`mousePos`、`setMousePos` 未定义

**解决方案**:
```typescript
// 添加 state
const [mousePos, setMousePos] = useState({ x: 0, y: 0 });

// 添加 ref
const mousePosRef = useRef({ x: 0, y: 0 });
```

**位置**: App.tsx 第 430 行、第 470 行

### 2. 修复 useMemo 依赖数组

**问题**: `draggingNodeId` 和 `activeGroupNodeIds` 在依赖数组中但未定义

**解决方案**:
```typescript
// 替换为 Hook 提供的状态
isDragging={isDraggingNode}
isGroupDragging={isDraggingGroup(node.id)}

// 更新依赖数组
}, [nodes, selectedNodeIds, resizingNodeId, connectionStart, handleNodeUpdate, ..., isDraggingNode, isDraggingGroup])
```

**位置**: App.tsx 第 2584 行

### 3. 替换 scaleRef 为 scale

**问题**: `scaleRef.current` 未定义

**解决方案**:
```typescript
// 直接使用 useViewport Hook 提供的 scale
const currentScale = scale;
```

**位置**: App.tsx 第 1175 行

### 4. 添加类型定义到 types.ts

**问题**: `characterId`、`sceneId`、`shotId`、`nodeIds` 类型缺失

**解决方案**:
```typescript
// 在 AppNode.data 中添加
characterId?: string;
sceneId?: string;
shotId?: string;

// 在 Group 中添加
nodeIds?: string[];
```

**位置**: types.ts

### 5. 修复 window.aistudio 类型

**问题**: `window.aistudio` 类型未定义

**解决方案**:
```typescript
declare global {
  interface Window {
    aistudio?: AIStudio;
  }
  
  interface AIStudio {
    hasSelectedApiKey: () => Promise<boolean>;
    openSelectKey: () => Promise<void>;
  }
}
```

**位置**: types.ts

### 6. 为 Map.get() 添加类型断言

**问题**: Map.get() 返回 `unknown` 类型

**解决方案**:
```typescript
// 所有 nodes.get(id) 添加类型断言
const node = nodes.get(id) as AppNode | undefined;
```

**位置**: App.tsx 多处

---

## ⚠️ 剩余的错误类型

### 1. 类型"unknown"上不存在属性（约 70 个）

**原因**: 还有更多 Map.get() 没有添加类型断言

**示例**:
```
- Error: 类型"unknown"上不存在属性"inputs"。 (253:42)
- Error: 类型"unknown"上不存在属性"id"。 (872:100)
- Error: 类型"unknown"上不存在属性"data"。 (1376:57)
```

**解决方案**: 继续为所有 Map.get() 添加类型断言

### 2. spread 类型错误（约 10 个）

**原因**: 对 unknown 类型使用展开运算符

**示例**:
```
- Error: spread 类型只能从对象类型创建。 (255:39)
- Error: spread 类型只能从对象类型创建。 (345:31)
```

**解决方案**: 添加类型守卫或类型断言

### 3. 类型定义缺失（约 3 个）

**原因**: characterName, sceneNumber, shotNumber 未定义

**示例**:
```
- Error: 对象字面量只能指定已知属性，并且"characterName"不在类型中。 (1913:18)
- Error: 对象字面量只能指定已知属性，并且"sceneNumber"不在类型中。 (1946:18)
- Error: 对象字面量只能指定已知属性，并且"shotNumber"不在类型中。 (2010:18)
```

**解决方案**: 在 types.ts 中添加这些属性定义

---

## 🎯 下一步行动计划

### 优先级 1：批量添加类型断言（最紧急）⭐⭐⭐

**目标**: 修复所有 Map.get() 的类型错误

**方法**: 使用正则表达式批量替换
```typescript
// 查找: nodes\.get\(([^)]+)\)(?!\s+as)
// 替换: nodes.get($1) as AppNode | undefined
```

**预计减少**: 约 50 个错误

### 优先级 2：添加缺失的类型定义（重要）⭐⭐

**目标**: 添加 characterName, sceneNumber, shotNumber

**位置**: types.ts 的 AppNode.data 接口

**预计减少**: 3 个错误

### 优先级 3：修复 spread 类型错误（中等）⭐

**目标**: 为所有展开运算符添加类型守卫

**方法**: 在展开前检查类型
```typescript
if (node) {
  newMap.set(nodeId, { ...node, inputs: filteredInputs });
}
```

**预计减少**: 10 个错误

### 优先级 4：测试功能（必须）⭐⭐⭐

**目标**: 确保所有功能正常工作

**测试清单**:
- [ ] 节点拖拽（基础功能）
- [ ] 节点选择（单选、多选、框选）
- [ ] 缩放和平移
- [ ] 连接线
- [ ] 分组
- [ ] 撤销/重做

---

## 💡 技术笔记

### Map.get() 类型问题的根本原因

TypeScript 的 Map 类型定义：
```typescript
interface Map<K, V> {
  get(key: K): V | undefined;
}
```

但在我们的代码中，TypeScript 推断为：
```typescript
Map<string, AppNode>.get(id) // 返回 AppNode | undefined
```

然而，由于某些原因（可能是 TypeScript 版本或配置），编译器将其推断为 `unknown`。

**解决方案**: 显式添加类型断言
```typescript
const node = nodes.get(id) as AppNode | undefined;
```

### 为什么不使用类型守卫？

类型守卫更安全，但会增加代码量：
```typescript
const node = nodes.get(id);
if (node && isAppNode(node)) {
  // 使用 node
}
```

类型断言更简洁，适合我们的场景：
```typescript
const node = nodes.get(id) as AppNode | undefined;
if (node) {
  // 使用 node
}
```

---

## 📈 预计完成时间

- **优先级 1**（批量类型断言）: 20-30 分钟
- **优先级 2**（类型定义）: 5 分钟
- **优先级 3**（spread 错误）: 10-15 分钟
- **优先级 4**（测试）: 15-20 分钟

**总计**: 约 50-70 分钟

---

## 🎉 里程碑

- ✅ 从 103 个错误降到 83 个错误（减少 20 个）
- ✅ types.ts 已无错误
- ✅ 所有缺失的变量已定义
- ✅ useMemo 依赖数组已修复
- 🔄 正在修复 Map.get() 类型错误

---

**更新时间**: 2026-01-27  
**更新人**: AI Assistant  
**状态**: 🔄 进行中
