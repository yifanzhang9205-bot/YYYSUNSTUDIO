# Group 拖动修复 - 第 9 步：跳跃问题修复（2026-01-28）

## 问题描述

用户报告：拖动组时有延迟，松开鼠标后会跳跃

**具体症状**：
- 拖动距离 10
- 按下鼠标时只移动到 4（拖不动）
- 松开鼠标后突然跳到 10

## 问题分析

### 真正的根本原因

在 `App.tsx` 中，组的 style 有这行代码：

```typescript
transition: 'all 0.3s cubic-bezier(0.32, 0.72, 0, 1)'
```

**这就是跳跃的根本原因！**

### 详细流程

```
1. 拖动中（updateGroupDrag）：
   - 使用 transform = translate(10px, 10px)
   - 视觉上：元素在位置 10

2. 松开时（endGroupDrag）：
   a. 更新 React state (x = 10)          ← state 更新
   b. 清除 transform = ''                ← 视觉上回到 0
   c. React 重新渲染，更新 left/top      ← 从 0 到 10 有 0.3s 的 transition 动画
   d. 用户看到：从 0 滑动到 10（跳跃）
```

**问题**：组有 `transition: all 0.3s`，清除 `transform` 后，React 更新 `left/top` 会触发 0.3 秒的动画，用户看到的就是"跳跃"。

### 为什么之前的修复没用？

之前在 `useGroup.ts` 中使用 `groupElement.style.transition = 'none'` 来禁用 transition，但是：
1. React 重新渲染时会把 inline style 覆盖掉
2. 所以 `transition: 'none'` 被 `transition: 'all 0.3s'` 覆盖了

## 解决方案

### 方案 1：拖动时禁用 transition（推荐）

在 `App.tsx` 中，根据 `isDraggingGroup` 状态动态设置 transition：

```typescript
style={{ 
  left: g.x, 
  top: g.y, 
  width: g.width, 
  height: g.height,
  // 🔥 修复跳跃：拖动时禁用 transition
  transition: isDraggingGroup ? 'none' : 'all 0.3s cubic-bezier(0.32, 0.72, 0, 1)'
}}
```

**优点**：
- 简单直接
- 不会被 React 覆盖
- 拖动时无 transition，松开后恢复 transition

### 方案 2：完全移除 transition（备选）

如果不需要组的动画效果，可以完全移除 transition：

```typescript
style={{ 
  left: g.x, 
  top: g.y, 
  width: g.width, 
  height: g.height,
  // transition: 'all 0.3s cubic-bezier(0.32, 0.72, 0, 1)' // ← 删除这行
}}
```

**优点**：
- 更简单
- 无任何动画，性能更好

**缺点**：
- 失去了组的动画效果（如果需要的话）

## 修改位置

### 修改 1：App.tsx（第 1963-1969 行）

```typescript
// ❌ 旧代码：始终有 transition
style={{ 
  left: g.x, 
  top: g.y, 
  width: g.width, 
  height: g.height,
  transition: 'all 0.3s cubic-bezier(0.32, 0.72, 0, 1)'
}}

// ✅ 新代码：拖动时禁用 transition
style={{ 
  left: g.x, 
  top: g.y, 
  width: g.width, 
  height: g.height,
  transition: isDraggingGroup ? 'none' : 'all 0.3s cubic-bezier(0.32, 0.72, 0, 1)'
}}
```

### 修改 2：hooks/useGroup.ts（可选，已在之前修复）

使用双重 `requestAnimationFrame` 确保 React 完成渲染后再清除 `transform`（这个修复仍然有用，可以避免其他潜在问题）。

## 修复效果

### 修复前
```
拖动 → 松开 → 清除 transform → React 更新 left/top（有 0.3s 动画）→ 视觉跳跃
```

### 修复后
```
拖动 → 松开 → 清除 transform → React 更新 left/top（无动画）→ 平滑停止
```

## 技术细节

### 为什么 transition 会导致跳跃？

CSS `transition` 会对所有属性变化添加动画效果。当清除 `transform` 后，元素从 `translate(10px, 10px)` 回到 `translate(0, 0)`，然后 React 更新 `left/top` 从 0 到 10，这个过程有 0.3 秒的动画，用户看到的就是"跳跃"。

### 为什么不能在 JS 中禁用 transition？

因为 React 重新渲染时会把 inline style 覆盖掉，所以在 JS 中设置 `transition: 'none'` 会被 React 的 `transition: 'all 0.3s'` 覆盖。

### 为什么要用 isDraggingGroup 状态？

`isDraggingGroup` 是 `useGroup` Hook 返回的状态，表示当前是否正在拖动组。通过这个状态，可以在拖动时禁用 transition，松开后恢复 transition。

## 相关文件

- `App.tsx`（第 1963-1969 行）
- `hooks/useGroup.ts`（`isDraggingGroup` 状态）

## 测试方法

1. 打开浏览器
2. 创建一个组，包含多个节点
3. 拖动组
4. 观察松开鼠标时是否有跳跃

**预期结果**：松开鼠标时，组平滑停在目标位置，无跳跃，无动画。

## 总结

跳跃问题的根本原因是组的 `transition: all 0.3s` 导致清除 `transform` 后，React 更新 `left/top` 时有 0.3 秒的动画。

通过在拖动时禁用 transition（`isDraggingGroup ? 'none' : 'all 0.3s'`），彻底解决了跳跃问题。

**核心原则**：
- 拖动时用 `transform`（性能）+ 无 transition（避免跳跃）
- 松开时用 state（数据）+ 无 transition（避免跳跃）
- 非拖动时可以有 transition（动画效果）

---

**修复完成时间**：2026-01-28
**修复人**：AI Assistant
**状态**：✅ 已完成
