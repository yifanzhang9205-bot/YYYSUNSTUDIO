# 数据持久化架构升级 - 紧急修复报告

**问题**：`nodes.values is not a function`  
**原因**：persist 中间件反序列化后，`nodes` 不是 Map 对象  
**时间**：2026-02-10

---

## 🔴 问题分析

### 错误信息
```
App.tsx:2115 Uncaught TypeError: nodes.values is not a function
```

### 根本原因

persist 中间件的序列化/反序列化逻辑有问题：

**当前代码**：
```typescript
serialize: (state) => {
  return JSON.stringify({
    nodes: Array.from(state.state.nodes.entries()),
  });
},
deserialize: (str) => {
  const data = JSON.parse(str);
  return {
    state: {
      nodes: new Map(data.nodes || []),
    },
    version: 0,
  };
},
```

**问题**：
1. `state.state.nodes` 路径不正确
2. 反序列化后的结构不匹配 Zustand 的期望格式
3. Map 对象没有正确恢复

---

## 🔧 解决方案

### 方案 1：暂时移除 persist 中间件（推荐）

**原因**：
- persist 中间件与 Map 数据结构不兼容
- 需要更复杂的序列化逻辑
- 当前阶段1的目标是图片持久化，节点元数据可以暂时不持久化

**实施**：
```typescript
export const useNodeStore = create<NodeStore>()(
  immer((set, get) => ({
    // ... 现有代码
  }))
);
```

**优点**：
- 立即解决崩溃问题
- 不影响图片持久化功能（已通过 IndexedDB 实现）
- 简化代码，减少复杂度

**缺点**：
- 节点元数据（位置、类型）不持久化
- 刷新页面后节点结构丢失（但图片已保存到 IndexedDB）

---

### 方案 2：修复 persist 序列化逻辑

**实施**：
```typescript
export const useNodeStore = create<NodeStore>()(
  persist(
    immer((set, get) => ({
      // ... 现有代码
    })),
    {
      name: 'canvas-nodes-storage',
      storage: createJSONStorage(() => localStorage),
      partialize: (state) => ({
        nodes: Array.from(state.nodes.entries()),
      }),
      onRehydrateStorage: () => {
        return (state, error) => {
          if (error) {
            console.error('[NodeStore] 恢复失败:', error);
            return;
          }
          if (state && Array.isArray(state.nodes)) {
            state.nodes = new Map(state.nodes);
          }
        };
      },
    }
  )
);
```

**优点**：
- 节点元数据持久化
- 刷新页面后完整恢复

**缺点**：
- 更复杂
- 需要测试验证
- 可能有其他兼容性问题

---

## 🚀 推荐实施步骤

### 步骤 1：立即移除 persist 中间件

```bash
# 修改 core/stores/nodeStore.ts
# 移除 persist 包裹
# 移除序列化/反序列化逻辑
```

### 步骤 2：清除 localStorage 中的旧数据

```javascript
// 在浏览器控制台执行
localStorage.removeItem('canvas-nodes-storage');
```

### 步骤 3：测试功能

- 创建节点 → 刷新页面 → 验证图片恢复（应该成功）
- 节点位置会丢失（预期行为，因为没有 persist）

### 步骤 4：后续优化（可选）

如果需要节点元数据持久化，可以：
1. 使用 App.tsx 中现有的 `saveToStorage('nodes', ...)` 逻辑
2. 在页面加载时手动恢复节点
3. 不依赖 persist 中间件

---

## 📝 修改清单

### 文件：`core/stores/nodeStore.ts`

**移除**：
```typescript
import { persist, createJSONStorage } from 'zustand/middleware';
```

**修改**：
```typescript
// 从
export const useNodeStore = create<NodeStore>()(
  persist(
    immer((set, get) => ({
      // ...
    })),
    {
      name: 'canvas-nodes-storage',
      storage: createJSONStorage(() => localStorage),
      serialize: (state) => { ... },
      deserialize: (str) => { ... },
    }
  )
);

// 改为
export const useNodeStore = create<NodeStore>()(
  immer((set, get) => ({
    // ...
  }))
);
```

---

## ✅ 验收标准

- [ ] 页面不再崩溃
- [ ] 可以创建节点
- [ ] 图片生成后保存到 IndexedDB
- [ ] 刷新页面后图片恢复（通过 App.tsx 的 useEffect）
- [ ] 删除节点时清理 IndexedDB

---

## 🎯 总结

**当前状态**：persist 中间件导致崩溃  
**解决方案**：暂时移除 persist 中间件  
**影响**：节点元数据不持久化，但图片持久化功能正常  
**下一步**：修复 persist 逻辑或使用手动持久化

---

**文档版本**：v1.0  
**创建日期**：2026-02-10  
**状态**：🔴 待修复
