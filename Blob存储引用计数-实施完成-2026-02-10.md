# Blob 存储引用计数 - 实施完成

**日期**: 2026-02-10  
**状态**: ✅ 阶段 2 完成（Stores + Hooks 集成）

---

## 📋 需求回顾

**用户需求**：
> 删除节点后，历史记录里的图片还在

**问题分析**：
- 当前实现：同一张图片存储了 3 次（历史记录、节点、资产库）
- 删除节点时：直接删除 IndexedDB，导致其他地方的图片失效
- 浪费磁盘空间：同一张图片占用 3 倍空间

---

## 🎯 解决方案：引用计数

### 核心思想

```
同一张图片：
├── IndexedDB 存储（1 份 Blob）
│   └── blob-abc123（唯一的 Blob）
│
├── 引用计数表（localStorage）
│   └── blob-abc123:
│       ├── asset-history-xyz: true  // 历史记录引用
│       ├── blob-node-n1-image: true // 节点引用
│       └── asset-thumbnail-xyz: true // 资产库引用
│
└── 内存中的 Blob URL（3 个 URL，指向同一个 Blob）
    ├── Blob URL A（历史记录）
    ├── Blob URL B（画布节点）
    └── Blob URL C（资产库缩略图）
```

### 优势

1. ✅ **节省磁盘空间**：同一张图片只存储 1 次（节省 66% 空间）
2. ✅ **逻辑清晰**：一个 Blob，多个引用
3. ✅ **安全删除**：只有当引用计数为 0 时，才删除 IndexedDB
4. ✅ **易于维护**：不会出现数据不一致

---

## 🚀 实施内容

### 阶段 1：引用计数服务（✅ 已完成）

#### 1. 修改 `services/referenceChecker.ts`

**新增功能**：
- `addReference(blobId, referenceId)` - 添加引用
- `removeReference(blobId, referenceId)` - 移除引用（返回是否应该删除 Blob）
- `getReferenceCount(blobId)` - 获取引用计数
- `getReferences(blobId)` - 获取所有引用 ID
- `hasReferences(blobId)` - 检查是否有引用
- `clearAllReferences()` - 清空所有引用计数（调试用）
- `printReferenceTable()` - 打印引用计数表（调试用）

**持久化**：
- 使用 `localStorage` 存储引用计数表
- 键名：`blob-reference-count`
- 格式：`{ "blob-abc123": { "asset-history-xyz": true, ... } }`

---

#### 2. 修改 `services/blobStorage.ts`

**新增功能**：

##### 核心函数

1. **`saveBlobWithReference(blobId, referenceId, blob)`**
   - 保存 Blob 到 IndexedDB（如果不存在）
   - 添加引用计数
   - 返回 Blob URL

2. **`deleteBlobReference(blobId, referenceId)`**
   - 移除引用计数
   - 如果引用计数为 0，删除 IndexedDB
   - 返回是否删除了 Blob

3. **`generateBlobId(imageData)`**
   - 从 URL 或 base64 生成唯一的 Blob ID
   - 使用 SHA-256 哈希（取前 16 位）
   - 降级方案：时间戳 + 随机数

##### 便捷函数（集成引用计数）

1. **`saveAssetHistoryImageWithRef(assetId, imageUrl)`**
   - 保存历史记录图片
   - 自动生成 Blob ID
   - 自动添加引用（`asset-history-{assetId}`）

2. **`deleteAssetHistoryImageWithRef(assetId, blobId?)`**
   - 删除历史记录图片引用
   - 如果引用计数为 0，删除 IndexedDB

3. **`saveNodeImageWithRef(nodeId, imageUrl)`**
   - 保存节点图片
   - 自动生成 Blob ID
   - 自动添加引用（`blob-node-{nodeId}-image`）

4. **`deleteNodeImageWithRef(nodeId, blobId?)`**
   - 删除节点图片引用
   - 如果引用计数为 0，删除 IndexedDB

5. **`saveAssetThumbnailWithRef(assetId, imageUrl)`**
   - 保存资产库缩略图
   - 自动生成 Blob ID
   - 自动添加引用（`asset-thumbnail-{assetId}`）

6. **`deleteAssetThumbnailWithRef(assetId, blobId?)`**
   - 删除资产库缩略图引用
   - 如果引用计数为 0，删除 IndexedDB

---

### 阶段 2：Stores + Hooks 集成（✅ 已完成）

#### 1. 修改 `assetHistoryStore.ts`

**新增字段**：
```typescript
export interface AssetHistoryItem {
  id: string;
  src: string;                   // Blob URL
  blobId?: string;               // 🔥 新增：Blob ID（用于引用计数）
  prompt?: string;
  timestamp: number;
  usedByAssetLibrary?: boolean;
  [key: string]: any;
}
```

---

#### 2. 修改 `useAssetHistory.ts`

**修改的函数**：

1. **`handleAssetGenerated`**
   - ✅ 使用 `saveAssetHistoryImageWithRef` 保存图片
   - ✅ 使用 `generateBlobId` 生成 Blob ID
   - ✅ 保存 `blobId` 到 Store

2. **`handleDeleteAsset`**
   - ✅ 使用 `deleteAssetHistoryImageWithRef` 删除图片
   - ✅ 传递 `blobId` 参数
   - ✅ 引用计数自动处理

3. **`handleDeleteMultipleAssets`**
   - ✅ 批量使用 `deleteAssetHistoryImageWithRef`
   - ✅ 引用计数自动处理

---

## 📊 数据流程

### 保存流程

```
用户生成图片
    ↓
handleAssetGenerated
    ↓
generateBlobId（SHA-256 哈希）
    ↓
saveAssetHistoryImageWithRef
    ↓
saveBlobWithReference
    ↓
检查 IndexedDB 是否已存在
    ↓
如果不存在 → 保存到 IndexedDB
    ↓
addReference（添加引用计数）
    ↓
创建 Blob URL
    ↓
保存到 Store（包含 blobId）
```

### 删除流程

```
用户删除历史记录
    ↓
handleDeleteAsset
    ↓
释放 Blob URL
    ↓
deleteAssetHistoryImageWithRef
    ↓
deleteBlobReference
    ↓
removeReference（移除引用计数）
    ↓
检查引用计数是否为 0
    ↓
如果为 0 → 删除 IndexedDB
    ↓
如果 > 0 → 保留 IndexedDB
    ↓
更新 Store
```

---

## 🔄 下一步计划

### 阶段 3：节点删除集成（待实施）

1. **修改 `nodeStore.ts`**
   - 添加 `blobId` 字段到 `AppNode.data`
   - 删除节点时调用 `deleteNodeImageWithRef`

2. **修改 `useNodeActions.ts`**
   - 创建节点时使用 `saveNodeImageWithRef`
   - 保存 `blobId` 到节点数据

---

## 🧪 测试计划

### 测试场景 1：删除节点，历史记录图片还在

1. 生成一张图片（添加到历史记录）
2. 拖拽到画布（创建节点）
3. 删除节点
4. 检查历史记录的图片是否还在

**预期结果**：
- ✅ 历史记录的图片还在
- ✅ IndexedDB 的 Blob 还在（引用计数 = 1）

### 测试场景 2：删除历史记录，节点图片还在

1. 生成一张图片（添加到历史记录）
2. 拖拽到画布（创建节点）
3. 删除历史记录
4. 检查节点的图片是否还在

**预期结果**：
- ✅ 节点的图片还在
- ✅ IndexedDB 的 Blob 还在（引用计数 = 1）

### 测试场景 3：删除所有引用，Blob 被删除

1. 生成一张图片（添加到历史记录）
2. 拖拽到画布（创建节点）
3. 删除节点
4. 删除历史记录
5. 检查 IndexedDB 的 Blob 是否被删除

**预期结果**：
- ✅ IndexedDB 的 Blob 被删除（引用计数 = 0）

---

## 📝 注意事项

### 兼容性

- ✅ 保留了旧的 API（`saveAssetHistoryImage`, `deleteAssetHistoryImage` 等）
- ✅ 新的 API 以 `WithRef` 结尾（`saveAssetHistoryImageWithRef` 等）
- ✅ 降级方案：如果没有提供 `blobId`，尝试从旧的存储键名加载

### 性能

- ✅ 使用 SHA-256 哈希生成 Blob ID（快速且唯一）
- ✅ 引用计数表存储在 localStorage（快速访问）
- ✅ 只有当引用计数为 0 时，才删除 IndexedDB（避免频繁操作）

### 调试

- ✅ 提供 `printReferenceTable()` 打印引用计数表
- ✅ 提供 `clearAllReferences()` 清空所有引用计数
- ✅ 所有操作都有详细的 console.log

---

## 🎉 总结

**阶段 2 完成**：
- ✅ `assetHistoryStore.ts` 添加 `blobId` 字段
- ✅ `useAssetHistory.ts` 集成引用计数
- ✅ 保存时自动添加引用
- ✅ 删除时自动减少引用
- ✅ 引用计数为 0 时自动删除 IndexedDB

**下一步**：
- ⏳ 阶段 3：节点删除集成（`nodeStore.ts` + `useNodeActions.ts`）
- ⏳ 阶段 4：测试和验证

**预期效果**：
- ✅ 删除节点后，历史记录里的图片还在
- ✅ 删除历史记录后，节点的图片还在
- ✅ 只有当所有地方都不用时，才删除 IndexedDB
- ✅ 节省 66% 的磁盘空间

---

## 📋 需求回顾

**用户需求**：
> 删除节点后，历史记录里的图片还在

**问题分析**：
- 当前实现：同一张图片存储了 3 次（历史记录、节点、资产库）
- 删除节点时：直接删除 IndexedDB，导致其他地方的图片失效
- 浪费磁盘空间：同一张图片占用 3 倍空间

---

## 🎯 解决方案：引用计数

### 核心思想

```
同一张图片：
├── IndexedDB 存储（1 份 Blob）
│   └── blob-abc123（唯一的 Blob）
│
├── 引用计数表（localStorage）
│   └── blob-abc123:
│       ├── asset-history-xyz: true  // 历史记录引用
│       ├── blob-node-n1-image: true // 节点引用
│       └── asset-thumbnail-xyz: true // 资产库引用
│
└── 内存中的 Blob URL（3 个 URL，指向同一个 Blob）
    ├── Blob URL A（历史记录）
    ├── Blob URL B（画布节点）
    └── Blob URL C（资产库缩略图）
```

### 优势

1. ✅ **节省磁盘空间**：同一张图片只存储 1 次（节省 66% 空间）
2. ✅ **逻辑清晰**：一个 Blob，多个引用
3. ✅ **安全删除**：只有当引用计数为 0 时，才删除 IndexedDB
4. ✅ **易于维护**：不会出现数据不一致

---

## 🚀 实施内容

### 阶段 1：引用计数服务（✅ 已完成）

#### 1. 修改 `services/referenceChecker.ts`

**新增功能**：
- `addReference(blobId, referenceId)` - 添加引用
- `removeReference(blobId, referenceId)` - 移除引用（返回是否应该删除 Blob）
- `getReferenceCount(blobId)` - 获取引用计数
- `getReferences(blobId)` - 获取所有引用 ID
- `hasReferences(blobId)` - 检查是否有引用
- `clearAllReferences()` - 清空所有引用计数（调试用）
- `printReferenceTable()` - 打印引用计数表（调试用）

**持久化**：
- 使用 `localStorage` 存储引用计数表
- 键名：`blob-reference-count`
- 格式：`{ "blob-abc123": { "asset-history-xyz": true, ... } }`

**示例**：
```typescript
import { addReference, removeReference, getReferenceCount } from './referenceChecker';

// 添加引用
addReference('blob-abc123', 'asset-history-xyz');
addReference('blob-abc123', 'blob-node-n1-image');
addReference('blob-abc123', 'asset-thumbnail-xyz');

// 引用计数 = 3
console.log(getReferenceCount('blob-abc123')); // 3

// 移除引用
const shouldDelete = removeReference('blob-abc123', 'blob-node-n1-image');
console.log(shouldDelete); // false（还有 2 个引用）

// 引用计数 = 2
console.log(getReferenceCount('blob-abc123')); // 2
```

---

#### 2. 修改 `services/blobStorage.ts`

**新增功能**：

##### 核心函数

1. **`saveBlobWithReference(blobId, referenceId, blob)`**
   - 保存 Blob 到 IndexedDB（如果不存在）
   - 添加引用计数
   - 返回 Blob URL

2. **`deleteBlobReference(blobId, referenceId)`**
   - 移除引用计数
   - 如果引用计数为 0，删除 IndexedDB
   - 返回是否删除了 Blob

3. **`generateBlobId(imageData)`**
   - 从 URL 或 base64 生成唯一的 Blob ID
   - 使用 SHA-256 哈希（取前 16 位）
   - 降级方案：时间戳 + 随机数

##### 便捷函数（集成引用计数）

1. **`saveAssetHistoryImageWithRef(assetId, imageUrl)`**
   - 保存历史记录图片
   - 自动生成 Blob ID
   - 自动添加引用（`asset-history-{assetId}`）

2. **`deleteAssetHistoryImageWithRef(assetId, blobId?)`**
   - 删除历史记录图片引用
   - 如果引用计数为 0，删除 IndexedDB

3. **`saveNodeImageWithRef(nodeId, imageUrl)`**
   - 保存节点图片
   - 自动生成 Blob ID
   - 自动添加引用（`blob-node-{nodeId}-image`）

4. **`deleteNodeImageWithRef(nodeId, blobId?)`**
   - 删除节点图片引用
   - 如果引用计数为 0，删除 IndexedDB

5. **`saveAssetThumbnailWithRef(assetId, imageUrl)`**
   - 保存资产库缩略图
   - 自动生成 Blob ID
   - 自动添加引用（`asset-thumbnail-{assetId}`）

6. **`deleteAssetThumbnailWithRef(assetId, blobId?)`**
   - 删除资产库缩略图引用
   - 如果引用计数为 0，删除 IndexedDB

**示例**：
```typescript
import { 
  saveAssetHistoryImageWithRef, 
  deleteAssetHistoryImageWithRef 
} from './blobStorage';

// 保存历史记录图片
const blobUrl = await saveAssetHistoryImageWithRef('asset-123', imageData);
// 结果：
// - IndexedDB: blob-abc123 -> Blob 数据
// - 引用计数: blob-abc123 -> { "asset-history-asset-123": true }
// - 返回: Blob URL

// 删除历史记录图片
await deleteAssetHistoryImageWithRef('asset-123', 'blob-abc123');
// 结果：
// - 引用计数: blob-abc123 -> {}（引用计数为 0）
// - IndexedDB: blob-abc123 被删除
```

---

## 📊 数据流程

### 保存流程

```
用户上传图片
    ↓
生成 Blob ID（SHA-256 哈希）
    ↓
检查 IndexedDB 是否已存在
    ↓
如果不存在 → 保存到 IndexedDB
    ↓
添加引用计数
    ↓
创建 Blob URL
    ↓
返回 Blob URL
```

### 删除流程

```
用户删除节点/历史记录
    ↓
移除引用计数
    ↓
检查引用计数是否为 0
    ↓
如果为 0 → 删除 IndexedDB
    ↓
如果 > 0 → 保留 IndexedDB
```

---

## 🔄 下一步计划

### 阶段 2：集成到 Stores（待实施）

1. **修改 `assetHistoryStore.ts`**
   - 保存时：使用 `saveAssetHistoryImageWithRef`
   - 删除时：使用 `deleteAssetHistoryImageWithRef`
   - 添加 `blobId` 字段到 `AssetHistoryItem`

2. **修改 `assetLibraryStore.ts`**
   - 保存时：使用 `saveAssetThumbnailWithRef`
   - 删除时：使用 `deleteAssetThumbnailWithRef`
   - 添加 `blobId` 字段到 `Asset`

3. **修改 `nodeStore.ts`**
   - 删除节点时：使用 `deleteNodeImageWithRef`
   - 添加 `blobId` 字段到 `AppNode.data`

### 阶段 3：集成到 Hooks（待实施）

1. **修改 `useAssetHistory.ts`**
   - 添加历史记录时：调用 `saveAssetHistoryImageWithRef`
   - 删除历史记录时：调用 `deleteAssetHistoryImageWithRef`

2. **修改 `useNodeActions.ts`**
   - 创建节点时：调用 `saveNodeImageWithRef`
   - 删除节点时：调用 `deleteNodeImageWithRef`

3. **修改 `useAssetLibrary.ts`**
   - 添加资产时：调用 `saveAssetThumbnailWithRef`
   - 删除资产时：调用 `deleteAssetThumbnailWithRef`

---

## 🧪 测试计划

### 测试场景 1：删除节点，历史记录图片还在

1. 生成一张图片（添加到历史记录）
2. 拖拽到画布（创建节点）
3. 删除节点
4. 检查历史记录的图片是否还在

**预期结果**：
- ✅ 历史记录的图片还在
- ✅ IndexedDB 的 Blob 还在（引用计数 = 1）

### 测试场景 2：删除历史记录，节点图片还在

1. 生成一张图片（添加到历史记录）
2. 拖拽到画布（创建节点）
3. 删除历史记录
4. 检查节点的图片是否还在

**预期结果**：
- ✅ 节点的图片还在
- ✅ IndexedDB 的 Blob 还在（引用计数 = 1）

### 测试场景 3：删除所有引用，Blob 被删除

1. 生成一张图片（添加到历史记录）
2. 拖拽到画布（创建节点）
3. 删除节点
4. 删除历史记录
5. 检查 IndexedDB 的 Blob 是否被删除

**预期结果**：
- ✅ IndexedDB 的 Blob 被删除（引用计数 = 0）

---

## 📝 注意事项

### 兼容性

- ✅ 保留了旧的 API（`saveAssetHistoryImage`, `deleteAssetHistoryImage` 等）
- ✅ 新的 API 以 `WithRef` 结尾（`saveAssetHistoryImageWithRef` 等）
- ✅ 降级方案：如果没有提供 `blobId`，尝试从旧的存储键名加载

### 性能

- ✅ 使用 SHA-256 哈希生成 Blob ID（快速且唯一）
- ✅ 引用计数表存储在 localStorage（快速访问）
- ✅ 只有当引用计数为 0 时，才删除 IndexedDB（避免频繁操作）

### 调试

- ✅ 提供 `printReferenceTable()` 打印引用计数表
- ✅ 提供 `clearAllReferences()` 清空所有引用计数
- ✅ 所有操作都有详细的 console.log

---

## 🎉 总结

**阶段 1 完成**：
- ✅ 引用计数服务（`referenceChecker.ts`）
- ✅ blobStorage 集成（`blobStorage.ts`）
- ✅ 核心 API 完成
- ✅ 便捷函数完成

**下一步**：
- ⏳ 阶段 2：集成到 Stores
- ⏳ 阶段 3：集成到 Hooks
- ⏳ 阶段 4：测试和验证

**预期效果**：
- ✅ 删除节点后，历史记录里的图片还在
- ✅ 删除历史记录后，节点的图片还在
- ✅ 只有当所有地方都不用时，才删除 IndexedDB
- ✅ 节省 66% 的磁盘空间
