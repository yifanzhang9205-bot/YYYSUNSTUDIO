# 组标题优化 - 动态大小 + 双击改名 - 2026-02-08

## 📋 需求

用户反馈组标题（"新建分组"）存在以下问题：
1. **看不清**：白色40%透明度（`text-white/40`）太淡
2. **太小**：`text-xs`（12px）太小
3. **随缩放缩小**：画布缩小时，标题也跟着缩小到看不见
4. **无法改名**：没有双击改名功能

## 🎯 解决方案

按照架构规则，分三层实施：

### 1. UI State → `core/stores/uiStore.ts`
添加编辑状态管理：
```typescript
// 数据
editingGroupId: string | null;  // 正在编辑的组 ID

// 操作
setEditingGroupId: (id: string | null) => void;
```

### 2. 改名逻辑 → `hooks/useGroup.ts`
添加改名方法：
```typescript
const renameGroup = useCallback((groupId: string, newTitle: string) => {
  if (!newTitle.trim()) return; // 空标题不允许
  onSaveHistory();
  onUpdateGroup(groupId, { title: newTitle.trim() });
}, [onSaveHistory, onUpdateGroup]);
```

### 3. 渲染逻辑 → `App.tsx`
修改组标题渲染：

#### 3.1 动态字体大小
```typescript
// 🔥 标题字体大小：根据缩放比例动态调整（像圆点一样）
const titleFontSize = Math.max(14, 16 / scale); // 基础16px，最小14px
```

#### 3.2 深灰色 + 更大字体
```tsx
// 从 text-xs text-white/40 改为：
className="text-gray-700"  // 深灰色，清晰可见
style={{ fontSize: `${titleFontSize}px` }}  // 动态大小
```

#### 3.3 双击改名
```tsx
{isEditingTitle ? (
  <input
    type="text"
    defaultValue={g.title}
    autoFocus
    onBlur={(e) => {
      const newTitle = e.target.value.trim();
      if (newTitle && newTitle !== g.title) {
        renameGroup(g.id, newTitle);
      }
      setEditingGroupId(null);
    }}
    onKeyDown={(e) => {
      if (e.key === 'Enter') e.currentTarget.blur();
      if (e.key === 'Escape') setEditingGroupId(null);
    }}
  />
) : (
  <div 
    onDoubleClick={(e) => {
      e.stopPropagation();
      setEditingGroupId(g.id);
    }}
  >
    {g.title}
  </div>
)}
```

## ✅ 实施完成

### 修改的文件

1. **core/stores/uiStore.ts**
   - 添加 `editingGroupId: string | null`
   - 添加 `setEditingGroupId(id: string | null)`

2. **hooks/useGroup.ts**
   - 添加 `renameGroup(groupId, newTitle)` 方法
   - 返回值中添加 `renameGroup`

3. **App.tsx**
   - 从 useUIStore 获取 `editingGroupId` 和 `setEditingGroupId`
   - 从 useGroup 获取 `renameGroup`
   - 添加 `titleFontSize` 计算（动态缩放）
   - 添加 `isEditingTitle` 判断
   - 修改标题渲染逻辑（条件渲染：输入框 vs 文本）

## 🎨 效果对比

### 修改前
- **颜色**：`text-white/40`（白色40%透明度，几乎看不见）
- **大小**：`text-xs`（12px，固定大小）
- **缩放**：随画布缩放而缩小（12% 缩放时几乎看不见）
- **改名**：无法改名

### 修改后
- **颜色**：`text-gray-700`（深灰色，清晰可见）
- **大小**：`16px`（基础大小，比之前大33%）
- **缩放**：动态调整大小（`Math.max(14, 16 / scale)`）
  - 100% 缩放：16px
  - 50% 缩放：32px（视觉大小保持一致）
  - 12% 缩放：133px（视觉大小保持一致）
  - 最小值：14px（防止太小）
- **改名**：双击标题进入编辑模式
  - Enter 确认
  - Escape 取消
  - 失焦自动保存

## 🔧 交互细节

### 双击改名流程
1. **触发**：双击组标题
2. **进入编辑**：标题变为输入框，自动聚焦
3. **编辑**：输入新名字
4. **确认**：
   - 按 Enter → 保存并退出
   - 点击外部 → 保存并退出
   - 按 Escape → 取消并退出
5. **验证**：
   - 空标题不保存
   - 自动 trim 前后空格
   - 与原标题相同不保存

### 防止冲突
- 双击组边框 → 不触发编辑（只选中组）
- 双击标题 → 触发编辑
- 编辑时点击输入框 → 不触发组拖动
- 编辑时鼠标按下 → 不触发组拖动

## 📊 性能影响

- **计算开销**：每个组多一个 `titleFontSize` 计算（O(1)）
- **渲染开销**：条件渲染（输入框 vs 文本），无额外开销
- **内存占用**：新增一个 `editingGroupId` state（8 bytes）
- **总体影响**：可忽略不计

## 🎯 验收标准

- [x] 标题清晰可见（深灰色）
- [x] 标题大小合适（16px，比之前大33%）
- [x] 标题不随缩放缩小（动态调整）
- [x] 双击标题可以改名
- [x] Enter 确认改名
- [x] Escape 取消改名
- [x] 失焦自动保存
- [x] 空标题不保存
- [x] 与原标题相同不保存
- [x] 编辑时不触发组拖动

## 🚀 未来扩展

1. **快捷键改名**：F2 快捷键触发改名
2. **右键菜单改名**：右键菜单添加"重命名"选项
3. **批量改名**：选中多个组，批量改名
4. **改名历史**：记录改名历史，支持撤销
5. **改名建议**：根据组内节点类型，智能建议名字

## 📝 注意事项

1. **架构规则**：严格遵守三层架构
   - UI State → uiStore
   - 改名逻辑 → useGroup Hook
   - 渲染逻辑 → App.tsx（只做组合和调用）

2. **不破坏现有功能**：
   - 组拖动正常
   - 组选中正常
   - 组右键菜单正常
   - 圆点调整大小正常

3. **用户体验**：
   - 双击标题才编辑（不是双击组边框）
   - 编辑时不触发其他交互
   - 自动聚焦，方便输入
   - Enter/Escape 快捷键

## 🎉 总结

成功实现组标题优化，解决了用户反馈的所有问题：
1. ✅ 标题清晰可见（深灰色）
2. ✅ 标题大小合适（16px）
3. ✅ 标题不随缩放缩小（动态调整）
4. ✅ 支持双击改名（完整交互）

同时严格遵守架构规则，代码清晰、可维护、可扩展。
