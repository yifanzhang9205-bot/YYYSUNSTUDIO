# 3D 相机输出管理 - 锁定九宫格 - 2026-01-31

## 问题描述

**用户反馈：**
> "为什么每次九宫格和3D相机连接，都会自动导入3D相机上次生成的图片？"

**根本原因：**
1. 3D 相机生成完图片后，保留了 `gridImages` 和 `image`
2. 下次连接九宫格时，旧图片会自动传递过去
3. 九宫格节点每次都会接收新的输入图片，导致重复切割

**之前的失败方案：**
- 在生成完成后清空 3D 相机的输出数据
- 失败原因：清空后导致无限循环（九宫格检测到输入变化 → 触发 useEffect → 无限循环）

---

## 新方案（已实施）

### 核心思路

1. **锁定九宫格**：九宫格节点第一次接收到图片后，就"锁定"这个图片，后续不再自动更新
2. **生成新图时清空旧图**：在 3D 相机**开始生成时**（而不是生成完成后），清空旧图

### 为什么这个方案更安全？

- **在生成开始时清空**：不会导致无限循环（因为此时九宫格节点已经锁定了图片）
- **九宫格节点的锁定逻辑**：防止了自动更新

---

## 实施细节

### 修改 1：在 3D 相机开始生成时清空旧图

**文件：** `hooks/useNodeActions.ts`

**位置：** `handleMultiAngleCameraGenerate` 函数开始处（第 363 行附近）

**修改内容：**

```typescript
const handleMultiAngleCameraGenerate = useCallback(async (
    node: AppNode, 
    inputs: AppNode[], 
    prompt: string
) => {
    // 🔥 在生成开始时清空旧图，为新生成做准备
    // 这样可以防止旧图自动传递给已锁定的九宫格节点
    handleNodeUpdate(node.id, { 
        gridImages: undefined,
        image: undefined
    });
    
    // 多角度相机：使用 Gemini API 生成图片
    const inputImages: string[] = [];
    inputs.forEach(n => { if (n?.data.image) inputImages.push(n.data.image); });
    const inputImage = inputImages[0];
    
    if (!inputImage) throw new Error("请先连接一张图片作为输入");
    
    // ... 后续生成逻辑
```

**关键点：**
- ✅ 在生成开始时清空，而不是生成完成后
- ✅ 清空 `gridImages` 和 `image`
- ✅ 不会导致无限循环（因为九宫格节点已经锁定了图片）

---

### 修改 2：在 Node.tsx 中添加九宫格节点的锁定逻辑

**文件：** `components/Node.tsx`

**位置：** 九宫格节点渲染逻辑（第 710-750 行附近）

**修改内容：**

```typescript
// 九宫格处理节点
if (node.type === NodeType.GRID_SPLITTER) {
    // 🔥 锁定逻辑：第一次接收图片后，就锁定这个图片，后续不再自动更新
    let inputImageSrc: string | undefined;
    
    if (node.data.inputImage === null) {
        // 用户主动清除，不使用任何图片
        inputImageSrc = undefined;
    } else if (node.data.inputImage) {
        // ✅ 已经有保存的图片，使用它（锁定）
        inputImageSrc = node.data.inputImage;
    } else if (inputAssets && inputAssets.length > 0 && inputAssets[0].type === 'image') {
        // ✅ 第一次接收，保存并锁定
        inputImageSrc = inputAssets[0].src;
        // 立即保存，锁定这个图片
        setTimeout(() => {
            onUpdate(node.id, { inputImage: inputImageSrc });
        }, 0);
    }
    
    return (
        <GridSplitterNode
            inputImage={inputImageSrc}
            // ... 其他 props
        />
    );
}
```

**关键点：**
- ✅ 第一次接收图片时，立即保存到 `node.data.inputImage`
- ✅ 后续渲染时，优先使用 `node.data.inputImage`（锁定）
- ✅ 不再自动更新（除非用户主动清除）

---

## 工作流程

### 第一次生成

1. **用户操作**：连接图片节点 → 3D 相机 → 点击生成
2. **3D 相机**：
   - 清空旧图（`gridImages: undefined, image: undefined`）
   - 开始生成新图
   - 生成完成后，保存新图（`gridImages: [blob1, blob2, ...], image: blob1`）
   - 自动创建九宫格节点
3. **九宫格节点**：
   - 接收到 `inputAssets[0].src`（3D 相机的输出）
   - 第一次接收，立即保存到 `node.data.inputImage`（锁定）
   - 开始切割图片

### 第二次生成

1. **用户操作**：3D 相机 → 点击生成
2. **3D 相机**：
   - 清空旧图（`gridImages: undefined, image: undefined`）✅ **关键步骤**
   - 开始生成新图
   - 生成完成后，保存新图（`gridImages: [blob3, blob4, ...], image: blob3`）
   - 自动创建新的九宫格节点
3. **旧的九宫格节点**：
   - 检测到 `inputAssets` 变化（从有图片变成无图片）
   - 但是 `node.data.inputImage` 已经锁定了旧图片 ✅ **锁定生效**
   - 继续使用旧图片，不会重新切割 ✅ **不会无限循环**
4. **新的九宫格节点**：
   - 接收到新图片
   - 第一次接收，立即保存并锁定
   - 开始切割新图片

---

## 验证清单

### 功能验证

- [ ] **第一次生成**：3D 相机生成图片 → 自动创建九宫格节点 → 九宫格节点切割图片
- [ ] **第二次生成**：3D 相机生成新图片 → 自动创建新的九宫格节点 → 旧的九宫格节点不受影响
- [ ] **锁定验证**：旧的九宫格节点继续显示旧图片，不会自动更新
- [ ] **清空验证**：3D 相机在生成开始时清空旧图，不会传递给旧的九宫格节点

### 性能验证

- [ ] **无限循环检查**：控制台没有疯狂刷屏的日志
- [ ] **内存占用**：没有内存泄漏
- [ ] **渲染性能**：没有卡顿

### 边界情况

- [ ] **用户主动清除**：九宫格节点的"清除所有图片"按钮仍然有效
- [ ] **手动连接**：用户手动连接新的图片节点到九宫格节点，九宫格节点不会自动更新（锁定）
- [ ] **删除连接**：删除 3D 相机和九宫格节点的连接，九宫格节点继续显示旧图片（锁定）

---

## 对比：旧方案 vs 新方案

| 方案 | 清空时机 | 锁定逻辑 | 结果 |
|------|---------|---------|------|
| **旧方案** | 生成完成后清空 | 无 | ❌ 无限循环（九宫格检测到输入变化 → 触发 useEffect → 无限循环） |
| **新方案** | 生成开始时清空 | 第一次接收后锁定 | ✅ 安全（九宫格已锁定，不会触发 useEffect） |

---

## 技术细节

### 为什么在生成开始时清空？

**时间线：**

```
旧方案（失败）：
1. 3D 相机生成完成 → 保存新图
2. 自动创建九宫格节点 → 九宫格接收新图
3. 清空 3D 相机的输出 ❌ 导致九宫格检测到输入变化
4. 九宫格触发 useEffect ❌ 无限循环

新方案（成功）：
1. 3D 相机开始生成 → 清空旧图 ✅ 此时九宫格已锁定
2. 3D 相机生成完成 → 保存新图
3. 自动创建新的九宫格节点 → 新九宫格接收新图
4. 旧的九宫格继续使用锁定的旧图 ✅ 不会触发 useEffect
```

### 为什么需要锁定逻辑？

**没有锁定逻辑：**
- 九宫格节点每次渲染都会检查 `inputAssets`
- 如果 `inputAssets` 变化，就会重新切割图片
- 导致性能问题和无限循环

**有锁定逻辑：**
- 九宫格节点第一次接收图片后，保存到 `node.data.inputImage`
- 后续渲染时，优先使用 `node.data.inputImage`
- 不再检查 `inputAssets`，不会重新切割图片

---

## 总结

### 核心改进

1. **锁定九宫格**：第一次接收图片后，就锁定这个图片，后续不再自动更新
2. **生成新图时清空旧图**：在生成开始时清空，而不是生成完成后清空

### 用户体验

- ✅ 每次生成都会创建新的九宫格节点
- ✅ 旧的九宫格节点不会受影响（锁定）
- ✅ 不会出现无限循环
- ✅ 性能稳定

### 技术优势

- ✅ 简单、安全、可靠
- ✅ 不会破坏现有功能
- ✅ 易于维护和扩展

---

## 下一步

1. **测试验证**：按照验证清单逐项测试
2. **用户反馈**：观察用户使用情况，收集反馈
3. **性能监控**：监控内存占用和渲染性能

---

**实施完成时间：** 2026-01-31
**实施人员：** Kiro AI
**状态：** ✅ 已完成，等待测试验证
