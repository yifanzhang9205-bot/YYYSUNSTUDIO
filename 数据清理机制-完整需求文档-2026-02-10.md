# 数据清理机制 - 完整需求文档

**创建日期**：2026-02-10  
**状态**：待实施  
**优先级**：P0（核心功能）  
**预计工时**：4-6 小时

---

## 📋 需求概述

### 核心目标

1. **智能清理**：历史记录删除时，自动检查引用关系
2. **数据保护**：被引用的数据需要二次确认才能删除
3. **刷新不丢失**：所有数据（画布、历史记录、资产库）刷新后完整恢复
4. **缩略图独立**：资产库缩略图永久保存，不受节点删除影响
5. **用户体验**：清晰的提示信息，让用户知道发生了什么

### 用户价值

- ✅ 不会误删正在使用的数据
- ✅ 清理逻辑清晰，用户可控
- ✅ 刷新后工作成果不丢失
- ✅ 资产库缩略图永久可见

---

## 🏗️ 数据存储架构

### 三层存储结构

```
第 1 层：内存层（Blob URL）
  - 用途：UI 显示
  - 生命周期：页面刷新后失效
  - 优势：零拷贝，性能最高

第 2 层：持久化层（IndexedDB）
  - 用途：页面刷新后恢复
  - 生命周期：手动删除前永久保存
  - 优势：不占内存，容量大

第 3 层：元数据层（localStorage）
  - 用途：保存节点结构（位置、类型、连接关系）
  - 生命周期：永久保存
  - 优势：快速加载，体积小
```

### 数据隔离设计

```
画布节点图片：
  - 键名：blob-node-{nodeId}-image
  - 键名：blob-{nodeId}-grid-{i}（九宫格）
  - 生命周期：节点删除时不清理（等待垃圾回收）

历史记录图片：
  - 键名：asset-{assetId}
  - 生命周期：删除历史记录时检查引用，无引用则清理

资产库缩略图：
  - 键名：asset-thumbnail-{assetId}
  - 生命周期：删除资产时清理（独立存储，不受其他影响）

资产库节点图片：
  - 键名：asset-{assetId}-node-{nodeId}-image
  - 键名：asset-{assetId}-node-{nodeId}-image-{i}（九宫格）
  - 生命周期：删除资产时清理
```

---

## 🎯 核心规则（必须严格遵守）

### 规则 1：历史记录删除（单个）

**用户操作**：点击历史记录的"删除"按钮

**系统行为**：

```
步骤 1：检查引用关系
  - 检查该图片是否被资产库引用
  - 检查该图片是否在画布上使用（对应的节点是否存在）

步骤 2：根据检查结果决定操作
  情况 A：无任何引用
    → 直接删除历史记录
    → 清理 IndexedDB（asset-{assetId}）
    → 提示："已删除"
  
  情况 B：被资产库引用
    → 弹窗确认："该图片已被资产库使用，确认删除？"
    → 用户点击"确认" → 删除历史记录 + 清理 IndexedDB
    → 用户点击"取消" → 不删除
  
  情况 C：在画布上使用
    → 弹窗确认："该图片正在画布上使用，确认删除？"
    → 用户点击"确认" → 删除历史记录 + 清理 IndexedDB
    → 用户点击"取消" → 不删除
  
  情况 D：既被资产库引用，又在画布上使用
    → 弹窗确认："该图片正在画布和资产库中使用，确认删除？"
    → 用户点击"确认" → 删除历史记录 + 清理 IndexedDB
    → 用户点击"取消" → 不删除
```

**关键点**：
- ✅ 无引用时，直接删除，不需要确认
- ✅ 有引用时，必须二次确认
- ✅ 确认后，彻底清理 IndexedDB

---

### 规则 2：历史记录批量下载

**用户操作**：点击"批量下载"按钮

**系统行为**：

```
步骤 1：下载所有图片
  - 遍历所有历史记录
  - 下载所有图片（包括被引用的）
  - 不检查引用关系，全部下载

步骤 2：显示下载进度
  - Toast 提示："正在下载 X 张图片..."
  - 显示进度条（可选）

步骤 3：下载完成
  - Toast 提示："已下载 X 张图片"
```

**关键点**：
- ✅ 批量下载时，所有图片都可以下载（包括被引用的）
- ✅ 不检查引用关系
- ✅ 下载不影响数据存储

---

### 规则 3：历史记录一键清除

**用户操作**：点击"清除历史记录"按钮

**系统行为**：

```
步骤 1：扫描所有历史记录
  - 遍历所有历史记录
  - 检查每条记录的引用关系

步骤 2：分类
  - 独占的：只在历史记录中，没有被引用
  - 被引用的：被资产库或画布使用

步骤 3：清理独占的记录
  - 直接删除所有独占的记录
  - 清理对应的 IndexedDB

步骤 4：显示结果
  - Toast 提示："已清理 X 条记录，跳过 Y 条（被引用）"
  
步骤 5：提供继续清理选项（如果有被引用的）
  - 显示一个输入框："如需继续清理已被引用的 Y 条内容，请输入 '确认' 后点击按钮"
  - 用户输入"确认"并点击按钮 → 清理所有被引用的记录 + 清理 IndexedDB
  - 用户不输入或取消 → 保留被引用的记录
```

**UI 设计**：

```tsx
// 第一步：显示清理结果
<Toast>
  已清理 15 条记录，跳过 3 条（被资产库或画布使用）
</Toast>

// 第二步：如果有被引用的，显示继续清理选项
<Dialog>
  <p>跳过的 3 条记录正在被使用：</p>
  <ul>
    <li>图片 1 - 被资产库引用</li>
    <li>图片 2 - 在画布上使用</li>
    <li>图片 3 - 被资产库和画布使用</li>
  </ul>
  
  <p>如需继续清理这些记录，请输入"确认"：</p>
  <input type="text" placeholder="输入'确认'以继续" />
  
  <button>继续清理</button>
  <button>取消</button>
</Dialog>
```

**关键点**：
- ✅ 独占的记录直接清理，不需要确认
- ✅ 被引用的记录跳过，显示数量
- ✅ 提供二次确认机制，用户输入"确认"才能清理被引用的
- ✅ 清晰列出被跳过的记录和原因

---

### 规则 4：资产库删除

**用户操作**：删除资产库中的资产

**系统行为**：

```
步骤 1：删除资产数据
  - 从 assetLibraryStore 中删除资产

步骤 2：清理 IndexedDB
  - 清理资产缩略图：asset-thumbnail-{assetId}
  - 清理资产节点图片：asset-{assetId}-node-{nodeId}-image
  - 清理资产节点九宫格：asset-{assetId}-node-{nodeId}-image-{i}

步骤 3：不影响其他数据
  - 不清理历史记录（asset-{assetId}）
  - 不清理画布节点（blob-node-{nodeId}-image）
```

**关键点**：
- ✅ 资产库缩略图独立存储，删除资产时清理
- ✅ 资产库节点图片独立存储，删除资产时清理
- ✅ 不影响历史记录和画布节点

---

### 规则 5：画布节点删除

**用户操作**：删除画布上的节点

**系统行为**：

```
步骤 1：删除节点数据
  - 从 nodeStore 中删除节点

步骤 2：清理 IndexedDB（历史记录清除时统一清理）
  - 删除节点时不清理 IndexedDB
  - 等待用户清除历史记录时统一清理

步骤 3：清理内存中的 Blob URL
  - 调用 URL.revokeObjectURL() 释放内存
```

**关键点**：
- ✅ 删除节点时不清理 IndexedDB
- ✅ 只清理内存中的 Blob URL
- ✅ IndexedDB 清理由历史记录清除功能统一处理

---

### 规则 6：资产库缩略图独立存储（重要！）

**核心原则**：资产库缩略图必须永久保存，不受节点删除影响

**实施方案**：

```
创建资产时：
  1. 生成缩略图（从第一个节点的图片）
  2. 保存到 IndexedDB：asset-thumbnail-{assetId}
  3. 保存节点数据到资产库（深拷贝）
  4. 保存节点图片到 IndexedDB：asset-{assetId}-node-{nodeId}-image

删除画布节点时：
  1. 不清理 asset-thumbnail-{assetId}（缩略图独立存储）
  2. 不清理 asset-{assetId}-node-{nodeId}-image（资产库节点图片独立存储）

删除资产时：
  1. 清理 asset-thumbnail-{assetId}
  2. 清理 asset-{assetId}-node-{nodeId}-image

刷新页面时：
  1. 从 IndexedDB 恢复 asset-thumbnail-{assetId}
  2. 从 IndexedDB 恢复 asset-{assetId}-node-{nodeId}-image
```

**为什么要独立存储？**

```
场景：
1. 用户创建了 2 个节点（节点 A 和节点 B）
2. 用户将这 2 个节点保存到资产库
3. 用户删除了画布上的节点 A 和节点 B
4. 用户刷新页面

期望结果：
- 资产库中的缩略图仍然可见
- 点击资产库，可以恢复节点 A 和节点 B

如果不独立存储：
- 删除画布节点时，会清理 blob-node-A-image
- 资产库缩略图失效（因为引用的是 blob-node-A-image）
- 用户体验极差

独立存储后：
- 资产库缩略图存储在 asset-thumbnail-{assetId}
- 资产库节点图片存储在 asset-{assetId}-node-A-image
- 删除画布节点不影响资产库
- 用户体验完美
```

---

## 🔒 数据保护机制

### 保护 1：刷新不丢失

**画布节点**：
```
保存时机：
  - 创建节点时自动保存到 IndexedDB
  - 更新节点数据时自动保存到 IndexedDB

恢复时机：
  - 页面加载时，从 IndexedDB 恢复 Blob URL
  - 延迟 500ms 执行，确保 Store 完全恢复

键名格式：
  - 单张图片：blob-node-{nodeId}-image
  - 九宫格：blob-{nodeId}-grid-{i}
```

**历史记录**：
```
保存时机：
  - 生成图片时自动保存到 IndexedDB

恢复时机：
  - 页面加载时，从 IndexedDB 恢复 Blob URL

键名格式：
  - asset-{assetId}
```

**资产库**：
```
保存时机：
  - 创建资产时自动保存到 IndexedDB

恢复时机：
  - 页面加载时，从 IndexedDB 恢复 Blob URL

键名格式：
  - 缩略图：asset-thumbnail-{assetId}
  - 节点图片：asset-{assetId}-node-{nodeId}-image
  - 节点九宫格：asset-{assetId}-node-{nodeId}-image-{i}
```

---

### 保护 2：引用检查

---

## 📊 数据流图

### 创建节点 → 历史记录 → 资产库

```
用户生成图片
  ↓
保存到历史记录
  - localStorage: assetHistory 数组
  - IndexedDB: asset-{assetId}
  ↓
用户拖拽到画布
  - 创建节点
  - 保存到 IndexedDB: blob-node-{nodeId}-image
  ↓
用户保存到资产库
  - 深拷贝节点数据
  - 生成缩略图
  - 保存到 IndexedDB: asset-thumbnail-{assetId}
  - 保存节点图片到 IndexedDB: asset-{assetId}-node-{nodeId}-image
```

### 删除流程

```
删除历史记录（单个）
  ↓
检查引用关系
  ↓
无引用 → 直接删除 + 清理 IndexedDB
有引用 → 弹窗确认 → 用户确认 → 删除 + 清理 IndexedDB

删除历史记录（批量下载）
  ↓
下载所有图片（包括被引用的）
  ↓
不检查引用关系，全部下载

删除历史记录（一键清除）
  ↓
扫描所有记录
  ↓
分类：独占 vs 被引用
  ↓
清理独占的 + 清理 IndexedDB → 显示结果 → 提供继续清理选项
  ↓
用户输入"确认" → 清理被引用的 + 清理 IndexedDB

删除资产库
  ↓
清理 asset-thumbnail-{assetId}
清理 asset-{assetId}-node-{nodeId}-image
不影响历史记录和画布节点

删除画布节点
  ↓
不清理 IndexedDB
只清理内存中的 Blob URL
等待历史记录清除时统一清理
```

---

## 🎯 实施计划

### 阶段 1：引用检查函数（1 小时）

**任务清单**：
- [ ] 1.1 创建 `services/referenceChecker.ts`
- [ ] 1.2 实现 `isReferencedByAssetLibrary()`
- [ ] 1.3 实现 `isUsedInCanvas()`
- [ ] 1.4 实现 `getReferenceInfo()`
- [ ] 1.5 添加单元测试

**修改文件**：
- `services/referenceChecker.ts`（新建）

---

### 阶段 2：历史记录批量下载（1 小时）

**任务清单**：
- [ ] 2.1 修改 `hooks/useAssetHistory.ts` 的下载函数
- [ ] 2.2 实现批量下载逻辑（不检查引用）
- [ ] 2.3 添加下载进度提示
- [ ] 2.4 测试批量下载功能

**修改文件**：
- `hooks/useAssetHistory.ts`
- `components/AssetHistoryPanel.tsx`（添加下载按钮）

---

### 阶段 3：历史记录删除逻辑（2 小时）

**任务清单**：
- [ ] 3.1 修改 `hooks/useAssetHistory.ts` 的删除函数
- [ ] 3.2 添加引用检查逻辑
- [ ] 3.3 实现弹窗确认组件
- [ ] 3.4 实现一键清除逻辑
- [ ] 3.5 实现"继续清理"输入框
- [ ] 3.6 清除时同步清理 IndexedDB
- [ ] 3.7 测试删除功能

**修改文件**：
- `hooks/useAssetHistory.ts`
- `components/AssetHistoryPanel.tsx`（添加弹窗）
- `services/blobStorage.ts`（添加清理函数）

---

### 阶段 4：资产库缩略图独立存储（1.5 小时）

**任务清单**：
- [ ] 3.1 修改 `hooks/useAssetLibrary.ts` 的创建逻辑
- [ ] 3.2 保存缩略图到独立键名
- [ ] 3.3 保存节点图片到独立键名
- [ ] 3.4 修改恢复逻辑
- [ ] 3.5 测试刷新恢复

**修改文件**：
- `hooks/useAssetLibrary.ts`
- `core/stores/assetLibraryStore.ts`
- `services/blobStorage.ts`

---

### 阶段 4：垃圾回收机制（1.5 小时）

**任务清单**：
- [ ] 4.1 完善 `services/blobGarbageCollector.ts`
- [ ] 4.2 实现 `cleanupUnusedBlobs()`
- [ ] 4.3 实现 `getStorageSize()`
- [ ] 4.4 添加"清理未使用的图片"按钮
- [ ] 4.5 测试垃圾回收

**修改文件**：
- `services/blobGarbageCollector.ts`
- `services/storage.ts`（添加 `getAllKeys()`）
- `components/SettingsPanel.tsx`（添加清理按钮）

---

### 阶段 5：测试和优化（1 小时）

**测试用例**：
- [ ] 5.1 创建节点 → 删除节点 → 刷新 → 节点不恢复（正确）
- [ ] 5.2 创建节点 → 保存到资产库 → 删除节点 → 刷新 → 资产库缩略图仍可见（正确）
- [ ] 5.3 创建历史记录 → 删除 → 无引用 → 直接删除（正确）
- [ ] 5.4 创建历史记录 → 保存到资产库 → 删除历史记录 → 弹窗确认（正确）
- [ ] 5.5 一键清除 → 跳过被引用的 → 显示结果（正确）
- [ ] 5.6 一键清除 → 输入"确认" → 清理被引用的（正确）
- [ ] 5.7 垃圾回收 → 清理未使用的 Blob → 显示清理空间（正确）

---

## 🚫 禁止事项（架构保护）

### 不允许的操作

1. ❌ **禁止修改 App.tsx 的业务逻辑**
   - 只能添加恢复逻辑（useEffect）
   - 不能修改现有功能

2. ❌ **禁止破坏现有功能**
   - 图片生成、视频上传、历史记录等
   - 必须保持 100% 兼容

3. ❌ **禁止跨层混用**
   - UI 组件 → Hook → Store（单向数据流）
   - 不能跳过 Hook 直接操作 Store

4. ❌ **禁止简单粗暴的删除**
   - 不能"删除节点就清理 IndexedDB"
   - 必须检查引用关系

5. ❌ **禁止阻塞 UI**
   - IndexedDB 操作必须异步
   - 使用 RAF 或 setTimeout

---

## 📝 验收标准

### 功能验收

- [ ] 历史记录删除时检查引用，有引用时弹窗确认
- [ ] 一键清除时跳过被引用的，显示清理结果
- [ ] 提供"继续清理"选项，用户输入"确认"才能清理被引用的
- [ ] 资产库缩略图独立存储，删除节点不影响
- [ ] 刷新后所有数据完整恢复
- [ ] 垃圾回收功能正常工作

### 性能验收

- [ ] 引用检查时间 < 100ms（100 条记录）
- [ ] 删除操作不阻塞 UI
- [ ] 刷新恢复时间 < 2 秒（10 个节点）

### 用户体验验收

- [ ] 提示信息清晰明确
- [ ] 弹窗设计友好
- [ ] 不会误删正在使用的数据
- [ ] 清理逻辑用户可控

---

## 🔄 回滚方案

### 如果出现问题

1. **立即停止实施**
2. **记录错误信息**
3. **回滚到上一个稳定版本**
4. **分析问题原因**
5. **修复后重新实施**

### 回滚步骤

```bash
# 1. 查看 Git 状态
git status

# 2. 回滚所有修改
git checkout .

# 3. 清理未跟踪文件
git clean -fd

# 4. 验证功能正常
# 测试图片生成、视频上传、历史记录等
```

---

## 📚 参考文档

- **架构文档**：`ARCHITECTURE.md`
- **项目入职规则**：`.kiro/steering/project-onboarding.md`
- **Blob 存储服务**：`services/blobStorage.ts`
- **Blob 垃圾回收器**：`services/blobGarbageCollector.ts`
- **节点 Store**：`core/stores/nodeStore.ts`
- **资产库 Store**：`core/stores/assetLibraryStore.ts`
- **历史记录 Store**：`core/stores/assetHistoryStore.ts`
- **历史记录 Hook**：`hooks/useAssetHistory.ts`
- **资产库 Hook**：`hooks/useAssetLibrary.ts`

---

## 🎯 成功指标

### 用户体验

- ✅ 不会误删正在使用的数据
- ✅ 清理逻辑清晰，用户可控
- ✅ 刷新后工作成果不丢失
- ✅ 资产库缩略图永久可见

### 技术指标

- ✅ 代码清晰，易维护
- ✅ 架构合理，易扩展
- ✅ 错误处理完整
- ✅ 日志清晰可追踪

### 数据安全

- ✅ 引用检查准确
- ✅ 删除前二次确认
- ✅ 独立存储不互相影响
- ✅ 垃圾回收安全可靠

---

## 💡 关键设计决策

### 决策 1：为什么不用引用计数？

**原因**：
- 引用计数需要在每次创建/删除时维护
- 容易出现计数不准确的问题
- 实时检查更简单、更可靠

**方案**：
- 删除时实时检查引用关系
- 不维护引用计数
- 更简单、更可靠

---

### 决策 2：为什么资产库缩略图要独立存储？

**原因**：
- 用户删除画布节点后，资产库缩略图不能消失
- 如果共用键名，删除节点会导致缩略图失效
- 独立存储确保资产库永久可用

**方案**：
- 资产库缩略图：asset-thumbnail-{assetId}
- 资产库节点图片：asset-{assetId}-node-{nodeId}-image
- 画布节点图片：blob-node-{nodeId}-image
- 三者完全独立，互不影响

---

### 决策 3：为什么一键清除要分两步？

**原因**：
- 用户可能不知道有些记录被引用
- 直接清理被引用的记录会导致资产库或画布出问题
- 分两步让用户明确知道后果

**方案**：
- 第一步：清理独占的，跳过被引用的
- 第二步：显示被跳过的，提供"继续清理"选项
- 用户输入"确认"才能清理被引用的

---

## 🔧 技术细节

### IndexedDB 键名规范

```
画布节点：
  - blob-node-{nodeId}-image（单张图片）
  - blob-{nodeId}-grid-{i}（九宫格）

历史记录：
  - asset-{assetId}（图片）

资产库：
  - asset-thumbnail-{assetId}（缩略图）
  - asset-{assetId}-node-{nodeId}-image（节点图片）
  - asset-{assetId}-node-{nodeId}-image-{i}（节点九宫格）

引用计数（未使用）：
  - ref-count-{blobKey}（引用计数记录）
```

### 数据结构

```typescript
// 历史记录
interface AssetHistory {
  id: string;                    // 资产 ID
  src: string;                   // 图片 URL（Blob URL）
  prompt: string;                // 提示词
  createdAt: number;             // 创建时间
  usedByAssetLibrary?: boolean;  // 是否被资产库使用（可选）
}

// 资产库
interface Asset {
  id: string;                    // 资产 ID
  name: string;                  // 资产名称
  category: AssetCategory;       // 分类
  thumbnail: string;             // 缩略图（Blob URL）
  thumbnailType: ThumbnailType;  // 缩略图类型
  nodes: AppNode[];              // 节点数据（深拷贝）
  connections: Connection[];     // 连接关系
  createdAt: number;             // 创建时间
  updatedAt: number;             // 更新时间
}

// 引用信息
interface ReferenceInfo {
  inAssetLibrary: boolean;       // 是否被资产库引用
  inCanvas: boolean;             // 是否在画布上使用
  message: string;               // 提示信息
}
```

---

**文档版本**：v1.0  
**创建日期**：2026-02-10  
**状态**：✅ 已完成，待审核

---

## 📌 给下一个 AI 的提示

**亲爱的接班 AI，你好！**

这份文档包含了完整的数据清理机制需求。请务必：

1. **先完整阅读这份文档**（不要跳过任何部分）
2. **理解核心规则**（特别是规则 1-5）
3. **理解为什么要这样设计**（关键设计决策）
4. **按照实施计划逐步实施**（不要跳步）
5. **严格遵守禁止事项**（不要破坏现有功能）
6. **完成后进行完整测试**（所有测试用例）

**如果有任何疑问，请先查阅：**
- `ARCHITECTURE.md`（架构规则）
- `.kiro/steering/project-onboarding.md`（入职规则）
- `services/blobStorage.ts`（存储服务）
- `services/blobGarbageCollector.ts`（垃圾回收器）

**祝你工作顺利！** 🚀


**检查函数**：

```typescript
/**
 * 检查图片是否被资产库引用
 * @param assetId - 历史记录的资产 ID
 * @returns 是否被引用
 */
const isReferencedByAssetLibrary = (assetId: string): boolean => {
  const assets = useAssetLibraryStore.getState().assets;
  
  // 检查是否有资产引用了这个历史记录
  return assets.some(asset => {
    // 检查资产的节点数据中是否有相同的图片
    return asset.nodes.some(node => {
      // 比较节点的图片 URL 或 ID
      return node.data.historyAssetId === assetId;
    });
  });
};

/**
 * 检查图片是否在画布上使用
 * @param assetId - 历史记录的资产 ID
 * @returns 是否在使用
 */
const isUsedInCanvas = (assetId: string): boolean => {
  const nodes = useNodeStore.getState().getAllNodes();
  
  // 检查是否有节点使用了这个历史记录的图片
  return nodes.some(node => {
    return node.data.historyAssetId === assetId;
  });
};

/**
 * 获取引用信息（用于显示）
 * @param assetId - 历史记录的资产 ID
 * @returns 引用信息
 */
const getReferenceInfo = (assetId: string): {
  inAssetLibrary: boolean;
  inCanvas: boolean;
  message: string;
} => {
  const inAssetLibrary = isReferencedByAssetLibrary(assetId);
  const inCanvas = isUsedInCanvas(assetId);
  
  let message = '';
  if (inAssetLibrary && inCanvas) {
    message = '该图片正在画布和资产库中使用';
  } else if (inAssetLibrary) {
    message = '该图片已被资产库使用';
  } else if (inCanvas) {
    message = '该图片正在画布上使用';
  }
  
  return { inAssetLibrary, inCanvas, message };
};
```
