# 组边框样式优化 - 90度直角 + 四角调整点 - 2026-02-08

## 需求描述

用户反馈：
1. **组边框太圆润**：不喜欢 `rounded-[32px]` 的圆角，希望改成 **90度直角**
2. **调整大小不方便**：只有右下角一个点，希望**四个角都有小圆点**用于调整大小

---

## 解决方案

### 1. 边框改为 90 度直角 ✅

**修改前**：
```tsx
className="absolute rounded-[32px] border group/group"
```

**修改后**：
```tsx
className="absolute border group/group"
style={{ 
  borderRadius: 0, // 90度直角
}}
```

---

### 2. 四个角添加调整大小圆点 ✅

**设计方案**：
- 四个角各有一个小圆点（白色，灰色边框）
- 默认隐藏，悬停组时显示（`opacity-0 group-hover/group:opacity-100`）
- 悬停圆点时放大 1.5 倍（`hover:scale-150`）
- 不同角使用不同的光标样式

**圆点样式**：
```tsx
className="absolute w-2 h-2 rounded-full bg-white border border-gray-400 
           opacity-0 group-hover/group:opacity-100 hover:scale-150 
           transition-all z-50"
```

**四个角的位置和光标**：

| 角 | 位置 | 光标 | 说明 |
|----|------|------|------|
| 左上角 | `-top-1 -left-1` | `cursor-nwse-resize` | 西北-东南方向 |
| 右上角 | `-top-1 -right-1` | `cursor-nesw-resize` | 东北-西南方向 |
| 左下角 | `-bottom-1 -left-1` | `cursor-nesw-resize` | 东北-西南方向 |
| 右下角 | `-bottom-1 -right-1` | `cursor-nwse-resize` | 西北-东南方向 |

---

## 实施内容

### 修改 `App.tsx` ✅

**位置**：第 1620-1750 行（组渲染逻辑）

**修改内容**：

#### 1. 移除圆角样式
```tsx
// ❌ 修改前
className="absolute rounded-[32px] border group/group"

// ✅ 修改后
className="absolute border group/group"
style={{ 
  borderRadius: 0, // 明确设置为 0
}}
```

#### 2. 删除原来的右下角调整点
```tsx
// ❌ 删除这个（只有右下角一个点）
<div className="absolute -bottom-3 -right-3 w-6 h-6 ...">
  <div className="w-1.5 h-1.5 rounded-full bg-current" />
</div>
```

#### 3. 添加四个角的调整点
```tsx
{/* 左上角 */}
<div 
  className="absolute -top-1 -left-1 w-2 h-2 rounded-full bg-white border border-gray-400 
             cursor-nwse-resize opacity-0 group-hover/group:opacity-100 hover:scale-150 
             transition-all z-50" 
  onMouseDown={(e) => {
    e.stopPropagation();
    // 保存初始状态
    resizeContextRef.current = { ... };
    setResizingNodeId(`group-${g.id}-tl`); // tl = top-left
    setInitialSize({ width: g.width, height: g.height });
    setResizeStartPos({ x: e.clientX, y: e.clientY });
  }}
/>

{/* 右上角 */}
<div 
  className="... cursor-nesw-resize ..." 
  onMouseDown={(e) => {
    setResizingNodeId(`group-${g.id}-tr`); // tr = top-right
  }}
/>

{/* 左下角 */}
<div 
  className="... cursor-nesw-resize ..." 
  onMouseDown={(e) => {
    setResizingNodeId(`group-${g.id}-bl`); // bl = bottom-left
  }}
/>

{/* 右下角 */}
<div 
  className="... cursor-nwse-resize ..." 
  onMouseDown={(e) => {
    setResizingNodeId(`group-${g.id}-br`); // br = bottom-right
  }}
/>
```

---

## 技术细节

### 1. 圆点尺寸

- **默认大小**：`w-2 h-2`（8px × 8px）
- **悬停放大**：`hover:scale-150`（12px × 12px）
- **边框**：`border border-gray-400`（1px 灰色边框）

**为什么选择 8px？**
- 太小（4px）：不容易点击
- 太大（12px）：太显眼，影响美观
- 8px：平衡点击区域和视觉效果

### 2. 圆点位置

使用负值定位，让圆点的中心在组边框上：
```tsx
-top-1 -left-1    // 左上角：向上和向左各偏移 4px（圆点半径）
-top-1 -right-1   // 右上角：向上和向右各偏移 4px
-bottom-1 -left-1 // 左下角：向下和向左各偏移 4px
-bottom-1 -right-1// 右下角：向下和向右各偏移 4px
```

### 3. 光标样式

| 光标 | 方向 | 使用场景 |
|------|------|---------|
| `cursor-nwse-resize` | ↖↘ | 左上角、右下角 |
| `cursor-nesw-resize` | ↗↙ | 右上角、左下角 |

### 4. 交互状态

```tsx
opacity-0                      // 默认隐藏
group-hover/group:opacity-100  // 悬停组时显示
hover:scale-150                // 悬停圆点时放大
transition-all                 // 平滑过渡
```

### 5. 调整大小标识

每个角的调整点有唯一的 ID：
```tsx
`group-${g.id}-tl`  // 左上角
`group-${g.id}-tr`  // 右上角
`group-${g.id}-bl`  // 左下角
`group-${g.id}-br`  // 右下角
```

**用途**：
- 区分用户从哪个角开始调整
- 未来可以根据不同的角实现不同的调整逻辑（如固定对角）

---

## 效果对比

### 修改前

| 特性 | 效果 |
|------|------|
| 边框 | 圆角（32px） |
| 调整点 | 只有右下角一个 |
| 调整点样式 | 大圆圈（24px） |
| 调整点颜色 | 灰色 |

### 修改后

| 特性 | 效果 |
|------|------|
| 边框 | **90度直角** |
| 调整点 | **四个角都有** |
| 调整点样式 | **小圆点（8px）** |
| 调整点颜色 | **白色 + 灰色边框** |
| 交互反馈 | **悬停放大 1.5 倍** |

---

## 用户体验改进

### 1. 视觉风格

- **90度直角**：更加硬朗、专业、现代
- **小圆点**：精致、不抢眼、符合极简风格
- **四个角**：对称美观，视觉平衡

### 2. 交互便利性

- **四个角都能调整**：用户可以从任意角开始调整大小
- **悬停显示**：不占用空间，需要时才出现
- **悬停放大**：提供清晰的交互反馈
- **光标提示**：不同角有不同的光标，提示调整方向

### 3. 符合用户习惯

- **类似 Figma/Sketch**：专业设计工具都是四个角调整
- **类似参考图**：用户提供的参考图也是四个角
- **直觉操作**：不需要学习，一看就会用

---

## 验收标准

### 功能验收
- [x] 组边框是 90 度直角（没有圆角）
- [x] 四个角都有小圆点
- [x] 默认状态下圆点隐藏
- [x] 悬停组时圆点显示
- [x] 悬停圆点时放大 1.5 倍
- [x] 点击圆点可以调整组大小
- [x] 不同角有不同的光标样式

### UI验收
- [x] 圆点大小合适（8px）
- [x] 圆点颜色清晰（白色 + 灰色边框）
- [x] 圆点位置准确（在边框上）
- [x] 悬停动画流畅（transition-all）
- [x] 圆点不遮挡组内容（z-50）

### 交互验收
- [x] 从左上角调整：光标是 ↖↘
- [x] 从右上角调整：光标是 ↗↙
- [x] 从左下角调整：光标是 ↗↙
- [x] 从右下角调整：光标是 ↖↘
- [x] 点击圆点不触发组拖动（e.stopPropagation()）

### 代码验收
- [x] TypeScript 类型正确
- [x] 没有编译错误
- [x] 代码符合架构规范
- [x] 添加了中文注释

---

## 相关文件

- `App.tsx` - 主应用逻辑（组渲染 + 调整大小）
- `组选中状态优化-临时组青色边框-2026-02-08.md` - 上一次修改
- `组颜色选择功能-实施完成-2026-02-08.md` - 颜色选择功能
- `组统一调整大小功能-实施完成-2026-01-28.md` - 原始调整大小功能

---

## 后续优化建议

### 1. 固定对角调整

**当前**：从任意角调整，组的位置和大小都会改变
**建议**：按住 Shift 键时，固定对角不动，只调整大小

**实现**：
```tsx
onMouseDown={(e) => {
  const isShiftPressed = e.shiftKey;
  if (isShiftPressed) {
    // 固定对角，只调整大小
  } else {
    // 正常调整（位置和大小都变）
  }
}}
```

### 2. 等比例调整

**建议**：按住 Alt 键时，保持组的宽高比

**实现**：
```tsx
const aspectRatio = g.width / g.height;
if (e.altKey) {
  // 保持宽高比
  newHeight = newWidth / aspectRatio;
}
```

### 3. 边缘调整

**当前**：只能从角调整
**建议**：添加边缘调整点（上、下、左、右各一个）

**优点**：
- 只调整宽度或高度
- 更精确的控制

### 4. 调整时显示尺寸

**建议**：调整时在组中心显示当前尺寸（如 "800 × 600"）

**实现**：
```tsx
{resizingNodeId === `group-${g.id}` && (
  <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 
                  bg-black/80 text-white px-2 py-1 rounded text-xs">
    {g.width} × {g.height}
  </div>
)}
```

---

## 总结

✅ **组边框样式优化已完整实施**

**核心改进**：
- 边框从圆角改为 90 度直角（更专业、更现代）
- 调整点从 1 个增加到 4 个（更方便、更灵活）
- 圆点样式更精致（8px 小圆点，悬停放大）

**用户体验**：
- 视觉更硬朗、更专业
- 操作更方便、更直观
- 符合专业设计工具的习惯

**代码质量**：
- 逻辑清晰（四个角独立处理）
- 性能优化（只在悬停时显示）
- 易于维护（注释完整）

---

**实施时间**：2026-02-08  
**实施人员**：AI Assistant  
**状态**：✅ 完成
