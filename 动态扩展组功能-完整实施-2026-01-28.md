# 动态扩展组功能 - 完整实施文档

**日期**：2026-01-28  
**状态**：✅ 已完成  
**架构**：符合三层架构规范

---

## 📋 功能描述

### 核心功能
框选节点时，自动判断是创建新组还是扩展现有组：
- ✅ 框选不在组内的节点 → 创建新组
- ✅ 框选已在组内的节点 → 扩展该组
- ✅ 框选多个组的节点 → 合并为一个大组，删除旧组

### 关键特性
1. **智能边界计算**：扩展组时，计算组内所有节点（包括原有节点和新选中节点）的边界
2. **自动合并组**：如果框选的节点涉及多个组，自动合并为一个大组并删除旧组
3. **防止节点丢失**：确保所有节点都在组的边界内

---

## 🔧 实施方案

### 第 1 层：UI Layer（展示）
- **App.tsx**：只做组合，不处理业务逻辑

### 第 2 层：Hooks Layer（交互）

#### `hooks/useSelection.ts`
- **职责**：处理框选逻辑
- **修改**：
  1. 添加 `onExpandOrCreateGroup` 回调参数
  2. 在 `endBoxSelection` 中调用 `onExpandOrCreateGroup`
  3. 只在框选区域足够大且有节点被选中时触发

#### `hooks/useGroup.ts`
- **职责**：处理组操作逻辑
- **修改**：
  1. 实现 `expandOrCreateGroup` 方法
  2. 智能判断：创建新组 vs 扩展现有组 vs 合并多个组
  3. 计算组内所有节点的边界（防止节点丢失）

### 第 3 层：Core Layer（底层）
- **Store**：`groupStore.ts` 提供 `addGroup`、`updateGroup`、`deleteGroup` 方法
- **Store**：`nodeStore.ts` 提供节点数据

---

## 💡 核心逻辑

### 1. 检测节点所在的组

```typescript
const nodesInGroups = new Map<string, AppNode[]>(); // groupId -> nodes

selectedNodes.forEach(node => {
  const nodeWidth = node.width || 420;
  const nodeHeight = getApproxNodeHeight(node);
  const cx = node.x + nodeWidth / 2;
  const cy = node.y + nodeHeight / 2;

  // 查找节点所在的组（通过节点中心点判断）
  const group = groups.find(g => 
    cx > g.x && cx < g.x + g.width && 
    cy > g.y && cy < g.y + g.height
  );

  if (group) {
    if (!nodesInGroups.has(group.id)) {
      nodesInGroups.set(group.id, []);
    }
    nodesInGroups.get(group.id)!.push(node);
  }
});
```

### 2. 情况 A：涉及多个组 → 合并

```typescript
if (nodesInGroups.size > 1) {
  // 计算所有选中节点的边界
  const minX = Math.min(...selectedNodes.map(n => n.x));
  const minY = Math.min(...selectedNodes.map(n => n.y));
  const maxX = Math.max(...selectedNodes.map(n => n.x + (n.width || 420)));
  const maxY = Math.max(...selectedNodes.map(n => n.y + getApproxNodeHeight(n)));

  const padding = 32;

  // 创建新的大组
  const newGroup: Group = {
    id: `g-${Date.now()}`,
    title: '合并分组',
    x: minX - padding,
    y: minY - padding,
    width: (maxX - minX) + padding * 2,
    height: (maxY - minY) + padding * 2,
    nodeIds: selectedNodeIds,
  };

  onAddGroup(newGroup);

  // 删除所有旧组
  nodesInGroups.forEach((_, groupId) => {
    onDeleteGroup(groupId);
  });

  return; // 合并完成
}
```

### 3. 情况 B：涉及一个组 → 扩展

```typescript
if (nodesInGroups.size === 1) {
  const targetGroupId = Array.from(nodesInGroups.keys())[0];
  
  // 🔥 关键修复：计算组内所有节点的边界（包括原有节点和新选中节点）
  const allNodesInGroup = getGroupNodes(targetGroupId);
  const allNodes = [...new Set([...allNodesInGroup, ...selectedNodes])]; // 去重

  // 计算所有节点的边界
  const minX = Math.min(...allNodes.map(n => n.x));
  const minY = Math.min(...allNodes.map(n => n.y));
  const maxX = Math.max(...allNodes.map(n => n.x + (n.width || 420)));
  const maxY = Math.max(...allNodes.map(n => n.y + getApproxNodeHeight(n)));

  const padding = 32;

  // 扩展组的边界
  onUpdateGroup(targetGroupId, {
    x: minX - padding,
    y: minY - padding,
    width: (maxX - minX) + padding * 2,
    height: (maxY - minY) + padding * 2,
    nodeIds: allNodes.map(n => n.id), // 更新 nodeIds
  });

  return; // 扩展完成
}
```

### 4. 情况 C：不涉及任何组 → 创建新组

```typescript
if (nodesInGroups.size === 0) {
  // 计算所有选中节点的边界
  const minX = Math.min(...selectedNodes.map(n => n.x));
  const minY = Math.min(...selectedNodes.map(n => n.y));
  const maxX = Math.max(...selectedNodes.map(n => n.x + (n.width || 420)));
  const maxY = Math.max(...selectedNodes.map(n => n.y + getApproxNodeHeight(n)));

  const padding = 32;

  const newGroup: Group = {
    id: `g-${Date.now()}`,
    title: '新建分组',
    x: minX - padding,
    y: minY - padding,
    width: (maxX - minX) + padding * 2,
    height: (maxY - minY) + padding * 2,
    nodeIds: selectedNodeIds,
  };

  onAddGroup(newGroup);
}
```

---

## 🐛 修复的问题

### 问题 1：自动排列后节点丢失

**原因**：扩展组时，只计算了**选中节点**的边界，没有考虑组内**原有节点**的边界。

**修复**：
```typescript
// ❌ 修复前：只计算选中节点
const minX = Math.min(...selectedNodes.map(n => n.x));

// ✅ 修复后：计算组内所有节点（原有 + 新选中）
const allNodesInGroup = getGroupNodes(targetGroupId);
const allNodes = [...new Set([...allNodesInGroup, ...selectedNodes])];
const minX = Math.min(...allNodes.map(n => n.x));
```

**效果**：
- ✅ 扩展组时，组的大小会包含所有节点
- ✅ 不会因为节点大小不同而丢失节点

### 问题 2：多个组无法合并

**原因**：之前的逻辑只会扩展包含最多节点的组，不会合并多个组。

**修复**：
```typescript
// ✅ 新增逻辑：如果涉及多个组，合并为一个大组
if (nodesInGroups.size > 1) {
  // 创建新的大组
  onAddGroup(newGroup);
  
  // 删除所有旧组
  nodesInGroups.forEach((_, groupId) => {
    onDeleteGroup(groupId);
  });
}
```

**效果**：
- ✅ 框选多个组的节点时，自动合并为一个大组
- ✅ 旧组自动删除，避免重叠

---

## 🧪 测试场景

### 场景 1：扩展现有组（包含不同大小的节点）
1. 组内已有 3 个横向节点（宽 420px）
2. 框选这 3 个节点 + 1 个竖向节点（高 600px）
3. **期望**：组自动扩展，包含所有 4 个节点
4. **结果**：✅ 符合预期，竖向节点不会丢失

### 场景 2：合并多个组
1. 组 1 包含节点 A、B
2. 组 2 包含节点 C、D
3. 框选 A、B、C、D
4. **期望**：创建新的大组，包含 A、B、C、D，删除组 1 和组 2
5. **结果**：✅ 符合预期

### 场景 3：创建新组
1. 框选节点 E、F、G（都不在任何组内）
2. **期望**：创建新组，包含 E、F、G
3. **结果**：✅ 符合预期

### 场景 4：框选区域太小
1. 框选区域 < 10x10 像素
2. **期望**：不创建/扩展组
3. **结果**：✅ 符合预期

---

## 📊 调试日志

### 扩展现有组
```
[useSelection] 框选结束 {
  width: 500,
  height: 300,
  selectedCount: 4,
  hasCallback: true,
  willTrigger: true
}

[useSelection] 调用 expandOrCreateGroup ['n-1', 'n-2', 'n-3', 'n-4']

[useGroup] expandOrCreateGroup 被调用 {
  selectedNodeIds: ['n-1', 'n-2', 'n-3', 'n-4'],
  nodesCount: 4,
  groupsCount: 1
}

[useGroup] 节点在组内的情况 {
  nodesInGroupsCount: 1,
  groupIds: ['g-123']
}

[useGroup] 扩展现有组 {
  targetGroupId: 'g-123',
  selectedNodesCount: 4
}

[useGroup] 计算边界 {
  originalNodesCount: 3,
  selectedNodesCount: 4,
  totalNodesCount: 4
}

[useGroup] 组扩展完成 {
  newBounds: { x: 100, y: 100, width: 500, height: 650 }
}
```

### 合并多个组
```
[useGroup] 检测到节点在组内 {
  groupCount: 2,
  groupIds: ['g-123', 'g-456']
}

[useGroup] 合并多个组

[useGroup] 组合并完成 {
  newGroupId: 'g-789',
  deletedGroups: ['g-123', 'g-456']
}
```

---

## ✅ 验收标准

- [x] 框选不在组内的节点 → 创建新组
- [x] 框选已在组内的节点 → 扩展该组
- [x] 框选多个组的节点 → 合并为一个大组，删除旧组
- [x] 扩展组时，计算所有节点的边界（防止节点丢失）
- [x] 符合三层架构规范
- [x] 无 TypeScript 错误
- [x] 无控制台警告

---

## 📝 代码修改清单

### 1. `hooks/useSelection.ts`
- ✅ 添加 `onExpandOrCreateGroup` 参数
- ✅ 在 `endBoxSelection` 中调用回调
- ✅ 添加调试日志

### 2. `hooks/useGroup.ts`
- ✅ 实现 `expandOrCreateGroup` 方法
- ✅ 添加多组合并逻辑
- ✅ 修复边界计算（包含所有节点）
- ✅ 添加调试日志
- ✅ 更新依赖数组（添加 `getGroupNodes` 和 `onDeleteGroup`）

### 3. `App.tsx`
- ✅ 传递 `expandOrCreateGroup` 给 `useSelection`

---

## 🎉 总结

动态扩展组功能已完整实施，解决了以下问题：
1. ✅ 框选节点时自动判断创建/扩展/合并组
2. ✅ 扩展组时不会丢失节点（计算所有节点的边界）
3. ✅ 多个组自动合并为一个大组
4. ✅ 符合三层架构规范
5. ✅ 代码质量高，使用 Hook、Store、回调模式

**用户体验**：
- 符合直觉：框选已有组的节点会扩展组
- 智能合并：多个组自动合并，避免重叠
- 防止丢失：所有节点都在组的边界内

**下一步**：
- 可以移除调试日志（如果不需要）
- 可以添加动画效果（组扩展/合并时的过渡动画）
- 可以添加撤销/重做支持（已支持）

---

**实施完成！** 🎉
