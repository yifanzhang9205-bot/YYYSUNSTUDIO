# 性能优化 - 虚拟化渲染 - 学习 Lovart - 2026-01-31

## 🔍 问题分析

**用户反馈**：
- 拖入 5 张 4-5MB 的图片，明显卡顿
- Lovart 拖入同样的图片，只增加 10MB 内存，无卡顿
- Lovart 放大缩小无像素损失

**关键发现**：
- ❌ 不是缩略图（会有像素损失）
- ✅ 是虚拟化渲染 + 懒加载 + 异步解码

---

## 🔥 Lovart 的真正策略

### 策略 1：虚拟化渲染（Virtualization）

**原理**：只渲染可见区域的节点，不可见的节点不渲染。

```typescript
// ❌ 你的代码：渲染所有节点
const App = () => {
  const nodes = useNodeStore(state => state.nodes);
  
  return (
    <div>
      {Array.from(nodes.values()).map(node => (
        <Node key={node.id} node={node} />
      ))}
    </div>
  );
};

// ✅ Lovart 的做法：只渲染可见节点
const App = () => {
  const nodes = useNodeStore(state => state.nodes);
  const viewport = useViewport();
  
  // 🔥 关键：过滤出可见节点
  const visibleNodes = useMemo(() => {
    return Array.from(nodes.values()).filter(node => {
      return isNodeInViewport(node, viewport);
    });
  }, [nodes, viewport]);
  
  return (
    <div>
      {visibleNodes.map(node => (
        <Node key={node.id} node={node} />
      ))}
    </div>
  );
};
```

**效果**：
- 画布上有 100 个节点，但只有 10 个可见
- 你的代码：渲染 100 个节点（卡顿）
- Lovart：渲染 10 个节点（流畅）

---

### 策略 2：懒加载（Lazy Loading）

**原理**：图片进入可见区域时才加载，离开可见区域时卸载。

```typescript
// ❌ 你的代码：立即加载所有图片
const Node = ({ node }) => {
  return (
    <div>
      <img src={node.data.image} />
    </div>
  );
};

// ✅ Lovart 的做法：懒加载图片
const Node = ({ node }) => {
  const [isLoaded, setIsLoaded] = useState(false);
  const imgRef = useRef<HTMLImageElement>(null);
  
  useEffect(() => {
    // 使用 Intersection Observer 检测可见性
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            setIsLoaded(true); // 进入可见区域，加载图片
          } else {
            setIsLoaded(false); // 离开可见区域，卸载图片
          }
        });
      },
      { threshold: 0.1 }
    );
    
    if (imgRef.current) {
      observer.observe(imgRef.current);
    }
    
    return () => observer.disconnect();
  }, []);
  
  return (
    <div ref={imgRef}>
      {isLoaded ? (
        <img src={node.data.image} />
      ) : (
        <div className="placeholder">加载中...</div>
      )}
    </div>
  );
};
```

**效果**：
- 拖入 5 张图片，但只有 2 张可见
- 你的代码：立即加载 5 张图片（卡顿）
- Lovart：只加载 2 张图片（流畅）

---

### 策略 3：异步解码（Decode Async）

**原理**：使用 `img.decode()` 异步解码图片，不阻塞主线程。

```typescript
// ❌ 你的代码：同步解码（阻塞主线程）
const Node = ({ node }) => {
  return (
    <div>
      <img src={node.data.image} />
      {/* 浏览器同步解码，阻塞主线程，卡顿！ */}
    </div>
  );
};

// ✅ Lovart 的做法：异步解码（不阻塞主线程）
const Node = ({ node }) => {
  const [imageSrc, setImageSrc] = useState<string | null>(null);
  const [isDecoding, setIsDecoding] = useState(false);
  
  useEffect(() => {
    if (!node.data.image) return;
    
    const loadImage = async () => {
      setIsDecoding(true);
      
      try {
        // 🔥 关键：异步解码
        const img = new Image();
        img.src = node.data.image;
        await img.decode(); // 异步解码，不阻塞主线程
        
        // 解码完成后才显示
        setImageSrc(node.data.image);
      } catch (error) {
        console.error('图片解码失败:', error);
      } finally {
        setIsDecoding(false);
      }
    };
    
    loadImage();
  }, [node.data.image]);
  
  return (
    <div>
      {isDecoding && <div>解码中...</div>}
      {imageSrc && <img src={imageSrc} />}
    </div>
  );
};
```

**效果**：
- 拖入 5 张图片
- 你的代码：浏览器同步解码 5 张图片，主线程阻塞 2 秒（卡顿）
- Lovart：异步解码，主线程不阻塞（流畅）

---

### 策略 4：Web Worker 解码（终极优化）

**原理**：在 Worker 线程中解码图片，完全不占用主线程。

```typescript
// ✅ Lovart 的终极优化：Worker 解码
// imageDecoder.worker.ts
self.onmessage = async (e) => {
  const { imageUrl } = e.data;
  
  try {
    // 在 Worker 中解码
    const response = await fetch(imageUrl);
    const blob = await response.blob();
    const imageBitmap = await createImageBitmap(blob);
    
    // 发送解码后的 ImageBitmap
    self.postMessage({ imageBitmap }, [imageBitmap]);
  } catch (error) {
    self.postMessage({ error: error.message });
  }
};

// 主线程
const Node = ({ node }) => {
  const [imageBitmap, setImageBitmap] = useState<ImageBitmap | null>(null);
  const canvasRef = useRef<HTMLCanvasElement>(null);
  
  useEffect(() => {
    const worker = new Worker('imageDecoder.worker.ts');
    
    worker.onmessage = (e) => {
      if (e.data.imageBitmap) {
        setImageBitmap(e.data.imageBitmap);
      }
    };
    
    worker.postMessage({ imageUrl: node.data.image });
    
    return () => worker.terminate();
  }, [node.data.image]);
  
  useEffect(() => {
    if (!imageBitmap || !canvasRef.current) return;
    
    const ctx = canvasRef.current.getContext('2d')!;
    ctx.drawImage(imageBitmap, 0, 0);
  }, [imageBitmap]);
  
  return <canvas ref={canvasRef} />;
};
```

---

## 🔥 立即可以实施的优化

### 优化 1：添加 `loading="lazy"` 属性

**最简单的优化，立即见效！**

```typescript
// components/Node.tsx

// ❌ 优化前
<img src={node.data.image} alt={node.data.title} />

// ✅ 优化后
<img 
  src={node.data.image} 
  alt={node.data.title}
  loading="lazy"  // 🔥 浏览器原生懒加载
  decoding="async" // 🔥 异步解码
/>
```

**效果**：
- 浏览器自动懒加载图片
- 异步解码，不阻塞主线程
- **零代码改动，立即见效！**

---

### 优化 2：使用 Intersection Observer

**更精确的懒加载控制**

```typescript
// hooks/useLazyImage.ts

export const useLazyImage = (imageUrl: string) => {
  const [src, setSrc] = useState<string | null>(null);
  const [isLoading, setIsLoading] = useState(false);
  const ref = useRef<HTMLDivElement>(null);
  
  useEffect(() => {
    if (!ref.current || !imageUrl) return;
    
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting && !src) {
            setIsLoading(true);
            
            // 异步加载图片
            const img = new Image();
            img.onload = () => {
              setSrc(imageUrl);
              setIsLoading(false);
            };
            img.onerror = () => {
              setIsLoading(false);
            };
            img.src = imageUrl;
          }
        });
      },
      { 
        threshold: 0.1,
        rootMargin: '50px' // 提前 50px 开始加载
      }
    );
    
    observer.observe(ref.current);
    
    return () => observer.disconnect();
  }, [imageUrl, src]);
  
  return { ref, src, isLoading };
};

// components/Node.tsx
const Node = ({ node }) => {
  const { ref, src, isLoading } = useLazyImage(node.data.image);
  
  return (
    <div ref={ref}>
      {isLoading && <div>加载中...</div>}
      {src && <img src={src} alt={node.data.title} />}
    </div>
  );
};
```

---

### 优化 3：虚拟化渲染（最重要！）

**只渲染可见节点**

```typescript
// hooks/useVisibleNodes.ts

export const useVisibleNodes = (
  nodes: Map<string, AppNode>,
  viewport: { x: number; y: number; width: number; height: number; scale: number }
) => {
  return useMemo(() => {
    const visibleNodes: AppNode[] = [];
    
    // 计算可见区域（加上边距）
    const margin = 200; // 提前 200px 开始渲染
    const viewportBounds = {
      left: -viewport.x / viewport.scale - margin,
      top: -viewport.y / viewport.scale - margin,
      right: (-viewport.x + viewport.width) / viewport.scale + margin,
      bottom: (-viewport.y + viewport.height) / viewport.scale + margin,
    };
    
    // 过滤可见节点
    for (const node of nodes.values()) {
      const nodeBounds = {
        left: node.x,
        top: node.y,
        right: node.x + (node.width || 420),
        bottom: node.y + (node.height || 300),
      };
      
      // 检查是否相交
      if (
        nodeBounds.right >= viewportBounds.left &&
        nodeBounds.left <= viewportBounds.right &&
        nodeBounds.bottom >= viewportBounds.top &&
        nodeBounds.top <= viewportBounds.bottom
      ) {
        visibleNodes.push(node);
      }
    }
    
    return visibleNodes;
  }, [nodes, viewport]);
};

// App.tsx
const App = () => {
  const nodes = useNodeStore(state => state.nodes);
  const { pan, scale } = useViewport();
  
  // 🔥 关键：只获取可见节点
  const visibleNodes = useVisibleNodes(nodes, {
    x: pan.x,
    y: pan.y,
    width: window.innerWidth,
    height: window.innerHeight,
    scale,
  });
  
  return (
    <div>
      {visibleNodes.map(node => (
        <Node key={node.id} node={node} />
      ))}
    </div>
  );
};
```

---

## 🔥 性能对比

### 优化前（当前代码）

```
拖入 5 张 4MB 图片：

1. React 立即渲染 5 个 <img> 标签
2. 浏览器立即加载 5 张图片（同步）
3. 浏览器立即解码 5 张图片（同步，阻塞主线程）
4. 主线程阻塞 2 秒（卡顿！）
5. 内存占用：5 × 16MB = 80MB

总耗时：2 秒（卡顿）
```

### 优化后（Lovart 策略）

```
拖入 5 张 4MB 图片：

1. React 立即渲染 5 个占位符（无图片）
2. Intersection Observer 检测可见性
3. 只加载可见的 2 张图片（异步）
4. 异步解码 2 张图片（不阻塞主线程）
5. 主线程不阻塞（流畅！）
6. 内存占用：2 × 16MB = 32MB

总耗时：0.5 秒（流畅）
```

---

## 🔥 实施优先级

### 第一阶段：立即见效（5 分钟）

1. ✅ 添加 `loading="lazy"` 和 `decoding="async"` 属性
   - 修改 `components/Node.tsx`
   - 零代码改动，立即见效

### 第二阶段：显著提升（30 分钟）

2. ✅ 实现虚拟化渲染
   - 创建 `hooks/useVisibleNodes.ts`
   - 修改 `App.tsx`
   - 只渲染可见节点

### 第三阶段：完美体验（1 小时）

3. ✅ 实现 Intersection Observer 懒加载
   - 创建 `hooks/useLazyImage.ts`
   - 修改 `components/Node.tsx`
   - 精确控制加载时机

### 第四阶段：终极优化（2 小时）

4. ✅ 实现 Web Worker 解码
   - 创建 `workers/imageDecoder.worker.ts`
   - 完全不占用主线程

---

## 相关文档

- `性能优化-向Lovart学习.md`
- `内存泄漏分析-拖拽图片占用1GB-2026-01-31.md`
- `内存优化-消除Base64和Canvas泄漏-2026-01-31.md`

---

**方案完成时间**：2026-01-31
**方案人员**：AI Assistant
**实施状态**：⏳ 待实施
