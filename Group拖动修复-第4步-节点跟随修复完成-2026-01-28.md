# Group 拖动修复 - 第 4 步：节点跟随修复完成

**日期**: 2026-01-28  
**状态**: ✅ 已完成  
**问题**: 子节点不跟随 Group 实时移动

---

## 问题分析

### 根本原因

**DOM 查询时机问题**：`startGroupDrag` 在 `onMouseDown` 时立即执行，此时 React 可能还没有完成渲染，导致 `document.querySelector` 找不到节点。

### 证据

1. **日志显示**：`[useGroup] 找不到节点: n-1769531533282-0ow6ixebr`
2. **代码检查**：Node.tsx 中 `data-node-id` 属性设置正确
3. **选择器正确**：`[data-node-id="${child.id}"]` 语法正确

### 结论

问题不是选择器错误，而是 **React 渲染延迟**。

---

## 修复方案

### 方案 A：使用 `requestAnimationFrame` 延迟查询（✅ 已采用）

**原理**：`requestAnimationFrame` 会在浏览器下一次重绘前执行，此时 React 已经完成了 DOM 更新。

**优点**：
- 简单、可靠
- 不会阻塞主线程
- 性能开销小（约 16ms）

**实现**：

```ts
// 在 startGroupDrag 中
requestAnimationFrame(() => {
  childNodes.forEach((child) => {
    const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
    if (nodeElement) {
      nodeElement.style.transition = 'none';
      nodeElement.style.willChange = 'transform';
    }
  });
});
```

### 方案 B：使用 `getElementById` 备用方案（✅ 已采用）

**原理**：如果 `querySelector` 失败，尝试使用 `getElementById`（更快、更可靠）。

**实现**：

```ts
// 方法 1：使用 data-node-id 属性选择器
let nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;

// 方法 2：使用 id 选择器（备用方案）
if (!nodeElement) {
  nodeElement = document.getElementById(`node-${child.id}`) as HTMLElement;
}
```

---

## 修复内容

### 1. `startGroupDrag` - 禁用子节点 transition

**修复前**：
```ts
childNodes.forEach((child) => {
  const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
  if (nodeElement) {
    nodeElement.style.transition = 'none';
  }
});
```

**修复后**：
```ts
requestAnimationFrame(() => {
  childNodes.forEach((child) => {
    let nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
    if (!nodeElement) {
      nodeElement = document.getElementById(`node-${child.id}`) as HTMLElement;
    }
    if (nodeElement) {
      nodeElement.style.transition = 'none';
      nodeElement.style.willChange = 'transform';
    }
  });
});
```

### 2. `updateGroupDrag` - 实时移动子节点

**修复前**：
```ts
childNodes.forEach((child) => {
  const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
  if (nodeElement) {
    nodeElement.style.transform = `translate(${screenDx}px, ${screenDy}px)`;
  }
});
```

**修复后**：
```ts
childNodes.forEach((child) => {
  let nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
  if (!nodeElement) {
    nodeElement = document.getElementById(`node-${child.id}`) as HTMLElement;
  }
  if (nodeElement) {
    nodeElement.style.transform = `translate(${screenDx}px, ${screenDy}px)`;
    nodeElement.style.willChange = 'transform';
  }
});
```

### 3. `endGroupDrag` - 清理子节点 transform

**修复前**：
```ts
childNodes.forEach((child) => {
  const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
  if (nodeElement) {
    nodeElement.style.transform = '';
  }
});
```

**修复后**：
```ts
childNodes.forEach((child) => {
  let nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
  if (!nodeElement) {
    nodeElement = document.getElementById(`node-${child.id}`) as HTMLElement;
  }
  if (nodeElement) {
    nodeElement.style.transition = 'none';
    nodeElement.style.transform = '';
    nodeElement.style.willChange = 'auto';
    requestAnimationFrame(() => {
      if (nodeElement) {
        nodeElement.style.transition = '';
      }
    });
  }
});
```

### 4. `cancelGroupDrag` - 清理子节点 transform

**修复后**：
```ts
childNodes.forEach((child) => {
  let nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
  if (!nodeElement) {
    nodeElement = document.getElementById(`node-${child.id}`) as HTMLElement;
  }
  if (nodeElement) {
    nodeElement.style.transform = '';
    nodeElement.style.willChange = 'auto';
  }
});
```

---

## 修复效果

### 修复前

1. ❌ Group 拖动时，子节点不跟随
2. ❌ 松手后，子节点突然跳到目标位置
3. ❌ 控制台报错：`找不到节点`

### 修复后

1. ✅ Group 拖动时，子节点实时跟随
2. ✅ 拖动丝滑，无延迟
3. ✅ 松手后，子节点位置正确
4. ✅ 无控制台报错

---

## 性能优化

### 1. 使用 CSS `transform` 代替 `left/top`

**优点**：
- GPU 加速
- 不触发重排（reflow）
- 不触发 React 重渲染

### 2. 使用 `willChange` 提示浏览器

**优点**：
- 提前创建合成层
- 优化动画性能

### 3. 使用 `requestAnimationFrame` 延迟查询

**优点**：
- 等待 React 渲染完成
- 不阻塞主线程
- 性能开销小（约 16ms）

---

## 测试清单

- [x] Group 可以拖动
- [x] 拖动丝滑，无滞涩
- [x] 子节点实时跟随 Group 移动
- [x] 松手后，子节点位置正确
- [x] 可以通过 Delete 键删除 Group
- [x] 可以选中 Group
- [x] 无控制台报错

---

## 下一步

Group 拖动功能已完全修复！现在可以：

1. **测试其他 Group 功能**：
   - 一键整理（拓扑排序、网格布局）
   - 调整 Group 大小
   - 折叠/展开 Group

2. **继续架构重构**：
   - 完成阶段 B：Store 迁移
   - 完成阶段 C：代码清理

3. **优化用户体验**：
   - 添加拖动动画
   - 添加拖动提示
   - 优化边界情况

---

## 总结

通过使用 `requestAnimationFrame` 延迟 DOM 查询和 `getElementById` 备用方案，成功修复了 Group 拖动时子节点不跟随的问题。现在 Group 拖动功能完全正常，拖动丝滑，子节点实时跟随。

**核心经验**：
1. **React 渲染是异步的**，DOM 查询需要延迟
2. **使用 `requestAnimationFrame`** 等待浏览器重绘
3. **提供备用方案**（`getElementById`）提高可靠性
4. **使用 CSS `transform`** 优化性能
