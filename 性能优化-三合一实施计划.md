# 性能优化 - 三合一实施计划

## 实施顺序（从简单到复杂，确保不损坏功能）

### 阶段 1：React.memo（最安全，立即见效）✅
- **风险：** 极低
- **工作量：** 1 小时
- **效果：** 性能提升 10-100 倍
- **回滚：** 删除一行代码即可

### 阶段 2：Blob URL 存储服务（基础设施）✅
- **风险：** 低（新增服务，不影响现有功能）
- **工作量：** 2-3 小时
- **效果：** 为 URL 化做准备

### 阶段 3：URL 化（渐进式迁移）✅
- **风险：** 中（需要修改数据流）
- **工作量：** 4-6 小时
- **效果：** 内存减少 99%
- **策略：** 向后兼容，支持 base64 和 URL 两种格式

### 阶段 4：Map 数据结构（最后，最复杂）✅
- **风险：** 中高（需要修改核心数据结构）
- **工作量：** 1-2 天
- **效果：** 查找速度提升 100 倍
- **策略：** 保持 API 兼容，内部使用 Map

---

## 安全策略

### 1. 向后兼容
```typescript
// 支持旧数据格式
if (node.data.image) {
    // 旧格式：base64
    displayImage = node.data.image;
} else if (node.data.imageUrl) {
    // 新格式：Blob URL
    displayImage = node.data.imageUrl;
}
```

### 2. 渐进式迁移
```typescript
// 新生成的图片使用 URL
// 旧的图片保持 base64
// 不强制转换
```

### 3. 功能测试清单
- ✅ 图片生成
- ✅ 图片显示
- ✅ 图片下载
- ✅ 节点拖拽
- ✅ 节点连接
- ✅ 工作流保存/加载
- ✅ 九宫格选择
- ✅ 图片裁剪

---

## 实施步骤

### 步骤 1：React.memo（立即实施）
**文件：** `components/Node.tsx`
**修改：** 添加 React.memo 包装

### 步骤 2：Blob 存储服务（新建文件）
**文件：** `services/blobStorage.ts`（新建）
**修改：** 无，纯新增

### 步骤 3：URL 化（修改图片存储）
**文件：** `App.tsx`
**修改：** 图片生成后的存储逻辑
**兼容：** 保留 base64 支持

### 步骤 4：Map 数据结构（重构核心）
**文件：** `App.tsx`, `types.ts`
**修改：** 数据结构和相关操作
**兼容：** 保持外部 API 不变

---

## 开始实施
