# UI 改版 - 阶段 1 - 步骤 3：侧边栏完整修改

> 📅 创建时间：2026-02-01  
> 🎯 目标：完成侧边栏所有样式的 React Flow 风格转换  
> 📊 预计耗时：30 分钟

---

## 📋 修改清单

### 1. 批量替换规则

| 原样式 | 新样式 | 说明 |
|--------|--------|------|
| `bg-[#2c2c2e]` | `bg-white` | 深色背景 → 白色 |
| `bg-[#1c1c1e]` | `bg-white` | 更深色背景 → 白色 |
| `bg-white/5` | `bg-gray-50` | 半透明白色 → 浅灰色 |
| `bg-white/10` | `bg-gray-100` | 半透明白色 → 灰色 |
| `bg-white/20` | `bg-gray-200` | 半透明白色 → 灰色 |
| `border-white/5` | `border-gray-200` | 半透明边框 → 灰色边框 |
| `border-white/10` | `border-gray-200` | 半透明边框 → 灰色边框 |
| `border-white/20` | `border-gray-300` | 半透明边框 → 深灰色边框 |
| `border-white/30` | `border-gray-400` | 半透明边框 → 更深灰色边框 |
| `border-white/40` | `border-gray-500` | 半透明边框 → 中灰色边框 |
| `text-white/30` | `text-gray-400` | 半透明文字 → 灰色文字 |
| `text-white/50` | `text-gray-500` | 半透明文字 → 灰色文字 |
| `text-white/60` | `text-gray-600` | 半透明文字 → 深灰色文字 |
| `text-white/80` | `text-gray-700` | 半透明文字 → 更深灰色文字 |
| `text-white/90` | `text-gray-900` | 半透明文字 → 黑色文字 |
| `hover:bg-white/10` | `hover:bg-gray-100` | 悬停背景 |
| `hover:bg-white/20` | `hover:bg-gray-200` | 悬停背景 |
| `hover:bg-white/30` | `hover:bg-gray-300` | 悬停背景 |
| `hover:border-white/20` | `hover:border-gray-300` | 悬停边框 |
| `hover:border-white/30` | `hover:border-gray-400` | 悬停边框 |
| `hover:text-white/80` | `hover:text-gray-700` | 悬停文字 |
| `backdrop-blur-xl` | `` | 移除模糊效果 |
| `backdrop-blur-md` | `` | 移除模糊效果 |
| `shadow-2xl` | `shadow-lg` | 减小阴影 |

### 2. 特殊处理

#### 2.1 面板头部
- 背景：`bg-white/5 backdrop-blur-xl` → `bg-white`
- 边框：`border-white/5` → `border-gray-200`

#### 2.2 按钮样式
- 主按钮：`bg-white text-black` → 保持不变（已经是白色）
- 次按钮：`bg-white/10 hover:bg-white/20` → `bg-white hover:bg-gray-100 border border-gray-200`
- 危险按钮：`bg-red-500/20 hover:bg-red-500/30 text-red-300` → `bg-red-50 hover:bg-red-100 text-red-600 border border-red-200`
- 成功按钮：`bg-green-500/20 hover:bg-green-500/30 text-green-300` → `bg-green-50 hover:bg-green-100 text-green-600 border border-green-200`

#### 2.3 卡片样式
- 背景：`bg-white/5 hover:bg-white/10` → `bg-white hover:bg-gray-50 border border-gray-200`
- 选中状态：`border-white/40 ring-1 ring-white/20` → `border-blue-500 ring-2 ring-blue-200`

#### 2.4 空状态
- 图标背景：`bg-white/5` → `bg-gray-100`
- 文字颜色：`text-white/30` → `text-gray-400`

#### 2.5 右键菜单
- 背景：`bg-[#2c2c2e] backdrop-blur-xl` → `bg-white`
- 边框：`border-white/10` → `border-gray-200`
- 菜单项：`hover:bg-white/10` → `hover:bg-gray-100`

---

## 🎯 实施步骤

### 步骤 1：备份当前文件（已完成）

已在 `backup-ui-redesign-20260201-0300/` 中备份。

### 步骤 2：批量替换基础样式

使用 `strReplace` 工具批量替换 `components/SidebarDock.tsx` 中的样式。

### 步骤 3：手动调整特殊样式

对于复杂的样式组合，手动调整确保正确性。

### 步骤 4：测试验证

运行应用，测试所有侧边栏功能：
- [ ] 历史记录面板
- [ ] 工作流面板
- [ ] 添加节点面板
- [ ] 右键菜单
- [ ] 按钮交互
- [ ] 卡片选中状态

---

## 📊 预期效果

### 修改前（深色风格）
- 背景：深色半透明 + 模糊
- 边框：半透明白色
- 文字：半透明白色
- 按钮：深色/半透明

### 修改后（React Flow 风格）
- 背景：纯白色
- 边框：灰色
- 文字：深灰色/黑色
- 按钮：白色 + 灰色边框

---

## ⚠️ 注意事项

1. **保持功能不变**：只修改样式，不修改逻辑
2. **保持层次感**：使用不同深度的灰色区分层次
3. **保持对比度**：确保文字可读性（WCAG AA 标准）
4. **保持一致性**：所有组件使用相同的颜色系统

---

## 🚀 开始实施

准备好了吗？让我们开始批量替换！
