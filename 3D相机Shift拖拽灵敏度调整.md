# 3D 相机 Shift + 拖拽灵敏度调整

## 问题描述

**现象：** 按住 Shift + 拖拽推进/拉远时，移动幅度很小，拖动很慢

**用户需求：**
> "现在按住 shift+拖拽很慢，拖动前后移动的幅度很小，稍微大一些就可以，也不要太大不好掌控"

## 原因分析

### 当前灵敏度系数

```typescript
// 问题代码
if (e.shiftKey) {
  newZ = Math.max(0, Math.min(14, dragStateRef.current.startZ + dy * 0.025)); // ❌ 太小
}
```

**灵敏度系数：** `0.025`

**效果：**
- 拖动 100 像素 → 距离变化 2.5
- 拖动 200 像素 → 距离变化 5.0
- 拖动 400 像素 → 距离变化 10.0

**问题：** 需要拖动很长的距离才能看到明显的变化，操作不够流畅

## 解决方案

### 调整灵敏度系数

```typescript
// ✅ 修复后
if (e.shiftKey) {
  newZ = Math.max(0, Math.min(14, dragStateRef.current.startZ + dy * 0.05)); // ✅ 提高到 0.05
}
```

**新灵敏度系数：** `0.05`（翻倍）

**效果：**
- 拖动 50 像素 → 距离变化 2.5
- 拖动 100 像素 → 距离变化 5.0
- 拖动 200 像素 → 距离变化 10.0

**优势：**
- 拖动距离减半，操作更流畅
- 仍然可控，不会太敏感
- 适合快速调整距离

## 灵敏度对比

### 修复前（0.025）

| 拖动距离 | 距离变化 | 说明 |
|---------|---------|------|
| 20px | 0.5 | 几乎看不出变化 |
| 40px | 1.0 | 变化很小 |
| 100px | 2.5 | 需要拖很远 |
| 200px | 5.0 | 需要拖很远 |
| 400px | 10.0 | 需要拖很远 |

**问题：** 拖动 400 像素才能从特写（0）到远景（10），太慢了

### 修复后（0.05）

| 拖动距离 | 距离变化 | 说明 |
|---------|---------|------|
| 10px | 0.5 | 微调 |
| 20px | 1.0 | 小幅调整 |
| 50px | 2.5 | 中等调整 |
| 100px | 5.0 | 大幅调整 |
| 200px | 10.0 | 从特写到远景 |

**优势：** 拖动 200 像素就能从特写（0）到远景（10），快了一倍

## 灵敏度选择原则

### 太小（< 0.02）
- ❌ 拖动距离太长
- ❌ 操作不流畅
- ❌ 用户体验差

### 合适（0.04 - 0.06）
- ✅ 拖动距离适中
- ✅ 操作流畅
- ✅ 容易掌控
- ✅ 用户体验好

### 太大（> 0.08）
- ❌ 太敏感，难以精确控制
- ❌ 轻轻一动就变化很大
- ❌ 不好掌控

### 当前选择：0.05
- ✅ 比原来快一倍
- ✅ 拖动 200px 可以覆盖全范围（0-10）
- ✅ 仍然可控，不会太敏感
- ✅ 平衡了速度和精度

## 测试步骤

### 测试 1：基本推进/拉远
1. 打开多角度相机节点
2. 按住 Shift + 向下拖拽 100 像素
3. **预期：** 距离增加约 5（比如从 5 到 10）
4. 按住 Shift + 向上拖拽 100 像素
5. **预期：** 距离减少约 5（比如从 10 到 5）

### 测试 2：快速调整
1. 从特写（距离 0）开始
2. 按住 Shift + 向下拖拽 200 像素
3. **预期：** 快速到达远景（距离 10）
4. **感受：** 操作流畅，不需要拖很远

### 测试 3：精确控制
1. 从中景（距离 5）开始
2. 按住 Shift + 向下拖拽 10 像素
3. **预期：** 距离增加约 0.5（到 5.5）
4. **感受：** 仍然可以精确控制，不会太敏感

### 测试 4：全范围调整
1. 从特写（距离 0）开始
2. 按住 Shift + 向下拖拽到大远景（距离 14）
3. **预期：** 拖动约 280 像素（14 / 0.05）
4. **感受：** 距离适中，不会太累

## 其他灵敏度参考

### 水平旋转（0.4）
```typescript
newH = (dragStateRef.current.startH + dx * 0.4 + 360) % 360;
```
- 拖动 100px → 旋转 40°
- 拖动 900px → 旋转 360°（一圈）

### 垂直旋转（0.25）
```typescript
newV = Math.max(-30, Math.min(60, dragStateRef.current.startV - dy * 0.25));
```
- 拖动 100px → 旋转 25°
- 拖动 360px → 旋转 90°（从俯拍到仰拍）

### 距离推进（0.05）✅ 新值
```typescript
newZ = Math.max(0, Math.min(14, dragStateRef.current.startZ + dy * 0.05));
```
- 拖动 100px → 距离变化 5
- 拖动 200px → 距离变化 10（从特写到远景）

## 如果需要进一步调整

### 如果还是太慢
```typescript
// 可以调整到 0.06 或 0.07
newZ = Math.max(0, Math.min(14, dragStateRef.current.startZ + dy * 0.06));
```

### 如果太快了
```typescript
// 可以调整到 0.04 或 0.045
newZ = Math.max(0, Math.min(14, dragStateRef.current.startZ + dy * 0.04));
```

### 推荐范围
- **最小：** 0.03（比较慢，但精确）
- **推荐：** 0.05（平衡速度和精度）✅
- **最大：** 0.07（比较快，但仍可控）

## 总结

这次调整提高了 Shift + 拖拽的灵敏度：

1. ✅ **灵敏度翻倍**
   - 从 0.025 提高到 0.05
   - 拖动距离减半

2. ✅ **操作更流畅**
   - 不需要拖很远就能看到明显变化
   - 快速调整距离更方便

3. ✅ **仍然可控**
   - 不会太敏感
   - 可以精确控制
   - 平衡了速度和精度

**核心原则：** 灵敏度要适中，既要快速流畅，又要容易掌控！
