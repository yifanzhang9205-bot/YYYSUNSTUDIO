# 历史记录下载功能

## 功能说明

在历史记录面板添加了"下载并清除"按钮，可以一键下载所有图片并清除历史记录。

## 实现细节

### 1. 下载逻辑（App.tsx）

```typescript
const downloadAllImagesAndClearHistory = useCallback(async () => {
    // 收集所有图片
    const allImages: { url: string, filename: string }[] = [];
    
    Array.from(nodes.values()).forEach(node => {
        // 收集 gridImages（九宫格图片）
        if (node.data.gridImages && Array.isArray(node.data.gridImages)) {
            node.data.gridImages.forEach((url: string, i: number) => {
                allImages.push({
                    url,
                    filename: `${node.title || 'node'}-grid-${i + 1}.png`
                });
            });
        }
        
        // 收集 image（单张图片）
        if (node.data.image) {
            allImages.push({
                url: node.data.image,
                filename: `${node.title || 'node'}-${imageIndex++}.png`
            });
        }
        
        // 收集 images（多张图片）
        if (node.data.images && Array.isArray(node.data.images)) {
            node.data.images.forEach((url: string, i: number) => {
                allImages.push({
                    url,
                    filename: `${node.title || 'node'}-${i + 1}.png`
                });
            });
        }
    });
    
    // 下载所有图片
    for (const { url, filename } of allImages) {
        const a = document.createElement('a');
        a.href = url;
        a.download = filename;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        
        // 延迟避免浏览器阻止多个下载
        await new Promise(resolve => setTimeout(resolve, 100));
    }
    
    // 清除历史
    setHistory([]);
    setHistoryIndex(-1);
    
    alert(`已下载 ${allImages.length} 张图片，历史记录已清除`);
}, [nodes]);
```

### 2. UI 按钮（SidebarDock.tsx）

在历史记录面板标题栏添加按钮：

```tsx
{onDownloadAllAndClearHistory && (
    <button 
        onClick={onDownloadAllAndClearHistory}
        className="px-3 py-1.5 bg-cyan-500/20 hover:bg-cyan-500/30 text-cyan-300 rounded-lg flex items-center gap-1.5 transition-all active:scale-95 text-[10px] font-semibold"
        title="下载所有图片并清除历史"
    >
        <Save size={12} />
        <span>下载并清除</span>
    </button>
)}
```

## 使用方法

1. 打开左侧边栏的"历史记录"面板
2. 点击右上角的"下载并清除"按钮
3. 浏览器会自动下载所有图片
4. 下载完成后，历史记录自动清除

## 下载的图片

### 文件命名规则
- 九宫格图片：`节点标题-grid-1.png`, `节点标题-grid-2.png`, ...
- 单张图片：`节点标题-0.png`, `节点标题-1.png`, ...
- 多张图片：`节点标题-1.png`, `节点标题-2.png`, ...

### 支持的图片类型
- `node.data.gridImages` - 九宫格图片（多角度相机）
- `node.data.image` - 单张图片
- `node.data.images` - 多张图片数组

## 注意事项

### 1. 浏览器下载限制
- 浏览器可能会提示"是否允许下载多个文件"
- 需要用户点击"允许"才能继续下载

### 2. 下载延迟
- 每张图片之间有 100ms 延迟
- 避免浏览器阻止批量下载

### 3. Blob URL 支持
- 支持 Blob URL（`blob:http://...`）
- 支持 base64（`data:image/...`）
- 支持普通 URL（`http://...`）

## 未来改进

### 1. 打包下载
可以将所有图片打包成 ZIP 文件：

```typescript
import JSZip from 'jszip';

const zip = new JSZip();
allImages.forEach(({ url, filename }) => {
    // 将图片添加到 ZIP
    zip.file(filename, fetch(url).then(r => r.blob()));
});

// 生成 ZIP 并下载
const blob = await zip.generateAsync({ type: 'blob' });
const a = document.createElement('a');
a.href = URL.createObjectURL(blob);
a.download = 'all-images.zip';
a.click();
```

### 2. 选择性下载
添加复选框，让用户选择要下载的图片

### 3. 下载进度
显示下载进度条

## 总结

✅ 功能已实现
✅ 支持所有图片类型
✅ 自动命名文件
✅ 下载后清除历史

现在用户可以方便地备份所有图片了！🎉
