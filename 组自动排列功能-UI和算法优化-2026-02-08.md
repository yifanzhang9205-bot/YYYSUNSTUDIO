# 组自动排列功能 - UI和算法优化

## 实施时间
2026-02-08

## 用户需求

### 1. UI 改进
- **问题**：工具栏在组上方，稍微挡住了组本身
- **解决**：工具栏位置从 `-32px` 改为 `-48px`，往上移 16px

### 2. 排列功能重构
- **问题**：只有一个"自动排列"按钮，没有选择
- **解决**：改为"排列"按钮 + 下拉菜单，提供两种排列方式

### 3. 排列算法
- **宫格排列**：每排4个，按层级关系，无连接的按原本顺序
- **竖排排列**：从上到下，一列排列

---

## 实施内容

### 1. ✅ GroupToolbar UI 优化

#### 修改文件
`components/GroupToolbar.tsx`

#### 主要改动

**1.1 工具栏位置调整**
```typescript
// ❌ 之前：-32px（稍微挡住组）
const toolbarY = groupY * scale + panY - 32;

// ✅ 现在：-48px（不遮挡组）
const toolbarY = groupY * scale + panY - 48;
```

**1.2 排列按钮改为下拉菜单**
```typescript
// ❌ 之前：单个按钮
<button onClick={() => onArrangeTopology(groupId)}>
  <Grid3X3 size={14} />
  <span>自动排列</span>
</button>

// ✅ 现在：下拉菜单
<button onClick={() => setShowArrangeMenu(!showArrangeMenu)}>
  <Grid3X3 size={14} />
  <span>排列</span>
  <ChevronDown size={12} />
</button>

{showArrangeMenu && (
  <div className="下拉菜单">
    <button onClick={() => onArrangeGrid(groupId)}>宫格排列</button>
    <button onClick={() => onArrangeVertical(groupId)}>竖排排列</button>
  </div>
)}
```

**1.3 Props 更新**
```typescript
interface GroupToolbarProps {
  // ❌ 删除
  // onArrangeTopology: (groupId: string) => void;
  
  // ✅ 新增
  onArrangeGrid: (groupId: string) => void; // 宫格排列
  onArrangeVertical: (groupId: string) => void; // 竖排排列
}
```

---

### 2. ✅ useGroup Hook 新增排列算法

#### 修改文件
`hooks/useGroup.ts`

#### 主要改动

**2.1 新增 `arrangeGrid` 函数（宫格排列）**

**算法流程：**
1. 使用 Kahn 算法拓扑排序（按连接关系）
2. 将所有层的节点展平
3. 按每排4个重新分组
4. 计算每行的实际宽度（使用 `node.width`）
5. 居中对齐
6. 更新节点位置和组边界

**关键代码：**
```typescript
const arrangeGrid = useCallback((groupId: string) => {
  // 1. 拓扑排序（有连接关系的节点）
  const layers: string[][] = [];
  // ... Kahn 算法 ...
  
  // 2. 处理无连接的节点（按原本顺序）
  const unprocessed = groupNodes.filter(n => !processed.has(n.id));
  if (unprocessed.length > 0) {
    layers.push(unprocessed.map(n => n.id));
  }
  
  // 3. 展平并按每排4个重新分组
  const NODES_PER_ROW = 4;
  const allNodeIds = layers.flat();
  const gridRows: string[][] = [];
  
  for (let i = 0; i < allNodeIds.length; i += NODES_PER_ROW) {
    gridRows.push(allNodeIds.slice(i, i + NODES_PER_ROW));
  }
  
  // 4. 计算每行的实际宽度（使用 node.width）
  gridRows.forEach(row => {
    let rowWidth = 0;
    row.forEach(nodeId => {
      const node = nodes.get(nodeId);
      if (node) {
        rowWidth += (node.width || 420);
      }
    });
    rowWidth += (row.length - 1) * horizontalGap;
    
    // 5. 居中对齐
    let currentX = group.x + padding + (group.width - padding * 2 - rowWidth) / 2;
    
    // 6. 更新节点位置
    row.forEach(nodeId => {
      onUpdateNode(nodeId, { x: currentX, y: currentY });
      currentX += nodeWidth + horizontalGap;
    });
  });
}, [/* ... */]);
```

**布局参数：**
- 每排节点数：4
- 水平间距：80px
- 垂直间距：100px
- 组边距：40px

---

**2.2 新增 `arrangeVertical` 函数（竖排排列）**

**算法流程：**
1. 计算所有节点的最大宽度
2. 从上到下排列
3. 每个节点居中对齐
4. 更新节点位置和组边界

**关键代码：**
```typescript
const arrangeVertical = useCallback((groupId: string) => {
  // 1. 计算最大宽度
  let maxWidth = 0;
  groupNodes.forEach(node => {
    const nodeWidth = node.width || 420;
    maxWidth = Math.max(maxWidth, nodeWidth);
  });
  
  // 2. 居中对齐
  const startX = group.x + padding + (group.width - padding * 2 - maxWidth) / 2;
  
  // 3. 从上到下排列
  let currentY = group.y + padding;
  groupNodes.forEach(node => {
    const nodeWidth = node.width || 420;
    const nodeHeight = getApproxNodeHeight(node);
    
    // 居中对齐
    const nodeX = startX + (maxWidth - nodeWidth) / 2;
    
    onUpdateNode(node.id, { x: nodeX, y: currentY });
    currentY += nodeHeight + verticalGap;
  });
}, [/* ... */]);
```

**布局参数：**
- 垂直间距：80px
- 组边距：40px

---

**2.3 保留旧版本（兼容性）**
```typescript
// 保留 arrangeTopology 函数（兼容性）
const arrangeTopology = useCallback((groupId: string) => {
  // ... 原有的拓扑排序逻辑 ...
}, [/* ... */]);
```

---

**2.4 导出新函数**
```typescript
return {
  // ... 其他函数 ...
  
  // 排列功能
  arrangeGrid, // 🔥 新增：宫格排列
  arrangeVertical, // 🔥 新增：竖排排列
  arrangeTopology, // 保留旧版本（兼容性）
  
  // ... 其他函数 ...
};
```

---

### 3. ✅ App.tsx 集成

#### 修改文件
`App.tsx`

#### 主要改动

**3.1 从 useGroup Hook 解构新函数**
```typescript
const {
  // ... 其他函数 ...
  arrangeGrid, // 🔥 新增：宫格排列
  arrangeVertical, // 🔥 新增：竖排排列
  arrangeTopology, // 保留旧版本（兼容性）
  // ... 其他函数 ...
} = useGroup({/* ... */});
```

**3.2 更新 GroupToolbar 调用**
```typescript
<GroupToolbar
  groupId={selectedGroupId}
  groupX={groups.find(g => g.id === selectedGroupId)!.x}
  groupY={groups.find(g => g.id === selectedGroupId)!.y}
  groupWidth={groups.find(g => g.id === selectedGroupId)!.width}
  scale={scale}
  panX={pan.x}
  panY={pan.y}
  onArrangeGrid={arrangeGrid} // 🔥 宫格排列
  onArrangeVertical={arrangeVertical} // 🔥 竖排排列
  isTemporary={groups.find(g => g.id === selectedGroupId)!.title === '临时分组'}
  onMakePermanent={handleMakePermanentGroup}
  onAddToAssetLibrary={handleAddGroupToAssetLibrary}
/>
```

---

## UI 效果

### 工具栏位置
```
之前：
┌─────────────────┐
│                 │ ← 组框
│  [打组] [排列]  │ ← 工具栏（-32px，稍微挡住组）
└─────────────────┘

现在：
┌─────────────────┐
│  [打组] [排列]  │ ← 工具栏（-48px，不遮挡组）
│                 │
│                 │ ← 组框
└─────────────────┘
```

### 排列按钮下拉菜单
```
┌─────────────────────────┐
│  [打组] [资产库] [排列▼]│ ← 工具栏
└─────────────────────────┘
                    │
                    ▼
            ┌──────────────┐
            │ 宫格排列     │
            │ 竖排排列     │
            └──────────────┘
```

---

## 排列效果对比

### 宫格排列（每排4个）
```
有连接关系的节点：
┌─────┐
│  A  │ ← 第1层（入度为0）
└─────┘
   │
   ▼
┌─────┐  ┌─────┐  ┌─────┐
│  B  │  │  C  │  │  D  │ ← 第2层
└─────┘  └─────┘  └─────┘
   │        │        │
   ▼        ▼        ▼
┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐
│  E  │  │  F  │  │  G  │  │  H  │ ← 第3层（每排4个）
└─────┘  └─────┘  └─────┘  └─────┘

无连接的节点：
┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐
│  X  │  │  Y  │  │  Z  │  │  W  │ ← 按原本顺序
└─────┘  └─────┘  └─────┘  └─────┘
```

### 竖排排列（一列）
```
┌─────┐
│  A  │
└─────┘
   ↓
┌─────┐
│  B  │
└─────┘
   ↓
┌─────┐
│  C  │
└─────┘
   ↓
┌─────┐
│  D  │
└─────┘
```

---

## 技术亮点

### 1. 智能排列
- **有连接关系**：按拓扑排序（层级关系）
- **无连接关系**：按原本顺序
- **混合场景**：先排有连接的，再排无连接的

### 2. 动态宽度
- 使用实际节点宽度（`node.width`）
- 不再假设固定420px
- 避免节点重叠

### 3. 居中对齐
- 每行基于实际宽度居中
- 竖排基于最大宽度居中
- 视觉效果更美观

### 4. 自动调整组边界
- 根据实际节点位置计算
- 自动扩展或收缩组框
- 保持合适的边距（40px）

---

## 文件清单

### 修改文件
- `components/GroupToolbar.tsx` - UI 优化（工具栏位置 + 下拉菜单）
- `hooks/useGroup.ts` - 新增排列算法（宫格 + 竖排）
- `App.tsx` - 集成新函数

### 编译验证
- ✅ `components/GroupToolbar.tsx` - 无错误
- ✅ `hooks/useGroup.ts` - 无错误
- ✅ `App.tsx` - 无错误

---

## 测试建议

### 功能测试
- [ ] 工具栏位置不遮挡组框
- [ ] 点击"排列"按钮显示下拉菜单
- [ ] 宫格排列：每排4个，按层级关系
- [ ] 竖排排列：从上到下一列
- [ ] 无连接的节点按原本顺序排列
- [ ] 节点宽度不同时不重叠
- [ ] 组边界自动调整

### 边界测试
- [ ] 1个节点（宫格：1行1个，竖排：1列1个）
- [ ] 4个节点（宫格：1行4个，竖排：1列4个）
- [ ] 5个节点（宫格：2行，竖排：1列5个）
- [ ] 大量节点（宫格：多行，竖排：长列）
- [ ] 节点宽度不同（动态计算）
- [ ] 有连接 + 无连接混合

### 交互测试
- [ ] 点击空白处关闭下拉菜单
- [ ] 选择排列方式后菜单自动关闭
- [ ] 拖动组时工具栏跟随
- [ ] 缩放画布时工具栏位置正确
- [ ] 取消选中时工具栏消失

---

## 用户体验改进

### 1. 工具栏位置
- ❌ 之前：稍微挡住组框，影响视觉
- ✅ 现在：完全不遮挡，视野清晰

### 2. 排列选择
- ❌ 之前：只有一个"自动排列"，不知道效果
- ✅ 现在：两种选择，清晰明确

### 3. 宫格排列
- ✅ 智能：有连接的按层级，无连接的按顺序
- ✅ 直观：每排4个，易于预测
- ✅ 美观：居中对齐，间距合理

### 4. 竖排排列
- ✅ 简单：从上到下，一目了然
- ✅ 紧凑：适合流程图、时间线
- ✅ 整齐：居中对齐，视觉统一

---

## 更新日志

### 2026-02-08
- ✅ 工具栏位置往上移 16px（-32px → -48px）
- ✅ 排列按钮改为下拉菜单
- ✅ 新增宫格排列算法（每排4个 + 层级关系）
- ✅ 新增竖排排列算法（从上到下一列）
- ✅ 使用实际节点宽度（`node.width`）
- ✅ 自动调整组边界
- ✅ 编译验证通过

---

## 总结

本次优化完成了两个核心改进：

1. **UI 优化**：工具栏位置不再遮挡组框，下拉菜单提供清晰的排列选择
2. **算法优化**：宫格排列智能处理有连接和无连接的节点，竖排排列简单直观

现在组的自动排列功能更加灵活、智能、易用：
- 工具栏位置合理，不遮挡内容
- 两种排列方式，满足不同场景
- 智能算法，自动处理连接关系
- 动态宽度，避免节点重叠
- 自动调整组边界，保持美观

所有功能都经过优化，用户体验更流畅！
