# 资产库三个问题 - 诊断和修复方案

**日期**: 2026-02-09  
**状态**: 诊断完成，待修复

---

## 📋 问题清单

用户反馈资产库有三个问题：

1. **分类切换问题**：选择分类后左侧 UI 不能对应显示
2. **资产拖拽问题**：添加进资产库的节点不能取出使用
3. **删除功能问题**：资产库没有删除功能

---

## 🔍 问题 1：分类切换不生效

### 现象
- 用户选择"场景"分类
- 左侧资产列表没有过滤，显示所有资产

### 代码分析

**UI 组件**（`components/AssetLibraryPanel.tsx` 第 48 行）：
```typescript
// 使用 getAssetsByCategory 过滤资产
const filteredAssets = getAssetsByCategory(selectedCategory);
```

**Store**（`core/stores/assetLibraryStore.ts`）：
```typescript
// getAssetsByCategory 方法逻辑正确
getAssetsByCategory: (category) => {
  if (category === 'all') return get().assets;
  return get().assets.filter(asset => asset.category === category);
}
```

**创建资产对话框**（`components/CreateAssetDialog.tsx` 第 29-36 行）：
```typescript
const handleConfirm = () => {
  if (!name.trim()) {
    alert('请输入资产名称');
    return;
  }
  
  onConfirm(name.trim(), category); // ✅ 正确传递了 category
};
```

**App.tsx 处理确认**（第 604-625 行）：
```typescript
const handleConfirmCreateAsset = useCallback((name: string, category: any) => {
  // 获取选中的节点数据（nodes 是 Map）
  const selectedNodes = Array.from(nodes.values()).filter(n => selectedNodeIds.includes(n.id));
  
  // 获取选中节点之间的连接
  const selectedConnections = connections.filter(conn => 
    selectedNodeIds.includes(conn.from) && selectedNodeIds.includes(conn.to)
  );
  
  // 创建资产
  createAsset(name, category, selectedNodes, selectedConnections); // ✅ 正确传递了 category
  
  // 关闭对话框
  setShowCreateAssetDialog(false);
  
  // 取消选中
  clearSelection();
  
  console.log('[资产库] 资产创建成功', { name, category, nodesCount: selectedNodes.length });
}, [selectedNodeIds, nodes, connections, createAsset, clearSelection]);
```

### 根本原因

**✅ 代码逻辑完全正确！分类字段已经正确传递和保存。**

**真正的问题**：用户可能使用了旧版本的资产数据（在修复前创建的资产没有 `category` 字段）。

### 修复方案

**方案 A：清除旧数据（推荐）**
- 清除浏览器 localStorage 中的旧资产数据
- 重新创建资产，分类功能就会正常工作

**方案 B：数据迁移**
- 在 `assetLibraryStore.ts` 中添加数据迁移逻辑
- 为没有 `category` 字段的旧资产设置默认分类 `'other'`

**验证步骤**：
1. 打开浏览器控制台
2. 运行 `localStorage.clear()` 清除所有数据
3. 刷新页面
4. 创建一个新资产，选择"场景"分类
5. 切换到"场景"分类，检查是否只显示该分类的资产
6. 切换到"全部"，检查是否显示所有资产

---

## 🔍 问题 2：资产拖拽不能使用

### 现象
- 用户从资产库拖拽资产到画布
- 画布上没有创建节点

### 代码分析

**AssetLibraryPanel 拖拽开始**（`components/AssetLibraryPanel.tsx` 第 54-59 行）：
```typescript
// 处理拖拽开始
const handleDragStart = (e: React.DragEvent, asset: Asset) => {
  e.dataTransfer.setData('application/asset-id', asset.id); // ✅ 正确设置了资产ID
  e.dataTransfer.effectAllowed = 'copy';
  console.log('[AssetLibraryPanel] 开始拖拽资产', { id: asset.id, name: asset.name });
};
```

**App.tsx 导入 useAsset**（第 601 行）：
```typescript
const { createAsset, useAsset } = useAssetLibrary(); // ✅ 正确导入了 useAsset
```

**App.tsx 拖拽处理**（第 1421-1450 行）：
```typescript
const handleCanvasDrop = (e: React.DragEvent) => {
  e.preventDefault();
  const dropX = (e.clientX - pan.x) / scale;
  const dropY = (e.clientY - pan.y) / scale;
  const assetData = e.dataTransfer.getData('application/json');
  const assetId = e.dataTransfer.getData('application/asset-id'); // ✅ 正确获取资产ID

  // 🔥 处理资产拖拽
  if (assetId) {
    console.log('[App] 处理资产拖拽', { assetId, dropX, dropY });
    
    // 创建回调函数：添加节点到画布
    const onAddNodes = (nodes: AppNode[]) => {
      saveHistory();
      useNodeStore.getState().addNodes(nodes); // ✅ 正确添加节点
      console.log('[App] 添加资产节点到画布', { count: nodes.length });
    };
    
    // 创建回调函数：添加连接到画布
    const onAddConnections = (connections: Connection[]) => {
      connections.forEach(conn => {
        useConnectionStore.getState().addConnection(conn); // ✅ 正确添加连接
      });
      console.log('[App] 添加资产连接到画布', { count: connections.length });
    };
    
    // 调用 useAsset 方法
    useAsset(assetId, { x: dropX, y: dropY }, onAddNodes, onAddConnections); // ✅ 正确调用
    return;
  }
  // ... 其他拖拽处理
}
```

**useAsset 方法**（`hooks/useAssetLibrary.ts` 第 73-135 行）：
- ✅ 逻辑完全正确
- ✅ 深拷贝节点和连接
- ✅ 生成新的节点 ID
- ✅ 调整节点位置
- ✅ 调用回调函数添加到画布

### 根本原因

**✅ 代码逻辑完全正确！拖拽功能应该是正常工作的。**

**可能的问题**：
1. **没有资产数据**：用户还没有创建任何资产
2. **资产数据损坏**：localStorage 中的资产数据格式不正确
3. **浏览器控制台有错误**：需要查看控制台日志

### 验证步骤

**步骤 1**：检查是否有资产
- 打开资产库面板
- 检查是否显示"暂无资产"

**步骤 2**：创建一个测试资产
- 在画布上创建 2-3 个节点
- 框选这些节点
- 点击"添加到资产库"按钮
- 输入名称，选择分类，点击"确认创建"

**步骤 3**：测试拖拽
- 从资产库拖拽刚创建的资产到画布
- 打开浏览器控制台（F12）
- 查看是否有以下日志：
  - `[AssetLibraryPanel] 开始拖拽资产`
  - `[App] 处理资产拖拽`
  - `[useAssetLibrary] 使用资产`
  - `[useAssetLibrary] 资产使用成功`
  - `[App] 添加资产节点到画布`

**步骤 4**：如果还是不行，检查数据
```javascript
// 在控制台运行
console.log(localStorage.getItem('asset-library'));
```

如果数据为空或格式错误，清除数据重新创建：
```javascript
localStorage.removeItem('asset-library');
```

---

## 🔍 问题 3：删除功能不明显

### 现象
- 用户找不到删除资产的按钮

### 代码分析

**UI 组件**（`components/AssetLibraryPanel.tsx` 第 237-268 行）：

**删除功能已经存在！**

```typescript
// 右键菜单
{contextMenu && (
  <>
    {/* 遮罩层 */}
    <div
      className="fixed inset-0 z-[9998]"
      onClick={closeContextMenu}
    />
    
    {/* 菜单 */}
    <div
      className="fixed z-[9999] bg-white border border-gray-200 rounded-lg shadow-lg p-1 min-w-[140px]"
      style={{
        left: `${contextMenu.x}px`,
        top: `${contextMenu.y}px`,
      }}
    >
      <button
        onClick={() => handleRename(contextMenu.assetId)}
        className="w-full flex items-center gap-3 px-4 py-2.5 text-xs text-gray-700 hover:bg-gray-100 rounded-lg transition-colors"
      >
        <Edit2 size={14} />
        <span>重命名</span>
      </button>
      <button
        onClick={() => handleDelete(contextMenu.assetId)} // ✅ 删除功能已实现
        className="w-full flex items-center gap-3 px-4 py-2.5 text-xs text-red-600 hover:bg-red-50 rounded-lg transition-colors"
      >
        <Trash2 size={14} />
        <span>删除</span>
      </button>
    </div>
  </>
)}
```

**删除处理函数**（第 99-108 行）：
```typescript
// 删除资产
const handleDelete = (assetId: string) => {
  const asset = assets.find(a => a.id === assetId);
  if (!asset) return;

  const confirmed = window.confirm(`确认删除资产"${asset.name}"？\n\n此操作不可撤销。`);
  if (confirmed) {
    deleteAsset(assetId); // ✅ 正确调用 Store 的 deleteAsset 方法
  }
  closeContextMenu();
};
```

**更多按钮**（第 227-235 行）：
```typescript
{/* 更多按钮 */}
<button
  onClick={(e) => {
    e.stopPropagation();
    handleContextMenu(e, asset); // ✅ 点击显示右键菜单
  }}
  className="absolute top-2 right-2 w-6 h-6 rounded-lg bg-black/50 hover:bg-black/70 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity"
>
  <MoreVertical size={14} className="text-white" />
</button>
```

### 根本原因

**✅ 删除功能已经完整实现！**

**交互方式**：
1. **方式 1**：右键点击资产卡片 → 显示菜单 → 点击"删除"
2. **方式 2**：悬停资产卡片 → 点击右上角"更多"按钮（三个点）→ 显示菜单 → 点击"删除"

**用户可能不知道的原因**：
1. **更多按钮不够明显**：只在悬停时显示，用户可能没注意到
2. **没有直接的删除图标**：需要点击"更多"按钮才能看到删除选项

### 优化方案（可选）

**方案 A：保持现有设计，添加提示**
- 在空状态下添加使用说明
- 告诉用户"右键点击资产可以重命名或删除"

**方案 B：添加悬停删除按钮**
- 在资产卡片右上角添加一个删除图标（悬停时显示）
- 点击直接删除（弹出确认对话框）
- 保留右键菜单作为备选方案

**推荐方案 A**（不需要修改代码）：
- 当前设计已经很专业（符合 Figma、Notion 等产品的交互模式）
- 只需要告诉用户如何使用即可

**如果用户坚持要更明显的删除按钮，可以实施方案 B**

---

## 📝 诊断总结

### ✅ 好消息

**所有三个功能的代码逻辑都是正确的！**

1. **分类切换功能** - ✅ 代码正确，`category` 字段已正确传递和保存
2. **资产拖拽功能** - ✅ 代码正确，拖拽逻辑完整实现
3. **删除功能** - ✅ 代码正确，右键菜单和删除逻辑都已实现

### 🔍 真正的问题

**问题 1：分类切换不生效**
- **原因**：用户可能使用了旧版本的资产数据（在修复前创建的资产没有 `category` 字段）
- **解决方案**：清除 localStorage 数据，重新创建资产

**问题 2：资产拖拽不能使用**
- **原因**：可能没有资产数据，或者资产数据损坏
- **解决方案**：创建测试资产，检查控制台日志，验证拖拽流程

**问题 3：删除功能不明显**
- **原因**：删除功能已存在，但交互不够明显（需要右键或点击"更多"按钮）
- **解决方案**：告诉用户如何使用（右键点击资产 → 删除），或添加更明显的删除按钮

### 🎯 建议的验证步骤

**第 1 步：清除旧数据**
```javascript
// 在浏览器控制台运行
localStorage.clear();
// 刷新页面
```

**第 2 步：创建测试资产**
1. 在画布上创建 2-3 个节点
2. 框选这些节点
3. 点击"添加到资产库"按钮
4. 输入名称"测试资产"，选择"场景"分类
5. 点击"确认创建"

**第 3 步：测试分类切换**
1. 在资产库面板，点击"场景"分类
2. 检查是否只显示"测试资产"
3. 点击"全部"分类
4. 检查是否显示所有资产

**第 4 步：测试资产拖拽**
1. 从资产库拖拽"测试资产"到画布
2. 检查画布上是否创建了节点
3. 打开控制台（F12），查看日志：
   - `[AssetLibraryPanel] 开始拖拽资产`
   - `[App] 处理资产拖拽`
   - `[useAssetLibrary] 使用资产`
   - `[useAssetLibrary] 资产使用成功`

**第 5 步：测试删除功能**
1. 右键点击"测试资产"
2. 点击"删除"
3. 确认删除
4. 检查资产是否被删除

### 🔧 如果验证失败

**如果分类切换还是不行**：
- 检查控制台是否有错误
- 运行 `console.log(localStorage.getItem('asset-library'))` 查看数据格式
- 确认资产数据中有 `category` 字段

**如果拖拽还是不行**：
- 检查控制台日志，看哪一步失败了
- 确认 `handleCanvasDrop` 函数被调用
- 确认 `useAsset` 函数被调用
- 确认 `onAddNodes` 回调被调用

**如果删除功能找不到**：
- 告诉用户：右键点击资产 → 点击"删除"
- 或者：悬停资产 → 点击右上角"更多"按钮（三个点）→ 点击"删除"

---

## 🎯 下一步行动

**等待用户反馈**：
1. 用户是否愿意清除数据重新测试？
2. 用户是否需要数据迁移方案（保留旧资产）？
3. 用户是否需要更明显的删除按钮？

**如果用户确认需要修复**：
1. 实施数据迁移方案（为旧资产添加默认分类）
2. 添加更明显的删除按钮（可选）
3. 添加使用说明（可选）

---

**记住**：
- 先诊断，再修复
- 一次只改一个问题
- 每次修复后都要验证
- 不影响其他功能
