# 滚轮交互 Bug 根本原因修复 - 2026-01-28

## 问题描述

用户反馈：
> "感觉就是不结束，我已经松开按键了，他还是在调整画布状态"

## 真正的问题根源

**不是滚轮本身的问题，而是 Shift + 滚轮和 Shift + 左键拖动的交互冲突！**

### 问题场景

1. 用户按住 Shift，滚动滚轮（想要平移画布）
2. 在滚动过程中，不小心触碰到鼠标左键
3. 触发了 `handleCanvasMouseDown` 中的逻辑：
   ```typescript
   if (e.button === 1 || (e.button === 0 && e.shiftKey)) { 
       // 中键 或 Shift+左键：开始拖拽画布
       startCanvasDrag(e);
   }
   ```
4. `isDraggingCanvas` 状态变为 `true`
5. 用户松开 Shift 和鼠标左键
6. 但是 `isDraggingCanvas` 状态没有被清除（因为没有触发 `mouseup` 事件）
7. 画布一直处于"拖动"状态，即使用户已经松开了所有按键

### 为什么会这样？

**Shift + 左键拖动画布** 和 **Shift + 滚轮平移画布** 是两个不同的交互：
- Shift + 滚轮：触发 `wheel` 事件，直接更新 `pan` 状态
- Shift + 左键：触发 `mousedown` 事件，设置 `isDraggingCanvas = true`，然后在 `mousemove` 中更新 `pan`

当用户在滚动滚轮时不小心按下左键，就会进入"拖动"模式，导致状态混乱。

---

## 根本解决方案

**移除 Shift + 左键拖动画布的功能，只保留中键拖动。**

### 理由

1. **避免交互冲突**：Shift + 滚轮和 Shift + 左键不会再冲突
2. **符合习惯**：主流设计工具（Figma、Sketch）都是用中键拖动画布
3. **功能不减少**：
   - 中键拖动：平移画布 ✅
   - Shift + 滚轮：平移画布 ✅
   - 普通滚轮：缩放画布 ✅
4. **Shift + 左键应该用于框选**：保留给多选功能

### 修改前

```typescript
if (e.button === 1 || (e.button === 0 && e.shiftKey)) { 
    // 中键 或 Shift+左键：开始拖拽画布
    startCanvasDrag(e);
}
```

### 修改后

```typescript
if (e.button === 1) { 
    // 中键：开始拖拽画布
    startCanvasDrag(e);
}
```

---

## 修改的文件

1. `App.tsx`
   - 修改 `handleCanvasMouseDown`：移除 `(e.button === 0 && e.shiftKey)` 判断
   - 移除之前添加的"治标不治本"的键盘监听器

2. `hooks/useViewport.ts`
   - 移除之前添加的 `wheelEndTimerRef`
   - 移除之前添加的 `forceEndCanvasDrag` 方法
   - 简化 `handleWheel` 方法

---

## 交互说明

### 修复后的交互

| 操作 | 功能 | 说明 |
|------|------|------|
| 滚轮 | 缩放画布 | 以鼠标位置为中心缩放 |
| Shift + 滚轮 | 平移画布 | 上下左右平移 |
| 中键拖动 | 平移画布 | 按住中键拖动 |
| 左键点击 | 框选 | 清空选择，开始框选 |
| Shift + 左键 | ~~拖动画布~~ | **已移除，避免冲突** |

### 为什么移除 Shift + 左键拖动？

1. **与 Shift + 滚轮冲突**：用户在滚动时容易误触左键
2. **不符合习惯**：主流设计工具都是用中键拖动
3. **功能重复**：已经有中键拖动和 Shift + 滚轮了
4. **Shift + 左键应该用于多选**：保留给框选功能

---

## 测试验证

### 测试用例 1：Shift + 滚轮平移
- [ ] 按住 Shift，滚动滚轮
- [ ] 画布应该平移
- [ ] 松开 Shift，画布应该停止平移
- [ ] **不应该出现"一直在调整"的状态**

### 测试用例 2：中键拖动
- [ ] 按住中键，拖动鼠标
- [ ] 画布应该跟随鼠标移动
- [ ] 松开中键，画布应该停止移动
- [ ] 鼠标移出窗口，画布应该停止移动

### 测试用例 3：普通滚轮缩放
- [ ] 滚动滚轮
- [ ] 画布应该缩放
- [ ] 以鼠标位置为中心缩放

### 测试用例 4：Shift + 滚轮 + 误触左键
- [ ] 按住 Shift，滚动滚轮
- [ ] 在滚动过程中，按下左键
- [ ] **画布不应该进入"拖动"状态**
- [ ] 松开 Shift 和左键，画布应该正常

---

## 对比主流设计工具

| 工具 | 滚轮 | Shift + 滚轮 | 中键拖动 | Shift + 左键 |
|------|------|-------------|---------|-------------|
| **Figma** | 缩放 | 平移 | 平移 | 多选 |
| **Sketch** | 缩放 | 平移 | 平移 | 多选 |
| **Adobe XD** | 缩放 | 平移 | 平移 | 多选 |
| **我们的工具（修复后）** | 缩放 | 平移 | 平移 | ~~拖动~~ → 框选 |

✅ 完全符合主流设计工具的交互习惯！

---

## 总结

### 问题根源
- Shift + 滚轮和 Shift + 左键拖动的交互冲突
- 用户在滚动时误触左键，导致进入"拖动"状态
- 松开按键后，状态没有被清除

### 解决方案
- 移除 Shift + 左键拖动画布的功能
- 只保留中键拖动和 Shift + 滚轮平移
- 避免交互冲突，符合主流设计工具习惯

### 修改文件
- `App.tsx`：修改 `handleCanvasMouseDown`
- `hooks/useViewport.ts`：简化代码，移除"治标不治本"的修复

---

## 反思

之前的修复方案（添加键盘监听器、定时器等）都是"治标不治本"，没有找到真正的问题根源。

真正的问题是：**交互设计不合理，导致状态冲突**。

解决方案应该是：**简化交互，移除冲突的功能**，而不是添加更多的补丁代码。

**记住：好的设计应该是简单的，不需要复杂的补丁来修复。**
