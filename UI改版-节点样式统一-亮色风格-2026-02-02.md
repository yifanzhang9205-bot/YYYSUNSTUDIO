# UI 改版 - 节点样式统一为亮色风格 - 2026-02-02

## 修改目标

将所有节点组件的深色背景统一替换为亮色风格，确保整体 UI 风格一致。

## 修改范围

### 已修改的文件（13 个）

1. ✅ `components/VideoNodeModules.tsx`
2. ✅ `components/StoryboardShotNode.tsx`
3. ✅ `components/SonicStudio.tsx`
4. ✅ `components/SmartSequenceDock.tsx`
5. ✅ `components/SketchEditor.tsx`
6. ✅ `components/MultiAngleCameraNode.tsx`
7. ✅ `components/GridSplitterNode.tsx`
8. ✅ `components/ScriptNode.tsx`
9. ✅ `components/SceneReferenceNode.tsx`
10. ✅ `components/CharacterReferenceNode.tsx`
11. ✅ `components/ImageCropper.tsx`
12. ✅ `components/ChatWindow.tsx`
13. ✅ `components/AssistantPanel.tsx`

### 无需修改的文件（1 个）

- `components/StoryStudioNode.tsx` - 已经是亮色风格

## 替换规则

### 1. 自定义深色背景颜色

| 原始颜色 | 替换为 |
|---------|--------|
| `bg-[#0a0a0c]` | `bg-gray-100` |
| `bg-[#1c1c1e]` | `bg-gray-100` |
| `bg-[#2c2c2e]` | `bg-gray-200` |
| `bg-[#121214]` | `bg-gray-100` |
| `bg-[#0a0a0a]` | `bg-gray-100` |
| `bg-[#1a1a1c]` | `bg-gray-100` |

### 2. Tailwind 深色背景

| 原始类名 | 替换为 |
|---------|--------|
| `bg-zinc-900` | `bg-gray-100` |
| `bg-gray-900` | `bg-gray-100` |

### 3. 半透明深色背景

| 原始类名 | 替换为 |
|---------|--------|
| `bg-gray-900/90` | `bg-gray-100/90` |
| `bg-gray-900/80` | `bg-gray-100/80` |
| `bg-gray-900/60` | `bg-gray-100/60` |
| `bg-gray-900/50` | `bg-gray-100/50` |
| `bg-gray-900/40` | `bg-gray-100/40` |
| `bg-gray-900/30` | `bg-gray-100/30` |
| `bg-gray-900/20` | `bg-gray-100/20` |
| `bg-gray-900/10` | `bg-gray-100/10` |

## 实施方法

使用 Python 脚本 `batch_replace_dark_bg.py` 批量替换所有文件。

```python
# 核心逻辑
replacements = [
    (r'bg-\[#0a0a0c\]', 'bg-gray-100'),
    (r'bg-\[#1c1c1e\]', 'bg-gray-100'),
    (r'bg-\[#2c2c2e\]', 'bg-gray-200'),
    # ... 更多规则
]

for file_path in files:
    content = read_file(file_path)
    for pattern, replacement in replacements:
        content = re.sub(pattern, replacement, content)
    write_file(file_path, content)
```

## 下一步工作

### 1. 文字颜色对比度调整（待用户反馈）

用户正在整理对比度和可读性问题清单。需要根据用户反馈调整：

- **浅色背景上的文字**：使用 `text-gray-900`、`text-gray-800` 确保对比度
- **深色背景上的文字**：保持 `text-white`、`text-gray-100`
- **确保符合 WCAG AA 标准**：对比度 4.5:1

### 2. 边框和阴影调整

- **浅色背景**：使用 `border-gray-200`、`border-gray-300`
- **深色背景**：使用 `border-white/10`、`border-white/20`

### 3. 测试和验证

- [ ] 启动开发服务器测试
- [ ] 检查所有节点的可读性
- [ ] 确认交互状态（hover、active、disabled）的对比度
- [ ] 验证不同缩放级别下的显示效果

## 注意事项

1. **保留了半透明效果**：`bg-gray-100/XX` 保持了原有的透明度
2. **未修改纯黑色**：`bg-black` 保持不变（通常用于特殊场景）
3. **未修改文字颜色**：等待用户反馈后统一调整

## 执行结果

```
✓ 已修改: components/VideoNodeModules.tsx
✓ 已修改: components/StoryboardShotNode.tsx
✓ 已修改: components/SonicStudio.tsx
✓ 已修改: components/SmartSequenceDock.tsx
✓ 已修改: components/SketchEditor.tsx
✓ 已修改: components/MultiAngleCameraNode.tsx
✓ 已修改: components/GridSplitterNode.tsx
✓ 已修改: components/ScriptNode.tsx
✓ 已修改: components/SceneReferenceNode.tsx
✓ 已修改: components/CharacterReferenceNode.tsx
✓ 已修改: components/ImageCropper.tsx
✓ 已修改: components/ChatWindow.tsx
✓ 已修改: components/AssistantPanel.tsx

总计修改了 13 个文件
```

## 状态

- ✅ **背景颜色统一完成**
- ⏳ **等待用户反馈对比度问题**
- ⏳ **准备调整文字颜色和边框**
