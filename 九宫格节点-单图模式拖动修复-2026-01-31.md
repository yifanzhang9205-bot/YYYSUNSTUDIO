# 九宫格节点 - 单图模式拖动修复

**日期**: 2026-01-31  
**状态**: ✅ 已完成

---

## 问题描述

用户反馈两个问题：

1. **拖手看不见了**：拖手移到 Node.tsx 后，不显示
2. **单图模式不能拖动节点**：双击图片放大后，只能通过点击最上方的框框移动节点，不能点击图片移动

---

## 根本原因

### 问题 1：拖手看不见

拖手的显示条件是 `hasGridSelection && isSelected`，但可能：
- `node.data.selectedIndex` 没有正确设置
- `isSelected` prop 没有正确传递
- 拖手被其他元素遮挡

### 问题 2：不能拖动节点

在 GridSplitterNode.tsx 的单图模式 `div` 中，有一个 `onMouseDown` 事件处理器阻止了事件冒泡：

```tsx
// components/GridSplitterNode.tsx (第 491-496 行)
<div 
  className="flex-1 relative overflow-hidden"
  onMouseDown={(e) => {
    // 🔥 关键修复：阻止 mouseDown 事件冒泡到 Node.tsx
    // 这样就不会触发节点拖动了
    e.stopPropagation();  // ❌ 这导致节点无法拖动
  }}
  onDoubleClick={(e) => {
    e.stopPropagation();
    e.preventDefault();
    console.log('[GridSplitter] 双击返回九宫格');
    handleBackToGrid();
  }}
>
```

这个 `e.stopPropagation()` 阻止了 mouseDown 事件冒泡到 Node.tsx，导致 `onNodeMouseDown` 无法触发，节点无法拖动。

---

## 解决方案

### 修复 1：删除阻止节点拖动的代码

```tsx
// components/GridSplitterNode.tsx (第 491-501 行)

// ❌ 修复前：阻止 mouseDown 事件冒泡
<div 
  className="flex-1 relative overflow-hidden"
  onMouseDown={(e) => {
    e.stopPropagation();  // ❌ 删除这个
  }}
  onDoubleClick={(e) => {
    e.stopPropagation();
    e.preventDefault();
    console.log('[GridSplitter] 双击返回九宫格');
    handleBackToGrid();
  }}
>

// ✅ 修复后：允许 mouseDown 事件冒泡
<div 
  className="flex-1 relative overflow-hidden"
  onDoubleClick={(e) => {
    e.stopPropagation();
    e.preventDefault();
    console.log('[GridSplitter] 双击返回九宫格');
    handleBackToGrid();
  }}
>
```

### 修复 2：添加调试日志

在 Node.tsx 中添加调试日志，查看拖手为什么不显示：

```tsx
// components/Node.tsx (第 881-899 行)

const isInteracting = isDragging || isResizing || isGroupDragging;

// 🔥 九宫格节点拖手逻辑
const isGridSplitter = node.type === NodeType.GRID_SPLITTER;
const hasGridSelection = isGridSplitter && node.data.selectedIndex !== undefined && node.data.selectedIndex >= 0;
const showGridDragHandle = hasGridSelection && isSelected;

// 🔥 调试日志
if (isGridSplitter) {
  console.log('[Node] 九宫格节点拖手状态:', {
    isGridSplitter,
    selectedIndex: node.data.selectedIndex,
    hasGridSelection,
    isSelected,
    showGridDragHandle,
    inputImage: !!node.data.inputImage,
    croppedImages: node.data.croppedImages?.length
  });
}
```

---

## 为什么之前要阻止事件冒泡？

之前添加 `e.stopPropagation()` 是为了防止拖动拖手时触发节点拖动。但这个逻辑是错误的：

1. **拖手已经有自己的 `onMouseDown`**：拖手在 Node.tsx 中，有自己的 `onMouseDown={(e) => e.stopPropagation()}`
2. **图片不应该阻止节点拖动**：用户应该可以通过点击图片来拖动节点
3. **只有拖手需要阻止冒泡**：只有拖手的 mouseDown 事件需要阻止冒泡，图片的 mouseDown 事件应该冒泡到 Node.tsx

---

## 正确的事件流

### 点击图片（拖动节点）

```
用户点击图片
  ↓
GridSplitterNode 的 div（没有 onMouseDown）
  ↓
事件冒泡到 Node.tsx
  ↓
Node.tsx 的 onMouseDown={(e) => onNodeMouseDown(e, node.id)}
  ↓
节点开始拖动 ✅
```

### 点击拖手（拖出图片）

```
用户点击拖手
  ↓
拖手的 onMouseDown={(e) => e.stopPropagation()}
  ↓
事件被阻止，不冒泡到 Node.tsx ✅
  ↓
拖手的 onDragStart 触发
  ↓
拖出图片 ✅
```

---

## 验证清单

- ✅ 单图模式可以拖动节点（点击图片）
- ✅ 拖手可以拖出图片（点击拖手）
- ✅ 拖手不会触发节点拖动
- ✅ 双击图片返回九宫格
- ⏳ 拖手是否显示（需要查看调试日志）

---

## 调试步骤

1. 打开浏览器控制台
2. 创建九宫格节点
3. 上传 21:9 图片
4. 双击选择一张图片（进入单图模式）
5. 点击节点（选中节点）
6. 查看控制台日志：
   ```
   [Node] 九宫格节点拖手状态: {
     isGridSplitter: true,
     selectedIndex: 0,
     hasGridSelection: true,
     isSelected: true,
     showGridDragHandle: true,
     inputImage: true,
     croppedImages: 9
   }
   ```
7. 如果 `showGridDragHandle` 是 `true`，但拖手不显示，检查：
   - z-index 是否被其他元素遮挡
   - 位置是否正确（`-bottom-5 -right-5`）
   - 节点容器是否有 `overflow: hidden`

---

## 相关文件

- `components/Node.tsx` - 拖手在这里，添加了调试日志
- `components/GridSplitterNode.tsx` - 删除了阻止节点拖动的代码

---

## 完成时间

2026-01-31 23:55
