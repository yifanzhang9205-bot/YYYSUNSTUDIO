# 3D 相机提示词优化 - 完整记录

## 项目概述

**功能：** 多角度相机节点 - 基于输入图片生成 9 宫格角度变化图
**模型：** NanoBanana Pro（通过西瓜皮 API）
**核心需求：** 用户在 3D UI 中设置角度、距离、高度，生成对应的 9 张角度变化图

---

## 优化历程时间线

### 阶段 1：角度发散方案（2026-01-24）

**问题：**
- Panel 5（九宫格中心）永远是 "front view"
- 完全忽略用户在 UI 中设置的 horizontalAngle
- 用户设置 45°，生成的还是正面

**解决方案：角度发散方案**
- 固定距离和高度（严格按 UI 设置）
- 让角度维度发散（围绕 UI 设置的角度展开 9 个细分）
- 创建 16 个主方向的角度分段系统
- 每个方向有 9 个细分描述，Panel 5 是基准

**实施内容：**
1. 创建 `getAngleSegment()` 函数 - 16 个主方向，每个覆盖 22.5°
2. 改进高度映射从 3 档到 6 档
3. 修改九宫格生成逻辑 - 只有角度发散，距离和高度锁定
4. 提示词强调距离和高度 LOCKED

**文档：**
- `3D相机提示词-角度发散方案实施.md`
- `.kiro/specs/camera-base-angle-fix/`

**代码位置：**
- `App.tsx` 第 1350-1500 行

---

### 阶段 2：UI 改进 - 距离控制和显示（2026-01-24）

**问题：**
- 滚轮控制距离范围错误（0-14，应该是 0-10）
- 左上角胶囊只显示方位和俯仰，缺少距离信息

**解决方案：**
- 统一所有距离控制范围为 0-10
- 在顶部胶囊添加距离显示：`[正面 · 平视 · 中景]`

**文档：**
- `3D相机UI改进-距离控制和显示.md`

**代码位置：**
- `components/MultiAngleCameraNode.tsx`

---

### 阶段 3：距离公式修复 - 实时更新（2026-01-24）

**问题：**
- 距离公式 `dist = 2.5 + cameraZoom * 0.5` 导致最近只能到 2.5
- 有变化检查逻辑（< 0.01 不更新），导致 UI 响应延迟

**解决方案：**
- 调整距离公式为 `dist = 0.5 + cameraZoom * 0.5`
- 移除变化检查逻辑，确保实时更新

**新的距离映射：**
- cameraZoom 0 → dist 0.5（极特写）
- cameraZoom 10 → dist 5.5（大远景）

**文档：**
- `3D相机距离公式修复-实时更新.md`

**代码位置：**
- `components/MultiAngleCameraNode.tsx`

---

### 阶段 4：高度约束强化（2026-01-24）

**问题：**
- 测试结果：特写 + 右上方成功，但大远景 + 正上方失败
- 当垂直角度很大（50-60°）时，AI 忽略高度信息，生成正面视角

**解决方案：强化高度约束**
1. 添加 `getHeightDescription()` 函数 - 详细描述高度
2. 在提示词的 5 个位置强调高度锁定
3. 使用 `CRITICAL`, `FIXED`, `LOCKED` 等强调词

**详细高度描述示例：**
```
改进前：Top-Down View
改进后：Camera positioned directly above, looking straight down at the character from a bird's eye view
```

**文档：**
- `3D相机提示词-高度约束强化.md`

**代码位置：**
- `App.tsx` 第 1710-1780 行

**状态：** 待测试验证

---

### 阶段 5：智能发散系统（2026-01-24）

**问题：**
- 大远景时 AI 基本给原图（因为环境约束太强）
- 输入图片是特写/中景，要求大远景，但没有环境信息
- `Lighting & Environment: LOCKED` 导致 AI 不敢添加环境

**解决方案：智能发散系统**

**核心思路：**
- 自动识别需要发散的 4 种情况
- 给 AI 明确的发散权限（✅ ALLOWED）
- 同时保持严格约束（❌ LOCKED）
- **关键：在整个图片的画风之下发散，不能改变画风和现有内容**

**四种发散情况：**

| 情况 | 触发条件 | 发散权限 | 严格约束 |
|------|---------|---------|---------|
| 1. 距离变远 | zoom >= 7 | 添加环境、缩小角色 | 角色、画风、光照一致 |
| 2. 距离变近 | zoom <= 2 | 裁切聚焦、增强细节 | 角色特征、服装一致 |
| 3. 极端俯视/仰视 | vAngle >= 50 或 <= -20 | 推测头顶/脚底 | 角色身份、服装一致 |
| 4. 背面/侧面 | hAngle 78.75°-281.25° | 推测背面/侧面 | 服装颜色/图案一致 |

**实施内容：**
1. 智能判断逻辑 - 4 个布尔变量判断是否需要发散
2. `getDivergenceGuidance()` 函数 - 动态生成发散指导
3. 修改约束：`Lighting & Environment: LOCKED` → `Lighting Mood & Art Style: LOCKED`
4. 动态插入发散指导到提示词

**文档：**
- `3D相机智能发散系统.md`（详细设计）
- `3D相机智能发散系统-实施完成.md`（实施总结）

**代码位置：**
- `App.tsx` 第 1710-1850 行

**状态：** 待测试验证

---

### 阶段 6：发散指导全局应用修复（2026-01-24）

**问题：**
- 只有 Panel 1 应用了发散指导
- Panel 2-9 基本是原图

**原因：**
- 发散指导和九宫格矩阵之间没有明确连接
- AI 不知道发散指导适用于所有 Panel

**解决方案：在 3 个位置强调"适用于所有 Panel"**

1. **CAMERA ANGLE VARIATION MATRIX 之前：**
   ```
   IMPORTANT: The divergence guidelines above apply to ALL 9 panels equally.
   ```

2. **SPATIAL LOGIC 部分：**
   ```
   - CRITICAL: All divergence permissions apply to ALL 9 panels
   ```

3. **RULES 和 NEGATIVE CONSTRAINTS 部分：**
   ```
   ✅ Apply all divergence guidelines to ALL 9 panels equally
   ❌ Do NOT apply divergence guidelines to only one panel
   ```

**文档：**
- `3D相机发散指导全局应用修复.md`

**代码位置：**
- `App.tsx` 第 1800-1900 行

**状态：** 待测试验证

---

## 当前提示词结构

### 完整结构概览

```
[ROLE & TASK]
- 角色定义和任务说明

[CRITICAL CONSTRAINT: IMG2IMG FEATURE LOCKING]
- Character Identity: LOCKED
- Outfit & Accessories: LOCKED
- Lighting Mood & Art Style: LOCKED  ← 改进：允许添加环境，但风格锁定
- Camera Distance: LOCKED at ${baseDistance}
- Camera Vertical Position: LOCKED - ${heightDescription}
- The ONLY variable: camera horizontal angle variations

${divergenceGuidance}  ← 动态插入：4 种发散情况的指导

[OUTPUT FORMAT]
- 21:9 单张图片，包含 3×3 网格

[BASE ANCHOR POINT - Panel 5]
- Distance: ${baseDistance} - LOCKED for all panels
- Camera Vertical Position: ${baseHeight} - ${heightDescription}
- Camera Horizontal Angle: ${baseAngleDesc} - BASE REFERENCE
- CRITICAL: 高度固定，不能改变

[CAMERA ANGLE VARIATION MATRIX]
${divergenceGuidance ? "IMPORTANT: 发散指导适用于所有 9 个 Panel" : ""}  ← 新增
- Panel 1-9 的具体描述

[SPATIAL LOGIC]
- Distance: LOCKED
- Vertical Position: LOCKED
- Horizontal Angle: Subtle variations
${divergenceGuidance ? "- CRITICAL: 所有发散权限适用于所有 Panel" : ""}  ← 新增

[RULES]
✅ 所有规则
${divergenceGuidance ? "✅ 应用发散指导到所有 Panel" : ""}  ← 新增

[NEGATIVE CONSTRAINTS]
❌ 所有禁止项
❌ 不要只对一个 Panel 应用发散指导  ← 新增
```

### 关键改进点

1. **角度发散方案**
   - Panel 5 反映用户选择的角度
   - 周围 8 张围绕基准角度发散
   - 16 个主方向 × 9 个细分 = 144 种角度描述

2. **高度约束强化**
   - 详细的高度描述（不只是术语）
   - 在 5 个位置强调高度锁定
   - 使用 CRITICAL, FIXED, LOCKED 强调词

3. **智能发散系统**
   - 自动识别 4 种需要发散的情况
   - 动态生成针对性的发散指导
   - 明确的发散权限和约束边界

4. **全局应用修复**
   - 在 3 个位置强调"适用于所有 Panel"
   - 明确禁止"只对一个 Panel 应用"

---

## 代码结构

### 核心函数

```typescript
// 1. 距离维度
const getDistanceTerm = (z: number): string => { ... }

// 2. 高度维度
const getHeightTerm = (angle: number): string => { ... }
const getHeightDescription = (angle: number): string => { ... }  // 详细描述

// 3. 角度维度
const getAngleSegment = (angle: number): AngleSegment => { ... }  // 16 个方向

// 4. 智能发散系统
const needEnvironmentExpansion = zoom >= 7;
const needCloseUpFocus = zoom <= 2;
const needVerticalInference = vAngle >= 50 || vAngle <= -20;
const needAngleInference = (normalized >= 78.75 && normalized <= 281.25);

const getDivergenceGuidance = (): string => { ... }

// 5. 九宫格矩阵生成
const generateCameraMatrix = () => { ... }

// 6. 提示词生成
let geminiPrompt = `...`;
```

### 文件位置

- **提示词生成逻辑：** `App.tsx` 第 1350-1900 行
- **3D UI 组件：** `components/MultiAngleCameraNode.tsx`
- **API 服务：** `services/xiguapiService.ts`

---

## 测试场景

### 优先级 1：基础功能测试

#### 场景 1：中景正面平视（基准测试）
- horizontalAngle: 0°
- verticalAngle: 0°
- cameraZoom: 5

**预期：**
- Panel 5 是正面平视中景
- 周围 8 张是正面的细微角度变化
- 不触发任何发散

#### 场景 2：右前方平视中景（角度发散测试）
- horizontalAngle: 45°
- verticalAngle: 0°
- cameraZoom: 5

**预期：**
- Panel 5 是右前方（three-quarter view from front-right）
- 周围 8 张围绕右前方发散
- 不触发任何发散

### 优先级 2：发散功能测试

#### 场景 3：大远景正面平视（环境扩展）⭐
- horizontalAngle: 0°
- verticalAngle: 0°
- cameraZoom: 10

**预期：**
- 所有 9 个 Panel 都有环境扩展
- 角色变小，周围有合适的环境
- 环境风格与输入图片一致
- 只有水平角度略有不同

**验收标准：**
- ✅ 不再是"原图不变"
- ✅ 所有 Panel 都有环境（不只是 Panel 1）
- ✅ 画风和光照一致

#### 场景 4：极特写正面平视（裁切聚焦）
- horizontalAngle: 0°
- verticalAngle: 0°
- cameraZoom: 0

**预期：**
- 所有 Panel 裁切到脸部/上半身
- 面部细节增强

#### 场景 5：大远景正上方俯视（环境扩展 + 垂直推测）⭐⭐
- horizontalAngle: 0°
- verticalAngle: 60°
- cameraZoom: 10

**预期：**
- 所有 9 个 Panel 都是俯视角度
- 所有 Panel 都有环境扩展
- 能看到头顶
- 只有水平角度略有不同

**验收标准：**
- ✅ 俯视角度正确（不是正面）
- ✅ 有环境扩展
- ✅ 所有 Panel 都应用了发散指导

#### 场景 6：中景背面平视（水平角度推测）
- horizontalAngle: 180°
- verticalAngle: 0°
- cameraZoom: 5

**预期：**
- 所有 Panel 显示背面或接近背面
- 服装风格一致

---

## 已知问题和待优化

### 待测试验证的功能

1. **高度约束强化**
   - 极端俯视（50-60°）是否能正确生成
   - 是否还会出现"要求俯视，生成正面"的问题

2. **智能发散系统**
   - 大远景是否不再是"原图不变"
   - 环境扩展是否保持画风一致
   - 极端角度是否能正确生成

3. **全局应用修复**
   - 所有 9 个 Panel 是否都应用了发散指导
   - 是否还会出现"只有 Panel 1 正确，其他是原图"

### 可能的后续优化方向

#### 方案 A：极端角度改为高度发散
**思路：**
- 当 `verticalAngle > 40°` 或 `< -20°` 时
- 改为高度发散而不是角度发散
- 因为从正上方/正下方看，水平角度意义不大

#### 方案 B：在提示词开头强调高度
**思路：**
- 把高度描述放在提示词最开头
- 让 AI 第一眼就看到高度约束

#### 方案 C：使用更直白的描述
**思路：**
- 避免专业术语（Top-Down View, High Angle）
- 使用更直白的描述（looking straight down from above）

#### 方案 D：添加视觉参考
**思路：**
- 在提示词中添加视觉参考
- 明确说明应该看到什么（头顶、肩膀等）

#### 方案 E：自适应偏移量
**思路：**
- 根据距离自动调整角度发散范围
- 近景用更小的偏移，远景用更大的偏移

#### 方案 F：角度预览
**思路：**
- 在 3D 视图中显示九宫格的 9 个角度
- 让用户直观看到变化范围

---

## 技术细节

### 距离映射（cameraZoom 0-10）

| cameraZoom | 距离术语 | 实际距离 | 说明 |
|-----------|---------|---------|------|
| 0 | Extreme Close-Up (ECU) | 0.5 | 极特写 |
| 1.5 | Close-Up (CU) | 1.25 | 特写 |
| 3 | Medium Shot (MS) | 2.0 | 近景 |
| 5 | Medium Full Shot (MFS) | 3.0 | 中景 |
| 7 | Full Shot (FS) | 4.0 | 全身 |
| 9 | Long Shot (LS) | 5.0 | 远景 |
| 10 | Extreme Long Shot (ELS) | 5.5 | 大远景 |

### 高度映射（verticalAngle -30° 到 60°）

| verticalAngle | 高度术语 | 说明 |
|--------------|---------|------|
| 50-60° | Top-Down View | 正上方俯视 |
| 30-50° | High Angle | 高角度 |
| 10-30° | Slightly High Angle | 略高角度 |
| -10-10° | Eye Level | 平视 |
| -20--10° | Slightly Low Angle | 略低角度 |
| -30--20° | Low Angle | 低角度 |

### 角度映射（horizontalAngle 0-360°）

16 个主方向，每个覆盖 22.5°：
1. 正面（0°）
2. 右前偏前（11.25-33.75°）
3. 右前方（33.75-56.25°）
4. 右前偏右（56.25-78.75°）
5. 右侧面（78.75-101.25°）
6. 右后偏右（101.25-123.75°）
7. 右后方（123.75-146.25°）
8. 右后偏后（146.25-168.75°）
9. 背面（168.75-191.25°）
10. 左后偏后（191.25-213.75°）
11. 左后方（213.75-236.25°）
12. 左后偏左（236.25-258.75°）
13. 左侧面（258.75-281.25°）
14. 左前偏左（281.25-303.75°）
15. 左前方（303.75-326.25°）
16. 左前偏前（326.25-348.75°）

每个方向有 9 个细分描述，Panel 5（index 4）是基准。

---

## 相关文档索引

### 实施文档（按时间顺序）
1. `3D相机提示词-角度发散方案实施.md` - 角度发散方案
2. `3D相机UI改进-距离控制和显示.md` - UI 改进
3. `3D相机距离公式修复-实时更新.md` - 距离公式修复
4. `3D相机提示词-高度约束强化.md` - 高度约束强化
5. `3D相机智能发散系统.md` - 智能发散系统（详细设计）
6. `3D相机智能发散系统-实施完成.md` - 智能发散系统（实施总结）
7. `3D相机发散指导全局应用修复.md` - 全局应用修复

### 历史文档（参考）
- `3D相机提示词-方案1实施.md` - 早期方案
- `3D相机提示词-方案A+D实施.md` - 方案 A+D
- `3D相机提示词优化-实施完成.md` - 早期优化总结
- `3D相机提示词-最终方案.md` - 原始设计方案

### 规格文档
- `.kiro/specs/camera-base-angle-fix/requirements.md` - 需求文档
- `.kiro/specs/camera-base-angle-fix/design.md` - 设计文档
- `.kiro/specs/camera-base-angle-fix/tasks.md` - 任务列表

---

## 总结

### 已完成的优化

1. ✅ **角度发散方案** - Panel 5 反映用户选择的角度
2. ✅ **UI 改进** - 距离控制和显示优化
3. ✅ **距离公式修复** - 实时更新，范围 0-10
4. ✅ **高度约束强化** - 5 个位置强调，详细描述
5. ✅ **智能发散系统** - 4 种情况自动识别，动态生成指导
6. ✅ **全局应用修复** - 3 个位置强调适用于所有 Panel

### 待测试验证

1. ⏳ 高度约束强化是否解决了极端俯视问题
2. ⏳ 智能发散系统是否解决了大远景原图问题
3. ⏳ 全局应用修复是否解决了只有 Panel 1 正确的问题

### 核心改进

- **从"完全锁定"到"智能发散"** - 允许发散，但保持画风一致
- **从"简单术语"到"详细描述"** - 让 AI 更容易理解
- **从"单次强调"到"多次强调"** - 在关键位置反复强调
- **从"全局指导"到"针对性指导"** - 根据情况动态生成

### 下一步行动

1. **测试验证** - 重点测试大远景和极端俯视场景
2. **收集反馈** - 记录哪些场景成功，哪些失败
3. **迭代优化** - 根据测试结果调整提示词或实施备选方案

---

**文档创建时间：** 2026-01-24  
**最后更新时间：** 2026-01-24  
**状态：** 待测试验证

**备注：** 本文档记录了 3D 相机提示词优化的完整过程，包括所有实施的改进、当前状态、测试场景和后续优化方向。供后续继续研究和优化使用。
