# 九宫格节点 - 拖动手柄 - 方案 3 实施

**日期**: 2026-01-31  
**状态**: ✅ 已实施（等待用户验收）

---

## 需求背景

**问题**：
- 用户反馈拖动功能容易误操作
- 整个图片区域都可拖动，和节点拖动冲突
- 没有明确的视觉提示

**解决方案**：
- 添加明确的拖动手柄
- 只有拖动手柄才能拖出图片
- 其他区域保持节点拖动功能

---

## 方案选择

经过讨论，选择了**方案 3：浮动标签抓手**

**理由**：
1. ✅ 最直观：有文字"拖出"，用户一眼就懂
2. ✅ 大气：胶囊形状，现代感强
3. ✅ 不碍眼：半透明背景，不抢眼
4. ✅ 品牌感：青色主题，和 UI 风格一致

---

## 设计规格

### 位置
- **右下角外侧**：紧贴节点边缘，不在图片内部
- **坐标**：`absolute -bottom-3 -right-3`

### 尺寸
- **高度**：28px（`h-7`）
- **宽度**：自适应内容（`px-2.5`）
- **形状**：胶囊形（`rounded-full`）

### 颜色
- **背景**：`bg-cyan-500/20` + `backdrop-blur-xl`（青色半透明 + 毛玻璃）
- **边框**：`border border-cyan-400/30`（青色边框）
- **图标**：`text-cyan-300/80`（青色图标）
- **文字**：`text-cyan-200/90`（青色文字）

### 悬停效果
- **背景变亮**：`hover:bg-cyan-500/30`
- **边框高亮**：`hover:border-cyan-400/50`
- **过渡动画**：`transition-all`

### 拖动效果
- **缩小**：`active:scale-95`
- **光标**：`cursor-grab active:cursor-grabbing`

### 出现动画
- **淡入 + 滑入**：`animate-in fade-in slide-in-from-right-2 duration-200`

---

## 显示条件

**必须同时满足两个条件**：

1. **单图模式**：`isSingleView = true`
   - 用户双击选中了某张图片
   - 进入单图展示模式

2. **节点选中**：`isSelected = true`
   - 节点被选中（点击节点）
   - 节点边框高亮

**逻辑**：
```typescript
const showDragHandle = isSingleView && isSelected;
```

**场景**：
- ✅ 双击图片 + 选中节点 → 显示手柄
- ❌ 双击图片 + 未选中节点 → 不显示手柄
- ❌ 九宫格模式 + 选中节点 → 不显示手柄
- ❌ 九宫格模式 + 未选中节点 → 不显示手柄

---

## 实施细节

### 1. 添加 `isSelected` prop

```typescript
interface GridSplitterNodeProps {
  // ... 其他 props
  isSelected?: boolean; // 🔥 新增：节点是否被选中
}
```

### 2. 计算显示条件

```typescript
// 是否显示拖动手柄：单图模式 + 节点选中
const showDragHandle = isSingleView && isSelected;
```

### 3. 渲染拖动手柄

```typescript
{/* 🔥 拖动手柄 - 方案 3：浮动标签抓手（右下角外侧）*/}
{showDragHandle && (
  <div 
    className="absolute -bottom-3 -right-3 h-7 px-2.5 rounded-full bg-cyan-500/20 backdrop-blur-xl border border-cyan-400/30 flex items-center gap-1 cursor-grab active:cursor-grabbing hover:bg-cyan-500/30 hover:border-cyan-400/50 active:scale-95 transition-all shadow-lg z-50 animate-in fade-in slide-in-from-right-2 duration-200"
    draggable={true}
    onDragStart={handleDragStart}
    onDragEnd={handleDragEnd}
    onMouseDown={(e) => e.stopPropagation()}
    title="拖动创建新图片节点"
  >
    <GripVertical size={12} className="text-cyan-300/80" />
    <span className="text-[10px] font-medium text-cyan-200/90">拖出</span>
  </div>
)}
```

### 4. 移除图片容器的拖动属性

**修改前**：
```typescript
<div 
  className="flex-1 relative overflow-hidden cursor-pointer"
  draggable={true}  // ❌ 整个图片可拖动
  onDragStart={handleDragStart}
  onDragEnd={handleDragEnd}
>
```

**修改后**：
```typescript
<div 
  className="flex-1 relative overflow-hidden"  // ✅ 移除 cursor-pointer
  // ✅ 移除 draggable、onDragStart、onDragEnd
  onMouseDown={(e) => e.stopPropagation()}  // ✅ 阻止节点拖动
>
```

### 5. 在 Node.tsx 中传递 `isSelected`

```typescript
<GridSplitterNode
  inputImage={inputImageSrc}
  croppedImages={node.data.croppedImages}
  selectedIndex={node.data.selectedGridIndex}
  outputImage={node.data.image}
  isWorking={node.status === NodeStatus.WORKING}
  isExpanded={isSelected || false}
  isSelected={isSelected || false} // 🔥 传递 isSelected prop
  onUpdate={(data) => {
    // ...
  }}
/>
```

---

## 交互流程

### 正常流程

1. **用户双击九宫格中的某张图片**
   - 进入单图模式
   - 图片放大显示

2. **用户点击节点选中**
   - 节点边框高亮
   - 拖动手柄出现（淡入 + 滑入动画）

3. **用户拖动手柄**
   - 手柄缩小（`scale-95`）
   - 光标变为 `grabbing`
   - 开始拖动

4. **用户松开鼠标**
   - 创建新的高质量图片节点
   - 九宫格节点保持原位
   - 手柄恢复正常大小

### 边界情况

1. **只双击图片，不选中节点**
   - 手柄不显示
   - 可以双击返回九宫格
   - 可以拖动节点

2. **只选中节点，不双击图片**
   - 手柄不显示
   - 显示九宫格
   - 可以拖动节点

3. **双击返回九宫格**
   - 手柄消失（淡出动画）
   - 显示九宫格
   - 可以再次双击选择

4. **取消选中节点**
   - 手柄消失（淡出动画）
   - 单图模式保持
   - 可以双击返回九宫格

---

## 视觉效果

### 默认状态
```
┌─────────────────────┐
│                     │
│   [单图内容]         │
│                     │
└─────────────────────┘
                  [⋮⋮ 拖出] ← 手柄（青色胶囊）
```

### 悬停状态
```
┌─────────────────────┐
│                     │
│   [单图内容]         │
│                     │
└─────────────────────┘
                  [⋮⋮ 拖出] ← 手柄（变亮 + 边框高亮）
                    ↑
                  光标：grab
```

### 拖动状态
```
┌─────────────────────┐
│                     │
│   [单图内容]         │
│                     │
└─────────────────────┘
                  [⋮⋮ 拖出] ← 手柄（缩小 + 半透明）
                    ↑
                  光标：grabbing
```

---

## 技术细节

### CSS 类名解析

```css
/* 位置 */
absolute -bottom-3 -right-3  /* 右下角外侧，距离 12px */

/* 尺寸 */
h-7 px-2.5  /* 高度 28px，左右内边距 10px */

/* 形状 */
rounded-full  /* 完全圆角（胶囊形） */

/* 背景 */
bg-cyan-500/20  /* 青色，20% 不透明度 */
backdrop-blur-xl  /* 毛玻璃效果 */

/* 边框 */
border border-cyan-400/30  /* 青色边框，30% 不透明度 */

/* 布局 */
flex items-center gap-1  /* 水平排列，垂直居中，间距 4px */

/* 光标 */
cursor-grab active:cursor-grabbing  /* 抓手光标 */

/* 悬停 */
hover:bg-cyan-500/30  /* 背景变亮到 30% */
hover:border-cyan-400/50  /* 边框变亮到 50% */

/* 拖动 */
active:scale-95  /* 缩小到 95% */

/* 过渡 */
transition-all  /* 所有属性平滑过渡 */

/* 阴影 */
shadow-lg  /* 大阴影 */

/* 层级 */
z-50  /* 最高层级 */

/* 动画 */
animate-in fade-in slide-in-from-right-2 duration-200  /* 淡入 + 从右滑入 */
```

### 图标和文字

```typescript
<GripVertical size={12} className="text-cyan-300/80" />  // 竖向抓手图标
<span className="text-[10px] font-medium text-cyan-200/90">拖出</span>  // 文字
```

---

## 备选方案

如果用户不满意方案 3，可以快速切换到其他方案：

### 方案 1：极简圆形抓手
```typescript
<div className="absolute -bottom-4 -right-4 w-8 h-8 rounded-full bg-white/10 backdrop-blur-xl border border-white/20 flex items-center justify-center cursor-grab active:cursor-grabbing hover:bg-white/20 hover:scale-110 active:scale-95 transition-all shadow-lg z-50">
  <div className="grid grid-cols-2 gap-0.5">
    <div className="w-1 h-1 rounded-full bg-white/60" />
    <div className="w-1 h-1 rounded-full bg-white/60" />
    <div className="w-1 h-1 rounded-full bg-white/60" />
    <div className="w-1 h-1 rounded-full bg-white/60" />
    <div className="w-1 h-1 rounded-full bg-white/60" />
    <div className="w-1 h-1 rounded-full bg-white/60" />
  </div>
</div>
```

### 方案 4：极简线条抓手
```typescript
<div className="absolute -bottom-4 -right-4 w-8 h-8 rounded-lg bg-white/5 backdrop-blur-xl border border-white/10 flex items-center justify-center cursor-grab active:cursor-grabbing hover:bg-white/15 hover:border-white/30 hover:scale-105 active:scale-95 transition-all shadow-lg z-50">
  <Menu size={14} className="text-white/40 hover:text-white/80 transition-colors" />
</div>
```

---

## 测试验收

### 功能测试

- [ ] 双击图片 + 选中节点 → 手柄显示
- [ ] 双击图片 + 未选中节点 → 手柄不显示
- [ ] 九宫格模式 + 选中节点 → 手柄不显示
- [ ] 拖动手柄 → 创建新图片节点
- [ ] 拖动手柄 → 九宫格节点保持原位
- [ ] 取消选中节点 → 手柄消失
- [ ] 双击返回九宫格 → 手柄消失

### 视觉测试

- [ ] 手柄位置正确（右下角外侧）
- [ ] 手柄大小合适（不太大不太小）
- [ ] 手柄颜色和谐（青色主题）
- [ ] 悬停效果明显（变亮 + 边框高亮）
- [ ] 拖动效果流畅（缩小 + 光标变化）
- [ ] 出现动画自然（淡入 + 滑入）
- [ ] 消失动画自然（淡出）

### 交互测试

- [ ] 手柄易于点击（不会误点）
- [ ] 拖动手柄不会触发节点拖动
- [ ] 拖动图片其他区域可以移动节点
- [ ] 双击图片可以返回九宫格
- [ ] 手柄不遮挡图片内容

---

## 相关文件

- `components/GridSplitterNode.tsx` - 九宫格节点组件（已修改）
- `components/Node.tsx` - 节点容器组件（已修改，传递 isSelected）

---

## 下一步

等待用户验收：
1. 查看手柄样式是否满意
2. 测试交互是否流畅
3. 如果不满意，可以快速切换到其他方案

---

**实施完成！等待用户反馈。** 🎨
