# 3D 相机提示词 - 高度约束强化

## 实施时间
2026-01-24

## 问题回顾

### 测试结果
- ✅ **特写 + 右上方**：成功
- ❌ **大远景 + 正上方**：出来 9 张正面（应该是俯视）
- ❌ **俯拍 + 大远景**：出来 9 张正面（应该是俯视）

### 问题分析

**核心问题：**
当垂直角度很大（如俯拍 50-60°）时，AI 忽略了高度信息，生成的全是正面视角。

**可能原因：**
1. 高度术语（`Top-Down View`）不够强，被角度描述（`front view`）覆盖
2. 角度发散方案在极端垂直角度时不适用（从正上方看，水平角度意义不大）
3. 提示词中的高度信息位置不够突出

## 解决方案：强化高度约束

### 核心改进

#### 1. 添加详细的高度描述函数

**新增代码：**
```typescript
const getHeightDescription = (angle: number): string => {
    if (angle >= 50) return 'Camera positioned directly above, looking straight down at the character from a bird\'s eye view';
    if (angle >= 30) return 'Camera elevated high above, looking down at the character from a high angle';
    if (angle >= 10) return 'Camera slightly elevated, looking down at the character from a slightly high angle';
    if (angle >= -10) return 'Camera at eye level, looking straight at the character horizontally';
    if (angle >= -20) return 'Camera slightly below, looking up at the character from a slightly low angle';
    return 'Camera positioned low, looking up at the character from a low angle';
};

const heightDescription = getHeightDescription(vAngle);
```

**改进点：**
- 从简单术语（`Top-Down View`）改为详细描述（`Camera positioned directly above, looking straight down...`）
- 明确说明相机的物理位置和视角方向
- 使用更直白的语言，避免专业术语被误解

#### 2. 在提示词中多次强调高度锁定

**改进位置 1：CRITICAL CONSTRAINT 部分**
```
[CRITICAL CONSTRAINT: IMG2IMG FEATURE LOCKING]
- Camera Vertical Position: LOCKED - ${heightDescription}
```

**改进位置 2：BASE ANCHOR POINT 部分**
```
[BASE ANCHOR POINT - Panel 5]
Camera Vertical Position: ${baseHeight} - ${heightDescription}

CRITICAL: The camera vertical position (${baseHeight}) is FIXED for ALL 9 panels. 
${heightDescription}. Do NOT change the camera height between panels.
```

**改进位置 3：SPATIAL LOGIC 部分**
```
[SPATIAL LOGIC]
- Vertical Position: LOCKED - ${heightDescription}
- The character stays in the same pose; the camera orbits horizontally around them at a FIXED vertical position
```

**改进位置 4：RULES 部分**
```
[RULES]
✅ ALL panels use EXACT same vertical position: ${baseHeight} - ${heightDescription}
✅ Maintain the camera at the SAME vertical height in all panels
```

**改进位置 5：NEGATIVE CONSTRAINTS 部分**
```
[NEGATIVE CONSTRAINTS]
❌ Do NOT change camera vertical position/height between panels - it MUST stay at ${baseHeight}
```

### 提示词对比

#### 改进前（高度约束弱）

```
[BASE ANCHOR POINT - Panel 5]
Distance: Extreme Long Shot (ELS) - LOCKED for all panels
Camera Height: Top-Down View - LOCKED for all panels
Camera Angle: front view - BASE REFERENCE
```

**问题：**
- 只有一次提到 `Top-Down View`
- 没有详细说明这意味着什么
- AI 可能不理解 `Top-Down View` 的含义

#### 改进后（高度约束强）

```
[CRITICAL CONSTRAINT: IMG2IMG FEATURE LOCKING]
- Camera Vertical Position: LOCKED - Camera positioned directly above, looking straight down at the character from a bird's eye view

[BASE ANCHOR POINT - Panel 5]
Distance: Extreme Long Shot (ELS) - LOCKED for all panels
Camera Vertical Position: Top-Down View - Camera positioned directly above, looking straight down at the character from a bird's eye view
Camera Horizontal Angle: front view - BASE REFERENCE

CRITICAL: The camera vertical position (Top-Down View) is FIXED for ALL 9 panels. 
Camera positioned directly above, looking straight down at the character from a bird's eye view. 
Do NOT change the camera height between panels.

[SPATIAL LOGIC]
- Vertical Position: LOCKED - Camera positioned directly above, looking straight down at the character from a bird's eye view

[RULES]
✅ ALL panels use EXACT same vertical position: Top-Down View - Camera positioned directly above, looking straight down at the character from a bird's eye view
✅ Maintain the camera at the SAME vertical height in all panels

[NEGATIVE CONSTRAINTS]
❌ Do NOT change camera vertical position/height between panels - it MUST stay at Top-Down View
```

**改进：**
- 在 5 个位置强调高度锁定
- 每次都附带详细描述
- 使用 `CRITICAL`, `FIXED`, `LOCKED`, `MUST` 等强调词
- 明确说明 "Do NOT change the camera height"

## 测试建议

### 重点测试场景

#### 场景 1：正上方俯视 + 大远景
**UI 设置：**
- horizontalAngle: 0°（正面）
- verticalAngle: 60°（正上方）
- cameraZoom: 10（大远景）

**预期结果：**
- 所有 9 张图片都应该是俯视角度
- Panel 5 应该是正上方俯视
- 周围 8 张应该是略微偏移的俯视（但仍然是俯视）

**验收标准：**
- ✅ 能看到角色的头顶
- ✅ 不是正面视角
- ✅ 9 张图片的高度一致（都是俯视）

#### 场景 2：俯拍 + 中景
**UI 设置：**
- horizontalAngle: 0°（正面）
- verticalAngle: 50°（俯拍）
- cameraZoom: 5（中景）

**预期结果：**
- 所有 9 张图片都应该是俯拍角度
- Panel 5 应该是正面俯拍
- 周围 8 张应该是略微偏移的俯拍

**验收标准：**
- ✅ 能看到角色的头顶和肩膀
- ✅ 不是平视视角
- ✅ 9 张图片的高度一致（都是俯拍）

#### 场景 3：高角度 + 特写
**UI 设置：**
- horizontalAngle: 45°（右前方）
- verticalAngle: 30°（高角度）
- cameraZoom: 2（特写）

**预期结果：**
- 所有 9 张图片都应该是高角度
- Panel 5 应该是右前方高角度
- 周围 8 张应该是略微偏移的高角度

**验收标准：**
- ✅ 相机略高于角色
- ✅ 能看到角色的头顶部分
- ✅ 9 张图片的高度一致（都是高角度）

### 对比测试

**测试方法：**
1. 使用相同的输入图片
2. 测试改进前后的提示词
3. 对比生成的 9 张图片

**对比指标：**
- 高度一致性：9 张图片的垂直角度是否一致
- 角度准确性：Panel 5 是否反映了用户选择的垂直角度
- 水平变化：周围 8 张图片是否有水平角度的细微变化

## 后续优化方向

### 如果仍然失败

#### 方案 A：极端角度改为高度发散
**思路：**
- 当 `verticalAngle > 40°` 或 `verticalAngle < -20°` 时
- 改为高度发散而不是角度发散
- 因为从正上方或正下方看，水平角度意义不大

**示例：**
```
用户设置：verticalAngle = 60°（正上方）

九宫格：
- 上排：verticalAngle = 70°（更俯视）
- 中排：verticalAngle = 60°（基准）
- 下排：verticalAngle = 50°（略俯视）
```

#### 方案 B：在提示词开头就强调高度
**思路：**
- 把高度描述放在提示词的最开头
- 让 AI 第一眼就看到高度约束

**示例：**
```
CRITICAL HEIGHT CONSTRAINT: Camera positioned directly above, looking straight down at the character from a bird's eye view. This vertical position is LOCKED for ALL 9 panels.

[ROLE & TASK]
...
```

#### 方案 C：使用更直白的描述
**思路：**
- 避免使用专业术语（`Top-Down View`, `High Angle`）
- 使用更直白的描述（`looking straight down from above`, `camera is above the character`）

**示例：**
```
改进前：Top-Down View
改进后：The camera is directly above the character, looking straight down
```

#### 方案 D：添加视觉参考
**思路：**
- 在提示词中添加视觉参考
- 明确说明应该看到什么（头顶、肩膀等）

**示例：**
```
Camera Vertical Position: Top-Down View
Visual Reference: You should see the top of the character's head, shoulders, and body from above. The character's face should be looking down or away from camera.
```

## 实施状态

### 已完成
- ✅ 添加 `getHeightDescription()` 函数
- ✅ 在 5 个位置强调高度锁定
- ✅ 使用详细描述替代简单术语
- ✅ 添加 `CRITICAL`, `FIXED`, `LOCKED` 等强调词

### 待测试
- ⏳ 测试正上方俯视 + 大远景
- ⏳ 测试俯拍 + 中景
- ⏳ 测试高角度 + 特写
- ⏳ 对比改进前后的效果

### 待优化（根据测试结果）
- ⏳ 如果仍然失败，考虑方案 A（极端角度改为高度发散）
- ⏳ 如果仍然失败，考虑方案 B（在提示词开头强调高度）
- ⏳ 如果仍然失败，考虑方案 C（使用更直白的描述）
- ⏳ 如果仍然失败，考虑方案 D（添加视觉参考）

## 文件变更

### 修改的文件
- `App.tsx`：提示词生成逻辑（第 1710-1780 行）

### 新增的文档
- `3D相机提示词-高度约束强化.md`：本文档

### 参考的文档
- `3D相机提示词-角度发散方案实施.md`：角度发散方案
- `3D相机提示词优化-实施完成.md`：之前的优化记录

---

**高度约束强化完成！** 🎉

现在的提示词：
- ✅ 在 5 个位置强调高度锁定
- ✅ 使用详细描述（`Camera positioned directly above...`）
- ✅ 添加强调词（`CRITICAL`, `FIXED`, `LOCKED`）
- ✅ 明确禁止改变高度（`Do NOT change camera height`）

**下一步：测试俯视角度（50-60°）的生成效果！**

**测试重点：**
1. 大远景 + 正上方（verticalAngle = 60°, cameraZoom = 10）
2. 俯拍 + 中景（verticalAngle = 50°, cameraZoom = 5）
3. 高角度 + 特写（verticalAngle = 30°, cameraZoom = 2）

**如果测试失败，考虑：**
- 方案 A：极端角度改为高度发散
- 方案 B：在提示词开头强调高度
- 方案 C：使用更直白的描述
- 方案 D：添加视觉参考
