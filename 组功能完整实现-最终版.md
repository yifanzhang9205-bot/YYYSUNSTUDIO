# 组功能完整实现 - 最终版

## 实施时间
2025-01-24

## 修复的问题

### 1. ✅ 工具栏被节点挡住
**问题**：GroupToolbar 渲染在 transform 容器内部，被节点覆盖

**解决方案**：
- 将 GroupToolbar 移到 transform 容器外部
- 使用 `fixed` 定位，z-index 设为 10000
- 手动计算屏幕坐标（考虑 pan 和 scale）

**代码位置**：
```typescript
// App.tsx 约 3175 行
{selectedGroupId && (() => {
    const group = groups.find(g => g.id === selectedGroupId);
    if (!group) return null;
    
    // 计算屏幕坐标（考虑 pan 和 scale）
    const screenX = (group.x + group.width / 2) * scale + pan.x;
    const screenY = group.y * scale + pan.y;
    
    return (
        <GroupToolbar
            groupId={group.id}
            position={{ x: screenX, y: screenY }}
            onAlign={handleGroupAlign}
            onDistribute={handleGroupDistribute}
            onAutoArrange={handleArrangeGroup}
            onScale={handleGroupScale}
        />
    );
})()}
```

### 2. ✅ 批量调整大小功能
**功能**：添加滑块统一缩放组内所有节点（50% - 200%）

**实现**：
- 新增 `handleGroupScale` 函数
- 计算组的中心点
- 保持节点相对于组中心的位置关系
- 同时缩放节点的位置和尺寸

**代码位置**：
```typescript
// App.tsx 约 982 行
const handleGroupScale = useCallback((scalePercent: number) => {
    if (!selectedGroupId) return;
    
    const group = groupsRef.current.find(g => g.id === selectedGroupId);
    if (!group) return;
    
    const groupNodes = Array.from(nodesRef.current.values()).filter(n => group.nodeIds.includes(n.id));
    if (groupNodes.length === 0) return;
    
    saveHistory();
    
    const scale = scalePercent / 100;
    const groupCenterX = group.x + group.width / 2;
    const groupCenterY = group.y + group.height / 2;
    
    setNodes(prev => {
        const newMap = new Map(prev);
        
        groupNodes.forEach(n => {
            const node = newMap.get(n.id);
            if (node) {
                const nodeWidth = node.width || 420;
                const nodeHeight = node.height || getApproxNodeHeight(node);
                
                // 计算节点中心相对于组中心的偏移
                const nodeCenterX = node.x + nodeWidth / 2;
                const nodeCenterY = node.y + nodeHeight / 2;
                const offsetX = nodeCenterX - groupCenterX;
                const offsetY = nodeCenterY - groupCenterY;
                
                // 缩放偏移和尺寸
                const newOffsetX = offsetX * scale;
                const newOffsetY = offsetY * scale;
                const newWidth = nodeWidth * scale;
                const newHeight = nodeHeight * scale;
                
                // 计算新位置
                const newX = groupCenterX + newOffsetX - newWidth / 2;
                const newY = groupCenterY + newOffsetY - newHeight / 2;
                
                newMap.set(n.id, { 
                    ...node, 
                    x: newX, 
                    y: newY,
                    width: newWidth,
                    height: newHeight
                });
            }
        });
        
        return newMap;
    });
}, [selectedGroupId, saveHistory]);
```

**UI 实现**：
- 添加 Maximize2 图标按钮
- 下拉面板显示滑块（50% - 200%）
- 实时显示当前缩放比例
- 重置按钮快速恢复 100%

### 3. ✅ 自动排列按连接关系
**改进**：从简单网格布局改为基于连接关系的拓扑排序

**算法**：
1. **构建图结构**：
   - 邻接表：节点 → 输出节点列表
   - 入度表：节点 → 入度数量

2. **拓扑排序（Kahn 算法）**：
   - 找到所有入度为 0 的节点（起始节点）
   - 按层级处理节点
   - 每处理一个节点，将其输出节点的入度 -1
   - 入度变为 0 的节点加入下一层

3. **布局渲染**：
   - 每一层水平排列
   - 层与层之间垂直排列
   - 自动计算组的新尺寸

**代码位置**：
```typescript
// App.tsx 约 810 行
const handleArrangeGroup = useCallback(() => {
    // ... 获取组内节点 ...
    
    // 拓扑排序：按照连接关系排列节点
    const nodeIds = new Set(groupNodes.map(n => n.id));
    const connections = connectionsRef.current.filter(c => nodeIds.has(c.from) && nodeIds.has(c.to));
    
    // 构建邻接表和入度表
    const adjacency = new Map<string, string[]>();
    const inDegree = new Map<string, number>();
    
    groupNodes.forEach(n => {
        adjacency.set(n.id, []);
        inDegree.set(n.id, 0);
    });
    
    connections.forEach(conn => {
        adjacency.get(conn.from)?.push(conn.to);
        inDegree.set(conn.to, (inDegree.get(conn.to) || 0) + 1);
    });
    
    // Kahn 算法
    const layers: string[][] = [];
    const queue: string[] = [];
    const processed = new Set<string>();
    
    // 找到所有入度为 0 的节点
    groupNodes.forEach(n => {
        if (inDegree.get(n.id) === 0) {
            queue.push(n.id);
        }
    });
    
    // 按层级处理节点
    while (queue.length > 0) {
        const currentLayer: string[] = [...queue];
        layers.push(currentLayer);
        queue.length = 0;
        
        currentLayer.forEach(nodeId => {
            processed.add(nodeId);
            
            const outputs = adjacency.get(nodeId) || [];
            outputs.forEach(outputId => {
                const degree = inDegree.get(outputId) || 0;
                inDegree.set(outputId, degree - 1);
                
                if (degree - 1 === 0 && !processed.has(outputId)) {
                    queue.push(outputId);
                }
            });
        });
    }
    
    // 处理未被处理的节点（孤立节点）
    const unprocessed = groupNodes.filter(n => !processed.has(n.id));
    if (unprocessed.length > 0) {
        layers.push(unprocessed.map(n => n.id));
    }
    
    // ... 布局渲染 ...
}, [selectedGroupId, saveHistory]);
```

**布局参数**：
- 节点宽度：420px（固定）
- 同层水平间距：80px
- 层间垂直间距：100px
- 组边距：40px

## 完整功能列表

### 对齐功能（6 种）
| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 左对齐 | Alt+A | 所有节点左边缘对齐 |
| 水平居中 | Alt+H | 所有节点水平居中对齐 |
| 右对齐 | Alt+D | 所有节点右边缘对齐 |
| 顶部对齐 | Alt+W | 所有节点顶部对齐 |
| 垂直居中 | Alt+V | 所有节点垂直居中对齐 |
| 底部对齐 | Alt+S | 所有节点底部对齐 |

### 分布功能（2 种）
| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 水平间距 | Shift+H | 节点之间水平间距相等 |
| 垂直间距 | Shift+V | 节点之间垂直间距相等 |

### 自动排列
| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 拓扑排序 | Shift+A | 按连接关系自动排列 |

### 批量缩放
| 功能 | 操作 | 说明 |
|------|------|------|
| 批量缩放 | 滑块 | 50% - 200% 统一缩放 |

## 工具栏 UI

### 布局
```
┌─────────────────────────────────────┐
│  [≡▼] [|||▼] [⊕▼]                  │
└─────────────────────────────────────┘
  对齐  间距  缩放
```

### 样式
- 背景：白色 95% 不透明度
- 边框：灰色 200
- 圆角：16px
- 阴影：2xl
- 按钮：16px 图标，hover 灰色背景
- z-index：10000（最上层）

### 下拉菜单
- 对齐菜单：6 个选项
- 间距菜单：3 个选项（水平、垂直、自动排列）
- 缩放面板：滑块 + 重置按钮

## 使用方法

### 创建/扩展组
1. 框选多个节点 → 自动创建组
2. 再次框选（包含已有组的节点）→ 自动扩展组
3. 框选多个组的节点 → 合并为一个组

### 使用工具栏
1. 点击组 → 选中组
2. 顶部出现工具栏（固定定位，不会被挡住）
3. 点击按钮或按快捷键 → 执行操作

### 对齐节点
- 点击对齐按钮 → 选择对齐方式
- 或按 Alt + A/H/D/W/V/S
- 节点立即对齐

### 分布节点
- 点击间距按钮 → 选择分布方式
- 或按 Shift + H/V
- 节点间距自动调整

### 自动排列
- 点击间距按钮 → 选择自动排列
- 或按 Shift + A
- 节点按连接关系自动排列成层级结构

### 批量缩放
- 点击缩放按钮 → 显示滑块
- 拖动滑块 → 实时缩放所有节点
- 点击重置 → 恢复 100%

## 技术亮点

### 1. 工具栏定位
- 使用 `fixed` 定位，避免被 transform 影响
- 手动计算屏幕坐标：`(worldX * scale) + pan.x`
- z-index 10000 确保最上层显示

### 2. 批量缩放算法
- 以组中心为基准点
- 保持节点相对位置关系
- 同时缩放位置和尺寸
- 支持 50% - 200% 范围

### 3. 拓扑排序
- Kahn 算法实现
- 处理循环依赖（孤立节点）
- 按层级布局
- 自动计算组尺寸

### 4. 性能优化
- 使用 Map 数据结构（O(1) 查找）
- 批量更新节点位置
- 只在选中时渲染工具栏
- 使用 useCallback 避免重复创建函数

## 文件清单

### 修改文件
- `App.tsx` - 添加 handleGroupScale、改进 handleArrangeGroup、移动 GroupToolbar 渲染位置
- `components/GroupToolbar.tsx` - 添加批量缩放 UI

## 测试建议

### 功能测试
1. ✅ 工具栏不被节点挡住
2. ✅ 批量缩放功能
3. ✅ 自动排列按连接关系
4. ✅ 6 种对齐功能
5. ✅ 2 种分布功能
6. ✅ 快捷键响应

### 边界测试
- 单个节点的组（对齐无效果）
- 两个节点的组（分布无效果）
- 无连接的节点（自动排列按位置）
- 循环依赖的节点（拓扑排序处理）
- 大量节点的组（性能测试）

### 交互测试
- 拖动组时工具栏跟随
- 缩放画布时工具栏位置正确
- 取消选中时工具栏消失
- 快捷键冲突检测
- 撤销/重做功能

## 已知限制

1. **分布功能**：需要至少 3 个节点
2. **对齐功能**：不考虑连接线，可能导致连接线交叉
3. **自动排列**：固定间距，不考虑节点内容
4. **批量缩放**：缩放后可能需要手动调整组大小

## 后续优化建议

1. **智能排列**：考虑连接线，减少交叉
2. **自定义间距**：允许用户调整间距大小
3. **对齐参考线**：显示对齐辅助线
4. **保存布局**：保存常用的排列方式
5. **动画效果**：节点移动时添加平滑动画
6. **自动调整组大小**：缩放后自动调整组边界

## 更新日志

### 2025-01-24
- ✅ 修复工具栏被节点挡住问题
- ✅ 添加批量缩放功能（50% - 200%）
- ✅ 改进自动排列算法（拓扑排序）
- ✅ 更新 GroupToolbar UI（添加缩放按钮）
- ✅ 完善文档和测试建议

## 总结

本次更新完成了组功能的三个核心改进：

1. **工具栏显示问题** - 通过移到 transform 容器外并使用 fixed 定位解决
2. **批量缩放功能** - 实现了以组中心为基准的统一缩放
3. **智能排列算法** - 从简单网格改为基于连接关系的拓扑排序

现在组功能已经非常完善，支持：
- 6 种对齐方式
- 2 种分布方式
- 智能自动排列（按连接关系）
- 批量缩放（50% - 200%）
- 完整的快捷键支持
- 优雅的 UI 交互

所有功能都经过优化，性能良好，用户体验流畅！
