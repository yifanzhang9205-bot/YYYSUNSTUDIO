# 3D 相机提示词优化 - 实施完成

## 实施时间
2026-01-24

## 实施内容

### 核心改进

#### 1. 三维空间锚定系统
- **锚定（Anchoring）**：输入图像 = 绝对基准点（Panel 5）
- **三维微调**：距离 → 高度 → 角度（符合人类空间理解顺序）
- **微调矩阵**：9 个格子围绕基准点的微小偏移

#### 2. 标准术语替换

**距离维度（Distance Axis）**
- 使用 Nano Banana 标准术语
- 替换前：`'extreme close-up'`, `'close-up'`, `'medium shot'` 等
- 替换后：`'Extreme Close-Up (ECU)'`, `'Close-Up (CU)'`, `'Medium Shot (MS)'` 等
- 九宫格逻辑：上排近一档，中排基准，下排远一档

**高度维度（Height Axis）**
- 使用电影摄影标准术语
- 替换前：`'top-down view'`, `'eye level'`, `'bottom-up view'` 等
- 替换后：`'High Angle'`, `'Eye Level'`, `'Low Angle'`
- 九宫格逻辑：上排高角度，中排基准，下排低角度

**角度维度（Angle Axis）**
- 使用角色转身图标准术语
- 替换前：`'front'`, `'front-right 45°'`, `'right side 90°'` 等
- 替换后：`'Front View'`, `'Front 3/4 View (right)'`, `'Side View (right)'` 等
- 九宫格逻辑：左列向左偏移，中列基准，右列向右偏移

#### 3. 提示词结构优化

**改进前（约 80 行）：**
- 大量重复的规则说明
- 详细的视觉描述（"Both eyes visible, far eye slightly foreshortened"）
- 冗长的约束条件

**改进后（约 40 行）：**
- 精简到核心要求
- 使用标准术语，不过度描述
- 强调锚定和微调逻辑

### 代码变更

#### 1. 新增函数

```typescript
// 距离维度
const getDistanceTerm = (z: number): string => { ... }
const getDistanceVariation = (baseZ: number, offset: -1 | 0 | 1): string => { ... }

// 高度维度
const getHeightTerm = (angle: number): string => { ... }
const getHeightVariation = (baseAngle: number, offset: -1 | 0 | 1): string => { ... }

// 角度维度
const getAngleTerm = (angle: number): string => { ... }
const getAngleVariation = (baseAngle: number, offset: -1 | 0 | 1): string => { ... }

// 九宫格矩阵生成
const generateCameraMatrix = (): string[] => { ... }
```

#### 2. 删除函数

```typescript
// 旧的视觉描述函数（已删除）
const generateVisualDescription = (h: number, v: number, z: number): string => { ... }
const generateCameraGrid = (centerH: number, centerV: number, centerZ: number) => { ... }

// 旧的术语映射函数（已删除）
const getDistanceDescEN = (z: number): string => { ... }
const getElevationDescEN = (angle: number): string => { ... }
const getAzimuthDescEN = (angle: number): string => { ... }
```

#### 3. 提示词模板

**新的提示词结构：**
```
IMPORTANT: Generate ONE SINGLE 21:9 image containing a 3×3 contact sheet grid.

[ROLE & TASK]
You are an expert CGI artist creating a character consistency contact sheet...

[CRITICAL CONSTRAINT: IMG2IMG FEATURE LOCKING]
Based strictly on the INPUT IMAGE, maintain perfect consistency...

[OUTPUT FORMAT]
- ONE single 21:9 image containing a 3×3 grid...

[BASE ANCHOR POINT - Panel 5]
Distance: {baseDistance}
Height: {baseHeight}
Angle: {baseAngle}

[CAMERA VARIATION MATRIX - 3D MICRO-ADJUSTMENTS]
Row 1 (Closer Distance + Higher Angle):
Panel 1: Slightly closer, camera elevated and panned left, looking down
Panel 2: Slightly closer, camera elevated straight, looking down
Panel 3: Slightly closer, camera elevated and panned right, looking down

Row 2 (Base Distance + Base Height):
Panel 4: Base distance, eye level, panned slightly left
Panel 5: ★ BASE ANCHOR ★ {baseDistance}, {baseHeight}, {baseAngle}
Panel 6: Base distance, eye level, panned slightly right

Row 3 (Farther Distance + Lower Angle):
Panel 7: Slightly farther, camera lowered and panned left, looking up
Panel 8: Slightly farther, camera lowered straight, looking up
Panel 9: Slightly farther, camera lowered and panned right, looking up

[SPATIAL LOGIC]
- Vertical axis (rows): Distance changes (closer → base → farther)
- Horizontal axis (columns): Angle changes (left → center → right)
- Diagonal: Combined distance + angle + height shifts

[RULES]
✅ Generate ONE image with 9 panels
✅ All variations are SUBTLE relative to Panel 5
✅ Maintain the illusion of rapid sequential photography
✅ Keep character pose, lighting, and style identical

[NEGATIVE CONSTRAINTS]
❌ Do NOT change character pose between panels
❌ Do NOT change lighting direction between panels
❌ Do NOT generate 9 separate images
❌ Do NOT introduce new elements not in input image
❌ Do NOT make drastic angle changes (e.g., front to back)
```

## 示例输出

### 场景 1：中景正面平视
**UI 设置：**
- horizontalAngle: 0° (正面)
- verticalAngle: 0° (平视)
- cameraZoom: 5 (中景)

**生成的基准点：**
```
Distance: Medium Full Shot (MFS)
Height: Eye Level
Angle: Front View
```

**九宫格矩阵：**
```
Panel 1: Slightly closer, camera elevated and panned left, looking down
Panel 2: Slightly closer, camera elevated straight, looking down
Panel 3: Slightly closer, camera elevated and panned right, looking down
Panel 4: Base distance, eye level, panned slightly left
Panel 5: ★ BASE ANCHOR ★ Medium Full Shot (MFS), Eye Level, Front View
Panel 6: Base distance, eye level, panned slightly right
Panel 7: Slightly farther, camera lowered and panned left, looking up
Panel 8: Slightly farther, camera lowered straight, looking up
Panel 9: Slightly farther, camera lowered and panned right, looking up
```

### 场景 2：特写侧面俯视
**UI 设置：**
- horizontalAngle: 90° (右侧)
- verticalAngle: 30° (俯视)
- cameraZoom: 2 (特写)

**生成的基准点：**
```
Distance: Close-Up (CU)
Height: High Angle
Angle: Side View (right)
```

**九宫格矩阵：**
```
Panel 1: Slightly closer, camera elevated and panned left, looking down
Panel 2: Slightly closer, camera elevated straight, looking down
Panel 3: Slightly closer, camera elevated and panned right, looking down
Panel 4: Base distance, eye level, panned slightly left
Panel 5: ★ BASE ANCHOR ★ Close-Up (CU), High Angle, Side View (right)
Panel 6: Base distance, eye level, panned slightly right
Panel 7: Slightly farther, camera lowered and panned left, looking up
Panel 8: Slightly farther, camera lowered straight, looking up
Panel 9: Slightly farther, camera lowered and panned right, looking up
```

## 优势分析

### 1. 空间逻辑清晰
- 先确定远近（最直观）
- 再确定高低（第二直观）
- 最后确定角度（最复杂）
- 符合人类理解 3D 空间的自然顺序

### 2. 术语标准化
- 使用 Nano Banana 训练数据中的标准术语
- 避免 AI 无法理解的度数描述
- 提高生成准确性

### 3. 锚定机制强大
- Panel 5 是绝对基准
- 其他 8 个格子都是相对偏移
- AI 更容易理解"微调"而不是"绝对值"

### 4. 提示词精简
- 从 80 行减少到 40 行
- 去掉冗余的规则说明
- 重点突出，易于理解

### 5. 符合行业标准
- 电影摄影术语（Distance, Height）
- 角色转身图术语（Angle）
- 专业工具（Scenario.com, Flux Kontext）的最佳实践

## 预期效果

### 角度准确性
- ✅ 使用 AI 熟悉的标准术语
- ✅ 减少理解偏差
- ✅ 提高角度生成的准确性

### 一致性
- ✅ 强调锚定机制
- ✅ 明确 Panel 5 是基准
- ✅ 其他格子是微调

### 生成速度
- ✅ 提示词更短
- ✅ 处理更快
- ✅ 减少 AI 混淆

## 测试建议

### 第一阶段：基础测试
1. 测试标准角度（0°, 45°, 90°, 180°）
2. 测试不同距离（特写、中景、远景）
3. 测试不同高度（俯视、平视、仰视）
4. 对比优化前后的效果

### 第二阶段：边缘测试
1. 测试极端角度（背面、侧面）
2. 测试极端距离（极特写、大远景）
3. 测试组合场景（侧面 + 俯视 + 特写）

### 第三阶段：用户反馈
1. 收集用户对角度准确性的反馈
2. 收集用户对一致性的反馈
3. 根据反馈微调参数

## 后续优化方向

### 可能的改进
1. **自适应偏移量**
   - 根据距离自动调整偏移量
   - 近景用更小的偏移，远景用更大的偏移

2. **角度预览**
   - 在 3D 视图中显示九宫格的 9 个角度
   - 让用户直观看到变化范围

3. **提示词模板**
   - 允许用户自定义提示词模板
   - 保存常用的提示词配置

## 文件变更

### 修改的文件
- `App.tsx`：提示词生成逻辑（第 1300-1600 行）

### 新增的文档
- `3D相机提示词-最终方案.md`：完整的方案设计文档
- `3D相机提示词优化-实施完成.md`：本文档

### 参考的文档
- `3D相机提示词分析和改进方案.md`：问题分析
- `多角度相机-提示词优化.md`：历史优化记录
- `.kiro/specs/camera-animation-preview/requirements.md`：需求文档

---

**实施完成！** 🎉

现在的提示词使用：
- ✅ 三维空间锚定系统
- ✅ 标准行业术语
- ✅ 精简的提示词结构
- ✅ 清晰的微调逻辑

**等待测试和用户反馈！**
