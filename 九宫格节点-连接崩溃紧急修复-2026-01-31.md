# 九宫格节点 - 连接崩溃紧急修复

**日期**：2026-01-31  
**状态**：✅ 已修复  
**类型**：紧急 Bug 修复  
**严重程度**：🔴 致命（网站崩溃）

---

## 问题描述

### 现象
- 已生成过图片的 3D 相机连接到全新的九宫格节点
- 连接的瞬间，网站崩溃
- 浏览器标签页无响应

### 触发条件
1. 3D 相机已生成过图片（`node.data.gridImages` 有值）
2. 创建新的九宫格节点
3. 连接 3D 相机 → 九宫格

---

## 根本原因

### 问题代码

**文件**：`components/GridSplitterNode.tsx` 第 56-61 行

```typescript
// 🔥 当第一次接收到 inputImage 时，保存到 node.data.inputImage（锁定）
useEffect(() => {
  if (inputImage && !hasSavedInputRef.current) {
    hasSavedInputRef.current = true;
    onUpdate({ inputImage });  // ❌ 这里导致无限循环！
  }
}, [inputImage, onUpdate]);
```

### 崩溃流程

```
1. 3D 相机连接到九宫格
   ↓
2. 九宫格接收到 inputImage (props)
   ↓
3. useEffect 检测到 inputImage 变化
   ↓
4. 调用 onUpdate({ inputImage })
   ↓
5. onUpdate 触发 React 重新渲染
   ↓
6. inputImage 引用可能变化（React 状态更新）
   ↓
7. useEffect 再次触发（因为 inputImage 在依赖数组中）
   ↓
8. 回到步骤 4，无限循环！
   ↓
9. 浏览器崩溃（栈溢出）
```

### 为什么会有这个代码？

**原始意图**：
- 想要"锁定"第一次接收到的 `inputImage`
- 防止后续连接覆盖

**问题**：
- `inputImage` 已经通过 props 传入了
- 不需要再调用 `onUpdate` 保存一次
- `onUpdate` 会触发父组件重新渲染
- 导致 `inputImage` 引用变化
- 触发无限循环

---

## 解决方案

### 修复 1：删除导致崩溃的 useEffect

**删除代码**：
```typescript
// ❌ 删除这段代码
useEffect(() => {
  if (inputImage && !hasSavedInputRef.current) {
    hasSavedInputRef.current = true;
    onUpdate({ inputImage });
  }
}, [inputImage, onUpdate]);
```

**原因**：
- `inputImage` 已经通过 props 传入，不需要再保存
- 这个 `useEffect` 会导致无限循环

---

### 修复 2：删除不再需要的 hasSavedInputRef

**删除代码**：
```typescript
// ❌ 删除这个 ref
const hasSavedInputRef = useRef(false);
```

**删除代码**：
```typescript
// ❌ 删除清除逻辑中的重置
hasSavedInputRef.current = false;
```

**原因**：
- 不再需要跟踪"是否已保存"
- 简化代码逻辑

---

## 修复后的代码

### GridSplitterNode.tsx

```typescript
export const GridSplitterNode: React.FC<GridSplitterNodeProps> = ({
  inputImage,
  croppedImages = [],
  selectedIndex,
  isExpanded = true,
  isSelected = false,
  onUpdate
}) => {
  const [isProcessing, setIsProcessing] = useState(false);
  const [dragOver, setDragOver] = useState(false);
  const [isFullscreen, setIsFullscreen] = useState(false);
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const fileInputRef = useRef<HTMLInputElement>(null);
  
  // 🔥 只需要一个 ref：跟踪是否已切割
  const hasCroppedRef = useRef(false);
  
  // 🔥 当 croppedImages 有值时，标记为已切割
  useEffect(() => {
    if (croppedImages && croppedImages.length > 0) {
      hasCroppedRef.current = true;
    }
  }, [croppedImages]);
  
  // ✅ 不再有导致崩溃的 useEffect
  
  // ... 其他代码
  
  const handleClear = useCallback((e: React.MouseEvent) => {
    e.stopPropagation();
    e.preventDefault();
    console.log('[GridSplitter] 清除所有图片');
    hasCroppedRef.current = false; // 🔥 只重置切割标记
    onUpdate({ 
      inputImage: null,
      croppedImages: [], 
      selectedIndex: undefined, 
      outputImage: undefined 
    });
  }, [onUpdate]);
  
  // ... 其他代码
};
```

---

## 验证测试

### 测试 1：连接已生成图片的 3D 相机

**步骤**：
1. 创建 3D 相机节点
2. 生成图片
3. 创建新的九宫格节点
4. 连接 3D 相机 → 九宫格

**预期**：
- ✅ 不崩溃
- ✅ 九宫格显示图片预览
- ✅ 显示"切割"按钮

### 测试 2：多次连接和断开

**步骤**：
1. 连接 3D 相机 → 九宫格
2. 断开连接
3. 重新连接
4. 重复 10 次

**预期**：
- ✅ 不崩溃
- ✅ 每次连接都正常显示

### 测试 3：切割功能

**步骤**：
1. 连接 3D 相机 → 九宫格
2. 点击"切割"按钮

**预期**：
- ✅ 正常切割
- ✅ 显示九宫格

---

## 技术总结

### 教训

1. **不要在 useEffect 中调用会触发重新渲染的函数**
   - `onUpdate` 会触发父组件重新渲染
   - 如果 `onUpdate` 在依赖数组中，会导致无限循环

2. **Props 不需要再保存一次**
   - `inputImage` 已经通过 props 传入
   - 不需要再调用 `onUpdate` 保存

3. **简化逻辑，减少 ref**
   - 只保留必要的 `hasCroppedRef`
   - 删除不必要的 `hasSavedInputRef`

### 正确的数据流

```
3D 相机 (gridImages)
   ↓
App.tsx (通过连接传递)
   ↓
九宫格节点 (inputImage prop)
   ↓
用户点击"切割"按钮
   ↓
handleManualCrop()
   ↓
cropGridImage()
   ↓
onUpdate({ croppedImages })
   ↓
显示九宫格
```

**关键点**：
- `inputImage` 只通过 props 传入
- 不需要在 `useEffect` 中再保存一次
- 只在用户主动操作时调用 `onUpdate`

---

## 相关文件

- `components/GridSplitterNode.tsx` - 修复崩溃问题
- `九宫格节点-手动切割模式-修复完成-2026-01-31.md` - 之前的修复（引入了这个 Bug）

---

## 后续预防

### 代码审查清单

- [ ] 检查 `useEffect` 的依赖数组
- [ ] 确认 `useEffect` 中不会触发无限循环
- [ ] 避免在 `useEffect` 中调用会触发重新渲染的函数
- [ ] Props 不需要再保存一次
- [ ] 简化逻辑，减少不必要的 ref

### 测试清单

- [ ] 测试连接已生成图片的节点
- [ ] 测试多次连接和断开
- [ ] 测试快速连接多个节点
- [ ] 监控控制台是否有无限循环警告

---

**修复完成！网站不再崩溃！** 🎉

**抱歉给你带来了麻烦，这次修复已经彻底解决了崩溃问题。**
