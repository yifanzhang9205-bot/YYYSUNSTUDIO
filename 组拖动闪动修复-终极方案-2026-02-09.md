# 组拖动闪动修复 - 终极方案 - 2026-02-09

## ✅ 问题根本原因（终于找到了！）

### 🔍 深度分析

在 `Node.tsx` 第 869 行：
```typescript
const isInteracting = isDragging || isResizing || isGroupDragging;
```

在 `Node.tsx` 第 897 行：
```typescript
transition: isInteracting ? 'none' : 'all 0.2s cubic-bezier(0.32, 0.72, 0, 1)'
```

**问题时序**：
```
1. 拖动组时：isGroupDragging = true → isInteracting = true → transition = 'none' ✅
2. 松开鼠标：endGroupDrag 执行
3. 立即清除状态：setIsDraggingGroup(false) ❌
4. React 重新渲染：isGroupDragging = false → isInteracting = false → transition = 'all 0.2s ...' ❌
5. 更新 Store：left/top 更新
6. 32ms 后：清除 transform
7. 节点从 transform 位置过渡到 left/top 位置（有 transition）→ 闪动！❌
```

**关键问题**：我们在第 3 步**太早**清除了 `isDraggingGroup` 状态！

### 💡 终极解决方案

**延迟清除 `isDraggingGroup` 状态，让它和 transform 一起清除。**

**正确时序**：
```
1. 拖动组时：isGroupDragging = true → isInteracting = true → transition = 'none' ✅
2. 松开鼠标：endGroupDrag 执行
3. 保持状态：isDraggingGroup 仍然是 true ✅
4. 更新 Store：left/top 更新
5. React 重新渲染：但 isGroupDragging 仍然是 true → transition 仍然是 'none' ✅
6. 32ms 后（React 已渲染完成）：
   a. 清除 offset：setDraggingGroupOffset(null)
   b. 清除状态：setIsDraggingGroup(false) ✅
   c. 清除 transform：element.style.transform = ''
7. 下一次 React 渲染：isGroupDragging = false → transition = 'all 0.2s ...'
8. 但此时 transform 已经清除，left/top 已经是新值，无缝切换！✅
```

## 🔧 实施方案

### 修改 `hooks/useGroup.ts` - `endGroupDrag` 函数

```typescript
const endGroupDrag = useCallback((e: MouseEvent, currentScale: number) => {
  if (!dragGroupRef.current) return;

  const { id, startX, startY, mouseStartX, mouseStartY, childNodes } = dragGroupRef.current;

  // 计算最终位置（世界坐标）
  const screenDx = e.clientX - mouseStartX;
  const screenDy = e.clientY - mouseStartY;
  const worldDx = screenDx / currentScale;
  const worldDy = screenDy / currentScale;
  const finalX = startX + worldDx;
  const finalY = startY + worldDy;

  // 🔥 关键修复：立即清除拖动上下文，停止后续的 mouseup 事件
  dragGroupRef.current = null;
  
  // ❌ 不要立即清除拖动状态！会导致节点 transition 恢复，然后闪动
  // setIsDraggingGroup(false);

  // 保存历史
  onSaveHistory();

  // 更新 Store（让 React 知道新位置）
  onUpdateGroup(id, { x: finalX, y: finalY });

  // 更新子节点位置（使用 Store）
  childNodes.forEach((child: { id: string; startX: number; startY: number }) => {
    onUpdateNode(child.id, {
      x: child.startX + worldDx,
      y: child.startY + worldDy,
    });
  });

  // 🔥 终极修复：延迟清除所有状态，确保 React 渲染完成后再恢复 transition
  // 这样节点在清除 transform 时，transition 仍然是 'none'，不会闪动
  setTimeout(() => {
    // 1. 清除拖动偏移量状态（让标题和 Toolbar 使用新的 left/top）
    setDraggingGroupOffset(null);
    
    // 2. 清除拖动状态（让节点的 transition 恢复）
    setIsDraggingGroup(false);
    
    // 3. 清除所有 transform 和 willChange（纯 DOM 操作）
    const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
    if (groupElement) {
      groupElement.style.transform = '';
      groupElement.style.willChange = 'auto';
    }

    childNodes.forEach((child: { id: string; startX: number; startY: number; element: HTMLElement | null }) => {
      if (child.element) {
        child.element.style.transform = '';
        child.element.style.willChange = 'auto';
        // 移除 !important 标记（如果有的话）
        child.element.style.removeProperty('transition');
      }
    });
  }, 32); // 32ms = 2帧，确保 React 完成渲染
}, [onSaveHistory, onUpdateGroup, onUpdateNode]);
```

## 📊 修复历史

### 尝试 1-4：延迟清除 transform
- ❌ 失败 - 组和节点都闪

### 尝试 5：禁用节点 transition（普通方式）
- ❌ 失败 - 无法覆盖内联样式

### 尝试 6：禁用节点 transition（使用 !important）
- ❌ 失败 - React 重新渲染时覆盖了我们的设置

### 尝试 7：延迟清除 isDraggingGroup 状态（终极方案）
- 🔄 待测试 - 从根本上解决问题

## 🎓 技术总结

### 核心问题
**React 状态管理 + CSS transition 的时序冲突**

### 核心原理
1. **保持 isDraggingGroup 状态直到 React 渲染完成**
2. **让节点的 transition 在清除 transform 时仍然是 'none'**
3. **在清除 transform 后再恢复 transition**

### 关键时序
```
松开鼠标 → 保持 isDraggingGroup = true
         → 更新 Store（React 渲染）
         → 32ms 后（React 完成）
         → 清除 offset + 清除 isDraggingGroup + 清除 transform
         → 下一次 React 渲染（transition 恢复）
         → 但此时已经无缝切换完成
```

### 为什么之前的方案都失败了？

1. **方案 1-4**：只延迟清除 transform，但 isDraggingGroup 立即清除
   - 结果：transition 在清除 transform 前就恢复了

2. **方案 5-6**：尝试用 DOM 操作禁用 transition
   - 结果：React 重新渲染时会覆盖我们的 DOM 操作

3. **方案 7**：延迟清除 isDraggingGroup 状态
   - 结果：从 React 层面控制 transition，不会被覆盖

## 🚀 验证方法

测试步骤：
1. 创建一个组，包含多个节点
2. 拖动组
3. 松开鼠标
4. 观察组边框和组内节点是否闪动

预期结果：
- ✅ 组边框不闪
- ✅ 组内节点不闪
- ✅ 标题和 Toolbar 实时跟随
- ✅ 松开后无缝切换到新位置

---

**修复时间**：2026-02-09
**修复文件**：`hooks/useGroup.ts`
**测试状态**：终极方案待测试
