# 数据持久化架构升级 - 紧急修复报告

**创建日期**：2026-02-10  
**状态**：🔥 紧急修复中  
**问题**：`Cannot access 'restoreHistoryImages' before initialization`

---

## 🚨 问题诊断

### 错误信息
```
App.tsx:425 Uncaught ReferenceError: Cannot access 'restoreHistoryImages' before initialization
```

### 根本原因

经过分析，问题的根本原因是：

1. **之前的实施不完整**：虽然代码看起来正确，但可能有隐藏的初始化顺序问题
2. **persist 中间件可能有问题**：connectionStore 和 groupStore 的 persist 配置可能不正确
3. **Blob URL 恢复逻辑可能有问题**：restoreHistoryImages 函数可能在 Store 初始化之前就被调用

### 用户反馈

用户明确指出：
- "告诉我需求全部做完了，几个小时几百点积分消费了"
- "然后再次检查的时候发现从0开始呢？？？"
- **这说明之前的工作根本没有完成，或者被回滚了**

---

## 🎯 修复策略

### 策略 1：完全重新实施（推荐）

**原因**：
- 之前的代码可能有多处修改导致混乱
- 用户反馈说"从0开始"，说明之前的工作没有保存
- 需要严格按照需求文档从头实施

**步骤**：
1. 检查所有相关文件的当前状态
2. 清理所有不完整的修改
3. 严格按照需求文档逐步实施
4. 每一步都验证功能
5. 完成后进行完整测试

### 策略 2：快速修复（不推荐）

**原因**：
- 可能只是修复表面问题
- 无法保证长期稳定性

---

## 📋 完整实施清单

### 阶段 1：检查当前状态

- [ ] 1.1 检查 `core/stores/connectionStore.ts` 的 persist 配置
- [ ] 1.2 检查 `core/stores/groupStore.ts` 的 persist 配置
- [ ] 1.3 检查 `core/stores/assetHistoryStore.ts` 是否有 `updateAssetSrc` 方法
- [ ] 1.4 检查 `hooks/useAssetHistory.ts` 是否有 `restoreHistoryImages` 函数
- [ ] 1.5 检查 `App.tsx` 的恢复逻辑是否正确
- [ ] 1.6 检查 `services/blobStorage.ts` 的保存/恢复函数

### 阶段 2：修复 persist 配置

- [ ] 2.1 确认 connectionStore 的 persist 配置正确
- [ ] 2.2 确认 groupStore 的 persist 配置正确
- [ ] 2.3 添加 Map 序列化/反序列化函数（如果需要）

### 阶段 3：修复历史记录恢复逻辑

- [ ] 3.1 确认 `assetHistoryStore.updateAssetSrc` 方法存在
- [ ] 3.2 确认 `useAssetHistory.restoreHistoryImages` 函数存在
- [ ] 3.3 确认 `App.tsx` 的 useEffect 调用顺序正确
- [ ] 3.4 确认 IndexedDB 键名格式正确（`asset-{assetId}`）

### 阶段 4：修复历史记录拖拽逻辑

- [ ] 4.1 确认拖拽时从 `asset-{assetId}` 读取 Blob
- [ ] 4.2 确认拖拽时保存到 `blob-node-{newNodeId}-image`
- [ ] 4.3 确认删除节点时只删除 `blob-node-{id}-image`

### 阶段 5：测试验证

- [ ] 5.1 测试普通图片上传 → 刷新 → 图片保留
- [ ] 5.2 测试历史记录持久化 → 刷新 → 历史记录图片可见
- [ ] 5.3 测试历史记录拖拽 → 刷新 → 画布节点图片保留
- [ ] 5.4 测试删除节点 → 历史记录仍然可见
- [ ] 5.5 测试 IndexedDB 键名格式

---

## 🔧 立即执行的修复步骤

### 步骤 1：检查 assetHistoryStore

检查是否有 `updateAssetSrc` 方法。

### 步骤 2：检查 useAssetHistory

检查是否有 `restoreHistoryImages` 函数，并且正确导出。

### 步骤 3：检查 App.tsx

检查 `useAssetHistory()` 的调用位置和 `restoreHistoryImages` 的使用位置。

### 步骤 4：修复初始化顺序问题

如果发现初始化顺序问题，调整代码顺序。

---

## ⚠️ 关键注意事项

1. **不要再次偷懒**：必须严格按照需求文档实施
2. **不要跳过步骤**：每一步都要验证
3. **不要假设代码正确**：必须实际测试
4. **不要修改 App.tsx 的业务逻辑**：只能添加恢复逻辑
5. **不要破坏现有功能**：必须保持 100% 兼容

---

## 📝 下一步行动

1. **立即检查所有相关文件的当前状态**
2. **找出真正的问题所在**
3. **制定详细的修复计划**
4. **逐步实施并验证**
5. **完成后进行完整测试**

---

**修复开始时间**：2026-02-10  
**预计完成时间**：30 分钟  
**负责人**：当前 AI

