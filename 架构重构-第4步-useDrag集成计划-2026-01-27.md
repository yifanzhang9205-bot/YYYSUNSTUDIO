# 架构重构 - 第 4 步 - useDrag 集成计划

**日期**: 2026-01-27  
**状态**: 📋 计划中

---

## 🎯 目标

将节点拖拽逻辑从 App.tsx 迁移到 useDrag Hook，同时保持性能和功能完整性。

---

## 📊 当前状态分析

### App.tsx 中的拖拽逻辑（约 200 行）

#### 1. 拖拽开始（onMouseDown）
- **位置**: 第 2490-2500 行
- **功能**:
  - 保存历史记录
  - 查找父分组
  - 查找同组节点
  - 缓存 DOM 元素
  - 设置拖拽状态

#### 2. 拖拽中（handleGlobalMouseMove）
- **位置**: 第 1040-1095 行
- **功能**:
  - 计算拖拽偏移
  - 碰撞检测（约 50 行）
  - 磁吸效果（约 30 行）
  - 使用 RAF + Transform 优化性能

#### 3. 拖拽结束（handleGlobalMouseUp）
- **位置**: 第 1100-1185 行
- **功能**:
  - 从 transform 读取最终位置
  - 碰撞检测和推开（约 50 行）
  - 更新 Store
  - 清理状态

---

## 🚀 集成策略

### 阶段 1：基础集成（本次完成）

**目标**: 将基础拖拽逻辑迁移到 useDrag Hook

**步骤**:
1. 在 App.tsx 中调用 useDrag Hook
2. 将拖拽开始逻辑迁移到 Hook
3. 将拖拽中逻辑迁移到 Hook（不包括碰撞检测和磁吸）
4. 将拖拽结束逻辑迁移到 Hook（不包括碰撞检测）
5. 注释旧的拖拽状态和 ref

**保留在 App.tsx 的逻辑**:
- 碰撞检测逻辑（约 50 行）
- 磁吸逻辑（约 30 行）
- 分组内拖拽逻辑（约 20 行）

**原因**: 这些逻辑依赖于大量的节点数据和几何计算，应该在 Core Layer 实现，而不是在 Hook 中。

### 阶段 2：优化（后续完成）

**目标**: 将复杂逻辑迁移到 Core Layer

**步骤**:
1. 在 `core/utils/geometry.ts` 中实现碰撞检测函数
2. 在 `core/utils/geometry.ts` 中实现磁吸计算函数
3. 在 useDrag Hook 中调用这些函数
4. 从 App.tsx 中删除这些逻辑

---

## 📝 实施细节

### 1. 在 App.tsx 中调用 useDrag Hook

```typescript
const {
  handleMouseDown: handleNodeDragStart,
  handleMouseMove: handleNodeDragMove,
  handleMouseUp: handleNodeDragEnd,
  cancelDrag: cancelNodeDrag,
  isDragging: isDraggingNode,
} = useDrag({
  scale,
  onUpdateNode: (id, updates) => {
    setNodes(prev => {
      const newMap = new Map(prev);
      const node = newMap.get(id);
      if (node) {
        newMap.set(id, { ...node, ...updates });
      }
      return newMap;
    });
  },
  onSaveHistory: saveHistory,
});
```

### 2. 更新节点的 onMouseDown 事件

```typescript
onMouseDown={(e) => {
  if (e.button === 0 && !e.shiftKey) {
    selectNode(node.id, e.ctrlKey || e.metaKey);
    
    // 查找父分组
    const w = node.width || 420;
    const h = getApproxNodeHeight(node);
    const cx = node.x + w/2;
    const cy = node.y + h/2;
    const pGroup = groups.find(g => {
      return cx > g.x && cx < g.x + g.width && cy > g.y && cy < g.y + g.height;
    });
    
    // 查找同组节点
    let siblingNodeIds: string[] = [];
    if (pGroup) {
      siblingNodeIds = Array.from(nodes.values())
        .filter(other => {
          if (other.id === node.id) return false;
          const b = getNodeBounds(other);
          const ocx = b.x + b.width/2;
          const ocy = b.y + b.height/2;
          return ocx > pGroup.x && ocx < pGroup.x + pGroup.width && 
                 ocy > pGroup.y && ocy < pGroup.y + pGroup.height;
        })
        .map(s => s.id);
    }
    
    // 调用 useDrag Hook
    handleNodeDragStart(e, node.id, node, pGroup?.id, siblingNodeIds);
    setDraggingNodeParentGroupId(pGroup?.id || null);
    setDraggingNodeId(node.id);
  }
}}
```

### 3. 更新 handleGlobalMouseMove

```typescript
const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
  // ...其他逻辑...
  
  // 调用 useDrag Hook
  if (isDraggingNode) {
    handleNodeDragMove(e);
  }
  
  // ...其他逻辑...
}, [isDraggingNode, handleNodeDragMove, ...]);
```

### 4. 更新 handleGlobalMouseUp

```typescript
const handleGlobalMouseUp = useCallback(() => {
  // ...其他逻辑...
  
  // 调用 useDrag Hook
  if (isDraggingNode) {
    handleNodeDragEnd(e);
  }
  
  // ...其他逻辑...
}, [isDraggingNode, handleNodeDragEnd, ...]);
```

### 5. 注释旧的状态和 ref

```typescript
// === 旧代码（已被 useDrag 替换，保留以便回滚）===
// const [draggingNodeId, setDraggingNodeId] = useState<string | null>(null);
// const dragNodeRef = useRef<DragContext | null>(null);
```

---

## ⚠️ 注意事项

1. **性能**: 确保使用 RAF + Transform 优化，不触发 React 重渲染
2. **功能完整性**: 确保所有拖拽相关的功能都正常工作
3. **渐进式**: 先完成基础集成，后续再优化复杂逻辑
4. **可回滚**: 保留旧代码，方便回滚

---

## 🎯 验收标准

- [ ] 单选节点拖拽正常
- [ ] 多选节点拖拽正常（暂不实现，后续优化）
- [ ] 分组内节点拖拽正常
- [ ] 拖拽性能流畅（60fps）
- [ ] 拖拽结束后位置正确
- [ ] 碰撞检测正常（保留在 App.tsx）
- [ ] 磁吸效果正常（保留在 App.tsx）

---

**下一步**: 开始实施集成
