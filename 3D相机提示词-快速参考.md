# 3D 相机提示词 - 快速参考

## 当前状态（2026-01-24）

### ✅ 已完成的优化

1. **角度发散方案** - Panel 5 反映用户选择的角度，周围 8 张发散
2. **UI 改进** - 距离范围 0-10，顶部显示距离信息
3. **距离公式修复** - `dist = 0.5 + cameraZoom * 0.5`，实时更新
4. **高度约束强化** - 5 个位置强调，详细描述
5. **智能发散系统** - 4 种情况自动识别（距离远/近、极端俯仰、背面侧面）
6. **全局应用修复** - 3 个位置强调"适用于所有 9 个 Panel"

### ⏳ 待测试验证

1. 大远景是否不再是"原图不变"
2. 极端俯视是否不再生成正面
3. 所有 9 个 Panel 是否都应用了发散指导

---

## 核心代码位置

- **提示词生成：** `App.tsx` 第 1350-1900 行
- **3D UI 组件：** `components/MultiAngleCameraNode.tsx`

---

## 关键函数

```typescript
// 距离映射
const getDistanceTerm = (z: number): string => { ... }

// 高度映射
const getHeightTerm = (angle: number): string => { ... }
const getHeightDescription = (angle: number): string => { ... }

// 角度分段（16 个方向 × 9 个细分）
const getAngleSegment = (angle: number): AngleSegment => { ... }

// 智能发散判断
const needEnvironmentExpansion = zoom >= 7;
const needCloseUpFocus = zoom <= 2;
const needVerticalInference = vAngle >= 50 || vAngle <= -20;
const needAngleInference = (normalized >= 78.75 && normalized <= 281.25);

// 发散指导生成
const getDivergenceGuidance = (): string => { ... }
```

---

## 测试场景（优先级排序）

### ⭐⭐ 最高优先级

**场景 1：大远景 + 正面平视**
```
horizontalAngle: 0°
verticalAngle: 0°
cameraZoom: 10
```
**验收：** 所有 9 个 Panel 都有环境扩展，不是原图

**场景 2：大远景 + 正上方俯视**
```
horizontalAngle: 0°
verticalAngle: 60°
cameraZoom: 10
```
**验收：** 所有 Panel 都是俯视 + 有环境，不是正面

### ⭐ 高优先级

**场景 3：中景 + 右前方平视**
```
horizontalAngle: 45°
verticalAngle: 0°
cameraZoom: 5
```
**验收：** Panel 5 是右前方，周围 8 张围绕发散

**场景 4：极特写 + 正面平视**
```
horizontalAngle: 0°
verticalAngle: 0°
cameraZoom: 0
```
**验收：** 所有 Panel 裁切到脸部

**场景 5：中景 + 背面平视**
```
horizontalAngle: 180°
verticalAngle: 0°
cameraZoom: 5
```
**验收：** 所有 Panel 显示背面或接近背面

---

## 智能发散系统

### 4 种发散情况

| 情况 | 触发条件 | 发散权限 | 严格约束 |
|------|---------|---------|---------|
| 距离变远 | zoom >= 7 | 添加环境、缩小角色 | 角色、画风、光照一致 |
| 距离变近 | zoom <= 2 | 裁切聚焦、增强细节 | 角色特征、服装一致 |
| 极端俯仰 | vAngle >= 50 或 <= -20 | 推测头顶/脚底 | 角色身份、服装一致 |
| 背面侧面 | hAngle 78.75°-281.25° | 推测背面/侧面 | 服装颜色/图案一致 |

### 发散指导示例

**大远景（zoom = 10）：**
```
[ENVIRONMENT EXPANSION - Long Shot Handling]
✅ ALLOWED: Add appropriate background/environment around the character
✅ ALLOWED: Scale down the character proportionally
❌ LOCKED: Character identity, outfit, and art style must remain identical
❌ LOCKED: Lighting mood and color palette must stay consistent
```

---

## 参数映射

### 距离（cameraZoom 0-10）

| 值 | 术语 | 实际距离 |
|----|------|---------|
| 0 | Extreme Close-Up (ECU) | 0.5 |
| 2 | Close-Up (CU) | 1.5 |
| 5 | Medium Full Shot (MFS) | 3.0 |
| 7 | Full Shot (FS) | 4.0 |
| 10 | Extreme Long Shot (ELS) | 5.5 |

### 高度（verticalAngle -30° 到 60°）

| 角度 | 术语 |
|------|------|
| 50-60° | Top-Down View |
| 30-50° | High Angle |
| 10-30° | Slightly High Angle |
| -10-10° | Eye Level |
| -20--10° | Slightly Low Angle |
| -30--20° | Low Angle |

### 角度（horizontalAngle 0-360°）

16 个主方向，每个 22.5°：
- 0°: 正面
- 45°: 右前方
- 90°: 右侧面
- 135°: 右后方
- 180°: 背面
- 225°: 左后方
- 270°: 左侧面
- 315°: 左前方

---

## 已知问题

### 问题 1：只有 Panel 1 应用发散指导
**状态：** 已修复（待测试）  
**修复：** 在 3 个位置强调"适用于所有 9 个 Panel"

### 问题 2：大远景给原图
**状态：** 已修复（待测试）  
**修复：** 智能发散系统，允许添加环境

### 问题 3：极端俯视生成正面
**状态：** 已修复（待测试）  
**修复：** 高度约束强化，5 个位置强调

---

## 后续优化方向

### 如果测试失败

**方案 A：** 极端角度改为高度发散  
**方案 B：** 在提示词开头强调高度  
**方案 C：** 使用更直白的描述  
**方案 D：** 添加视觉参考  

### 功能增强

**方案 E：** 自适应偏移量（根据距离调整发散范围）  
**方案 F：** 角度预览（在 3D 视图显示 9 个角度）  

---

## 相关文档

### 完整记录
- `3D相机提示词优化-完整记录.md` - 完整的优化历程和技术细节

### 实施文档
1. `3D相机提示词-角度发散方案实施.md`
2. `3D相机UI改进-距离控制和显示.md`
3. `3D相机距离公式修复-实时更新.md`
4. `3D相机提示词-高度约束强化.md`
5. `3D相机智能发散系统.md`
6. `3D相机智能发散系统-实施完成.md`
7. `3D相机发散指导全局应用修复.md`

---

## 快速调试

### 查看生成的提示词
```typescript
console.log('[MultiAngleCamera] 提示词:', geminiPrompt);
```

### 查看发散判断
```typescript
console.log('发散情况:', {
    needEnvironmentExpansion,
    needCloseUpFocus,
    needVerticalInference,
    needAngleInference
});
```

### 查看角度分段
```typescript
console.log('角度分段:', angleSegment);
```

---

**最后更新：** 2026-01-24  
**状态：** 待测试验证

**下一步：** 测试大远景和极端俯视场景，验证修复效果
