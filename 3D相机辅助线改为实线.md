# 3D 相机辅助线改为实线

## 问题描述

**现象：** 即使移除了 `computeLineDistances()` 调用，拖动相机越过地面连线时，画面还是会"像是复位一下似的"

**用户反馈：**
> "还是会有，每次越过这个线的时候，都会像是复位一下似的"

## 问题分析

### 虚线材质的固有问题

即使不调用 `computeLineDistances()`，**虚线材质（`LineDashedMaterial`）本身也有渲染问题**：

1. **虚线模式依赖线段长度**
   - 虚线段和间隙的分布取决于线段的总长度
   - 当线段长度变化时，虚线的视觉效果会改变

2. **地面连线长度不断变化**
   - 起点：相机在地面的投影点（随相机移动）
   - 终点：目标中心（固定）
   - 当相机旋转时，这条线的长度和方向都在变化

3. **虚线渲染的离散性**
   - 虚线是由一系列"实线段"和"间隙"组成
   - 当线段长度变化时，虚线段的数量和位置会改变
   - 导致视觉上的"跳动"或"复位"感

### 特别是"越过这条线"时

当相机旋转经过某些特殊角度时：
- 地面连线的长度会快速变化
- 虚线段的分布会重新调整
- 产生视觉上的"跳动"或"复位"感

**示例：**
```
角度 0°：地面连线长度 = 5，虚线段数量 = 25
角度 45°：地面连线长度 = 3.5，虚线段数量 = 17
角度 90°：地面连线长度 = 5，虚线段数量 = 25

从 0° 到 45°：虚线段数量从 25 减少到 17 → 视觉上"跳动"
从 45° 到 90°：虚线段数量从 17 增加到 25 → 视觉上"跳动"
```

## 解决方案

### 核心思路

**把虚线改成实线，彻底避免虚线渲染的问题**

### 修复代码

#### 地面连线（groundLine）

**修复前：**
```typescript
// ❌ 虚线材质
const groundLine = new THREE.Line(groundLineGeo, new THREE.LineDashedMaterial({ 
  color: 0x555555, 
  dashSize: 0.2, 
  gapSize: 0.1 
}));
groundLine.computeLineDistances();
```

**修复后：**
```typescript
// ✅ 实线材质
const groundLine = new THREE.Line(groundLineGeo, new THREE.LineBasicMaterial({ 
  color: 0x555555,
  transparent: true,
  opacity: 0.5
}));
// 实线不需要 computeLineDistances()
```

#### 垂直线（verticalLine）

**修复前：**
```typescript
// ❌ 虚线材质
const verticalLine = new THREE.Line(verticalLineGeo, new THREE.LineDashedMaterial({ 
  color: 0x555555, 
  dashSize: 0.15, 
  gapSize: 0.1 
}));
verticalLine.computeLineDistances();
```

**修复后：**
```typescript
// ✅ 实线材质
const verticalLine = new THREE.Line(verticalLineGeo, new THREE.LineBasicMaterial({ 
  color: 0x555555,
  transparent: true,
  opacity: 0.5
}));
// 实线不需要 computeLineDistances()
```

### 保留虚线的辅助线

**高度参考圆环（heightRing）** 保持虚线：
- 这是一个圆环，长度固定
- 虚线效果更好看
- 不会导致视觉跳动

```typescript
// ✅ 保持虚线（圆环长度固定，不会跳动）
const heightRing = new THREE.Line(heightRingGeo, new THREE.LineDashedMaterial({ 
  color: 0x666666, 
  dashSize: 0.2, 
  gapSize: 0.1,
  transparent: true,
  opacity: 0.5
}));
heightRing.computeLineDistances();
```

## 实线 vs 虚线对比

### 虚线材质（LineDashedMaterial）

**优点：**
- ✅ 视觉效果更丰富
- ✅ 可以区分不同类型的辅助线

**缺点：**
- ❌ 长度变化时会"跳动"
- ❌ 需要调用 `computeLineDistances()`
- ❌ 渲染性能稍差

**适用场景：**
- 长度固定的线（如圆环）
- 不需要频繁更新的线

### 实线材质（LineBasicMaterial）

**优点：**
- ✅ 渲染稳定，不会"跳动"
- ✅ 不需要 `computeLineDistances()`
- ✅ 渲染性能更好

**缺点：**
- ❌ 视觉效果相对单调

**适用场景：**
- 长度频繁变化的线（如地面连线、垂直线）
- 需要流畅动画的线

## 视觉效果调整

为了让实线不那么单调，我们添加了透明度：

```typescript
new THREE.LineBasicMaterial({ 
  color: 0x555555,      // 灰色
  transparent: true,    // 启用透明
  opacity: 0.5          // 50% 透明度
})
```

**效果：**
- 实线看起来更柔和
- 不会太突兀
- 仍然清晰可见

## 修复前后对比

### 修复前 ❌

```
辅助线类型：
  - 高度参考圆环：虚线 ✅（长度固定，不跳动）
  - 垂直线：虚线 ❌（长度变化，会跳动）
  - 地面连线：虚线 ❌（长度变化，会跳动）
  - 地面投影点：实心圆 ✅

拖动相机：
  → 地面连线长度变化
  → 虚线段分布改变
  → 视觉上"跳动"或"复位"
```

### 修复后 ✅

```
辅助线类型：
  - 高度参考圆环：虚线 ✅（长度固定，不跳动）
  - 垂直线：实线 ✅（长度变化，不跳动）
  - 地面连线：实线 ✅（长度变化，不跳动）
  - 地面投影点：实心圆 ✅

拖动相机：
  → 地面连线长度变化
  → 实线平滑移动
  → 没有"跳动"或"复位"
```

## 测试步骤

### 测试 1：拖动相机不跳动
1. 打开多角度相机节点
2. 观察画面底部的辅助线
3. 拖动相机旋转一圈
4. **预期：** 所有辅助线平滑移动，没有"跳动"或"复位"

### 测试 2：越过地面连线不跳动
1. 拖动相机，让视角经过地面连线（红框标出的那条线）
2. 观察线的移动
3. **预期：** 线平滑移动，没有"像是复位一下"的感觉

### 测试 3：快速旋转不跳动
1. 快速拖动相机旋转
2. 观察辅助线的变化
3. **预期：** 即使快速移动，辅助线也平滑流畅

### 测试 4：Shift + 拖拽不跳动
1. 按住 Shift + 拖拽推进/拉远
2. 观察辅助线的变化
3. **预期：** 辅助线平滑缩放，没有跳动

## 如果还有问题

如果改成实线后还有"复位"的感觉，可能是以下原因：

### 1. 相机本身的"复位"问题
- 检查 `currentAnglesRef` 是否正确更新
- 检查拖动开始时是否使用了正确的起始值

### 2. 渲染频率问题
- 确保每次更新后都调用 `renderer.render()`
- 检查是否有其他代码触发了重新渲染

### 3. 其他辅助线的问题
- 检查高度参考圆环是否还在调用 `computeLineDistances()`
- 检查是否有其他虚线导致的问题

## 总结

这次修复彻底解决了辅助线"跳动"的问题：

1. ✅ **地面连线改为实线**
   - 从虚线材质改为实线材质
   - 长度变化时不会"跳动"

2. ✅ **垂直线改为实线**
   - 从虚线材质改为实线材质
   - 长度变化时不会"跳动"

3. ✅ **保留圆环虚线**
   - 高度参考圆环保持虚线
   - 长度固定，不会跳动

4. ✅ **添加透明度**
   - 实线看起来更柔和
   - 不会太突兀

**核心原则：** 长度频繁变化的线使用实线材质，长度固定的线可以使用虚线材质！
