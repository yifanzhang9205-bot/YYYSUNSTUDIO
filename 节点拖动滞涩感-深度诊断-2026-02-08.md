# 节点拖动滞涩感 - 深度诊断

**日期**：2026-02-08  
**问题**：用户反馈拖动节点仍然有滞涩感

---

## 🔍 已实施的优化

### 1. ✅ Transform + RAF 方案
- 拖动时使用 `transform` 直接操作 DOM
- 使用 `requestAnimationFrame` 节流
- 拖动结束时才更新 Store

### 2. ✅ Transition 禁用
- `handleMouseDown` 中立即设置 `element.style.transition = 'none'`
- `handleMouseUp` 中恢复 transition

### 3. ✅ 辅助线检测节流
- 每 3 帧检测一次辅助线（20 FPS）
- 避免每次 mousemove 都触发 `setHelperLines`

### 4. ✅ GPU 加速
- 设置 `willChange: 'transform'`

---

## 🐛 可能的滞涩原因

### 原因 1：辅助线渲染开销 ⭐⭐⭐

**问题**：
- 辅助线是 SVG 元素，每次 `setHelperLines` 都会触发 React 重渲染
- 即使节流到 20 FPS，仍然会有 50ms 的渲染延迟
- 如果有多条辅助线，SVG 渲染开销更大

**验证方法**：
```typescript
// 临时禁用辅助线，看是否还滞涩
// 在 useDrag.ts 中注释掉 setHelperLines 调用
```

**解决方案**：
- 方案 A：使用 Canvas 绘制辅助线（性能更好）
- 方案 B：进一步降低辅助线检测频率（每 5 帧）
- 方案 C：使用 CSS 伪元素绘制辅助线（避免 React 渲染）

---

### 原因 2：连接线实时更新开销 ⭐⭐⭐

**问题**：
- 拖动节点时，所有连接到该节点的连接线都需要更新
- 连接线使用 SVG `<path>`，计算路径开销较大
- 如果节点有多条连接线，开销成倍增加

**验证方法**：
```typescript
// 测试：拖动没有连接线的节点 vs 有多条连接线的节点
// 如果有连接线的节点更滞涩，说明是连接线的问题
```

**解决方案**：
- 方案 A：连接线也使用 RAF 节流更新
- 方案 B：拖动时使用简化的连接线路径（直线）
- 方案 C：使用 CSS 变量 + CSS `calc()` 计算路径（避免 JS 计算）

---

### 原因 3：节点数量过多 ⭐⭐

**问题**：
- 如果画布上有大量节点（>50 个），即使不拖动它们，React 也需要检查是否需要重渲染
- `useMemo` 依赖项变化会导致所有节点重新计算

**验证方法**：
```typescript
// 测试：在只有 5 个节点的画布 vs 50 个节点的画布上拖动
// 如果节点多时更滞涩，说明是节点数量的问题
```

**解决方案**：
- 方案 A：虚拟化渲染（只渲染可见区域的节点）
- 方案 B：使用 `React.memo` 优化 Node 组件
- 方案 C：拖动时暂时隐藏不相关的节点

---

### 原因 4：浏览器性能限制 ⭐

**问题**：
- 用户的电脑性能较低
- 浏览器开启了硬件加速限制
- 其他标签页占用了大量资源

**验证方法**：
```typescript
// 打开浏览器性能分析工具（F12 → Performance）
// 录制拖动过程，查看帧率和瓶颈
```

**解决方案**：
- 方案 A：提示用户关闭其他标签页
- 方案 B：提供"性能模式"（禁用所有动画和特效）
- 方案 C：降低画布缩放比例

---

### 原因 5：鼠标采样率不足 ⭐

**问题**：
- 鼠标的采样率较低（如 125Hz）
- `mousemove` 事件触发频率不够高
- 导致拖动轨迹不连续

**验证方法**：
```typescript
// 在 handleMouseMove 中打印时间戳
console.log('mousemove', Date.now());
// 如果两次事件间隔 > 8ms，说明采样率不足
```

**解决方案**：
- 方案 A：使用 Pointer Events API（更高精度）
- 方案 B：使用插值算法平滑轨迹
- 方案 C：提示用户使用更好的鼠标

---

## 🎯 推荐的诊断步骤

### 步骤 1：临时禁用辅助线

在 `useDrag.ts` 中注释掉辅助线相关代码：

```typescript
// 🔥 临时禁用辅助线，测试性能
// helperLinesRafRef.current = requestAnimationFrame(() => {
//   ...
//   setHelperLines(lines);
// });
```

**如果禁用后流畅了**：说明是辅助线渲染的问题
**如果仍然滞涩**：继续下一步

---

### 步骤 2：检查连接线数量

在控制台打印拖动节点的连接线数量：

```typescript
const connections = useConnectionStore.getState().connections;
const nodeConnections = connections.filter(c => c.from === nodeId || c.to === nodeId);
console.log('连接线数量:', nodeConnections.length);
```

**如果连接线 > 5 条**：说明可能是连接线更新的问题
**如果连接线 < 5 条**：继续下一步

---

### 步骤 3：使用浏览器性能分析

1. 打开 Chrome DevTools（F12）
2. 切换到 Performance 标签
3. 点击录制按钮
4. 拖动节点 2-3 秒
5. 停止录制
6. 查看帧率和瓶颈

**关注指标**：
- FPS：应该 > 55 FPS
- Scripting：JS 执行时间应该 < 5ms/帧
- Rendering：渲染时间应该 < 5ms/帧
- Painting：绘制时间应该 < 3ms/帧

---

### 步骤 4：对比不同场景

测试以下场景，找出差异：

| 场景 | 节点数量 | 连接线数量 | 辅助线 | 预期流畅度 |
|------|---------|-----------|--------|-----------|
| A    | 5       | 0         | 禁用   | ✅ 非常流畅 |
| B    | 5       | 0         | 启用   | ✅ 流畅 |
| C    | 5       | 5         | 禁用   | ✅ 流畅 |
| D    | 5       | 5         | 启用   | ⚠️ 可能滞涩 |
| E    | 50      | 0         | 禁用   | ⚠️ 可能滞涩 |
| F    | 50      | 10        | 启用   | ❌ 滞涩 |

---

## 💡 快速优化方案

### 方案 1：降低辅助线检测频率（最简单）

```typescript
// 从每 3 帧改为每 5 帧
helperLinesRafRef.current = requestAnimationFrame(() => {
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          // 检测辅助线（降低到 12 FPS）
          if (!dragRef.current) return;
          const currentNode = nodes.get(id);
          if (currentNode) {
            const lines = detectHelperLines(
              { ...currentNode, x: newX, y: newY },
              nodes,
              5
            );
            setHelperLines(lines);
          }
        });
      });
    });
  });
});
```

**优点**：简单，只需修改一行代码
**缺点**：辅助线响应变慢

---

### 方案 2：使用 CSS 变量优化连接线（推荐）

当前连接线已经使用了 CSS 变量方案，但可能需要优化：

```typescript
// 确保连接线使用 CSS 变量读取偏移量
// 在 App.tsx 的连接线渲染中：
const dragOffsetX = parseFloat(
  getComputedStyle(fromElement).getPropertyValue('--drag-offset-x') || '0'
);
const dragOffsetY = parseFloat(
  getComputedStyle(fromElement).getPropertyValue('--drag-offset-y') || '0'
);
```

**优点**：连接线不需要 React 重渲染
**缺点**：需要确保所有连接线都使用这个方案

---

### 方案 3：添加性能模式开关（用户可选）

在设置中添加"性能模式"：

```typescript
// 性能模式：禁用所有非必要的视觉效果
const performanceMode = useSettingsStore(state => state.performanceMode);

if (performanceMode) {
  // 禁用辅助线
  // 禁用节点动画
  // 禁用连接线动画
  // 使用简化的连接线路径
}
```

**优点**：用户可以根据自己的电脑性能选择
**缺点**：需要添加设置界面

---

## 🚀 下一步行动

1. **立即测试**：临时禁用辅助线，看是否流畅
2. **性能分析**：使用 Chrome DevTools 录制拖动过程
3. **对比测试**：测试不同场景下的流畅度
4. **实施优化**：根据诊断结果选择合适的方案

---

## 📊 预期效果

| 优化方案 | 预期 FPS | 实施难度 | 推荐度 |
|---------|---------|---------|--------|
| 降低辅助线频率 | 55-60 | ⭐ | ⭐⭐⭐ |
| 优化连接线渲染 | 58-60 | ⭐⭐ | ⭐⭐⭐⭐ |
| 虚拟化渲染 | 60 | ⭐⭐⭐⭐ | ⭐⭐ |
| 性能模式开关 | 60 | ⭐⭐ | ⭐⭐⭐⭐ |

---

**建议**：先实施方案 1（降低辅助线频率），如果仍然滞涩，再考虑方案 2 和方案 4。
