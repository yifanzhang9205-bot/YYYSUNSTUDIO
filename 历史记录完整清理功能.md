# 历史记录完整清理功能

## 功能说明

一键下载所有图片（包括原图）并清除所有缓存，释放内存。

## 完整流程

### 1. 收集所有图片
```typescript
// 从 IndexedDB 加载原图（高清大图）
const originalImages = await loadFromStorage<string[]>(`grid-original-${node.id}`);

// 收集缩略图
node.data.gridImages  // 九宫格缩略图
node.data.image       // 单张图片
node.data.images      // 多张图片
```

### 2. 批量下载
```typescript
for (const { url, filename } of allImages) {
    const a = document.createElement('a');
    a.href = url;  // 支持 base64、Blob URL、普通 URL
    a.download = filename;
    a.click();
    
    await new Promise(resolve => setTimeout(resolve, 100));  // 延迟避免浏览器阻止
}
```

### 3. 清除 Blob URL（释放内存）
```typescript
// 遍历所有节点的所有图片
if (url.startsWith('blob:')) {
    URL.revokeObjectURL(url);  // 释放 Blob 内存
}
```

### 4. 清除 IndexedDB 缓存
```typescript
// 清除原图
await deleteFromStorage(`grid-original-${node.id}`);

// 清除其他缓存
await deleteFromStorage(`blob-${node.id}-thumbnail`);
await deleteFromStorage(`blob-${node.id}-original`);
```

### 5. 清除历史记录
```typescript
setHistory([]);
setHistoryIndex(-1);
```

### 6. 触发垃圾回收
```typescript
if (window.gc) {
    window.gc();  // 提示浏览器进行垃圾回收
}
```

## 下载的图片

### 优先级
1. **原图**（如果存在）- 从 IndexedDB 加载
   - 文件名：`节点标题-original-1.png`
   - 最高清晰度

2. **缩略图**（如果没有原图）
   - 文件名：`节点标题-grid-1.png`
   - 512px 宽度

### 文件命名
- 原图：`节点标题-original-1.png`, `节点标题-original-2.png`, ...
- 九宫格：`节点标题-grid-1.png`, `节点标题-grid-2.png`, ...
- 单张图：`节点标题-0.png`, `节点标题-1.png`, ...
- 多张图：`节点标题-1.png`, `节点标题-2.png`, ...

## 清理的内容

### 1. Blob URL 内存
- 所有 `blob:http://...` URL
- 释放浏览器内存

### 2. IndexedDB 缓存
- `grid-original-${nodeId}` - 原图
- `blob-${nodeId}-thumbnail` - 缩略图
- `blob-${nodeId}-original` - 原图备份

### 3. 历史记录
- 撤销/重做历史
- 释放历史记录占用的内存

### 4. 垃圾回收
- 提示浏览器进行 GC
- 释放所有未引用的对象

## 使用方法

1. 打开左侧边栏的"历史记录"面板
2. 点击右上角的"下载并清除"按钮
3. 浏览器会自动下载所有图片（包括原图）
4. 下载完成后，自动清除所有缓存
5. 弹出提示：显示下载数量和清理结果

## 提示信息

```
✅ 完成！

已下载: 27 张图片
已清除: 18 个 Blob URL
已清除: IndexedDB 缓存
已清除: 历史记录

内存已释放！
```

## 内存释放效果

### 清理前
- Blob URL：占用浏览器内存
- IndexedDB：占用磁盘空间
- 历史记录：占用 JS 堆内存

### 清理后
- Blob URL：✅ 已释放
- IndexedDB：✅ 已清空
- 历史记录：✅ 已清除
- 垃圾回收：✅ 已触发

### 预期效果
- 内存占用：减少 90%+
- 磁盘空间：释放所有缓存
- 性能提升：显著

## 技术细节

### Blob URL 清理
```typescript
URL.revokeObjectURL(blobUrl);
```
- 立即释放 Blob 对象
- 浏览器回收内存

### IndexedDB 清理
```typescript
const tx = db.transaction(STORE_NAME, 'readwrite');
const store = tx.objectStore(STORE_NAME);
store.delete(key);
```
- 删除指定键的数据
- 释放磁盘空间

### 垃圾回收
```typescript
if (window.gc) {
    window.gc();
}
```
- 仅在开发模式下可用
- 需要启动 Chrome 时添加 `--js-flags="--expose-gc"`
- 生产环境浏览器会自动 GC

## 注意事项

### 1. 浏览器下载限制
- 浏览器可能会提示"是否允许下载多个文件"
- 需要用户点击"允许"

### 2. 下载延迟
- 每张图片之间有 100ms 延迟
- 避免浏览器阻止批量下载

### 3. 原图优先
- 优先下载 IndexedDB 中的原图
- 如果没有原图，下载缩略图

### 4. 不可逆操作
- 清除后无法恢复
- 建议在清除前确认

## 未来改进

### 1. 确认对话框
```typescript
const confirmed = confirm('确定要下载所有图片并清除缓存吗？\n\n此操作不可逆！');
if (!confirmed) return;
```

### 2. 进度显示
```typescript
const progress = document.createElement('div');
progress.textContent = `下载中... ${downloadedCount}/${allImages.length}`;
```

### 3. ZIP 打包
```typescript
import JSZip from 'jszip';
const zip = new JSZip();
// 将所有图片打包成一个 ZIP 文件
```

### 4. 选择性清理
- 只清理 Blob URL
- 只清理 IndexedDB
- 只清理历史记录

## 总结

✅ 下载所有图片（包括原图）
✅ 清除所有 Blob URL
✅ 清除所有 IndexedDB 缓存
✅ 清除历史记录
✅ 触发垃圾回收
✅ 释放内存

**现在用户可以彻底清理缓存，释放内存了！** 🎉
