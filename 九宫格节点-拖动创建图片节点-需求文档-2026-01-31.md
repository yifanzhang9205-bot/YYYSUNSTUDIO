# 九宫格节点 - 拖动创建图片节点功能

**日期**: 2026-01-31  
**状态**: 📋 需求分析  
**优先级**: 🔴 高（用户反馈的 Bug）

---

## 🎯 功能概述

用户可以从九宫格节点的单图模式拖动图片到画布，创建新的高质量图片节点。

---

## 🐛 当前问题

### 问题 1：拖动时九宫格节点闪现
- **现象**：拖动单图时，九宫格节点不动，松开鼠标时突然闪现到鼠标位置
- **影响**：九宫格节点挡住新创建的图片节点
- **原因**：可能是拖动事件处理不当，或者节点位置被意外更新

### 问题 2：拖出的图片不清晰
- **现象**：拖出的图片节点显示的是切割后的低质量图片
- **原因**：使用的是 Blob URL（WebP 95%），而不是原始图片
- **期望**：使用原始高质量图片（3D 相机生成的 1K 图片）

### 问题 3：有时会拖出好几张
- **现象**：一次拖动操作创建了多个图片节点
- **原因**：可能是事件重复触发或防抖机制缺失

---

## 📋 需求分析

### 用户场景
1. 用户上传 21:9 九宫格图片（或从 3D 相机自动生成）
2. 九宫格自动切割成 9 张图片
3. 用户双击其中一张 → 放大显示（单图模式）
4. 用户拖动放大后的图片到画布空白处
5. 松开鼠标 → 创建新的图片节点
6. 新节点显示**原始高质量图片**（不是切割后的图片）
7. 九宫格节点保持原位，不移动

### 核心需求
1. **拖动交互**：从单图模式拖动图片到画布
2. **创建节点**：在鼠标松开位置创建新的图片节点
3. **高质量图片**：使用原始图片而不是切割后的图片
4. **防止闪现**：九宫格节点保持原位
5. **防止重复**：一次拖动只创建一个节点

---

## 🔧 技术方案

### 方案 A：保存原始图片 URL（推荐）

**核心思路**：
- 在切割九宫格时，保存原始图片 URL
- 拖动时传递原始图片 URL 而不是切割后的 Blob URL
- 创建节点时使用原始图片

**优点**：
- ✅ 图片质量最高（原始 1K 图片）
- ✅ 实现简单
- ✅ 内存占用小（不需要重新生成）

**缺点**：
- ❌ 需要修改数据结构（添加 `originalImage` 字段）
- ❌ 需要修改切割逻辑

**数据结构变更**：
```typescript
interface GridSplitterNodeData {
  inputImage?: string;          // 原始 21:9 图片
  croppedImages?: string[];     // 切割后的 9 张图片（Blob URL）
  selectedIndex?: number;       // 当前选中的索引
  outputImage?: string;         // 输出图片（切割后的）
  originalImage?: string;       // 🔥 新增：原始图片 URL（用于拖出）
}
```

---

### 方案 B：重新生成高质量图片

**核心思路**：
- 拖动时从原始图片重新切割对应的格子
- 使用更高的质量参数（WebP 100% 或 PNG）

**优点**：
- ✅ 不需要修改数据结构
- ✅ 可以动态调整质量

**缺点**：
- ❌ 需要重新切割（性能开销）
- ❌ 实现复杂
- ❌ 仍然不如原始图片清晰

---

### 方案 C：直接使用原始 21:9 图片

**核心思路**：
- 拖出的节点显示完整的 21:9 图片
- 不进行切割

**优点**：
- ✅ 最简单
- ✅ 质量最高

**缺点**：
- ❌ 不符合用户期望（用户想要单张图片，不是整个九宫格）

---

## ✅ 推荐方案：方案 A

**理由**：
1. 图片质量最高（原始 1K 图片）
2. 实现相对简单
3. 性能最好（不需要重新生成）
4. 符合用户期望

---

## 📝 实施步骤

### 步骤 1：修改数据结构
在 `types.ts` 中添加 `originalImage` 字段（如果需要）

### 步骤 2：修改 GridSplitterNode
1. 保存原始图片 URL
2. 添加拖动开始事件（`onDragStart`）
3. 传递原始图片 URL 和选中索引
4. 防止节点移动（`e.stopPropagation()`）

### 步骤 3：修改 App.tsx
1. 在 `handleCanvasDrop` 中处理九宫格图片拖放
2. 创建新的图片节点
3. 使用原始图片 URL

### 步骤 4：修复闪现 Bug
1. 检查拖动事件是否正确阻止冒泡
2. 检查节点位置更新逻辑
3. 添加拖动状态标记

### 步骤 5：防止重复创建
1. 添加防抖机制
2. 检查 `dataTransfer` 数据是否重复

---

## 🚨 风险和注意事项

### 风险 1：原始图片 URL 失效
- **场景**：如果原始图片是 Blob URL，刷新页面后会失效
- **缓解**：使用 IndexedDB 持久化存储

### 风险 2：拖动事件冲突
- **场景**：与节点拖动事件冲突
- **缓解**：正确使用 `e.stopPropagation()`

### 风险 3：性能问题
- **场景**：拖动大图片时卡顿
- **缓解**：使用 `requestAnimationFrame` 优化

---

## 📊 验收标准

- [ ] 可以从单图模式拖动图片到画布
- [ ] 松开鼠标后创建新的图片节点
- [ ] 新节点显示原始高质量图片
- [ ] 九宫格节点保持原位，不移动
- [ ] 一次拖动只创建一个节点
- [ ] 拖动过程流畅，无卡顿
- [ ] 拖动时有视觉反馈（鼠标样式、半透明预览等）

---

## 🎨 交互设计

### 拖动开始
- 鼠标样式：`cursor: grabbing`
- 图片半透明：`opacity: 0.5`
- 显示拖动提示（可选）

### 拖动中
- 鼠标样式：`cursor: grabbing`
- 显示拖动预览（可选）

### 拖动结束
- 创建新节点
- 新节点淡入动画（可选）
- 图片恢复正常透明度

---

## 🔄 后续优化

1. **拖动预览**：显示半透明的图片预览跟随鼠标
2. **智能布局**：自动避开现有节点
3. **批量拖动**：支持同时拖动多张图片
4. **快捷键**：Shift + 拖动 = 复制，Alt + 拖动 = 移动

---

**下一步**：开始实施方案 A
