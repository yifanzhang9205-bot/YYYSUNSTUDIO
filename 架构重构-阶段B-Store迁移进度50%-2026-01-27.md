# 架构重构 - 阶段 B - Store 迁移进度 50% - 2026-01-27

> 🎯 **目标**：将 App.tsx 的 useState 迁移到 Zustand Store（requirements.md 第 2 步）

---

## ✅ 已完成的迁移（50%）

### 1. Store 引入和初始化
```typescript
import { useNodeStore } from './core/stores/nodeStore';
import { useConnectionStore } from './core/stores/connectionStore';
import { useGroupStore } from './core/stores/groupStore';

const nodes = useNodeStore(state => state.nodes);
const connections = useConnectionStore(state => state.connections);
const groups = useGroupStore(state => state.groups);
```

### 2. Hooks 回调更新
- ✅ useHistory Hook → 使用 Store 的 setNodes/setConnections/setGroups
- ✅ useSelection Hook → 使用 Store 的 deleteNodes
- ✅ useConnection Hook → 使用 Store 的 addConnection/deleteConnection
- ✅ useGroup Hook → 使用 Store 的 addGroup/updateGroup/deleteGroup
- ✅ useDrag Hook → 使用 Store 的 updateNode

### 3. 核心函数迁移
- ✅ `deleteNodesCallback` → 使用 `useNodeStore.getState().deleteNodes()`
- ✅ `addNode` → 使用 `useNodeStore.getState().addNode()`
- ✅ `handleNodeUpdate` → 使用 `useNodeStore.getState().updateNode()`
- ✅ `handleNodeAction` → 使用 `useNodeStore.getState().updateNodeStatus()`
- ✅ 数据加载（useEffect）→ 使用 Store 的 setNodes/setConnections/setGroups
- ✅ `loadWorkflow` → 使用 Store 加载工作流
- ✅ 键盘快捷键（粘贴）→ 使用 Store 添加节点

---

## ⏳ 未完成的迁移（50%）

### 还需要更新的地方（约 15 处）

#### 1. 一键整理功能（约 951 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    Object.keys(newPositions).forEach(nodeId => { ... });
    return newMap;
});
```
**需要改为**：批量更新节点位置

#### 2. 节点拖拽结束（约 1068 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    const node = newMap.get(draggingNodeId);
    // ...
    return newMap;
});
```
**需要改为**：使用 `updateNode`

#### 3. 分组拖拽（约 1126 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    childNodes.forEach(child => { ... });
    return newMap;
});
```
**需要改为**：批量更新节点位置

#### 4. 节点 Resize（约 1148 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    const node = newMap.get(resizingNodeId);
    // ...
    return newMap;
});
```
**需要改为**：使用 `updateNodeSize`

#### 5. 分镜生成（约 1387 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    newNodes.forEach(node => newMap.set(node.id, node));
    return newMap;
});
setGroups(prev => [...prev, newGroup]);
```
**需要改为**：使用 `addNodes` 和 `addGroup`

#### 6. 多帧生成（约 1995 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    newNodes.forEach(node => newMap.set(node.id, node));
    return newMap;
});
```
**需要改为**：使用 `addNodes`

#### 7. 拖拽导入工作流（约 2107 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    newNodes.forEach(node => newMap.set(node.id, node));
    return newMap;
});
```
**需要改为**：使用 `addNodes`

#### 8. 连接线创建（约 2446 行）
```typescript
setConnections(p => [...p, { from: fromId, to: toId }]);
setNodes(p => {
    const newMap = new Map(p);
    const targetNode = newMap.get(toId);
    // ...
    return newMap;
});
```
**需要改为**：使用 `addConnection` 和 `updateNodeInputs`

#### 9. 输入重排序（约 2483 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    newMap.set(nodeId, { ...node, inputs: newOrder });
    return newMap;
});
```
**需要改为**：使用 `updateNodeInputs`

#### 10. 右键菜单 - 添加节点（约 2577 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    const sourceNode = newMap.get(contextMenuTarget.sourceNodeId);
    // ...
    return newMap;
});
setConnections(prev => [...prev, { from: newNodeId, to: contextMenuTarget.sourceNodeId }]);
```
**需要改为**：使用 `addNode`、`addConnection`、`updateNodeInputs`

#### 11. 右键菜单 - 添加新节点（约 2591 行）
```typescript
setNodes(prev => {
    const newMap = new Map(prev);
    newMap.set(newNode.id, newNode);
    return newMap;
});
```
**需要改为**：使用 `addNode`

#### 12. 右键菜单 - 删除连接（约 2617 行）
```typescript
setConnections(prev => prev.filter(c => c.from !== contextMenuTarget.from || c.to !== contextMenuTarget.to));
setNodes(prev => {
    const newMap = new Map(prev);
    const targetNode = newMap.get(contextMenuTarget.to);
    // ...
    return newMap;
});
```
**需要改为**：使用 `deleteConnection` 和 `updateNodeInputs`

---

## 📊 当前进度

### 阶段 B 完成度：**50%**

- ✅ Store 引入和初始化（100%）
- ✅ Hooks 回调更新（100%）
- ✅ 核心函数迁移（60%）
- ⏳ 业务逻辑迁移（40%）

### 总体完成度：**55%**

- ✅ 第 1 步：修复致命 Bug（100%）
- 🔄 第 2 步：抽离底层逻辑（50%）
- ✅ 第 3 步：抽离交互逻辑（80%，功能失效但架构就位）

---

## 🎯 下一步计划

### 立即执行（完成剩余 50%）

1. **更新一键整理功能**
   - 使用 `useNodeStore.getState().updateNodesPosition()`

2. **更新拖拽相关**
   - 节点拖拽结束
   - 分组拖拽
   - 节点 Resize

3. **更新生成相关**
   - 分镜生成
   - 多帧生成
   - 拖拽导入工作流

4. **更新连接线相关**
   - 连接线创建
   - 输入重排序
   - 删除连接

5. **更新右键菜单**
   - 添加节点
   - 删除连接

### 预计完成时间
- **剩余工作**：2-3 小时
- **阶段 B 完成**：今天内
- **阶段 C（Bug 修复）**：明天

---

## ✅ 验收标准（来自 requirements.md）

### 功能验收
- [ ] 所有现有功能正常工作
- [ ] 节点拖拽流畅
- [ ] 连接线正常
- [ ] 撤销/重做正常
- [ ] 数据持久化正常

### 结构验收
- [x] App.tsx 不再有 `useState` 管理 nodes/connections/groups（50%）
- [x] 所有数据操作通过 Store（50%）
- [x] Hooks 使用 Store 而不是 props（100%）

### 扩展性验收
- [ ] 添加新节点类型：只需注册到 NodeRegistry
- [ ] 添加新交互：只需创建新 Hook
- [ ] 添加新数据：只需扩展 Store

### 性能验收
- [ ] 拖拽 100 个节点不卡顿
- [ ] 撤销/重做响应 < 100ms
- [ ] 内存占用稳定

---

## 🚫 禁止事项（来自 requirements.md）

- ❌ **不回滚代码**：功能坏了继续推进，最后修复
- ❌ **不恢复旧状态**：不添加 draggingNodeId 等旧状态
- ❌ **不跨层混用**：UI → Hooks → Store，单向数据流
- ❌ **不在 UI 写逻辑**：所有逻辑在 Hooks 或 Store

---

## 📝 总结

**当前状态**：
- Store 已引入并初始化 ✅
- Hooks 回调已完全更新 ✅
- 核心函数已部分迁移 ⏳
- 业务逻辑还有约 15 处需要更新 ⏳

**下一步**：
- 继续迁移剩余的 15 处 `setNodes`、`setConnections`、`setGroups`
- 目标：App.tsx 不再有任何直接的状态更新
- 原则：向前推进，不回滚

**预期结果**：
- 阶段 B 完成后，App.tsx 行数减少 20-30%
- 所有数据操作通过 Store
- 为阶段 C（Bug 修复）打好基础
