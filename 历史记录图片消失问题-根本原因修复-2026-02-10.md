# 历史记录图片消失问题 - 根本原因修复

**修复日期**：2026-02-10  
**状态**：✅ 已修复  
**问题**：刷新后历史记录里看不见图片了

---

## 问题诊断

### 用户反馈
> "一刷新历史记录里看不见图片了"

### 根本原因

**问题出在 `blobId` 不一致**：

1. **保存时**（`handleAssetGenerated`）：
   ```typescript
   // 第一次生成 blobId
   blobId = await generateBlobId(src);
   
   // 第二次生成 blobId（在 saveAssetHistoryImageWithRef 内部）
   const blobId = await generateBlobId(imageUrl);
   ```

2. **恢复时**（`restoreHistoryImages`）：
   ```typescript
   // 使用第一次生成的 blobId 加载
   const blob = await loadFromStorage<Blob>(asset.blobId);
   ```

3. **问题**：
   - 保存时使用的是**第二次生成的 blobId**（在 `saveAssetHistoryImageWithRef` 内部）
   - Store 中保存的是**第一次生成的 blobId**（在 `handleAssetGenerated` 中）
   - 恢复时使用**第一次生成的 blobId**，但 IndexedDB 中的键名是**第二次生成的 blobId**
   - **结果**：找不到 Blob，图片消失！

---

## 修复方案

### 修复 1：修改 `saveAssetHistoryImageWithRef` 返回 `blobId`

**文件**：`services/blobStorage.ts`

**修改前**：
```typescript
export const saveAssetHistoryImageWithRef = async (
  assetId: string,
  imageUrl: string
): Promise<string> => {
  // ...
  return blobUrl; // ❌ 只返回 blobUrl
};
```

**修改后**：
```typescript
export const saveAssetHistoryImageWithRef = async (
  assetId: string,
  imageUrl: string
): Promise<{ blobUrl: string; blobId: string }> => {
  // ...
  return { blobUrl, blobId }; // ✅ 返回 blobUrl 和 blobId
};
```

**原因**：让调用方获取到实际保存时使用的 `blobId`，确保一致性。

---

### 修复 2：修改 `handleAssetGenerated` 使用返回的 `blobId`

**文件**：`hooks/useAssetHistory.ts`

**修改前**：
```typescript
// 第一次生成 blobId
blobId = await generateBlobId(src);

// 第二次生成 blobId（在函数内部）
historySrc = await saveAssetHistoryImageWithRef(assetId, src);
```

**修改后**：
```typescript
// 使用返回的 blobId（确保一致性）
const result = await saveAssetHistoryImageWithRef(assetId, src);
historySrc = result.blobUrl;
blobId = result.blobId; // ✅ 使用函数返回的 blobId
```

**原因**：使用 `saveAssetHistoryImageWithRef` 返回的 `blobId`，确保 Store 中保存的 `blobId` 和 IndexedDB 中的键名一致。

---

## 修复后的完整流程

### 1. 保存流程（`handleAssetGenerated`）

```typescript
// 1. 调用 saveAssetHistoryImageWithRef
const result = await saveAssetHistoryImageWithRef(assetId, src);
//    ↓
//    内部生成 blobId（如 'blob-abc123'）
//    保存到 IndexedDB（键名：'blob-abc123'）
//    添加引用计数（'blob-abc123' <- 'asset-history-a-123'）
//    返回 { blobUrl, blobId: 'blob-abc123' }

// 2. 保存到 Store
addHistory({ 
  id: assetId, 
  src: result.blobUrl,
  blobId: result.blobId, // ✅ 'blob-abc123'（和 IndexedDB 键名一致）
  // ...
});
```

### 2. 恢复流程（`restoreHistoryImages`）

```typescript
// 1. 从 Store 获取历史记录
const assets = useAssetHistoryStore.getState().assetHistory;

// 2. 遍历恢复
for (const asset of assets) {
  // 3. 使用 blobId 从 IndexedDB 加载
  const blob = await loadFromStorage<Blob>(asset.blobId); // ✅ 'blob-abc123'
  
  // 4. 创建新的 Blob URL
  const newBlobUrl = URL.createObjectURL(blob);
  
  // 5. 更新 Store
  updateAssetSrc(asset.id, newBlobUrl);
}
```

---

## 验证步骤

### 步骤 1：清空旧数据（可选）

如果之前有旧数据，可能需要清空：

```javascript
// 打开浏览器控制台
localStorage.clear();
indexedDB.deleteDatabase('sunstudio-storage');
```

### 步骤 2：生成新图片

1. 在画布上生成一张图片（如文字节点 → 生成图片）
2. 打开浏览器控制台，查看日志：
   ```
   [AssetHistory] 历史记录已保存（引用计数）{ assetId: 'a-123', blobId: 'blob-abc123', ... }
   [AssetHistory] 历史记录已添加 { assetId: 'a-123', blobId: 'blob-abc123', ... }
   ```

### 步骤 3：刷新页面

1. 按 F5 或 Ctrl+R 刷新页面
2. 打开浏览器控制台，查看日志：
   ```
   [AssetHistory] 开始恢复历史记录图片: 1 个
   [BlobStorage] 节点图片已恢复: blob-abc123, 大小: 123.45KB
   [AssetHistory] 图片已恢复（引用计数）: a-123 -> blob-abc123
   [AssetHistory] 历史记录图片恢复完成
   ```

### 步骤 4：检查历史记录面板

1. 打开历史记录面板
2. ✅ 预期：图片正常显示
3. ❌ 如果图片消失，检查控制台是否有错误

---

## 技术细节

### 为什么会出现 `blobId` 不一致？

**原因**：`generateBlobId` 使用 SHA-256 哈希计算 Blob ID，但：

1. **异步操作**：`generateBlobId` 是异步函数，每次调用都会重新计算哈希
2. **Blob 数据可能不同**：
   - 第一次调用：使用原始 `src`（可能是 Blob URL）
   - 第二次调用：使用 `imageUrl`（可能已经转换为 Blob）
3. **结果**：两次生成的 `blobId` 可能不同

### 为什么修改后就一致了？

**原因**：只调用一次 `generateBlobId`，并将结果返回给调用方：

1. **单一来源**：`saveAssetHistoryImageWithRef` 内部生成 `blobId`
2. **返回给调用方**：调用方使用返回的 `blobId` 保存到 Store
3. **结果**：Store 中的 `blobId` 和 IndexedDB 中的键名完全一致

---

## 总结

### 修复内容

1. ✅ 修改 `saveAssetHistoryImageWithRef` 返回 `{ blobUrl, blobId }`
2. ✅ 修改 `handleAssetGenerated` 使用返回的 `blobId`

### 修复结果

- ✅ 刷新后历史记录图片正常显示
- ✅ `blobId` 一致性得到保证
- ✅ 引用计数系统正常工作

### 下一步

- 继续实施 Phase 3：节点删除集成（删除节点后，历史记录里的图片还在）

---

**修复完成！现在刷新页面后，历史记录里的图片应该能正常显示了。**
