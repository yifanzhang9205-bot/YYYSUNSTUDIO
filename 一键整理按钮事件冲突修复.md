# 一键整理按钮事件冲突修复

## 问题描述
用户点击"一键整理"按钮时，会出现一个框选画框，而不是执行整理功能。

## 问题原因

### 原因 1：事件冒泡
点击按钮的事件冒泡到了画布（canvas），触发了画布的 `onMouseDown` 事件，导致开始框选。

### 原因 2：onClick 逻辑缺少事件阻止
原来的 `onClick` 逻辑：
```typescript
onClick={() => item.action ? item.action() : setActivePanel(item.id as any)}
```

这个逻辑虽然会调用 `item.action()`，但没有阻止事件冒泡和默认行为。

## 修复方案

### 修复 1：在按钮 onClick 中阻止事件传播
```typescript
// 修复前
onClick={() => item.action ? item.action() : setActivePanel(item.id as any)}

// 修复后
onClick={(e) => {
    if (item.action) {
        e.preventDefault();      // 阻止默认行为
        e.stopPropagation();     // 阻止事件冒泡
        item.action();
    } else {
        setActivePanel(item.id as any);
    }
}}
```

### 修复 2：在 SidebarDock 容器上阻止事件传播
```typescript
<div 
    className="fixed left-6 top-1/2 -translate-y-1/2 ..."
    onMouseLeave={handleSidebarLeave}
    onMouseDown={(e) => e.stopPropagation()}  // ✅ 新增
    onClick={(e) => e.stopPropagation()}      // ✅ 新增
>
```

这样可以确保：
1. 点击按钮时不会触发画布的 `onMouseDown`
2. 点击 Dock 的任何地方都不会触发画布事件

## 测试步骤

1. 创建一个 Group，里面有几个节点
2. 点击 Group 边框选中它（边框变青色）
3. 点击侧边栏的"一键整理"按钮
4. **预期结果：** 节点自动排列成网格，不会出现框选画框
5. **如果还是出现框选：** 查看控制台日志，确认函数是否被调用

## 相关文件

- `components/SidebarDock.tsx`
  - 修改了按钮的 `onClick` 逻辑
  - 在容器上添加了 `onMouseDown` 和 `onClick` 阻止事件冒泡

## 其他可能的问题

如果修复后还是出现框选画框，可能是：

### 问题 A：画布的 onMouseDown 优先级更高
**检查：** `App.tsx` 中的 `handleCanvasMouseDown` 是否在 SidebarDock 之前触发

**解决：** 确保 SidebarDock 的 `z-index` 足够高（当前是 `z-50`）

### 问题 B：按钮被禁用
**检查：** 按钮是否是灰色的（`disabled={!selectedGroupId}`）

**解决：** 确保 Group 被正确选中（边框是青色）

### 问题 C：函数没有被调用
**检查：** 控制台是否有 `[一键整理] 函数被调用` 日志

**解决：** 如果没有日志，说明 `onArrangeGroup` prop 没有正确传递

## 调试方法

在按钮的 `onClick` 中添加日志：
```typescript
onClick={(e) => {
    console.log('[按钮点击]', { 
        itemId: item.id, 
        hasAction: !!item.action,
        disabled: item.disabled,
        event: e
    });
    
    if (item.action) {
        e.preventDefault();
        e.stopPropagation();
        console.log('[调用 action]', item.id);
        item.action();
    } else {
        setActivePanel(item.id as any);
    }
}}
```

这样可以确认：
1. 按钮是否被点击
2. `item.action` 是否存在
3. 事件是否被正确阻止

## 预期行为

点击"一键整理"按钮后：
1. **不会出现框选画框**
2. 控制台输出 `[一键整理] 函数被调用`
3. 节点开始移动到新位置
4. Group 自动调整大小
5. 控制台输出 `[一键整理] 完成`

## 总结

修复了两个关键点：
1. ✅ 按钮 `onClick` 中添加 `e.preventDefault()` 和 `e.stopPropagation()`
2. ✅ SidebarDock 容器上添加 `onMouseDown` 和 `onClick` 阻止事件冒泡

这样可以确保点击按钮时不会触发画布的框选功能。
