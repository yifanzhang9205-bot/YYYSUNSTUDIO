# 数据持久化架构升级 - 完整实施方案

**创建日期**：2026-02-10  
**状态**：待实施  
**优先级**：P0（核心功能）

---

## 📋 问题诊断总结

### 🔴 核心问题

根据需求文档和代码分析，发现以下**致命问题**：

1. **历史记录没有独立存储**
   - 历史记录只保存了 Blob URL（内存层）
   - 没有保存到 IndexedDB（持久化层）
   - 刷新后 Blob URL 失效，历史记录图片全丢失

2. **资产库没有独立存储**
   - 资产库的缩略图引用节点的 Blob URL
   - 当节点被删除时，资产库的缩略图也失效

3. **删除节点时清理了 IndexedDB**
   - 虽然不 revoke Blob URL，但清理了 IndexedDB
   - 导致历史记录和资产库的 Blob URL 变成"空壳"

4. **数据隔离设计没有实现**
   - 需求文档要求：
     - 画布节点：`blob-node-{nodeId}-image`
     - 资产库：`asset-thumbnail-{assetId}`
     - 历史记录：`asset-{assetId}`
   - 但实际代码中，历史记录和画布节点共享同一个 Blob URL

---

## 🎯 实施计划

### 阶段 1：历史记录独立存储（核心，1 小时）

#### 任务 1.1：修改历史记录保存逻辑

**文件**：`hooks/useAssetHistory.ts`

**修改点**：`handleAssetGenerated` 函数

**当前逻辑**（错误）：
```typescript
// 只保存 Blob URL 到 Store（内存层）
addAsset({
  id: assetId,
  src: imageUrl, // Blob URL（刷新后失效）
  type: 'image',
  timestamp: Date.now(),
});
```

**正确逻辑**：
```typescript
// 1. 保存 Blob URL 到 Store（内存层）
addAsset({
  id: assetId,
  src: imageUrl, // Blob URL
  type: 'image',
  timestamp: Date.now(),
});

// 2. 同时保存到 IndexedDB（持久化层）
(async () => {
  try {
    const { saveToStorage } = await import('../services/storage');
    
    // 从 Blob URL 获取 Blob
    const blob = await fetch(imageUrl).then(r => r.blob());
    
    // 保存到 IndexedDB（键名：asset-{assetId}）
    const storageKey = `asset-${assetId}`;
    await saveToStorage(storageKey, blob);
    
    console.log(`[AssetHistory] 图片已保存到 IndexedDB: ${storageKey}, 大小: ${(blob.size / 1024).toFixed(2)}KB`);
  } catch (error) {
    console.error('[AssetHistory] 保存到 IndexedDB 失败:', error);
  }
})();
```

---

#### 任务 1.2：修改历史记录恢复逻辑

**文件**：`hooks/useAssetHistory.ts`

**添加函数**：`restoreHistoryImages`

**逻辑**：
```typescript
/**
 * 恢复历史记录图片（页面加载时调用）
 */
const restoreHistoryImages = useCallback(async () => {
  const assets = useAssetHistoryStore.getState().assets;
  
  console.log(`[AssetHistory] 开始恢复历史记录图片: ${assets.length} 个`);
  
  for (const asset of assets) {
    if (asset.type === 'image' && asset.src.startsWith('blob:')) {
      try {
        // 从 IndexedDB 恢复 Blob
        const storageKey = `asset-${asset.id}`;
        const { loadFromStorage } = await import('../services/storage');
        const blob = await loadFromStorage<Blob>(storageKey);
        
        if (blob) {
          // 创建新的 Blob URL
          const newBlobUrl = URL.createObjectURL(blob);
          
          // 更新 Store
          useAssetHistoryStore.getState().updateAssetSrc(asset.id, newBlobUrl);
          
          console.log(`[AssetHistory] 图片已恢复: ${asset.id}`);
        } else {
          console.warn(`[AssetHistory] 图片未找到: ${asset.id}`);
        }
      } catch (error) {
        console.error(`[AssetHistory] 恢复图片失败: ${asset.id}`, error);
      }
    }
  }
  
  console.log('[AssetHistory] 历史记录图片恢复完成');
}, []);
```

**在 App.tsx 中调用**：
```typescript
// 页面加载时恢复历史记录图片
useEffect(() => {
  restoreHistoryImages();
}, [restoreHistoryImages]);
```

---

#### 任务 1.3：修改历史记录拖拽逻辑

**文件**：`App.tsx` 第 1636-1676 行

**当前逻辑**（错误）：
```typescript
// 从历史记录的 asset-{assetId} 复制到节点的 blob-node-{nodeId}-image
// 但是历史记录本身的 Blob 可能不存在
```

**正确逻辑**：
```typescript
// 历史记录拖拽
if (asset.type === 'image') {
  // 1. 先生成节点 ID
  const newNodeId = `n-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
  
  // 2. 从历史记录的 IndexedDB 读取 Blob
  (async () => {
    try {
      const { loadFromStorage } = await import('./services/storage');
      const storageKey = `asset-${asset.id}`;
      const blob = await loadFromStorage<Blob>(storageKey);
      
      if (blob) {
        // 3. 创建新的 Blob URL
        const newBlobUrl = URL.createObjectURL(blob);
        
        // 4. 保存到节点的 IndexedDB
        const { saveToStorage } = await import('./services/storage');
        const nodeStorageKey = `blob-node-${newNodeId}-image`;
        await saveToStorage(nodeStorageKey, blob);
        
        // 5. 创建节点
        addNode(NodeType.IMAGE_GENERATOR, dropX - 210, dropY - 180, { 
          image: newBlobUrl,
          prompt: '历史记录图片'
        }, newNodeId);
        
        console.log('[App] 历史记录图片复制成功', { nodeId: newNodeId });
      } else {
        console.warn('[App] 历史记录图片未找到', { assetId: asset.id });
      }
    } catch (error) {
      console.error('[App] 复制历史记录图片失败:', error);
    }
  })();
}
```

---

#### 任务 1.4：修改删除节点逻辑

**文件**：`App.tsx` 第 485-540 行

**当前逻辑**（错误）：
```typescript
// 删除节点时，清理 IndexedDB
await deleteNodeImageBlob(id); // 这会删除 blob-node-{id}-image
```

**正确逻辑**：
```typescript
// 删除节点时，只清理节点专用的 IndexedDB
// 不清理历史记录和资产库的 IndexedDB
ids.forEach(async (id) => {
  try {
    const { deleteFromStorage } = await import('./services/storage');
    
    // 只删除节点专用的键
    await deleteFromStorage(`blob-node-${id}-image`);
    
    // 不删除 asset-{id}（历史记录）
    // 不删除 asset-thumbnail-{id}（资产库）
    
    console.log(`[App] 节点 ${id} IndexedDB 已清理`);
  } catch (error) {
    console.error(`[App] 清理节点 ${id} IndexedDB 失败:`, error);
  }
});
```

---

### 阶段 2：资产库独立存储（30 分钟）

#### 任务 2.1：修改资产库创建逻辑

**文件**：`hooks/useAssetLibrary.ts`

**修改点**：`createAsset` 函数

**当前逻辑**（部分正确）：
```typescript
// 已经有保存缩略图的逻辑，但需要确保正确执行
if (thumbnail) {
  (async () => {
    try {
      const { saveAssetThumbnail } = await import('../services/blobStorage');
      await saveAssetThumbnail(asset.id, thumbnail);
      console.log('[useAssetLibrary] 缩略图已保存到 IndexedDB', { assetId: asset.id });
    } catch (error) {
      console.error('[useAssetLibrary] 保存缩略图失败:', error);
    }
  })();
}
```

**确保**：
- 缩略图保存到 `asset-thumbnail-{assetId}`
- 不依赖节点的 Blob URL

---

#### 任务 2.2：修改资产库拖拽逻辑

**文件**：`hooks/useAssetLibrary.ts`

**修改点**：`useAsset` 函数

**当前逻辑**（部分正确）：
```typescript
// 已经有保存节点图片的逻辑
for (const node of newNodes) {
  if (node.data.image && node.data.image.startsWith('blob:')) {
    await saveNodeImageBlob(node.id, node.data.image);
  }
}
```

**确保**：
- 从资产的节点数据中读取图片
- 保存到 `blob-node-{newNodeId}-image`
- 不影响资产库的缩略图

---

### 阶段 3：测试验证（30 分钟）

#### 测试用例 1：普通图片上传

1. 上传一张图片到画布
2. 刷新页面（Ctrl+F5）
3. 检查图片是否保留
4. 检查 IndexedDB 中的键名：`blob-node-{nodeId}-image`

#### 测试用例 2：历史记录拖拽

1. 生成一张图片（添加到历史记录）
2. 刷新页面（Ctrl+F5）
3. 检查历史记录图片是否可见
4. 从历史记录拖拽到画布
5. 刷新页面（Ctrl+F5）
6. 检查画布节点图片是否保留
7. 检查 IndexedDB 中的键名：
   - 历史记录：`asset-{assetId}`
   - 画布节点：`blob-node-{nodeId}-image`

#### 测试用例 3：资产库拖拽

1. 创建一个资产（包含图片节点）
2. 刷新页面（Ctrl+F5）
3. 检查资产库缩略图是否可见
4. 从资产库拖拽到画布
5. 刷新页面（Ctrl+F5）
6. 检查画布节点图片是否保留
7. 检查 IndexedDB 中的键名：
   - 资产库：`asset-thumbnail-{assetId}`
   - 画布节点：`blob-node-{nodeId}-image`

#### 测试用例 4：删除节点后历史记录和资产库仍然可见

1. 生成一张图片（添加到历史记录）
2. 创建一个资产（包含这张图片）
3. 删除画布上的节点
4. 检查历史记录图片是否仍然可见
5. 检查资产库缩略图是否仍然可见
6. 检查 IndexedDB 中的键名：
   - 历史记录：`asset-{assetId}` 仍然存在
   - 资产库：`asset-thumbnail-{assetId}` 仍然存在
   - 画布节点：`blob-node-{nodeId}-image` 已删除

---

## 📝 修改文件清单

### 必须修改的文件

1. **hooks/useAssetHistory.ts**
   - 修改 `handleAssetGenerated`：添加 IndexedDB 保存
   - 添加 `restoreHistoryImages`：恢复历史记录图片
   - 导出 `restoreHistoryImages`

2. **App.tsx**
   - 修改历史记录拖拽逻辑（第 1636-1676 行）
   - 修改删除节点逻辑（第 485-540 行）
   - 添加 `restoreHistoryImages` 调用

3. **hooks/useAssetLibrary.ts**
   - 确认 `createAsset` 的缩略图保存逻辑
   - 确认 `useAsset` 的节点图片保存逻辑

4. **core/stores/assetHistoryStore.ts**
   - 添加 `updateAssetSrc` 方法（用于恢复时更新 Blob URL）

---

## 🚫 禁止事项

1. ❌ 不允许修改 App.tsx 的业务逻辑（只能添加恢复逻辑和修改清理逻辑）
2. ❌ 不允许破坏现有功能
3. ❌ 不允许跨层混用（UI → Hooks → Core）
4. ❌ 不允许使用 Base64 存储
5. ❌ 不允许阻塞 UI
6. ❌ 不允许 revoke 被多处引用的 Blob URL

---

## ✅ 验收标准

- [ ] 普通图片上传 → 刷新 → 图片保留
- [ ] 历史记录拖拽 → 刷新 → 图片保留
- [ ] 资产库拖拽 → 刷新 → 图片保留
- [ ] 删除节点 → 历史记录和资产库仍然可见
- [ ] IndexedDB 键名格式正确
- [ ] 内存占用合理（< 50MB，10 个节点）

---

## 🔄 实施顺序

1. **第一步**：修改 `hooks/useAssetHistory.ts`（历史记录独立存储）
2. **第二步**：修改 `App.tsx`（历史记录拖拽和删除节点）
3. **第三步**：确认 `hooks/useAssetLibrary.ts`（资产库独立存储）
4. **第四步**：测试验证（所有测试用例）

---

**准备开始实施，严格按照需求文档和本方案执行。**
