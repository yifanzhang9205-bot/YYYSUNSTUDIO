# 打组功能 - 永久组保留修复

## 实施时间
2026-02-08

## 🐛 Bug 描述

**问题**：点击"打组"后，取消选择时，组还是会消失

**用户期望**：
1. 框选后 → 创建**临时组**（title = "临时分组"）
2. 点击"打组" → 变成**永久组**（title = "新建分组"）
3. 点击空白处 → **只删除临时组**，**保留永久组**

**实际情况**：
- 点击空白处时，**所有选中的组都被删除**，不管是临时还是永久

---

## 🔍 根本原因

### 问题代码（App.tsx 第 973-978 行）

```typescript
// ❌ 错误：删除所有选中的组
if (selectedGroupId) {
  console.log('[App] 点击空白处，删除临时组', { selectedGroupId });
  deleteGroupFromHook(selectedGroupId); // 删除所有选中的组
  selectGroup(null);
  clearSelection();
}
```

**问题**：
- 代码没有判断组是临时还是永久
- 只要有选中的组，就直接删除
- 导致永久组也被删除

---

## ✅ 修复方案

### 修改后的代码

```typescript
// ✅ 正确：只删除临时组（title === '临时分组'）
if (selectedGroupId) {
  const selectedGroup = groups.find(g => g.id === selectedGroupId);
  if (selectedGroup && selectedGroup.title === '临时分组') {
    console.log('[App] 点击空白处，删除临时组', { selectedGroupId });
    deleteGroupFromHook(selectedGroupId);
  } else {
    console.log('[App] 点击空白处，保留永久组', { selectedGroupId, title: selectedGroup?.title });
  }
  selectGroup(null);
  clearSelection();
}
```

**修复逻辑**：
1. 查找选中的组
2. 判断组的 title 是否为"临时分组"
3. 如果是临时组 → 删除
4. 如果是永久组 → 保留（只取消选中）

---

## 🎯 功能说明

### 完整的用户流程

**1. 框选节点**：
- 用户在画布上拖动鼠标，框选多个节点
- 框选结束后，自动创建**临时组**
- 组的 title 为"临时分组"
- 组框显示透明蓝色边框

**2. 临时组工具栏**：
- 在组框上方显示工具栏
- 显示三个按钮：
  - **"打组"** - 把临时组变成永久组
  - **"资产库"** - 添加到资产库
  - **"排列"** - 自动排列节点

**3. 打组操作**：
- 用户点击"打组"按钮
- 组的 title 从"临时分组"改为"新建分组"
- 组变成**永久组**
- 取消选中

**4. 点击空白处**：
- **临时组**：自动删除，节点保持在新位置
- **永久组**：保留组框，只取消选中

---

## 🎨 视觉效果

### 临时组（点击空白处会删除）
```
┌─────────────────────────────────────┐
│ [打组] [资产库] | [排列]            │  ← 工具栏
└─────────────────────────────────────┘
      ↓
┏━━━━━━━━━━━━━━━━━━━━━┓  ← 临时组框（透明蓝色）
┃  ┌──────┐  ┌──────┐ ┃  title = "临时分组"
┃  │节点1 │  │节点2 │ ┃
┃  └──────┘  └──────┘ ┃
┗━━━━━━━━━━━━━━━━━━━━━┛

点击空白处 → 组框消失 ✓
```

### 永久组（点击空白处会保留）
```
┌─────────────────┐
│ [排列]          │  ← 工具栏（只有排列按钮）
└─────────────────┘
      ↓
┏━━━━━━━━━━━━━━━━━━━━━┓  ← 永久组框
┃  ┌──────┐  ┌──────┐ ┃  title = "新建分组"
┃  │节点1 │  │节点2 │ ┃
┃  └──────┘  └──────┘ ┃
┗━━━━━━━━━━━━━━━━━━━━━┛

点击空白处 → 组框保留 ✓（只取消选中）
```

---

## 🔧 技术实现

### 判断逻辑

```typescript
// 1. 查找选中的组
const selectedGroup = groups.find(g => g.id === selectedGroupId);

// 2. 判断是否是临时组
if (selectedGroup && selectedGroup.title === '临时分组') {
  // 临时组 → 删除
  deleteGroupFromHook(selectedGroupId);
} else {
  // 永久组 → 保留（只取消选中）
  console.log('[App] 点击空白处，保留永久组', { 
    selectedGroupId, 
    title: selectedGroup?.title 
  });
}

// 3. 取消选中
selectGroup(null);
clearSelection();
```

### 标记临时组

**创建时**（`hooks/useGroup.ts`）：
```typescript
const newGroup: Group = {
  id: `g-${Date.now()}`,
  title: '临时分组', // 🔥 标记为临时组
  x: minX - padding,
  y: minY - padding,
  width: (maxX - minX) + padding * 2,
  height: (maxY - minY) + padding * 2,
  nodeIds: selectedNodeIds,
};
```

**打组时**（`App.tsx`）：
```typescript
const handleMakePermanentGroup = useCallback(() => {
  if (!selectedGroupId) return;
  
  // 修改 title，变成永久组
  useGroupStore.getState().updateGroup(selectedGroupId, {
    title: '新建分组' // 🔥 变成永久组
  });
  
  // 取消选中
  selectGroup(null);
}, [selectedGroupId, selectGroup]);
```

---

## ✅ 验收标准

### 功能验收
- [x] 框选后自动创建临时组（title = "临时分组"）
- [x] 点击"打组"后，组变成永久组（title = "新建分组"）
- [x] 点击空白处，临时组被删除
- [x] 点击空白处，永久组被保留（只取消选中）
- [x] 编译无错误

### 用户体验验收
- [x] 临时组和永久组的行为符合预期
- [x] 视觉反馈清晰（临时组有"打组"按钮，永久组没有）
- [x] 操作简单，符合直觉

---

## 📊 测试用例

### 测试用例 1：临时组删除
1. 框选多个节点 → 创建临时组
2. 不点击"打组"
3. 点击画布空白处
4. **预期**：临时组被删除，节点保持在新位置 ✓

### 测试用例 2：永久组保留
1. 框选多个节点 → 创建临时组
2. 点击"打组" → 变成永久组
3. 点击画布空白处
4. **预期**：永久组被保留，只取消选中 ✓

### 测试用例 3：多次框选
1. 框选节点 A → 创建临时组 A
2. 点击空白处 → 临时组 A 被删除
3. 框选节点 B → 创建临时组 B
4. 点击"打组" → 变成永久组 B
5. 点击空白处 → 永久组 B 被保留
6. **预期**：所有行为符合预期 ✓

---

## 🎉 总结

修复了打组功能的关键 Bug：

**问题**：点击空白处时，所有选中的组都被删除

**修复**：只删除临时组（title === '临时分组'），保留永久组

**用户体验**：
- 临时组：快速批量移动，用完即删
- 永久组：长期保存，点击空白处不会删除

**代码质量**：
- 逻辑清晰：通过 title 判断临时/永久
- 易于维护：只修改一处代码
- 符合预期：完全符合用户需求

---

## 📝 文件清单

### 修改文件
- `App.tsx` - 修复 handleCanvasMouseDown 逻辑（第 973-985 行）

### 编译验证
- ✅ `App.tsx` - 无错误

---

**修复完成！现在打组功能完全符合预期了！**
