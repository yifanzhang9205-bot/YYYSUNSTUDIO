# 九宫格节点 - 21:9 修复和 nodeSize Bug 修复

**日期**: 2026-01-31  
**状态**: ✅ 完成  
**文件**: `hooks/useNodeActions.ts`, `core/registry/NodeRegistry.ts`

---

## 🐛 问题描述

### 问题 1：右键创建的九宫格节点不是 21:9 比例
- **现象**：右键菜单创建的九宫格节点是 420×480（方形）
- **原因**：NodeRegistry 中的默认尺寸设置错误
- **影响**：用户手动创建的节点比例不正确

### 问题 2：3D 相机自动生成九宫格失败
- **现象**：显示错误 "nodeSize is not defined"
- **原因**：代码中定义了 `gridNodeWidth` 和 `gridNodeHeight`，但使用的是 `nodeSize.width` 和 `nodeSize.height`
- **影响**：3D 相机无法自动创建九宫格节点，必须手动连接

---

## 🔧 修复内容

### 修复 1：NodeRegistry 默认尺寸（core/registry/NodeRegistry.ts）

**修改前**：
```typescript
{
  type: NodeType.GRID_SPLITTER,
  name: '九宫格处理',
  iconName: 'Grid3X3',
  defaultWidth: 420,   // ❌ 方形
  defaultHeight: 480,  // ❌ 方形
  defaultData: {
    gridImages: [],
    selectedGridIndex: undefined,
  },
  category: 'advanced',
  description: '自动切割和选择九宫格图片',
},
```

**修改后**：
```typescript
{
  type: NodeType.GRID_SPLITTER,
  name: '九宫格处理',
  iconName: 'Grid3X3',
  defaultWidth: 630,   // ✅ 21:9 比例
  defaultHeight: 270,  // ✅ 21:9 比例
  defaultData: {
    gridImages: [],
    selectedGridIndex: undefined,
  },
  category: 'advanced',
  description: '自动切割和选择九宫格图片',
},
```

---

### 修复 2：nodeSize 未定义 Bug（hooks/useNodeActions.ts）

**修改前**：
```typescript
// 3. 🔥 固定 21:9 比例（不依赖图片加载）
const gridNodeWidth = 630;   // ❌ 定义了但没用
const gridNodeHeight = 270;  // ❌ 定义了但没用

console.log(`[MultiAngleCamera] 创建九宫格节点，尺寸: ${gridNodeWidth}×${gridNodeHeight}`);

// 4. 计算九宫格节点的位置（防遮挡）
const preferredX = node.x + (node.width || 420) + 300;
const preferredY = node.y + ((gridNodeIndex - 1) * 150);

const existingRects = getAllNodeRects();
const gridNodePos = findNonOverlappingPosition(
    { x: preferredX, y: preferredY },
    { width: nodeSize.width, height: nodeSize.height },  // ❌ nodeSize 未定义
    existingRects,
    { gap: 40 }
);

// 5. 创建九宫格节点
const gridNodeId = `grid-${Date.now()}-${gridNodeIndex}`;
const newGridNode: AppNode = {
    id: gridNodeId,
    type: NodeType.GRID_SPLITTER,
    x: gridNodePos.x,
    y: gridNodePos.y,
    width: nodeSize.width,   // ❌ nodeSize 未定义
    height: nodeSize.height, // ❌ nodeSize 未定义
    title: gridNodeTitle,
    status: NodeStatus.SUCCESS,
    data: {
        inputImage: blobUrls[0],
        croppedImages: [],
        selectedIndex: -1,
    },
    inputs: [node.id],
};
```

**修改后**：
```typescript
// 3. 🔥 固定 21:9 比例（不依赖图片加载）
const nodeSize = {
    width: 630,
    height: 270
};

console.log(`[MultiAngleCamera] 创建九宫格节点，尺寸: ${nodeSize.width}×${nodeSize.height}`);

// 4. 计算九宫格节点的位置（防遮挡）
const preferredX = node.x + (node.width || 420) + 300;
const preferredY = node.y + ((gridNodeIndex - 1) * 150);

const existingRects = getAllNodeRects();
const gridNodePos = findNonOverlappingPosition(
    { x: preferredX, y: preferredY },
    { width: nodeSize.width, height: nodeSize.height },  // ✅ 正确使用
    existingRects,
    { gap: 40 }
);

// 5. 创建九宫格节点
const gridNodeId = `grid-${Date.now()}-${gridNodeIndex}`;
const newGridNode: AppNode = {
    id: gridNodeId,
    type: NodeType.GRID_SPLITTER,
    x: gridNodePos.x,
    y: gridNodePos.y,
    width: nodeSize.width,   // ✅ 正确使用
    height: nodeSize.height, // ✅ 正确使用
    title: gridNodeTitle,
    status: NodeStatus.SUCCESS,
    data: {
        inputImage: blobUrls[0],
        croppedImages: [],
        selectedIndex: -1,
    },
    inputs: [node.id],
};
```

---

## ✅ 修复效果

### 修复前
- ❌ 右键创建的九宫格节点是方形（420×480）
- ❌ 3D 相机自动生成九宫格失败，显示 "nodeSize is not defined"
- ❌ 必须手动连接才能生成九宫格

### 修复后
- ✅ 右键创建的九宫格节点是 21:9 比例（630×270）
- ✅ 3D 相机自动生成九宫格成功
- ✅ 自动连接，自动切割，自动命名

---

## 📊 测试清单

- [x] 右键菜单创建九宫格节点，检查比例是否为 21:9
- [x] 3D 相机生成图片后，检查是否自动创建九宫格节点
- [x] 检查九宫格节点是否自动连接到 3D 相机
- [x] 检查九宫格节点的智能命名是否正确
- [x] 检查九宫格节点的防遮挡布局是否正常
- [x] 编译检查通过，无错误

---

## 🎯 根本原因分析

### 为什么会出现这个 Bug？

1. **变量命名不一致**：
   - 定义了 `gridNodeWidth` 和 `gridNodeHeight`
   - 但使用的是 `nodeSize.width` 和 `nodeSize.height`
   - 导致运行时错误

2. **NodeRegistry 配置错误**：
   - 九宫格节点的默认尺寸是 420×480（方形）
   - 应该是 630×270（21:9）

3. **缺少测试**：
   - 代码修改后没有测试 3D 相机自动生成功能
   - 导致 Bug 进入生产环境

---

## 🚀 预防措施

### 1. 统一变量命名
- 使用对象 `nodeSize = { width, height }` 而不是分开的变量
- 避免命名不一致导致的错误

### 2. 配置集中管理
- 所有节点的默认尺寸都在 NodeRegistry 中定义
- 避免硬编码尺寸

### 3. 完整测试
- 修改代码后必须测试所有相关功能
- 特别是自动化流程（如 3D 相机自动生成）

---

## 📝 总结

这次修复解决了两个关键问题：
1. **右键创建的九宫格节点比例错误** - 修改 NodeRegistry 默认尺寸
2. **3D 相机自动生成失败** - 修复 nodeSize 未定义 Bug

现在九宫格节点在所有场景下都是正确的 21:9 比例，3D 相机也能正常自动生成九宫格节点。

---

**修复完成** ✅
