# 3D 相机输出问题 - 切割失败诊断

**日期**：2026-01-31  
**状态**：🔍 诊断中  
**问题**：九宫格节点开始切割但没有完成

---

## 📋 问题分析

### 从日志发现的问题

**正常流程**：
```
✅ [MultiAngleCamera] 生成完成，输出数据
✅ [MultiAngleCamera] 创建九宫格节点数据
✅ [MultiAngleCamera] 自动创建九宫格节点 "背面-平视-全景"
✅ [Node] 九宫格节点数据: hasInputImage: true
✅ [Node] 九宫格节点输入图片: inputImageSrc: 'blob:http://...'
✅ [GridSplitter] 检测到输入图片，开始切割
```

**异常现象**：
```
❌ [GridSplitter] 切割完成，共 9 张  // 这个日志没有出现！
```

**关键发现**：
1. `[GridSplitter] 检测到输入图片，开始切割` 出现了 **3 次**
2. 但是 `[GridSplitter] 切割完成` **一次都没有出现**
3. 也没有 `[GridSplitter] 切割失败` 的错误日志

---

## 🔍 可能的原因

### 原因 1：切割过程中出错，但错误被吞掉

**症状**：
- 开始切割的日志出现了
- 但是既没有"切割完成"，也没有"切割失败"

**可能性**：
- `cropGridImage` 函数内部出错
- Promise 没有正确 reject
- 错误被 catch 但没有日志

### 原因 2：useEffect 重复触发

**症状**：
- `[GridSplitter] 检测到输入图片，开始切割` 出现了 3 次

**原因**：
- useEffect 的依赖项包含了 `croppedImages`
- 每次 `croppedImages` 变化都会触发 useEffect
- 但是 `croppedImages` 初始值是 `[]`，不是 `undefined`

**代码**：
```typescript
useEffect(() => {
    // 如果已经有切割结果，不再重复切割
    if (croppedImages && croppedImages.length > 0) {
      console.log('[GridSplitter] 已有切割结果，跳过切割');
      return;
    }
    
    // 如果正在处理中，不重复处理
    if (isProcessing) {
      console.log('[GridSplitter] 正在处理中，跳过');
      return;
    }
    
    // 如果有输入图片但没有切割结果，开始切割
    if (inputImage) {
      console.log('[GridSplitter] 检测到输入图片，开始切割');
      setIsProcessing(true);
      
      cropGridImage(inputImage)
        .then(cropped => {
          console.log('[GridSplitter] 切割完成，共', cropped.length, '张');
          onUpdate({ 
            croppedImages: cropped,
            selectedIndex: undefined,
            outputImage: undefined
          });
        })
        .catch(err => {
          console.error('[GridSplitter] 切割失败:', err);
        })
        .finally(() => {
          setIsProcessing(false);
        });
    }
  }, [inputImage, croppedImages, cropGridImage, onUpdate]);
```

**问题**：
- 依赖项包含了 `croppedImages`
- 每次 `croppedImages` 变化都会重新执行 useEffect
- 但是 `croppedImages` 初始值是 `[]`，不是 `undefined`
- 所以条件 `croppedImages && croppedImages.length > 0` 为 `false`
- 会重复开始切割

### 原因 3：cropGridImage 函数有问题

**可能性**：
- 图片加载失败
- Canvas 操作失败
- 图片格式不支持
- 图片尺寸不对

---

## ✅ 已完成的修复

### 1. 添加详细的错误日志

**位置**：`components/GridSplitterNode.tsx` 第 218-235 行

**添加的日志**：
```typescript
console.log('[GridSplitter] 输入图片 URL:', inputImage.substring(0, 100));

.catch(err => {
  console.error('[GridSplitter] 切割失败:', err);
  console.error('[GridSplitter] 错误详情:', {
    message: err.message,
    stack: err.stack,
    inputImage: inputImage?.substring(0, 100),
  });
})
```

**作用**：
- 输出输入图片的 URL（前 100 个字符）
- 输出详细的错误信息（message, stack）
- 帮助定位问题

---

## 🧪 下一步测试

### 步骤 1：清除控制台

1. 打开浏览器开发者工具
2. 清除控制台日志

### 步骤 2：重新生成 3D 相机图片

1. 删除之前创建的九宫格节点
2. 点击 3D 相机的"生成"按钮
3. 等待生成完成

### 步骤 3：查看控制台日志

**预期日志**：

**如果切割成功**：
```
[GridSplitter] 检测到输入图片，开始切割
[GridSplitter] 输入图片 URL: blob:http://localhost:3000/...
[GridSplitter] 切割完成，共 9 张
```

**如果切割失败**：
```
[GridSplitter] 检测到输入图片，开始切割
[GridSplitter] 输入图片 URL: blob:http://localhost:3000/...
[GridSplitter] 切割失败: Error: ...
[GridSplitter] 错误详情: {
    message: "...",
    stack: "...",
    inputImage: "blob:http://..."
}
```

---

## 🔍 可能的错误类型

### 错误 1：图片加载失败

**错误信息**：
```
Failed to load image
```

**原因**：
- Blob URL 无效
- Blob URL 已被撤销
- 图片格式不支持

**解决方案**：
- 检查 Blob URL 是否有效
- 检查 Blob URL 是否在使用前被撤销
- 检查图片格式（应该是 PNG）

---

### 错误 2：Canvas 操作失败

**错误信息**：
```
Cannot read property 'getContext' of null
```

**原因**：
- Canvas 元素创建失败
- Canvas 尺寸过大

**解决方案**：
- 检查 Canvas 创建逻辑
- 检查图片尺寸是否合理

---

### 错误 3：图片尺寸不对

**错误信息**：
```
Image dimensions are not 21:9
```

**原因**：
- 输入图片不是 21:9 比例
- 图片尺寸计算错误

**解决方案**：
- 检查输入图片的尺寸
- 检查 cropGridImage 函数的尺寸验证逻辑

---

## 📝 待确认的问题

1. **为什么 `[GridSplitter] 检测到输入图片，开始切割` 出现了 3 次？**
   - 是 useEffect 重复触发吗？
   - 还是组件重新渲染了 3 次？

2. **为什么没有"切割完成"或"切割失败"的日志？**
   - 是 Promise 没有 resolve/reject 吗？
   - 还是错误被吞掉了？

3. **cropGridImage 函数内部发生了什么？**
   - 需要查看 cropGridImage 函数的实现
   - 需要添加更详细的日志

---

## 🎯 下一步行动

1. **用户测试**：请用户重新生成一次 3D 相机图片，并提供控制台日志
2. **分析错误**：根据错误日志确定问题所在
3. **修复问题**：根据诊断结果修复问题
4. **验证修复**：再次测试确认问题解决

---

**状态**：✅ 错误日志添加完成，等待用户测试验证
