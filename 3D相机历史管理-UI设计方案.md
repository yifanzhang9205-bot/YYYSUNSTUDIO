# 3D相机历史管理 - UI 设计方案

**日期**: 2026-01-30  
**目标**: 设计一个好看、自然、好用的历史管理界面

---

## 🎨 设计灵感来源

### **参考产品**
1. **Apple Photos** - 照片网格、选中状态、快速预览
2. **Figma** - 版本历史、时间轴、对比视图
3. **Midjourney** - 图片网格、放大查看、变体生成
4. **Linear** - 极简设计、流畅动画、清晰层级

### **设计原则**
- ✨ **极简主义**：只显示必要信息，减少视觉噪音
- 🎯 **清晰层级**：主要操作突出，次要操作隐藏
- 🌊 **流畅动画**：所有状态变化都有过渡动画
- 👆 **直观交互**：符合用户直觉，不需要学习

---

## 📐 整体布局设计

### **方案 A：底部抽屉式（推荐）** ⭐⭐⭐⭐⭐

```
┌─────────────────────────────────────────────────────┐
│  3D 相机节点                                         │
├─────────────────────────────────────────────────────┤
│                                                      │
│                                                      │
│              [3D 预览区域]                           │
│              - 拖动调整角度                          │
│              - 实时预览                              │
│                                                      │
│                                                      │
├─────────────────────────────────────────────────────┤
│  [生成] [历史 (3)] ← 点击展开/收起                   │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐   │
│  │ 生成历史 (可展开/收起，默认收起)             │   │
│  │ ┌─────┬─────┬─────┬─────┬─────┬─────┐     │   │
│  │ │  1  │  2  │  3  │  4  │  5  │  6  │ ... │   │
│  │ │ ✓   │     │     │     │     │     │     │   │
│  │ │正面 │侧面 │背面 │45° │俯视 │仰视 │     │   │
│  │ └─────┴─────┴─────┴─────┴─────┴─────┘     │   │
│  │ - 单击选中（蓝色边框）                      │   │
│  │ - 双击放大查看                              │   │
│  │ - 右键菜单：导出/删除/重新生成              │   │
│  │ [清空历史]                                  │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**优点**：
- ✅ 不占用主要空间（默认收起）
- ✅ 需要时展开（点击"历史"按钮）
- ✅ 类似 Apple Music 的播放列表
- ✅ 适合移动端和桌面端

---

### **方案 B：右侧边栏式**

```
┌─────────────────────────────┬─────────────────┐
│  3D 相机节点                 │  生成历史       │
├─────────────────────────────┤  ┌───┐         │
│                              │  │ 1 │ 正面    │
│                              │  │✓ │         │
│                              │  └───┘         │
│      [3D 预览区域]           │  ┌───┐         │
│                              │  │ 2 │ 侧面    │
│                              │  │   │         │
│                              │  └───┘         │
│                              │  ┌───┐         │
│                              │  │ 3 │ 背面    │
│                              │  │   │         │
│                              │  └───┘         │
├─────────────────────────────┤  ...           │
│  [生成]                      │  [清空]        │
└─────────────────────────────┴─────────────────┘
```

**优点**：
- ✅ 始终可见（不需要展开）
- ✅ 类似 Figma 的版本历史

**缺点**：
- ❌ 占用空间（节点变宽）
- ❌ 移动端不友好

---

### **方案 C：时间轴式（高级）**

```
┌─────────────────────────────────────────────────────┐
│  3D 相机节点                                         │
├─────────────────────────────────────────────────────┤
│              [3D 预览区域]                           │
├─────────────────────────────────────────────────────┤
│  [生成] [历史]                                       │
├─────────────────────────────────────────────────────┤
│  时间轴 ━━●━━━●━━━●━━━●━━━●━━━●━━━>              │
│         1   2   3   4   5   6                       │
│       正面 侧面 背面 45° 俯视 仰视                   │
│                                                      │
│  ┌─────┐  ← 当前选中的版本（大图预览）              │
│  │     │                                             │
│  │  2  │  侧面 · 2026-01-30 14:23                   │
│  │     │  [导出] [删除] [重新生成]                  │
│  └─────┘                                             │
└─────────────────────────────────────────────────────┘
```

**优点**：
- ✅ 时间感强（适合对比版本）
- ✅ 类似 Figma 的版本历史

**缺点**：
- ❌ 复杂度高
- ❌ 占用空间大

---

## 🎯 推荐方案：方案 A（底部抽屉式）

**为什么？**
1. ✅ 空间利用率高（默认收起）
2. ✅ 交互自然（点击展开/收起）
3. ✅ 适合移动端和桌面端
4. ✅ 符合现代设计趋势（Apple、Spotify 都用抽屉式）

---

## 🎨 详细 UI 设计

### **1. 历史按钮（收起状态）**

```
┌─────────────────────────────────────────────────────┐
│  [生成]  [历史 (3) ▼]                                │
└─────────────────────────────────────────────────────┘
```

**设计细节**：
- 按钮样式：iOS 风格，圆角、毛玻璃
- 数字徽章：显示历史数量（如 `(3)`）
- 箭头图标：`▼` 表示可展开，`▲` 表示可收起
- 颜色：
  - 默认：`bg-white/5 text-white/60`
  - 悬停：`bg-white/10 text-white/80`
  - 有历史：`bg-blue-500/20 text-blue-300`（提示用户有新内容）

---

### **2. 历史面板（展开状态）**

```
┌─────────────────────────────────────────────────────┐
│  生成历史 (3)                              [清空] [×]│
├─────────────────────────────────────────────────────┤
│  ┌─────┬─────┬─────┬─────┬─────┬─────┐             │
│  │  1  │  2  │  3  │  4  │  5  │  6  │ ...         │
│  │ ✓   │     │     │     │     │     │             │
│  │正面 │侧面 │背面 │45° │俯视 │仰视 │             │
│  │14:20│14:21│14:22│14:23│14:24│14:25│             │
│  └─────┴─────┴─────┴─────┴─────┴─────┘             │
│  ← 横向滚动（如果超过 6 个）                         │
└─────────────────────────────────────────────────────┘
```

**设计细节**：

#### **面板容器**
- 背景：`bg-black/40 backdrop-blur-xl`（毛玻璃效果）
- 边框：`border border-white/10`
- 圆角：`rounded-xl`
- 内边距：`p-4`
- 动画：展开/收起时有滑动动画（`transition-all duration-300`）

#### **标题栏**
- 文字：`text-[13px] text-white/70 font-medium`
- 右侧按钮：
  - 清空：`text-red-400 hover:text-red-300`
  - 关闭：`text-white/40 hover:text-white/60`

#### **图片网格**
- 布局：横向滚动（`flex overflow-x-auto`）
- 间距：`gap-3`
- 滚动条：自定义样式（细、半透明）

---

### **3. 历史卡片（单个版本）**

```
┌─────────────┐
│             │
│   [图片]    │  ← 九宫格缩略图（3×3）
│             │
├─────────────┤
│ ✓ 正面      │  ← 选中标记 + 角度描述
│ 14:20       │  ← 生成时间
└─────────────┘
```

**设计细节**：

#### **默认状态**
- 尺寸：`w-32 h-40`（宽高比 4:5）
- 背景：`bg-white/5`
- 边框：`border border-white/10`
- 圆角：`rounded-lg`
- 阴影：`shadow-sm`

#### **悬停状态**
- 边框：`border-white/30`
- 阴影：`shadow-lg shadow-white/10`
- 缩放：`scale-105`（轻微放大）
- 过渡：`transition-all duration-200`

#### **选中状态**
- 边框：`border-2 border-blue-500`（蓝色粗边框）
- 背景：`bg-blue-500/10`
- 选中标记：左上角显示 `✓` 图标
  - 背景：`bg-blue-500`
  - 颜色：`text-white`
  - 圆形：`w-6 h-6 rounded-full`

#### **图片区域**
- 九宫格缩略图：显示 3×3 的小图
- 尺寸：`w-full h-24`
- 圆角：`rounded-t-lg`
- 对象适配：`object-cover`

#### **信息区域**
- 背景：`bg-black/20`
- 内边距：`p-2`
- 角度描述：
  - 文字：`text-[11px] text-white/80 font-medium`
  - 图标：显示角度图标（如 `↑` `→` `↓` `←`）
- 时间：
  - 文字：`text-[10px] text-white/40`
  - 格式：`HH:mm` 或 `刚刚`、`1分钟前`

---

### **4. 右键菜单**

```
┌─────────────────────┐
│ 📤 导出为图片节点    │
│ 🔄 重新生成          │
│ 👁️ 放大查看          │
│ ─────────────────   │
│ 🗑️ 删除             │
└─────────────────────┘
```

**设计细节**：
- 背景：`bg-black/90 backdrop-blur-xl`
- 边框：`border border-white/20`
- 圆角：`rounded-lg`
- 阴影：`shadow-2xl`
- 菜单项：
  - 默认：`text-white/70 hover:bg-white/10`
  - 危险操作（删除）：`text-red-400 hover:bg-red-500/10`
  - 图标：`size-14`，左对齐
  - 文字：`text-[13px]`
- 动画：淡入 + 轻微缩放（`animate-in fade-in zoom-in-95`）

---

### **5. 放大查看（Modal）**

```
┌─────────────────────────────────────────────────────┐
│                    [全屏遮罩]                        │
│                                                      │
│  ┌─────────────────────────────────────────────┐   │
│  │                                              │   │
│  │                                              │   │
│  │            [九宫格大图]                      │   │
│  │                                              │   │
│  │                                              │   │
│  ├─────────────────────────────────────────────┤   │
│  │ 正面 · 2026-01-30 14:20                     │   │
│  │ [← 上一个] [导出] [删除] [下一个 →]         │   │
│  └─────────────────────────────────────────────┘   │
│                                                      │
│  点击遮罩或按 ESC 关闭                               │
└─────────────────────────────────────────────────────┘
```

**设计细节**：
- 遮罩：`bg-black/95 backdrop-blur-2xl`
- 图片容器：
  - 最大宽度：`max-w-4xl`
  - 背景：`bg-black/40`
  - 边框：`border border-white/10`
  - 圆角：`rounded-2xl`
- 图片：
  - 对象适配：`object-contain`
  - 最大高度：`max-h-[70vh]`
- 底部信息栏：
  - 背景：`bg-black/60`
  - 内边距：`p-4`
- 按钮：
  - 样式：iOS 风格
  - 间距：`gap-3`
- 键盘快捷键：
  - `ESC`：关闭
  - `←`：上一个
  - `→`：下一个
  - `Delete`：删除

---

## 🎬 动画设计

### **1. 展开/收起动画**

```css
/* 展开 */
.history-panel-enter {
  max-height: 0;
  opacity: 0;
  transform: translateY(-10px);
}
.history-panel-enter-active {
  max-height: 300px;
  opacity: 1;
  transform: translateY(0);
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

/* 收起 */
.history-panel-exit {
  max-height: 300px;
  opacity: 1;
  transform: translateY(0);
}
.history-panel-exit-active {
  max-height: 0;
  opacity: 0;
  transform: translateY(-10px);
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
}
```

### **2. 卡片悬停动画**

```css
.history-card {
  transition: all 200ms cubic-bezier(0.4, 0, 0.2, 1);
}
.history-card:hover {
  transform: scale(1.05) translateY(-2px);
  box-shadow: 0 10px 30px rgba(255, 255, 255, 0.1);
}
```

### **3. 选中动画**

```css
.history-card-selected {
  animation: select-pulse 300ms cubic-bezier(0.4, 0, 0.2, 1);
}
@keyframes select-pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.05); }
  100% { transform: scale(1); }
}
```

### **4. 新增卡片动画**

```css
.history-card-new {
  animation: slide-in-right 400ms cubic-bezier(0.4, 0, 0.2, 1);
}
@keyframes slide-in-right {
  0% {
    opacity: 0;
    transform: translateX(20px);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}
```

---

## 🎯 交互流程

### **流程 1：生成新版本**

```
1. 用户调整角度 → 点击"生成"
   ↓
2. 生成中：按钮显示加载动画
   ↓
3. 生成完成：
   - 新卡片从右侧滑入（slide-in-right）
   - 自动选中新卡片（蓝色边框）
   - 历史按钮数字 +1
   - 如果历史面板是收起的，按钮闪烁提示（pulse）
   ↓
4. 用户可以：
   - 点击其他卡片切换选中
   - 双击卡片放大查看
   - 右键卡片打开菜单
```

### **流程 2：查看历史**

```
1. 用户点击"历史 (3)"按钮
   ↓
2. 历史面板从底部滑出（slide-up）
   ↓
3. 显示所有历史卡片（横向滚动）
   ↓
4. 用户可以：
   - 单击卡片选中（蓝色边框 + pulse 动画）
   - 双击卡片放大查看（Modal）
   - 右键卡片打开菜单
   - 点击"清空"删除所有历史（需要确认）
   - 点击"×"或再次点击"历史"按钮收起面板
```

### **流程 3：导出为图片节点**

```
1. 用户右键卡片 → 点击"导出为图片节点"
   ↓
2. 在画布上自动创建新的图片节点
   - 位置：3D 相机节点右侧（自动排列）
   - 内容：选中的九宫格图片
   - 连接：自动连接到 3D 相机节点的输出端口
   ↓
3. 卡片显示"已导出"标记（小图标）
   ↓
4. 用户可以继续导出其他版本
```

### **流程 4：删除版本**

```
1. 用户右键卡片 → 点击"删除"
   ↓
2. 确认对话框（Toast）：
   "确定删除这个版本吗？"
   [取消] [删除]
   ↓
3. 用户点击"删除"：
   - 卡片淡出 + 缩小（fade-out + scale-out）
   - 其他卡片平滑移动填补空位
   - 历史按钮数字 -1
   ↓
4. 如果删除的是选中的版本：
   - 自动选中下一个版本（或上一个）
   - 如果没有其他版本，清空选中状态
```

---

## 🎨 颜色方案

### **深色主题（当前）**

| 元素 | 颜色 | 说明 |
|------|------|------|
| **背景** | `#0a0a0a` | 节点背景 |
| **面板背景** | `rgba(0,0,0,0.4)` + `backdrop-blur-xl` | 毛玻璃效果 |
| **边框** | `rgba(255,255,255,0.1)` | 默认边框 |
| **边框（悬停）** | `rgba(255,255,255,0.3)` | 悬停边框 |
| **边框（选中）** | `#3b82f6` | 蓝色选中边框 |
| **文字（主要）** | `rgba(255,255,255,0.8)` | 主要文字 |
| **文字（次要）** | `rgba(255,255,255,0.6)` | 次要文字 |
| **文字（提示）** | `rgba(255,255,255,0.4)` | 提示文字 |
| **按钮（主要）** | `#ffffff` | 生成按钮 |
| **按钮（次要）** | `rgba(255,255,255,0.05)` | 历史按钮 |
| **危险操作** | `#ef4444` | 删除按钮 |

---

## 📱 响应式设计

### **桌面端（>= 1024px）**
- 历史卡片：`w-32 h-40`
- 每行显示：6-8 个
- 横向滚动

### **平板端（768px - 1023px）**
- 历史卡片：`w-28 h-36`
- 每行显示：4-6 个
- 横向滚动

### **移动端（< 768px）**
- 历史卡片：`w-24 h-32`
- 每行显示：3-4 个
- 横向滚动
- 放大查看改为全屏

---

## 🎯 可访问性

### **键盘导航**
- `Tab`：在卡片之间切换焦点
- `Enter`：选中当前焦点的卡片
- `Space`：放大查看当前焦点的卡片
- `Delete`：删除当前焦点的卡片
- `←` `→`：在放大查看模式下切换版本
- `ESC`：关闭放大查看或右键菜单

### **屏幕阅读器**
- 卡片：`aria-label="版本 1，正面，生成于 14:20"`
- 选中状态：`aria-selected="true"`
- 按钮：`aria-label="展开历史，当前有 3 个版本"`

### **焦点指示**
- 焦点边框：`ring-2 ring-blue-500 ring-offset-2 ring-offset-black`

---

## 🚀 实施计划

### **阶段 1：基础功能（2 小时）**
1. 添加历史按钮（收起/展开）
2. 添加历史面板（底部抽屉）
3. 添加历史卡片（网格布局）
4. 实现选中状态

### **阶段 2：交互优化（1 小时）**
1. 添加展开/收起动画
2. 添加卡片悬停/选中动画
3. 添加新增卡片动画
4. 实现横向滚动

### **阶段 3：高级功能（1 小时）**
1. 添加右键菜单
2. 添加放大查看（Modal）
3. 添加导出功能
4. 添加删除功能

### **阶段 4：细节打磨（30 分钟）**
1. 优化动画曲线
2. 添加键盘快捷键
3. 添加可访问性支持
4. 响应式适配

**总计**：约 4.5 小时

---

## 🎨 视觉效果预览

### **收起状态（默认）**
```
┌─────────────────────────────────────────────────────┐
│  3D 相机节点                                         │
│  ┌─────────────────────────────────────────────┐   │
│  │                                              │   │
│  │           [3D 预览区域]                      │   │
│  │                                              │   │
│  └─────────────────────────────────────────────┘   │
│  [生成]  [历史 (3) ▼]                               │
└─────────────────────────────────────────────────────┘
```

### **展开状态**
```
┌─────────────────────────────────────────────────────┐
│  3D 相机节点                                         │
│  ┌─────────────────────────────────────────────┐   │
│  │           [3D 预览区域]                      │   │
│  └─────────────────────────────────────────────┘   │
│  [生成]  [历史 (3) ▲]                               │
│  ┌─────────────────────────────────────────────┐   │
│  │ 生成历史 (3)                      [清空] [×]│   │
│  │ ┌───┬───┬───┬───┬───┬───┐                 │   │
│  │ │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │ ...             │   │
│  │ │ ✓ │   │   │   │   │   │                 │   │
│  │ └───┴───┴───┴───┴───┴───┘                 │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

---

## 💡 额外的 UX 优化建议

### **1. 智能提示**
- 第一次生成后，历史按钮闪烁 + 显示提示气泡："点击查看历史版本"
- 生成 3 个版本后，提示："可以右键导出为图片节点"

### **2. 快速操作**
- 长按卡片：显示快速操作菜单（导出、删除）
- 拖拽卡片：拖到画布上自动创建图片节点

### **3. 批量操作**
- Shift + 点击：多选卡片
- 多选后显示批量操作栏："导出 (3)"、"删除 (3)"

### **4. 对比模式**
- 点击"对比"按钮：并排显示 2-4 个版本
- 方便用户选择最佳版本

### **5. 自动保存**
- 历史记录自动保存到 IndexedDB
- 刷新页面后仍然保留

---

**这个设计方案够详细吗？你觉得哪些地方需要调整？** 🎨
