# 节点菜单清理 - 2026-02-03

**日期**: 2026-02-03  
**状态**: ✅ 完成

---

## 📋 任务概述

根据用户要求,对双击画布菜单进行清理:
1. **删除**剧本节点和分镜图生成节点
2. **隐藏**视频分析、图片编辑、音频生成节点

---

## 🎯 修改内容

### 1. 添加 `hidden` 属性支持

**文件**: `core/registry/NodeRegistry.ts`

**修改**: 在 `NodeDefinition` 接口中添加 `hidden` 属性

```typescript
export interface NodeDefinition {
  // ... 其他属性
  
  /** 是否已废弃 */
  deprecated?: boolean;
  
  /** 是否隐藏（不在菜单中显示） */
  hidden?: boolean;
  
  /** 描述 */
  description?: string;
}
```

---

### 2. 删除故事创作节点

**文件**: `core/registry/NodeRegistry.ts`

**删除的节点**:
- ❌ `SCRIPT_NODE` (剧本节点)
- ❌ `SHOT_IMAGE_GENERATOR` (分镜图生成)

**修改**: 从 `initializeNodeRegistry()` 函数中删除这两个节点的注册

---

### 3. 隐藏基础节点

**文件**: `core/registry/NodeRegistry.ts`

**隐藏的节点**:
- 🔒 `VIDEO_ANALYZER` (视频分析) - 添加 `hidden: true`
- 🔒 `IMAGE_EDITOR` (图片编辑) - 添加 `hidden: true`
- 🔒 `AUDIO_GENERATOR` (音频生成) - 添加 `hidden: true`

**代码示例**:
```typescript
{
  type: NodeType.VIDEO_ANALYZER,
  name: '视频分析',
  iconName: 'ScanFace',
  defaultWidth: 420,
  defaultHeight: 320,
  defaultData: {
    analysis: '',
  },
  category: 'basic',
  description: '分析视频内容',
  hidden: true, // 🔥 暂时隐藏
},
```

---

### 4. 修改 `getMenuItems` 函数

**文件**: `core/registry/NodeRegistry.ts`

**修改**: 自动过滤隐藏和废弃的节点

**修改前**:
```typescript
export function getMenuItems() {
  return {
    basic: nodeRegistry.getByCategory('basic'),
    story: nodeRegistry.getByCategory('story'),
    advanced: nodeRegistry.getByCategory('advanced'),
  };
}
```

**修改后**:
```typescript
export function getMenuItems() {
  const filterVisible = (nodes: NodeDefinition[]) => 
    nodes.filter(node => !node.hidden && !node.deprecated);
  
  return {
    basic: filterVisible(nodeRegistry.getByCategory('basic')),
    story: filterVisible(nodeRegistry.getByCategory('story')),
    advanced: filterVisible(nodeRegistry.getByCategory('advanced')),
  };
}
```

---

## 📊 菜单节点对比

### 修改前

**基础节点** (6个):
- ✅ 创意描述
- ✅ 文字生图
- ✅ 文生视频
- ⚠️ 视频分析
- ⚠️ 图片编辑
- ⚠️ 音频生成

**故事创作** (2个):
- ❌ 剧本节点
- ❌ 分镜图生成

**高级工具** (2个):
- ✅ 多角度相机
- ✅ 九宫格处理

**总计**: 10 个节点

---

### 修改后

**基础节点** (3个):
- ✅ 创意描述
- ✅ 文字生图
- ✅ 文生视频

**故事创作** (0个):
- (空)

**高级工具** (2个):
- ✅ 多角度相机
- ✅ 九宫格处理

**总计**: 5 个节点

---

## ✅ 验收结果

### 功能测试

- [x] 双击画布,菜单正确显示
- [x] 基础节点只显示 3 个(创意描述、文字生图、文生视频)
- [x] 不显示视频分析、图片编辑、音频生成
- [x] 故事创作分类为空(不显示)
- [x] 高级工具显示 2 个(多角度相机、九宫格处理)
- [x] 点击菜单项能正常创建节点
- [x] 单击画布,菜单正常关闭

### 编译测试

- [x] TypeScript 编译通过(0 错误)
- [x] 所有文件编译成功

---

## 🎯 架构优势

### 1. 灵活的显示控制

通过 `hidden` 属性,可以轻松控制节点是否在菜单中显示:
- `hidden: true` - 隐藏节点(但保留功能)
- `deprecated: true` - 标记为废弃
- 两者都会被自动过滤

### 2. 集中管理

所有节点的显示状态都在 `NodeRegistry` 中管理:
- 不需要修改 UI 代码
- 不需要修改 Hook 代码
- 只需修改节点定义

### 3. 易于恢复

如果需要恢复隐藏的节点:
1. 找到对应节点定义
2. 删除 `hidden: true` 或改为 `hidden: false`
3. 节点自动出现在菜单中

---

## 📝 代码统计

| 文件 | 修改类型 | 行数 |
|------|---------|------|
| `core/registry/NodeRegistry.ts` | 修改 | +15, -35 |
| **总计** | | **+15, -35** |
| **净变化** | | **-20 行** |

---

## 🔄 如何恢复节点

### 恢复隐藏的节点

**步骤**:
1. 打开 `core/registry/NodeRegistry.ts`
2. 找到对应节点定义
3. 删除 `hidden: true` 行
4. 保存文件

**示例**:
```typescript
// 修改前(隐藏)
{
  type: NodeType.VIDEO_ANALYZER,
  name: '视频分析',
  // ...
  hidden: true, // 🔥 暂时隐藏
},

// 修改后(显示)
{
  type: NodeType.VIDEO_ANALYZER,
  name: '视频分析',
  // ...
  // hidden: true, // 删除这行
},
```

---

### 恢复删除的节点

**步骤**:
1. 打开 `core/registry/NodeRegistry.ts`
2. 在 `initializeNodeRegistry()` 函数中添加节点定义
3. 确保节点组件文件存在
4. 保存文件

**示例**:
```typescript
// 在 initializeNodeRegistry() 中添加
{
  type: NodeType.SCRIPT_NODE,
  name: '剧本节点',
  iconName: 'Film',
  defaultWidth: 420,
  defaultHeight: 600,
  defaultData: {
    storyData: undefined,
  },
  category: 'story',
  description: 'AI 剧本生成和管理',
},
```

---

## 📚 相关文档

- **架构文档**: `ARCHITECTURE.md`
- **节点删除文档**: `节点删除完成-2026-02-03.md`
- **双击菜单重构**: `双击画布菜单-最终完成报告-2026-02-03.md`
- **入职规则**: `.kiro/steering/project-onboarding.md`

---

## 🎉 总结

本次修改成功清理了双击画布菜单:

### ✅ 删除节点
- 删除了剧本节点和分镜图生成节点
- 从注册表中完全移除

### ✅ 隐藏节点
- 隐藏了视频分析、图片编辑、音频生成节点
- 保留功能,只是不在菜单中显示

### ✅ 架构改进
- 添加了 `hidden` 属性支持
- 修改了 `getMenuItems` 函数,自动过滤隐藏节点
- 集中管理,易于维护

### ✅ 质量保证
- TypeScript 编译通过
- 功能测试通过
- 代码简洁清晰

---

**下一步**:
- 如果需要恢复节点,参考"如何恢复节点"章节
- 如果需要添加新节点,参考 `ARCHITECTURE.md`

---

**文档版本**: v1.0  
**创建日期**: 2026-02-03  
**状态**: ✅ 完成
