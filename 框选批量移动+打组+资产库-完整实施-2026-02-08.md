# 框选批量移动 + 打组 + 资产库 - 完整实施

**日期**: 2026-02-08  
**状态**: ✅ 完成  

---

## 📋 用户需求

**用户的真实需求**：
1. 框选后 → 显示临时组框（透明蓝色）
2. 在组框上显示两个按钮：
   - **"打组"按钮** → 把临时组变成永久组
   - **"添加到资产库"按钮** → 把选中的节点保存为资产
3. 拖动组框 → 所有节点一起移动
4. 点击空白处 → 自动取消临时组

---

## ✅ 实施内容

### 1. 修改 `components/GroupToolbar.tsx`

**新增 Props**：
```typescript
interface GroupToolbarProps {
  // ... 原有 props
  onMakePermanent?: () => void; // 把临时组变成永久组
  onAddToAssetLibrary?: () => void; // 添加到资产库
  isTemporary?: boolean; // 是否是临时组
}
```

**新增按钮**：
- 当 `isTemporary === true` 时，显示"打组"和"资产库"按钮
- 使用 `Package` 和 `FolderHeart` 图标
- 按钮之间有分隔线

### 2. 修改 `App.tsx`

**新增回调函数**：

1. `handleMakePermanentGroup`: 把临时组变成永久组（修改 title）
2. `handleAddGroupToAssetLibrary`: 显示创建资产对话框

**传递给 GroupToolbar**：
- `isTemporary`: 判断组的 title 是否为"临时分组"
- `onMakePermanent`: 打组回调
- `onAddToAssetLibrary`: 添加到资产库回调

### 3. 修改 `hooks/useGroup.ts`

**标记临时组**：
- `expandOrCreateGroup` 创建的组，title 设置为"临时分组"
- 用户点击"打组"后，title 改为"新建分组"

---

## 🎯 功能说明

### 用户操作流程

**1. 框选节点**：
- 用户在画布上拖动鼠标，框选多个节点
- 框选结束后，自动创建临时组（透明蓝色边框）
- 组的 title 为"临时分组"

**2. 临时组工具栏**：
- 在组框上方显示工具栏
- 显示三个按钮：
  - **"打组"** - 把临时组变成永久组
  - **"资产库"** - 添加到资产库
  - **"自动排列"** - 自动排列节点

**3. 打组操作**：
- 用户点击"打组"按钮
- 组的 title 从"临时分组"改为"新建分组"
- 组变成永久组，不会被自动删除
- 取消选中

**4. 添加到资产库**：
- 用户点击"资产库"按钮
- 显示创建资产对话框
- 用户输入资产名称和分类
- 保存为资产

**5. 取消临时组**：
- 用户点击画布空白处
- 自动删除临时组
- 节点保持在新位置

---

## 🎨 视觉效果

### 临时组工具栏
```
┌─────────────────────────────────────┐
│ [打组] [资产库] | [自动排列]        │  ← 工具栏
└─────────────────────────────────────┘
      ↓
┏━━━━━━━━━━━━━━━━━━━━━┓  ← 临时组框（透明蓝色）
┃  ┌──────┐  ┌──────┐ ┃
┃  │节点1 │  │节点2 │ ┃
┃  └──────┘  └──────┘ ┃
┗━━━━━━━━━━━━━━━━━━━━━┛
```

### 永久组工具栏
```
┌─────────────────┐
│ [自动排列]      │  ← 只显示自动排列
└─────────────────┘
      ↓
┏━━━━━━━━━━━━━━━━━━━━━┓  ← 永久组框
┃  ┌──────┐  ┌──────┐ ┃
┃  │节点1 │  │节点2 │ ┃
┃  └──────┘  └──────┘ ┃
┗━━━━━━━━━━━━━━━━━━━━━┛
```

---

## ✅ 验收标准

### 功能验收
- [x] 框选后自动创建临时组
- [x] 临时组显示"打组"和"资产库"按钮
- [x] 点击"打组"后，组变成永久组
- [x] 点击"资产库"后，显示创建资产对话框
- [x] 点击空白处，自动删除临时组
- [x] 永久组只显示"自动排列"按钮
- [x] 编译无错误

### 用户体验验收
- [x] 操作简单，符合直觉
- [x] 视觉反馈清晰
- [x] 按钮位置合理

---

## 🎉 总结

完成了框选批量移动 + 打组 + 资产库的完整功能！

**用户体验**：
- 框选后自动创建临时组 → 直观
- 工具栏显示"打组"和"资产库"按钮 → 方便
- 点击空白处自动取消临时组 → 符合习惯

**代码质量**：
- 复用现有的组拖动逻辑 → 简洁
- 通过 title 标记临时组 → 简单
- 遵守架构规则 → 可维护
