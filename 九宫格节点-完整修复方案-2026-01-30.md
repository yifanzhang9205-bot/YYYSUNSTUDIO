# 九宫格节点 - 完整修复方案 - 2026-01-30

## 📋 需要修复的问题

### 问题 1: 节点默认比例不对
- **现状**: 节点默认是方形（420×300）
- **期望**: 节点默认是 21:9 长方形
- **原因**: `useNodeActions.ts` 中创建节点时使用了自动检测尺寸，但图片还没加载完成

### 问题 2: 上传替换功能未实现
- **现状**: 上传按钮（Upload 图标）没有功能
- **期望**: 点击可以重新上传图片，替换当前图片

### 问题 3: 全屏功能未实现
- **现状**: 全屏按钮（Maximize 图标）没有功能
- **期望**: 点击可以全屏查看当前图片

---

## 🎯 解决方案

### 方案 1: 修复节点默认比例

**问题分析:**
- 当前代码使用 `autoCalculateNodeSize` 检测图片尺寸
- 但图片是异步加载的，创建节点时图片还没加载完成
- 导致无法获取正确的尺寸

**解决方案:**
- 直接使用固定的 21:9 比例
- 宽度 630px，高度 270px（630 × 9 / 21 = 270）
- 不依赖图片加载

**修改文件:**
- `hooks/useNodeActions.ts` - 第 750-760 行

---

### 方案 2: 实现上传替换功能

**功能设计:**
1. 点击上传按钮
2. 打开文件选择对话框
3. 选择新图片
4. 替换当前图片
5. 重新切割九宫格

**交互流程:**
```
用户点击上传按钮
  ↓
打开文件选择器
  ↓
用户选择图片
  ↓
显示处理中状态
  ↓
切割九宫格
  ↓
更新显示
```

**修改文件:**
- `components/GridSplitterNode.tsx` - 头部按钮区域

---

### 方案 3: 实现全屏功能

**功能设计:**
1. 点击全屏按钮
2. 创建全屏遮罩层
3. 显示当前图片（九宫格或单图）
4. 点击遮罩或按 ESC 关闭

**交互流程:**
```
用户点击全屏按钮
  ↓
创建全屏遮罩
  ↓
显示图片（充满屏幕）
  ↓
用户点击遮罩 / 按 ESC
  ↓
关闭全屏
```

**技术实现:**
- 使用 React Portal 创建全屏层
- 使用 `position: fixed` 覆盖整个屏幕
- 监听 ESC 键关闭

**修改文件:**
- `components/GridSplitterNode.tsx` - 添加全屏状态和组件

---

## ✅ 实施步骤

### 步骤 1: 修复节点默认比例
```typescript
// hooks/useNodeActions.ts

// 删除自动检测尺寸的代码
// const { autoCalculateNodeSize } = await import('../core/utils/imageUtils');
// const nodeSize = await autoCalculateNodeSize(blobUrls[0], 800, 600);

// 直接使用固定的 21:9 比例
const gridNodeWidth = 630;
const gridNodeHeight = 270;
```

### 步骤 2: 添加上传替换功能
```typescript
// components/GridSplitterNode.tsx

// 添加上传按钮
<button
  onClick={() => fileInputRef.current?.click()}
  onMouseDown={(e) => e.stopPropagation()}
  className="w-7 h-7 rounded-lg bg-white/5 hover:bg-white/10 flex items-center justify-center text-white/40 hover:text-white/80 transition-all"
>
  <Upload size={14} />
</button>
```

### 步骤 3: 添加全屏功能
```typescript
// components/GridSplitterNode.tsx

// 添加全屏状态
const [isFullscreen, setIsFullscreen] = useState(false);

// 添加全屏按钮
<button
  onClick={() => setIsFullscreen(true)}
  onMouseDown={(e) => e.stopPropagation()}
  className="w-7 h-7 rounded-lg bg-white/5 hover:bg-white/10 flex items-center justify-center text-white/40 hover:text-white/80 transition-all"
>
  <Maximize size={14} />
</button>

// 添加全屏组件
{isFullscreen && (
  <div className="fixed inset-0 z-50 bg-black/90 flex items-center justify-center"
       onClick={() => setIsFullscreen(false)}>
    <img src={currentImage} className="max-w-full max-h-full" />
  </div>
)}
```

---

## 📊 验收标准

### 功能验收
- [ ] 节点默认创建时是 21:9 比例（630×270）
- [ ] 点击上传按钮可以选择新图片
- [ ] 上传新图片后自动切割九宫格
- [ ] 点击全屏按钮可以全屏查看
- [ ] 全屏时点击遮罩可以关闭
- [ ] 全屏时按 ESC 可以关闭

### 视觉验收
- [ ] 节点比例正确（21:9）
- [ ] 按钮图标清晰可见
- [ ] 按钮 hover 效果正常
- [ ] 全屏显示居中对齐

---

## 🚀 开始实施
