# UI 改版 - 高对比度极简主义风格 - 2026-02-02

## 改版目标

将界面从"React Flow 亮色风格"改为"高对比度极简主义风格"（High Contrast Minimalist），实现最大化的清晰度和专业画廊般的美学。

---

## 设计原则

### 1. 全局风格
- **背景色**：保持 `#F4F6F7` 浅灰背景
- **字体**：改为 `Inter` 或 `Noto Sans SC`（思源黑体）
- **阴影**：从柔和阴影改为锐利硬边阴影 `0 4px 12px rgba(0,0,0,0.15)`
- **边框**：使用细黑边框 `1px solid #000000`
- **圆角**：从 `8px/12px` 改为 `4px/6px`（更方正）

### 2. 颜色系统
- **主色**：纯黑 `#000000`
- **背景**：纯白 `#FFFFFF`
- **画布**：浅灰 `#F4F6F7`
- **文字**：纯黑 `#000000` 或深灰 `#3F3F46`
- **Hover**：浅灰 `#F9FAFB`

### 3. 视觉特点
- **高对比度**：黑白分明，零装饰
- **锐利边缘**：硬边阴影，方正圆角
- **极简主义**：去除所有不必要的装饰
- **建筑感**：清晰、精确、有结构

---

## 具体改动

### 1. 全局字体（App.tsx）

**修改前：**
```tsx
<div className="w-screen h-screen overflow-hidden bg-[#0a0a0c]">
```

**修改后：**
```tsx
<div className="w-screen h-screen overflow-hidden bg-[#0a0a0c] font-['Inter','Noto_Sans_SC',sans-serif]">
```

**说明：**
- 添加全局字体：Inter（英文）+ Noto Sans SC（中文）
- 确保所有文字使用统一字体

---

### 2. 画布背景（App.tsx）

**修改前：**
```tsx
className="text-gray-900 selection:bg-blue-500/30"
backgroundColor: '#F4F6F7'
```

**修改后：**
```tsx
className="text-black selection:bg-black/10"
backgroundColor: '#F4F6F7'
```

**说明：**
- 文字颜色：从 `text-gray-900` 改为 `text-black`
- 选中背景：从 `bg-blue-500/30` 改为 `bg-black/10`
- 画布背景：保持 `#F4F6F7`

---

### 3. 节点样式（Node.tsx）

**修改前：**
```tsx
const GLASS_PANEL = "bg-white border border-gray-200 shadow-sm";
```

**修改后：**
```tsx
const GLASS_PANEL = "bg-white border border-black shadow-[0_4px_12px_rgba(0,0,0,0.15)]";
```

**说明：**
- 背景：保持纯白 `bg-white`
- 边框：从 `border-gray-200` 改为 `border-black`
- 阴影：从 `shadow-sm` 改为 `shadow-[0_4px_12px_rgba(0,0,0,0.15)]`（锐利硬边）

---

### 4. 侧边栏（SidebarDock.tsx）

**修改前：**
```tsx
bg-white/95 backdrop-blur-sm border border-gray-200 rounded-lg shadow-sm
text-gray-600
```

**修改后：**
```tsx
bg-white border border-black rounded-md shadow-[0_4px_12px_rgba(0,0,0,0.15)]
text-black
```

**说明：**
- 背景：从半透明改为纯白
- 边框：从灰色改为黑色
- 圆角：从 `rounded-lg` 改为 `rounded-md`
- 图标：从灰色改为黑色
- 移除：`backdrop-blur-sm`（毛玻璃效果）

---

### 5. 控制栏（App.tsx）

**修改前：**
```tsx
bg-white/95 backdrop-blur-xl border border-gray-200 rounded-full shadow-lg
text-gray-600
bg-blue-500 (滑块)
```

**修改后：**
```tsx
bg-white border border-black rounded-full shadow-[0_4px_12px_rgba(0,0,0,0.15)]
text-black
bg-black (滑块)
```

**说明：**
- 背景：从半透明改为纯白
- 边框：从灰色改为黑色
- 图标和文字：从灰色改为黑色
- 滑块：从蓝色改为黑色
- 移除：`backdrop-blur-xl`

---

### 6. 小地图（Minimap.tsx）

**修改前：**
```tsx
border-gray-200
text-gray-600
```

**修改后：**
```tsx
border-black
text-black
```

**说明：**
- 边框和图标改为黑色

---

### 7. 组工具栏（GroupToolbar.tsx）

**修改前：**
```tsx
bg-white/95 backdrop-blur-sm border border-gray-200 rounded-lg shadow-sm
text-gray-600
```

**修改后：**
```tsx
bg-white border border-black rounded-md shadow-[0_4px_12px_rgba(0,0,0,0.15)]
text-black
```

**说明：**
- 与侧边栏保持一致的高对比度风格

---

## 批量替换规则

使用 `apply_high_contrast_style.py` 脚本批量替换：

| 原样式 | 新样式 | 说明 |
|--------|--------|------|
| `bg-white/95 backdrop-blur-sm border border-gray-200 rounded-lg shadow-sm` | `bg-white border border-black rounded-md shadow-[0_4px_12px_rgba(0,0,0,0.15)]` | 节点容器 |
| `rounded-lg` | `rounded-md` | 圆角 8px → 6px |
| `rounded-xl` | `rounded-md` | 圆角 12px → 6px |
| `text-gray-600` | `text-black` | 文字颜色 |
| `text-gray-700` | `text-black` | 文字颜色 |
| `text-gray-900` | `text-black` | 文字颜色 |
| `border-gray-200` | `border-black` | 边框颜色 |
| `bg-blue-500` | `bg-black` | 滑块颜色 |
| `hover:text-gray-900` | `hover:text-black` | Hover 状态 |
| `backdrop-blur-sm` | `` | 移除毛玻璃 |
| `backdrop-blur-xl` | `` | 移除毛玻璃 |

---

## 视觉效果对比

### 修改前（React Flow 亮色风格）
- 柔和的灰色边框
- 半透明背景 + 毛玻璃效果
- 蓝色强调色
- 圆润的圆角（8-12px）
- 柔和的阴影

### 修改后（高对比度极简主义）
- 锐利的黑色边框
- 纯白背景，无透明度
- 纯黑强调色
- 方正的圆角（4-6px）
- 硬边阴影

---

## 备份信息

**备份文件夹：** `backup-high-contrast-20260202-XXXX`

**备份文件：**
- App.tsx
- components/Node.tsx
- components/SidebarDock.tsx
- components/Minimap.tsx
- components/GroupToolbar.tsx

**恢复命令：**
```powershell
Copy-Item backup-high-contrast-20260202-XXXX/* . -Force
```

---

## 实施步骤

1. ✅ 创建备份文件夹
2. ✅ 添加全局字体（Inter + Noto Sans SC）
3. ✅ 修改画布文字颜色（text-black）
4. ✅ 批量替换节点样式（黑色边框 + 锐利阴影）
5. ✅ 批量替换圆角（rounded-md）
6. ✅ 批量替换文字颜色（text-black）
7. ✅ 批量替换边框颜色（border-black）
8. ✅ 批量替换滑块颜色（bg-black）
9. ✅ 移除毛玻璃效果（backdrop-blur）

---

## 验收标准

- [ ] 所有节点使用纯白背景 + 黑色边框
- [ ] 所有文字使用纯黑色
- [ ] 所有边框使用黑色
- [ ] 所有圆角统一为 4-6px
- [ ] 所有阴影使用锐利硬边样式
- [ ] 滑块使用黑色
- [ ] 无毛玻璃效果
- [ ] 字体统一为 Inter/Noto Sans SC

---

## 风险和注意事项

### 可能的问题
1. **对比度过高**：黑白对比可能过于强烈，长时间使用可能疲劳
2. **缺少层次感**：去除灰色后，层次感可能减弱
3. **视觉冲击**：从柔和风格突然切换到硬朗风格，用户可能不适应

### 缓解方案
1. 如果对比度过高，可以将黑色改为深灰 `#1A1A1A`
2. 如果缺少层次感，可以在 Hover 状态添加更明显的反馈
3. 如果用户不适应，可以随时回滚到备份

---

## 下一步优化

如果用户喜欢这个风格，可以进一步优化：

1. **添加微交互**：按钮点击时的缩放动画
2. **优化 Hover 状态**：更明显的反馈
3. **添加焦点状态**：键盘导航时的高亮
4. **优化加载状态**：黑白风格的加载动画
5. **添加空状态插画**：黑白线条风格的插画

---

## 总结

成功将界面从"React Flow 亮色风格"改为"高对比度极简主义风格"，实现了：
- ✅ 最大化的清晰度
- ✅ 专业画廊般的美学
- ✅ 零装饰的极简主义
- ✅ 锐利精确的建筑感

如果效果不理想，可以随时回滚到备份。
