# 数据持久化 - 键名格式修复报告

**修复日期**：2026-02-10  
**问题类型**：键名格式不统一导致恢复失败  
**状态**：✅ 已修复

---

## 🐛 问题描述

从控制台日志发现：

```
App.tsx:328 [App] 开始恢复 2 个节点的图片...
blobStorage.ts:317 [BlobStorage] 节点图片已恢复: n-1770658690164-p1mxqgsur, 大小: 13277.32KB
App.tsx:338 [App] 节点 n-1770658690164-p1mxqgsur 图片已恢复
App.tsx:356 [App] 节点图片恢复完成
blobStorage.ts:87 [BlobStorage] 未找到: blob-n-1770658690164-p1mxqgsur-image-0
```

**问题分析**：
- ✅ 第一个节点恢复成功（单张图片）
- ❌ 第二个节点恢复失败（图片数组）
- 原因：键名格式不统一

---

## 🔍 根本原因

在 `blobStorage.ts` 中存在两套不同的键名格式：

### 旧的函数（用于九宫格节点等）
```typescript
// 保存
saveImageToBlob(base64, nodeId, 'image-0')
// 键名：blob-{nodeId}-image-0
// 例如：blob-n-1770658690164-p1mxqgsur-image-0

// 加载
loadImageFromBlob(nodeId, 'image-0')
// 键名：blob-{nodeId}-image-0
```

### 新的函数（用于画布节点持久化）
```typescript
// 保存
saveNodeImageBlob(nodeId, imageUrl)
// 键名：blob-node-{nodeId}-image
// 例如：blob-node-n-1770658690164-p1mxqgsur-image

// 加载
loadNodeImageBlob(nodeId)
// 键名：blob-node-{nodeId}-image
```

### 问题所在

`saveNodeImagesBlob` 和 `loadNodeImagesBlob` 函数内部调用了旧的函数，导致：
- **保存时**：使用旧格式 `blob-{nodeId}-image-0`
- **恢复时**：查找新格式 `blob-node-{nodeId}-image-0`
- **结果**：找不到数据，恢复失败

---

## ✅ 修复方案

统一使用新的键名格式：`blob-node-{nodeId}-image-{index}`

### 修改 1：`loadNodeImagesBlob` 函数

**修改前**：
```typescript
export const loadNodeImagesBlob = async (
  nodeId: string,
  count: number
): Promise<string[]> => {
  // ...
  for (let i = 0; i < count; i++) {
    const url = await loadImageFromBlob(nodeId, `image-${i}`);
    // 键名：blob-{nodeId}-image-{i} ❌
    if (url) {
      blobUrls.push(url);
    }
  }
  // ...
};
```

**修改后**：
```typescript
export const loadNodeImagesBlob = async (
  nodeId: string,
  count: number
): Promise<string[]> => {
  // ...
  for (let i = 0; i < count; i++) {
    // 直接使用新的键名格式
    const storageKey = `blob-node-${nodeId}-image-${i}`;
    const blob = await loadFromStorage<Blob>(storageKey);
    
    if (blob) {
      const blobUrl = URL.createObjectURL(blob);
      blobUrls.push(blobUrl);
      console.log(`[BlobStorage] 节点图片已恢复: ${storageKey}, 大小: ${(blob.size / 1024).toFixed(2)}KB`);
    }
  }
  // ...
};
```

### 修改 2：`saveNodeImagesBlob` 函数

**修改前**：
```typescript
export const saveNodeImagesBlob = async (
  nodeId: string,
  images: string[]
): Promise<void> => {
  // ...
  await Promise.all(
    images.map((imageUrl, idx) => 
      saveImageToBlob(imageUrl, nodeId, `image-${idx}`)
      // 键名：blob-{nodeId}-image-{idx} ❌
    )
  );
  // ...
};
```

**修改后**：
```typescript
export const saveNodeImagesBlob = async (
  nodeId: string,
  images: string[]
): Promise<void> => {
  // ...
  for (let i = 0; i < images.length; i++) {
    const imageUrl = images[i];
    
    // 转换为 Blob
    let blob: Blob;
    if (imageUrl.startsWith('blob:')) {
      blob = await fetch(imageUrl).then(r => r.blob());
    } else if (imageUrl.startsWith('data:')) {
      blob = await base64ToBlob(imageUrl);
    } else {
      console.warn(`[BlobStorage] 不支持的图片格式: ${imageUrl.substring(0, 50)}`);
      continue;
    }
    
    // 使用新的键名格式
    const storageKey = `blob-node-${nodeId}-image-${i}`;
    await saveToStorage(storageKey, blob);
    console.log(`[BlobStorage] 节点图片已保存: ${storageKey}, 大小: ${(blob.size / 1024).toFixed(2)}KB`);
  }
  // ...
};
```

---

## 📊 键名格式总结

修复后，所有节点图片使用统一的键名格式：

| 数据类型 | 键名格式 | 示例 |
|---------|---------|------|
| 单张图片 | `blob-node-{nodeId}-image` | `blob-node-n-123-image` |
| 图片数组 | `blob-node-{nodeId}-image-{index}` | `blob-node-n-123-image-0` |
| 资产缩略图 | `asset-thumbnail-{assetId}` | `asset-thumbnail-a-456` |
| 历史记录 | `asset-{assetId}` | `asset-a-789` |

**优点**：
- ✅ 键名清晰，一眼看出是什么类型的数据
- ✅ 完全隔离，不会互相冲突
- ✅ 易于调试和维护

---

## 🧪 测试建议

### 测试 1：单张图片节点
```
1. 生成 1 个图片节点（单张图片）
2. 刷新页面（F5）
3. 检查：图片是否完整显示
4. 打开 IndexedDB，检查键名：blob-node-{nodeId}-image
```

### 测试 2：图片数组节点（九宫格）
```
1. 生成 1 个九宫格节点（9 张图片）
2. 刷新页面（F5）
3. 检查：9 张图片是否完整显示
4. 打开 IndexedDB，检查键名：
   - blob-node-{nodeId}-image-0
   - blob-node-{nodeId}-image-1
   - ...
   - blob-node-{nodeId}-image-8
```

### 测试 3：混合场景
```
1. 生成 2 个单张图片节点
2. 生成 1 个九宫格节点
3. 刷新页面（Ctrl+F5）
4. 检查：所有图片是否完整显示
```

---

## ⚠️ 注意事项

### 旧数据兼容性

**问题**：修复前保存的数据使用旧的键名格式，修复后无法恢复。

**影响范围**：
- 使用旧格式保存的九宫格节点图片
- 键名：`blob-{nodeId}-image-0`, `blob-{nodeId}-image-1`...

**解决方案**：

#### 方案 A：数据迁移（推荐）
创建一个迁移脚本，将旧键名的数据复制到新键名：

```typescript
// 迁移脚本（可选）
const migrateOldBlobKeys = async () => {
  const { getAllNodes } = useNodeStore.getState();
  const nodes = getAllNodes();
  
  for (const node of nodes) {
    if (node.data.images && node.data.images.length > 0) {
      for (let i = 0; i < node.data.images.length; i++) {
        // 尝试从旧键名加载
        const oldKey = `blob-${node.id}-image-${i}`;
        const blob = await loadFromStorage<Blob>(oldKey);
        
        if (blob) {
          // 保存到新键名
          const newKey = `blob-node-${node.id}-image-${i}`;
          await saveToStorage(newKey, blob);
          console.log(`[Migration] 迁移成功: ${oldKey} → ${newKey}`);
          
          // 删除旧键名（可选）
          await deleteFromStorage(oldKey);
        }
      }
    }
  }
};
```

#### 方案 B：兼容加载（临时方案）
在 `loadNodeImagesBlob` 中添加回退逻辑：

```typescript
// 先尝试新格式
const newKey = `blob-node-${nodeId}-image-${i}`;
let blob = await loadFromStorage<Blob>(newKey);

// 如果失败，尝试旧格式
if (!blob) {
  const oldKey = `blob-${nodeId}-image-${i}`;
  blob = await loadFromStorage<Blob>(oldKey);
  
  if (blob) {
    console.log(`[BlobStorage] 使用旧格式恢复: ${oldKey}`);
    // 可选：迁移到新格式
    await saveToStorage(newKey, blob);
    await deleteFromStorage(oldKey);
  }
}
```

#### 方案 C：清空重建（最简单）
如果旧数据不重要，可以清空 IndexedDB，重新生成：

```typescript
// 清空所有 Blob 数据
const clearAllBlobData = async () => {
  const db = await openDB('sunstudio_db', 1);
  const tx = db.transaction('app_data', 'readwrite');
  const store = tx.objectStore('app_data');
  
  const allKeys = await store.getAllKeys();
  for (const key of allKeys) {
    if (key.startsWith('blob-')) {
      await store.delete(key);
    }
  }
  
  await tx.done;
  console.log('[BlobStorage] 所有 Blob 数据已清空');
};
```

---

## 📝 后续工作

1. **测试验证**：按照测试建议进行完整测试
2. **数据迁移**：如果有旧数据，执行迁移脚本
3. **文档更新**：更新测试指南，说明新的键名格式
4. **监控日志**：观察控制台，确保没有"未找到"的错误

---

## ✅ 验收标准

- [ ] 单张图片节点刷新后完整恢复
- [ ] 图片数组节点刷新后完整恢复
- [ ] 控制台无"未找到"错误
- [ ] IndexedDB 键名格式正确
- [ ] 旧数据已迁移或清空

---

**文档版本**：v1.0  
**创建日期**：2026-02-10  
**状态**：✅ 修复完成，待测试验证
