# 历史记录 - 清除缓存功能

## 功能说明

在历史记录面板中添加"清除缓存"按钮，用于删除所有**未选中**的图片，不下载直接删除。

### 使用场景

- 生成了很多测试图片，只想保留几张好的
- 想快速清理不需要的图片，释放内存
- 不想下载，直接删除

## UI 设计

### 按钮布局（防止误操作）

```
[清除缓存(红色)]  [全选(白色)]  [下载并清除(绿色)]
     ↑                              ↑
  危险操作                        常用操作
  左侧位置                        右侧位置
```

### 视觉区分

1. **清除缓存（左侧）**
   - 红色背景 `bg-red-500/20`
   - 红色文字 `text-red-300`
   - 垃圾桶图标 `<Trash2>`
   - 提示：删除所有未选中的图片（不下载）

2. **全选（中间）**
   - 白色背景 `bg-white/10`
   - 白色文字 `text-white`
   - 无图标

3. **下载并清除（右侧）**
   - 绿色背景 `bg-green-500/20`
   - 绿色文字 `text-green-300`
   - 保存图标 `<Save>`
   - 提示：下载选中的图片并清除缓存

### 防误操作设计

1. **物理距离：** 危险操作（左）和常用操作（右）距离最远
2. **颜色区分：** 红色（危险）vs 绿色（安全）
3. **二次确认：** 点击"清除缓存"会弹出确认对话框
4. **禁用状态：** 
   - 没有图片时禁用
   - 全部选中时禁用（没有未选中的图片）

## 工作流程

### 清除缓存流程

1. 用户勾选要保留的图片
2. 点击"清除缓存"按钮
3. **弹出二次确认对话框：**
   ```
   ⚠️ 确认删除？
   
   将永久删除 X 张未选中的图片
   
   此操作不可恢复！
   ```
4. 用户点击"确定"
5. 删除所有未选中的图片
6. 立即保存到 localStorage
7. 清除 Blob URL 释放内存
8. 显示成功提示：`✅ 已删除 X 张图片\n\n内存已释放！`

### 对比：下载并清除

1. 用户勾选要下载的图片
2. 点击"下载并清除"按钮
3. **不需要确认**（因为有下载备份）
4. 打包成 ZIP 下载
5. 删除选中的图片
6. 立即保存到 localStorage
7. 清除 Blob URL 释放内存

## 代码实现

### 1. SidebarDock.tsx - 添加按钮

```typescript
{/* 左侧：清除缓存（危险操作，红色） */}
<button 
    onClick={() => {
        const unselectedCount = filteredAssets.length - selectedImageIds.size;
        if (unselectedCount === 0) {
            alert('没有未选中的图片');
            return;
        }
        
        // 二次确认
        const confirmed = window.confirm(
            `⚠️ 确认删除？\n\n将永久删除 ${unselectedCount} 张未选中的图片\n\n此操作不可恢复！`
        );
        
        if (confirmed) {
            // 删除未选中的图片
            const unselectedIds = filteredAssets
                .filter(img => !selectedImageIds.has(img.id))
                .map(img => img.id);
            
            // 调用删除函数
            unselectedIds.forEach(id => onDeleteAsset(id));
            
            alert(`✅ 已删除 ${unselectedCount} 张图片\n\n内存已释放！`);
        }
    }}
    className="px-3 py-2 bg-red-500/20 hover:bg-red-500/30 text-red-300 rounded-lg flex items-center justify-center gap-1.5 transition-all active:scale-95 text-[11px] font-semibold disabled:opacity-50 disabled:cursor-not-allowed"
    disabled={!hasImages || selectedImageIds.size === filteredAssets.length}
    title="删除所有未选中的图片（不下载）"
>
    <Trash2 size={12} />
    清除缓存
</button>
```

### 2. App.tsx - 删除资源函数

```typescript
// 删除单个历史记录资源
const handleDeleteAsset = useCallback(async (id: string) => {
    console.log('[删除资源] 开始...', id);
    
    // 找到要删除的资源
    const asset = assetHistory.find(a => a.id === id);
    if (!asset) {
        console.warn('[删除资源] 未找到资源:', id);
        return;
    }
    
    // 从历史记录中删除
    const newAssetHistory = assetHistory.filter(a => a.id !== id);
    setAssetHistory(newAssetHistory);
    
    // 立即保存到 localStorage
    await saveToStorage('assets', newAssetHistory);
    console.log('[删除资源] 已保存到 localStorage');
    
    // 清除 Blob URL
    if (asset.src.startsWith('blob:')) {
        URL.revokeObjectURL(asset.src);
        console.log('[删除资源] 已清除 Blob URL');
    }
    
    // 触发垃圾回收
    if (window.gc) {
        window.gc();
    }
}, [assetHistory]);
```

## 使用示例

### 场景 1：保留精选图片

1. 生成了 9 张多角度相机图片
2. 只想保留 2 张最好的
3. **操作：**
   - 勾选 2 张要保留的图片
   - 点击"清除缓存"
   - 确认删除 7 张未选中的图片
   - 完成！只剩下 2 张精选图片

### 场景 2：全部下载后清空

1. 生成了很多图片
2. 全部下载保存
3. **操作：**
   - 点击"全选"
   - 点击"下载并清除"（绿色）
   - 所有图片打包下载
   - 历史记录清空

### 场景 3：部分下载，其余删除

1. 生成了 20 张图片
2. 只想下载 5 张，其余删除
3. **操作：**
   - 勾选 5 张要下载的图片
   - 点击"下载并清除"（绿色）→ 下载这 5 张
   - 点击"清除缓存"（红色）→ 删除剩余 15 张
   - 完成！

## 按钮状态

### 清除缓存按钮

| 状态 | 条件 | 说明 |
|------|------|------|
| 禁用 | 没有图片 | 历史记录为空 |
| 禁用 | 全部选中 | 没有未选中的图片可删除 |
| 启用 | 有未选中的图片 | 可以删除未选中的图片 |

### 下载并清除按钮

| 状态 | 条件 | 说明 |
|------|------|------|
| 禁用 | 没有选中图片 | 需要先勾选图片 |
| 启用 | 有选中图片 | 可以下载选中的图片 |

## 二次确认对话框

### 确认信息

```
⚠️ 确认删除？

将永久删除 7 张未选中的图片

此操作不可恢复！
```

### 按钮

- **确定** → 执行删除
- **取消** → 取消操作

## 测试步骤

### 测试 1：清除缓存

1. 生成 9 张多角度相机图片
2. 勾选 3 张图片
3. 点击"清除缓存"（红色）
4. 应该弹出确认对话框：`将永久删除 6 张未选中的图片`
5. 点击"确定"
6. 应该显示：`✅ 已删除 6 张图片`
7. 历史记录中只剩下 3 张勾选的图片
8. 刷新页面，仍然只有 3 张图片 ✅

### 测试 2：防误操作

1. 生成一些图片
2. 点击"全选"
3. "清除缓存"按钮应该变灰（禁用）
4. 取消勾选 1 张图片
5. "清除缓存"按钮应该变红（启用）

### 测试 3：二次确认

1. 生成一些图片
2. 不勾选任何图片
3. 点击"清除缓存"
4. 应该弹出确认对话框
5. 点击"取消"
6. 图片应该没有被删除

### 测试 4：组合操作

1. 生成 20 张图片
2. 勾选 5 张
3. 点击"下载并清除"（绿色）→ 下载这 5 张
4. 剩余 15 张
5. 点击"清除缓存"（红色）→ 删除这 15 张
6. 历史记录清空

## 完成状态

✅ 添加"清除缓存"按钮（红色，左侧）
✅ 二次确认对话框
✅ 删除未选中的图片
✅ 立即保存到 localStorage
✅ 清除 Blob URL 释放内存
✅ 按钮禁用状态控制
✅ 视觉区分（红色 vs 绿色）
✅ 物理距离防误操作
✅ 构建成功，无错误

---

**修改时间：** 2025-01-24
**修改文件：** `components/SidebarDock.tsx`, `App.tsx`
**测试状态：** 待用户测试
