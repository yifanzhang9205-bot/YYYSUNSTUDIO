# 图片选择下载功能

## 功能说明

在历史记录面板可以：
1. 查看所有节点的所有图片
2. 勾选要下载的图片
3. 全选/取消全选
4. 下载选中的原图并清除缓存

## 界面设计

### 标题栏
```
[X]  图片管理 (3/27)  
```
- 显示已选择数量 / 总数量

### 操作按钮
```
[全选/取消全选]  [下载并清除]
```
- 全选：选中所有图片
- 取消全选：取消所有选择
- 下载并清除：下载选中的原图并清除缓存（禁用状态：未选择图片时）

### 图片网格
```
┌─────────┬─────────┐
│ ☑ 图片1  │ ☐ 图片2  │
│ 节点A #1 │ 节点A #2 │
├─────────┼─────────┤
│ ☑ 图片3  │ ☐ 图片4  │
│ 节点B #1 │ 节点B #2 │
└─────────┴─────────┘
```
- 每张图片左上角有复选框
- 底部显示节点标题和序号
- 点击图片切换选中状态
- 选中的图片有青色边框和光晕

## 实现细节

### 1. 收集所有图片

```typescript
const allNodeImages: Array<{
    id: string,           // 唯一ID: nodeId-type-index
    nodeId: string,       // 节点ID
    nodeTitle: string,    // 节点标题
    url: string,          // 图片URL（Blob URL 或 base64）
    type: string,         // 类型：grid 或 image
    index: number         // 索引
}> = [];

// 从所有节点收集
nodes.forEach(node => {
    // 收集 gridImages（九宫格）
    node.data.gridImages?.forEach((url, i) => {
        allNodeImages.push({
            id: `${node.id}-grid-${i}`,
            nodeId: node.id,
            nodeTitle: node.title || '未命名',
            url,
            type: 'grid',
            index: i
        });
    });
    
    // 收集 image（单张图片）
    if (node.data.image) {
        allNodeImages.push({
            id: `${node.id}-image-0`,
            nodeId: node.id,
            nodeTitle: node.title || '未命名',
            url: node.data.image,
            type: 'image',
            index: 0
        });
    }
});
```

### 2. 选择状态管理

```typescript
const [selectedImageIds, setSelectedImageIds] = useState<Set<string>>(new Set());

// 全选
setSelectedImageIds(new Set(allNodeImages.map(img => img.id)));

// 取消全选
setSelectedImageIds(new Set());

// 切换单个图片
const newSelected = new Set(selectedImageIds);
if (isSelected) {
    newSelected.delete(img.id);
} else {
    newSelected.add(img.id);
}
setSelectedImageIds(newSelected);
```

### 3. 下载选中的图片

```typescript
const downloadSelectedImagesAndClear = async (selectedIds: Set<string>) => {
    for (const imageId of selectedIds) {
        // 解析 imageId: nodeId-type-index
        const [nodeId, type, index] = imageId.split('-');
        const node = nodes.get(nodeId);
        
        // 优先从 IndexedDB 加载原图
        const originalImages = await loadFromStorage(`grid-original-${nodeId}`);
        
        let imageUrl: string | undefined;
        let filename: string;
        
        if (originalImages && originalImages[parseInt(index)]) {
            // 使用原图（高清）
            imageUrl = originalImages[parseInt(index)];
            filename = `${node.title}-original-${parseInt(index) + 1}.png`;
        } else if (type === 'grid') {
            // 使用缩略图
            imageUrl = node.data.gridImages[parseInt(index)];
            filename = `${node.title}-grid-${parseInt(index) + 1}.png`;
        }
        
        // 下载
        const a = document.createElement('a');
        a.href = imageUrl;
        a.download = filename;
        a.click();
        
        await new Promise(resolve => setTimeout(resolve, 100));
    }
    
    // 清除缓存
    for (const nodeId of clearedNodeIds) {
        // 清除 Blob URL
        if (node.data.gridImages) {
            node.data.gridImages.forEach(url => {
                if (url.startsWith('blob:')) {
                    URL.revokeObjectURL(url);
                }
            });
        }
        
        // 清除 IndexedDB
        await deleteFromStorage(`grid-original-${nodeId}`);
    }
    
    // 触发垃圾回收
    if (window.gc) {
        window.gc();
    }
};
```

## 使用流程

### 1. 打开图片管理
- 点击左侧边栏的"历史记录"图标
- 面板显示所有节点的所有图片

### 2. 选择图片
- **方式1**：点击"全选"按钮，选中所有图片
- **方式2**：点击单张图片，切换选中状态
- **方式3**：多次点击，选择多张图片

### 3. 下载并清除
- 点击"下载并清除"按钮
- 浏览器自动下载选中的图片（优先下载原图）
- 自动清除已下载图片的缓存
- 弹出提示：显示下载数量和清理结果

### 4. 完成
- 图片已保存到本地
- 缓存已清除
- 内存已释放

## 文件命名

### 原图（优先）
- `节点标题-original-1.png`
- `节点标题-original-2.png`
- ...

### 缩略图（备选）
- `节点标题-grid-1.png`
- `节点标题-grid-2.png`
- ...

## 清理内容

### 1. Blob URL
- 清除选中图片的 Blob URL
- 释放浏览器内存

### 2. IndexedDB
- 清除选中图片的原图数据
- 释放磁盘空间

### 3. 垃圾回收
- 触发浏览器 GC
- 释放未引用的对象

## 提示信息

```
✅ 完成！

已下载: 9 张图片
已清除: 1 个节点的缓存

内存已释放！
```

## 界面状态

### 未选择图片
- "下载并清除"按钮禁用
- 显示 (0/27)

### 选择部分图片
- "下载并清除"按钮启用
- 显示 (3/27)
- "全选"按钮显示

### 全选
- "下载并清除"按钮启用
- 显示 (27/27)
- "取消全选"按钮显示

### 无图片
- 显示"暂无图片"提示
- 所有按钮禁用

## 视觉反馈

### 未选中
- 白色半透明边框
- 悬停时边框变亮

### 选中
- 青色边框
- 青色光晕
- 复选框显示勾选

### 复选框
- 未选中：黑色半透明背景 + 白色边框
- 选中：青色背景 + 白色勾选图标

## 性能优化

### 1. 只渲染可见图片
- 使用虚拟滚动（未来）
- 懒加载图片

### 2. 批量操作
- 批量下载
- 批量清除

### 3. 内存管理
- 及时清除 Blob URL
- 触发垃圾回收

## 总结

✅ 显示所有节点的所有图片
✅ 复选框选择
✅ 全选/取消全选
✅ 下载选中的原图
✅ 清除缓存
✅ 释放内存

**现在用户可以精确控制要下载和清除的图片了！** 🎉
