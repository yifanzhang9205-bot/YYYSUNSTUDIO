# 内存优化 - 完整实施方案

> 📅 2026-01-31  
> 🎯 目标：零拷贝 + 异步持久化 + 刷新恢复 + 遵守架构规则

---

## ⚠️ 架构规则（必须遵守）

### 三层架构：

```
UI Layer (App.tsx, Node.tsx)
    ↓ 只调用 Hook
Hooks Layer (useNodeActions.ts, useAssetHistory.ts)
    ↓ 调用 Store 和 Service
Core Layer (stores/, services/)
```

### 禁止事项：

- ❌ 不在 App.tsx 写业务逻辑
- ❌ 不在 UI 组件直接操作 Store
- ❌ 不跨层调用

### 允许修改：

- ✅ App.tsx：只修改调用 Hook 的方式
- ✅ Node.tsx：只添加 `loading="lazy"` 和 `decoding="async"`
- ✅ useNodeActions.ts：添加图片处理逻辑
- ✅ useAssetHistory.ts：添加清理逻辑
- ✅ services/blobStorage.ts：添加持久化逻辑

---

## 📋 用户补充的关键需求

### 1. 持久化恢复（刷新后图片不挂）

**问题：**
- Blob URL 刷新会失效
- 需要从 IndexedDB 重新读取并生成新 URL

**解决方案：**
- 在页面初始化时，遍历所有节点
- 根据 `assetId` 从 IndexedDB 读取 Blob
- 重新生成 Blob URL
- 更新节点的 `data.image`

---

### 2. 异步体验（拖入不卡顿）

**问题：**
- 大文件写入 IndexedDB 会卡顿
- 不能 `await` 保存过程

**解决方案：**
- 先 `addNode` 显示图片（立即响应）
- 再后台异步执行 `saveToIndexedDB`（不阻塞）

---

## 🚀 完整实施方案

### 阶段 1：修改 services/blobStorage.ts（底层）

**目标：** 添加异步保存和恢复逻辑

**新增函数：**

```typescript
/**
 * 异步保存文件到 IndexedDB（不阻塞）
 * @param assetId 资产 ID
 * @param file 文件对象
 */
export const saveFileToIndexedDBAsync = (assetId: string, file: File): void => {
  // 不返回 Promise，不阻塞调用方
  saveToStorage(`blob-${assetId}`, file).catch(err => {
    console.error(`[BlobStorage] 异步保存失败: ${assetId}`, err);
  });
};

/**
 * 从 IndexedDB 恢复 Blob URL
 * @param assetId 资产 ID
 * @returns Blob URL 或 null
 */
export const restoreBlobUrlFromIndexedDB = async (assetId: string): Promise<string | null> => {
  try {
    const blob = await loadFromStorage(`blob-${assetId}`);
    if (blob) {
      const blobUrl = URL.createObjectURL(blob);
      console.log(`[BlobStorage] 恢复成功: ${assetId}, URL: ${blobUrl}`);
      return blobUrl;
    }
    return null;
  } catch (err) {
    console.error(`[BlobStorage] 恢复失败: ${assetId}`, err);
    return null;
  }
};

/**
 * 批量恢复 Blob URL
 * @param assetIds 资产 ID 列表
 * @returns Map<assetId, blobUrl>
 */
export const restoreMultipleBlobUrls = async (assetIds: string[]): Promise<Map<string, string>> => {
  const results = new Map<string, string>();
  
  await Promise.all(
    assetIds.map(async (assetId) => {
      const blobUrl = await restoreBlobUrlFromIndexedDB(assetId);
      if (blobUrl) {
        results.set(assetId, blobUrl);
      }
    })
  );
  
  return results;
};
```

---

### 阶段 2：修改 hooks/useNodeActions.ts（业务逻辑）

**目标：** 添加图片处理逻辑（零拷贝 + 异步保存）

**新增函数：**

```typescript
import { saveFileToIndexedDBAsync } from '../services/blobStorage';

/**
 * 处理拖入/上传的图片文件
 * @param file 文件对象
 * @param position 节点位置
 */
const handleImageFile = (file: File, position: { x: number, y: number }) => {
  // 1. 生成唯一 ID
  const assetId = `asset-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
  
  // 2. 立即创建 Blob URL（零拷贝，瞬间完成）
  const blobUrl = URL.createObjectURL(file);
  
  // 3. 立即创建节点（不等待保存）
  const nodeId = addNode({
    type: NodeType.IMAGE_GENERATOR,
    x: position.x,
    y: position.y,
    data: {
      image: blobUrl,      // ✅ 立即显示
      assetId: assetId,    // ✅ 记录 ID
      fileName: file.name  // ✅ 记录文件名
    }
  });
  
  // 4. 后台异步保存到 IndexedDB（不阻塞）
  saveFileToIndexedDBAsync(assetId, file);
  
  return nodeId;
};

/**
 * 处理拖入/上传的视频文件
 * @param file 文件对象
 * @param position 节点位置
 */
const handleVideoFile = (file: File, position: { x: number, y: number }) => {
  const assetId = `asset-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
  const blobUrl = URL.createObjectURL(file);
  
  const nodeId = addNode({
    type: NodeType.VIDEO_GENERATOR,
    x: position.x,
    y: position.y,
    data: {
      videoUri: blobUrl,
      assetId: assetId,
      fileName: file.name
    }
  });
  
  saveFileToIndexedDBAsync(assetId, file);
  
  return nodeId;
};
```

**导出给 App.tsx 使用：**

```typescript
return {
  // ... 其他函数
  handleImageFile,
  handleVideoFile
};
```

---

### 阶段 3：修改 App.tsx（UI 层，只调用 Hook）

**目标：** 修改拖入逻辑，调用 Hook 的新函数

**修改 handleCanvasDrop：**

```typescript
const handleCanvasDrop = (e: React.DragEvent) => {
  e.preventDefault();
  
  const files = Array.from(e.dataTransfer.files);
  const dropX = (e.clientX - pan.x) / scale;
  const dropY = (e.clientY - pan.y) / scale;
  
  files.forEach((file, index) => {
    const position = {
      x: dropX + (index * 20),
      y: dropY + (index * 20)
    };
    
    if (file.type.startsWith('image/')) {
      // ✅ 调用 Hook 的函数（遵守架构）
      handleImageFile(file, position);
    } else if (file.type.startsWith('video/')) {
      // ✅ 调用 Hook 的函数（遵守架构）
      handleVideoFile(file, position);
    }
  });
};
```

---

### 阶段 4：修改 components/Node.tsx（UI 层）

**目标：** 添加懒加载和异步解码

**修改 1：handleUploadImage**

```typescript
const handleUploadImage = (e: React.ChangeEvent<HTMLInputElement>) => { 
  const file = e.target.files?.[0]; 
  if (file) { 
    // ✅ 零拷贝
    const blobUrl = URL.createObjectURL(file);
    const assetId = `asset-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    
    // ✅ 立即更新
    onUpdate(node.id, { 
      image: blobUrl,
      assetId: assetId,
      fileName: file.name
    });
    
    // ✅ 后台异步保存
    saveFileToIndexedDBAsync(assetId, file);
  }
};
```

**修改 2：handleUploadVideo**

```typescript
const handleUploadVideo = (e: React.ChangeEvent<HTMLInputElement>) => { 
  const file = e.target.files?.[0]; 
  if (file) { 
    const blobUrl = URL.createObjectURL(file);
    const assetId = `asset-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    
    onUpdate(node.id, { 
      videoUri: blobUrl,
      assetId: assetId,
      fileName: file.name
    });
    
    saveFileToIndexedDBAsync(assetId, file);
  }
};
```

**修改 3：图片渲染（添加懒加载）**

```typescript
<img 
  ref={mediaRef as any} 
  src={node.data.image} 
  className="w-full h-full object-cover transition-transform duration-700 group-hover/media:scale-105 bg-zinc-900" 
  draggable={false} 
  loading="lazy"        // ✅ 懒加载
  decoding="async"      // ✅ 异步解码
  style={{ filter: showImageGrid ? 'blur(10px)' : 'none' }} 
  onContextMenu={(e) => onMediaContextMenu?.(e, node.id, 'image', node.data.image!)} 
/>
```

---

### 阶段 5：添加页面初始化恢复逻辑

**目标：** 刷新后从 IndexedDB 恢复 Blob URL

**在 App.tsx 添加 useEffect：**

```typescript
import { restoreMultipleBlobUrls } from './services/blobStorage';

// 页面初始化时恢复 Blob URL
useEffect(() => {
  const restoreBlobUrls = async () => {
    const nodes = nodeStore.getState().nodes;
    const assetIds: string[] = [];
    
    // 收集所有需要恢复的 assetId
    nodes.forEach(node => {
      if (node.data.assetId && node.data.image?.startsWith('blob:')) {
        assetIds.push(node.data.assetId);
      }
      if (node.data.assetId && node.data.videoUri?.startsWith('blob:')) {
        assetIds.push(node.data.assetId);
      }
    });
    
    if (assetIds.length === 0) return;
    
    console.log(`[App] 恢复 ${assetIds.length} 个 Blob URL...`);
    
    // 批量恢复
    const restoredUrls = await restoreMultipleBlobUrls(assetIds);
    
    // 更新节点
    nodes.forEach(node => {
      if (node.data.assetId) {
        const newUrl = restoredUrls.get(node.data.assetId);
        if (newUrl) {
          if (node.data.image?.startsWith('blob:')) {
            handleNodeUpdate(node.id, { image: newUrl });
          }
          if (node.data.videoUri?.startsWith('blob:')) {
            handleNodeUpdate(node.id, { videoUri: newUrl });
          }
        }
      }
    });
    
    console.log(`[App] 恢复完成，成功恢复 ${restoredUrls.size} 个`);
  };
  
  restoreBlobUrls();
}, []); // 只在页面加载时执行一次
```

---

### 阶段 6：修改 hooks/useAssetHistory.ts（清理逻辑）

**目标：** 彻底释放内存

**修改 handleDeleteMultipleAssets：**

```typescript
const handleDeleteMultipleAssets = async (assetIds: string[]) => {
  const { assets } = assetHistoryStore.getState();
  
  // 1. 获取要删除的资产
  const assetsToDelete = assets.filter(a => assetIds.includes(a.id));
  
  // 2. 【关键】释放 Blob URL
  assetsToDelete.forEach(asset => {
    if (asset.url && asset.url.startsWith('blob:')) {
      URL.revokeObjectURL(asset.url);
      console.log(`[AssetHistory] 释放 URL: ${asset.url}`);
    }
  });
  
  // 3. 删除 IndexedDB
  await Promise.all(
    assetIds.map(id => deleteFromStorage(`blob-${id}`))
  );
  
  // 4. 清空节点的 data.image
  const nodes = nodeStore.getState().nodes;
  nodes.forEach(node => {
    if (node.data.assetId && assetIds.includes(node.data.assetId)) {
      nodeStore.getState().updateNode(node.id, { 
        image: undefined,
        videoUri: undefined
      });
    }
  });
  
  // 5. 更新 Store
  const newAssets = assets.filter(a => !assetIds.includes(a.id));
  assetHistoryStore.setState({ assets: newAssets });
  
  console.log(`[AssetHistory] 删除完成，释放 ${assetsToDelete.length} 个资产`);
};
```

---

## 📋 实施清单

### ✅ 阶段 1：services/blobStorage.ts（5 分钟）

- [ ] 添加 `saveFileToIndexedDBAsync` 函数
- [ ] 添加 `restoreBlobUrlFromIndexedDB` 函数
- [ ] 添加 `restoreMultipleBlobUrls` 函数

### ✅ 阶段 2：hooks/useNodeActions.ts（10 分钟）

- [ ] 添加 `handleImageFile` 函数
- [ ] 添加 `handleVideoFile` 函数
- [ ] 导出给 App.tsx 使用

### ✅ 阶段 3：App.tsx（5 分钟）

- [ ] 修改 `handleCanvasDrop`，调用 Hook 的函数
- [ ] 添加页面初始化恢复逻辑（useEffect）

### ✅ 阶段 4：components/Node.tsx（5 分钟）

- [ ] 修改 `handleUploadImage`（零拷贝 + 异步保存）
- [ ] 修改 `handleUploadVideo`（零拷贝 + 异步保存）
- [ ] 添加 `loading="lazy"` 和 `decoding="async"`

### ✅ 阶段 5：hooks/useAssetHistory.ts（5 分钟）

- [ ] 修改 `handleDeleteMultipleAssets`（彻底释放内存）

---

## 🎯 架构遵守情况

| 文件 | 修改内容 | 是否遵守架构 | 说明 |
|------|---------|-------------|------|
| `services/blobStorage.ts` | 添加函数 | ✅ 是 | Core Layer，纯函数 |
| `hooks/useNodeActions.ts` | 添加函数 | ✅ 是 | Hooks Layer，业务逻辑 |
| `hooks/useAssetHistory.ts` | 修改函数 | ✅ 是 | Hooks Layer，业务逻辑 |
| `App.tsx` | 调用 Hook | ✅ 是 | UI Layer，只调用 Hook |
| `components/Node.tsx` | 添加属性 | ✅ 是 | UI Layer，只修改渲染 |

---

## 🚀 预期效果

### 拖入图片：

- ✅ 瞬间显示（零拷贝）
- ✅ 不卡顿（异步保存）
- ✅ 内存占用降低 90%

### 刷新页面：

- ✅ 图片不挂（从 IndexedDB 恢复）
- ✅ 自动恢复 Blob URL

### 清理图片：

- ✅ 立即释放内存
- ✅ 不需要重启浏览器

---

## ⚠️ 注意事项

1. **不破坏架构**
   - App.tsx 只调用 Hook
   - 业务逻辑在 useNodeActions.ts
   - 底层函数在 services/blobStorage.ts

2. **异步保存不阻塞**
   - 先 addNode 显示图片
   - 再后台异步保存
   - 不 await 保存过程

3. **刷新恢复**
   - 页面初始化时自动恢复
   - 从 IndexedDB 读取 Blob
   - 重新生成 Blob URL

4. **彻底清理**
   - 释放 Blob URL
   - 删除 IndexedDB
   - 清空节点数据

---

**准备好了吗？我现在开始实施！** 🚀
