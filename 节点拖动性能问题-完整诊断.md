# 节点拖动性能问题 - 完整诊断

## 用户反馈
- 拖动节点像"拖着走"，不是"跟着走"
- 有 0.5 秒延迟
- 动画很慢
- 所有节点都很滞涩

## 问题排查

### ❌ 问题 1：每次 mousemove 都触发 React 重渲染
**位置：** `App.tsx` - `handleGlobalMouseMove`
```typescript
setMousePos({ x: clientX, y: clientY });  // ❌ 每次 mousemove 都更新 state
```
**影响：** 每秒 60+ 次 React 重渲染，导致严重卡顿

**修复：** 使用 ref 存储鼠标位置，只在需要时更新 state

---

### ❌ 问题 2：Group 拖动时每次都查询所有子节点 DOM
**位置：** `App.tsx` - `handleGlobalMouseMove` - Group 拖动部分
```typescript
childNodes.forEach(child => {
    const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`);  // ❌ 每次都查询
});
```
**影响：** 如果 Group 有 10 个子节点，每次 mousemove 查询 10 次 DOM

**修复：** 在拖动开始时缓存所有子节点的 DOM 元素

---

### ❌ 问题 3：Resize 时每次都查询 DOM
**位置：** `App.tsx` - `handleGlobalMouseMove` - Resize 部分
```typescript
const nodeElement = document.querySelector(`[data-node-id="${resizingNodeId}"]`);  // ❌ 每次都查询
```
**影响：** 每次 mousemove 都查询 DOM

**修复：** 在 resize 开始时缓存 DOM 元素

---

### ❌ 问题 4：handleGlobalMouseMove 依赖项过多
**位置：** `App.tsx` - `handleGlobalMouseMove` useCallback
```typescript
}, [selectionRect, isDraggingCanvas, draggingNodeId, resizingNodeId, initialSize, resizeStartPos, scale, lastMousePos]);
```
**影响：** 依赖项变化会重新创建函数，导致事件监听器重新绑定

**修复：** 使用 ref 减少依赖项

---

### ❌ 问题 5：Node.tsx transition 太慢
**位置：** `components/Node.tsx` - style
```typescript
transition: isInteracting ? 'none' : 'all 0.5s cubic-bezier(0.32, 0.72, 0, 1)',  // ❌ 0.5秒太慢
```
**影响：** 拖动结束后，节点移动到最终位置需要 0.5 秒

**修复：** 改为 0.2s 或更快

---

### ❌ 问题 6：拖动结束后 transition 未恢复
**位置：** `App.tsx` - `handleGlobalMouseUp`
```typescript
nodeElement.style.transition = 'none';
// ... 设置位置 ...
// ❌ 没有恢复 transition
```
**影响：** 第二次拖动时没有平滑过渡

**修复：** 使用 requestAnimationFrame 延迟恢复 transition

---

## 完整修复方案

### 修复 1：使用 ref 存储鼠标位置
```typescript
const mousePosRef = useRef({ x: 0, y: 0 });

const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
    const { clientX, clientY } = e;
    mousePosRef.current = { x: clientX, y: clientY };
    
    // 只在绘制连接线时更新 state
    if (connectionStartRef.current) {
        setMousePos({ x: clientX, y: clientY });
    }
    // ...
});
```

### 修复 2：缓存 Group 子节点 DOM
```typescript
const dragGroupRef = useRef<{
    id: string,
    // ...
    groupElement: HTMLElement | null,
    childElements: Map<string, HTMLElement>  // 缓存子节点 DOM
} | null>(null);

// 拖动开始时缓存
const groupElement = document.querySelector(`[data-group-id="${id}"]`);
const childElements = new Map();
childNodes.forEach(child => {
    const el = document.querySelector(`[data-node-id="${child.id}"]`);
    if (el) childElements.set(child.id, el);
});
dragGroupRef.current = { ..., groupElement, childElements };

// 拖动过程中直接使用
dragGroupRef.current.childElements.forEach((el, id) => {
    el.style.transform = `translate(${dx}px, ${dy}px)`;
});
```

### 修复 3：缓存 Resize DOM
```typescript
const resizeContextRef = useRef<{
    nodeId: string,
    element: HTMLElement | null,  // 缓存 DOM
    // ...
} | null>(null);
```

### 修复 4：减少 useCallback 依赖项
```typescript
// 使用 ref 代替 state
const scaleRef = useRef(scale);
const panRef = useRef(pan);

useEffect(() => {
    scaleRef.current = scale;
    panRef.current = pan;
}, [scale, pan]);

const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
    // 使用 ref 而不是依赖项
    const currentScale = scaleRef.current;
    // ...
}, []);  // 空依赖项
```

### 修复 5：加快 transition
```typescript
transition: isInteracting ? 'none' : 'all 0.2s cubic-bezier(0.32, 0.72, 0, 1)',
```

### 修复 6：恢复 transition（已修复）
```typescript
nodeElement.style.transition = 'none';
// ... 设置位置 ...
requestAnimationFrame(() => {
    nodeElement.style.transition = '';
});
```

---

## 预期效果

### 修复前
- 拖动延迟：200-500ms
- 帧率：10-20 FPS
- 每次 mousemove：5-10ms
- 动画时长：0.5s

### 修复后
- 拖动延迟：< 16ms（即时响应）
- 帧率：60 FPS
- 每次 mousemove：< 1ms
- 动画时长：0.2s

**性能提升：10-50 倍**

---

## 实施步骤

1. ✅ 修复 Node.tsx transition（0.5s → 0.2s）
2. ✅ 修复拖动结束后 transition 恢复
3. ✅ 缓存单个节点拖动的 DOM 元素
4. ⏳ 使用 ref 存储鼠标位置
5. ⏳ 缓存 Group 子节点 DOM
6. ⏳ 缓存 Resize DOM
7. ⏳ 减少 useCallback 依赖项

---

## 测试清单

- [ ] 拖动单个节点 - 应该丝滑跟随鼠标
- [ ] 拖动 Group - 应该丝滑跟随鼠标
- [ ] Resize 节点 - 应该丝滑调整大小
- [ ] 拖动结束 - 应该快速到达最终位置（0.2s）
- [ ] 连续拖动 - 每次都应该丝滑
- [ ] 多个节点 - 性能不应该下降
