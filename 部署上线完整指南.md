# SunStudio 部署上线完整指南

## 📋 目录
1. [部署概述](#部署概述)
2. [准备工作](#准备工作)
3. [技术选型](#技术选型)
4. [详细步骤](#详细步骤)
5. [成本预算](#成本预算)
6. [安全注意事项](#安全注意事项)
7. [常见问题](#常见问题)

---

## 部署概述

### 什么是部署？
简单来说，就是把你的项目从本地电脑搬到互联网上，让任何人都能通过网址访问。

### 当前状态 vs 目标状态

**当前状态**：
- ✅ 只能在你的电脑上运行
- ✅ 访问地址：http://localhost:5173
- ✅ 只有你能用

**目标状态**：
- 🎯 任何人都能访问
- 🎯 访问地址：https://sunstudio.com（你的域名）
- 🎯 需要账号密码登录
- 🎯 多用户同时使用

---

## 准备工作

### 需要准备的东西

#### 1. 域名（必需）💰
**是什么**：网站的地址，比如 `sunstudio.com`

**在哪买**：
- 阿里云：https://wanwang.aliyun.com/
- 腾讯云：https://dnspod.cloud.tencent.com/
- GoDaddy：https://www.godaddy.com/
- Namecheap：https://www.namecheap.com/

**价格**：
- .com 域名：约 60-100 元/年
- .cn 域名：约 30-50 元/年

**推荐**：
- 如果面向国内用户：阿里云或腾讯云
- 如果面向国际用户：GoDaddy 或 Namecheap

#### 2. 服务器（必需）💰
**是什么**：一台 24 小时运行的电脑，托管你的网站

**选择**：
- **云服务器**（推荐新手）
- **虚拟主机**（便宜但功能受限）
- **Serverless**（按使用量付费）

**推荐服务商**：
- 阿里云：https://www.aliyun.com/
- 腾讯云：https://cloud.tencent.com/
- AWS：https://aws.amazon.com/
- Vercel：https://vercel.com/（免费，推荐）
- Netlify：https://www.netlify.com/（免费，推荐）

#### 3. 数据库（必需）💰
**是什么**：存储用户账号、密码、数据的地方

**选择**：
- MySQL（传统，稳定）
- PostgreSQL（功能强大）
- MongoDB（灵活，适合快速开发）
- Firebase（Google 提供，简单易用）
- Supabase（开源，类似 Firebase）

#### 4. SSL 证书（必需，但可免费）🆓
**是什么**：让网站使用 https（安全连接）

**获取方式**：
- Let's Encrypt（免费，推荐）
- 阿里云/腾讯云（免费版）
- Cloudflare（免费）

#### 5. 后端服务（必需）
**是什么**：处理用户登录、数据存储、API 调用的程序

**需要开发**：
- 用户注册/登录系统
- API Key 管理（每个用户独立的 Key）
- 数据存储和读取
- 权限控制

---

## 技术选型

### 方案 1：全栈部署（完整但复杂）

#### 架构图
```
用户浏览器
    ↓
前端（React）- 部署在 Vercel/Netlify
    ↓
后端（Node.js）- 部署在云服务器
    ↓
数据库（MongoDB）- 云数据库
```

#### 技术栈
- **前端**：React + TypeScript（已有）
- **后端**：Node.js + Express
- **数据库**：MongoDB 或 PostgreSQL
- **认证**：JWT（JSON Web Token）
- **部署**：
  - 前端：Vercel（免费）
  - 后端：阿里云/腾讯云（付费）
  - 数据库：MongoDB Atlas（免费版）

#### 优点
- ✅ 完全控制
- ✅ 功能强大
- ✅ 可扩展性好

#### 缺点
- ❌ 需要开发后端
- ❌ 需要维护服务器
- ❌ 成本较高

---

### 方案 2：Serverless 部署（推荐新手）⭐

#### 架构图
```
用户浏览器
    ↓
前端（React）- Vercel
    ↓
后端（Serverless Functions）- Vercel
    ↓
数据库（Supabase）- 免费
    ↓
认证（Supabase Auth）- 免费
```

#### 技术栈
- **前端**：React + TypeScript（已有）
- **后端**：Vercel Serverless Functions
- **数据库**：Supabase（PostgreSQL）
- **认证**：Supabase Auth（内置）
- **存储**：Supabase Storage（存储用户数据）

#### 优点
- ✅ 简单易用
- ✅ 免费额度大
- ✅ 自动扩展
- ✅ 不需要维护服务器

#### 缺点
- ❌ 功能相对受限
- ❌ 依赖第三方服务

---

### 方案 3：Firebase 全家桶（最简单）⭐⭐

#### 架构图
```
用户浏览器
    ↓
前端（React）- Firebase Hosting
    ↓
后端（Cloud Functions）- Firebase
    ↓
数据库（Firestore）- Firebase
    ↓
认证（Firebase Auth）- Firebase
```

#### 技术栈
- **全部使用 Firebase**
- **前端**：React + TypeScript（已有）
- **后端**：Firebase Cloud Functions
- **数据库**：Firestore
- **认证**：Firebase Authentication
- **存储**：Firebase Storage

#### 优点
- ✅ 最简单
- ✅ 一站式解决方案
- ✅ 免费额度大
- ✅ Google 官方支持

#### 缺点
- ❌ 完全依赖 Google
- ❌ 国内访问可能较慢

---

## 详细步骤

### 推荐方案：Vercel + Supabase（免费开始）

#### 阶段 1：准备工作（1-2 天）

##### 1.1 注册账号
```
✅ Vercel 账号：https://vercel.com/
✅ Supabase 账号：https://supabase.com/
✅ 域名（可选，先用免费域名）
```

##### 1.2 学习基础知识
```
📚 了解什么是前后端分离
📚 了解什么是 API
📚 了解什么是数据库
📚 了解什么是认证（登录系统）
```

**推荐教程**：
- Vercel 官方文档：https://vercel.com/docs
- Supabase 官方文档：https://supabase.com/docs
- YouTube 搜索："Vercel Supabase tutorial"

---

#### 阶段 2：开发后端功能（1-2 周）

##### 2.1 设置 Supabase 数据库

**创建数据表**：

```sql
-- 用户表
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  username TEXT UNIQUE NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  subscription_tier TEXT DEFAULT 'free' -- free, pro, enterprise
);

-- 用户工作流表
CREATE TABLE workflows (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  title TEXT NOT NULL,
  data JSONB NOT NULL, -- 存储节点和连接数据
  thumbnail TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 用户资产表
CREATE TABLE assets (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  type TEXT NOT NULL, -- image, video, audio
  url TEXT NOT NULL,
  title TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- API Key 使用记录
CREATE TABLE api_usage (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  api_type TEXT NOT NULL, -- gemini, nano_banana
  tokens_used INTEGER,
  created_at TIMESTAMP DEFAULT NOW()
);
```

##### 2.2 设置认证

**Supabase 自动提供**：
- ✅ 邮箱密码登录
- ✅ Google 登录
- ✅ GitHub 登录
- ✅ 密码重置
- ✅ 邮箱验证

**只需要在前端调用**：
```typescript
import { createClient } from '@supabase/supabase-js'

const supabase = createClient(
  'YOUR_SUPABASE_URL',
  'YOUR_SUPABASE_KEY'
)

// 注册
const { data, error } = await supabase.auth.signUp({
  email: 'user@example.com',
  password: 'password123'
})

// 登录
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'user@example.com',
  password: 'password123'
})
```

##### 2.3 修改前端代码

**需要添加的功能**：

1. **登录页面**
```typescript
// components/LoginPage.tsx
import { useState } from 'react'
import { supabase } from '../lib/supabase'

export const LoginPage = () => {
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')

  const handleLogin = async () => {
    const { data, error } = await supabase.auth.signInWithPassword({
      email,
      password
    })
    if (error) alert(error.message)
    else window.location.href = '/app'
  }

  return (
    <div className="login-container">
      <h1>SunStudio</h1>
      <input 
        type="email" 
        placeholder="邮箱"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
      />
      <input 
        type="password" 
        placeholder="密码"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
      />
      <button onClick={handleLogin}>登录</button>
    </div>
  )
}
```

2. **保护路由**
```typescript
// App.tsx
import { useEffect, useState } from 'react'
import { supabase } from './lib/supabase'
import { LoginPage } from './components/LoginPage'

export const App = () => {
  const [user, setUser] = useState(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    // 检查用户是否登录
    supabase.auth.getSession().then(({ data: { session } }) => {
      setUser(session?.user ?? null)
      setLoading(false)
    })

    // 监听登录状态变化
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (_event, session) => {
        setUser(session?.user ?? null)
      }
    )

    return () => subscription.unsubscribe()
  }, [])

  if (loading) return <div>加载中...</div>
  if (!user) return <LoginPage />

  // 原有的应用界面
  return <div>你的应用</div>
}
```

3. **API Key 管理**
```typescript
// 不再使用固定的 API Key
// 每个用户使用自己的 API Key
// 或者使用服务器端代理（推荐）

// services/apiProxy.ts
export const generateImage = async (prompt: string) => {
  // 通过后端代理调用 Gemini API
  const response = await fetch('/api/generate-image', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${userToken}`
    },
    body: JSON.stringify({ prompt })
  })
  return response.json()
}
```

##### 2.4 创建 API 代理（重要）⚠️

**为什么需要代理**：
- 保护 API Key 不被泄露
- 控制用户使用量
- 统一计费

**创建 Vercel Serverless Function**：

```typescript
// api/generate-image.ts
import { createClient } from '@supabase/supabase-js'
import { GoogleGenerativeAI } from '@google/generative-ai'

export default async function handler(req, res) {
  // 1. 验证用户身份
  const token = req.headers.authorization?.replace('Bearer ', '')
  const supabase = createClient(
    process.env.SUPABASE_URL,
    process.env.SUPABASE_KEY
  )
  
  const { data: { user }, error } = await supabase.auth.getUser(token)
  if (error || !user) {
    return res.status(401).json({ error: '未授权' })
  }

  // 2. 检查用户配额
  const { data: usage } = await supabase
    .from('api_usage')
    .select('tokens_used')
    .eq('user_id', user.id)
    .gte('created_at', new Date(Date.now() - 24 * 60 * 60 * 1000))
  
  const totalUsage = usage?.reduce((sum, u) => sum + u.tokens_used, 0) || 0
  if (totalUsage > 1000) { // 每天 1000 次限制
    return res.status(429).json({ error: '配额已用完' })
  }

  // 3. 调用 Gemini API（使用服务器端的 API Key）
  const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY)
  const model = genAI.getGenerativeModel({ model: 'gemini-2.5-flash-image' })
  
  try {
    const result = await model.generateContent(req.body.prompt)
    const imageUrl = result.response.text()

    // 4. 记录使用量
    await supabase.from('api_usage').insert({
      user_id: user.id,
      api_type: 'gemini',
      tokens_used: 1
    })

    // 5. 返回结果
    return res.status(200).json({ imageUrl })
  } catch (error) {
    return res.status(500).json({ error: error.message })
  }
}
```

---

#### 阶段 3：部署上线（1 天）

##### 3.1 部署到 Vercel

**步骤**：

1. **连接 GitHub**
```bash
# 确保代码已推送到 GitHub
git push origin main
```

2. **在 Vercel 导入项目**
   - 访问：https://vercel.com/new
   - 选择你的 GitHub 仓库
   - 点击 "Import"

3. **配置环境变量**
```
SUPABASE_URL=你的_Supabase_URL
SUPABASE_KEY=你的_Supabase_Key
GEMINI_API_KEY=你的_Gemini_Key
NANO_BANANA_KEY=你的_Nano_Banana_Key
```

4. **部署**
   - 点击 "Deploy"
   - 等待 2-3 分钟
   - 完成！

5. **获得网址**
```
你的网站：https://your-project.vercel.app
```

##### 3.2 绑定自定义域名（可选）

**如果你买了域名**：

1. 在 Vercel 项目设置中添加域名
2. 在域名服务商添加 DNS 记录：
   ```
   类型: CNAME
   名称: www
   值: cname.vercel-dns.com
   ```
3. 等待 DNS 生效（几分钟到几小时）
4. 完成！访问 https://www.yourdomain.com

---

#### 阶段 4：测试和优化（持续）

##### 4.1 功能测试
```
✅ 用户注册
✅ 用户登录
✅ 密码重置
✅ 创建节点
✅ 生成图片
✅ 生成视频
✅ 保存工作流
✅ 加载工作流
```

##### 4.2 性能优化
```
✅ 图片压缩
✅ 懒加载
✅ CDN 加速
✅ 缓存策略
```

##### 4.3 安全加固
```
✅ HTTPS
✅ CSRF 保护
✅ XSS 防护
✅ SQL 注入防护（Supabase 自动处理）
✅ 速率限制
```

---

## 成本预算

### 免费方案（适合起步）

| 服务 | 提供商 | 免费额度 | 成本 |
|------|--------|---------|------|
| 前端托管 | Vercel | 无限带宽 | 🆓 免费 |
| 数据库 | Supabase | 500MB + 2GB 存储 | 🆓 免费 |
| 认证 | Supabase | 50,000 用户 | 🆓 免费 |
| SSL 证书 | Let's Encrypt | 无限 | 🆓 免费 |
| 域名 | - | - | 💰 60-100元/年 |

**总成本**：约 100 元/年（只需要域名费用）

**适合**：
- 个人项目
- 测试阶段
- 小规模用户（< 1000 人）

---

### 付费方案（适合正式运营）

| 服务 | 提供商 | 配置 | 成本 |
|------|--------|------|------|
| 前端托管 | Vercel Pro | 无限带宽 | 💰 $20/月 |
| 数据库 | Supabase Pro | 8GB + 100GB 存储 | 💰 $25/月 |
| 云服务器 | 阿里云 | 2核4G | 💰 300元/月 |
| CDN | 阿里云 | 按流量 | 💰 100元/月 |
| 域名 | 阿里云 | .com | 💰 100元/年 |

**总成本**：约 700-800 元/月

**适合**：
- 正式产品
- 中等规模用户（1000-10000 人）
- 需要稳定性和性能

---

### 企业方案（适合大规模）

| 服务 | 提供商 | 配置 | 成本 |
|------|--------|------|------|
| 前端托管 | Vercel Enterprise | 定制 | 💰 协商 |
| 数据库 | AWS RDS | 定制 | 💰 2000元/月起 |
| 云服务器 | AWS EC2 | 多台 | 💰 5000元/月起 |
| CDN | Cloudflare | 企业版 | 💰 $200/月起 |
| 域名 | - | - | 💰 100元/年 |

**总成本**：约 10,000+ 元/月

**适合**：
- 大型产品
- 大规模用户（> 10000 人）
- 需要高可用性

---

## 安全注意事项

### 1. API Key 保护 ⚠️⚠️⚠️

**绝对不要**：
- ❌ 把 API Key 写在前端代码里
- ❌ 把 API Key 提交到 GitHub
- ❌ 让用户直接调用 API

**正确做法**：
- ✅ API Key 只存在服务器端
- ✅ 通过后端代理调用 API
- ✅ 使用环境变量存储 Key

### 2. 用户数据保护

**必须做到**：
- ✅ 密码加密存储（Supabase 自动处理）
- ✅ HTTPS 加密传输
- ✅ 定期备份数据
- ✅ 遵守隐私政策

### 3. 防止滥用

**实施措施**：
- ✅ 速率限制（每用户每天 X 次）
- ✅ 验证码（防止机器人）
- ✅ 邮箱验证
- ✅ 监控异常行为

### 4. 法律合规

**需要准备**：
- ✅ 用户协议
- ✅ 隐私政策
- ✅ 版权声明
- ✅ 备案（如果在中国大陆）

---

## 常见问题

### Q1: 我完全不懂后端，能部署吗？
**A**: 可以！使用 Firebase 或 Supabase，它们提供了现成的后端服务，你只需要调用 API 即可。

### Q2: 免费方案够用吗？
**A**: 对于起步阶段（< 1000 用户）完全够用。超过后再升级。

### Q3: 需要备案吗？
**A**: 
- 如果使用国内服务器（阿里云、腾讯云）：需要备案
- 如果使用国外服务器（Vercel、AWS）：不需要备案
- 推荐：先用 Vercel（免费 + 不需要备案）

### Q4: 多久能完成部署？
**A**: 
- 如果使用 Vercel + Supabase：1-2 周
- 如果自己搭建后端：1-2 个月
- 如果完全外包：1-3 个月

### Q5: API 费用怎么算？
**A**: 
- Gemini API：按使用量计费
- 建议：设置每用户每天限额
- 或者：让用户使用自己的 API Key

### Q6: 如何收费？
**A**: 
- 免费版：基础功能 + 每天 10 次生成
- 付费版：$9.9/月，每天 100 次生成
- 企业版：定制价格

---

## 推荐学习路径

### 第 1 周：了解基础概念
```
📚 什么是前后端分离
📚 什么是 API
📚 什么是数据库
📚 什么是认证
```

### 第 2 周：学习 Supabase
```
📚 Supabase 官方文档
📚 创建数据表
📚 使用认证功能
📚 调用 API
```

### 第 3 周：修改前端代码
```
💻 添加登录页面
💻 添加注册页面
💻 保护路由
💻 连接数据库
```

### 第 4 周：部署上线
```
🚀 部署到 Vercel
🚀 配置域名
🚀 测试功能
🚀 优化性能
```

---

## 总结

### 最简单的方案（推荐新手）

```
1. 使用 Vercel（免费托管）
2. 使用 Supabase（免费数据库 + 认证）
3. 不需要买服务器
4. 不需要备案
5. 总成本：100 元/年（域名）
```

### 下一步行动

```
✅ 注册 Vercel 账号
✅ 注册 Supabase 账号
✅ 学习 Supabase 文档（1-2 天）
✅ 修改前端代码，添加登录功能（1 周）
✅ 部署到 Vercel（1 天）
✅ 测试和优化（持续）
```

---

**不要被吓到！一步一步来，从免费方案开始，慢慢学习和优化。** 🚀

**有问题随时问我，我会帮你解决！** 💪
