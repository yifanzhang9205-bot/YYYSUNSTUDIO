# 3D 相机彻底禁止复位

## 修复的问题

### 问题：相机和图片会自动复位 ❌ → ✅

**现象：**
1. 拖拽调整相机角度后，点击其他位置，相机会突然跳回初始角度
2. 图片纹理会重新加载，导致闪烁
3. 无论如何操作，相机总是会"复位"到某个位置

**用户需求：**
> "我不希望相机自己动，他永远跟着我的拖动而动"
> "把复位的能力彻底取消了"

## 根本原因分析

### 原因 1：useEffect 不必要的重新执行
```typescript
// 问题代码
useEffect(() => {
  // 更新相机位置
  cameraRig.position.set(x, y, z);
  // ...
}, [horizontalAngle, verticalAngle, cameraZoom]);
```

**问题：**
- 即使 props 值没变，React 也可能触发重新渲染
- useEffect 会重新执行，导致相机"复位"

### 原因 2：初始化时使用硬编码值
```typescript
// 问题代码
useEffect(() => {
  // 初始化 Three.js 场景
  const cameraRig = new THREE.Group();
  // 相机位置使用硬编码的默认值
  cameraRig.position.set(5, 1.5, 5); // ❌ 硬编码
  // ...
}, []);
```

**问题：**
- 如果组件重新挂载（父组件重新渲染），场景会重新初始化
- 相机会跳回硬编码的默认位置，而不是保持用户调整的位置

### 原因 3：图片纹理重复加载
```typescript
// 问题代码
useEffect(() => {
  loader.load(inputImage, (texture) => {
    // 加载纹理
  });
}, [inputImage]);
```

**问题：**
- 每次组件重新渲染时，即使 `inputImage` 没变，也可能重新加载纹理
- 导致图片闪烁

## 完整修复方案

### 修复 1：添加值比较检查（相机角度）

```typescript
// ✅ 添加 prevAnglesRef 记录上一次的值
const prevAnglesRef = useRef({ h: horizontalAngle, v: verticalAngle, z: cameraZoom });

useEffect(() => {
  if (!sceneRef.current) return;
  
  // ✅ 检查是否真的变化了（避免不必要的更新）
  const prev = prevAnglesRef.current;
  if (prev.h === horizontalAngle && prev.v === verticalAngle && prev.z === cameraZoom) {
    return; // 没有变化，不更新
  }
  
  // ✅ 更新记录
  prevAnglesRef.current = { h: horizontalAngle, v: verticalAngle, z: cameraZoom };
  
  // 更新相机位置...
}, [horizontalAngle, verticalAngle, cameraZoom]);
```

**效果：** 只有角度/距离真正变化时才更新相机

### 修复 2：初始化时使用当前 props 值

```typescript
// ✅ 使用 ref 存储最新的 props 值
const propsRef = useRef({ horizontalAngle, verticalAngle, cameraZoom });
propsRef.current = { horizontalAngle, verticalAngle, cameraZoom };

useEffect(() => {
  // 初始化 Three.js 场景...
  
  // ✅ 初始化后立即设置相机位置为当前 props 值（从 ref 获取最新值）
  const currentProps = propsRef.current;
  const hRad = (currentProps.horizontalAngle * Math.PI) / 180;
  const vRad = (currentProps.verticalAngle * Math.PI) / 180;
  const dist = 2.5 + currentProps.cameraZoom * 0.5;
  
  const x = dist * Math.sin(hRad) * Math.cos(vRad);
  const y = dist * Math.sin(vRad) + 1.5;
  const z = dist * Math.cos(hRad) * Math.cos(vRad);
  
  cameraRig.position.set(x, y, z);
  cameraRig.lookAt(0, 1.5, 0);
  
  // 更新辅助线...
  
  // 观察相机跟随
  const camDist = 10 + currentProps.cameraZoom * 0.15;
  camera.position.set(
    camDist * Math.sin(hRad * 0.1),
    5 + currentProps.verticalAngle * 0.02,
    camDist * Math.cos(hRad * 0.1)
  );
  camera.lookAt(0, 1, 0);
  
  renderer.render(scene, camera);
}, []); // 只在挂载时执行一次
```

**效果：** 组件重新挂载时，相机保持在用户调整的位置

### 修复 3：添加值比较检查（图片纹理）

```typescript
// ✅ 添加 prevImageRef 记录上一次的图片
const prevImageRef = useRef<string | undefined>(undefined);

useEffect(() => {
  if (!sceneRef.current || !inputImage) return;
  
  // ✅ 检查图片是否真的变化了
  if (prevImageRef.current === inputImage) {
    return; // 图片没变，不重新加载
  }
  
  // ✅ 更新记录
  prevImageRef.current = inputImage;
  
  const { card, renderer, scene, camera } = sceneRef.current;
  
  const loader = new THREE.TextureLoader();
  loader.load(inputImage, (texture) => {
    texture.colorSpace = THREE.SRGBColorSpace;
    (card.material as THREE.MeshStandardMaterial).map = texture;
    (card.material as THREE.MeshStandardMaterial).needsUpdate = true;
    renderer.render(scene, camera);
  });
}, [inputImage]);
```

**效果：** 只有图片真正变化时才重新加载纹理

### 修复 4：滚轮正确控制相机/画布

```typescript
const handleWheel = (e: React.WheelEvent) => {
  if (!isExpanded) {
    return; // 不阻止冒泡，让画布处理滚轮事件
  }
  
  // 选中时：阻止事件冒泡和默认行为
  e.stopPropagation(); // 阻止 React 合成事件冒泡
  e.preventDefault(); // 阻止默认滚动行为
  e.nativeEvent.stopImmediatePropagation(); // ✅ 立即停止原生事件传播
  
  // 控制相机...
};
```

**效果：**
- 选中时：滚轮只控制相机，画布完全不动
- 未选中时：滚轮只控制画布，相机完全不动

## 技术细节

### 为什么需要 prevAnglesRef 和 prevImageRef？

React 的 useEffect 依赖数组是**浅比较**：
```typescript
// 即使值相同，React 也可能认为需要重新执行
useEffect(() => {
  // 这里会执行
}, [horizontalAngle, verticalAngle, cameraZoom]);
```

使用 ref 进行**深度比较**：
```typescript
// 只有值真正变化时才执行
const prev = prevAnglesRef.current;
if (prev.h === horizontalAngle && prev.v === verticalAngle && prev.z === cameraZoom) {
  return; // 值没变，跳过更新
}
```

### 为什么需要 propsRef？

初始化 useEffect 的依赖数组是空的 `[]`，意味着它只在组件挂载时执行一次。但是，在初始化时，我们需要使用**当前的 props 值**，而不是硬编码的默认值。

使用 ref 可以在初始化时获取最新的 props：
```typescript
const propsRef = useRef({ horizontalAngle, verticalAngle, cameraZoom });
propsRef.current = { horizontalAngle, verticalAngle, cameraZoom }; // 每次渲染都更新

useEffect(() => {
  // 初始化时使用最新的 props
  const currentProps = propsRef.current;
  // ...
}, []); // 只执行一次，但使用的是最新的 props
```

### 为什么需要 stopImmediatePropagation？

React 的事件系统是**合成事件**：
- `e.stopPropagation()` 只阻止 React 合成事件冒泡
- 原生 DOM 事件还是会冒泡
- `e.nativeEvent.stopImmediatePropagation()` 立即停止原生事件传播

```
用户滚动滚轮
  ↓
原生 wheel 事件
  ↓
React 合成事件 (onWheel)
  ├─ e.stopPropagation() → 阻止 React 事件冒泡
  └─ e.nativeEvent.stopImmediatePropagation() → 阻止原生事件冒泡
```

## 测试步骤

### 测试 1：拖拽后不会复位
1. 打开多角度相机节点
2. 拖拽调整相机角度到某个位置（比如 90°）
3. 点击节点选择框内的其他位置（比如顶部标题栏）
4. **预期：** 相机保持在 90°，不会跳回 0°

### 测试 2：点击其他节点后不会复位
1. 拖拽相机到某个角度
2. 点击画布上的其他节点
3. 再次点击多角度相机节点
4. **预期：** 相机保持在之前的角度，不会复位

### 测试 3：图片不会闪烁
1. 连接图片到多角度相机节点
2. 拖拽调整相机角度
3. 点击其他位置
4. **预期：** 图片不会重新加载，不会闪烁

### 测试 4：选中时滚轮控制相机
1. 点击节点选中（边框变青色）
2. 确认操作提示显示 "拖拽旋转 · 滚轮推进"
3. 滚动鼠标滚轮
4. **预期：**
   - 相机推进/拉远
   - 画布完全不动（不缩放）
   - 距离数值变化

### 测试 5：未选中时滚轮控制画布
1. 点击画布空白处（取消选中）
2. 确认操作提示显示 "点击选中以使用滚轮"
3. 滚动鼠标滚轮
4. **预期：**
   - 画布缩放
   - 相机完全不动（距离不变）

### 测试 6：连续操作不会复位
1. 拖拽相机到 90°
2. 滚轮推进到距离 8
3. 点击设置按钮
4. 关闭设置面板
5. 点击其他节点
6. 再次点击多角度相机节点
7. **预期：** 相机保持在 90°，距离保持在 8

## 修复前后对比

### 修复前 ❌
```
拖拽相机到 90° → 点击其他位置 → 相机跳回 0° ❌
连接图片 → 点击其他位置 → 图片重新加载，闪烁 ❌
选中后滚轮 → 画布也跟着缩放 ❌
```

### 修复后 ✅
```
拖拽相机到 90° → 点击其他位置 → 相机保持 90° ✅
连接图片 → 点击其他位置 → 图片不重新加载 ✅
选中后滚轮 → 只有相机动，画布不动 ✅
```

## 总结

这次修复彻底解决了"复位"问题：

1. ✅ **相机不会自动复位**
   - 使用 `prevAnglesRef` 记录上一次的值
   - 只有值真正变化时才更新相机
   - 初始化时使用当前 props 值（通过 `propsRef`）

2. ✅ **图片不会重新加载**
   - 使用 `prevImageRef` 记录上一次的图片
   - 只有图片真正变化时才重新加载纹理

3. ✅ **滚轮正确控制相机/画布**
   - 使用 `stopImmediatePropagation()` 阻止原生事件传播
   - 选中时：滚轮只控制相机
   - 未选中时：滚轮只控制画布

**核心原则：** 相机永远跟着用户的操作而动，不会自己跳动或复位！
