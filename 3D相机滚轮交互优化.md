# 3D 相机滚轮交互优化

## 优化需求
用户希望：
1. **选中相机节点时**：滚轮控制相机推进/拉远（不影响画布）
2. **未选中时**：滚轮正常控制画布缩放

## 实现方案

### 核心逻辑
根据节点的选中状态（`isExpanded`）决定是否处理滚轮事件：

```typescript
const handleWheel = (e: React.WheelEvent) => {
  // 只有在节点被选中时才控制相机
  if (!isExpanded) {
    return; // 不阻止冒泡，让画布处理滚轮事件
  }
  
  e.stopPropagation(); // 阻止冒泡到画布
  
  // 控制相机推进/拉远...
  const newZoom = Math.max(0, Math.min(10, cameraZoom + e.deltaY * 0.008));
  // 更新 Three.js 场景...
  onZoomChange(newZoom);
};
```

### 关键点

#### 1. 选中状态判断
使用 `isExpanded` prop 判断节点是否被选中：
- `isExpanded = true` → 节点被选中，滚轮控制相机
- `isExpanded = false` → 节点未选中，滚轮控制画布

#### 2. 事件冒泡控制
- **选中时**：调用 `e.stopPropagation()` 阻止事件冒泡到画布
- **未选中时**：不调用 `e.stopPropagation()`，让事件冒泡到画布

#### 3. 操作提示动态显示
根据选中状态显示不同的提示：
```typescript
{isExpanded ? '拖拽旋转 · 滚轮推进' : '点击选中以使用滚轮'}
```

## 交互流程

### 场景 1：节点未选中
```
用户滚动滚轮
  → handleWheel 检测到 isExpanded = false
  → 不阻止事件冒泡
  → 事件传递到画布
  → 画布缩放
```

**效果：** 正常的画布缩放行为

### 场景 2：节点已选中
```
用户点击节点选中
  → isExpanded = true
  → 操作提示变为 "拖拽旋转 · 滚轮推进"

用户滚动滚轮
  → handleWheel 检测到 isExpanded = true
  → 调用 e.stopPropagation() 阻止冒泡
  → 直接更新 Three.js 场景
  → 相机推进/拉远
  → 画布不受影响
```

**效果：** 相机推进/拉远，画布不动

## 操作说明

### 1. 选中节点
- **方法：** 点击节点的任意位置
- **视觉反馈：**
  - 节点边框变为青色高光
  - 操作提示变为 "拖拽旋转 · 滚轮推进"

### 2. 使用滚轮控制相机
- **前提：** 节点已选中
- **操作：** 滚动鼠标滚轮
- **效果：**
  - 向上滚 → 拉远（远景）
  - 向下滚 → 推进（特写）
  - 画布不受影响

### 3. 取消选中
- **方法：** 点击画布空白处或其他节点
- **效果：** 滚轮恢复控制画布缩放

## 其他操作方式

### 拖拽旋转（始终可用）
- **操作：** 在 3D 视口中拖拽
- **效果：**
  - 水平拖动 → 改变方位角
  - 垂直拖动 → 改变仰角

### Shift + 拖拽推进（始终可用）
- **操作：** 按住 Shift + 拖拽
- **效果：**
  - 向上拖 → 拉远
  - 向下拖 → 推进

**注意：** Shift + 拖拽方式仍然保留，作为备选操作

## 优势

### 1. 更直观的交互
- 滚轮是最常用的缩放操作
- 符合用户的操作习惯
- 不需要记住 Shift 键

### 2. 避免冲突
- 选中时：滚轮控制相机
- 未选中时：滚轮控制画布
- 两种行为互不干扰

### 3. 清晰的视觉反馈
- 操作提示动态变化
- 用户清楚当前的操作模式

## 测试步骤

### 测试 1：未选中时滚轮控制画布
1. 打开多角度相机节点
2. **不要点击节点**（保持未选中状态）
3. 滚动鼠标滚轮
4. **预期：** 画布缩放，相机不动

### 测试 2：选中后滚轮控制相机
1. 点击多角度相机节点（选中）
2. 确认边框变为青色
3. 确认操作提示变为 "拖拽旋转 · 滚轮推进"
4. 滚动鼠标滚轮
5. **预期：** 相机推进/拉远，画布不动

### 测试 3：取消选中后恢复画布控制
1. 选中多角度相机节点
2. 点击画布空白处（取消选中）
3. 滚动鼠标滚轮
4. **预期：** 画布缩放，相机不动

### 测试 4：Shift + 拖拽仍然可用
1. 选中或未选中都可以
2. 按住 Shift + 在 3D 视口中拖拽
3. **预期：** 相机推进/拉远

## 技术细节

### 事件传播机制
```
滚轮事件触发顺序：
1. MultiAngleCameraNode.handleWheel
2. Node 组件的 onWheel
3. 画布的 onWheel

如果在步骤 1 调用 e.stopPropagation()：
  → 事件不会传递到步骤 2 和 3
  → 只有相机响应

如果在步骤 1 不调用 e.stopPropagation()：
  → 事件继续传递
  → 最终由画布处理
```

### 性能说明
- 滚轮事件处理是同步的，无延迟
- 直接操作 Three.js，不经过 React state
- 立即渲染，60 FPS 流畅体验

## 总结

优化后的交互：
- ✅ 选中时滚轮控制相机（直观）
- ✅ 未选中时滚轮控制画布（不冲突）
- ✅ 动态操作提示（清晰反馈）
- ✅ 保留 Shift + 拖拽方式（备选）
- ✅ 60 FPS 流畅体验（性能优化）

现在的交互更加符合用户习惯，操作更加直观！
