# 组右键菜单 - 阻止浏览器默认菜单

**日期**：2026-02-09  
**状态**：✅ 完成  
**影响范围**：组功能 - 右键菜单

---

## 🎯 问题描述

右键点击组的时候，会同时出现：
1. ✅ 自定义的右键菜单（正确）
2. ❌ 浏览器默认的右键菜单（不应该出现）

### 根本原因

组的 `onContextMenu` 事件处理中：
- ✅ 有 `e.stopPropagation()`（阻止事件冒泡）
- ❌ **缺少 `e.preventDefault()`**（阻止浏览器默认行为）

---

## 💡 解决方案

在 `onContextMenu` 事件处理中添加 `e.preventDefault()`。

### 修改文件
`App.tsx`（第1751行）

### 修改前
```typescript
onContextMenu={e => { 
    e.stopPropagation(); 
    openContextMenu({visible:true, x:e.clientX, y:e.clientY, id:g.id}, {type:'group', id:g.id}); 
}}
```

### 修改后
```typescript
onContextMenu={e => { 
    e.preventDefault(); // 🔥 阻止浏览器默认右键菜单
    e.stopPropagation(); 
    openContextMenu({visible:true, x:e.clientX, y:e.clientY, id:g.id}, {type:'group', id:g.id}); 
}}
```

---

## 📚 知识点

### `e.preventDefault()` vs `e.stopPropagation()`

| 方法 | 作用 | 用途 |
|------|------|------|
| `e.preventDefault()` | 阻止浏览器默认行为 | 阻止右键菜单、链接跳转、表单提交等 |
| `e.stopPropagation()` | 阻止事件冒泡 | 防止事件传递到父元素 |

### 右键菜单的正确处理

```typescript
onContextMenu={e => {
    e.preventDefault();    // 🔥 必须：阻止浏览器默认右键菜单
    e.stopPropagation();   // 🔥 必须：阻止事件冒泡到画布
    // 显示自定义右键菜单
    openContextMenu({...});
}}
```

---

## ✅ 验证结果

### 修复前
```
右键点击组
  ↓
✅ 显示自定义右键菜单
❌ 同时显示浏览器默认右键菜单（不应该）
```

### 修复后
```
右键点击组
  ↓
✅ 显示自定义右键菜单
✅ 不显示浏览器默认右键菜单（正确）
```

---

## 🎯 其他地方的检查

让我检查一下其他地方是否也有类似的问题：

### 画布右键菜单（第1582行）
```typescript
onContextMenu={(e) => { 
    e.preventDefault(); // ✅ 有
    if(e.target === e.currentTarget) closeContextMenu(); 
}}
```
✅ 正确

### 节点右键菜单（第1953行）
```typescript
onContextMenu={(e) => { 
    e.preventDefault(); // ✅ 有
    e.stopPropagation(); 
    // ...
}}
```
✅ 正确

### 组右键菜单（第1751行）
```typescript
onContextMenu={e => { 
    e.preventDefault(); // ✅ 现在有了
    e.stopPropagation(); 
    // ...
}}
```
✅ 已修复

---

## 🎉 总结

成功修复了组右键菜单的问题，现在右键点击组时：
- ✅ 只显示自定义右键菜单
- ✅ 不显示浏览器默认右键菜单
- ✅ 不影响其他功能
- ✅ 代码逻辑清晰

**关键点：**
- `e.preventDefault()` 阻止浏览器默认行为
- `e.stopPropagation()` 阻止事件冒泡
- 两者都需要，缺一不可
