---
inclusion: always
---

# 项目入职规则（必读）

> ⚠️ **强制规则**：任何新接手此项目的 AI 必须先执行以下步骤

---

## 🌐 语言规则

**必须使用中文进行所有交流和文档编写。**

- ✅ 回复用户：中文
- ✅ 编写代码注释：中文
- ✅ 编写文档：中文
- ✅ 提交信息：中文
- ❌ 禁止使用英文（除非代码本身需要）

---

## 📖 入职流程（强制执行）

### 🔴🔴🔴 第 0 步：阅读架构文档（最高优先级，必须最先阅读）

> ⚠️ **这是第一步！必须完整阅读，不能跳过！**

在开始任何工作前，**必须最先完整阅读架构文档**：

```
#[[file:ARCHITECTURE.md]]
```

**⚠️ 强制要求：必须从头到尾完整阅读，不能只读部分！**

**这份文档定义了：**
- �️ **代码组织规则**：三层架构（UI → Hooks → Core）
- 🎯 **新增功能的唯一入口**：添加节点、交互、数据、计算的正确位置
- 🚫 **禁止的开发模式**：不能在 App.tsx 写业务逻辑
- � **必须遵守的*原则**：职责分离、单向数据流
- � **目录结构**：每个文件夹的职责
- 🔧 **开发规范**：如何正确地添加新功能

**为什么这份文档最重要？**
1. **防止乱改代码**：知道什么能改、什么不能改
2. **找到正确位置**：知道新功能应该加在哪里
3. **避免破坏架构**：不会因为不了解规则而破坏项目结构
4. **提高效率**：不用猜测，直接按规则做

**阅读后必须回答：**
- [ ] 三层架构是什么？（UI Layer、Hooks Layer、Core Layer）
- [ ] App.tsx 的唯一职责是什么？（组合和配置，不写业务逻辑）
- [ ] 添加新节点应该改哪个文件？（core/registry/NodeRegistry.ts）
- [ ] 添加新交互应该改哪个文件？（hooks/ 对应的 Hook）
- [ ] 添加新数据应该改哪个文件？（core/stores/ 对应的 Store）
- [ ] 为什么不能在 App.tsx 写业务逻辑？（职责分离，方便维护和扩展）
- [ ] 什么情况下可以修改 App.tsx？（架构调整、重构、紧急修复、用户明确要求）

---

### 🔴 第 1 步：阅读重构需求文档（第二优先级，必须完整阅读）

> ⚠️ **这是第一原则！所有工作必须围绕这个需求文档进行！**

在理解了架构规则后，**必须完整阅读重构需求文档**：

```
#[[file:.kiro/specs/canvas-architecture-refactor/requirements.md]]
```

**⚠️ 强制要求：必须使用 readFile 完整阅读，不能只读部分！**

**这份文档是项目的最高指导原则，定义了：**
- 🎯 **核心目标**：让以后加功能时，不再碰 App.tsx
- 📋 **重构计划**：三步走策略（修复 Bug → 抽离底层 → 抽离交互）
- 🏗️ **架构设计**：三层分离（UI Layer → Hooks Layer → Core Layer）
- 📊 **实施路径**：阶段 A、B、C 的详细计划
- ✅ **验收标准**：功能、结构、扩展性、性能的具体要求
- 🚫 **禁止事项**：不允许做的事情
- 💡 **核心原则**：先隔离再优化、功能不变结构变、渐进式重构
- 🚀 **性能优化**：虚拟化渲染、RAF 拖拽、Canvas 连接线
- 📦 **模块化设计**：目录结构、职责划分、状态管理
- 🔧 **节点注册表**：NodeRegistry 设计和使用
- 📝 **实施计划**：13 天详细时间表

**为什么这份文档重要？**
1. **统一目标**：所有 AI 都按照同一个计划工作
2. **避免偏离**：不会因为个人理解不同而走偏
3. **可追溯**：所有决策都有依据
4. **可验收**：有明确的完成标准
5. **完整信息**：包含所有技术细节和实施方案

**阅读后必须回答：**
- [ ] 重构的核心目标是什么？
- [ ] 当前处于哪个阶段？完成度多少？
- [ ] 下一步要做什么？
- [ ] 有哪些禁止事项？
- [ ] 虚拟化渲染的原理是什么？
- [ ] RAF 拖拽优化的原理是什么？
- [ ] NodeRegistry 的作用是什么？
- [ ] 三层架构的职责划分是什么？

---

### 第 2 步：理解三层架构（必须）

```
第 1 层：UI Layer (展示)
  - 只负责渲染，不管逻辑
  - App.tsx, Canvas.tsx, Node.tsx

第 2 层：Hooks Layer (交互)
  - 处理交互逻辑，调用 Store 和 Core
  - useDrag.ts, useSelection.ts, useConnection.ts

第 3 层：Core Layer (底层)
  - 数据管理 + 纯计算，不依赖 React
  - stores/, utils/, registry/
```

### 第 3 步：记住核心规则（必须）

| 需求 | 唯一入口 | 禁止 |
|------|---------|------|
| 添加新节点 | `core/registry/NodeRegistry.ts` | ❌ 改 App.tsx |
| 添加新交互 | `hooks/` 对应的 Hook | ❌ 在 Node.tsx 塞逻辑 |
| 添加新数据 | `core/stores/` 对应的 Store | ❌ 在 App.tsx 加 useState |
| 添加新计算 | `core/utils/geometry.ts` | ❌ 在 UI 文件里写计算 |

### 第 4 步：查看当前进度（必须）

查看当前重构的完成度和下一步计划：

```
#[[file:架构重构-阶段A-最终完成报告-2026-01-27.md]]
```

**这份文档告诉你：**
- 当前完成度：70%
- 已完成的工作：Hooks 集成、Bug 修复
- 未完成的工作：业务逻辑抽离（handleNodeAction 等）
- 下一步计划：继续重构 → 抽离业务逻辑 → 清理 App.tsx

---

## 🎯 工作原则（永远记住）

### 原则 0：一切围绕需求文档

**requirements.md 是第一原则！**

- ✅ 所有工作必须符合 requirements.md 的目标
- ✅ 所有决策必须参考 requirements.md 的计划
- ✅ 所有验收必须对照 requirements.md 的标准
- ❌ 不允许偏离 requirements.md 的方向

**如果发现需求文档有问题：**
1. 立即告知用户
2. 讨论修改方案
3. 更新需求文档
4. 继续按新文档执行

**如果用户要求与需求文档冲突：**
1. 指出冲突点
2. 说明可能的影响
3. 建议解决方案
4. 等待用户决策

---

## 🚫 禁止的操作（违反将被拒绝）

1. ❌ **禁止在 App.tsx 写业务逻辑**
   - App.tsx 只能做组合和配置，不能写业务逻辑
   - 不是控制行数，而是控制职责
   - **例外**：重构过程中、架构调整、用户明确要求、紧急修复时可以修改

2. ❌ **禁止在 UI 组件里写几何计算**
   - 所有计算必须在 `core/utils/geometry.ts`

3. ❌ **禁止跨层混用**
   - UI 组件 → Hook → Store（单向数据流）
   - 不能跳过 Hook 直接操作 Store

4. ❌ **禁止 Map/Array 混用**
   - Map 就用 Map 的操作（set, get, delete）
   - 不能用数组展开 `[...map]`

5. ❌ **禁止使用英文回复用户**
   - 所有交流必须使用中文

---

## ✅ 允许修改 App.tsx 的情况

虽然 App.tsx 是高优先级保护区，但以下情况**允许**修改：

### 1. 架构调整
- 添加新的全局 Provider（如 Zustand Store Provider）
- 调整路由结构
- 修改应用入口逻辑

### 2. 重构过程中（当前阶段）
- 正在实施架构重构
- 需要抽离逻辑到 Hooks/Stores
- 需要修复致命 Bug

### 3. 用户明确要求
- 用户说"修改 App.tsx"
- 用户说"必须改 App.tsx"
- 用户批准了修改方案

### 4. 紧急修复
- 修复崩溃问题
- 修复数据丢失问题
- 修复安全漏洞

### ⚠️ 修改 App.tsx 的原则（必须遵守）

**即使允许修改，也要遵守：**

1. **说清楚为什么要动**
   - 为什么必须改 App.tsx？
   - 不改会有什么问题？
   - 有没有其他方案不修改 App.tsx？

2. **说清楚要怎么动**
   - 具体要改哪些行？
   - 要添加/删除/修改什么？
   - 改动的范围有多大？

3. **说清楚有什么后果**
   - 会影响哪些功能？
   - 会不会引入新的 Bug？
   - 会不会影响性能？
   - 会不会影响其他开发者？

4. **等待用户确认**
   - 不要自作主张
   - 必须得到用户明确同意
   - 如果用户说"不"，就找其他方案

5. **最小化改动**
   - 只改必须改的部分
   - 不顺便"优化"其他代码

6. **保持简洁**
   - 不添加业务逻辑
   - 只做组合和配置

7. **记录原因**
   - 说明为什么要改
   - 说明改了什么
   - 说明影响范围

---

## ✅ 开始工作前的检查清单

在开始任何开发工作前，确认：

- [ ] 🔴🔴🔴 已完整阅读 `ARCHITECTURE.md`（最高优先级，必须最先阅读）
- [ ] 🔴 已完整阅读 `.kiro/specs/canvas-architecture-refactor/requirements.md`（第二优先级）
- [ ] 理解三层架构（UI → Hooks → Core）
- [ ] 知道添加新功能的唯一入口
- [ ] 记住禁止的操作（不能在 App.tsx 写业务逻辑）
- [ ] 了解当前重构进度（70%）
- [ ] 知道下一步要做什么
- [ ] 使用中文进行所有交流

**特别提醒：**
- 🔴🔴🔴 ARCHITECTURE.md 是第一步，必须最先阅读，不能跳过
- 🔴 requirements.md 是第一原则，所有工作围绕它进行
- 🔴 不允许偏离 requirements.md 的目标和计划
- 🔴 如果有疑问，先查 ARCHITECTURE.md，再查 requirements.md，最后问用户

---

## 📚 快速参考

### 必读文档（按优先级排序）

1. **🔴🔴🔴 架构文档**（最高优先级，必须最先阅读）
   - 路径：`ARCHITECTURE.md`
   - 作用：代码组织规则、开发规范、三层架构
   - 必须：最先阅读，完整理解，不能跳过
   - 为什么：防止乱改代码，知道新功能应该加在哪里

2. **🔴 重构需求文档**（第二优先级）
   - 路径：`.kiro/specs/canvas-architecture-refactor/requirements.md`
   - 作用：项目的第一原则，所有工作的指导方针
   - 必须：第二个阅读，完整理解

3. **当前进度报告**
   - 路径：`架构重构-阶段A-最终完成报告-2026-01-27.md`
   - 作用：了解当前完成度和下一步计划
   - 必须：第三个阅读

4. **快速参考**
   - 路径：`.kiro/QUICK_REFERENCE.md`
   - 作用：常用命令和快捷方式
   - 可选：需要时查阅

5. **AI 工作规范**
   - 路径：`AI工作规范-必读.md`
   - 作用：AI 协作规范
   - 可选：需要时查阅

### 目录结构

```
src/
├── App.tsx                        # 主入口（只做组合和配置）
├── core/                          # 底层（不依赖 React）
│   ├── stores/                    # 数据管理（Zustand）
│   ├── utils/                     # 纯计算
│   └── registry/                  # 注册表
├── hooks/                         # 交互逻辑
└── components/                    # UI 组件
```

---

## 🎯 核心目标（永远记住）

**不是控制代码行数，而是控制代码职责。**

**App.tsx 是高优先级保护区，轻易不能动。**

**如果要动，必须说清楚：为什么、怎么动、有什么后果。**

**以后加功能，通过扩展模块实现，而不是修改 App.tsx。**

---

## ⚠️ 违规后果

违反本文档规则的代码：
- ❌ 将被拒绝合并
- ❌ 需要重新编写
- ❌ 可能导致项目混乱

---

**记住：这不是建议，这是规则。**
