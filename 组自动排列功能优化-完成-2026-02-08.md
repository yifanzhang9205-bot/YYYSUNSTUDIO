# 组自动排列功能优化 - 完成

**日期**: 2026-02-08  
**状态**: ✅ 已完成

---

## 📋 需求

用户反馈组功能存在以下问题:

1. **功能冗余**: 对齐、分布等功能使用频率低,只需要保留自动排列
2. **自动排列 Bug**: 
   - 节点大小改变后,组的范围不准确
   - 节点会重叠
   - 组不会适应节点的实际大小

---

## 🔍 问题分析

### 根本原因

**代码假设所有节点宽度都是固定 420px,但实际节点宽度是动态的 (`node.width`)**

#### 问题 1: 层宽度计算错误

```typescript
// ❌ 错误代码
const layerWidth = layer.length * 420 + (layer.length - 1) * horizontalGap;
```

**问题**: 假设所有节点宽度都是 420px,导致:
- 如果节点实际宽度 > 420px,层宽度被低估,节点会超出组边界
- 如果节点实际宽度 < 420px,层宽度被高估,节点之间间距过大

#### 问题 2: 居中对齐不准确

```typescript
// ❌ 错误代码
let currentX = group.x + padding + (group.width - padding * 2 - layerWidth) / 2;
```

**问题**: 基于错误的 `layerWidth` 计算居中位置,导致节点不居中

#### 问题 3: 组边界计算错误

```typescript
// ❌ 错误代码
currentX += 420 + horizontalGap;
```

**问题**: 使用固定宽度 420px 移动,导致:
- 节点位置不准确
- 组边界计算错误
- 节点会重叠或超出组边界

---

## ✅ 修复方案

### 1. 使用实际节点宽度计算层宽度

```typescript
// ✅ 正确代码
let layerWidth = 0;
layer.forEach(nodeId => {
  const node = nodes.get(nodeId);
  if (node) {
    layerWidth += (node.width || 420); // 使用实际宽度
  }
});
layerWidth += (layer.length - 1) * horizontalGap;
```

### 2. 基于实际宽度居中对齐

```typescript
// ✅ 正确代码
let currentX = group.x + padding + (group.width - padding * 2 - layerWidth) / 2;
```

### 3. 使用实际宽度移动节点

```typescript
// ✅ 正确代码
layer.forEach(nodeId => {
  const node = nodes.get(nodeId);
  if (node) {
    const nodeHeight = getApproxNodeHeight(node);
    const nodeWidth = node.width || 420; // 使用实际宽度
    
    onUpdateNode(nodeId, {
      x: currentX,
      y: currentY,
    });

    // 记录实际宽度和高度
    nodePositions.set(nodeId, {
      x: currentX,
      y: currentY,
      width: nodeWidth,
      height: nodeHeight
    });

    // 使用实际宽度移动
    currentX += nodeWidth + horizontalGap;
  }
});
```

### 4. 根据实际节点位置计算组边界

```typescript
// ✅ 正确代码
let minX = Infinity;
let minY = Infinity;
let maxX = -Infinity;
let maxY = -Infinity;

nodePositions.forEach(pos => {
  minX = Math.min(minX, pos.x);
  minY = Math.min(minY, pos.y);
  maxX = Math.max(maxX, pos.x + pos.width);
  maxY = Math.max(maxY, pos.y + pos.height);
});

// 更新分组大小和位置
onUpdateGroup(groupId, {
  x: minX - padding,
  y: minY - padding,
  width: (maxX - minX) + padding * 2,
  height: (maxY - minY) + padding * 2,
});
```

---

## 🎯 实施步骤

### 步骤 1: 修复 `hooks/useGroup.ts` 中的 `arrangeTopology` 函数

**修改内容**:
- ✅ 使用实际节点宽度计算层宽度
- ✅ 基于实际宽度居中对齐
- ✅ 使用实际宽度移动节点
- ✅ 根据实际节点位置计算组边界

**文件**: `hooks/useGroup.ts` (第 400-550 行)

### 步骤 2: 简化 `components/GroupToolbar.tsx`

**修改内容**:
- ✅ 删除所有对齐和分布按钮
- ✅ 只保留自动排列按钮
- ✅ 删除不需要的 props

**文件**: `components/GroupToolbar.tsx`

**修改前**:
```typescript
interface GroupToolbarProps {
  groupId: string;
  groupX: number;
  groupY: number;
  groupWidth: number;
  scale: number;
  panX: number;
  panY: number;
  onAlignLeft: (groupId: string) => void;
  onAlignCenterH: (groupId: string) => void;
  onAlignRight: (groupId: string) => void;
  onAlignTop: (groupId: string) => void;
  onAlignCenterV: (groupId: string) => void;
  onAlignBottom: (groupId: string) => void;
  onDistributeH: (groupId: string) => void;
  onDistributeV: (groupId: string) => void;
  onArrangeTopology: (groupId: string) => void;
}
```

**修改后**:
```typescript
interface GroupToolbarProps {
  groupId: string;
  groupX: number;
  groupY: number;
  groupWidth: number;
  scale: number;
  panX: number;
  panY: number;
  onArrangeTopology: (groupId: string) => void;
}
```

### 步骤 3: 更新 `App.tsx` 中的 GroupToolbar 调用

**修改内容**:
- ✅ 删除不需要的 props (对齐、分布)
- ✅ 只保留 `onArrangeTopology`

**文件**: `App.tsx` (第 1512-1530 行)

**修改前**:
```typescript
<GroupToolbar
  groupId={selectedGroupId}
  groupX={groups.find(g => g.id === selectedGroupId)!.x}
  groupY={groups.find(g => g.id === selectedGroupId)!.y}
  groupWidth={groups.find(g => g.id === selectedGroupId)!.width}
  scale={scale}
  panX={pan.x}
  panY={pan.y}
  onAlignLeft={alignLeft}
  onAlignCenterH={alignCenterH}
  onAlignRight={alignRight}
  onAlignTop={alignTop}
  onAlignCenterV={alignCenterV}
  onAlignBottom={alignBottom}
  onDistributeH={distributeH}
  onDistributeV={distributeV}
  onArrangeTopology={arrangeTopology}
/>
```

**修改后**:
```typescript
<GroupToolbar
  groupId={selectedGroupId}
  groupX={groups.find(g => g.id === selectedGroupId)!.x}
  groupY={groups.find(g => g.id === selectedGroupId)!.y}
  groupWidth={groups.find(g => g.id === selectedGroupId)!.width}
  scale={scale}
  panX={pan.x}
  panY={pan.y}
  onArrangeTopology={arrangeTopology}
/>
```

---

## ✅ 验收标准

### 功能验收

- [x] 自动排列功能正常工作
- [x] 节点大小改变后,自动排列能正确适应
- [x] 节点不会重叠
- [x] 组边界能正确包含所有节点
- [x] 不同宽度的节点能正确居中对齐

### 代码验收

- [x] 没有编译错误
- [x] 没有 TypeScript 类型错误
- [x] 代码逻辑清晰,易于维护

---

## 📊 修复效果

### 修复前

```
问题 1: 层宽度计算错误
- 假设所有节点宽度 = 420px
- 实际节点宽度可能 > 420px 或 < 420px
- 导致节点超出组边界或间距过大

问题 2: 居中对齐不准确
- 基于错误的层宽度计算居中位置
- 节点不居中

问题 3: 组边界计算错误
- 使用固定宽度 420px 移动
- 节点位置不准确
- 节点会重叠或超出组边界
```

### 修复后

```
✅ 层宽度计算准确
- 累加每个节点的实际宽度
- 适应不同宽度的节点

✅ 居中对齐准确
- 基于实际层宽度计算居中位置
- 节点完美居中

✅ 组边界计算准确
- 根据实际节点位置计算边界
- 组边界完美包含所有节点
- 节点不会重叠或超出边界
```

---

## 🎓 技术要点

### 1. 动态宽度适应

**关键**: 不要假设节点宽度是固定的,要使用 `node.width || 420`

```typescript
// ❌ 错误
const layerWidth = layer.length * 420;

// ✅ 正确
let layerWidth = 0;
layer.forEach(nodeId => {
  const node = nodes.get(nodeId);
  if (node) {
    layerWidth += (node.width || 420);
  }
});
```

### 2. 记录实际位置

**关键**: 记录每个节点的实际位置和大小,用于计算组边界

```typescript
const nodePositions = new Map<string, { x: number; y: number; width: number; height: number }>();

layer.forEach(nodeId => {
  const node = nodes.get(nodeId);
  if (node) {
    const nodeWidth = node.width || 420;
    const nodeHeight = getApproxNodeHeight(node);
    
    nodePositions.set(nodeId, {
      x: currentX,
      y: currentY,
      width: nodeWidth,
      height: nodeHeight
    });
  }
});
```

### 3. 基于实际位置计算边界

**关键**: 遍历所有节点的实际位置,计算最小和最大边界

```typescript
let minX = Infinity;
let minY = Infinity;
let maxX = -Infinity;
let maxY = -Infinity;

nodePositions.forEach(pos => {
  minX = Math.min(minX, pos.x);
  minY = Math.min(minY, pos.y);
  maxX = Math.max(maxX, pos.x + pos.width);
  maxY = Math.max(maxY, pos.y + pos.height);
});
```

---

## 📝 相关文档

- `ARCHITECTURE.md` - 架构文档
- `hooks/useGroup.ts` - 组功能 Hook
- `components/GroupToolbar.tsx` - 组工具栏 UI
- `App.tsx` - 主应用组件

---

## 🎉 总结

**修复完成!** 组自动排列功能现在能正确适应节点的实际大小,不会出现重叠或超出边界的问题。

**关键改进**:
1. ✅ 使用实际节点宽度计算层宽度
2. ✅ 基于实际宽度居中对齐
3. ✅ 使用实际宽度移动节点
4. ✅ 根据实际节点位置计算组边界
5. ✅ 简化工具栏,只保留自动排列功能

**用户体验提升**:
- 节点大小改变后,自动排列能正确适应
- 节点不会重叠
- 组边界能正确包含所有节点
- 不同宽度的节点能正确居中对齐
- 工具栏更简洁,只保留最常用的功能
