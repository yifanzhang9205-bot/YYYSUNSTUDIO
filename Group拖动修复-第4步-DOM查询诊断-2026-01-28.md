# Group 拖动修复 - 第 4 步：DOM 查询诊断

**日期**: 2026-01-28  
**状态**: 🔍 诊断中  
**问题**: `document.querySelector([data-node-id="..."])` 找不到节点

---

## 问题描述

Group 拖动时，子节点不跟随移动。日志显示：

```
[useGroup] 找不到节点: n-1769531533282-0ow6ixebr
```

---

## 代码检查

### 1. Node.tsx 中的 DOM 属性（✅ 正确）

```tsx
<div 
    id={`node-${node.id}`}
    data-node-id={node.id}
    ...
>
```

- `id`: `node-n-1769531533282-0ow6ixebr`
- `data-node-id`: `n-1769531533282-0ow6ixebr`

### 2. useGroup Hook 中的选择器（✅ 正确）

```ts
const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
```

- 选择器: `[data-node-id="n-1769531533282-0ow6ixebr"]`

### 3. 渲染逻辑（✅ 正确）

App.tsx 中正确渲染了 Node 组件：

```tsx
<Node
    key={node.id} 
    node={node} 
    ...
/>
```

---

## 可能的原因

### 假设 1：节点还没渲染到 DOM

**原因**: `startGroupDrag` 在 `onMouseDown` 时立即执行，此时 React 可能还没有完成渲染。

**测试方法**: 
1. 添加 `requestAnimationFrame` 延迟查询
2. 检查 DOM 中是否真的存在这些节点

### 假设 2：节点在不同的 DOM 层级

**原因**: 节点可能在 Group 的子元素中，或者被其他元素遮挡。

**测试方法**: 
1. 使用 `document.querySelectorAll` 查找所有匹配的节点
2. 检查节点的父元素

### 假设 3：选择器语法问题

**原因**: 属性选择器可能有特殊字符需要转义。

**测试方法**: 
1. 尝试使用 `getElementById` 代替
2. 尝试使用 `document.querySelector('#node-' + child.id)`

### 假设 4：React 虚拟 DOM 延迟

**原因**: React 的批量更新可能导致 DOM 还没有更新。

**测试方法**: 
1. 使用 `setTimeout` 延迟查询
2. 使用 `flushSync` 强制同步更新

---

## 诊断步骤

### 步骤 1：添加详细日志（✅ 已完成）

在 `startGroupDrag` 中添加了详细日志：

```ts
console.log('[useGroup] 查找节点:', {
  childId: child.id,
  selector,
  found: !!nodeElement,
  element: nodeElement,
});
```

### 步骤 2：在浏览器中检查 DOM（⏳ 待执行）

打开浏览器开发者工具，检查：

1. 节点是否存在于 DOM 中
2. `data-node-id` 属性是否正确
3. 节点的父元素是什么

### 步骤 3：尝试替代选择器（⏳ 待执行）

如果 `querySelector` 失败，尝试：

```ts
// 方法 1：使用 getElementById
const byId = document.getElementById(`node-${child.id}`);

// 方法 2：使用 querySelectorAll
const all = document.querySelectorAll(`[data-node-id="${child.id}"]`);

// 方法 3：延迟查询
requestAnimationFrame(() => {
  const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`);
});
```

### 步骤 4：检查 React 渲染时机（⏳ 待执行）

在 Node.tsx 中添加日志：

```tsx
useEffect(() => {
  console.log('[Node] 渲染完成:', node.id);
}, [node.id]);
```

---

## 下一步

1. **运行项目**，查看详细日志
2. **在浏览器中检查 DOM**，确认节点是否存在
3. **根据日志结果**，选择合适的修复方案

---

## 修复方案（待确认）

### 方案 A：延迟查询（推荐）

```ts
const startGroupDrag = useCallback((e: React.MouseEvent, groupId: string, group: Group) => {
  // ... 其他代码 ...
  
  // 延迟查询，等待 React 渲染完成
  requestAnimationFrame(() => {
    childNodes.forEach((child) => {
      const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
      if (nodeElement) {
        nodeElement.style.transition = 'none';
        nodeElement.style.willChange = 'transform';
      }
    });
  });
}, [getGroupNodes]);
```

### 方案 B：使用 getElementById

```ts
childNodes.forEach((child) => {
  const nodeElement = document.getElementById(`node-${child.id}`);
  if (nodeElement) {
    nodeElement.style.transition = 'none';
    nodeElement.style.willChange = 'transform';
  }
});
```

### 方案 C：使用 Ref 缓存 DOM 元素

在 Node.tsx 中：

```tsx
const nodeRef = useRef<HTMLDivElement>(null);

useEffect(() => {
  if (nodeRef.current) {
    // 将 ref 存储到全局 Map
    window.nodeElementsMap = window.nodeElementsMap || new Map();
    window.nodeElementsMap.set(node.id, nodeRef.current);
  }
}, [node.id]);

return (
  <div ref={nodeRef} data-node-id={node.id} ...>
```

在 useGroup Hook 中：

```ts
const nodeElement = window.nodeElementsMap?.get(child.id);
```

---

## 总结

当前问题是 DOM 查询找不到节点。已添加详细日志，等待运行项目后查看结果，然后选择合适的修复方案。

**最可能的原因**: React 渲染延迟，需要使用 `requestAnimationFrame` 延迟查询。
