# 节点拖动滞涩感 - 优化方案

**日期**: 2026-02-08  
**问题**: 拖动节点时仍然有滞涩感  
**状态**: 已诊断，待实施

---

## 🔍 问题诊断

### 已实施的优化（确认存在）✅
1. ✅ Transform + RAF 方案（`useDrag.ts` 第 155-180 行）
2. ✅ Transition 禁用（`useDrag.ts` 第 199-200 行）
3. ✅ 辅助线检测节流（每 3 帧，`useDrag.ts` 第 189-203 行）
4. ✅ GPU 加速（`useDrag.ts` 第 332 行）

### 🔥 发现的性能瓶颈

**主要问题：连接线实时更新开销过大**

在 `App.tsx` 第 1602-1625 行，每条连接线都在**每次渲染时**执行 DOM 查询：

```typescript
// 🔥 性能瓶颈：每条连接线都查询 DOM（O(n) 复杂度）
const fElement = document.querySelector(`[data-node-id="${conn.from}"]`) as HTMLElement;
const tElement = document.querySelector(`[data-node-id="${conn.to}"]`) as HTMLElement;
const fOffsetX = fElement ? parseFloat(fElement.style.getPropertyValue('--drag-offset-x') || '0') : 0;
const fOffsetY = fElement ? parseFloat(fElement.style.getPropertyValue('--drag-offset-y') || '0') : 0;
const tOffsetX = tElement ? parseFloat(tElement.style.getPropertyValue('--drag-offset-x') || '0') : 0;
const tOffsetY = tElement ? parseFloat(tElement.style.getPropertyValue('--drag-offset-y') || '0') : 0;
```

**问题分析：**
- 如果有 10 条连接线，每次渲染就要执行 20 次 `querySelector`
- 如果有 50 条连接线，每次渲染就要执行 100 次 `querySelector`
- DOM 查询是同步操作，会阻塞主线程
- 拖动时每帧都会触发重渲染（因为 `mousePos` 更新）

**性能影响：**
- 10 条连接线：20 次 DOM 查询 ≈ 2-5ms（轻微卡顿）
- 50 条连接线：100 次 DOM 查询 ≈ 10-20ms（明显卡顿）
- 100 条连接线：200 次 DOM 查询 ≈ 20-40ms（严重卡顿）

---

## 🎯 优化方案

### 方案 1：只更新拖动节点相关的连接线 ⭐⭐⭐⭐⭐

**原理：**
- 只对拖动节点的连接线执行 DOM 查询
- 其他连接线使用缓存的位置

**优点：**
- 性能提升最大（10 倍以上）
- 实现简单
- 不影响现有逻辑

**缺点：**
- 需要在 `useDrag` 中暴露 `draggingNodeId`

**实施步骤：**

1. **修改 `useDrag.ts`**：暴露 `draggingNodeId`
   ```typescript
   export const useDrag = ({ scale, onUpdateNode, onSaveHistory, nodes }: UseDragOptions) => {
     const dragRef = useRef<DragContext | null>(null);
     
     // 🆕 暴露当前拖动的节点 ID
     const draggingNodeId = dragRef.current?.id || null;
     
     return {
       handleMouseDown,
       cancelDrag,
       isDragging,
       helperLines,
       draggingNodeId, // 🆕 返回拖动节点 ID
     };
   };
   ```

2. **修改 `App.tsx`**：只查询拖动节点的 DOM
   ```typescript
   const { draggingNodeId } = useDrag({ ... });
   
   // 在连接线渲染中
   {connections.map((conn) => {
     const f = nodes.get(conn.from);
     const t = nodes.get(conn.to);
     if (!f || !t) return null;
     
     // 🔥 优化：只查询拖动节点的 DOM
     let fOffsetX = 0, fOffsetY = 0, tOffsetX = 0, tOffsetY = 0;
     
     if (draggingNodeId === conn.from) {
       const fElement = document.querySelector(`[data-node-id="${conn.from}"]`) as HTMLElement;
       fOffsetX = fElement ? parseFloat(fElement.style.getPropertyValue('--drag-offset-x') || '0') : 0;
       fOffsetY = fElement ? parseFloat(fElement.style.getPropertyValue('--drag-offset-y') || '0') : 0;
     }
     
     if (draggingNodeId === conn.to) {
       const tElement = document.querySelector(`[data-node-id="${conn.to}"]`) as HTMLElement;
       tOffsetX = tElement ? parseFloat(tElement.style.getPropertyValue('--drag-offset-x') || '0') : 0;
       tOffsetY = tElement ? parseFloat(tElement.style.getPropertyValue('--drag-offset-y') || '0') : 0;
     }
     
     // ... 其余逻辑不变
   })}
   ```

**性能提升：**
- 10 条连接线，拖动节点有 3 条连接：20 次 → 3 次（提升 6.7 倍）
- 50 条连接线，拖动节点有 5 条连接：100 次 → 5 次（提升 20 倍）
- 100 条连接线，拖动节点有 8 条连接：200 次 → 8 次（提升 25 倍）

---

### 方案 2：降低辅助线检测频率 ⭐⭐⭐

**原理：**
- 将辅助线检测从每 3 帧改为每 5 帧
- 降低 React 重渲染频率

**优点：**
- 实现简单（只改一行代码）
- 立即生效

**缺点：**
- 辅助线响应会稍微延迟
- 性能提升有限（约 20%）

**实施步骤：**

修改 `useDrag.ts` 第 189-203 行：
```typescript
// 🔥 方案 2：降低辅助线检测频率（从每 3 帧改为每 5 帧）
helperLinesRafRef.current = requestAnimationFrame(() => {
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          // 🔥 修复：只在拖动状态下检测辅助线
          if (!dragRef.current) return;
          
          // 检测辅助线（降低频率，避免每帧都触发 React 重渲染）
          const currentNode = nodes.get(id);
          if (currentNode) {
            const lines = detectHelperLines(
              { ...currentNode, x: newX, y: newY },
              nodes,
              5
            );
            setHelperLines(lines);
          }
        });
      });
    });
  });
});
```

---

### 方案 3：添加性能模式开关 ⭐⭐

**原理：**
- 用户可以选择关闭辅助线
- 用户可以选择降低连接线更新频率

**优点：**
- 用户可控
- 适合不同性能需求

**缺点：**
- 需要 UI 改动
- 增加复杂度

**实施步骤：**

1. 在 `uiStore.ts` 添加性能模式状态
2. 在设置面板添加开关
3. 根据开关决定是否启用辅助线和连接线实时更新

---

## 📊 推荐方案

**推荐：方案 1（只更新拖动节点相关的连接线）**

**理由：**
1. 性能提升最大（10-25 倍）
2. 实现简单（只需修改 2 处代码）
3. 不影响用户体验（连接线仍然实时跟随）
4. 不需要 UI 改动

**实施优先级：**
1. 🔥 **立即实施**：方案 1（连接线优化）
2. 🔥 **可选实施**：方案 2（辅助线频率降低）
3. 💡 **未来考虑**：方案 3（性能模式开关）

---

## 🧪 测试方法

**测试步骤：**
1. 创建 20 个节点，建立 30 条连接线
2. 拖动一个有 5 条连接线的节点
3. 使用 Chrome DevTools Performance 分析
4. 对比优化前后的帧率和 DOM 查询次数

**预期结果：**
- 优化前：帧率 30-40 FPS，DOM 查询 60 次/帧
- 优化后：帧率 55-60 FPS，DOM 查询 5 次/帧

---

## 📝 总结

**根本原因：**
- 连接线渲染时对所有节点执行 DOM 查询
- 连接线数量越多，性能越差

**解决方案：**
- 只查询拖动节点的 DOM
- 其他节点使用缓存位置

**预期效果：**
- 拖动流畅度提升 10-25 倍
- 完全消除滞涩感
