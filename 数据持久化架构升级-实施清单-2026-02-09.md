# 数据持久化架构升级 - 实施清单

**创建日期**：2026-02-09  
**负责人**：AI Assistant  
**预计工时**：2-3 小时  
**状态**：🟡 待开始

---

## 📋 总体进度

- [ ] **阶段 1**：画布节点持久化（1 小时）
- [ ] **阶段 2**：资产库独立存储（1 小时）
- [ ] **阶段 3**：引用计数保护（30 分钟）
- [ ] **阶段 4**：测试和优化（30 分钟）

**总进度**：0 / 4 阶段完成（0%）

---

## 🎯 阶段 1：画布节点持久化（1 小时）

### 目标
画布图片不随页面刷新丢失

### 任务清单

#### 1.1 添加 persist 中间件到 nodeStore
- [ ] 修改 `core/stores/nodeStore.ts`
- [ ] 导入 `persist` 和 `createJSONStorage`
- [ ] 添加自定义序列化函数（Map → Array）
- [ ] 添加自定义反序列化函数（Array → Map）
- [ ] 测试 localStorage 存储

**修改位置**：
```typescript
// core/stores/nodeStore.ts
export const useNodeStore = create<NodeStore>()(
  persist(
    immer((set, get) => ({
      // ... 现有代码
    })),
    {
      name: 'canvas-nodes-storage',
      storage: createJSONStorage(() => localStorage),
      serialize: (state) => {
        return JSON.stringify({
          nodes: Array.from(state.nodes.entries()),
        });
      },
      deserialize: (str) => {
        const data = JSON.parse(str);
        return {
          nodes: new Map(data.nodes),
        };
      },
    }
  )
);
```

**风险**：persist 中间件可能与 Map 不兼容  
**缓解**：使用自定义序列化函数

---

#### 1.2 实现图片 Blob 保存到 IndexedDB
- [ ] 修改 `services/blobStorage.ts`
- [ ] 新增 `saveNodeImageBlob` 函数
- [ ] 新增 `loadNodeImageBlob` 函数
- [ ] 新增 `deleteNodeImageBlob` 函数

**新增代码**：
```typescript
// services/blobStorage.ts

/**
 * 保存节点图片到 IndexedDB
 */
export const saveNodeImageBlob = async (
  nodeId: string,
  imageUrl: string
): Promise<void> => {
  if (!imageUrl.startsWith('blob:')) return;
  
  try {
    const blob = await fetch(imageUrl).then(r => r.blob());
    await saveToStorage(`blob-node-${nodeId}-image`, blob);
    console.log(`[BlobStorage] 节点图片已保存: ${nodeId}`);
  } catch (error) {
    console.error('[BlobStorage] 保存节点图片失败:', error);
  }
};

/**
 * 从 IndexedDB 加载节点图片
 */
export const loadNodeImageBlob = async (
  nodeId: string
): Promise<string | undefined> => {
  try {
    const blob = await loadFromStorage<Blob>(`blob-node-${nodeId}-image`);
    if (!blob) return undefined;
    
    const blobUrl = URL.createObjectURL(blob);
    console.log(`[BlobStorage] 节点图片已恢复: ${nodeId}`);
    return blobUrl;
  } catch (error) {
    console.error('[BlobStorage] 加载节点图片失败:', error);
    return undefined;
  }
};

/**
 * 删除节点图片
 */
export const deleteNodeImageBlob = async (
  nodeId: string
): Promise<void> => {
  try {
    await deleteFromStorage(`blob-node-${nodeId}-image`);
    console.log(`[BlobStorage] 节点图片已删除: ${nodeId}`);
  } catch (error) {
    console.error('[BlobStorage] 删除节点图片失败:', error);
  }
};
```

---

#### 1.3 实现页面加载时恢复 Blob URL
- [ ] 修改 `App.tsx`
- [ ] 添加 `useEffect` 恢复逻辑
- [ ] 遍历所有节点
- [ ] 恢复图片 Blob URL
- [ ] 恢复图片数组 Blob URL

**新增代码**：
```typescript
// App.tsx

useEffect(() => {
  const restoreNodeImages = async () => {
    const nodes = getAllNodes();
    console.log(`[App] 开始恢复 ${nodes.length} 个节点的图片`);
    
    for (const node of nodes) {
      // 恢复单张图片
      if (node.data.image && node.data.image.startsWith('blob:')) {
        const newBlobUrl = await loadNodeImageBlob(node.id);
        if (newBlobUrl) {
          updateNodeData(node.id, { image: newBlobUrl });
        }
      }
      
      // 恢复图片数组
      if (node.data.images && node.data.images.length > 0) {
        const newImages = await Promise.all(
          node.data.images.map(async (img, idx) => {
            if (img.startsWith('blob:')) {
              const blob = await loadFromStorage(`blob-node-${node.id}-image-${idx}`);
              return blob ? URL.createObjectURL(blob) : img;
            }
            return img;
          })
        );
        updateNodeData(node.id, { images: newImages });
      }
    }
    
    console.log('[App] 节点图片恢复完成');
  };
  
  restoreNodeImages();
}, []);
```

**风险**：可能阻塞 UI  
**缓解**：使用异步操作，不阻塞渲染

---

#### 1.4 修改节点创建逻辑（保存 Blob）
- [ ] 修改 `hooks/useNodeActions.ts`
- [ ] 在图片生成后保存到 IndexedDB
- [ ] 不影响现有功能

**修改位置**：
```typescript
// hooks/useNodeActions.ts

const handleImageGenerated = async (nodeId: string, imageUrl: string) => {
  // 1. 更新节点（现有逻辑）
  updateNodeData(nodeId, { image: imageUrl });
  
  // 2. 保存到 IndexedDB（新增）
  await saveNodeImageBlob(nodeId, imageUrl);
};
```

---

#### 1.5 修改节点删除逻辑（清理 Blob）
- [ ] 修改 `hooks/useNodeActions.ts`
- [ ] 在删除节点时清理 IndexedDB
- [ ] 清理 Blob URL

**修改位置**：
```typescript
// hooks/useNodeActions.ts

const handleDeleteNode = async (nodeId: string) => {
  const node = getNode(nodeId);
  
  // 1. 清理 Blob URL（现有逻辑）
  if (node.data.image && node.data.image.startsWith('blob:')) {
    URL.revokeObjectURL(node.data.image);
  }
  
  // 2. 清理 IndexedDB（新增）
  await deleteNodeImageBlob(nodeId);
  
  // 3. 删除节点（现有逻辑）
  deleteNode(nodeId);
};
```

---

### 测试清单
- [ ] 创建 5 个图片节点
- [ ] 刷新页面
- [ ] 验证图片完整显示
- [ ] 验证节点位置正确
- [ ] 验证连接关系正确
- [ ] 删除节点，验证 IndexedDB 清理

---

## 🎯 阶段 2：资产库独立存储（1 小时）

### 目标
资产库永久保存，不受其他操作影响

### 任务清单

#### 2.1 创建资产时保存缩略图到 IndexedDB
- [ ] 修改 `hooks/useAssetLibrary.ts`
- [ ] 在 `createAsset` 中保存缩略图
- [ ] 使用独立的 IndexedDB 键名

**修改位置**：
```typescript
// hooks/useAssetLibrary.ts

const createAsset = async (
  name: string,
  category: AssetCategory,
  nodes: AppNode[],
  connections: Connection[],
  customThumbnail?: string
) => {
  // ... 现有逻辑
  
  // 🔥 新增：保存缩略图到 IndexedDB
  if (thumbnail && thumbnail.startsWith('blob:')) {
    const blob = await fetch(thumbnail).then(r => r.blob());
    await saveToStorage(`asset-thumbnail-${asset.id}`, blob);
    console.log(`[AssetLibrary] 缩略图已保存: ${asset.id}`);
  }
  
  addAsset(asset);
  return asset;
};
```

---

#### 2.2 页面加载时恢复资产库缩略图
- [ ] 修改 `core/stores/assetLibraryStore.ts`
- [ ] 添加 `onRehydrateStorage` 回调
- [ ] 恢复所有资产的缩略图

**修改位置**：
```typescript
// core/stores/assetLibraryStore.ts

export const useAssetLibraryStore = create<AssetLibraryState>()(
  persist(
    immer((set, get) => ({
      // ... 现有代码
    })),
    {
      name: 'asset-library-storage',
      storage: createJSONStorage(() => localStorage),
      onRehydrateStorage: () => {
        return async (state) => {
          if (!state) return;
          
          console.log(`[AssetLibrary] 开始恢复 ${state.assets.length} 个资产的缩略图`);
          
          for (const asset of state.assets) {
            if (asset.thumbnail && asset.thumbnail.startsWith('blob:')) {
              const blob = await loadFromStorage(`asset-thumbnail-${asset.id}`);
              if (blob) {
                const newBlobUrl = URL.createObjectURL(blob);
                asset.thumbnail = newBlobUrl;
              }
            }
          }
          
          console.log('[AssetLibrary] 缩略图恢复完成');
        };
      },
    }
  )
);
```

---

#### 2.3 删除资产时清理 IndexedDB
- [ ] 修改 `core/stores/assetLibraryStore.ts`
- [ ] 在 `deleteAsset` 中清理 IndexedDB

**修改位置**：
```typescript
// core/stores/assetLibraryStore.ts

deleteAsset: async (id: string) => {
  set((state) => {
    const index = state.assets.findIndex(a => a.id === id);
    if (index !== -1) {
      const asset = state.assets[index];
      
      // 清理 Blob URL
      if (asset.thumbnail && asset.thumbnail.startsWith('blob:')) {
        URL.revokeObjectURL(asset.thumbnail);
      }
      
      // 🔥 新增：清理 IndexedDB
      deleteFromStorage(`asset-thumbnail-${id}`).catch(err => {
        console.error('[AssetLibrary] 清理 IndexedDB 失败:', err);
      });
      
      state.assets.splice(index, 1);
    }
  });
},
```

---

### 测试清单
- [ ] 创建 3 个资产
- [ ] 刷新页面
- [ ] 验证资产库完整显示
- [ ] 验证缩略图正确
- [ ] 删除资产，验证 IndexedDB 清理

---

## 🎯 阶段 3：引用计数保护（30 分钟）

### 目标
防止误删被资产库引用的图片

### 任务清单

#### 3.1 历史记录添加 `usedByAssetLibrary` 字段
- [ ] 修改 `core/stores/assetHistoryStore.ts`
- [ ] 添加字段到数据结构

**修改位置**：
```typescript
// core/stores/assetHistoryStore.ts

export interface AssetHistoryItem {
  id: string;
  type: 'image' | 'video' | 'audio';
  src: string;
  title: string;
  timestamp: number;
  usedByAssetLibrary?: boolean; // 🔥 新增字段
}
```

---

#### 3.2 创建资产时标记历史记录
- [ ] 修改 `hooks/useAssetLibrary.ts`
- [ ] 在 `createAsset` 中标记历史记录

**修改位置**：
```typescript
// hooks/useAssetLibrary.ts

const createAsset = async (...) => {
  // ... 现有逻辑
  
  // 🔥 新增：标记历史记录
  const history = useAssetHistoryStore.getState().assetHistory;
  const item = history.find(h => h.src === thumbnail);
  if (item) {
    useAssetHistoryStore.getState().updateHistory(item.id, {
      usedByAssetLibrary: true,
    });
  }
  
  return asset;
};
```

---

#### 3.3 清除历史记录时检查引用
- [ ] 修改 `hooks/useAssetHistory.ts`
- [ ] 在 `handleDeleteAsset` 中检查引用
- [ ] 添加提示弹窗

**修改位置**：
```typescript
// hooks/useAssetHistory.ts

const handleDeleteAsset = async (id: string) => {
  const asset = assetHistory.find(a => a.id === id);
  
  // 🔥 新增：检查引用
  if (asset.usedByAssetLibrary) {
    const confirmed = window.confirm(
      `⚠️ 此资源已被资产库使用\n\n` +
      `删除后，资产库中的相关资产将无法显示缩略图。\n\n` +
      `确认删除？`
    );
    if (!confirmed) return;
  }
  
  // ... 继续删除
};
```

---

### 测试清单
- [ ] 创建资产（引用历史记录）
- [ ] 尝试删除历史记录
- [ ] 验证提示弹窗
- [ ] 取消删除，验证数据完整
- [ ] 确认删除，验证数据清理

---

## 🎯 阶段 4：测试和优化（30 分钟）

### 功能测试

- [ ] **画布节点刷新恢复**
  - 创建 10 个图片节点
  - 刷新页面
  - 验证图片完整显示
  - 验证节点位置正确
  - 验证连接关系正确

- [ ] **资产库刷新恢复**
  - 创建 5 个资产
  - 刷新页面
  - 验证资产库完整显示
  - 验证缩略图正确

- [ ] **清除逻辑测试**
  - 清除历史记录 → 资产库不受影响
  - 删除资产 → 历史记录不受影响
  - 引用保护 → 提示用户

### 性能测试

- [ ] **内存占用测试**
  - 创建 10 个节点
  - 测量内存占用
  - 目标：< 50MB

- [ ] **加载速度测试**
  - 刷新页面
  - 测量加载时间
  - 目标：< 2 秒

### 边界测试

- [ ] **空数据测试**
  - 无节点时刷新
  - 无资产时刷新
  - 验证不报错

- [ ] **大量数据测试**
  - 50 个节点
  - 20 个资产
  - 验证性能

---

## ✅ 验收标准

### 功能验收
- [ ] 画布节点刷新后完整恢复
- [ ] 资产库刷新后完整恢复
- [ ] 历史记录清除时检查引用
- [ ] 资产库删除时不影响历史记录
- [ ] 内存占用可控（< 50MB）

### 性能验收
- [ ] 页面加载时间 < 2 秒
- [ ] 图片恢复时间 < 100ms
- [ ] 内存占用 < 50MB
- [ ] 磁盘占用合理（< 100MB）

### 兼容性验收
- [ ] 旧数据能正常加载
- [ ] 现有功能不受影响
- [ ] 错误处理完整
- [ ] 日志清晰可追踪

---

## 📝 实施记录

### 阶段 1 记录
- **开始时间**：
- **完成时间**：
- **遇到的问题**：
- **解决方案**：
- **测试结果**：

### 阶段 2 记录
- **开始时间**：
- **完成时间**：
- **遇到的问题**：
- **解决方案**：
- **测试结果**：

### 阶段 3 记录
- **开始时间**：
- **完成时间**：
- **遇到的问题**：
- **解决方案**：
- **测试结果**：

### 阶段 4 记录
- **开始时间**：
- **完成时间**：
- **遇到的问题**：
- **解决方案**：
- **测试结果**：

---

## 🔄 回滚方案

如果出现问题，立即执行：

```bash
# 1. 查看 Git 状态
git status

# 2. 回滚所有修改
git checkout .

# 3. 清理未跟踪文件
git clean -fd

# 4. 验证功能正常
```

---

**文档版本**：v1.0  
**创建日期**：2026-02-09  
**状态**：🟡 待开始
