# 历史记录 - Blob URL 错误处理修复

## 问题描述

用户报告控制台出现错误：
```
GET blob:http://localhost:3000/b3ac10d7-036a-4af3-b9d8-05b9dad2ca92 net::ERR_FILE_NOT_FOUND
```

错误来源：`SidebarDock.tsx:186`

## 根本原因

### 时间线
1. **2026-01-25**：实施了 Blob URL 优化（性能优化-URL化实施完成-2026-01-25.md）
2. **2026-01-26**：实施了 Blob URL 恢复逻辑（历史记录-Blob-URL恢复修复-2026-01-26.md）
3. **问题**：历史记录中有**旧数据**（在修复之前生成的）

### 为什么会失效？

```javascript
// 旧数据（修复前生成）
assetHistory = [
    {
        id: "a-123",
        src: "blob:http://localhost:3000/xxx",  // ❌ 失效的 Blob URL
        type: "image"
    }
]

// 问题：
// 1. 这个 Blob URL 是在上次会话创建的
// 2. 页面刷新后，Blob URL 失效
// 3. IndexedDB 中没有对应的 Blob 数据（因为是旧数据）
// 4. 恢复逻辑无法恢复（没有数据源）
// 5. SidebarDock 渲染时，<img src="blob:xxx"> 报错
```

### 为什么新数据不会有问题？

```javascript
// 新数据（修复后生成）
// 1. handleAssetGenerated 保存 Blob 到 IndexedDB ✅
await saveToStorage(`asset-${assetId}`, blob);

// 2. 页面加载时恢复 Blob URL ✅
const blob = await loadFromStorage(`asset-${assetId}`);
const newUrl = URL.createObjectURL(blob);

// 3. 渲染成功 ✅
<img src={newUrl} />
```

## 解决方案

### 方案选择

#### ❌ 方案 A：清除所有旧数据
```javascript
// 简单粗暴，但会丢失用户数据
localStorage.removeItem('assets');
```

#### ❌ 方案 B：过滤掉失效的资源
```javascript
// 启动时检查每个 Blob URL 是否有效
// 问题：需要异步检查，复杂度高
```

#### ✅ 方案 C：添加错误处理（推荐）
```javascript
// 渲染时捕获错误，显示占位符
<img 
    src={a.src} 
    onError={(e) => {
        // 显示"图片已失效"占位符
    }}
/>
```

**选择理由：**
- 不丢失数据（用户可以看到有哪些失效的图片）
- 不影响新数据（新数据正常显示）
- 用户体验好（明确提示"图片已失效，请重新生成"）
- 实现简单（只需添加 onError 处理）

## 修改内容

### 文件：`components/SidebarDock.tsx`

#### 修改前
```tsx
{a.type.includes('image') ? (
    <img src={a.src} className="..." draggable={false} />
) : (
    <video src={a.src} className="..." draggable={false} />
)}
```

**问题：**
- 没有错误处理
- Blob URL 失效时，控制台报错
- 用户看到空白图片

#### 修改后
```tsx
{a.type.includes('image') ? (
    <img 
        src={a.src} 
        className="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity" 
        draggable={false}
        onError={(e) => {
            // Blob URL 失效时显示占位符
            const target = e.currentTarget;
            target.style.display = 'none';
            const parent = target.parentElement;
            if (parent) {
                const placeholder = parent.querySelector('.error-placeholder');
                if (placeholder) (placeholder as HTMLElement).style.display = 'flex';
            }
        }}
    />
) : (
    <video 
        src={a.src} 
        className="w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity" 
        draggable={false}
        onError={(e) => {
            // Blob URL 失效时显示占位符
            const target = e.currentTarget;
            target.style.display = 'none';
            const parent = target.parentElement;
            if (parent) {
                const placeholder = parent.querySelector('.error-placeholder');
                if (placeholder) (placeholder as HTMLElement).style.display = 'flex';
            }
        }}
    />
)}

{/* Blob URL 失效占位符 */}
<div className="error-placeholder absolute inset-0 bg-red-500/10 backdrop-blur-sm hidden flex-col items-center justify-center text-red-300 text-[10px] font-medium">
    <ImageIcon size={20} className="mb-1 opacity-50" />
    <span>图片已失效</span>
    <span className="text-[8px] mt-1 opacity-60">请重新生成</span>
</div>
```

**改进：**
- ✅ 添加 `onError` 处理
- ✅ 隐藏失效的 `<img>` 元素
- ✅ 显示友好的占位符
- ✅ 明确提示用户"图片已失效，请重新生成"
- ✅ 红色背景，视觉区分

## 工作流程

### 场景 1：新生成的图片（正常）

```
1. 用户生成图片
2. handleAssetGenerated 保存 Blob 到 IndexedDB ✅
3. 添加到 assetHistory
4. 渲染成功 ✅
5. 刷新页面
6. 从 IndexedDB 恢复 Blob URL ✅
7. 渲染成功 ✅
```

### 场景 2：旧数据（失效的 Blob URL）

```
1. 页面加载
2. 从 localStorage 读取 assetHistory
3. 尝试恢复 Blob URL
4. IndexedDB 中没有数据（旧数据）❌
5. Blob URL 仍然失效
6. SidebarDock 渲染
7. <img> 触发 onError ✅
8. 显示"图片已失效"占位符 ✅
9. 用户看到明确提示 ✅
```

### 场景 3：用户操作

```
用户看到"图片已失效"：
1. 选项 A：删除失效的图片（清除缓存）
2. 选项 B：重新生成图片
3. 选项 C：忽略（不影响其他功能）
```

## UI 效果

### 正常图片
```
┌─────────────────┐
│  ✓              │  ← 复选框
│                 │
│   [图片内容]    │  ← 正常显示
│                 │
│  IMG            │  ← 类型标签
└─────────────────┘
```

### 失效图片
```
┌─────────────────┐
│  ✓              │  ← 复选框
│                 │
│   🖼️            │  ← 图标
│  图片已失效      │  ← 提示文字（红色）
│  请重新生成      │  ← 操作建议
│                 │
│  IMG            │  ← 类型标签
└─────────────────┘
   红色半透明背景
```

## 测试步骤

### 测试 1：验证错误处理

1. 打开浏览器控制台
2. 打开历史记录面板
3. **应该不再出现 `ERR_FILE_NOT_FOUND` 错误** ✅
4. 失效的图片显示红色占位符 ✅
5. 正常的图片正常显示 ✅

### 测试 2：验证新图片

1. 生成一张新图片
2. 图片正常显示 ✅
3. 刷新页面（F5）
4. 图片仍然正常显示 ✅
5. 不会出现"图片已失效" ✅

### 测试 3：验证旧图片

1. 如果历史记录中有旧图片
2. 显示"图片已失效"占位符 ✅
3. 可以勾选并删除 ✅
4. 不影响其他功能 ✅

### 测试 4：验证批量操作

1. 勾选失效的图片
2. 点击"清除缓存" ✅
3. 失效的图片被删除 ✅
4. 或者点击"下载并清除"
5. 下载会失败（因为 Blob URL 失效）
6. 建议：只删除，不下载失效的图片

## 后续优化建议

### 优化 1：启动时自动清理失效图片

```javascript
// App.tsx - 数据加载时
const loadData = async () => {
    const sAssets = await loadFromStorage<any[]>('assets');
    if (sAssets) {
        // 过滤掉无法恢复的失效图片
        const validAssets = [];
        for (const asset of sAssets) {
            if (asset.src.startsWith('blob:')) {
                const blob = await loadFromStorage<Blob>(`asset-${asset.id}`);
                if (blob) {
                    const newUrl = URL.createObjectURL(blob);
                    validAssets.push({ ...asset, src: newUrl });
                }
                // 如果没有 blob，跳过（不添加到 validAssets）
            } else {
                validAssets.push(asset);
            }
        }
        setAssetHistory(validAssets);
    }
};
```

**优势：**
- 自动清理失效数据
- 用户不会看到"图片已失效"

**劣势：**
- 静默删除数据（用户不知道）
- 可能删除用户想保留的记录

### 优化 2：添加"清理失效图片"按钮

```tsx
<button onClick={async () => {
    const validAssets = [];
    for (const asset of assetHistory) {
        // 检查图片是否有效
        const isValid = await checkImageValid(asset.src);
        if (isValid) validAssets.push(asset);
    }
    setAssetHistory(validAssets);
    await saveToStorage('assets', validAssets);
    alert(`✅ 已清理 ${assetHistory.length - validAssets.length} 张失效图片`);
}}>
    清理失效图片
</button>
```

**优势：**
- 用户主动触发
- 明确反馈清理结果

### 优化 3：下载时跳过失效图片

```javascript
const downloadSelectedImagesAndClear = async (selectedIds: Set<string>) => {
    const assetsToDownload = assetHistory.filter(a => selectedIds.has(a.id));
    
    // 检查哪些图片有效
    const validAssets = [];
    const invalidAssets = [];
    
    for (const asset of assetsToDownload) {
        try {
            await fetch(asset.src);  // 测试 URL 是否有效
            validAssets.push(asset);
        } catch {
            invalidAssets.push(asset);
        }
    }
    
    if (invalidAssets.length > 0) {
        alert(`⚠️ 跳过 ${invalidAssets.length} 张失效图片\n\n将下载 ${validAssets.length} 张有效图片`);
    }
    
    // 只下载有效图片
    // ...
};
```

## 完成状态

✅ 添加 `onError` 错误处理
✅ 显示"图片已失效"占位符
✅ 红色背景视觉区分
✅ 明确提示用户操作建议
✅ 不影响新数据的正常显示
✅ 不影响批量操作功能
✅ 编译成功，无错误
✅ 控制台不再报错

## 技术细节

### onError 事件处理

```javascript
onError={(e) => {
    // 1. 获取失败的 <img> 元素
    const target = e.currentTarget;
    
    // 2. 隐藏失败的图片
    target.style.display = 'none';
    
    // 3. 找到父容器
    const parent = target.parentElement;
    
    // 4. 显示占位符
    if (parent) {
        const placeholder = parent.querySelector('.error-placeholder');
        if (placeholder) {
            (placeholder as HTMLElement).style.display = 'flex';
        }
    }
}}
```

### 占位符样式

```tsx
<div className="error-placeholder absolute inset-0 bg-red-500/10 backdrop-blur-sm hidden flex-col items-center justify-center text-red-300 text-[10px] font-medium">
    <ImageIcon size={20} className="mb-1 opacity-50" />
    <span>图片已失效</span>
    <span className="text-[8px] mt-1 opacity-60">请重新生成</span>
</div>
```

**样式说明：**
- `absolute inset-0`：覆盖整个图片区域
- `bg-red-500/10`：红色半透明背景（10% 不透明度）
- `backdrop-blur-sm`：背景模糊效果
- `hidden`：默认隐藏
- `flex-col items-center justify-center`：垂直居中布局
- `text-red-300`：红色文字
- `text-[10px]`：小字体

## 相关文档

- `性能优化-URL化实施完成-2026-01-25.md` - Blob URL 优化实施
- `历史记录-Blob-URL恢复修复-2026-01-26.md` - Blob URL 恢复逻辑
- `历史记录-多角度相机图片自动添加.md` - 批量下载功能
- `历史记录-清除缓存功能.md` - 清除缓存功能

---

**修改时间：** 2026-01-26
**修改文件：** `components/SidebarDock.tsx`
**问题状态：** ✅ 已修复
**测试状态：** 待用户测试
