# 拖动调试指南

## 问题现象
- 节点不能跟着鼠标走
- 非常不丝滑

## 调试步骤

### 步骤 1：检查事件监听器

打开浏览器开发者工具（F12），在 Console 中粘贴：

```javascript
// 1. 检查 mousemove 事件是否被触发
let moveCount = 0;
const originalAddEventListener = window.addEventListener;
window.addEventListener = function(type, listener, options) {
    if (type === 'mousemove') {
        console.log('[事件监听] mousemove 监听器已添加');
    }
    return originalAddEventListener.call(this, type, listener, options);
};

// 2. 监听所有 mousemove
window.addEventListener('mousemove', (e) => {
    moveCount++;
    if (moveCount % 30 === 0) {
        console.log(`[mousemove] 第 ${moveCount} 次，位置: (${e.clientX}, ${e.clientY})`);
    }
}, true);

// 3. 监听所有 mousedown
window.addEventListener('mousedown', (e) => {
    console.log('[mousedown] 目标:', e.target.tagName, e.target.className);
    console.log('[mousedown] data-node-id:', e.target.getAttribute('data-node-id'));
}, true);

console.log('✅ 调试监听器已安装');
```

### 步骤 2：检查 dragNodeRef

在 Console 中粘贴：

```javascript
// 每秒检查一次 dragNodeRef 的状态
setInterval(() => {
    // 尝试从 React DevTools 获取
    const nodes = document.querySelectorAll('[data-node-id]');
    console.log(`[检查] 当前有 ${nodes.length} 个节点`);
    
    // 检查是否有节点正在被拖动
    nodes.forEach(node => {
        const transform = node.style.transform;
        if (transform && transform !== 'none' && transform !== '') {
            console.log(`[检查] 节点 ${node.getAttribute('data-node-id')} 的 transform:`, transform);
        }
    });
}, 1000);
```

### 步骤 3：手动测试拖动

1. 点击一个节点（按住鼠标左键）
2. 移动鼠标
3. 观察 Console 输出

**预期输出：**
```
[mousedown] 目标: DIV ...
[mousedown] data-node-id: node-xxx
[mousemove] 第 30 次，位置: (500, 300)
[mousemove] 第 60 次，位置: (520, 310)
[检查] 节点 node-xxx 的 transform: translate(20px, 10px)
```

**如果没有 transform 输出，说明：**
- `handleGlobalMouseMove` 没有被调用
- 或者 `dragNodeRef.current` 是 null
- 或者 DOM 元素没有被正确缓存

### 步骤 4：检查 React 状态

在 Console 中粘贴：

```javascript
// 检查 React Fiber 树（需要 React DevTools）
const findReactFiber = (dom) => {
    const key = Object.keys(dom).find(key => key.startsWith('__reactFiber$'));
    return dom[key];
};

const checkNode = () => {
    const node = document.querySelector('[data-node-id]');
    if (node) {
        const fiber = findReactFiber(node);
        if (fiber) {
            console.log('[React] 找到 Fiber 节点');
            // 尝试找到 props
            let current = fiber;
            while (current) {
                if (current.memoizedProps && current.memoizedProps.onNodeMouseDown) {
                    console.log('[React] 找到 onNodeMouseDown:', typeof current.memoizedProps.onNodeMouseDown);
                    break;
                }
                current = current.return;
            }
        }
    }
};

checkNode();
```

## 可能的问题

### 问题 1：事件监听器没有绑定
**症状：** Console 中没有 "mousemove 监听器已添加" 的输出
**原因：** `useEffect` 没有执行
**解决：** 检查 `handleGlobalMouseMove` 的依赖项

### 问题 2：dragNodeRef 没有被设置
**症状：** 点击节点后，`dragNodeRef.current` 仍然是 null
**原因：** `onNodeMouseDown` 没有被调用，或者 DOM 查询失败
**解决：** 检查 `onNodeMouseDown` 的绑定和 DOM 查询

### 问题 3：transform 没有被应用
**症状：** `handleGlobalMouseMove` 被调用，但节点没有移动
**原因：** DOM 元素没有被正确缓存，或者 `element.style.transform` 失败
**解决：** 检查 `dragNodeRef.current.element` 是否存在

### 问题 4：transition 干扰
**症状：** 节点移动很慢，有延迟
**原因：** `transition` 没有被禁用
**解决：** 确保拖动时 `isInteracting` 为 true，`transition` 为 'none'

## 快速修复

如果以上都正常，但还是不丝滑，尝试这个临时修复：

```javascript
// 在 Console 中执行，强制禁用所有 transition
document.querySelectorAll('[data-node-id]').forEach(node => {
    node.style.transition = 'none !important';
});
```

然后再次尝试拖动。如果这样就丝滑了，说明问题在于 `isInteracting` 状态没有正确传递。
