# 3D 相机提示词 - 角度发散方案实施

## 实施时间
2026-01-24

## 问题回顾

### 用户反馈
> "我在 3D 里选好的，跟提示词可能都对不上，根本没有我要的。纯靠发散啊，起码从最基本的角度上得给我一张我要的啊。"

### 核心问题
- Panel 5（九宫格中心）应该是用户在 3D UI 中设置的角度
- 当前实现：Panel 5 永远是 `camera centered - direct front view`
- 不管用户设置 `horizontalAngle` 是 0°, 45°, 90° 还是任意角度，提示词都一样
- **用户设置的角度完全被忽略了**

## 解决方案：角度发散方案

### 核心思路

**固定 2 个维度，让角度维度发散**

- **固定维度 1：距离**（严格按照 UI 选择的 `cameraZoom`）
- **固定维度 2：高度**（严格按照 UI 选择的 `verticalAngle`）
- **发散维度：角度**（围绕 UI 选择的 `horizontalAngle` 展开 9 个细分）

### 示例

**用户选择：**
- horizontalAngle = 45°（右前方）
- verticalAngle = 0°（平视）
- cameraZoom = 5（中景）

**生成的九宫格：**
```
所有 9 张图片：
- 距离：固定为 Medium Full Shot (MFS)
- 高度：固定为 Eye Level
- 角度：围绕 45° 在 30-60° 范围内发散

Panel 1: MFS, Eye Level, three-quarter view from front-right, closer to front
Panel 2: MFS, Eye Level, three-quarter view from front-right, slightly towards front
Panel 3: MFS, Eye Level, three-quarter view from front-right, slightly towards side
Panel 4: MFS, Eye Level, three-quarter view from front-right, leaning towards front
Panel 5: MFS, Eye Level, three-quarter view from front-right ★ 基准
Panel 6: MFS, Eye Level, three-quarter view from front-right, leaning towards side
Panel 7: MFS, Eye Level, three-quarter view from front-right, mostly front
Panel 8: MFS, Eye Level, three-quarter view from front-right, balanced
Panel 9: MFS, Eye Level, three-quarter view from front-right, mostly side
```

## 代码实施

### 1. 角度分段系统（16 个主方向）

**新增代码：**
```typescript
interface AngleSegment {
    name: string;           // 中文名称
    baseDesc: string;       // 基础英文描述
    variations: string[];   // 9 个细分描述
}

const getAngleSegment = (angle: number): AngleSegment => {
    const normalized = ((angle % 360) + 360) % 360;
    
    // 16 个主方向（每个方向覆盖 22.5°）
    if (normalized < 11.25 || normalized >= 348.75) {
        return {
            name: '正面',
            baseDesc: 'front view',
            variations: [
                'front view, slightly from left',
                'front view, leaning left',
                'front view, leaning right',
                'front view, slightly left',
                'front view',  // Panel 5 基准
                'front view, slightly right',
                'front view, mostly left',
                'front view, centered',
                'front view, mostly right',
            ]
        };
    }
    
    // ... 继续其他 15 个方向
};
```

**16 个主方向：**
1. 正面（0°）
2. 右前偏前（11.25-33.75°）
3. 右前方（33.75-56.25°）
4. 右前偏右（56.25-78.75°）
5. 右侧面（78.75-101.25°）
6. 右后偏右（101.25-123.75°）
7. 右后方（123.75-146.25°）
8. 右后偏后（146.25-168.75°）
9. 背面（168.75-191.25°）
10. 左后偏后（191.25-213.75°）
11. 左后方（213.75-236.25°）
12. 左后偏左（236.25-258.75°）
13. 左侧面（258.75-281.25°）
14. 左前偏左（281.25-303.75°）
15. 左前方（303.75-326.25°）
16. 左前偏前（326.25-348.75°）

### 2. 改进高度映射

**改进前（3 个档位）：**
```typescript
const getHeightTerm = (angle: number): string => {
    if (angle >= 20) return 'High Angle';
    if (angle <= -20) return 'Low Angle';
    return 'Eye Level';
};
```

**改进后（6 个档位）：**
```typescript
const getHeightTerm = (angle: number): string => {
    if (angle >= 50) return 'Top-Down View';           // 50-60°
    if (angle >= 30) return 'High Angle';              // 30-50°
    if (angle >= 10) return 'Slightly High Angle';     // 10-30°
    if (angle >= -10) return 'Eye Level';              // -10-10°
    if (angle >= -20) return 'Slightly Low Angle';     // -20--10°
    return 'Low Angle';                                // -30--20°
};
```

### 3. 修改九宫格生成逻辑

**改进前（三维同时变化）：**
```typescript
const generateCameraMatrix = () => {
    const panels = [];
    
    // 距离偏移：上排近(-1)，中排基准(0)，下排远(+1)
    const distanceOffsets = [-1, -1, -1, 0, 0, 0, 1, 1, 1];
    
    // 高度偏移：上排高(+1)，中排基准(0)，下排低(-1)
    const heightOffsets = [1, 1, 1, 0, 0, 0, -1, -1, -1];
    
    // 相机位置偏移：左列左(-1)，中列基准(0)，右列右(+1)
    const cameraOffsets = [-1, 0, 1, -1, 0, 1, -1, 0, 1];
    
    for (let i = 0; i < 9; i++) {
        const distance = getDistanceVariation(zoom, distanceOffsets[i]);
        const height = getHeightVariation(vAngle, heightOffsets[i]);
        const cameraPos = getCameraPositionDesc(cameraOffsets[i], heightOffsets[i]);
        // ...
    }
};
```

**改进后（只有角度发散）：**
```typescript
const generateCameraMatrix = () => {
    const panels = [];
    const angleVariations = angleSegment.variations;
    
    for (let i = 0; i < 9; i++) {
        const row = Math.floor(i / 3);
        const col = i % 3;
        const panelNum = i + 1;
        const label = getPositionLabel(row, col);
        const angleDesc = angleVariations[i];
        
        if (panelNum === 5) {
            panels.push(`Panel 5: ${label} ${baseDistance}, ${baseHeight}, ${angleDesc} ★ BASE ANCHOR`);
        } else {
            panels.push(`Panel ${panelNum}: ${label} ${baseDistance}, ${baseHeight}, ${angleDesc}`);
        }
    }
    
    return panels;
};
```

### 4. 更新提示词结构

**关键改进：**

1. **强调距离和高度锁定：**
```
- Camera Distance: LOCKED at ${baseDistance}
- Camera Height: LOCKED at ${baseHeight}
- The ONLY variable: camera angle variations around ${baseAngleDesc}
```

2. **明确基准点：**
```
[BASE ANCHOR POINT - Panel 5]
Distance: ${baseDistance} - LOCKED for all panels
Camera Height: ${baseHeight} - LOCKED for all panels
Camera Angle: ${baseAngleDesc} - BASE REFERENCE
```

3. **强调空间逻辑：**
```
[SPATIAL LOGIC]
- Distance: LOCKED at ${baseDistance} for ALL panels
- Height: LOCKED at ${baseHeight} for ALL panels
- Angle: Subtle variations around ${baseAngleDesc}
- The character stays in the same pose; the camera orbits horizontally around them
```

4. **添加明确规则：**
```
[RULES]
✅ ALL panels use EXACT same distance: ${baseDistance}
✅ ALL panels use EXACT same height: ${baseHeight}
✅ ONLY camera angle varies slightly around ${baseAngleDesc}

[NEGATIVE CONSTRAINTS]
❌ Do NOT change camera distance between panels
❌ Do NOT change camera height between panels
```

## 实施效果

### 改进前

**用户设置：** horizontalAngle = 45°, verticalAngle = 0°, cameraZoom = 5

**Panel 5 提示词：**
```
Panel 5: [BASE ANCHOR] Medium Full Shot, Eye Level, camera centered - direct front view
```

**问题：**
- ❌ 完全忽略了用户设置的 45°
- ❌ 永远是 "front view"
- ❌ 用户得不到想要的角度

### 改进后

**用户设置：** horizontalAngle = 45°, verticalAngle = 0°, cameraZoom = 5

**Panel 5 提示词：**
```
Panel 5: [BASE ANCHOR] Medium Full Shot, Eye Level, three-quarter view from front-right ★ BASE ANCHOR
```

**改进：**
- ✅ 反映了用户设置的 45°（right-front 方向）
- ✅ 距离和高度严格锁定
- ✅ 周围 8 张图片围绕 45° 发散
- ✅ 用户能得到想要的角度

## 测试用例

### 测试 1：正面（0°）

**输入：**
- horizontalAngle = 0°
- verticalAngle = 0°
- cameraZoom = 5

**预期输出：**
- 所有 Panel：Medium Full Shot, Eye Level
- Panel 5：front view
- 周围 Panel：front view 的细分（slightly from left, leaning right 等）

### 测试 2：右前方（45°）

**输入：**
- horizontalAngle = 45°
- verticalAngle = 0°
- cameraZoom = 5

**预期输出：**
- 所有 Panel：Medium Full Shot, Eye Level
- Panel 5：three-quarter view from front-right
- 周围 Panel：three-quarter view from front-right 的细分

### 测试 3：右侧面（90°）

**输入：**
- horizontalAngle = 90°
- verticalAngle = 0°
- cameraZoom = 5

**预期输出：**
- 所有 Panel：Medium Full Shot, Eye Level
- Panel 5：side view from right
- 周围 Panel：side view from right 的细分

### 测试 4：俯视特写（0°, 40°, 2）

**输入：**
- horizontalAngle = 0°
- verticalAngle = 40°
- cameraZoom = 2

**预期输出：**
- 所有 Panel：Close-Up (CU), High Angle
- Panel 5：front view
- 周围 Panel：front view 的细分

## 验收标准

### 功能验收

- ✅ Panel 5 的角度描述反映用户在 UI 中选择的 `horizontalAngle`
- ✅ 所有 9 张图片的距离术语完全一致（基于 `cameraZoom`）
- ✅ 所有 9 张图片的高度术语完全一致（基于 `verticalAngle`）
- ✅ 周围 8 张图片的角度描述围绕 Panel 5 发散
- ✅ 提示词明确强调距离和高度锁定

### 质量验收

- ✅ 生成的 9 张图片距离一致（如都是中景）
- ✅ 生成的 9 张图片高度一致（如都是平视）
- ✅ 生成的 9 张图片角度有细微变化（围绕基准角度）
- ✅ Panel 5 的角度最接近用户选择的角度

### 用户体验验收

- ✅ 用户在 UI 中设置 45°，生成的 Panel 5 就是右前方视角
- ✅ 用户能从 9 张图片中找到"想要的那张"（Panel 5 或其周围）
- ✅ 不再出现"纯靠发散"的情况

## 文件变更

### 修改的文件
- `App.tsx`：提示词生成逻辑（第 1350-1500 行）

### 新增的文档
- `.kiro/specs/camera-base-angle-fix/requirements.md`：需求文档
- `.kiro/specs/camera-base-angle-fix/design.md`：设计文档
- `.kiro/specs/camera-base-angle-fix/tasks.md`：任务列表
- `3D相机提示词-角度发散方案实施.md`：本文档

### 参考的文档
- `3D相机提示词-方案1实施.md`：方案 1 实施总结
- `3D相机提示词-方案A+D实施.md`：方案 A+D 实施总结
- `3D相机提示词-最终方案.md`：原始设计方案

## 关键改进点

### 1. 角度分段系统

**16 个主方向，每个方向 9 个细分**
- 覆盖 0-360° 的所有角度
- 每个方向有独特的语义化描述
- Panel 5（index 4）是基准描述

### 2. 严格锁定距离和高度

**所有 9 张图片使用相同的距离和高度术语**
- 距离：基于 `cameraZoom`，使用 Nano Banana 标准术语
- 高度：基于 `verticalAngle`，从 3 档改进为 6 档
- 提示词中多次强调 "LOCKED"

### 3. 角度发散

**围绕用户选择的角度展开 9 个细分**
- Panel 5 是基准（用户选择的角度）
- 周围 8 张是细微变化（closer to front, leaning towards side 等）
- 从左到右渐变（左列偏前，右列偏侧）

### 4. 提示词结构优化

**参考 Gemini 建议，强调约束**
- 明确标注 "LOCKED" 维度
- 明确标注 "ONLY variable" 维度
- 添加 NEGATIVE CONSTRAINTS（不要改变距离和高度）

## 下一步

### 1. 测试验证
- 测试不同角度的生成效果
- 验证距离和高度是否真的锁定
- 验证 Panel 5 是否反映用户选择的角度

### 2. 收集反馈
- 用户对 Panel 5 准确度的反馈
- 用户对角度发散范围的反馈
- 用户对距离和高度锁定的反馈

### 3. 迭代优化
- 根据生成效果调整 variations 描述
- 优化特殊角度（正面、侧面、背面）的细分
- 确保 AI 能准确理解每个细分描述

---

**方案实施完成！** 🎉

现在的提示词：
- ✅ Panel 5 反映用户选择的角度（如 45° → "three-quarter view from front-right"）
- ✅ 距离和高度严格锁定（所有 Panel 相同）
- ✅ 角度围绕基准发散（9 个细分描述）
- ✅ 提示词明确强调约束（LOCKED, ONLY variable）

**等待测试反馈！特别关注 Panel 5 是否是用户想要的角度。**

