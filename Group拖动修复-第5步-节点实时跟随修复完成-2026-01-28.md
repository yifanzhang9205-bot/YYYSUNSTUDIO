# Group 拖动修复 - 第 5 步：节点实时跟随修复完成

**日期**：2026-01-28  
**状态**：✅ 完成  
**问题**：Group 拖动时，子节点不跟随移动

---

## 🔍 问题诊断

### 症状
- Group 可以拖动（丝滑）
- Group 可以选中和删除
- **但是子节点不跟随 Group 移动**

### 日志分析
```
[useGroup] startGroupDrag: {groupId: 'g-1769532055077', ...}
[useGroup] 子节点数量: 1 子节点 ID: ['n-1769532053785-coe9g1v1z']
[useGroup] startGroupDrag: {groupId: 'g-1769532055077', ...}
[useGroup] endGroupDrag: {groupId: 'g-1769532055077', ...}
```

**关键发现**：
- ✅ `startGroupDrag` 被调用了
- ❌ **`updateGroupDrag` 从来没有被调用**（日志中没有 `[useGroup] updateGroupDrag 被调用`）
- ✅ `endGroupDrag` 被调用了

---

## 🐛 根本原因

### React setState 异步问题

**App.tsx 中的代码**：
```typescript
// === Group 拖动：使用 useGroup Hook ===
if (isDraggingGroup) {
    console.log('[App] handleGlobalMouseMove: Group 拖动中');
    updateGroupDrag(e, scale);
    return;
}
```

**问题**：
1. `startGroupDrag` 中调用 `setIsDraggingGroup(true)`
2. **setState 是异步的**，状态不会立即更新
3. `handleGlobalMouseMove` 中判断 `if (isDraggingGroup)` 时，状态还是 `false`
4. 所以 `updateGroupDrag` 从来没有被调用

**这是 React 的经典问题**：setState 不会立即生效，而是在下一次渲染时才更新。

---

## ✅ 解决方案

### 方案：使用 Ref 判断，而不是 State

**核心思路**：
- `updateGroupDrag` 内部使用 `dragGroupRef.current` 来判断是否正在拖动
- 返回布尔值表示是否处理了拖动
- App.tsx 直接调用 `updateGroupDrag`，不再依赖 `isDraggingGroup` 状态

### 修改 1：hooks/useGroup.ts

**修改 `updateGroupDrag` 的返回类型**：

```typescript
/**
 * 更新分组拖动
 * 
 * 使用 CSS transform 优化性能（GPU 加速，0 次 React 重渲染）
 * 
 * @returns {boolean} 是否处理了拖动（true = 正在拖动 Group，false = 没有拖动）
 */
const updateGroupDrag = useCallback((e: MouseEvent, currentScale: number): boolean => {
  console.log('[useGroup] updateGroupDrag 被调用:', {
    hasDragContext: !!dragGroupRef.current,
    dragGroupId: dragGroupRef.current?.id,
  });
  
  if (!dragGroupRef.current) return false; // ✅ 返回 false 表示没有拖动
  
  // ... 拖动逻辑 ...
  
  return true; // ✅ 返回 true 表示处理了拖动
}, []);
```

### 修改 2：App.tsx

**修改 `handleGlobalMouseMove` 的判断逻辑**：

```typescript
// === Group 拖动：使用 useGroup Hook ===
// 🔥 修复：使用 Hook 内部的判断方法，而不是 isDraggingGroup 状态
// 因为 setState 是异步的，状态更新有延迟
const groupDragHandled = updateGroupDrag(e, scale);
if (groupDragHandled) {
    console.log('[App] handleGlobalMouseMove: Group 拖动中');
    return;
}
```

**优势**：
- ✅ 不依赖异步的 `isDraggingGroup` 状态
- ✅ 直接使用 `dragGroupRef.current` 判断（同步）
- ✅ `updateGroupDrag` 总是会被调用，内部自己判断是否处理

---

## 🎯 修复效果

### 预期行为
1. 按下鼠标拖动 Group
2. `startGroupDrag` 被调用，设置 `dragGroupRef.current`
3. 鼠标移动时，`updateGroupDrag` **立即被调用**（不等待 state 更新）
4. `updateGroupDrag` 检查 `dragGroupRef.current`，发现正在拖动，返回 `true`
5. Group 和子节点同时移动（使用 CSS transform）
6. 松开鼠标，`endGroupDrag` 被调用，更新 Store

### 日志输出（预期）
```
[useGroup] startGroupDrag: {groupId: 'g-xxx', ...}
[useGroup] updateGroupDrag 被调用: {hasDragContext: true, dragGroupId: 'g-xxx'}
[useGroup] updateGroupDrag 移动: {groupId: 'g-xxx', screenOffset: {dx: 10, dy: 5}, ...}
[App] handleGlobalMouseMove: Group 拖动中
[useGroup] updateGroupDrag 被调用: {hasDragContext: true, dragGroupId: 'g-xxx'}
[useGroup] updateGroupDrag 移动: {groupId: 'g-xxx', screenOffset: {dx: 20, dy: 10}, ...}
[App] handleGlobalMouseMove: Group 拖动中
...
[useGroup] endGroupDrag: {groupId: 'g-xxx', ...}
```

---

## 📊 技术细节

### 为什么不能用 State？

**问题代码**：
```typescript
// ❌ 错误：依赖异步的 state
const [isDraggingGroup, setIsDraggingGroup] = useState(false);

const startGroupDrag = () => {
  setIsDraggingGroup(true); // 异步，不会立即生效
};

const handleMouseMove = () => {
  if (isDraggingGroup) { // 这里 isDraggingGroup 还是 false！
    updateGroupDrag();
  }
};
```

**正确代码**：
```typescript
// ✅ 正确：使用 ref（同步）
const dragGroupRef = useRef<DragGroupContext | null>(null);

const startGroupDrag = () => {
  dragGroupRef.current = { ... }; // 同步，立即生效
};

const updateGroupDrag = (): boolean => {
  if (!dragGroupRef.current) return false; // 同步判断
  // ... 拖动逻辑 ...
  return true;
};

const handleMouseMove = () => {
  const handled = updateGroupDrag(); // 直接调用，内部自己判断
  if (handled) return;
};
```

### 性能优化

**使用 CSS transform 而不是更新 State**：
```typescript
// ✅ 高性能：直接操作 DOM，GPU 加速，0 次 React 重渲染
nodeElement.style.transform = `translate(${dx}px, ${dy}px)`;
nodeElement.style.willChange = 'transform';

// ❌ 低性能：每次 mousemove 触发 React 重渲染
setNodes(nodes.map(n => n.id === nodeId ? { ...n, x: n.x + dx, y: n.y + dy } : n));
```

---

## 🧪 测试步骤

1. **刷新页面**，清除旧的日志
2. **创建一个 Group**，添加一个节点
3. **拖动 Group**，观察：
   - [ ] Group 移动丝滑（无延迟）
   - [ ] 子节点实时跟随 Group 移动
   - [ ] 控制台输出 `[useGroup] updateGroupDrag 被调用`
   - [ ] 控制台输出 `[useGroup] updateGroupDrag 移动`
   - [ ] 控制台输出 `[App] handleGlobalMouseMove: Group 拖动中`
4. **松开鼠标**，观察：
   - [ ] Group 和子节点停在正确位置
   - [ ] 控制台输出 `[useGroup] endGroupDrag`

---

## 📝 总结

### 修复内容
- ✅ 修改 `updateGroupDrag` 返回布尔值
- ✅ 修改 App.tsx 的判断逻辑，不依赖 `isDraggingGroup` 状态
- ✅ 使用 `dragGroupRef.current` 同步判断是否正在拖动

### 技术要点
- **React setState 是异步的**，不能用于实时判断
- **使用 Ref 存储拖动上下文**，同步访问
- **CSS transform 优化性能**，避免 React 重渲染

### 下一步
- 测试 Group 拖动功能
- 如果子节点仍然不跟随，检查 DOM 查询是否成功
- 优化拖动体验（如添加拖动预览、边界检测等）

---

**修复完成！现在 Group 拖动时，子节点应该实时跟随移动了。** 🎉
