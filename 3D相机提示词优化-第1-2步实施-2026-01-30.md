# 3D相机提示词优化 - 第1-2步实施完成

**日期**: 2026-01-30  
**目标**: 修复九宫格逻辑Bug + 强化提示词描述，提高角度和距离的精确度

---

## 📋 实施内容

### ✅ **第1步：修复九宫格逻辑Bug**

#### **问题诊断（Gemini指出）**
```javascript
// ❌ 原代码的Bug
const h = ((centerH + hOffsets[col]) % 360 + 360) % 360;  // 水平角度用col
const z = Math.max(0, Math.min(10, centerZ + zOffsets[col]));  // 距离也用col ❌
```

**问题**：水平角度和距离都用 `col` 变化，导致：
1. 语义混乱：用户选了一个角度，结果9个面板的角度和距离都在变
2. 变化耦合：无法单独控制角度或距离
3. AI理解困难：提示词中变量太多，AI容易搞混

#### **解决方案：智能发散策略**

根据用户选择的参数，自动决定九宫格的变化方式：

**策略1：特殊角度（背面、侧面）**
- 固定距离，变化角度（±15°）和高度
- 目的：提高角度精确度
- 适用：67.5°-112.5°（右侧面）、157.5°-202.5°（背面）、247.5°-292.5°（左侧面）

**策略2：极端距离（特写/全景）**
- 固定角度，变化距离（±0.5）和高度
- 目的：提高距离精确度
- 适用：z < 2（特写）或 z > 8（全景）

**策略3：常规情况（推荐）**
- 固定距离，变化角度（±20°）和高度
- 目的：让用户看到角色在不同角度下的样子
- 适用：其他所有情况

```javascript
// ✅ 修复后的代码
if (isSpecialAngle) {
    // 特殊角度：固定距离，变化角度和高度
    const h = ((centerH + hOffsets[col]) % 360 + 360) % 360;
    const v = Math.max(-90, Math.min(90, centerV + vOffsets[row]));
    const z = centerZ;  // ✅ 距离固定
} else if (isExtremeDistance) {
    // 极端距离：固定角度，变化距离和高度
    const h = centerH;  // ✅ 角度固定
    const v = Math.max(-90, Math.min(90, centerV + vOffsets[row]));
    const z = Math.max(0, Math.min(10, centerZ + zOffsets[col]));
} else {
    // 常规情况：固定距离，变化角度和高度
    const h = ((centerH + hOffsets[col]) % 360 + 360) % 360;
    const v = Math.max(-90, Math.min(90, centerV + vOffsets[row]));
    const z = centerZ;  // ✅ 距离固定
}
```

---

### ✅ **第2步：分级强化提示词**

#### **核心思路：中心面板 vs 周围面板**

- **中心面板（Panel 5）**：用户选择的精确角度，使用**最强描述**
- **周围面板（其他8个）**：变体，使用**标准描述**

#### **实施细节**

**1. 水平角度描述强化**

```javascript
// 🎯 中心面板：使用最强描述
if (isCenter) {
    if (normalized >= 67.5 && normalized < 112.5) {
        return 'strictly side profile view, 90-degree side angle, from the side, no front or back visible, pure lateral view';
    }
    if (normalized >= 157.5 && normalized < 202.5) {
        return 'direct back view, from behind, rear view, back of head visible, no face visible, dorsal view';
    }
    // ... 其他角度
}

// 🎯 周围面板：使用标准描述
if (normalized >= 67.5 && normalized < 112.5) {
    return 'side profile view';
}
if (normalized >= 157.5 && normalized < 202.5) {
    return 'back view';
}
```

**2. 垂直角度描述强化**

```javascript
// 🎯 中心面板
if (isCenter) {
    if (angle >= -10 && angle < 10) {
        return 'eye-level view, straight-on perspective, horizontal camera';
    }
    if (angle >= 40) {
        return 'high-angle view, elevated camera position, looking down';
    }
    // ... 其他角度
}

// 🎯 周围面板
if (angle >= -10 && angle < 10) {
    return 'eye-level';
}
if (angle >= 40) {
    return 'high-angle view';
}
```

**3. 距离描述强化**

```javascript
// 🎯 中心面板
if (isCenter) {
    if (z <= 0.5) {
        return 'extreme close-up shot, face only, very tight framing, macro detail';
    }
    if (z <= 1.5) {
        return 'close-up shot, head and shoulders, portrait framing';
    }
    // ... 其他距离
}

// 🎯 周围面板
if (z <= 0.5) {
    return 'extreme close-up';
}
if (z <= 1.5) {
    return 'close-up';
}
```

---

### ✅ **第3步：优化主提示词模板**

#### **关键改进**

1. **明确中心面板优先级**
```
**PRIMARY TARGET (Panel 5 - Center) - HIGHEST PRIORITY:**
This is the EXACT angle the user requested. Pay maximum attention to this panel.
```

2. **添加正交投影要求**
```
✅ Use orthographic camera projection (no perspective distortion)
```

3. **强化特殊角度指导**
```
${baseAzimuthEN.includes('side profile') ? `
- Show the character from the SIDE (90° from front)
- Pure side view as the base angle
- Only ONE eye should be visible
` : ...}
```

4. **标记中心面板**
```
Panel 5: ${zDescEN}, ${hDescEN}, ${vDescEN} ← USER SELECTED (MOST IMPORTANT)
```

---

## 📊 预期效果

### **场景1：用户选择"侧面 + 半身"**
- **系统判断**：特殊角度（侧面）
- **策略**：固定距离（半身），变化角度（75°-105°）和高度
- **结果**：9个面板都是半身像，角度在侧面附近微调
- **用户收益**：精确的侧面角度，可以选择最满意的那个

### **场景2：用户选择"正面 + 特写"**
- **系统判断**：极端距离（特写）
- **策略**：固定角度（正面），变化距离（特写-近景）和高度
- **结果**：9个面板都是正面，距离在特写附近微调
- **用户收益**：精确的特写效果，可以选择最合适的景别

### **场景3：用户选择"45° + 中景"**
- **系统判断**：常规情况
- **策略**：固定距离（中景），变化角度（25°-65°）和高度
- **结果**：9个面板都是中景，角度在45°附近变化
- **用户收益**：看到角色在不同角度下的样子

---

## 🎯 核心改进点

| 维度 | 优化前 | 优化后 |
|------|--------|--------|
| **九宫格逻辑** | 混乱（角度+距离耦合） | 清晰（智能自适应策略） |
| **中心面板** | 无特殊处理 | 最强描述 + 最高优先级 |
| **角度精确度** | 低（±20°误差） | 高（分级描述 + 智能策略） |
| **距离精确度** | 低（同时变化） | 高（智能策略） |
| **AI理解难度** | 高（变量太多） | 中（分级描述降低难度） |
| **用户灵活性** | 低（无法控制） | 高（根据需求自适应） |

---

## 📝 技术细节

### **修改的文件**
- `App.tsx` (第 1074-1350 行)

### **新增的函数**
1. `getAzimuthDescEN(angle, isCenter)` - 分级水平角度描述
2. `getElevationDescEN(angle, isCenter)` - 分级垂直角度描述
3. `getDistanceDescEN(z, isCenter)` - 分级距离描述
4. `generateCameraGrid(centerH, centerV, centerZ)` - 智能发散策略

### **关键参数**
- 特殊角度范围：67.5°-112.5°, 157.5°-202.5°, 247.5°-292.5°
- 极端距离范围：z < 2 或 z > 8
- 角度偏移：常规 ±20°，特殊 ±15°
- 距离偏移：±0.5
- 高度偏移：±10° 或 ±15°

---

## ✅ 验收标准

1. **九宫格逻辑正确**
   - [ ] 特殊角度时，距离固定
   - [ ] 极端距离时，角度固定
   - [ ] 常规情况时，距离固定

2. **提示词分级生效**
   - [ ] 中心面板使用强化描述
   - [ ] 周围面板使用标准描述
   - [ ] 特殊角度有额外指导

3. **生成效果提升**
   - [ ] 侧面角度更精确（不再露出两只眼睛）
   - [ ] 背面角度更精确（不再画正脸）
   - [ ] 距离更精确（特写就是特写，不是近景）

---

## 🚀 下一步计划

如果第1-2步效果好，继续实施：

**第3步：添加负面提示词**
- 背面：禁止 `face visible, looking at camera, front view`
- 侧面：禁止 `both eyes visible, three-quarter view, turned head`
- 正面：禁止 `side view, back view, profile`

**第4步：进一步优化**
- 添加 `orthographic camera` 强制要求
- 添加 `consistent lighting` 要求
- 优化九宫格布局描述

---

## 📌 注意事项

1. **不要修改3D相机UI组件**：这次只改提示词生成逻辑
2. **保持向后兼容**：旧的节点数据仍然可以正常工作
3. **测试多种场景**：正面、侧面、背面、特写、全景都要测试
4. **观察AI行为**：看AI是否真的理解了强化描述

---

**实施完成时间**: 2026-01-30  
**预计测试时间**: 1-2小时  
**预计效果**: 角度精确度提升 30-50%
