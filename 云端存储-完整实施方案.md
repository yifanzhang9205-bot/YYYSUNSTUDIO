# 云端存储 - 完整实施方案

## 一、需求概述

将图片从浏览器本地存储迁移到云端，实现：
- 无限存储容量
- 跨设备同步
- 数据永久保存
- 性能优化（缩略图 + 原图）

---

## 二、技术架构

### 整体架构图

```
┌─────────────┐
│   前端 Web   │
│  (React)    │
└──────┬──────┘
       │ HTTPS
       ↓
┌─────────────┐
│  后端 API    │
│  (Node.js)  │
└──────┬──────┘
       │
       ├─→ ┌──────────┐
       │   │ 数据库    │ (用户数据、元信息)
       │   │ MySQL/PG │
       │   └──────────┘
       │
       └─→ ┌──────────┐
           │ 对象存储  │ (图片文件)
           │ OSS/S3   │
           └──────────┘
```

---

## 三、需要准备的资源

### 1. 服务器（后端 API）

**选项 A：云服务器（推荐新手）**
- **阿里云 ECS** / **腾讯云 CVM** / **AWS EC2**
- 配置：2核4G，40GB SSD
- 带宽：5Mbps（按需升级）
- 系统：Ubuntu 22.04 LTS
- 费用：~¥100-200/月

**选项 B：Serverless（推荐进阶）**
- **Vercel** / **Netlify** / **AWS Lambda**
- 按请求计费，自动扩展
- 无需管理服务器
- 费用：免费额度 + 按量计费

**我的建议：** 先用阿里云 ECS（稳定，好调试）

### 2. 对象存储（图片文件）

**选项 A：阿里云 OSS（推荐国内）**
- 存储费用：~¥0.12/GB/月
- 流量费用：~¥0.5/GB（CDN 加速）
- 免费额度：新用户有优惠
- 特点：国内访问快，文档完善

**选项 B：AWS S3（推荐国际）**
- 存储费用：~$0.023/GB/月
- 流量费用：~$0.09/GB
- 特点：全球覆盖，稳定性高

**选项 C：七牛云 / 又拍云（推荐小团队）**
- 免费额度：10GB 存储 + 10GB 流量/月
- 费用：超出后按量计费
- 特点：有免费额度，适合起步

**我的建议：** 先用七牛云（有免费额度，够测试用）

### 3. 数据库（用户数据）

**选项 A：MySQL / PostgreSQL**
- 云数据库：阿里云 RDS / AWS RDS
- 费用：~¥50-100/月
- 特点：稳定，功能完善

**选项 B：MongoDB**
- 云数据库：MongoDB Atlas
- 免费额度：512MB
- 特点：灵活，适合快速开发

**选项 C：Supabase（推荐快速开发）**
- 免费额度：500MB 数据库 + 1GB 存储
- 特点：开箱即用，自带认证
- 费用：免费 → $25/月

**我的建议：** 先用 Supabase（免费，功能全）

### 4. CDN 加速（可选）

**作用：** 加速图片访问，减少服务器压力

**选项：**
- 阿里云 CDN
- 腾讯云 CDN
- Cloudflare（免费）

**我的建议：** 先不用，等用户多了再加

### 5. 域名 + SSL 证书

**域名：**
- 阿里云 / 腾讯云 / GoDaddy
- 费用：~¥50-100/年
- 例如：`api.sunstudio.com`

**SSL 证书：**
- Let's Encrypt（免费）
- 阿里云 SSL（免费版）

---

## 四、后端 API 设计

### 技术栈选择

**推荐方案：Node.js + Express**

```javascript
// 技术栈
- Node.js 18+
- Express.js (Web 框架)
- Multer (文件上传)
- Sharp (图片处理)
- Prisma (数据库 ORM)
- JWT (用户认证)
```

**为什么选 Node.js？**
- 和前端同语言（TypeScript）
- 生态丰富，库多
- 适合 I/O 密集型（图片上传）
- 部署简单

### API 接口设计

#### 1. 图片上传

```http
POST /api/images/upload
Content-Type: multipart/form-data
Authorization: Bearer <token>

Body:
- image: File (图片文件)
- title: String (可选)
- metadata: JSON (可选，节点信息)

Response:
{
  "success": true,
  "data": {
    "id": "img_123456",
    "thumbnailUrl": "https://cdn.example.com/thumb/xxx.jpg",
    "originalUrl": "https://cdn.example.com/original/xxx.jpg",
    "size": 524288,
    "width": 1024,
    "height": 1024,
    "createdAt": "2025-01-24T10:00:00Z"
  }
}
```

**处理流程：**
1. 接收图片文件
2. 生成缩略图（128x128）
3. 上传原图到 OSS
4. 上传缩略图到 OSS
5. 保存元信息到数据库
6. 返回 URL

#### 2. 图片列表

```http
GET /api/images?page=1&limit=50
Authorization: Bearer <token>

Response:
{
  "success": true,
  "data": {
    "images": [
      {
        "id": "img_123456",
        "thumbnailUrl": "...",
        "originalUrl": "...",
        "title": "多角度相机 - 角度 1",
        "createdAt": "2025-01-24T10:00:00Z"
      }
    ],
    "total": 150,
    "page": 1,
    "limit": 50
  }
}
```

#### 3. 图片删除

```http
DELETE /api/images/:id
Authorization: Bearer <token>

Response:
{
  "success": true,
  "message": "图片已删除"
}
```

**处理流程：**
1. 验证用户权限
2. 从 OSS 删除文件
3. 从数据库删除记录

#### 4. 批量下载

```http
POST /api/images/download
Authorization: Bearer <token>

Body:
{
  "imageIds": ["img_123", "img_456"]
}

Response:
{
  "success": true,
  "data": {
    "zipUrl": "https://cdn.example.com/downloads/xxx.zip",
    "expiresAt": "2025-01-24T11:00:00Z"
  }
}
```

#### 5. 用户认证

```http
POST /api/auth/register
Body:
{
  "email": "user@example.com",
  "password": "******"
}

POST /api/auth/login
Body:
{
  "email": "user@example.com",
  "password": "******"
}

Response:
{
  "success": true,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "user": {
      "id": "user_123",
      "email": "user@example.com",
      "name": "张三"
    }
  }
}
```

---

## 五、数据库设计

### 表结构

#### 1. users 表（用户）

```sql
CREATE TABLE users (
  id VARCHAR(36) PRIMARY KEY,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  name VARCHAR(100),
  avatar_url VARCHAR(500),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 2. images 表（图片）

```sql
CREATE TABLE images (
  id VARCHAR(36) PRIMARY KEY,
  user_id VARCHAR(36) NOT NULL,
  title VARCHAR(255),
  thumbnail_url VARCHAR(500) NOT NULL,
  original_url VARCHAR(500) NOT NULL,
  file_size INT,
  width INT,
  height INT,
  mime_type VARCHAR(50),
  metadata JSON,  -- 存储节点信息、提示词等
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_user_created (user_id, created_at)
);
```

#### 3. workflows 表（工作流）

```sql
CREATE TABLE workflows (
  id VARCHAR(36) PRIMARY KEY,
  user_id VARCHAR(36) NOT NULL,
  title VARCHAR(255) NOT NULL,
  thumbnail_url VARCHAR(500),
  data JSON NOT NULL,  -- 存储节点、连接、组等
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_user_updated (user_id, updated_at)
);
```

---

## 六、前端改造

### 1. 添加云存储服务

```typescript
// services/cloudStorage.ts

interface UploadResult {
  id: string;
  thumbnailUrl: string;
  originalUrl: string;
  size: number;
  width: number;
  height: number;
}

export async function uploadImage(
  blob: Blob, 
  title?: string
): Promise<UploadResult> {
  const formData = new FormData();
  formData.append('image', blob);
  if (title) formData.append('title', title);
  
  const token = localStorage.getItem('authToken');
  
  const response = await fetch('https://api.sunstudio.com/api/images/upload', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`
    },
    body: formData
  });
  
  if (!response.ok) {
    throw new Error('上传失败');
  }
  
  const { data } = await response.json();
  return data;
}

export async function getImages(page = 1, limit = 50) {
  const token = localStorage.getItem('authToken');
  
  const response = await fetch(
    `https://api.sunstudio.com/api/images?page=${page}&limit=${limit}`,
    {
      headers: {
        'Authorization': `Bearer ${token}`
      }
    }
  );
  
  const { data } = await response.json();
  return data;
}

export async function deleteImage(id: string) {
  const token = localStorage.getItem('authToken');
  
  await fetch(`https://api.sunstudio.com/api/images/${id}`, {
    method: 'DELETE',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
}
```

### 2. 修改图片生成流程

```typescript
// App.tsx

// 之前：生成 Blob URL
const blob = await response.blob();
const blobUrl = URL.createObjectURL(blob);
setAssetHistory(prev => [...prev, { src: blobUrl, ... }]);

// 之后：上传到云端
const blob = await response.blob();
const { thumbnailUrl, originalUrl } = await uploadImage(blob, '多角度相机 - 角度 1');
setAssetHistory(prev => [...prev, { 
  thumbnailUrl,   // 缩略图 URL
  originalUrl,    // 原图 URL
  ... 
}]);
```

### 3. 修改历史记录显示

```typescript
// components/SidebarDock.tsx

// 之前：显示 Blob URL
<img src={asset.src} />

// 之后：显示缩略图 URL
<img src={asset.thumbnailUrl} />

// 点击查看时加载原图
onClick={() => {
  setExpandedImage(asset.originalUrl);
}}
```

---

## 七、实施步骤

### 阶段 1：准备工作（1-2 天）

1. **注册云服务**
   - [ ] 注册阿里云账号
   - [ ] 开通 ECS 服务器（2核4G）
   - [ ] 开通 OSS 对象存储
   - [ ] 注册域名（可选）

2. **配置服务器**
   - [ ] 安装 Node.js 18+
   - [ ] 安装 Nginx（反向代理）
   - [ ] 配置防火墙（开放 80/443 端口）
   - [ ] 申请 SSL 证书

3. **配置数据库**
   - [ ] 注册 Supabase 账号
   - [ ] 创建数据库
   - [ ] 创建表结构

### 阶段 2：后端开发（3-5 天）

1. **搭建项目**
   ```bash
   mkdir sunstudio-api
   cd sunstudio-api
   npm init -y
   npm install express multer sharp @supabase/supabase-js ali-oss jsonwebtoken bcrypt
   ```

2. **开发 API**
   - [ ] 用户认证（注册/登录）
   - [ ] 图片上传（含缩略图生成）
   - [ ] 图片列表（分页）
   - [ ] 图片删除
   - [ ] 批量下载

3. **测试 API**
   - [ ] 使用 Postman 测试
   - [ ] 编写单元测试

### 阶段 3：前端改造（2-3 天）

1. **添加云存储服务**
   - [ ] 创建 `services/cloudStorage.ts`
   - [ ] 实现上传/下载/删除

2. **修改图片生成流程**
   - [ ] 多角度相机上传
   - [ ] 普通图片生成上传
   - [ ] 视频生成上传

3. **修改历史记录**
   - [ ] 显示缩略图
   - [ ] 点击加载原图
   - [ ] 分页加载

4. **添加用户认证**
   - [ ] 登录/注册界面
   - [ ] Token 管理
   - [ ] 自动登录

### 阶段 4：部署上线（1-2 天）

1. **部署后端**
   - [ ] 上传代码到服务器
   - [ ] 配置环境变量
   - [ ] 启动服务（PM2）
   - [ ] 配置 Nginx 反向代理

2. **部署前端**
   - [ ] 修改 API 地址
   - [ ] 构建生产版本
   - [ ] 上传到服务器/CDN

3. **测试验证**
   - [ ] 功能测试
   - [ ] 性能测试
   - [ ] 安全测试

---

## 八、成本估算

### 初期（测试阶段）

| 项目 | 费用 | 说明 |
|------|------|------|
| 服务器 | ¥100/月 | 阿里云 ECS 2核4G |
| 对象存储 | 免费 | 七牛云免费额度 10GB |
| 数据库 | 免费 | Supabase 免费版 |
| 域名 | ¥50/年 | .com 域名 |
| SSL 证书 | 免费 | Let's Encrypt |
| **合计** | **¥100/月** | 约 ¥1200/年 |

### 正式运营（100 用户）

| 项目 | 费用 | 说明 |
|------|------|------|
| 服务器 | ¥200/月 | 4核8G |
| 对象存储 | ¥50/月 | 100GB 存储 + 流量 |
| 数据库 | ¥100/月 | 云数据库 |
| CDN | ¥50/月 | 加速访问 |
| **合计** | **¥400/月** | 约 ¥4800/年 |

### 规模化（1000+ 用户）

| 项目 | 费用 | 说明 |
|------|------|------|
| 服务器 | ¥500/月 | 8核16G + 负载均衡 |
| 对象存储 | ¥300/月 | 1TB 存储 + 流量 |
| 数据库 | ¥300/月 | 高可用版 |
| CDN | ¥200/月 | 全球加速 |
| **合计** | **¥1300/月** | 约 ¥15600/年 |

---

## 九、技术难点和解决方案

### 1. 大文件上传

**问题：** 图片可能很大（5-10MB），上传慢

**解决方案：**
- 分片上传（Multipart Upload）
- 断点续传
- 压缩后上传

### 2. 并发上传

**问题：** 九宫格 9 张图片同时上传，服务器压力大

**解决方案：**
- 限制并发数（3-5 个）
- 队列上传
- 显示上传进度

### 3. 图片处理性能

**问题：** 生成缩略图耗时

**解决方案：**
- 使用 Sharp（比 ImageMagick 快 4-5 倍）
- 异步处理（后台任务）
- 缓存缩略图

### 4. 安全性

**问题：** 防止恶意上传、盗链

**解决方案：**
- JWT 认证
- 文件类型检查
- 文件大小限制
- 防盗链（Referer 检查）
- 签名 URL（临时访问）

---

## 十、推荐学习资源

### 后端开发

1. **Node.js + Express**
   - 官方文档：https://expressjs.com/
   - 教程：Node.js 从入门到实战

2. **阿里云 OSS**
   - 官方文档：https://help.aliyun.com/product/31815.html
   - SDK：ali-oss (npm)

3. **Supabase**
   - 官方文档：https://supabase.com/docs
   - 快速开始：15 分钟搭建后端

### 前端改造

1. **文件上传**
   - FormData API
   - Fetch API
   - 上传进度显示

2. **图片优化**
   - 懒加载
   - 渐进式加载
   - WebP 格式

---

## 十一、总结

### 最小可行方案（MVP）

**只需要：**
1. 一台服务器（¥100/月）
2. 对象存储（免费额度）
3. 数据库（免费额度）
4. 1-2 周开发时间

**就能实现：**
- 无限存储
- 跨设备同步
- 性能提升 10x

### 下一步

等你准备好服务器后，我可以帮你：
1. 编写完整的后端代码
2. 改造前端上传逻辑
3. 部署和测试

**需要我现在给你一份后端代码模板吗？**
