# 九宫格节点 - Blob URL 失效问题 - 2026-02-02

**日期**: 2026-02-02  
**状态**: 🔍 诊断中  
**类型**: Bug 修复

---

## 问题现象

### 错误日志

```
blob:http://localhost:3000/56148bd1-b40f-488e-8717-9abc634f8e1c:1  
Failed to load resource: net::ERR_FILE_NOT_FOUND

GridSplitterNode.tsx:162 [GridSplitter] 自动切割失败: Error: 图片加载失败
    at img.onerror (GridSplitterNode.tsx:114:34)
```

**特征**：
- 错误重复出现数百次
- 同一个 Blob URL 反复加载失败
- 触发无限循环的自动切割

---

## 根本原因分析

### 原因 1：Blob URL 已被释放

**Blob URL 的生命周期**：
```typescript
// 创建 Blob URL
const blobUrl = URL.createObjectURL(blob);

// 使用 Blob URL
<img src={blobUrl} />

// 释放 Blob URL（重要！）
URL.revokeObjectURL(blobUrl);
```

**问题**：
- 3D 相机生成图片后，创建了 Blob URL
- 图片被传递给九宫格节点
- 3D 相机或其他组件释放了 Blob URL
- 九宫格节点试图加载已释放的 Blob URL
- 加载失败，触发 `img.onerror`

---

### 原因 2：自动切割逻辑触发无限循环

**当前逻辑**：
```typescript
useEffect(() => {
  // 条件 1：有输入图片
  if (!inputImage) return;
  
  // 条件 2：输入图片改变了
  if (inputImage === lastInputImageRef.current) return;
  
  // 条件 3：不在处理中
  if (isProcessing) return;
  
  // 开始切割
  lastInputImageRef.current = inputImage;
  setIsProcessing(true);
  
  cropGridImage(inputImage)
    .then(/* ... */)
    .catch((err) => {
      // 🔥 问题：切割失败后，重置跟踪
      lastInputImageRef.current = undefined;
    })
    .finally(() => {
      setIsProcessing(false);
    });
}, [inputImage, isProcessing, cropGridImage, onUpdate]);
```

**无限循环的原因**：
1. `inputImage` 是一个失效的 Blob URL
2. 切割失败，触发 `catch`
3. `lastInputImageRef.current = undefined`（重置跟踪）
4. `setIsProcessing(false)`
5. useEffect 再次触发（因为 `inputImage !== lastInputImageRef.current`）
6. 再次尝试切割
7. 再次失败
8. **无限循环！**

---

## 解决方案

### 方案 1：不重置 `lastInputImageRef`（推荐）

**修改 `catch` 逻辑**：
```typescript
.catch((err) => {
  console.error('[GridSplitter] 自动切割失败:', err);
  // 🔥 不重置 lastInputImageRef，防止无限重试
  // lastInputImageRef.current = undefined; // ❌ 删除这行
})
```

**效果**：
- 切割失败后，不再重试
- 避免无限循环
- 用户可以手动清除后重新上传

**缺点**：
- 如果是临时网络问题，不会自动重试
- 需要用户手动清除后重新上传

---

### 方案 2：添加重试次数限制

**使用 ref 跟踪重试次数**：
```typescript
const retryCountRef = useRef<number>(0);
const MAX_RETRIES = 3;

useEffect(() => {
  // ... 其他条件检查
  
  // 🔥 检查重试次数
  if (retryCountRef.current >= MAX_RETRIES) {
    console.error('[GridSplitter] 达到最大重试次数，停止切割');
    return;
  }
  
  // 开始切割
  lastInputImageRef.current = inputImage;
  setIsProcessing(true);
  retryCountRef.current += 1; // 增加重试次数
  
  cropGridImage(inputImage)
    .then((cropped) => {
      // 成功，重置重试次数
      retryCountRef.current = 0;
      onUpdate({ /* ... */ });
    })
    .catch((err) => {
      console.error('[GridSplitter] 自动切割失败:', err);
      // 失败，重置跟踪，允许重试
      lastInputImageRef.current = undefined;
    })
    .finally(() => {
      setIsProcessing(false);
    });
}, [inputImage, isProcessing, cropGridImage, onUpdate]);
```

**效果**：
- 最多重试 3 次
- 避免无限循环
- 允许临时网络问题自动恢复

**缺点**：
- 增加代码复杂度
- 需要在清除时重置重试次数

---

### 方案 3：验证 Blob URL 是否有效

**在切割前验证 URL**：
```typescript
/**
 * 验证 Blob URL 是否有效
 */
const validateBlobUrl = useCallback(async (url: string): Promise<boolean> => {
  if (!url.startsWith('blob:')) {
    return true; // 不是 Blob URL，假设有效
  }
  
  return new Promise((resolve) => {
    const img = new Image();
    img.onload = () => resolve(true);
    img.onerror = () => resolve(false);
    img.src = url;
    
    // 超时检查（1 秒）
    setTimeout(() => resolve(false), 1000);
  });
}, []);

useEffect(() => {
  // ... 其他条件检查
  
  // 🔥 验证 URL 是否有效
  validateBlobUrl(inputImage).then((isValid) => {
    if (!isValid) {
      console.error('[GridSplitter] Blob URL 已失效，跳过切割');
      lastInputImageRef.current = inputImage; // 标记为已处理，避免重试
      return;
    }
    
    // URL 有效，开始切割
    lastInputImageRef.current = inputImage;
    setIsProcessing(true);
    
    cropGridImage(inputImage)
      .then(/* ... */)
      .catch(/* ... */)
      .finally(/* ... */);
  });
}, [inputImage, isProcessing, cropGridImage, onUpdate, validateBlobUrl]);
```

**效果**：
- 在切割前验证 URL 是否有效
- 避免尝试加载失效的 Blob URL
- 避免无限循环

**缺点**：
- 增加代码复杂度
- 每次都需要验证，增加性能开销

---

## 推荐方案：方案 1（最简单）

**理由**：
1. **最简单**：只需删除一行代码
2. **最安全**：不会无限循环
3. **最直观**：切割失败就停止，不重试

**实施**：
```typescript
.catch((err) => {
  console.error('[GridSplitter] 自动切割失败:', err);
  // 🔥 不重置 lastInputImageRef，防止无限重试
  // 用户可以手动清除后重新上传
})
```

---

## 根本解决方案：修复 Blob URL 管理

**问题根源**：Blob URL 被过早释放

**需要检查的地方**：

### 1. 3D 相机节点（MultiAngleCameraNode.tsx）

**检查**：
- 3D 相机生成图片后，是否立即释放了 Blob URL？
- 是否在传递给九宫格节点前就释放了？

**修复**：
- 不要在生成后立即释放 Blob URL
- 让九宫格节点负责释放（或者使用 IndexedDB 持久化）

### 2. 九宫格节点（GridSplitterNode.tsx）

**检查**：
- 九宫格节点是否正确管理 Blob URL 的生命周期？
- 是否在使用完后释放了 Blob URL？

**修复**：
- 在组件卸载时释放 Blob URL
- 在清除时释放 Blob URL

### 3. 使用 IndexedDB 持久化（推荐）

**方案**：
- 3D 相机生成图片后，保存到 IndexedDB
- 返回持久化的 Blob URL（从 IndexedDB 读取）
- 九宫格节点从 IndexedDB 读取图片

**优点**：
- Blob URL 不会失效
- 刷新页面后图片仍然存在
- 不需要担心 Blob URL 的生命周期

---

## 临时解决方案（立即修复）

### 步骤 1：修改 GridSplitterNode.tsx

**删除重置逻辑**：
```typescript
.catch((err) => {
  console.error('[GridSplitter] 自动切割失败:', err);
  // ❌ 删除这行，防止无限重试
  // lastInputImageRef.current = undefined;
})
```

### 步骤 2：清除浏览器缓存

**方法 1：硬刷新**
- Windows: `Ctrl + Shift + R`
- Mac: `Cmd + Shift + R`

**方法 2：清除缓存**
- 打开开发者工具（F12）
- 右键点击刷新按钮
- 选择"清空缓存并硬性重新加载"

### 步骤 3：测试验证

1. 创建 3D 相机节点
2. 创建九宫格节点
3. 连接 3D 相机 → 九宫格
4. 3D 相机生成图片
5. 观察控制台，确认没有无限循环

---

## 长期解决方案（根本修复）

### 使用 IndexedDB 持久化图片

**修改 3D 相机节点**：
```typescript
// 生成图片后，保存到 IndexedDB
const { saveFileToIndexedDBAsync } = await import('../services/blobStorage');
const blob = await fetch(imageUrl).then(r => r.blob());
await saveFileToIndexedDBAsync(`camera-${Date.now()}`, blob);

// 返回持久化的 Blob URL
const persistedUrl = URL.createObjectURL(blob);
onUpdate({ image: persistedUrl });
```

**修改九宫格节点**：
```typescript
// 从 IndexedDB 读取图片
const { loadFileFromIndexedDB } = await import('../services/blobStorage');
const blob = await loadFileFromIndexedDB(imageId);
const blobUrl = URL.createObjectURL(blob);

// 使用 Blob URL
cropGridImage(blobUrl);
```

---

## 总结

### 立即修复（方案 1）

1. ✅ 删除 `catch` 中的 `lastInputImageRef.current = undefined`
2. ✅ 清除浏览器缓存（硬刷新）
3. ✅ 测试验证

### 根本修复（长期）

1. 🔄 使用 IndexedDB 持久化图片
2. 🔄 修改 3D 相机节点的图片管理
3. 🔄 修改九宫格节点的图片加载

---

**下一步**：立即实施方案 1，修复无限循环问题。
