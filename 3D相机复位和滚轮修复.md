 # 3D 相机复位和滚轮修复

## 修复的问题

### 问题 1：相机自动复位 ❌ → ✅
**现象：** 点击选择框内其他位置时，相机会突然跳回初始角度

**原因：** 
- `useEffect` 依赖 `[horizontalAngle, verticalAngle, cameraZoom]`
- 每次这些 props 变化时，useEffect 都会重新执行
- 即使 props 值没变，React 也可能触发重新渲染，导致相机"复位"

**修复：**
```typescript
// 添加 prevAnglesRef 记录上一次的值
const prevAnglesRef = useRef({ h: horizontalAngle, v: verticalAngle, z: cameraZoom });

useEffect(() => {
  if (!sceneRef.current) return;
  
  // 检查是否真的变化了（避免不必要的更新）
  const prev = prevAnglesRef.current;
  if (prev.h === horizontalAngle && prev.v === verticalAngle && prev.z === cameraZoom) {
    return; // 没有变化，不更新
  }
  
  // 更新记录
  prevAnglesRef.current = { h: horizontalAngle, v: verticalAngle, z: cameraZoom };
  
  // ... 更新相机位置
}, [horizontalAngle, verticalAngle, cameraZoom]);
```

**效果：** 
- 只有角度/距离真正变化时才更新相机
- 点击其他位置不会触发相机复位
- 相机永远跟着拖动而动，不会自己跳

### 问题 2：滚轮还是控制画布 ❌ → ✅
**现象：** 选中节点后滚轮还是控制画布缩放，而不是相机距离

**原因：** 
- `e.stopPropagation()` 只阻止了 React 合成事件的冒泡
- 原生事件还是会冒泡到画布
- 需要同时阻止原生事件的传播

**修复：**
```typescript
const handleWheel = (e: React.WheelEvent) => {
  if (!isExpanded) {
    return; // 不阻止冒泡，让画布处理滚轮事件
  }
  
  // 选中时：阻止事件冒泡和默认行为
  e.stopPropagation(); // 阻止 React 合成事件冒泡
  e.preventDefault(); // 阻止默认滚动行为
  e.nativeEvent.stopImmediatePropagation(); // ✅ 立即停止原生事件传播
  
  // ... 控制相机
};
```

**效果：**
- 选中时：滚轮只控制相机，画布完全不动
- 未选中时：滚轮只控制画布，相机完全不动

## 技术细节

### 为什么需要 prevAnglesRef？

React 的 useEffect 依赖数组是**浅比较**：
```typescript
// 即使值相同，React 也可能认为需要重新执行
useEffect(() => {
  // 这里会执行
}, [horizontalAngle, verticalAngle, cameraZoom]);
```

使用 ref 进行**深度比较**：
```typescript
// 只有值真正变化时才执行
const prev = prevAnglesRef.current;
if (prev.h === horizontalAngle && prev.v === verticalAngle && prev.z === cameraZoom) {
  return; // 值没变，跳过更新
}
```

### 为什么需要 stopImmediatePropagation？

React 的事件系统是**合成事件**：
- `e.stopPropagation()` 只阻止 React 合成事件冒泡
- 原生 DOM 事件还是会冒泡
- `e.nativeEvent.stopImmediatePropagation()` 立即停止原生事件传播

```
用户滚动滚轮
  ↓
原生 wheel 事件
  ↓
React 合成事件 (onWheel)
  ├─ e.stopPropagation() → 阻止 React 事件冒泡
  └─ e.nativeEvent.stopImmediatePropagation() → 阻止原生事件冒泡
```

## 测试步骤

### 测试 1：相机不会自动复位
1. 打开多角度相机节点
2. 拖拽调整相机角度到某个位置（比如 45°）
3. 点击节点选择框内的其他位置（比如顶部标题栏）
4. **预期：** 相机保持在 45°，不会跳回 0°

### 测试 2：拖拽相机不会复位
1. 拖拽相机到某个角度
2. 松开鼠标
3. 再次拖拽
4. **预期：** 相机从上次的位置继续拖动，不会突然跳到其他位置

### 测试 3：选中时滚轮控制相机
1. 点击节点选中（边框变青色）
2. 确认操作提示显示 "拖拽旋转 · 滚轮推进"
3. 滚动鼠标滚轮
4. **预期：** 
   - 相机推进/拉远
   - 画布完全不动（不缩放）
   - 距离数值变化

### 测试 4：未选中时滚轮控制画布
1. 点击画布空白处（取消选中）
2. 确认操作提示显示 "点击选中以使用滚轮"
3. 滚动鼠标滚轮
4. **预期：**
   - 画布缩放
   - 相机完全不动（距离不变）

### 测试 5：连续操作不会复位
1. 拖拽相机到 90°
2. 滚轮推进到距离 8
3. 点击设置按钮
4. 关闭设置面板
5. **预期：** 相机保持在 90°，距离保持在 8

## 修复前后对比

### 修复前
```
点击其他位置 → useEffect 重新执行 → 相机跳回初始位置 ❌
选中后滚轮 → 原生事件冒泡 → 画布缩放 ❌
```

### 修复后
```
点击其他位置 → 检查值是否变化 → 没变化，跳过更新 → 相机保持不动 ✅
选中后滚轮 → 阻止原生事件传播 → 只控制相机 → 画布不动 ✅
```

## 总结

这次修复解决了两个核心问题：

1. ✅ **相机不会自动复位**
   - 使用 `prevAnglesRef` 记录上一次的值
   - 只有值真正变化时才更新相机
   - 相机永远跟着拖动而动

2. ✅ **滚轮正确控制相机/画布**
   - 使用 `stopImmediatePropagation()` 阻止原生事件传播
   - 选中时：滚轮只控制相机
   - 未选中时：滚轮只控制画布

现在的交互更加稳定、可预测、符合直觉！
