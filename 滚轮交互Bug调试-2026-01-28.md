# 滚轮交互 Bug 调试 - 2026-01-28

## 问题描述

用户反馈：
- Shift + 滚轮会有 bug，有时会自动吸附
- 即使松开 Shift 也处于调整的状态
- 即使只使用滚轮按住也会出现问题

## 可能的原因

### 1. 浏览器默认行为未完全阻止
- Shift + 滚轮在某些浏览器中会触发前进/后退
- 需要同时使用 `preventDefault()` 和 `stopPropagation()`

### 2. 事件监听器重复注册
- 如果 `handleWheel` 被多次注册，会导致状态更新混乱
- 已检查：只注册了一次 ✅

### 3. 状态未正确清除
- 滚轮事件没有"结束"状态，不像鼠标拖动有 `mouseup`
- 可能导致某些状态一直保持

### 4. React 状态更新冲突
- 如果 `setPan` 被频繁调用，可能导致状态更新冲突
- 需要使用函数式更新：`setPan(prev => ...)`

---

## 已实施的修复

### 修复 1：阻止所有默认行为（`hooks/useViewport.ts`）

```typescript
const handleWheel = useCallback((e: WheelEvent) => {
  // 🔥 修复：阻止所有默认行为（包括 Shift + 滚轮的前进/后退）
  e.preventDefault();
  e.stopPropagation();
  
  // 🔥 恢复 Shift + 滚轮平移功能
  if (e.shiftKey) {
    // Shift + 滚轮：平移
    setPan(prev => ({
      x: prev.x - e.deltaX,
      y: prev.y - e.deltaY,
    }));
  } else {
    // 普通滚轮：缩放
    const delta = -e.deltaY * 0.001;
    handleZoom(delta, e.clientX, e.clientY);
  }
}, [handleZoom]);
```

**改动说明：**
- 添加 `e.stopPropagation()`，阻止事件冒泡
- 使用函数式更新 `setPan(prev => ...)`，避免状态冲突
- 恢复 Shift + 滚轮平移功能

---

## 调试步骤

### 步骤 1：检查浏览器控制台
打开浏览器控制台（F12），查看是否有错误或警告。

### 步骤 2：添加调试日志
在 `hooks/useViewport.ts` 的 `handleWheel` 方法中添加日志：

```typescript
const handleWheel = useCallback((e: WheelEvent) => {
  console.log('[handleWheel]', {
    shiftKey: e.shiftKey,
    deltaX: e.deltaX,
    deltaY: e.deltaY,
    currentPan: panRef.current,
    currentScale: scaleRef.current,
  });
  
  e.preventDefault();
  e.stopPropagation();
  
  // ... 其余代码
}, [handleZoom]);
```

### 步骤 3：测试场景
1. **普通滚轮缩放**
   - 滚动滚轮，观察控制台日志
   - 检查 `shiftKey` 是否为 `false`
   - 检查 `scale` 是否正常变化

2. **Shift + 滚轮平移**
   - 按住 Shift，滚动滚轮
   - 检查 `shiftKey` 是否为 `true`
   - 检查 `pan` 是否正常变化

3. **松开 Shift**
   - 按住 Shift，滚动滚轮
   - 松开 Shift，再滚动滚轮
   - 检查是否从平移切换到缩放

4. **快速切换**
   - 快速按下/松开 Shift，同时滚动滚轮
   - 检查是否有状态混乱

### 步骤 4：检查其他事件监听器
查找是否有其他地方也在监听 `wheel` 事件：

```bash
# 在项目根目录执行
grep -r "addEventListener.*wheel" --include="*.tsx" --include="*.ts"
```

### 步骤 5：检查 CSS 属性
某些 CSS 属性可能影响滚轮行为：

```css
/* 检查 canvas-container 是否有这些属性 */
overscroll-behavior: none; /* 阻止滚动链 */
touch-action: none; /* 阻止触摸手势 */
```

---

## 可能的 Bug 原因分析

### 原因 1：浏览器默认行为未完全阻止
**症状：** Shift + 滚轮时，浏览器会前进/后退，导致页面状态混乱

**解决方案：**
- 使用 `e.preventDefault()` 阻止默认行为 ✅
- 使用 `e.stopPropagation()` 阻止事件冒泡 ✅
- 在 `addEventListener` 中使用 `{ passive: false }` ✅

### 原因 2：状态更新冲突
**症状：** 快速滚动时，状态更新不准确，出现"吸附"现象

**解决方案：**
- 使用函数式更新 `setPan(prev => ...)` ✅
- 使用 `useRef` 缓存最新状态 ✅
- 使用 RAF 节流（可选）

### 原因 3：事件监听器未正确清理
**症状：** 组件卸载后，事件监听器仍然存在，导致状态混乱

**解决方案：**
- 在 `useEffect` 的 cleanup 函数中移除监听器 ✅
- 确保 `handleWheel` 的依赖项正确 ✅

### 原因 4：多个组件同时监听滚轮
**症状：** 多个组件都在监听滚轮事件，导致状态冲突

**解决方案：**
- 检查是否有其他组件也在监听 `wheel` 事件
- 使用 `e.stopPropagation()` 阻止事件冒泡 ✅

---

## 进一步的修复方案

### 方案 1：使用 RAF 节流（如果频繁触发导致性能问题）

```typescript
const handleWheel = useCallback((e: WheelEvent) => {
  e.preventDefault();
  e.stopPropagation();
  
  // 取消之前的 RAF
  if (wheelRafRef.current) {
    cancelAnimationFrame(wheelRafRef.current);
  }
  
  // 使用 RAF 节流
  wheelRafRef.current = requestAnimationFrame(() => {
    if (e.shiftKey) {
      setPan(prev => ({
        x: prev.x - e.deltaX,
        y: prev.y - e.deltaY,
      }));
    } else {
      const delta = -e.deltaY * 0.001;
      handleZoom(delta, e.clientX, e.clientY);
    }
  });
}, [handleZoom]);
```

### 方案 2：添加防抖（如果快速切换导致状态混乱）

```typescript
const handleWheel = useCallback((e: WheelEvent) => {
  e.preventDefault();
  e.stopPropagation();
  
  // 清除之前的定时器
  if (wheelTimerRef.current) {
    clearTimeout(wheelTimerRef.current);
  }
  
  // 立即执行
  if (e.shiftKey) {
    setPan(prev => ({
      x: prev.x - e.deltaX,
      y: prev.y - e.deltaY,
    }));
  } else {
    const delta = -e.deltaY * 0.001;
    handleZoom(delta, e.clientX, e.clientY);
  }
  
  // 设置定时器，确保状态稳定
  wheelTimerRef.current = setTimeout(() => {
    console.log('[handleWheel] 状态稳定');
  }, 100);
}, [handleZoom]);
```

### 方案 3：检测 Shift 键状态变化

```typescript
const [isShiftPressed, setIsShiftPressed] = useState(false);

useEffect(() => {
  const handleKeyDown = (e: KeyboardEvent) => {
    if (e.key === 'Shift') {
      setIsShiftPressed(true);
    }
  };
  
  const handleKeyUp = (e: KeyboardEvent) => {
    if (e.key === 'Shift') {
      setIsShiftPressed(false);
    }
  };
  
  window.addEventListener('keydown', handleKeyDown);
  window.addEventListener('keyup', handleKeyUp);
  
  return () => {
    window.removeEventListener('keydown', handleKeyDown);
    window.removeEventListener('keyup', handleKeyUp);
  };
}, []);

const handleWheel = useCallback((e: WheelEvent) => {
  e.preventDefault();
  e.stopPropagation();
  
  // 使用 state 而不是 e.shiftKey
  if (isShiftPressed) {
    setPan(prev => ({
      x: prev.x - e.deltaX,
      y: prev.y - e.deltaY,
    }));
  } else {
    const delta = -e.deltaY * 0.001;
    handleZoom(delta, e.clientX, e.clientY);
  }
}, [isShiftPressed, handleZoom]);
```

---

## 测试清单

### 基础功能测试
- [ ] 普通滚轮缩放是否正常
- [ ] Shift + 滚轮平移是否正常
- [ ] 松开 Shift 后是否切换到缩放
- [ ] 快速按下/松开 Shift 是否有状态混乱

### 边界情况测试
- [ ] 快速滚动时是否有"吸附"现象
- [ ] 滚动到缩放边界（0.1x 或 3x）时是否正常
- [ ] 同时按住多个修饰键（Shift + Ctrl）时是否正常
- [ ] 在不同浏览器中测试（Chrome、Firefox、Edge）

### 性能测试
- [ ] 快速滚动时是否有卡顿
- [ ] 长时间滚动后是否有内存泄漏
- [ ] 控制台是否有错误或警告

---

## 下一步

1. **保存文件**：按 Ctrl+S 保存 `hooks/useViewport.ts`
2. **测试**：在浏览器中测试滚轮交互
3. **添加日志**：如果问题仍然存在，添加调试日志
4. **反馈**：告诉我具体的症状和控制台日志

---

## 总结

已实施的修复：
1. ✅ 添加 `e.stopPropagation()` 阻止事件冒泡
2. ✅ 使用函数式更新 `setPan(prev => ...)`
3. ✅ 恢复 Shift + 滚轮平移功能

如果问题仍然存在，请：
1. 打开浏览器控制台（F12）
2. 添加调试日志（见上文）
3. 测试并记录具体症状
4. 反馈给我，我会继续修复
