# 架构重构 - 阶段 B - Store 迁移实施中

**日期**：2026-01-27  
**状态**：🚧 进行中  
**完成度**：30%

---

## 📋 任务概述

将 App.tsx 中的所有 React state 迁移到 Zustand Store，实现统一的数据管理。

---

## ✅ 已完成的工作

### 1. 创建新的 Store（100%）

| Store | 文件路径 | 职责 | 状态 |
|-------|---------|------|------|
| `selectionStore` | `core/stores/selectionStore.ts` | 管理节点和分组选择状态 | ✅ 完成 |
| `uiStore` | `core/stores/uiStore.ts` | 管理 UI 面板开关状态 | ✅ 完成 |
| `workflowStore` | `core/stores/workflowStore.ts` | 管理工作流数据 | ✅ 完成 |
| `assetHistoryStore` | `core/stores/assetHistoryStore.ts` | 管理资源历史记录 | ✅ 完成 |

### 2. 修改 Hooks 使用 Store（50%）

| Hook | 修改内容 | 状态 |
|------|---------|------|
| `useSelection` | 使用 `selectionStore` 管理选择状态 | ✅ 完成 |
| `useGroup` | 待检查是否需要修改 | ⏳ 待处理 |
| `useViewport` | 待检查是否需要修改 | ⏳ 待处理 |

---

## 🚧 进行中的工作

### 3. 在 App.tsx 中替换 state（0%）

需要替换的 state：

#### 第 1 组：UI 面板状态（高优先级）
```typescript
// 旧代码（需要删除）
const [isChatOpen, setIsChatOpen] = useState(false);
const [isSketchEditorOpen, setIsSketchEditorOpen] = useState(false);
const [isMultiFrameOpen, setIsMultiFrameOpen] = useState(false);
const [isSonicStudioOpen, setIsSonicStudioOpen] = useState(false);
const [isSettingsOpen, setIsSettingsOpen] = useState(false);
const [isLoaded, setIsLoaded] = useState(false);

// 新代码（需要添加）
const isChatOpen = useUIStore(state => state.isChatOpen);
const isSketchEditorOpen = useUIStore(state => state.isSketchEditorOpen);
const isMultiFrameOpen = useUIStore(state => state.isMultiFrameOpen);
const isSonicStudioOpen = useUIStore(state => state.isSonicStudioOpen);
const isSettingsOpen = useUIStore(state => state.isSettingsOpen);
const isLoaded = useUIStore(state => state.isLoaded);

const { 
  setChatOpen, 
  setSketchEditorOpen, 
  setMultiFrameOpen, 
  setSonicStudioOpen, 
  setSettingsOpen, 
  setLoaded 
} = useUIStore();
```

#### 第 2 组：工作流状态（高优先级）
```typescript
// 旧代码（需要删除）
const [workflows, setWorkflows] = useState<Workflow[]>([]);
const [selectedWorkflowId, setSelectedWorkflowId] = useState<string | null>(null);

// 新代码（需要添加）
const workflows = useWorkflowStore(state => state.workflows);
const selectedWorkflowId = useWorkflowStore(state => state.selectedWorkflowId);
const { addWorkflow, updateWorkflow, deleteWorkflow, selectWorkflow, setWorkflows } = useWorkflowStore();
```

#### 第 3 组：资源历史状态（中优先级）
```typescript
// 旧代码（需要删除）
const [assetHistory, setAssetHistory] = useState<any[]>([]);

// 新代码（需要添加）
const assetHistory = useAssetHistoryStore(state => state.assetHistory);
const { addHistory, clearHistory, setHistory } = useAssetHistoryStore();
```

#### 第 4 组：分组选择状态（中优先级）
```typescript
// 旧代码（需要删除）
const [selectedGroupId, setSelectedGroupId] = useState<string | null>(null);

// 新代码（需要添加）
const selectedGroupId = useSelectionStore(state => state.selectedGroupId);
const { selectGroup, clearGroupSelection } = useSelectionStore();
```

---

## 📝 下一步计划

### 步骤 1：在 App.tsx 中导入所有 Store
```typescript
import { useSelectionStore } from './core/stores/selectionStore';
import { useUIStore } from './core/stores/uiStore';
import { useWorkflowStore } from './core/stores/workflowStore';
import { useAssetHistoryStore } from './core/stores/assetHistoryStore';
```

### 步骤 2：替换 UI 面板状态
- 删除所有 `useState` 声明
- 添加 Store 的 selector
- 替换所有 `setXxx` 调用为 Store 的方法

### 步骤 3：替换工作流状态
- 删除 `workflows` 和 `selectedWorkflowId` 的 `useState`
- 添加 Store 的 selector
- 替换所有相关的 `setWorkflows` 和 `setSelectedWorkflowId` 调用

### 步骤 4：替换资源历史状态
- 删除 `assetHistory` 的 `useState`
- 添加 Store 的 selector
- 替换所有 `setAssetHistory` 调用

### 步骤 5：替换分组选择状态
- 删除 `selectedGroupId` 的 `useState`
- 添加 Store 的 selector
- 替换所有 `setSelectedGroupId` 调用

### 步骤 6：测试和验证
- 编译检查
- 功能测试
- 性能测试

---

## 🎯 预期效果

完成阶段 B 后：

1. **数据流清晰**
   - 所有数据都在 Store 中
   - 不再有 React state 和 Store 混用
   - 数据流向单一：Store → UI

2. **容易调试**
   - 可以使用 Zustand DevTools 查看所有状态
   - 状态变化可追踪
   - 不会出现状态不同步的问题

3. **为阶段 C 做准备**
   - App.tsx 只剩下 UI 组合逻辑
   - 可以大刀阔斧地删除旧代码
   - 架构清晰，易于维护

---

## 📊 完成度统计

| 任务 | 完成度 |
|------|--------|
| 创建 Store | 100% (4/4) |
| 修改 Hooks | 50% (1/2) |
| 替换 App.tsx state | 0% (0/5) |
| **总体完成度** | **30%** |

---

## 🚀 继续执行

现在开始执行步骤 1-5，将 App.tsx 中的所有 state 替换为 Store。
