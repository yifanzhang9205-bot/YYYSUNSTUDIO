# 3D 相机距离范围修复

## 修复时间
2026-01-24

## 问题描述

用户反馈：**在 3D 模型里选择也选不到特写，在设置里也选不到**

## 问题分析

### 配置不一致

**需求文档：**
- 距离滑块：0 - 10，步长 0.5

**实际实现（修复前）：**
- 滑块范围：0 - 14
- 预设按钮：0, 2, 4, 6, 8, 10, 12（最大 12）
- 提示词映射：只处理到 10

**矛盾：**
1. 滑块可以到 14，但提示词映射只到 10
2. 预设按钮最大到 12，与滑块和映射都不一致
3. 预设按钮的标签（"特写"、"近景"等）与实际值不对应

### 预设按钮标签问题

**修复前的预设：**
```typescript
const DISTANCE = [
  { v: 0, label: '特写' },      // 0 对应 ECU（极特写）
  { v: 2, label: '近景' },      // 2 对应 CU（特写）
  { v: 4, label: '中近' },      // 4 对应 MS（中景）
  { v: 6, label: '中景' },      // 6 对应 MFS（中全景）
  { v: 8, label: '中远' },      // 8 对应 LS（远景）
  { v: 10, label: '远景' },     // 10 对应 ELS（大远景）
  { v: 12, label: '大远景' },   // 12 超出映射范围！
];
```

**问题：**
- `v: 0` 标签是"特写"，但实际对应 `Extreme Close-Up (ECU)`（极特写）
- `v: 12` 超出了提示词映射的范围（最大 10）
- 标签与实际术语不匹配

## 解决方案

### 1. 统一范围到 0-10

**修复后：**
- 滑块范围：0 - 10（符合需求文档）
- 预设按钮：0, 1.5, 3, 5, 7, 9, 10（在 0-10 范围内）
- 提示词映射：0 - 10（已经正确）

### 2. 修正预设按钮标签

**修复后的预设：**
```typescript
const DISTANCE = [
  { v: 0, label: '极特写' },    // 0 → ECU (Extreme Close-Up)
  { v: 1.5, label: '特写' },    // 1.5 → CU (Close-Up)
  { v: 3, label: '近景' },      // 3 → MS (Medium Shot)
  { v: 5, label: '中景' },      // 5 → MFS (Medium Full Shot)
  { v: 7, label: '全身' },      // 7 → FS (Full Shot)
  { v: 9, label: '远景' },      // 9 → LS (Long Shot)
  { v: 10, label: '大远景' },   // 10 → ELS (Extreme Long Shot)
];
```

**对应关系：**
| 预设值 | 中文标签 | 英文术语 | 说明 |
|--------|---------|---------|------|
| 0 | 极特写 | Extreme Close-Up (ECU) | 局部特写 |
| 1.5 | 特写 | Close-Up (CU) | 头部特写 |
| 3 | 近景 | Medium Shot (MS) | 腰部以上 |
| 5 | 中景 | Medium Full Shot (MFS) | 膝盖以上 |
| 7 | 全身 | Full Shot (FS) | 全身 |
| 9 | 远景 | Long Shot (LS) | 远景 |
| 10 | 大远景 | Extreme Long Shot (ELS) | 大远景 |

### 3. 修正滑块步长

**修复前：**
- 步长：0.1（太细，不实用）

**修复后：**
- 步长：0.5（符合需求文档）

## 代码变更

### 文件：`components/MultiAngleCameraNode.tsx`

**第 38-46 行（预设按钮）：**
```typescript
// 修复前
const DISTANCE = [
  { v: 0, label: '特写' },
  { v: 2, label: '近景' },
  { v: 4, label: '中近' },
  { v: 6, label: '中景' },
  { v: 8, label: '中远' },
  { v: 10, label: '远景' },
  { v: 12, label: '大远景' },
];

// 修复后
const DISTANCE = [
  { v: 0, label: '极特写' },
  { v: 1.5, label: '特写' },
  { v: 3, label: '近景' },
  { v: 5, label: '中景' },
  { v: 7, label: '全身' },
  { v: 9, label: '远景' },
  { v: 10, label: '大远景' },
];
```

**第 987 行（滑块配置）：**
```typescript
// 修复前
{ l: '距离', v: cameraZoom, min: 0, max: 14, fn: onZoomChange, step: 0.1, unit: '' }

// 修复后
{ l: '距离', v: cameraZoom, min: 0, max: 10, fn: onZoomChange, step: 0.5, unit: '' }
```

## 修复效果

### 1. 现在可以选择特写了

**修复前：**
- 预设按钮最小值是 0，标签是"特写"
- 但实际对应的是 `Extreme Close-Up (ECU)`（极特写）
- 用户找不到真正的"特写"（`Close-Up (CU)`）

**修复后：**
- 预设按钮有 `v: 0, label: '极特写'` 和 `v: 1.5, label: '特写'`
- 用户可以明确选择"极特写"或"特写"
- 标签与实际术语一致

### 2. 滑块范围统一

**修复前：**
- 滑块可以到 14
- 但提示词映射只到 10
- 用户拖到 11-14 时，提示词会错误

**修复后：**
- 滑块最大值是 10
- 与提示词映射完全一致
- 不会出现超出范围的情况

### 3. 步长更合理

**修复前：**
- 步长 0.1，太细
- 用户需要拖很久才能从 0 到 10

**修复后：**
- 步长 0.5，符合需求文档
- 用户可以快速调整

## 测试建议

### 1. 测试预设按钮

- 点击"极特写"按钮，检查是否设置为 0
- 点击"特写"按钮，检查是否设置为 1.5
- 点击"中景"按钮，检查是否设置为 5
- 点击"大远景"按钮，检查是否设置为 10

### 2. 测试滑块

- 拖动滑块到最小值（0），检查是否显示"极特写"
- 拖动滑块到 1.5，检查是否显示"特写"
- 拖动滑块到最大值（10），检查是否显示"大远景"
- 确认滑块不能超过 10

### 3. 测试提示词生成

- 设置距离为 0，生成九宫格，检查提示词是否包含 `Extreme Close-Up (ECU)`
- 设置距离为 1.5，生成九宫格，检查提示词是否包含 `Close-Up (CU)`
- 设置距离为 5，生成九宫格，检查提示词是否包含 `Medium Full Shot (MFS)`
- 设置距离为 10，生成九宫格，检查提示词是否包含 `Extreme Long Shot (ELS)`

## 相关文档

- 需求文档：`.kiro/specs/camera-animation-preview/requirements.md`（第 2.2 节）
- 提示词映射：`App.tsx`（`getDistanceTerm` 函数）
- UI 组件：`components/MultiAngleCameraNode.tsx`

---

**修复完成！** 🎉

现在：
- ✅ 滑块范围：0 - 10（符合需求）
- ✅ 预设按钮：7 个档位，覆盖 0 - 10 范围
- ✅ 标签准确：与提示词术语一致
- ✅ 步长合理：0.5（符合需求）

**用户现在可以选择特写了！**
