# Group 拖动修复 - 第 6 步：删除重复函数定义 - 2026-01-28

## 问题描述

运行时错误：`Cannot access 'getApproxNodeHeight' before initialization` (第 344 行)

## 问题根源

**第 627-689 行有重复定义的函数**，与 `useNodeHelpers` Hook 返回的函数冲突：

```typescript
// ❌ 重复定义（第 627-657 行）
const getApproxNodeHeight = (node: AppNode) => {
    // ... 函数实现
};

// ❌ 重复定义（第 659-663 行）
const getNodeBounds = (node: AppNode) => {
    // ... 函数实现
};

// ❌ 重复定义（第 666-689 行）
const getNodeNameCN = useCallback((t: string) => {
    // ... 函数实现
}, []);

const getNodeIcon = useCallback((t: string) => {
    // ... 函数实现
}, []);
```

**这些函数已经在 `useNodeHelpers` Hook 中定义并返回了**（第 381-386 行）：

```typescript
// ✅ 正确的定义（来自 useNodeHelpers Hook）
const { 
  getApproxNodeHeight, 
  getNodeBounds, 
  getNodeNameCN, 
  getNodeIcon 
} = useNodeHelpers();
```

## 修复方案

**删除第 627-689 行的重复函数定义**

使用 PowerShell 精确删除：

```powershell
$lines = Get-Content App.tsx -Encoding UTF8
$newLines = $lines[0..625] + $lines[689..($lines.Count-1)]
$newLines | Set-Content App.tsx -Encoding UTF8
```

## 修复结果

✅ **编译成功**
```
vite v6.4.1 building for production...
✓ 1737 modules transformed.
✓ built in 2.37s
```

✅ **无编译错误**

✅ **代码结构正确**
- `useNodeHelpers` 在第 381-386 行调用
- `useViewport` 在第 342-346 行调用，使用 `getApproxNodeHeight`
- 没有重复定义的函数

## 架构原则验证

✅ **遵循三层架构**
- UI Layer (App.tsx)：只做组合和配置
- Hooks Layer (useNodeHelpers)：定义业务逻辑函数
- Core Layer：数据管理和纯计算

✅ **职责分离**
- `useNodeHelpers` Hook 负责定义节点相关的辅助函数
- App.tsx 只负责调用 Hook 并使用返回的函数
- 没有在 App.tsx 中重复定义业务逻辑

## 下一步

测试 Group 拖动功能是否正常：

1. 创建一个 Group
2. 拖动 Group
3. 验证节点是否跟随 Group 移动
4. 验证连接线是否实时更新

## 相关文件

- `App.tsx` - 删除了重复函数定义
- `hooks/useNodeHelpers.ts` - 定义了 `getApproxNodeHeight` 等函数
- `hooks/useViewport.ts` - 使用 `getApproxNodeHeight`
- `hooks/useGroup.ts` - 使用 `getApproxNodeHeight`

## 技术细节

**为什么会有重复定义？**

用户的同事恢复了旧版本 App.tsx，导致：
1. 旧版本的函数定义被恢复（第 627-689 行）
2. 新版本的 `useNodeHelpers` Hook 调用还在（第 381-386 行）
3. 两者冲突，导致运行时错误

**为什么是运行时错误而不是编译错误？**

因为 JavaScript 的变量提升（Hoisting）机制：
- `const` 声明会被提升到作用域顶部
- 但是初始化不会被提升
- 所以在初始化之前访问会报错：`Cannot access before initialization`

**正确的顺序应该是：**

```typescript
// 1. 调用 useNodeHelpers，获取函数
const { getApproxNodeHeight, ... } = useNodeHelpers();

// 2. 使用 getApproxNodeHeight
const { ... } = useViewport({
  getNodeHeight: getApproxNodeHeight,
});

// ❌ 不应该再重复定义
// const getApproxNodeHeight = (node: AppNode) => { ... };
```

## 总结

成功删除了 App.tsx 中的重复函数定义，修复了运行时错误。现在代码结构清晰，遵循三层架构原则，职责分离明确。
