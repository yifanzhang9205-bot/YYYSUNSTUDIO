# 3D相机内存占用分析和优化方案

**日期**: 2026-01-30  
**问题**: 为什么九宫格节点这么占内存？和其他节点有什么区别？

---

## 🔍 内存占用对比

### **典型节点内存占用**

| 节点类型 | 内存占用 | 主要内容 |
|---------|---------|---------|
| **剧本节点** | ~1-2 MB | React 组件 + 文本数据 + DOM 元素 |
| **角色参考节点** | ~2-5 MB | React 组件 + 图片（Base64/URL）+ DOM 元素 |
| **九宫格切割节点** | ~5-10 MB | React 组件 + Canvas 2D + 9 张图片 |
| **3D相机节点** | ~50-100 MB | React 组件 + **Three.js 3D 场景** + WebGL 上下文 |

### **为什么 3D 相机节点占用这么多内存？**

---

## 📊 3D 相机节点的内存构成

### **1. WebGL 上下文（最大头）**
```
每个 WebGL 上下文 ≈ 20-40 MB
```

**为什么这么大？**
- WebGL 需要在 GPU 上分配显存
- 包括：帧缓冲区、深度缓冲区、模板缓冲区
- 浏览器会为每个上下文预留内存

**对比**：
- Canvas 2D：只需要 CPU 内存，~1-2 MB
- WebGL：需要 GPU 显存 + CPU 内存，~20-40 MB

---

### **2. Three.js 场景对象（10-20 MB）**

#### **几何体（Geometry）**
```javascript
// 每个几何体占用内存
const floor = new THREE.PlaneGeometry(20, 20);           // ~1 KB
const grid = new THREE.GridHelper(16, 16);               // ~2 KB
const card = new THREE.BoxGeometry(2.2, 3, 0.05);        // ~1 KB
const camera = new THREE.BoxGeometry(0.35, 0.4, 0.6);    // ~1 KB
const lens = new THREE.CylinderGeometry(...);            // ~2 KB
const heightRing = new THREE.BufferGeometry();           // ~5 KB（65个点）
const verticalLine = new THREE.BufferGeometry();         // ~1 KB
const groundLine = new THREE.BufferGeometry();           // ~1 KB
// ... 更多辅助线、取景框等

总计：~20-30 KB（看起来不多，但在 GPU 上会放大）
```

**GPU 放大效应**：
- CPU 上 1 KB 的数据
- 上传到 GPU 后可能占用 10-100 KB（因为需要对齐、填充、缓存）

#### **材质（Material）**
```javascript
// 每个材质占用内存
const floorMat = new THREE.MeshStandardMaterial(...);    // ~5 KB
const cardMat = new THREE.MeshStandardMaterial(...);     // ~5 KB
const cameraMat = new THREE.MeshStandardMaterial(...);   // ~5 KB
const lineMat = new THREE.LineBasicMaterial(...);        // ~2 KB
// ... 更多材质

总计：~50-100 KB
```

#### **纹理（Texture）**
```javascript
// 如果加载了输入图片
const texture = new THREE.TextureLoader().load(inputImage);

// 纹理内存占用 = 宽度 × 高度 × 4（RGBA）
// 例如：1024×1024 图片 = 1024 × 1024 × 4 = 4 MB
// GPU 会自动生成 Mipmap（多级纹理），再 ×1.33 = 5.3 MB
```

**对比**：
- 普通节点显示图片：只需要 CPU 内存，~500 KB - 2 MB
- Three.js 纹理：需要 GPU 显存，~5-10 MB（包括 Mipmap）

#### **灯光（Light）**
```javascript
const ambient = new THREE.AmbientLight(...);             // ~1 KB
const mainLight = new THREE.DirectionalLight(...);       // ~2 KB（包括阴影贴图）
const fillLight = new THREE.DirectionalLight(...);       // ~2 KB

// 阴影贴图（Shadow Map）
mainLight.shadow.mapSize.set(1024, 1024);
// 阴影贴图内存 = 1024 × 1024 × 4 = 4 MB
```

---

### **3. 渲染器（Renderer）（10-20 MB）**

```javascript
const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setSize(width, height);
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.shadowMap.enabled = true;
```

**内存占用**：
- 颜色缓冲区：`width × height × 4` = 400×500×4 = 800 KB
- 深度缓冲区：`width × height × 4` = 800 KB
- 模板缓冲区：`width × height × 1` = 200 KB
- 抗锯齿（MSAA）：×4 = 8 MB
- 阴影贴图：1024×1024×4 = 4 MB

**总计**：~10-15 MB

---

### **4. 动画循环（Animation Loop）（持续占用 CPU）**

```javascript
const animate = () => {
  frameRef.current = requestAnimationFrame(animate);
  renderer.render(scene, camera);
};
animate();
```

**问题**：
- 即使不拖动，也在 60 FPS 渲染
- 每秒调用 `renderer.render()` 60 次
- 持续占用 CPU 和 GPU

**对比**：
- 普通节点：只在需要时重新渲染（React 的按需渲染）
- 3D 相机节点：持续渲染（60 FPS）

---

## 🆚 和其他节点的核心区别

### **1. 渲染方式**

| 节点类型 | 渲染方式 | 内存占用 | CPU 占用 |
|---------|---------|---------|---------|
| **普通节点** | React DOM | ~1-5 MB | 按需 |
| **Canvas 2D 节点** | Canvas 2D API | ~5-10 MB | 按需 |
| **3D 相机节点** | WebGL + Three.js | ~50-100 MB | 持续（60 FPS） |

### **2. GPU 使用**

| 节点类型 | GPU 使用 | 显存占用 |
|---------|---------|---------|
| **普通节点** | 无（CSS 渲染） | 0 MB |
| **Canvas 2D 节点** | 无（CPU 渲染） | 0 MB |
| **3D 相机节点** | 是（WebGL） | 20-40 MB |

### **3. 资源清理**

| 节点类型 | 清理难度 | 泄漏风险 |
|---------|---------|---------|
| **普通节点** | 简单（React 自动清理） | 低 |
| **Canvas 2D 节点** | 中等（需要手动清理 Canvas） | 中 |
| **3D 相机节点** | 困难（需要清理 Geometry、Material、Texture、Renderer、WebGL 上下文） | **高** |

---

## 💡 为什么 3D 相机节点需要这么多资源？

### **1. 实时 3D 渲染**
- 需要在 GPU 上计算光照、阴影、材质
- 需要实时更新相机位置、辅助线
- 需要 60 FPS 流畅渲染

### **2. 复杂的场景**
- 10+ 个几何体（地面、网格、卡片、相机、辅助线等）
- 10+ 个材质
- 3 个灯光（包括阴影）
- 动态辅助元素（高度圆环、垂直线、地面连线）

### **3. WebGL 的开销**
- WebGL 上下文本身就很重（20-40 MB）
- 每个 WebGL 上下文都是独立的（不能共享）
- 浏览器对 WebGL 上下文数量有限制（通常 8-16 个）

---

## 🚀 优化方案

### **方案 1：按需渲染（推荐，简单有效）**

**当前问题**：
```javascript
// ❌ 持续渲染（60 FPS）
const animate = () => {
  frameRef.current = requestAnimationFrame(animate);
  renderer.render(scene, camera);
};
animate();
```

**优化方案**：
```javascript
// ✅ 只在需要时渲染
const renderOnce = () => {
  if (sceneRef.current) {
    sceneRef.current.renderer.render(sceneRef.current.scene, sceneRef.current.camera);
  }
};

// 只在以下情况渲染：
// 1. 拖动时
// 2. 参数变化时
// 3. 窗口大小变化时
```

**预期效果**：
- CPU 占用：从 5-10% 降到 0%
- 内存占用：不变（~50-100 MB）
- 流畅度：不变（拖动时仍然 60 FPS）

**实施难度**：⭐（简单）

---

### **方案 2：降低渲染质量（中等效果）**

**当前设置**：
```javascript
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));  // 高清
renderer.shadowMap.enabled = true;                              // 阴影
renderer.shadowMap.type = THREE.PCFSoftShadowMap;              // 软阴影
mainLight.shadow.mapSize.set(1024, 1024);                      // 高分辨率阴影
```

**优化方案**：
```javascript
renderer.setPixelRatio(1);                                      // 标准清晰度
renderer.shadowMap.enabled = false;                             // 禁用阴影
// 或者
mainLight.shadow.mapSize.set(512, 512);                        // 降低阴影分辨率
```

**预期效果**：
- 内存占用：从 ~50-100 MB 降到 ~30-60 MB
- 视觉质量：略有下降（但可能不明显）

**实施难度**：⭐（简单）

---

### **方案 3：延迟初始化（中等效果）**

**当前问题**：
- 节点一创建就初始化 Three.js 场景
- 即使节点不可见也占用内存

**优化方案**：
```javascript
// ✅ 只在节点可见时初始化
const [isVisible, setIsVisible] = useState(false);

useEffect(() => {
  // 使用 IntersectionObserver 检测节点是否可见
  const observer = new IntersectionObserver((entries) => {
    setIsVisible(entries[0].isIntersecting);
  });
  observer.observe(containerRef.current);
  return () => observer.disconnect();
}, []);

useEffect(() => {
  if (!isVisible) return;
  // 初始化 Three.js 场景...
}, [isVisible]);
```

**预期效果**：
- 不可见的节点：0 MB
- 可见的节点：~50-100 MB

**实施难度**：⭐⭐（中等）

---

### **方案 4：共享 WebGL 上下文（高级，复杂）**

**当前问题**：
- 每个节点都创建独立的 WebGL 上下文
- 3 个节点 = 3 个 WebGL 上下文 = 60-120 MB

**优化方案**：
```javascript
// ✅ 多个节点共享同一个 WebGL 上下文
// 使用 OffscreenCanvas 或 共享 Renderer

// 1. 创建全局 Renderer
const globalRenderer = new THREE.WebGLRenderer({ canvas: sharedCanvas });

// 2. 每个节点使用独立的 Scene 和 Camera
const scene1 = new THREE.Scene();
const scene2 = new THREE.Scene();

// 3. 渲染到不同的区域
globalRenderer.setViewport(x1, y1, w1, h1);
globalRenderer.render(scene1, camera1);

globalRenderer.setViewport(x2, y2, w2, h2);
globalRenderer.render(scene2, camera2);
```

**预期效果**：
- 3 个节点：从 ~150-300 MB 降到 ~80-150 MB
- 节省 1-2 个 WebGL 上下文

**实施难度**：⭐⭐⭐⭐（困难，需要重构）

---

### **方案 5：使用 CSS 3D Transform 代替 Three.js（激进）**

**思路**：
- 用 CSS 3D Transform 实现相机预览
- 只在需要精确控制时才用 Three.js

**优点**：
- 内存占用：从 ~50-100 MB 降到 ~1-2 MB
- 不需要 WebGL 上下文

**缺点**：
- 功能受限（无法实现复杂的 3D 效果）
- 需要完全重写

**实施难度**：⭐⭐⭐⭐⭐（非常困难，不推荐）

---

## 📋 推荐的优化顺序

### **第 1 步：按需渲染（立即实施）**
- **效果**：CPU 占用降到 0%
- **难度**：⭐（简单）
- **风险**：低
- **预计时间**：30 分钟

### **第 2 步：降低渲染质量（可选）**
- **效果**：内存占用减少 30-40%
- **难度**：⭐（简单）
- **风险**：低（视觉质量略有下降）
- **预计时间**：15 分钟

### **第 3 步：延迟初始化（可选）**
- **效果**：不可见节点不占内存
- **难度**：⭐⭐（中等）
- **风险**：中（需要测试可见性检测）
- **预计时间**：1 小时

### **第 4 步：共享 WebGL 上下文（未来优化）**
- **效果**：内存占用减少 50%
- **难度**：⭐⭐⭐⭐（困难）
- **风险**：高（需要大量重构和测试）
- **预计时间**：1-2 天

---

## 🎯 预期效果对比

| 优化方案 | 内存占用 | CPU 占用 | 实施难度 | 推荐度 |
|---------|---------|---------|---------|--------|
| **当前** | ~100 MB/节点 | 5-10% | - | - |
| **方案 1（按需渲染）** | ~100 MB/节点 | 0% | ⭐ | ⭐⭐⭐⭐⭐ |
| **方案 2（降低质量）** | ~60 MB/节点 | 5-10% | ⭐ | ⭐⭐⭐⭐ |
| **方案 1+2** | ~60 MB/节点 | 0% | ⭐ | ⭐⭐⭐⭐⭐ |
| **方案 3（延迟初始化）** | 0 MB（不可见） | 0% | ⭐⭐ | ⭐⭐⭐ |
| **方案 4（共享上下文）** | ~50 MB/节点 | 0% | ⭐⭐⭐⭐ | ⭐⭐ |

---

## 💬 总结

### **为什么 3D 相机节点占内存？**
1. **WebGL 上下文**：每个节点 20-40 MB（最大头）
2. **Three.js 场景**：几何体、材质、纹理、灯光 = 10-20 MB
3. **渲染器**：帧缓冲区、阴影贴图 = 10-20 MB
4. **持续渲染**：60 FPS 持续占用 CPU

### **和其他节点的区别？**
- 普通节点：React DOM，~1-5 MB，按需渲染
- 3D 相机节点：WebGL + Three.js，~50-100 MB，持续渲染

### **最佳优化方案？**
1. **立即实施**：按需渲染（CPU 占用降到 0%）
2. **可选实施**：降低渲染质量（内存减少 30-40%）
3. **未来优化**：延迟初始化、共享 WebGL 上下文

### **能支持多少个节点？**
- **当前**：3-5 个节点（~300-500 MB）
- **优化后**：10-15 个节点（~600-900 MB）

---

**下一步**：你想先实施哪个优化方案？我推荐从"按需渲染"开始，简单有效！
