# 3D 相机智能发散系统

## 实施时间
2026-01-24

## 问题背景

### 用户反馈
> "在强化约束下景别，尤其是景别，我要大远景他基本就给我个原图，是因为他自己画不出来周边吗？还是因为环境约束太强了？"

### 核心问题

**矛盾 1：图生图的本质限制**
- 输入图片：角色特写/中景（背景信息少）
- 要求输出：大远景（需要大量环境信息）
- AI 困境：输入图片没有环境，但被要求"环境不能变"

**矛盾 2：约束过强导致 AI 不敢改变**
```
当前约束：
- Character: LOCKED
- Outfit: LOCKED
- Lighting & Environment: LOCKED  ← 这个太强了
- Distance: LOCKED at Extreme Long Shot

AI 的想法：
- 我要生成大远景（需要添加环境）
- 但环境被 LOCKED（不能添加环境）
- 矛盾！算了，我保持原图最安全
```

## 解决方案：智能发散系统

### 核心思路

**"控图发散"策略：**
- 识别需要发散的情况（距离、角度、高度超出输入图片范围）
- 给 AI 明确的发散权限（可以添加/推测什么）
- 同时保持严格约束（不能改变什么）
- **关键：在整个图片的画风之下发散，不能改变画风和现有内容**

### 四种需要发散的情况

#### 情况 1：距离变远（需要扩展环境）

**触发条件：**
```typescript
const needEnvironmentExpansion = zoom >= 7; // 远景或大远景
```

**发散权限：**
- ✅ 可以添加背景/环境
- ✅ 可以缩小角色比例
- ✅ 可以添加符合风格的环境元素

**严格约束：**
- ❌ 角色身份、服装必须完全一致
- ❌ 整体画风、艺术风格必须一致
- ❌ 光照氛围、色调必须一致

**发散指导（添加到提示词）：**
```
[ENVIRONMENT EXPANSION - Long Shot Handling]
Since this is a Extreme Long Shot, you may need to extend the environment:
✅ ALLOWED: Add appropriate background/environment around the character
✅ ALLOWED: Scale down the character proportionally to show more surroundings
✅ ALLOWED: Add contextual elements that match the input image's style
❌ LOCKED: Character identity, outfit, and overall art style must remain identical
❌ LOCKED: Lighting mood and color palette must stay consistent

The character should become smaller in frame, surrounded by appropriate environment 
that matches the style of the input image.
```

#### 情况 2：距离变近（需要裁切聚焦）

**触发条件：**
```typescript
const needCloseUpFocus = zoom <= 2; // 特写或极特写
```

**发散权限：**
- ✅ 可以裁切到脸部/上半身
- ✅ 可以增强面部细节
- ✅ 可以显示更多服装细节

**严格约束：**
- ❌ 角色特征、服装设计必须一致
- ❌ 不能改变面部结构、表情
- ❌ 不能改变服装颜色/图案

**发散指导：**
```
[CLOSE-UP FOCUS - Detail Enhancement]
Since this is a Extreme Close-Up, you may need to focus closer:
✅ ALLOWED: Crop closer to the character's face/upper body
✅ ALLOWED: Enhance facial details and textures
✅ ALLOWED: Show more detail in clothing and accessories
❌ LOCKED: Character features, outfit design, and style must remain identical
❌ LOCKED: Do not change facial structure, expression, or outfit colors

Focus on the character's upper body/face while maintaining perfect consistency.
```

#### 情况 3：极端俯视/仰视（需要补充看不见的部分）

**触发条件：**
```typescript
const needVerticalInference = vAngle >= 50 || vAngle <= -20;
```

**发散权限：**
- ✅ 可以推测生成头顶/脚底
- ✅ 可以推测身体其他角度
- ✅ 可以调整可见部分以匹配角度

**严格约束：**
- ❌ 角色身份、服装细节必须一致
- ❌ 整体风格必须一致
- ❌ 只改变视角，不改变角色本身

**发散指导：**
```
[VERTICAL ANGLE INFERENCE - Top-Down View]
Since this is an extreme vertical angle (Top-Down View), you may need to infer unseen parts:
✅ ALLOWED: Infer and generate the top/bottom view of the character based on visible parts
✅ ALLOWED: Show the character from this extreme angle naturally
✅ ALLOWED: Adjust visible body parts to match the camera angle
❌ LOCKED: Character identity, outfit details, and style must remain identical
❌ LOCKED: Do not change the character's appearance, only the viewing angle

The new angle should look natural and consistent with the visible parts of the input image.
```

#### 情况 4：背面/侧面（需要推测看不见的角度）

**触发条件：**
```typescript
const normalized = ((hAngle % 360) + 360) % 360;
const needAngleInference = (normalized >= 78.75 && normalized <= 281.25); // 侧面到背面
```

**发散权限：**
- ✅ 可以推测背面/侧面外观
- ✅ 可以推测发型背面
- ✅ 可以从不同角度显示服装

**严格约束：**
- ❌ 服装颜色、材质、图案必须一致
- ❌ 角色应该看起来是同一个人
- ❌ 整体风格必须一致

**发散指导：**
```
[HORIZONTAL ANGLE INFERENCE - side view from right]
Since this requires a side/back view, you may need to infer unseen angles:
✅ ALLOWED: Infer the back/side appearance of the character based on the front view
✅ ALLOWED: Generate hairstyle back/side view that matches the front
✅ ALLOWED: Show outfit from different angles while maintaining design consistency
❌ LOCKED: Outfit colors, textures, patterns, and overall style must remain identical
❌ LOCKED: Character should look like the same person from a different angle

The character should appear as the same person viewed from side view from right.
```

## 代码实施

### 1. 智能判断系统

```typescript
// === 智能发散系统：判断需要发散的情况 ===

// 情况 1：距离变远（需要扩展环境）
const needEnvironmentExpansion = zoom >= 7; // 远景或大远景

// 情况 2：距离变近（需要裁切聚焦）
const needCloseUpFocus = zoom <= 2; // 特写或极特写

// 情况 3：极端俯视/仰视（需要补充看不见的部分）
const needVerticalInference = vAngle >= 50 || vAngle <= -20;

// 情况 4：背面/侧面（需要推测看不见的角度）
const normalized = ((hAngle % 360) + 360) % 360;
const needAngleInference = (normalized >= 78.75 && normalized <= 281.25); // 侧面到背面范围
```

### 2. 发散指导生成函数

```typescript
const getDivergenceGuidance = (): string => {
    const guidelines: string[] = [];
    
    if (needEnvironmentExpansion) {
        guidelines.push(`[ENVIRONMENT EXPANSION - Long Shot Handling]...`);
    }
    
    if (needCloseUpFocus) {
        guidelines.push(`[CLOSE-UP FOCUS - Detail Enhancement]...`);
    }
    
    if (needVerticalInference) {
        guidelines.push(`[VERTICAL ANGLE INFERENCE - ${baseHeight}]...`);
    }
    
    if (needAngleInference) {
        guidelines.push(`[HORIZONTAL ANGLE INFERENCE - ${baseAngleDesc}]...`);
    }
    
    return guidelines.length > 0 ? guidelines.join('\n') : '';
};

const divergenceGuidance = getDivergenceGuidance();
```

### 3. 提示词集成

```typescript
let geminiPrompt = `...

[CRITICAL CONSTRAINT: IMG2IMG FEATURE LOCKING]
Based strictly on the INPUT IMAGE, maintain perfect consistency across all 9 panels:
- Character Identity: LOCKED. EXACT same face, features, and expression.
- Outfit & Accessories: LOCKED. EXACT same clothing, colors, and textures.
- Lighting Mood & Art Style: LOCKED. Consistent lighting direction, color palette, and artistic style.
- Camera Distance: LOCKED at ${baseDistance}
- Camera Vertical Position: LOCKED - ${heightDescription}
- The ONLY variable: camera horizontal angle variations around ${baseAngleDesc}

${divergenceGuidance ? divergenceGuidance : ''}

...
`;
```

**关键改进：**
- 将 `Lighting & Environment: LOCKED` 改为 `Lighting Mood & Art Style: LOCKED`
- 允许添加环境，但光照氛围和艺术风格必须一致
- 只在需要时添加发散指导（动态生成）

## 测试场景

### 场景 1：大远景 + 正面平视

**UI 设置：**
- horizontalAngle: 0°（正面）
- verticalAngle: 0°（平视）
- cameraZoom: 10（大远景）

**触发的发散：**
- ✅ 情况 1：距离变远（需要扩展环境）

**预期效果：**
- 角色变小，占画面 10-20%
- 周围添加合适的环境（街道、房间、自然景观等）
- 环境风格与输入图片一致
- 角色本身完全一致

**验收标准：**
- ✅ 能看到完整的角色和周围环境
- ✅ 环境风格与输入图片匹配
- ✅ 角色特征、服装完全一致
- ✅ 不再是"原图不变"

### 场景 2：极特写 + 正面平视

**UI 设置：**
- horizontalAngle: 0°（正面）
- verticalAngle: 0°（平视）
- cameraZoom: 0（极特写）

**触发的发散：**
- ✅ 情况 2：距离变近（需要裁切聚焦）

**预期效果：**
- 裁切到脸部/上半身
- 面部细节增强
- 服装细节更清晰

**验收标准：**
- ✅ 聚焦到脸部
- ✅ 面部特征完全一致
- ✅ 细节更丰富

### 场景 3：大远景 + 正上方俯视

**UI 设置：**
- horizontalAngle: 0°（正面）
- verticalAngle: 60°（正上方）
- cameraZoom: 10（大远景）

**触发的发散：**
- ✅ 情况 1：距离变远（需要扩展环境）
- ✅ 情况 3：极端俯视（需要补充看不见的部分）

**预期效果：**
- 从正上方俯视角色
- 角色变小，周围有环境
- 能看到头顶、肩膀、身体

**验收标准：**
- ✅ 俯视角度正确
- ✅ 有环境扩展
- ✅ 角色一致性保持

### 场景 4：中景 + 背面

**UI 设置：**
- horizontalAngle: 180°（背面）
- verticalAngle: 0°（平视）
- cameraZoom: 5（中景）

**触发的发散：**
- ✅ 情况 4：背面（需要推测看不见的角度）

**预期效果：**
- 显示角色背面
- 发型背面合理
- 服装背面与正面风格一致

**验收标准：**
- ✅ 背面视角正确
- ✅ 服装颜色/图案一致
- ✅ 看起来是同一个角色

## 关键改进点

### 1. 从"完全锁定"到"智能发散"

**改进前：**
```
- Lighting & Environment: LOCKED
```
- 问题：环境被完全锁定，AI 无法添加环境
- 结果：大远景时 AI 不敢改变，保持原图

**改进后：**
```
- Lighting Mood & Art Style: LOCKED

[ENVIRONMENT EXPANSION]
✅ ALLOWED: Add appropriate background/environment
❌ LOCKED: Lighting mood and color palette must stay consistent
```
- 改进：允许添加环境，但光照和风格必须一致
- 结果：AI 可以生成大远景，同时保持风格一致

### 2. 明确的发散权限和约束

**每个发散指导都包含：**
- ✅ ALLOWED：明确告诉 AI 可以做什么
- ❌ LOCKED：明确告诉 AI 不能改变什么

**优势：**
- AI 知道边界在哪里
- 不会过度发散（改变画风）
- 不会过度保守（完全不变）

### 3. 动态生成发散指导

**只在需要时添加：**
- 正面平视中景：不添加任何发散指导（不需要）
- 大远景：添加环境扩展指导
- 俯视：添加垂直角度推测指导
- 背面：添加水平角度推测指导

**优势：**
- 提示词更精简（不需要时不添加）
- 更有针对性（只添加相关的指导）
- 减少 AI 混淆

### 4. 保持画风和现有内容

**核心原则：**
> "可以发散，但要在整个图片的画风之下发散，不能改变画风和现有内容"

**实施方式：**
- 允许添加环境，但必须"match the style of the input image"
- 允许推测角度，但必须"maintain design consistency"
- 允许增强细节，但必须"remain identical"

## 预期效果

### 大远景不再是原图

**改进前：**
- 输入：角色中景照
- 要求：大远景
- 输出：原图不变（因为环境被锁定）

**改进后：**
- 输入：角色中景照
- 要求：大远景
- 输出：角色变小，周围有合适的环境，风格一致

### 极端角度可以生成

**改进前：**
- 输入：正面照
- 要求：俯视 60°
- 输出：9 张正面照（因为 AI 不敢推测）

**改进后：**
- 输入：正面照
- 要求：俯视 60°
- 输出：9 张俯视照，能看到头顶

### 背面/侧面可以推测

**改进前：**
- 输入：正面照
- 要求：背面
- 输出：可能失败或不准确

**改进后：**
- 输入：正面照
- 要求：背面
- 输出：合理的背面视角，服装风格一致

## 文件变更

### 修改的文件
- `App.tsx`：提示词生成逻辑（第 1710-1850 行）

### 新增的文档
- `3D相机智能发散系统.md`：本文档

### 参考的文档
- `3D相机提示词-高度约束强化.md`：高度约束强化
- `3D相机提示词-角度发散方案实施.md`：角度发散方案

---

**智能发散系统实施完成！** 🎉

现在的系统：
- ✅ 自动识别 4 种需要发散的情况
- ✅ 动态生成针对性的发散指导
- ✅ 明确的发散权限（✅ ALLOWED）
- ✅ 严格的约束边界（❌ LOCKED）
- ✅ 保持画风和现有内容一致

**下一步：测试各种场景，验证发散效果！**

**重点测试：**
1. 大远景 + 正面平视（环境扩展）
2. 极特写 + 正面平视（裁切聚焦）
3. 大远景 + 正上方俯视（环境扩展 + 垂直推测）
4. 中景 + 背面（水平角度推测）
