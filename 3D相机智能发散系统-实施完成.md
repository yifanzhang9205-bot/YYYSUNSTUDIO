# 3D 相机智能发散系统 - 实施完成

## 实施时间
2026-01-24

## 问题回顾

用户反馈：
> "在强化约束下景别，尤其是景别，我要大远景他基本就给我个原图，是因为他自己画不出来周边吗？还是因为环境约束太强了？"

**核心问题：**
- 输入图片是特写/中景（背景信息少）
- 要求生成大远景（需要大量环境）
- 但提示词中 `Lighting & Environment: LOCKED` 太强
- AI 无法添加环境，只能保持原图

## 解决方案：智能发散系统

### 核心思路

**"控图发散"策略：**
1. 自动识别需要发散的情况
2. 给 AI 明确的发散权限（✅ ALLOWED）
3. 同时保持严格约束（❌ LOCKED）
4. **关键：在整个图片的画风之下发散，不能改变画风和现有内容**

### 四种发散情况

| 情况 | 触发条件 | 发散权限 | 严格约束 |
|------|---------|---------|---------|
| **1. 距离变远** | zoom >= 7 | 添加环境、缩小角色 | 角色、画风、光照一致 |
| **2. 距离变近** | zoom <= 2 | 裁切聚焦、增强细节 | 角色特征、服装一致 |
| **3. 极端俯视/仰视** | vAngle >= 50 或 <= -20 | 推测头顶/脚底 | 角色身份、服装一致 |
| **4. 背面/侧面** | hAngle 78.75°-281.25° | 推测背面/侧面 | 服装颜色/图案一致 |

## 代码实施

### 1. 智能判断逻辑

```typescript
// === 智能发散系统：判断需要发散的情况 ===

// 情况 1：距离变远（需要扩展环境）
const needEnvironmentExpansion = zoom >= 7; // 远景或大远景

// 情况 2：距离变近（需要裁切聚焦）
const needCloseUpFocus = zoom <= 2; // 特写或极特写

// 情况 3：极端俯视/仰视（需要补充看不见的部分）
const needVerticalInference = vAngle >= 50 || vAngle <= -20;

// 情况 4：背面/侧面（需要推测看不见的角度）
const normalized = ((hAngle % 360) + 360) % 360;
const needAngleInference = (normalized >= 78.75 && normalized <= 281.25);
```

### 2. 动态生成发散指导

```typescript
const getDivergenceGuidance = (): string => {
    const guidelines: string[] = [];
    
    if (needEnvironmentExpansion) {
        guidelines.push(`[ENVIRONMENT EXPANSION - Long Shot Handling]...`);
    }
    
    if (needCloseUpFocus) {
        guidelines.push(`[CLOSE-UP FOCUS - Detail Enhancement]...`);
    }
    
    if (needVerticalInference) {
        guidelines.push(`[VERTICAL ANGLE INFERENCE - ${baseHeight}]...`);
    }
    
    if (needAngleInference) {
        guidelines.push(`[HORIZONTAL ANGLE INFERENCE - ${baseAngleDesc}]...`);
    }
    
    return guidelines.length > 0 ? guidelines.join('\n') : '';
};

const divergenceGuidance = getDivergenceGuidance();
```

### 3. 提示词改进

**改进前：**
```
[CRITICAL CONSTRAINT: IMG2IMG FEATURE LOCKING]
- Lighting & Environment: LOCKED. Consistent lighting direction and mood.
```

**改进后：**
```
[CRITICAL CONSTRAINT: IMG2IMG FEATURE LOCKING]
- Lighting Mood & Art Style: LOCKED. Consistent lighting direction, color palette, and artistic style.

${divergenceGuidance ? divergenceGuidance : ''}
```

**关键变化：**
- `Lighting & Environment: LOCKED` → `Lighting Mood & Art Style: LOCKED`
- 允许添加环境，但光照氛围和艺术风格必须一致
- 动态添加发散指导（只在需要时）

## 发散指导示例

### 情况 1：大远景（zoom = 10）

```
[ENVIRONMENT EXPANSION - Long Shot Handling]
Since this is a Extreme Long Shot (ELS), you may need to extend the environment:
✅ ALLOWED: Add appropriate background/environment around the character
✅ ALLOWED: Scale down the character proportionally to show more surroundings
✅ ALLOWED: Add contextual elements that match the input image's style
❌ LOCKED: Character identity, outfit, and overall art style must remain identical
❌ LOCKED: Lighting mood and color palette must stay consistent

The character should become smaller in frame, surrounded by appropriate environment 
that matches the style of the input image.
```

### 情况 3：正上方俯视（vAngle = 60°）

```
[VERTICAL ANGLE INFERENCE - Top-Down View]
Since this is an extreme vertical angle (Top-Down View), you may need to infer unseen parts:
✅ ALLOWED: Infer and generate the top/bottom view of the character based on visible parts
✅ ALLOWED: Show the character from this extreme angle naturally
✅ ALLOWED: Adjust visible body parts to match the camera angle
❌ LOCKED: Character identity, outfit details, and style must remain identical
❌ LOCKED: Do not change the character's appearance, only the viewing angle

The new angle should look natural and consistent with the visible parts of the input image.
```

### 组合情况：大远景 + 正上方俯视

**触发：**
- ✅ 情况 1：距离变远
- ✅ 情况 3：极端俯视

**生成的发散指导：**
```
[ENVIRONMENT EXPANSION - Long Shot Handling]
...

[VERTICAL ANGLE INFERENCE - Top-Down View]
...
```

**效果：**
- AI 知道可以添加环境（因为是大远景）
- AI 知道可以推测头顶（因为是俯视）
- 同时保持角色、画风、光照一致

## 测试场景

### 场景 1：大远景 + 正面平视 ⭐

**UI 设置：**
- horizontalAngle: 0°
- verticalAngle: 0°
- cameraZoom: 10

**触发发散：**
- ✅ 情况 1：环境扩展

**预期效果：**
- 角色变小（占画面 10-20%）
- 周围有合适的环境
- 环境风格与输入图片一致
- 角色本身完全一致

**验收标准：**
- ✅ 不再是"原图不变"
- ✅ 有明显的环境扩展
- ✅ 画风和光照一致

### 场景 2：极特写 + 正面平视

**UI 设置：**
- horizontalAngle: 0°
- verticalAngle: 0°
- cameraZoom: 0

**触发发散：**
- ✅ 情况 2：裁切聚焦

**预期效果：**
- 裁切到脸部
- 面部细节增强

### 场景 3：大远景 + 正上方俯视 ⭐⭐

**UI 设置：**
- horizontalAngle: 0°
- verticalAngle: 60°
- cameraZoom: 10

**触发发散：**
- ✅ 情况 1：环境扩展
- ✅ 情况 3：垂直推测

**预期效果：**
- 从正上方俯视
- 角色变小，有环境
- 能看到头顶

**验收标准：**
- ✅ 俯视角度正确（不是正面）
- ✅ 有环境扩展
- ✅ 角色一致性保持

### 场景 4：中景 + 背面

**UI 设置：**
- horizontalAngle: 180°
- verticalAngle: 0°
- cameraZoom: 5

**触发发散：**
- ✅ 情况 4：水平角度推测

**预期效果：**
- 显示角色背面
- 服装风格一致

## 关键改进点

### 1. 从"完全锁定"到"智能发散"

| 维度 | 改进前 | 改进后 |
|------|--------|--------|
| 环境 | LOCKED（不能变） | 允许添加，但风格必须一致 |
| 距离 | LOCKED | 允许扩展/裁切，但角色一致 |
| 角度 | LOCKED | 允许推测，但特征一致 |

### 2. 明确的权限和约束

**每个发散指导都包含：**
- ✅ ALLOWED：明确可以做什么
- ❌ LOCKED：明确不能改变什么

**优势：**
- AI 知道边界
- 不会过度发散（改变画风）
- 不会过度保守（完全不变）

### 3. 动态生成（按需添加）

**不需要发散时：**
- 正面平视中景：不添加任何发散指导
- 提示词更简洁

**需要发散时：**
- 大远景：添加环境扩展指导
- 俯视：添加垂直推测指导
- 背面：添加水平推测指导

### 4. 保持画风和现有内容

**核心原则：**
> "可以发散，但要在整个图片的画风之下发散，不能改变画风和现有内容"

**实施方式：**
- "match the input image's style"
- "maintain design consistency"
- "remain identical"

## 预期效果对比

### 大远景

| 改进前 | 改进后 |
|--------|--------|
| 原图不变（因为环境被锁定） | 角色变小，周围有环境，风格一致 |

### 极端俯视

| 改进前 | 改进后 |
|--------|--------|
| 9 张正面照（AI 不敢推测） | 9 张俯视照，能看到头顶 |

### 背面视角

| 改进前 | 改进后 |
|--------|--------|
| 可能失败或不准确 | 合理的背面视角，服装风格一致 |

## 文件变更

### 修改的文件
- `App.tsx`：提示词生成逻辑（第 1710-1850 行）
  - 添加智能判断逻辑（4 种情况）
  - 添加发散指导生成函数
  - 修改提示词约束（环境 → 光照氛围和艺术风格）
  - 动态插入发散指导

### 新增的文档
- `3D相机智能发散系统.md`：详细设计文档
- `3D相机智能发散系统-实施完成.md`：本文档

### 参考的文档
- `3D相机提示词-高度约束强化.md`：高度约束强化
- `3D相机提示词-角度发散方案实施.md`：角度发散方案

---

**智能发散系统实施完成！** 🎉

## 实施总结

### 已完成
- ✅ 4 种发散情况的智能判断
- ✅ 动态生成发散指导
- ✅ 明确的发散权限（✅ ALLOWED）
- ✅ 严格的约束边界（❌ LOCKED）
- ✅ 保持画风和现有内容一致

### 核心改进
1. **环境约束放宽**：从 `Environment: LOCKED` 改为 `Art Style: LOCKED`
2. **智能判断**：自动识别需要发散的情况
3. **动态指导**：只在需要时添加发散指导
4. **明确边界**：清晰的 ALLOWED 和 LOCKED

### 下一步
**测试验证：**
1. ⭐ 大远景 + 正面平视（环境扩展）
2. 极特写 + 正面平视（裁切聚焦）
3. ⭐⭐ 大远景 + 正上方俯视（环境扩展 + 垂直推测）
4. 中景 + 背面（水平角度推测）

**重点关注：**
- 大远景是否不再是"原图不变"
- 环境扩展是否保持画风一致
- 极端角度是否能正确生成
- 角色一致性是否保持

**如果测试成功：**
- ✅ 大远景问题解决
- ✅ 极端角度问题解决
- ✅ 背面/侧面推测改善

**如果测试失败：**
- 根据具体情况调整发散指导的措辞
- 可能需要更强调某些约束
- 可能需要调整触发条件的阈值
