# 资产库"更多"菜单位置修复

> 日期：2026-02-09  
> 状态：✅ 已修复

---

## 🎯 问题描述

**现象：**
- 点击资产卡片右上角的"更多"按钮（三个竖点）
- 弹出的菜单位置不对，可能超出屏幕
- 无法正常操作（点击删除/重命名）

**用户反馈：**
> "点完显示出的位置不对，都没办法操作"

---

## ✅ 修复方案

### 核心思路

**让菜单像下拉框一样，紧贴在"更多"按钮下方，并智能避免超出屏幕。**

### 定位策略

1. **默认位置**：按钮右下方（右对齐）
   ```
   ┌─────────────┐
   │  资产卡片    │
   │         [⋮] │ ← 更多按钮
   └─────────────┘
           ┌──────────┐
           │ 重命名   │ ← 菜单（右对齐）
           │ 删除     │
           └──────────┘
   ```

2. **边界检测**：
   - 右边界：如果超出，改为左对齐
   - 下边界：如果超出，显示在按钮上方
   - 左边界：如果超出，距离左边10px
   - 上边界：如果超出，距离上边10px

---

## 📝 修改内容

### 修改：`handleMoreClick` 函数

**修改前（❌ 问题）：**
```typescript
const handleMoreClick = (e: React.MouseEvent, asset: Asset) => {
  e.stopPropagation();
  
  const button = e.currentTarget as HTMLElement;
  const rect = button.getBoundingClientRect();
  
  // ❌ 默认左对齐，不够直观
  let menuX = rect.left;
  let menuY = rect.bottom + 4;
  
  // ❌ 菜单高度估算不准确
  const menuWidth = 140;
  const menuHeight = 100;
  
  // 边界检测...
  
  setContextMenu({
    assetId: asset.id,
    x: menuX,
    y: menuY,
  });
};
```

**修改后（✅ 正确）：**
```typescript
const handleMoreClick = (e: React.MouseEvent, asset: Asset) => {
  e.stopPropagation();
  
  const button = e.currentTarget as HTMLElement;
  const rect = button.getBoundingClientRect();
  
  // 菜单尺寸（固定）
  const menuWidth = 140;
  const menuHeight = 88; // ✅ 2个选项，每个44px
  
  // ✅ 默认：在按钮右下方（像下拉框）
  let menuX = rect.right - menuWidth; // ✅ 右对齐
  let menuY = rect.bottom + 4; // 按钮下方4px
  
  // ✅ 检查右边界（如果超出，改为左对齐）
  if (menuX + menuWidth > window.innerWidth - 10) {
    menuX = rect.left;
  }
  
  // 检查左边界
  if (menuX < 10) {
    menuX = 10;
  }
  
  // ✅ 检查下边界（如果超出，显示在按钮上方）
  if (menuY + menuHeight > window.innerHeight - 10) {
    menuY = rect.top - menuHeight - 4;
  }
  
  // 检查上边界
  if (menuY < 10) {
    menuY = 10;
  }
  
  setContextMenu({
    assetId: asset.id,
    x: menuX,
    y: menuY,
  });
};
```

**关键改动：**
1. ✅ 默认右对齐（`rect.right - menuWidth`）
2. ✅ 菜单高度精确计算（88px = 2个选项 × 44px）
3. ✅ 边界检测更准确（留10px边距）
4. ✅ 超出下边界时，显示在按钮上方

---

### 删除：未使用的 `handleContextMenu` 函数

**删除前：**
```typescript
// 处理右键菜单
const handleContextMenu = (e: React.MouseEvent, asset: Asset) => {
  e.preventDefault();
  // ... 40行代码
};

// 关闭右键菜单
const closeContextMenu = () => {
  setContextMenu(null);
};
```

**删除后：**
```typescript
// 关闭右键菜单
const closeContextMenu = () => {
  setContextMenu(null);
};
```

**原因：**
- 右键菜单功能已经改为点击"更多"按钮
- `handleContextMenu` 函数不再使用
- 删除未使用的代码，保持代码整洁

---

## 🎨 视觉效果

### 正常情况（按钮右下方）

```
┌─────────────────────┐
│  资产卡片            │
│                 [⋮] │ ← 更多按钮
└─────────────────────┘
                ┌──────────┐
                │ 重命名   │ ← 菜单（右对齐）
                │ 删除     │
                └──────────┘
```

### 靠近右边界（改为左对齐）

```
                ┌─────────────────────┐
                │  资产卡片            │
                │ [⋮]                 │ ← 更多按钮
                └─────────────────────┘
                ┌──────────┐
                │ 重命名   │ ← 菜单（左对齐）
                │ 删除     │
                └──────────┘
```

### 靠近下边界（显示在上方）

```
                ┌──────────┐
                │ 重命名   │ ← 菜单（上方）
                │ 删除     │
                └──────────┘
┌─────────────────────┐
│  资产卡片            │
│                 [⋮] │ ← 更多按钮
└─────────────────────┘
```

---

## 🧪 测试场景

### 场景 1：正常位置
1. 点击资产卡片右上角的"更多"按钮
2. **验证**：菜单出现在按钮右下方
3. **验证**：菜单右对齐
4. **验证**：可以正常点击"重命名"和"删除"

### 场景 2：靠近右边界
1. 将资产库面板拖到屏幕右侧
2. 点击最右侧资产的"更多"按钮
3. **验证**：菜单改为左对齐（不超出屏幕）
4. **验证**：可以正常操作

### 场景 3：靠近下边界
1. 滚动到资产列表底部
2. 点击最下方资产的"更多"按钮
3. **验证**：菜单显示在按钮上方（不超出屏幕）
4. **验证**：可以正常操作

### 场景 4：点击遮罩层关闭
1. 点击"更多"按钮打开菜单
2. 点击菜单外的任意位置
3. **验证**：菜单关闭

### 场景 5：点击菜单项
1. 点击"更多"按钮打开菜单
2. 点击"重命名"
3. **验证**：弹出输入框
4. **验证**：菜单关闭

---

## 📊 修复前后对比

### 修复前（❌ 问题）

```
问题 1：菜单位置不对
- 默认左对齐，不够直观
- 可能超出屏幕右边界
- 可能超出屏幕下边界

问题 2：无法操作
- 菜单位置不对，点不到
- 菜单可能在视野外

问题 3：代码冗余
- 有未使用的 handleContextMenu 函数
- 代码不整洁
```

### 修复后（✅ 正确）

```
✅ 菜单位置正确
- 默认右对齐，像下拉框
- 智能避免超出屏幕
- 始终在可见范围内

✅ 可以正常操作
- 菜单位置准确
- 可以点击所有选项

✅ 代码整洁
- 删除未使用的函数
- 逻辑清晰
```

---

## 🎯 技术细节

### 菜单尺寸计算

```typescript
// 菜单固定尺寸
const menuWidth = 140;  // 宽度
const menuHeight = 88;  // 高度 = 2个选项 × 44px

// 每个选项的高度
// px-4 py-2 = padding: 8px 16px
// text-sm = 14px
// 总高度 ≈ 44px
```

### 定位算法

```typescript
// 1. 默认位置（右下方）
let menuX = rect.right - menuWidth;  // 右对齐
let menuY = rect.bottom + 4;         // 下方4px

// 2. 边界检测（优先级：右 > 左 > 下 > 上）
if (menuX + menuWidth > window.innerWidth - 10) {
  menuX = rect.left;  // 改为左对齐
}

if (menuX < 10) {
  menuX = 10;  // 最小距离左边10px
}

if (menuY + menuHeight > window.innerHeight - 10) {
  menuY = rect.top - menuHeight - 4;  // 改为上方
}

if (menuY < 10) {
  menuY = 10;  // 最小距离上边10px
}
```

### 遮罩层

```typescript
{/* 遮罩层 - 点击关闭菜单 */}
<div
  className="fixed inset-0 z-[9998]"
  onClick={closeContextMenu}
/>

{/* 菜单 - 在遮罩层上方 */}
<div
  className="fixed z-[9999] ..."
  style={{
    left: `${contextMenu.x}px`,
    top: `${contextMenu.y}px`,
  }}
>
  {/* 菜单内容 */}
</div>
```

---

## 🚀 下一步

**"更多"菜单位置已修复！✅**

现在菜单会像下拉框一样，紧贴在按钮下方，并智能避免超出屏幕。

---

**修复完成！现在可以正常使用删除和重命名功能了。** 🎉
