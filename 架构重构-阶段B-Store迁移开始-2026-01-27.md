# 架构重构 - 阶段 B - Store 迁移开始 - 2026-01-27

> 🎯 **目标**：将 App.tsx 的 useState 迁移到 Zustand Store（requirements.md 第 2 步）

---

## 📋 迁移计划

### 核心目标（来自 requirements.md）
**让以后加功能时，不再碰 App.tsx**

### 实施路径
1. ✅ **第 1 步**：修复致命 Bug（100%）
2. 🔄 **第 2 步**：抽离底层逻辑（Store + Registry）← **当前阶段**
3. ⏳ **第 3 步**：抽离交互逻辑（Hooks）

---

## 🎯 本次迁移内容

### 已完成的工作

#### 1. 引入 Store
```typescript
// 引入 Stores（架构重构 - 阶段 B - 第 2 步）
import { useNodeStore } from './core/stores/nodeStore';
import { useConnectionStore } from './core/stores/connectionStore';
import { useGroupStore } from './core/stores/groupStore';
```

#### 2. 替换 useState 为 Store
```typescript
// ❌ 旧代码（useState）
const [nodes, setNodes] = useState<Map<string, AppNode>>(new Map());
const [connections, setConnections] = useState<Connection[]>([]);
const [groups, setGroups] = useState<Group[]>([]);

// ✅ 新代码（Zustand Store）
const nodes = useNodeStore(state => state.nodes);
const connections = useConnectionStore(state => state.connections);
const groups = useGroupStore(state => state.groups);

// 获取操作方法
const { addNode: addNodeToStore, updateNode, deleteNode, setNodes } = useNodeStore();
const { addConnection, deleteConnection, setConnections } = useConnectionStore();
const { addGroup, updateGroup, deleteGroup: deleteGroupFromStore, setGroups } = useGroupStore();
```

#### 3. 更新 useHistory Hook
```typescript
// ✅ 恢复历史状态到 Store
onRestore: (state) => {
  setNodes(state.nodes); // setNodes 会自动处理 Array → Map 转换
  setConnections(state.connections);
  setGroups(state.groups);
}
```

#### 4. 更新 deleteNodes 函数
```typescript
// ✅ 使用 Store 删除节点
useNodeStore.getState().deleteNodes(ids);

// ✅ 使用 Store 删除连接
useConnectionStore.getState().deleteNodesConnections(ids);
```

#### 5. 更新 useConnection Hook 回调
```typescript
onAddConnection: (connection) => {
  useConnectionStore.getState().addConnection(connection);
},
onDeleteConnection: (from, to) => {
  useConnectionStore.getState().deleteConnection(from, to);
},
onUpdateNodeInputs: (nodeId, inputs) => {
  useNodeStore.getState().updateNodeInputs(nodeId, inputs);
},
```

#### 6. 更新 useGroup Hook 回调
```typescript
onAddGroup: (group) => {
  useGroupStore.getState().addGroup(group);
},
onUpdateGroup: (id, updates) => {
  useGroupStore.getState().updateGroup(id, updates);
},
onDeleteGroup: (id) => {
  useGroupStore.getState().deleteGroup(id);
},
onUpdateNode: (id, updates) => {
  useNodeStore.getState().updateNode(id, updates);
},
```

#### 7. 更新 useDrag Hook 回调
```typescript
onUpdateNode: (id, updates) => {
  useNodeStore.getState().updateNode(id, updates);
},
```

---

## ⚠️ 未完成的工作

### 需要继续迁移的地方

App.tsx 中还有大量直接使用 `setNodes`、`setConnections`、`setGroups` 的地方：

1. **addNode 函数**（约 740 行）
   ```typescript
   setNodes(prev => new Map(prev).set(newNode.id, newNode));
   ```

2. **handleNodeUpdate 函数**（约 1291 行）
   ```typescript
   setNodes(prev => { ... });
   ```

3. **handleGenerate 函数**（多处）
   ```typescript
   setNodes(p => { ... });
   ```

4. **loadWorkflow 函数**（约 2032 行）
   ```typescript
   setNodes(nodesMap);
   setConnections(...);
   setGroups(...);
   ```

5. **键盘快捷键处理**（约 2053 行）
   ```typescript
   setNodes(prev => new Map(prev).set(newNode.id, newNode));
   ```

6. **拖拽处理**（多处）
   ```typescript
   setNodes(prev => { ... });
   ```

7. **右键菜单处理**（多处）
   ```typescript
   setNodes(prev => { ... });
   setConnections(prev => [...prev, ...]);
   ```

8. **数据加载**（useEffect 中）
   ```typescript
   setNodes(nodesMap);
   setConnections(sConns);
   setGroups(sGroups);
   ```

---

## 📊 当前进度

### 阶段 B 完成度：**20%**

- ✅ Store 引入和初始化
- ✅ Hooks 回调更新
- ✅ deleteNodes 函数迁移
- ⏳ addNode 函数迁移
- ⏳ handleNodeUpdate 函数迁移
- ⏳ handleGenerate 函数迁移
- ⏳ 数据加载迁移
- ⏳ 其他业务逻辑迁移

### 总体完成度：**45%**

- ✅ 第 1 步：修复致命 Bug（100%）
- 🔄 第 2 步：抽离底层逻辑（20%）
- ✅ 第 3 步：抽离交互逻辑（80%，功能失效但架构就位）

---

## 🎯 下一步计划

### 立即执行（继续 Store 迁移）

1. **更新 addNode 函数**
   - 使用 `useNodeStore.getState().addNode()`

2. **更新 handleNodeUpdate 函数**
   - 使用 `useNodeStore.getState().updateNode()`

3. **更新 handleGenerate 函数**
   - 使用 Store 方法替代所有 `setNodes`

4. **更新数据加载**
   - 使用 `setNodes()`, `setConnections()`, `setGroups()`

5. **更新所有业务逻辑**
   - 搜索所有 `setNodes(`, `setConnections(`, `setGroups(`
   - 逐一替换为 Store 方法

### 预计完成时间
- **阶段 B**：1-2 天（Store 迁移）
- **阶段 C**：1-2 天（Bug 修复）

---

## ✅ 验收标准（来自 requirements.md）

### 功能验收
- [ ] 所有现有功能正常工作
- [ ] 节点拖拽流畅
- [ ] 连接线正常
- [ ] 撤销/重做正常
- [ ] 数据持久化正常

### 结构验收
- [ ] App.tsx 不再有 `useState` 管理 nodes/connections/groups
- [ ] 所有数据操作通过 Store
- [ ] Hooks 使用 Store 而不是 props

### 扩展性验收
- [ ] 添加新节点类型：只需注册到 NodeRegistry
- [ ] 添加新交互：只需创建新 Hook
- [ ] 添加新数据：只需扩展 Store

### 性能验收
- [ ] 拖拽 100 个节点不卡顿
- [ ] 撤销/重做响应 < 100ms
- [ ] 内存占用稳定

---

## 🚫 禁止事项（来自 requirements.md）

- ❌ **不回滚代码**：功能坏了继续推进，最后修复
- ❌ **不恢复旧状态**：不添加 draggingNodeId 等旧状态
- ❌ **不跨层混用**：UI → Hooks → Store，单向数据流
- ❌ **不在 UI 写逻辑**：所有逻辑在 Hooks 或 Store

---

## 📝 总结

**当前状态**：
- Store 已引入并初始化
- Hooks 回调已更新为使用 Store
- 部分函数已迁移（deleteNodes）
- 大量业务逻辑仍使用 `setNodes` 等旧方法

**下一步**：
- 继续迁移所有 `setNodes`、`setConnections`、`setGroups`
- 目标：App.tsx 不再有任何 `useState` 管理核心数据
- 原则：向前推进，不回滚

**预期结果**：
- 阶段 B 完成后，App.tsx 行数减少 30%
- 所有数据操作通过 Store
- 为阶段 C（Bug 修复）打好基础
