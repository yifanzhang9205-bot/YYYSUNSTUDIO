# 资产库重构 - 阶段2 - 资产拖拽完成 - 2026-02-08

**日期**: 2026-02-08  
**状态**: ✅ 完成

---

## ✅ 完成的工作

### 任务 2.6：实现资产拖拽到画布

**目标**: 用户可以从资产库面板拖拽资产到画布，自动创建节点和连接

---

## 📝 实施细节

### 1. 修改 `useAssetLibrary` Hook 调用

**文件**: `App.tsx` (第 610 行)

**修改前**:
```typescript
const { createAsset } = useAssetLibrary();
```

**修改后**:
```typescript
const { createAsset, useAsset } = useAssetLibrary();
```

**说明**: 同时获取 `createAsset` 和 `useAsset` 方法，用于创建和使用资产。

---

### 2. 在 `handleCanvasDrop` 中添加资产ID检测

**文件**: `App.tsx` (第 1408-1410 行)

**添加代码**:
```typescript
const assetId = e.dataTransfer.getData('application/asset-id'); // 🔥 资产库重构：检测资产ID
```

**说明**: 从拖拽事件中获取资产ID，用于判断是否是资产拖拽。

---

### 3. 实现资产拖拽处理逻辑

**文件**: `App.tsx` (第 1432-1460 行)

**添加代码**:
```typescript
// 🔥 资产库重构：处理资产拖拽
if (assetId) {
    console.log('[App] 处理资产拖拽', { assetId, dropX, dropY });
    
    // 创建回调函数：添加节点到画布
    const onAddNodes = (nodes: AppNode[]) => {
        saveHistory();
        useNodeStore.getState().addNodes(nodes);
        console.log('[App] 添加资产节点到画布', { count: nodes.length });
    };
    
    // 创建回调函数：添加连接到画布
    const onAddConnections = (connections: Connection[]) => {
        connections.forEach(conn => {
            useConnectionStore.getState().addConnection(conn);
        });
        console.log('[App] 添加资产连接到画布', { count: connections.length });
    };
    
    // 调用 useAsset 方法
    useAsset(assetId, { x: dropX, y: dropY }, onAddNodes, onAddConnections);
    return;
}
```

**说明**:
1. **检测资产ID**: 如果 `assetId` 存在，说明是资产拖拽
2. **创建回调函数**: 
   - `onAddNodes`: 添加节点到画布，并保存历史记录
   - `onAddConnections`: 添加连接到画布
3. **调用 useAsset**: 传入资产ID、鼠标位置和回调函数
4. **提前返回**: 避免继续执行其他拖拽逻辑

---

## 🔄 数据流

```
用户从资产库拖拽资产
  ↓
AssetLibraryPanel.handleDragStart
  ↓
设置 dataTransfer: 'application/asset-id' = assetId
  ↓
用户拖拽到画布并释放
  ↓
App.handleCanvasDrop
  ↓
检测 assetId
  ↓
调用 useAsset(assetId, position, onAddNodes, onAddConnections)
  ↓
useAssetLibrary.useAsset
  ↓
1. 从 assetLibraryStore 获取资产数据
2. 深拷贝节点数据
3. 生成新的节点 ID
4. 计算资产边界（minX, minY）
5. 调整节点位置（相对于鼠标位置）
6. 深拷贝连接数据并更新 ID
  ↓
调用 onAddNodes(newNodes)
  ↓
1. 保存历史记录（saveHistory）
2. 批量添加节点到 nodeStore
  ↓
调用 onAddConnections(newConnections)
  ↓
批量添加连接到 connectionStore
  ↓
画布重新渲染，显示新节点和连接
```

---

## 🎯 核心逻辑

### 1. 资产ID传递

**AssetLibraryPanel.tsx**:
```typescript
const handleDragStart = (e: React.DragEvent, asset: Asset) => {
    e.dataTransfer.setData('application/asset-id', asset.id);
    e.dataTransfer.effectAllowed = 'copy';
};
```

### 2. 资产使用（useAsset 方法）

**hooks/useAssetLibrary.ts**:
```typescript
const useAsset = useCallback((
    assetId: string,
    position: { x: number; y: number },
    onAddNodes: (nodes: AppNode[]) => void,
    onAddConnections: (connections: Connection[]) => void
) => {
    // 1. 获取资产数据
    const asset = useAssetLibraryStore.getState().assets.find(a => a.id === assetId);
    
    // 2. 深拷贝节点
    const copiedNodes = deepCopyNodes(asset.nodes);
    
    // 3. 生成新ID映射
    const idMap = new Map<string, string>();
    copiedNodes.forEach(node => {
        const newId = `node-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
        idMap.set(node.id, newId);
    });
    
    // 4. 计算边界
    let minX = Infinity, minY = Infinity;
    copiedNodes.forEach(node => {
        if (node.x < minX) minX = node.x;
        if (node.y < minY) minY = node.y;
    });
    
    // 5. 调整位置和ID
    const newNodes = copiedNodes.map(node => ({
        ...node,
        id: idMap.get(node.id)!,
        x: position.x + (node.x - minX),
        y: position.y + (node.y - minY),
    }));
    
    // 6. 更新连接ID
    const newConnections = copiedConnections.map(conn => ({
        ...conn,
        from: idMap.get(conn.from) || conn.from,
        to: idMap.get(conn.to) || conn.to,
    }));
    
    // 7. 调用回调
    onAddNodes(newNodes);
    onAddConnections(newConnections);
}, [deepCopyNodes, deepCopyConnections]);
```

### 3. 节点和连接添加

**App.tsx**:
```typescript
const onAddNodes = (nodes: AppNode[]) => {
    saveHistory(); // 保存历史记录
    useNodeStore.getState().addNodes(nodes); // 批量添加节点
};

const onAddConnections = (connections: Connection[]) => {
    connections.forEach(conn => {
        useConnectionStore.getState().addConnection(conn);
    });
};
```

---

## ✅ 验收标准

- [x] 用户可以从资产库拖拽资产到画布
- [x] 资产在鼠标位置创建（相对定位正确）
- [x] 资产的所有节点都被创建
- [x] 资产的所有连接都被创建
- [x] 节点ID是新生成的（不会冲突）
- [x] 连接ID正确映射到新节点
- [x] 操作可以撤销（保存历史记录）
- [x] 无编译错误
- [x] 无TypeScript类型错误

---

## 🐛 已知问题

无

---

## 📊 完成度

**阶段2总体进度**: 70% → 80%

| 任务 | 状态 | 完成度 |
|------|------|--------|
| 2.1 创建 assetLibraryStore | ✅ 完成 | 100% |
| 2.2 创建 useAssetLibrary Hook | ✅ 完成 | 100% |
| 2.3 创建 CreateAssetDialog | ✅ 完成 | 100% |
| 2.4 创建 AssetLibraryPanel | ✅ 完成 | 100% |
| 2.5 修改 SidebarDock | ✅ 完成 | 100% |
| 2.6 实现资产拖拽到画布 | ✅ 完成 | 100% |
| 2.7 实现资产右键菜单 | ✅ 完成 | 100% |
| 2.8 完善缩略图生成 | ❌ 未完成 | 0% |
| 2.9 数据持久化 | ❌ 未完成 | 0% |
| 2.10 清理工作流代码 | ❌ 未完成 | 0% |

---

## 🎯 下一步工作

### 任务 2.8：完善缩略图生成（3小时）

**需要实现**：
1. 实现 3 种缩略图生成规则：
   - 有图片节点：使用第一个图片
   - 无图片 + 用户上传：使用自定义图片
   - 无图片 + 无上传：生成首字母占位符（Canvas渲染）
2. 实现 `generateTextThumbnail` 方法
3. 在 `CreateAssetDialog` 中添加缩略图上传功能

**代码位置**：
- `hooks/useAssetLibrary.ts`：完善 `generateThumbnail` 方法
- `components/CreateAssetDialog.tsx`：添加上传按钮

---

### 任务 2.9：数据持久化（2小时）

**需要实现**：
1. 将 assetLibraryStore 持久化到 IndexedDB
2. 实现 Blob URL 管理（saveBlobUrl, loadBlobUrl）
3. 删除资产时清理 Blob URL 和 IndexedDB 数据

**代码位置**：
- `core/stores/assetLibraryStore.ts`：添加持久化中间件
- `hooks/useAssetLibrary.ts`：添加 Blob URL 管理方法

---

### 任务 2.10：清理工作流代码（1小时）

**需要删除**：
1. `components/SidebarDock.tsx` 中的工作流面板渲染逻辑
2. `App.tsx` 中的工作流相关状态和方法
3. `core/stores/workflowStore.ts`（可选：保留但不使用）

---

## 🎉 总结

资产拖拽功能已完成！用户现在可以：
1. ✅ 从资产库拖拽资产到画布
2. ✅ 资产在鼠标位置创建
3. ✅ 所有节点和连接正确创建
4. ✅ 操作可以撤销

剩余工作：
- ❌ 完善缩略图生成（3种规则）
- ❌ 数据持久化（IndexedDB）
- ❌ 清理工作流代码

预计剩余时间：**6小时**

---

**架构遵守情况**:
- ✅ 遵守三层架构（UI → Hooks → Core）
- ✅ 没有在 App.tsx 添加业务逻辑（只是调用 Hook）
- ✅ 使用 Store 管理状态
- ✅ 使用 Hook 处理交互逻辑
- ✅ 代码清晰，易于维护

---

**下一步**: 继续实施任务 2.8（完善缩略图生成）或任务 2.9（数据持久化）

