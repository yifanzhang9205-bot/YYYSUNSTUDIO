# 架构重构 - 验收第 13 步 - Group 拖拽核查 - 2026-01-27

## 验收目标

确认 Group 的渲染逻辑是否正确包裹了内部的节点？拖动 Group 时，内部的节点是否跟随移动？

**期望结果**：创建一个组，拖动组的标题栏，里面的节点跟着一起跑。

---

## 核查内容

### 1. groupStore 实现 ✅

**文件**：`core/stores/groupStore.ts`

#### 1.1 数据结构

```typescript
export interface GroupStore {
  /** 所有分组 */
  groups: Group[];
  
  // 查询操作
  getAllGroups: () => Group[];
  getGroup: (id: string) => Group | undefined;
  
  // 增删改操作
  addGroup: (group: Group) => void;
  updateGroup: (id: string, updates: Partial<Group>) => void;
  deleteGroup: (id: string) => void;
  
  // 特殊操作
  updateGroupPosition: (id: string, x: number, y: number) => void;
  updateGroupSize: (id: string, width: number, height: number) => void;
}
```

✅ **正确**：
- 使用数组存储分组
- 提供完整的 CRUD 接口
- 使用 Immer 中间件优化性能

---

### 2. useGroup Hook 实现 ⚠️

**文件**：`hooks/useGroup.ts`

#### 2.1 拖拽逻辑

**开始拖拽**：
```typescript
const startGroupDrag = useCallback((
  e: React.MouseEvent,
  groupId: string,
  group: Group
) => {
  e.stopPropagation();
  onSaveHistory();

  // 获取分组元素
  const groupElement = document.getElementById(`group-${groupId}`);
  if (!groupElement) return;

  // 获取所有子节点元素
  const childElements = new Map<string, HTMLElement>();
  group.nodeIds.forEach(nodeId => {
    const element = document.getElementById(`node-${nodeId}`);
    if (element) {
      childElements.set(nodeId, element);
    }
  });

  // 初始化拖拽上下文
  dragGroupRef.current = {
    id: groupId,
    startX: group.x,
    startY: group.y,
    mouseStartX: e.clientX,
    mouseStartY: e.clientY,
    childNodes: group.nodeIds.map(nodeId => {
      const node = nodes.get(nodeId);
      return {
        id: nodeId,
        startX: node?.x || 0,
        startY: node?.y || 0,
      };
    }),
    groupElement,
    childElements,
  };

  // 启用 GPU 加速
  groupElement.style.willChange = 'transform';
  groupElement.style.transition = 'none';
  childElements.forEach(element => {
    element.style.willChange = 'transform';
    element.style.transition = 'none';
  });
}, [nodes, onSaveHistory]);
```

⚠️ **问题**：使用 `document.getElementById('group-${groupId}')` 查找元素

**拖拽中**：
```typescript
const updateGroupDrag = useCallback((e: MouseEvent, scale: number) => {
  if (!dragGroupRef.current) return;

  // 取消上一帧
  if (rafRef.current) {
    cancelAnimationFrame(rafRef.current);
  }

  // 下一帧执行
  rafRef.current = requestAnimationFrame(() => {
    if (!dragGroupRef.current) return;

    const { groupElement, childElements, mouseStartX, mouseStartY } = dragGroupRef.current;
    const dx = e.clientX - mouseStartX;
    const dy = e.clientY - mouseStartY;

    // 直接操作 DOM（不触发 React 渲染）
    if (groupElement) {
      groupElement.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
    }

    childElements.forEach(element => {
      element.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
    });
  });
}, []);
```

✅ **正确**：
- 使用 RAF 节流
- 直接操作 DOM（不触发 React 渲染）
- 同时移动 Group 和子节点

**拖拽结束**：
```typescript
const endGroupDrag = useCallback((e: MouseEvent, scale: number) => {
  if (!dragGroupRef.current) return;

  const {
    id,
    startX,
    startY,
    mouseStartX,
    mouseStartY,
    childNodes,
    groupElement,
    childElements,
  } = dragGroupRef.current;

  // 计算最终位置
  const dx = (e.clientX - mouseStartX) / scale;
  const dy = (e.clientY - mouseStartY) / scale;

  // 更新分组位置
  onUpdateGroup(id, {
    x: startX + dx,
    y: startY + dy,
  });

  // 更新子节点位置
  childNodes.forEach(({ id: nodeId, startX: nodeStartX, startY: nodeStartY }) => {
    onUpdateNode(nodeId, {
      x: nodeStartX + dx,
      y: nodeStartY + dy,
    });
  });

  // 清理 DOM
  if (groupElement) {
    groupElement.style.transform = '';
    groupElement.style.willChange = '';
    groupElement.style.transition = '';
  }

  childElements.forEach(element => {
    element.style.transform = '';
    element.style.willChange = '';
    element.style.transition = '';
  });

  // 清理上下文
  dragGroupRef.current = null;
  if (rafRef.current) {
    cancelAnimationFrame(rafRef.current);
    rafRef.current = null;
  }
}, [onUpdateGroup, onUpdateNode]);
```

✅ **正确**：
- 计算最终位置（除以 scale）
- 更新 Group 和子节点的 Store
- 清理 DOM 样式

---

### 3. App.tsx 集成 ❌

**文件**：`App.tsx`

#### 3.1 Group 渲染

**Group 元素**（第 2086-2133 行）：
```typescript
{groups.map(g => (
  <div 
    key={g.id} 
    data-group-id={g.id}  // ❌ 只有 data-group-id，没有 id
    className={`absolute rounded-[32px] border ${
      selectedGroupId === g.id 
        ? 'border-cyan-500/30 bg-cyan-500/5' 
        : 'border-white/10 bg-white/5'
    }`} 
    style={{ 
      left: g.x, 
      top: g.y, 
      width: g.width, 
      height: g.height,
      transition: 'all 0.3s cubic-bezier(0.32, 0.72, 0, 1)'
    }} 
    onMouseDown={(e) => { 
      e.stopPropagation(); 
      selectGroup(g.id);
      
      // ... 拖拽逻辑
      
      dragGroupRef.current = { 
        id: g.id, 
        startX: g.x, 
        startY: g.y, 
        mouseStartX: e.clientX, 
        mouseStartY: e.clientY, 
        childNodes,
        groupElement,
        childElements
      };
    }} 
    onDoubleClick={(e) => e.stopPropagation()}
    onContextMenu={e => { 
      e.stopPropagation(); 
      setContextMenu({visible:true, x:e.clientX, y:e.clientY, id:g.id}); 
      setContextMenuTarget({type:'group', id:g.id}); 
    }}
  >
    <div className="absolute -top-8 left-4 text-xs font-bold text-white/40 uppercase tracking-widest">
      {g.title}
    </div>
  </div>
))}
```

❌ **问题 1**：Group 元素只有 `data-group-id` 属性，没有 `id` 属性

**原因**：
- useGroup Hook 使用 `document.getElementById('group-${groupId}')` 查找元素
- 但 App.tsx 中只设置了 `data-group-id={g.id}`
- 导致 `groupElement` 为 `null`，拖拽失败

**解决方案**：添加 `id` 属性
```typescript
<div 
  key={g.id} 
  id={`group-${g.id}`}  // ✅ 添加 id 属性
  data-group-id={g.id}
  // ...
>
```

❌ **问题 2**：App.tsx 中重复实现了拖拽逻辑

**App.tsx 中的拖拽逻辑**（第 2103-2130 行）：
```typescript
onMouseDown={(e) => { 
  e.stopPropagation(); 
  selectGroup(g.id);
  
  const childNodes = Array.from(nodes.values()).filter(n => { 
    const b = getNodeBounds(n); 
    const cx = b.x + b.width/2; 
    const cy = b.y + b.height/2; 
    return cx>g.x && cx<g.x+g.width && cy>g.y && cy<g.y+g.height; 
  }).map(n=>({id:n.id, startX:n.x, startY:n.y}));
  
  // 缓存 Group 和子节点的 DOM 元素
  const groupElement = document.querySelector(`[data-group-id="${g.id}"]`) as HTMLElement;
  const childElements = new Map<string, HTMLElement>();
  childNodes.forEach(child => {
    const el = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
    if (el) childElements.set(child.id, el);
  });
  
  dragGroupRef.current = { 
    id: g.id, 
    startX: g.x, 
    startY: g.y, 
    mouseStartX: e.clientX, 
    mouseStartY: e.clientY, 
    childNodes,
    groupElement,
    childElements
  };
}} 
```

❌ **问题**：
- App.tsx 中直接实现了拖拽逻辑，没有调用 useGroup Hook 的 `startGroupDrag`
- 重复代码，违反 DRY 原则
- 逻辑不一致：App.tsx 使用 `querySelector`，useGroup 使用 `getElementById`

**解决方案**：调用 useGroup Hook
```typescript
onMouseDown={(e) => { 
  startGroupDrag(e, g.id, g);
}} 
```

#### 3.2 Group 拖拽处理

**拖拽中**（第 1004-1007 行）：
```typescript
// === Group 拖动：使用 useGroup Hook ===
if (isDraggingGroup) {
  updateGroupDrag(e, scale);
  return;
}
```

✅ **正确**：调用 useGroup Hook 的 `updateGroupDrag`

**拖拽结束**（第 1017-1020 行）：
```typescript
// === Group 拖拽结束：使用 useGroup Hook ===
if (isDraggingGroup) {
  endGroupDrag(e, scale);
}
```

✅ **正确**：调用 useGroup Hook 的 `endGroupDrag`

---

### 4. 子节点跟随逻辑 ⚠️

#### 4.1 子节点查找

**useGroup Hook**：
```typescript
group.nodeIds.forEach(nodeId => {
  const element = document.getElementById(`node-${nodeId}`);
  if (element) {
    childElements.set(nodeId, element);
  }
});
```

⚠️ **问题**：使用 `document.getElementById('node-${nodeId}')`

**Node.tsx**（第 877 行）：
```typescript
<div 
  data-node-id={node.id}  // ✅ 有 data-node-id
  // 但没有 id 属性
>
```

❌ **问题**：Node 元素只有 `data-node-id` 属性，没有 `id` 属性

**解决方案**：
1. 给 Node 元素添加 `id` 属性
2. 或者修改 useGroup Hook 使用 `querySelector`

#### 4.2 子节点移动

**拖拽中**：
```typescript
childElements.forEach(element => {
  element.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
});
```

✅ **正确**：同时移动所有子节点

**拖拽结束**：
```typescript
childNodes.forEach(({ id: nodeId, startX: nodeStartX, startY: nodeStartY }) => {
  onUpdateNode(nodeId, {
    x: nodeStartX + dx,
    y: nodeStartY + dy,
  });
});
```

✅ **正确**：更新所有子节点的 Store

---

## 验收结果

### ❌ 功能验收 - 未通过

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Group 渲染正确 | ⚠️ | 缺少 `id` 属性 |
| Group 包裹节点 | ✅ | 通过位置判断包含关系 |
| 拖拽逻辑正确 | ⚠️ | Hook 实现正确，但未被调用 |
| 子节点跟随移动 | ❌ | 找不到元素，无法移动 |
| Store 更新正确 | ✅ | 更新 Group 和子节点位置 |

### ❌ 架构验收 - 未通过

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 使用 useGroup Hook | ❌ | App.tsx 重复实现拖拽逻辑 |
| Hook 职责清晰 | ✅ | useGroup 职责明确 |
| Store 职责清晰 | ✅ | groupStore 职责明确 |
| 代码可维护性 | ❌ | 重复代码，逻辑不一致 |

---

## 问题总结

### 🔴 问题 1：Group 元素缺少 id 属性

**位置**：`App.tsx` 第 2089 行

**当前代码**：
```typescript
<div 
  key={g.id} 
  data-group-id={g.id}  // ❌ 只有 data-group-id
  // ...
>
```

**问题**：
- useGroup Hook 使用 `document.getElementById('group-${groupId}')` 查找元素
- 但 Group 元素只有 `data-group-id` 属性
- 导致 `groupElement` 为 `null`，拖拽失败

**修复方案**：
```typescript
<div 
  key={g.id} 
  id={`group-${g.id}`}  // ✅ 添加 id 属性
  data-group-id={g.id}
  // ...
>
```

---

### 🔴 问题 2：Node 元素缺少 id 属性

**位置**：`components/Node.tsx` 第 877 行

**当前代码**：
```typescript
<div 
  data-node-id={node.id}  // ❌ 只有 data-node-id
  // ...
>
```

**问题**：
- useGroup Hook 使用 `document.getElementById('node-${nodeId}')` 查找子节点元素
- 但 Node 元素只有 `data-node-id` 属性
- 导致 `childElements` 为空，子节点无法跟随移动

**修复方案 A**（推荐）：添加 id 属性
```typescript
<div 
  id={`node-${node.id}`}  // ✅ 添加 id 属性
  data-node-id={node.id}
  // ...
>
```

**修复方案 B**：修改 useGroup Hook 使用 querySelector
```typescript
// useGroup.ts
const element = document.querySelector(`[data-node-id="${nodeId}"]`) as HTMLElement;
```

**推荐**：方案 A（添加 id 属性）
- 更高效（getElementById 比 querySelector 快）
- 与现有代码风格一致

---

### 🔴 问题 3：App.tsx 重复实现拖拽逻辑

**位置**：`App.tsx` 第 2103-2130 行

**当前代码**：
```typescript
onMouseDown={(e) => { 
  e.stopPropagation(); 
  selectGroup(g.id);
  
  // ❌ 重复实现拖拽逻辑
  const childNodes = Array.from(nodes.values()).filter(n => { 
    const b = getNodeBounds(n); 
    const cx = b.x + b.width/2; 
    const cy = b.y + b.height/2; 
    return cx>g.x && cx<g.x+g.width && cy>g.y && cy<g.y+g.height; 
  }).map(n=>({id:n.id, startX:n.x, startY:n.y}));
  
  const groupElement = document.querySelector(`[data-group-id="${g.id}"]`) as HTMLElement;
  const childElements = new Map<string, HTMLElement>();
  childNodes.forEach(child => {
    const el = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
    if (el) childElements.set(child.id, el);
  });
  
  dragGroupRef.current = { 
    id: g.id, 
    startX: g.x, 
    startY: g.y, 
    mouseStartX: e.clientX, 
    mouseStartY: e.clientY, 
    childNodes,
    groupElement,
    childElements
  };
}} 
```

**问题**：
- App.tsx 中直接实现了拖拽逻辑
- 没有调用 useGroup Hook 的 `startGroupDrag`
- 重复代码，违反 DRY 原则
- 逻辑不一致：App.tsx 使用 `querySelector`，useGroup 使用 `getElementById`

**修复方案**：
```typescript
onMouseDown={(e) => { 
  startGroupDrag(e, g.id, g);
}} 
```

**注意**：修复后需要确保 Group 和 Node 元素都有 `id` 属性（问题 1 和 2）

---

### 🔴 问题 4：子节点查找逻辑不一致

**useGroup Hook**：
```typescript
// 通过 Group 的 nodeIds 查找子节点
group.nodeIds.forEach(nodeId => {
  const element = document.getElementById(`node-${nodeId}`);
  if (element) {
    childElements.set(nodeId, element);
  }
});
```

**App.tsx**：
```typescript
// 通过位置判断子节点
const childNodes = Array.from(nodes.values()).filter(n => { 
  const b = getNodeBounds(n); 
  const cx = b.x + b.width/2; 
  const cy = b.y + b.height/2; 
  return cx>g.x && cx<g.x+g.width && cy>g.y && cy<g.y+g.height; 
}).map(n=>({id:n.id, startX:n.x, startY:n.y}));
```

**问题**：
- useGroup Hook 使用 `group.nodeIds`（数据驱动）
- App.tsx 使用位置判断（视觉驱动）
- 两种方式可能得到不同的结果

**分析**：
- `group.nodeIds` 是正确的数据源（Group 创建时记录的节点）
- 位置判断可能不准确（节点可能移出 Group 范围）

**建议**：统一使用 `group.nodeIds`（useGroup Hook 的方式）

---

## 修复方案

### 修复步骤

#### 1. 修复 Group 元素（App.tsx）

```typescript
<div 
  key={g.id} 
  id={`group-${g.id}`}  // ✅ 添加 id 属性
  data-group-id={g.id}
  className={`absolute rounded-[32px] border ${
    selectedGroupId === g.id 
      ? 'border-cyan-500/30 bg-cyan-500/5' 
      : 'border-white/10 bg-white/5'
  }`} 
  style={{ 
    left: g.x, 
    top: g.y, 
    width: g.width, 
    height: g.height,
    transition: 'all 0.3s cubic-bezier(0.32, 0.72, 0, 1)'
  }} 
  onMouseDown={(e) => { 
    startGroupDrag(e, g.id, g);  // ✅ 调用 Hook
  }} 
  onDoubleClick={(e) => e.stopPropagation()}
  onContextMenu={e => { 
    e.stopPropagation(); 
    setContextMenu({visible:true, x:e.clientX, y:e.clientY, id:g.id}); 
    setContextMenuTarget({type:'group', id:g.id}); 
  }}
>
  <div className="absolute -top-8 left-4 text-xs font-bold text-white/40 uppercase tracking-widest">
    {g.title}
  </div>
</div>
```

#### 2. 修复 Node 元素（Node.tsx）

```typescript
<div 
  id={`node-${node.id}`}  // ✅ 添加 id 属性
  data-node-id={node.id}
  className={`absolute rounded-[24px] group ${
    isSelected 
      ? 'ring-1 ring-cyan-500/50 shadow-[0_0_40px_-10px_rgba(34,211,238,0.3)] z-30' 
      : 'ring-1 ring-white/10 hover:ring-white/20 z-10'
  }`}
  // ...
>
```

#### 3. 删除 App.tsx 中的重复代码

删除 `dragGroupRef` 的定义（第 508-520 行），因为 useGroup Hook 已经有了。

---

## 验收标准（修复后）

### 功能验收

- [ ] Group 元素有 `id` 属性
- [ ] Node 元素有 `id` 属性
- [ ] 拖拽 Group 时，Group 移动
- [ ] 拖拽 Group 时，子节点跟随移动
- [ ] 松手后，Group 和子节点位置正确
- [ ] Store 更新正确

### 架构验收

- [ ] App.tsx 调用 useGroup Hook 的 `startGroupDrag`
- [ ] 没有重复代码
- [ ] 逻辑一致（都使用 `group.nodeIds`）

---

## 总结

### ❌ 验收未通过

**主要问题**：
1. ❌ Group 元素缺少 `id` 属性
2. ❌ Node 元素缺少 `id` 属性
3. ❌ App.tsx 重复实现拖拽逻辑，未调用 useGroup Hook
4. ❌ 子节点查找逻辑不一致

**影响**：
- Group 拖拽完全失效（找不到元素）
- 子节点无法跟随移动
- 代码重复，难以维护

**修复优先级**：
1. 🔴 **高优先级**：添加 `id` 属性（问题 1 和 2）
2. 🔴 **高优先级**：调用 useGroup Hook（问题 3）
3. 🟡 **中优先级**：统一子节点查找逻辑（问题 4）

---

## 下一步

### 立即修复

1. 修复 Group 元素（添加 `id` 属性）
2. 修复 Node 元素（添加 `id` 属性）
3. 修复 App.tsx（调用 useGroup Hook）
4. 删除重复代码

### 验收流程继续

- [x] 第 10 步：画线交互核查 ✅
- [x] 第 11 步：连线同步核查 ✅
- [x] 第 12 步：选择逻辑核查 ✅
- [x] 第 13 步：Group 拖拽核查 ❌（需要修复）
- [ ] 第 14 步：历史记录核查

---

**日期**：2026-01-27  
**状态**：❌ 验收未通过（需要修复）  
**下一步**：修复 Group 和 Node 的 `id` 属性，调用 useGroup Hook
