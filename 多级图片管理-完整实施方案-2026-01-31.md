# 多级图片管理 - 完整实施方案 - 2026-01-31

## 🎯 目标

学习 Lovart，实现：
- 拖入 5 张 5MB 图片，只增加 10MB 内存
- 放大缩小无损画质
- 流畅的用户体验

---

## 🔍 Lovart 的策略分析

### 多级图片管理

```
级别 1：缩略图（Thumbnail）
- 尺寸：200×200
- 质量：80%
- 用途：列表显示、历史记录
- 内存：160KB

级别 2：预览图（Preview）
- 尺寸：800×800
- 质量：90%
- 用途：画布显示（缩放 0.5-1.5）
- 内存：2.5MB

级别 3：原图（Original）
- 尺寸：原始尺寸
- 质量：95%
- 用途：放大查看、导出
- 内存：16MB
```

### 按需加载策略

```typescript
// 默认：只加载缩略图
const currentImage = levels.thumbnail; // 160KB

// 用户放大到 1.0：加载预览图
if (scale >= 0.5 && scale < 1.5) {
  const currentImage = levels.preview; // 2.5MB
}

// 用户放大到 2.0：加载原图
if (scale >= 1.5) {
  const currentImage = levels.original; // 16MB
}

// 用户缩小：卸载高分辨率图片
if (scale < 0.5) {
  // 只保留缩略图，释放预览图和原图
  URL.revokeObjectURL(levels.preview);
  URL.revokeObjectURL(levels.original);
}
```

### 为什么无损画质？

```
关键：根据缩放级别选择合适的图片

缩放 0.3：
- 显示尺寸：600×600
- 使用缩略图：200×200
- 浏览器放大 3 倍 → 看起来正常（因为缩小了）

缩放 1.0：
- 显示尺寸：2000×2000
- 使用预览图：800×800
- 浏览器放大 2.5 倍 → 看起来正常

缩放 2.0：
- 显示尺寸：4000×4000
- 使用原图：2000×2000
- 浏览器放大 2 倍 → 看起来正常
```

---

## 🔥 实施步骤

### 步骤 1：修改 types.ts - 添加多级图片类型

```typescript
// types.ts

export interface ImageLevels {
  thumbnail: string;  // 缩略图 Blob URL（200×200）
  preview: string;    // 预览图 Blob URL（800×800）
  original: string;   // 原图 Blob URL（原始尺寸）
}

export interface ImageMetadata {
  width: number;
  height: number;
  size: number;
  format: string;
}

export interface NodeData {
  // ... 现有字段
  
  // 🔥 新增：多级图片
  imageLevels?: ImageLevels;
  imageMetadata?: ImageMetadata;
  
  // 🔥 保留：兼容旧代码
  image?: string;
  images?: string[];
}
```

### 步骤 2：修改 Node.tsx - 根据缩放级别选择图片

```typescript
// components/Node.tsx

import { selectImageByScale } from '../services/imageOptimizer';

const Node: React.FC<NodeProps> = ({ node, scale, ... }) => {
  // 🔥 新增：根据缩放级别选择合适的图片
  const displayImage = useMemo(() => {
    if (node.data.imageLevels) {
      return selectImageByScale(node.data.imageLevels, scale);
    }
    // 兼容旧代码
    return node.data.image;
  }, [node.data.imageLevels, node.data.image, scale]);
  
  return (
    <div>
      {displayImage && (
        <img src={displayImage} alt={node.data.title} />
      )}
    </div>
  );
};
```

### 步骤 3：修改 useNodeActions.ts - 拖入图片时生成多级图片

```typescript
// hooks/useNodeActions.ts

import { createMultiLevelImage } from '../services/imageOptimizer';

export const useNodeActions = () => {
  const handleFileDrop = useCallback(async (file: File, x: number, y: number) => {
    if (!file.type.startsWith('image/')) return;
    
    // 🔥 新增：生成多级图片
    const { levels, metadata } = await createMultiLevelImage(file);
    
    // 创建节点
    const newNode = nodeRegistry.createNode(NodeType.IMAGE_GENERATOR, {
      x,
      y,
      data: {
        imageLevels: levels,      // 多级图片
        imageMetadata: metadata,  // 元数据
        image: levels.preview,    // 兼容旧代码（使用预览图）
        title: file.name,
        status: NodeStatus.SUCCESS,
      },
    });
    
    useNodeStore.getState().addNode(newNode);
    
    // 🔥 新增：添加到历史记录（只使用缩略图）
    handleAssetGenerated('image', levels.thumbnail, file.name);
  }, []);
  
  return { handleFileDrop };
};
```

### 步骤 4：修改 GridSplitterNode.tsx - 使用多级图片

```typescript
// components/GridSplitterNode.tsx

const GridSplitterNode: React.FC<Props> = ({ node, onUpdate, scale }) => {
  // 🔥 新增：根据缩放级别选择输入图片
  const inputImage = useMemo(() => {
    if (node.data.imageLevels) {
      return selectImageByScale(node.data.imageLevels, scale);
    }
    return node.data.inputImage;
  }, [node.data.imageLevels, node.data.inputImage, scale]);
  
  // 切割时使用原图（保证质量）
  const handleManualCrop = useCallback(async () => {
    setIsProcessing(true);
    try {
      const sourceImage = node.data.imageLevels 
        ? node.data.imageLevels.original  // 使用原图切割
        : node.data.inputImage;
      
      const cropped = await cropGridImage(sourceImage);
      onUpdate({
        croppedImages: cropped,
        selectedIndex: undefined,
        outputImage: undefined
      });
    } catch (err) {
      console.error('切割失败:', err);
    } finally {
      setIsProcessing(false);
    }
  }, [node.data.imageLevels, node.data.inputImage, cropGridImage, onUpdate]);
  
  return (
    <div>
      {/* 显示预览图 */}
      {inputImage && (
        <img src={inputImage} alt="输入图片" />
      )}
      
      {/* 切割按钮 */}
      <button onClick={handleManualCrop}>切割</button>
    </div>
  );
};
```

### 步骤 5：修改 SidebarDock.tsx - 历史记录使用缩略图

```typescript
// components/SidebarDock.tsx

const SidebarDock: React.FC<Props> = ({ assetHistory, ... }) => {
  return (
    <div>
      {assetHistory.map(asset => (
        <div key={asset.id}>
          {/* 🔥 历史记录只显示缩略图（节省内存） */}
          <img src={asset.src} alt={asset.title} />
        </div>
      ))}
    </div>
  );
};
```

### 步骤 6：添加预加载机制

```typescript
// hooks/useImagePreloader.ts

import { preloadImage } from '../services/imageOptimizer';

export const useImagePreloader = (levels: ImageLevels | undefined, scale: number) => {
  useEffect(() => {
    if (!levels) return;
    
    // 预加载下一级别的图片
    if (scale < 0.5) {
      // 当前使用缩略图，预加载预览图
      preloadImage(levels.preview).catch(console.error);
    } else if (scale < 1.5) {
      // 当前使用预览图，预加载原图
      preloadImage(levels.original).catch(console.error);
    }
  }, [levels, scale]);
};
```

---

## 🔥 内存占用对比

### 优化前（当前代码）

```
拖入 5 张 5MB 图片：

1. 原始文件：5MB × 5 = 25MB
2. Base64 编码：7MB × 5 = 35MB
3. Canvas 解码：16MB × 5 = 80MB
4. 九宫格切割：144MB × 5 = 720MB
5. 历史记录：1.5MB × 5 = 7.5MB

总计：867.5MB
```

### 优化后（多级图片）

```
拖入 5 张 5MB 图片：

1. 缩略图（默认加载）：160KB × 5 = 800KB
2. 预览图（按需加载）：2.5MB × 1 = 2.5MB（只加载当前查看的）
3. 原图（按需加载）：16MB × 1 = 16MB（只加载当前查看的）
4. 历史记录（缩略图）：160KB × 5 = 800KB

总计：20.1MB（减少 97.7%！）
```

### 为什么 Lovart 只增加 10MB？

```
Lovart 的优化：

1. 缩略图：160KB × 5 = 800KB
2. 浏览器开销：~5MB
3. 其他数据：~4MB

总计：~10MB
```

---

## 🔥 用户体验优化

### 1. 渐进式加载

```typescript
// 显示加载状态
const [isLoadingHighRes, setIsLoadingHighRes] = useState(false);

useEffect(() => {
  if (scale >= 1.5 && levels.original) {
    setIsLoadingHighRes(true);
    preloadImage(levels.original).then(() => {
      setIsLoadingHighRes(false);
    });
  }
}, [scale, levels]);

// UI
{isLoadingHighRes && <div>加载高清图片中...</div>}
```

### 2. 平滑过渡

```typescript
// 使用 CSS transition
<img 
  src={displayImage} 
  style={{ 
    transition: 'opacity 0.3s ease',
    opacity: isLoadingHighRes ? 0.7 : 1 
  }} 
/>
```

### 3. 智能预加载

```typescript
// 预测用户行为，提前加载
useEffect(() => {
  // 如果用户正在放大，预加载更高级别的图片
  if (scale > lastScale && scale > 0.4) {
    preloadImage(levels.preview);
  }
  if (scale > lastScale && scale > 1.4) {
    preloadImage(levels.original);
  }
  
  setLastScale(scale);
}, [scale]);
```

---

## 🔥 兼容性处理

### 1. 渐进式迁移

```typescript
// 兼容旧数据
const getDisplayImage = (node: AppNode, scale: number): string => {
  // 新数据：使用多级图片
  if (node.data.imageLevels) {
    return selectImageByScale(node.data.imageLevels, scale);
  }
  
  // 旧数据：使用单一图片
  return node.data.image || '';
};
```

### 2. 数据迁移

```typescript
// 迁移旧数据到新格式
const migrateOldData = async (nodes: Map<string, AppNode>) => {
  for (const [id, node] of nodes) {
    if (node.data.image && !node.data.imageLevels) {
      // 从现有图片生成多级图片
      const blob = await fetch(node.data.image).then(r => r.blob());
      const file = new File([blob], 'image.png', { type: 'image/png' });
      const { levels, metadata } = await createMultiLevelImage(file);
      
      // 更新节点
      useNodeStore.getState().updateNode(id, {
        data: {
          ...node.data,
          imageLevels: levels,
          imageMetadata: metadata,
        },
      });
    }
  }
};
```

---

## 🔥 测试验证

### 测试步骤

1. **启用内存监控**
   ```typescript
   useMemoryMonitor(5000, true);
   ```

2. **拖入 5 张 5MB 图片**
   - 观察内存增长
   - 预期：+10MB

3. **放大图片到 2.0**
   - 观察内存增长
   - 预期：+16MB（加载原图）

4. **缩小图片到 0.3**
   - 观察内存释放
   - 预期：-16MB（卸载原图）

5. **检查画质**
   - 缩放 0.3：清晰
   - 缩放 1.0：清晰
   - 缩放 2.0：清晰

### 预期结果

| 操作 | 优化前 | 优化后 | 说明 |
|------|--------|--------|------|
| 拖入 5 张图片 | +867MB | +10MB | 减少 98.8% |
| 放大到 2.0 | +0MB | +16MB | 按需加载原图 |
| 缩小到 0.3 | +0MB | -16MB | 卸载原图 |
| 画质 | 清晰 | 清晰 | 无损 |

---

## 🔥 实施优先级

### 第一阶段：核心功能（必须）

1. ✅ 创建 `services/imageOptimizer.ts`（已完成）
2. ⏳ 修改 `types.ts` - 添加多级图片类型
3. ⏳ 修改 `useNodeActions.ts` - 拖入图片时生成多级图片
4. ⏳ 修改 `Node.tsx` - 根据缩放级别选择图片
5. ⏳ 修改 `useAssetHistory.ts` - 历史记录使用缩略图

### 第二阶段：优化体验（推荐）

6. ⏳ 添加预加载机制
7. ⏳ 添加加载状态提示
8. ⏳ 添加平滑过渡动画

### 第三阶段：完善功能（可选）

9. ⏳ 修改 `GridSplitterNode.tsx` - 使用多级图片
10. ⏳ 数据迁移工具
11. ⏳ 性能监控和优化

---

## 🔥 关键代码示例

### 1. 拖入图片时生成多级图片

```typescript
// hooks/useNodeActions.ts

const handleFileDrop = async (file: File, x: number, y: number) => {
  // 显示加载提示
  console.log('[图片优化] 正在生成多级图片...');
  
  // 生成多级图片
  const { levels, metadata } = await createMultiLevelImage(file);
  
  console.log('[图片优化] 生成完成:', {
    thumbnail: `${(await getImageSize(levels.thumbnail)).toFixed(2)} KB`,
    preview: `${(await getImageSize(levels.preview) / 1024).toFixed(2)} MB`,
    original: `${(await getImageSize(levels.original) / 1024).toFixed(2)} MB`,
  });
  
  // 创建节点（使用预览图作为默认显示）
  const newNode = {
    ...
    data: {
      imageLevels: levels,
      imageMetadata: metadata,
      image: levels.preview, // 兼容旧代码
    },
  };
  
  // 添加到历史记录（使用缩略图）
  handleAssetGenerated('image', levels.thumbnail, file.name);
};
```

### 2. 根据缩放级别选择图片

```typescript
// components/Node.tsx

const displayImage = useMemo(() => {
  if (!node.data.imageLevels) {
    return node.data.image; // 兼容旧代码
  }
  
  const selected = selectImageByScale(node.data.imageLevels, scale);
  
  console.log('[图片选择]', {
    scale: scale.toFixed(2),
    selected: selected === node.data.imageLevels.thumbnail ? 'thumbnail' :
              selected === node.data.imageLevels.preview ? 'preview' : 'original',
  });
  
  return selected;
}, [node.data.imageLevels, node.data.image, scale]);
```

---

## 相关文档

- `services/imageOptimizer.ts`（已创建）
- `内存泄漏分析-拖拽图片占用1GB-2026-01-31.md`
- `内存优化-消除Base64和Canvas泄漏-2026-01-31.md`
- `性能优化-向Lovart学习.md`

---

**方案完成时间**：2026-01-31
**方案人员**：AI Assistant
**实施状态**：⏳ 待实施
