# 3D 相机输出问题 - 调试日志添加

**日期**：2026-01-31  
**状态**：🔍 调试中  
**问题**：用户反馈"3D相机还是不对劲 输出有问题"

---

## 📋 问题描述

用户反馈 3D 相机的输出有问题，但没有具体说明是什么问题。

**可能的问题**：
1. 输出节点没有自动创建
2. 输出的图片不对
3. 输出节点的位置不对
4. 输出图片的质量或格式问题
5. 九宫格节点没有正确接收输入图片

---

## 🔍 调试策略

### 策略 1：添加调试日志

在关键位置添加调试日志，追踪数据流：

1. **3D 相机生成完成**（`hooks/useNodeActions.ts`）
   - 输出 `blobUrls` 的数量和第一个 URL
   - 输出 `image` 字段的值

2. **九宫格节点创建**（`hooks/useNodeActions.ts`）
   - 输出创建的节点数据
   - 输出 `inputImage` 字段的值

3. **九宫格节点渲染**（`components/Node.tsx`）
   - 输出节点的 `data.inputImage` 字段
   - 输出 `inputAssets` 的数量和类型
   - 输出最终传递给 `GridSplitterNode` 的 `inputImageSrc`

---

## ✅ 已完成的工作

### 1. 修复编译错误

**位置**：`components/Node.tsx` 第 290 行

**问题**：`{...props}` 中的 `props` 变量未定义

**修复**：删除 `{...props}`

**修复前**：
```tsx
<div 
   {...props}
    key={asset.id}
    className={`...`}
```

**修复后**：
```tsx
<div 
    key={asset.id}
    className={`...`}
```

---

### 2. 添加 3D 相机输出日志

**位置**：`hooks/useNodeActions.ts` 第 720-727 行

**添加的日志**：
```typescript
console.log('[MultiAngleCamera] 生成完成，输出数据:', {
    nodeId: node.id,
    blobUrlsCount: blobUrls.length,
    firstBlobUrl: blobUrls[0]?.substring(0, 50),
    imageOutput: blobUrls[0]?.substring(0, 50),
});
```

**作用**：确认 3D 相机是否正确生成了 Blob URL

---

### 3. 添加九宫格节点创建日志

**位置**：`hooks/useNodeActions.ts` 第 790-796 行

**添加的日志**：
```typescript
console.log('[MultiAngleCamera] 创建九宫格节点数据:', {
    gridNodeId,
    inputImage: blobUrls[0]?.substring(0, 50),
    inputImageType: typeof blobUrls[0],
    hasCroppedImages: false,
    selectedIndex: -1,
});
```

**作用**：确认九宫格节点创建时是否正确设置了 `inputImage`

---

### 4. 添加九宫格节点渲染日志

**位置**：`components/Node.tsx` 第 708-720 行

**添加的日志**：
```typescript
console.log('[Node] 九宫格节点数据:', {
    nodeId: node.id,
    hasInputImage: !!node.data.inputImage,
    inputImageType: typeof node.data.inputImage,
    inputImageLength: node.data.inputImage?.length,
    hasCroppedImages: !!node.data.croppedImages,
    croppedImagesCount: node.data.croppedImages?.length,
    selectedIndex: node.data.selectedIndex,
    hasInputAssets: !!inputAssets,
    inputAssetsCount: inputAssets?.length,
});

console.log('[Node] 九宫格节点输入图片:', {
    inputImageSrc: inputImageSrc?.substring(0, 50),
    shouldSaveInput,
});
```

**作用**：确认九宫格节点是否正确接收了 `inputImage`

---

## 🧪 测试步骤

### 步骤 1：清除控制台

1. 打开浏览器开发者工具
2. 清除控制台日志

### 步骤 2：生成 3D 相机图片

1. 创建 3D 相机节点
2. 连接输入图片
3. 点击"生成"按钮
4. 等待生成完成

### 步骤 3：查看控制台日志

**预期日志顺序**：

```
[MultiAngleCamera] 生成完成，输出数据: {
    nodeId: "...",
    blobUrlsCount: 1,
    firstBlobUrl: "blob:http://localhost:5173/...",
    imageOutput: "blob:http://localhost:5173/...",
}

[MultiAngleCamera] 创建九宫格节点数据: {
    gridNodeId: "grid-...",
    inputImage: "blob:http://localhost:5173/...",
    inputImageType: "string",
    hasCroppedImages: false,
    selectedIndex: -1,
}

[MultiAngleCamera] 自动创建九宫格节点 "正面-平视-中景"，位置: (x, y)，尺寸: 630×270

[Node] 九宫格节点数据: {
    nodeId: "grid-...",
    hasInputImage: true,
    inputImageType: "string",
    inputImageLength: 50+,
    hasCroppedImages: false,
    croppedImagesCount: 0,
    selectedIndex: -1,
    hasInputAssets: true,
    inputAssetsCount: 1,
}

[Node] 九宫格节点输入图片: {
    inputImageSrc: "blob:http://localhost:5173/...",
    shouldSaveInput: false,
}

[GridSplitter] 检测到输入图片，开始切割
[GridSplitter] 切割完成，共 9 张
```

---

## 🔍 问题诊断

### 情况 1：3D 相机没有输出 Blob URL

**症状**：
- `[MultiAngleCamera] 生成完成` 日志中 `blobUrlsCount: 0` 或 `firstBlobUrl: undefined`

**原因**：
- API 调用失败
- Blob 转换失败

**解决方案**：
- 检查 API 调用日志
- 检查 `blobStorage.ts` 的 `saveImagesToBlob` 函数

---

### 情况 2：九宫格节点没有接收到 inputImage

**症状**：
- `[Node] 九宫格节点数据` 日志中 `hasInputImage: false`

**原因**：
- 节点创建时 `data.inputImage` 没有设置
- Store 更新失败

**解决方案**：
- 检查 `useNodeStore.getState().addNode(newGridNode)` 是否成功
- 检查 `newGridNode.data.inputImage` 是否正确

---

### 情况 3：九宫格节点没有切割图片

**症状**：
- `[GridSplitter] 检测到输入图片，开始切割` 日志没有出现

**原因**：
- `inputImage` 为空
- `croppedImages` 已经存在（跳过切割）
- `isProcessing` 为 true（跳过切割）

**解决方案**：
- 检查 `inputImageSrc` 是否有值
- 检查 `croppedImages` 是否为空数组
- 检查 `isProcessing` 状态

---

### 情况 4：九宫格节点切割失败

**症状**：
- `[GridSplitter] 切割失败:` 错误日志

**原因**：
- 图片加载失败
- 图片格式不支持
- Canvas 操作失败

**解决方案**：
- 检查 Blob URL 是否有效
- 检查图片是否为 21:9 比例
- 检查 `cropGridImage` 函数

---

## 📝 下一步

1. **用户测试**：请用户生成一次 3D 相机图片，并提供控制台日志
2. **分析日志**：根据日志确定问题所在
3. **修复问题**：根据诊断结果修复问题
4. **验证修复**：再次测试确认问题解决

---

## 🎯 预期结果

**正常流程**：
1. ✅ 3D 相机生成完成，输出 Blob URL
2. ✅ 自动创建九宫格节点，设置 `inputImage`
3. ✅ 九宫格节点接收到 `inputImage`
4. ✅ 九宫格节点自动切割图片
5. ✅ 九宫格节点显示 9 张图片

**异常流程**：
- ❌ 如果任何一步失败，控制台会有对应的错误日志
- ❌ 根据日志定位问题并修复

---

## 📊 编译检查

```bash
✅ components/Node.tsx: No diagnostics found
✅ hooks/useNodeActions.ts: No diagnostics found
```

---

**状态**：✅ 调试日志添加完成，等待用户测试验证
