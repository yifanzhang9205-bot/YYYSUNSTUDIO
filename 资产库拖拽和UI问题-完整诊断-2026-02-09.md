# 资产库拖拽和UI问题 - 完整诊断

**日期**: 2026-02-09  
**状态**: 诊断完成，发现关键问题

---

## 🔴 问题 1：拖拽资产不能产生节点（关键Bug）

### 根本原因

**App.tsx 第 2397-2400 行：`onUseAsset` 回调只打印日志，没有实际调用 `useAsset` 方法！**

```typescript
onUseAsset={(assetId, position) => {
  console.log('[App] 使用资产', { assetId, position });
  // TODO: 实现资产使用逻辑  // ❌ 只有 TODO，没有实际代码！
}}
```

**正确的实现应该是**：

```typescript
onUseAsset={(assetId, position) => {
  console.log('[App] 使用资产', { assetId, position });
  
  // 创建回调函数：添加节点到画布
  const onAddNodes = (nodes: AppNode[]) => {
    saveHistory();
    useNodeStore.getState().addNodes(nodes);
    console.log('[App] 添加资产节点到画布', { count: nodes.length });
  };
  
  // 创建回调函数：添加连接到画布
  const onAddConnections = (connections: Connection[]) => {
    connections.forEach(conn => {
      useConnectionStore.getState().addConnection(conn);
    });
    console.log('[App] 添加资产连接到画布', { count: connections.length });
  };
  
  // 调用 useAsset 方法
  useAsset(assetId, position, onAddNodes, onAddConnections);
}}
```

### 为什么会这样？

**这是一个未完成的功能！**

查看代码历史：
1. `handleCanvasDrop` 函数中有完整的资产拖拽逻辑（第 1421-1450 行）
2. 但 `AssetLibraryPanel` 的 `onUseAsset` 回调没有实现（第 2397-2400 行）
3. 这导致：
   - **拖拽到画布**：✅ 正常工作（使用 `handleCanvasDrop`）
   - **点击资产**：❌ 不工作（`onUseAsset` 只是 TODO）

### 修复方案

**步骤 1**：修改 `App.tsx` 第 2397-2400 行

```typescript
onUseAsset={(assetId, position) => {
  console.log('[App] 使用资产（点击）', { assetId, position });
  
  // 如果 position 是 {x: 0, y: 0}，说明是点击资产，需要计算画布中心位置
  let targetPosition = position;
  if (position.x === 0 && position.y === 0) {
    // 计算画布中心位置（考虑缩放和平移）
    const centerX = (window.innerWidth / 2 - pan.x) / scale;
    const centerY = (window.innerHeight / 2 - pan.y) / scale;
    targetPosition = { x: centerX, y: centerY };
    console.log('[App] 使用画布中心位置', targetPosition);
  }
  
  // 创建回调函数：添加节点到画布
  const onAddNodes = (nodes: AppNode[]) => {
    saveHistory();
    useNodeStore.getState().addNodes(nodes);
    console.log('[App] 添加资产节点到画布', { count: nodes.length });
  };
  
  // 创建回调函数：添加连接到画布
  const onAddConnections = (connections: Connection[]) => {
    connections.forEach(conn => {
      useConnectionStore.getState().addConnection(conn);
    });
    console.log('[App] 添加资产连接到画布', { count: connections.length });
  };
  
  // 调用 useAsset 方法
  useAsset(assetId, targetPosition, onAddNodes, onAddConnections);
}}
```

**步骤 2**：验证修复
1. 点击资产 → 检查画布中心是否创建节点
2. 拖拽资产到画布 → 检查拖拽位置是否创建节点

---

## 🟡 问题 2：删除功能UI问题

### 具体问题

用户反馈"删除可以，但是UI有问题"。

### 可能的UI问题

**问题 A：右键菜单位置不对**
- 右键菜单可能超出屏幕边界
- 右键菜单可能被其他元素遮挡

**问题 B：右键菜单样式不统一**
- 与其他右键菜单（节点、组）的样式不一致
- 字体、间距、颜色可能不协调

**问题 C：删除确认对话框不友好**
- 使用原生 `window.confirm`，样式丑陋
- 没有动画效果
- 没有撤销选项

**问题 D：删除后没有反馈**
- 删除后没有提示信息（Toast）
- 用户不知道是否删除成功

### 当前实现（AssetLibraryPanel.tsx 第 237-268 行）

```typescript
{/* 右键菜单 */}
{contextMenu && (
  <>
    {/* 遮罩层 */}
    <div
      className="fixed inset-0 z-[9998]"
      onClick={closeContextMenu}
    />
    
    {/* 菜单 */}
    <div
      className="fixed z-[9999] bg-white border border-gray-200 rounded-lg shadow-lg p-1 min-w-[140px]"
      style={{
        left: `${contextMenu.x}px`,
        top: `${contextMenu.y}px`,
      }}
    >
      <button
        onClick={() => handleRename(contextMenu.assetId)}
        className="w-full flex items-center gap-3 px-4 py-2.5 text-xs text-gray-700 hover:bg-gray-100 rounded-lg transition-colors"
      >
        <Edit2 size={14} />
        <span>重命名</span>
      </button>
      <button
        onClick={() => handleDelete(contextMenu.assetId)}
        className="w-full flex items-center gap-3 px-4 py-2.5 text-xs text-red-600 hover:bg-red-50 rounded-lg transition-colors"
      >
        <Trash2 size={14} />
        <span>删除</span>
      </button>
    </div>
  </>
)}
```

### 需要用户确认的问题

**请用户描述具体的UI问题**：
1. 右键菜单位置不对？（超出屏幕、被遮挡）
2. 右键菜单样式不好看？（字体、颜色、间距）
3. 删除确认对话框不好看？（原生弹窗）
4. 删除后没有反馈？（没有提示信息）
5. 其他问题？

---

## 🟡 问题 3：资产库整体UI不便

### 用户反馈

"感觉场景库整个UI都稍微有些不便"

### 需要详细分析的UI问题

#### 问题 A：布局和尺寸

**当前设计**：
- 宽度：`w-72`（288px）
- 资产网格：2列
- 资产卡片：正方形（`aspect-square`）

**可能的问题**：
1. **太窄**：288px 可能不够宽，资产卡片太小
2. **2列太少**：只能显示2个资产，浏览效率低
3. **正方形不合适**：资产可能是横向或纵向的，正方形会裁剪

**改进方案**：
- 增加宽度到 `w-80` 或 `w-96`（320px 或 384px）
- 支持 3 列网格
- 支持自定义宽高比（16:9、4:3、1:1）

#### 问题 B：分类标签

**当前设计**：
- 6个分类：全部、人物、场景、物品、风格、其他
- 横向滚动
- 选中状态：白色背景 + 阴影

**可能的问题**：
1. **分类太多**：6个分类可能太多，用户不知道选哪个
2. **横向滚动不方便**：需要滚动才能看到所有分类
3. **选中状态不明显**：白色背景不够突出

**改进方案**：
- 减少分类数量（全部、人物、场景、其他）
- 改为纵向布局（侧边栏）
- 增强选中状态（蓝色背景、加粗文字）

#### 问题 C：资产卡片

**当前设计**：
- 缩略图：自动生成或首字母占位符
- 资产名称：底部渐变遮罩 + 白色文字
- 更多按钮：右上角，悬停显示

**可能的问题**：
1. **缩略图质量差**：自动生成的缩略图可能不清晰
2. **名称不易读**：底部渐变遮罩可能影响可读性
3. **更多按钮不明显**：悬停才显示，用户可能找不到

**改进方案**：
- 提高缩略图质量（使用高分辨率图片）
- 改进名称显示（纯色背景、更大字体）
- 更多按钮始终显示（或改为右键菜单）

#### 问题 D：空状态

**当前设计**：
- 图标：`FolderHeart`
- 文案："暂无资产" + "框选节点后创建资产"

**可能的问题**：
1. **文案不够清晰**：用户可能不知道如何创建资产
2. **没有引导**：没有"创建第一个资产"的按钮

**改进方案**：
- 更详细的文案："选中节点 → 点击'添加到资产库' → 输入名称和分类"
- 添加"了解更多"按钮（打开帮助文档）

#### 问题 E：交互反馈

**当前设计**：
- 拖拽：`cursor-grab` + `active:cursor-grabbing`
- 悬停：`hover:border-gray-400` + `hover:scale-[1.02]`
- 点击：直接创建节点（无反馈）

**可能的问题**：
1. **拖拽反馈不明显**：没有拖拽预览
2. **悬停效果太弱**：缩放 1.02 倍几乎看不出来
3. **点击没有反馈**：用户不知道是否成功

**改进方案**：
- 添加拖拽预览（半透明的资产卡片跟随鼠标）
- 增强悬停效果（缩放 1.05 倍 + 阴影）
- 添加点击反馈（Toast 提示 + 动画）

#### 问题 F：性能问题

**当前设计**：
- 所有资产一次性渲染
- 没有虚拟化滚动
- 没有懒加载

**可能的问题**：
1. **资产太多时卡顿**：100+ 资产会导致渲染缓慢
2. **内存占用高**：所有缩略图都加载到内存

**改进方案**：
- 实施虚拟化滚动（只渲染可见的资产）
- 懒加载缩略图（滚动到才加载）
- 限制资产数量（最多 1000 个）

---

## 📊 UI问题优先级

### 🔴 高优先级（严重影响使用）

1. **拖拽不能产生节点** - 功能完全不可用
2. **删除UI问题** - 需要用户具体描述

### 🟡 中优先级（影响体验）

3. **布局太窄** - 资产卡片太小，浏览不便
4. **分类标签不便** - 横向滚动不方便
5. **交互反馈不明显** - 用户不知道操作是否成功

### 🟢 低优先级（优化体验）

6. **空状态文案不清晰** - 新用户可能不知道如何使用
7. **性能问题** - 资产少时不明显，资产多时才会卡顿

---

## 🎯 建议的修复顺序

### 第 1 步：修复拖拽功能（必须）

**修改 App.tsx 第 2397-2400 行**，实现 `onUseAsset` 回调。

**预计时间**：10 分钟  
**影响范围**：只修改 1 个函数，不影响其他功能

### 第 2 步：确认删除UI问题（必须）

**询问用户具体的UI问题**：
- 右键菜单位置不对？
- 右键菜单样式不好看？
- 删除确认对话框不好看？
- 删除后没有反馈？

**根据用户反馈决定修复方案**。

### 第 3 步：优化整体UI（可选）

**根据用户反馈，优先修复最影响使用的问题**：
- 如果用户说"太窄"，增加宽度
- 如果用户说"分类不便"，改进分类标签
- 如果用户说"交互不明显"，增强反馈

---

## 🔍 需要用户回答的问题

### 关于删除UI问题

1. **右键菜单位置**：右键菜单是否超出屏幕边界？是否被其他元素遮挡？
2. **右键菜单样式**：右键菜单的字体、颜色、间距是否不协调？
3. **删除确认对话框**：原生的 `window.confirm` 是否太丑？需要自定义对话框吗？
4. **删除反馈**：删除后是否需要 Toast 提示？

### 关于整体UI不便

5. **布局和尺寸**：资产库是否太窄？资产卡片是否太小？
6. **分类标签**：分类标签是否太多？横向滚动是否不方便？
7. **资产卡片**：缩略图质量是否差？名称是否不易读？
8. **交互反馈**：拖拽、悬停、点击的反馈是否不明显？
9. **空状态**：空状态的文案是否不清晰？是否需要引导？
10. **其他问题**：还有哪些具体的不便之处？

---

## 💡 推荐的UI改进方案（待用户确认）

### 方案 A：增加宽度 + 3列网格

```typescript
// 修改 AssetLibraryPanel.tsx
<div className="w-96 h-full ...">  // 从 w-72 改为 w-96（384px）
  {/* 资产网格 */}
  <div className="grid grid-cols-3 gap-2">  // 从 2 列改为 3 列
    {/* 资产卡片 */}
  </div>
</div>
```

**优点**：
- 更宽的面板，资产卡片更大
- 3 列网格，浏览效率更高
- 修改简单，不影响其他功能

**缺点**：
- 占用更多屏幕空间
- 可能需要调整其他面板的位置

### 方案 B：改进分类标签（纵向布局）

```typescript
// 修改 AssetLibraryPanel.tsx
<div className="flex flex-col gap-1 p-2">  // 纵向布局
  {categories.map((cat) => (
    <button
      key={cat.value}
      onClick={() => setSelectedCategory(cat.value)}
      className={`px-3 py-2 rounded-lg text-xs font-semibold text-left ${
        selectedCategory === cat.value
          ? 'bg-blue-500 text-white'  // 蓝色背景
          : 'text-gray-600 hover:bg-gray-100'
      }`}
    >
      {cat.label}
    </button>
  ))}
</div>
```

**优点**：
- 所有分类一目了然，不需要滚动
- 选中状态更明显（蓝色背景）
- 更符合侧边栏的设计习惯

**缺点**：
- 占用更多垂直空间
- 可能需要调整资产网格的高度

### 方案 C：增强交互反馈

```typescript
// 1. 拖拽预览
const handleDragStart = (e: React.DragEvent, asset: Asset) => {
  e.dataTransfer.setData('application/asset-id', asset.id);
  e.dataTransfer.effectAllowed = 'copy';
  
  // 创建拖拽预览
  const dragImage = document.createElement('div');
  dragImage.className = 'w-32 h-32 bg-white border-2 border-blue-500 rounded-lg shadow-xl opacity-80';
  dragImage.textContent = asset.name;
  document.body.appendChild(dragImage);
  e.dataTransfer.setDragImage(dragImage, 64, 64);
  setTimeout(() => document.body.removeChild(dragImage), 0);
};

// 2. 悬停效果
<div
  className="... hover:scale-105 hover:shadow-xl transition-all"  // 从 1.02 改为 1.05
>

// 3. 点击反馈（需要添加 Toast 组件）
const handleClickAsset = (asset: Asset) => {
  onUseAsset(asset.id, { x: 0, y: 0 });
  showToast(`已添加资产"${asset.name}"到画布中心`);  // Toast 提示
};
```

**优点**：
- 拖拽预览让用户知道正在拖什么
- 更明显的悬停效果
- 点击后有明确的反馈

**缺点**：
- 需要添加 Toast 组件
- 拖拽预览可能需要调整样式

---

## 🎯 下一步行动

1. **立即修复拖拽功能**（10 分钟）
2. **询问用户具体的UI问题**（等待反馈）
3. **根据用户反馈实施UI改进**（30-60 分钟）

---

**等待用户确认后开始修复。**
