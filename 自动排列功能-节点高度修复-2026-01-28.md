# 自动排列功能 - 节点高度修复

**日期**：2026-01-28  
**状态**：✅ 已完成

---

## 🐛 问题描述

**现象**：
- 自动排列后，高度较大的节点（如竖向长方形）会从组中露出来
- 如果节点太大，会完全从组中丢失

**原因**：
`arrangeTopology` 函数使用了**固定的节点高度**（300px），但实际节点的高度可能不同（如竖向节点可能有 600px）。

```typescript
// ❌ 修复前：使用固定高度
const nodeHeight = 300;
currentY += nodeHeight + verticalGap;

// 更新组大小时也使用固定高度
const totalHeight = layers.length * nodeHeight + (layers.length - 1) * verticalGap + padding * 2;
```

---

## ✅ 修复方案

### 1. 使用实际节点高度

```typescript
// ✅ 修复后：使用实际节点高度
layer.forEach(nodeId => {
  const node = nodes.get(nodeId);
  if (node) {
    const nodeHeight = getApproxNodeHeight(node); // 获取实际高度
    maxLayerHeight = Math.max(maxLayerHeight, nodeHeight);
  }
});

// 使用这一层的最大高度
currentY += maxLayerHeight + verticalGap;
```

### 2. 根据实际节点位置计算组边界

```typescript
// 记录每个节点的实际位置和大小
const nodePositions = new Map<string, { x: number; y: number; width: number; height: number }>();

layer.forEach(nodeId => {
  const node = nodes.get(nodeId);
  if (node) {
    const nodeHeight = getApproxNodeHeight(node);
    
    // 记录节点位置和大小
    nodePositions.set(nodeId, {
      x: currentX,
      y: currentY,
      width: nodeWidth,
      height: nodeHeight
    });
  }
});

// 根据实际节点位置计算组的边界
let minX = Infinity;
let minY = Infinity;
let maxX = -Infinity;
let maxY = -Infinity;

nodePositions.forEach(pos => {
  minX = Math.min(minX, pos.x);
  minY = Math.min(minY, pos.y);
  maxX = Math.max(maxX, pos.x + pos.width);
  maxY = Math.max(maxY, pos.y + pos.height);
});

// 更新分组大小和位置
onUpdateGroup(groupId, {
  x: minX - padding,
  y: minY - padding,
  width: (maxX - minX) + padding * 2,
  height: (maxY - minY) + padding * 2,
});
```

---

## 🎯 修复效果

### 修复前
```
组高度 = 层数 × 300px + 间距
❌ 问题：如果节点高度 > 300px，会露出来
```

### 修复后
```
组高度 = 实际节点高度之和 + 间距
✅ 效果：所有节点都在组内，无论高度多少
```

---

## 🧪 测试场景

### 场景 1：混合高度节点
1. 组内有 3 个横向节点（高度 300px）
2. 组内有 1 个竖向节点（高度 600px）
3. 点击自动排列
4. **期望**：所有节点都在组内，组高度自动调整
5. **结果**：✅ 符合预期

### 场景 2：所有节点高度相同
1. 组内有 4 个横向节点（高度 300px）
2. 点击自动排列
3. **期望**：正常排列，组高度正确
4. **结果**：✅ 符合预期

### 场景 3：极端高度节点
1. 组内有 1 个超高节点（高度 1000px）
2. 点击自动排列
3. **期望**：节点不会从组中丢失
4. **结果**：✅ 符合预期

---

## 📝 代码修改

### 文件：`hooks/useGroup.ts`

**修改位置**：`arrangeTopology` 函数（第 540-650 行）

**关键改动**：
1. ✅ 移除固定的 `nodeHeight = 300`
2. ✅ 使用 `getApproxNodeHeight(node)` 获取实际高度
3. ✅ 计算每层的最大高度
4. ✅ 记录每个节点的实际位置和大小
5. ✅ 根据实际节点位置计算组边界

---

## 🎉 总结

修复完成！现在自动排列功能会：
- ✅ 使用节点的实际高度进行布局
- ✅ 根据实际节点位置计算组边界
- ✅ 确保所有节点都在组内，无论高度多少
- ✅ 支持混合高度的节点（横向 + 竖向）

**用户体验**：
- 不会再出现节点从组中露出来的情况
- 不会再出现节点从组中丢失的情况
- 组的大小会自动适应所有节点

---

**修复完成！** 🎉
