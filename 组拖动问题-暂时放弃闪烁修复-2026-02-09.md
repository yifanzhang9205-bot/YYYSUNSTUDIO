# 组拖动问题 - 暂时放弃闪烁修复

## 实施日期
2026-02-09

## 当前状态

经过多次尝试修复组拖动的闪烁问题，发现：

1. **添加复杂的修复逻辑会导致性能下降**
   - 拖动变得迟钝
   - 用户体验更差

2. **简单的方案虽然有轻微闪烁，但性能好**
   - 拖动流畅
   - 响应及时

## 当前实施的方案

### hooks/useGroup.ts（第 232-310 行）

```typescript
const endGroupDrag = useCallback((e: MouseEvent, currentScale: number) => {
  if (!dragGroupRef.current) return;

  const { id, startX, startY, mouseStartX, mouseStartY, childNodes } = dragGroupRef.current;

  // 计算最终位置
  const screenDx = e.clientX - mouseStartX;
  const screenDy = e.clientY - mouseStartY;
  const worldDx = screenDx / currentScale;
  const worldDy = screenDy / currentScale;
  const finalX = startX + worldDx;
  const finalY = startY + worldDy;

  // 清除拖动上下文
  dragGroupRef.current = null;

  // 保存历史
  onSaveHistory();

  // 更新 Store
  onUpdateGroup(id, { x: finalX, y: finalY });
  childNodes.forEach((child) => {
    onUpdateNode(child.id, {
      x: child.startX + worldDx,
      y: child.startY + worldDy,
    });
  });

  // 使用 RAF 确保 React 渲染完成后再清除 transform
  requestAnimationFrame(() => {
    setIsDraggingGroup(false);
    setDraggingGroupOffset(null);
    
    // 清除 transform
    const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
    if (groupElement) {
      groupElement.style.transform = '';
      groupElement.style.willChange = 'auto';
    }

    childNodes.forEach((child) => {
      if (child.element) {
        child.element.style.transform = '';
        child.element.style.willChange = 'auto';
      }
    });
  });
}, [onSaveHistory, onUpdateGroup, onUpdateNode]);
```

## 特点

### 优点
- ✅ 拖动流畅，性能好
- ✅ 代码简单，易于维护
- ✅ 不会导致迟钝

### 缺点
- ❌ 松开鼠标后有轻微闪烁
- ❌ 闪烁持续约 0.2 秒（transition 时间）

## 尝试过的方案

### 方案 1-8：各种 setTimeout 和强制重绘
- 结果：仍然闪烁

### 方案 9：先清除 DOM，再更新 Store
- 结果：拖动变得非常迟钝，用户体验更差

## 结论

**暂时接受轻微闪烁，保持性能优先。**

理由：
1. 闪烁只在松开鼠标的瞬间发生，持续时间很短（0.2秒）
2. 拖动过程中完全流畅，这是用户最关心的
3. 复杂的修复方案会导致性能下降，得不偿失

## 未来优化方向

如果要彻底解决闪烁问题，可能需要：

1. **重构组拖动的实现方式**
   - 不使用 transform + left/top 的混合方式
   - 统一使用一种定位方式

2. **使用 CSS 变量传递偏移量**
   - 类似节点拖动的方式
   - 让连线能读取偏移量

3. **使用 Web Animations API**
   - 更精确地控制动画时序
   - 避免 CSS transition 的时序问题

但这些方案都需要大量重构，风险较高。

## 建议

**暂时保持当前方案，优先处理其他更重要的功能。**

闪烁问题虽然存在，但不影响核心功能使用，可以作为低优先级的优化项。

---

**实施时间**：2026-02-09  
**实施人员**：Kiro AI Assistant  
**状态**：✅ 完成，暂时接受轻微闪烁

---

## 用户反馈

如果用户对闪烁问题非常在意，可以考虑：
1. 调整 transition 时间（从 0.2s 改为 0.1s）
2. 使用 ease-out 缓动函数，让闪烁不那么明显
3. 完全禁用组的 transition（但会失去平滑移动的效果）

当前方案是性能和视觉效果的平衡点。
