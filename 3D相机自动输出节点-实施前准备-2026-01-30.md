# 3D 相机自动输出节点 - 实施前准备

**日期**: 2026-01-30  
**状态**: 等待架构迁移完成后实施

---

## 📋 改动内容清单（事无巨细）

### 改动 1：修改 3D 相机生成完成后的逻辑

**位置**: `hooks/useNodeActions.ts` 中的 `handleMultiAngleCameraGenerate` 函数（迁移后）

**当前逻辑**（App.tsx 第 1074-1420 行）:
```typescript
// 生成完成后，只更新节点数据
handleNodeUpdate(id, { 
    gridImages: blobUrls,
    image: blobUrls[0]
});
```

**新逻辑**:
```typescript
// 生成完成后：
// 1. 更新 3D 相机节点数据
handleNodeUpdate(id, { 
    gridImages: blobUrls,
    image: blobUrls[0]
});

// 2. 检测输出连接
const outputConnections = useConnectionStore.getState().getOutputConnections(id);

// 3. 自动创建九宫格节点
const gridNodeCounter = getGridNodeCounter(); // 全局计数器
const newNodeId = nodeRegistry.createNode(NodeType.GRID_SPLITTER, {
    x: calculateNewNodePosition(node, allNodes, gridNodeCounter),
    y: node.y + (gridNodeCounter * 150),
    title: `九宫格 ${gridNodeCounter + 1}`,
    data: {
        inputImage: blobUrls[0],
        croppedImages: [],
        selectedIndex: -1,
    },
});

// 4. 添加节点到 Store
useNodeStore.getState().addNode(newNodeId);

// 5. 自动连接
useConnectionStore.getState().addConnection({
    from: id,
    to: newNodeId.id,
});

// 6. 更新计数器
incrementGridNodeCounter();
```

**涉及的文件**:
- `hooks/useNodeActions.ts` - 新增函数
- `core/utils/geometry.ts` - 新增位置计算函数
- `types.ts` - 可能需要新增类型定义

---

### 改动 2：添加位置计算逻辑

**位置**: `core/utils/geometry.ts`

**新增函数 1: `calculateNewNodePosition`**
```typescript
/**
 * 计算新节点的位置
 * @param sourceNode 源节点（3D 相机）
 * @param existingNodes 所有现有节点
 * @param offset 偏移量（用于多次生成）
 * @returns 新节点的位置 {x, y}
 */
export function calculateNewNodePosition(
  sourceNode: AppNode,
  existingNodes: AppNode[],
  offset: number = 0
): { x: number; y: number } {
  // 1. 优先位置：右侧 + 300px
  const preferredX = sourceNode.x + (sourceNode.width || 420) + 300;
  const preferredY = sourceNode.y + (offset * 150);
  
  // 2. 检测是否有空间（200×200 区域）
  const hasSpace = !existingNodes.some(node => 
    isOverlapping(
      { x: preferredX, y: preferredY },
      { x: node.x, y: node.y },
      200,
      200
    )
  );
  
  if (hasSpace) {
    return { x: preferredX, y: preferredY };
  }
  
  // 3. 自动寻找空白区域
  return findEmptySpace(sourceNode, existingNodes, 200, 200);
}
```

**新增函数 2: `isOverlapping`**
```typescript
/**
 * 检测两个矩形是否重叠
 * @param pos1 第一个矩形的位置
 * @param pos2 第二个矩形的位置
 * @param width 矩形宽度
 * @param height 矩形高度
 * @returns 是否重叠
 */
export function isOverlapping(
  pos1: { x: number; y: number },
  pos2: { x: number; y: number },
  width: number,
  height: number
): boolean {
  return !(
    pos1.x + width < pos2.x ||
    pos1.x > pos2.x + width ||
    pos1.y + height < pos2.y ||
    pos1.y > pos2.y + height
  );
}
```

**新增函数 3: `findEmptySpace`**
```typescript
/**
 * 寻找空白区域
 * @param sourceNode 源节点
 * @param existingNodes 所有现有节点
 * @param width 需要的宽度
 * @param height 需要的高度
 * @returns 空白区域的位置
 */
export function findEmptySpace(
  sourceNode: AppNode,
  existingNodes: AppNode[],
  width: number,
  height: number
): { x: number; y: number } {
  // 搜索策略：从源节点开始，螺旋式向外搜索
  const step = 50; // 每次移动 50px
  const maxAttempts = 100; // 最多尝试 100 次
  
  for (let i = 0; i < maxAttempts; i++) {
    const angle = (i * 30) % 360; // 每次旋转 30 度
    const distance = Math.floor(i / 12) * 200 + 300; // 每 12 次增加距离
    
    const x = sourceNode.x + Math.cos(angle * Math.PI / 180) * distance;
    const y = sourceNode.y + Math.sin(angle * Math.PI / 180) * distance;
    
    const hasSpace = !existingNodes.some(node =>
      isOverlapping({ x, y }, { x: node.x, y: node.y }, width, height)
    );
    
    if (hasSpace) {
      return { x, y };
    }
  }
  
  // 如果找不到空白区域，就放在右下角（可能重叠）
  return {
    x: sourceNode.x + 300,
    y: sourceNode.y + 300,
  };
}
```

---

### 改动 3：添加生成中禁用交互

**位置**: `components/MultiAngleCameraNode.tsx`

**修改点 1: 添加遮罩层**
```typescript
{/* 生成中遮罩 */}
{isWorking && (
  <div className="absolute inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-10">
    <div className="flex flex-col items-center gap-3">
      <div className="w-12 h-12 border-4 border-white/20 border-t-white rounded-full animate-spin" />
      <span className="text-[13px] text-white/80 font-medium">生成中...</span>
    </div>
  </div>
)}
```

**修改点 2: 禁用 3D 视口交互**
```typescript
<div
  ref={containerRef}
  className={`
    ${isDragging ? 'cursor-grabbing' : 'cursor-grab'}
    ${isWorking ? 'pointer-events-none' : ''}  // ✅ 禁止交互
  `}
  onMouseDown={isWorking ? undefined : handleViewportMouseDown}  // ✅ 禁止拖动
  onWheel={isWorking ? undefined : handleWheel}  // ✅ 禁止滚轮
/>
```

**修改点 3: 禁用滑块**
```typescript
<input
  type="range"
  disabled={isWorking}  // ✅ 禁用滑块
  className={isWorking ? 'opacity-50 cursor-not-allowed' : ''}
  // ...
/>
```

**修改点 4: 禁用按钮**
```typescript
<button
  onClick={onGenerate}
  disabled={isWorking}  // ✅ 禁用按钮
  className="..."
>
  {isWorking ? (
    <>
      <div className="w-4 h-4 border-2 border-black/20 border-t-black rounded-full animate-spin" />
      <span>生成中...</span>
    </>
  ) : (
    <>
      <Play size={14} fill="black" />
      <span>生成</span>
    </>
  )}
</button>
```

---

### 改动 4：添加全局计数器

**位置**: `hooks/useNodeActions.ts` 或 `core/stores/nodeStore.ts`

**方案 A: 使用 ref（推荐）**
```typescript
// hooks/useNodeActions.ts
const gridNodeCounterRef = useRef(0);

const getGridNodeCounter = () => gridNodeCounterRef.current;
const incrementGridNodeCounter = () => {
  gridNodeCounterRef.current++;
};
```

**方案 B: 使用 Store**
```typescript
// core/stores/nodeStore.ts
export interface NodeStore {
  // ... 现有字段
  gridNodeCounter: number;
  incrementGridNodeCounter: () => void;
}

// 实现
gridNodeCounter: 0,
incrementGridNodeCounter: () => set((state) => {
  state.gridNodeCounter++;
}),
```

---

## 📊 改动影响分析

### 影响的文件

| 文件 | 改动类型 | 改动量 | 风险等级 |
|------|---------|--------|---------|
| `hooks/useNodeActions.ts` | 新建 | 约 500 行 | 🟢 低（新文件） |
| `core/utils/geometry.ts` | 新增函数 | 约 100 行 | 🟢 低（纯函数） |
| `components/MultiAngleCameraNode.tsx` | 修改 UI | 约 50 行 | 🟡 中（UI 改动） |
| `App.tsx` | 调用 Hook | 约 10 行 | 🟢 低（只是调用） |
| `types.ts` | 可能新增类型 | 约 10 行 | 🟢 低（类型定义） |

### 功能影响

| 功能 | 影响 | 说明 |
|------|------|------|
| 3D 相机生成 | ✅ 增强 | 自动创建九宫格节点 |
| 九宫格节点 | ✅ 增强 | 自动接收图片 |
| 节点连接 | ✅ 增强 | 自动连接 |
| 其他节点 | ✅ 无影响 | 不涉及其他节点 |
| 性能 | ✅ 无影响 | 只是逻辑调整 |

---

## ✅ 验收标准

### 功能测试
- [ ] 第一次生成：自动创建九宫格节点 1 + 自动连接
- [ ] 第二次生成：自动创建九宫格节点 2 + 自动连接
- [ ] 第三次生成：自动创建九宫格节点 3 + 自动连接
- [ ] 节点位置：优先右侧，智能避让
- [ ] 节点命名：按序号命名（九宫格 1, 2, 3...）
- [ ] 生成中：按钮禁用 + 3D 视口禁止交互

### 边界测试
- [ ] 右侧没有空间：自动寻找空白区域
- [ ] 画布已满：仍然能创建节点（可能重叠，但有连线）
- [ ] 快速连续点击生成：只执行一次（防止重复）

### 性能测试
- [ ] 创建 10 个九宫格节点：不卡顿
- [ ] 生成中拖动画布：流畅

---

## 🚀 实施顺序

1. **等待架构迁移完成**（`handleNodeAction` → `useNodeActions.ts`）
2. **添加位置计算函数**（`core/utils/geometry.ts`）
3. **修改 3D 相机生成逻辑**（`hooks/useNodeActions.ts`）
4. **添加生成中禁用交互**（`components/MultiAngleCameraNode.tsx`）
5. **测试验收**

---

**状态**: ⏸️ 等待架构迁移完成后实施
