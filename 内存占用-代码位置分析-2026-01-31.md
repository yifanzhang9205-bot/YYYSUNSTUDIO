# 内存占用 - 代码位置分析

> 📅 2026-01-31  
> 🎯 目标：找出所有负责处理图片、占用内存的代码位置

---

## 🔴 问题 1：拖入图片使用 Base64（内存杀手）

### 位置 1：App.tsx - handleCanvasDrop（主要问题）

**文件：** `App.tsx` 第 1514-1653 行

**问题代码：**
```typescript
const handleCanvasDrop = (e: React.DragEvent) => {
  // ...
  const reader = new FileReader();
  reader.onload = (event) => {
    const res = event.target?.result as string; // ❌ Base64 字符串
    // 直接创建节点，没有任何优化
    addNode({
      data: { image: res } // ❌ 18MB Base64 存入 Store
    });
  };
  reader.readAsDataURL(file); // ❌ 转成 Base64（体积膨胀 33%）
};
```

**内存占用：**
- 5MB 图片 → 6.65MB Base64 字符串
- 5 张图片 = 33MB Base64 字符串留在内存中
- 没有压缩、没有懒加载、没有虚拟化

**影响：**
- 🔴 **拖入图片格外卡顿**
- 🔴 **内存占用巨大**
- 🔴 **浏览器解码所有图片，阻塞主线程**

---

### 位置 2：Node.tsx - handleUploadImage

**文件：** `components/Node.tsx` 第 438 行

**问题代码：**
```typescript
const handleUploadImage = (e: React.ChangeEvent<HTMLInputElement>) => { 
  const file = e.target.files?.[0]; 
  if (file) { 
    const reader = new FileReader(); 
    reader.onload = (e) => onUpdate(node.id, { image: e.target?.result as string }); 
    reader.readAsDataURL(file); // ❌ Base64
  }
};
```

**影响：**
- 点击上传按钮也使用 Base64
- 和拖入图片一样的问题

---

### 位置 3：Node.tsx - handleUploadVideo

**文件：** `components/Node.tsx` 第 437 行

**问题代码：**
```typescript
const handleUploadVideo = (e: React.ChangeEvent<HTMLInputElement>) => { 
  const file = e.target.files?.[0]; 
  if (file) { 
    const reader = new FileReader(); 
    reader.onload = (e) => onUpdate(node.id, { videoUri: e.target?.result as string }); 
    reader.readAsDataURL(file); // ❌ Base64
  }
};
```

**影响：**
- 视频文件更大，Base64 后更恐怖
- 可能导致浏览器崩溃

---

### 位置 4：GridSplitterNode.tsx - 上传替换

**文件：** `components/GridSplitterNode.tsx` 第 138-155 行

**问题代码：**
```typescript
const reader = new FileReader();
reader.onload = async (e) => {
  const dataUrl = e.target?.result as string; // ❌ Base64
  // ...
};
reader.readAsDataURL(file); // ❌ Base64
```

---

### 位置 5：其他组件也使用 Base64

**文件：**
- `components/SonicStudio.tsx` 第 149-151, 175-177 行
- `components/SmartSequenceDock.tsx` 第 132-138, 152-158 行
- `components/SketchEditor.tsx` 第 103-109 行

**影响：**
- 整个项目都在使用 Base64
- 没有统一的图片处理策略

---

## 🔴 问题 2：清理不彻底（内存泄漏）

### 位置 1：useAssetHistory.ts - handleDeleteMultipleAssets

**文件：** `hooks/useAssetHistory.ts`

**问题代码：**
```typescript
const handleDeleteMultipleAssets = async (assetIds: string[]) => {
  // 1. ✅ 删除 IndexedDB Blob
  await deleteFromStorage(id);
  
  // 2. ✅ 更新 Store
  assetHistoryStore.setState({ assets: newAssets });
  
  // 3. ❌ 但是没有清理 Blob URL！
  // 4. ❌ 没有清理 DOM 中的 <img> 引用！
  // 5. ❌ 没有强制垃圾回收！
};
```

**缺少的清理步骤：**
1. ❌ 没有调用 `URL.revokeObjectURL()`
2. ❌ 没有清空节点的 `data.image`
3. ❌ 没有强制 React 重新渲染
4. ❌ 没有清理浏览器缓存

**影响：**
- 🔴 **清理后内存没有真正释放**
- 🔴 **需要重启浏览器才流畅**
- 🔴 **Blob URL 泄漏**

---

### 位置 2：blobStorage.ts - revokeBlobUrl

**文件：** `services/blobStorage.ts` 第 213-217 行

**现有代码：**
```typescript
export const revokeBlobUrl = (blobUrl: string): void => {
  if (blobUrl && blobUrl.startsWith('blob:')) {
    URL.revokeObjectURL(blobUrl);
    console.log(`[BlobStorage] 清理 URL: ${blobUrl}`);
  }
};
```

**问题：**
- ✅ 函数存在
- ❌ 但是没有被调用！
- ❌ `handleDeleteMultipleAssets` 没有使用这个函数

---

### 位置 3：imageOptimizer.ts - revokeMultiLevelImage

**文件：** `services/imageOptimizer.ts` 第 168-178 行

**现有代码：**
```typescript
export const revokeMultiLevelImage = (levels: ImageLevel) => {
  if (levels.thumbnail && levels.thumbnail.startsWith('blob:')) {
    URL.revokeObjectURL(levels.thumbnail);
  }
  if (levels.preview && levels.preview.startsWith('blob:')) {
    URL.revokeObjectURL(levels.preview);
  }
  if (levels.original && levels.original.startsWith('blob:')) {
    URL.revokeObjectURL(levels.original);
  }
};
```

**问题：**
- ✅ 函数存在
- ❌ 但是没有被调用！
- ❌ 多级图片管理功能没有启用

---

## 🔴 问题 3：没有虚拟化渲染（渲染所有节点）

### 位置：App.tsx - 渲染节点

**文件：** `App.tsx`

**问题代码：**
```typescript
{Array.from(nodes.values()).map(node => (
  <Node key={node.id} node={node} ... /> // ❌ 渲染所有节点
))}
```

**问题：**
- ❌ 没有 Viewport Culling（视口裁剪）
- ❌ 没有 Intersection Observer
- ❌ 拖入 5 张图片就渲染 5 个节点
- ❌ 拖入 100 张图片就渲染 100 个节点

**影响：**
- 🔴 **节点越多越卡**
- 🔴 **所有图片都解码，阻塞主线程**
- 🔴 **内存占用线性增长**

---

## 🔴 问题 4：没有懒加载和异步解码

### 位置：Node.tsx - 图片渲染

**文件：** `components/Node.tsx` 第 812 行

**问题代码：**
```typescript
<img 
  ref={mediaRef as any} 
  src={node.data.image} 
  className="..." 
  draggable={false} 
  // ❌ 没有 loading="lazy"
  // ❌ 没有 decoding="async"
/>
```

**问题：**
- ❌ 浏览器立即加载所有图片
- ❌ 同步解码，阻塞主线程
- ❌ 没有优先级控制

**影响：**
- 🔴 **拖入图片时卡顿**
- 🔴 **主线程被阻塞**

---

## 📊 内存占用分析

### 当前内存占用（拖入 5 张 5MB 图片）

| 项目 | 单张 | 5 张 | 说明 |
|------|------|------|------|
| **原始文件** | 5MB | 25MB | 磁盘大小 |
| **Base64 字符串** | 6.65MB | 33MB | 膨胀 33% |
| **解码后图片数据** | 16MB | 80MB | 浏览器内存 |
| **DOM 引用** | 1MB | 5MB | React 状态 |
| **总计** | ~24MB | **~118MB** | 每张图片 |

### 为什么清理后不流畅？

**清理前：**
- Base64 字符串：33MB
- 解码后图片：80MB
- DOM 引用：5MB
- **总计：118MB**

**清理后（当前实现）：**
- ✅ IndexedDB Blob 已删除
- ✅ Store 已更新
- ❌ Blob URL 没有 revoke：**80MB 泄漏**
- ❌ DOM 引用没有清理：**5MB 泄漏**
- ❌ 浏览器缓存没有清理：**未知**
- **总计：~85MB 泄漏**

**重启浏览器后：**
- ✅ 所有内存释放
- ✅ 浏览器缓存清空
- **总计：0MB**

---

## 💡 解决方案

### 方案 1：修复拖入图片（紧急）

**目标：** 拖入图片不再使用 Base64

**修改位置：**
1. `App.tsx` - `handleCanvasDrop`
2. `components/Node.tsx` - `handleUploadImage`
3. `components/Node.tsx` - `handleUploadVideo`

**修改方案：**
```typescript
// ❌ 旧代码
reader.readAsDataURL(file);

// ✅ 新代码
reader.readAsArrayBuffer(file);
reader.onload = async (e) => {
  const arrayBuffer = e.target?.result as ArrayBuffer;
  const blob = new Blob([arrayBuffer], { type: file.type });
  const blobUrl = URL.createObjectURL(blob);
  // 存储到 IndexedDB
  await saveToStorage(`blob-${nodeId}`, blob);
  // 使用 Blob URL
  addNode({ data: { image: blobUrl } });
};
```

---

### 方案 2：修复清理逻辑（必须）

**目标：** 清理后立即释放内存

**修改位置：**
1. `hooks/useAssetHistory.ts` - `handleDeleteMultipleAssets`

**修改方案：**
```typescript
const handleDeleteMultipleAssets = async (assetIds: string[]) => {
  // 1. 先 revoke 所有 Blob URL
  assetIds.forEach(id => {
    const asset = assets.find(a => a.id === id);
    if (asset?.url) {
      URL.revokeObjectURL(asset.url);
    }
  });
  
  // 2. 删除 IndexedDB Blob
  await Promise.all(assetIds.map(id => deleteFromStorage(id)));
  
  // 3. 清空节点的 data.image
  const nodes = nodeStore.getState().nodes;
  nodes.forEach(node => {
    if (assetIds.includes(node.data.assetId)) {
      nodeStore.getState().updateNode(node.id, { image: undefined });
    }
  });
  
  // 4. 更新 Store
  assetHistoryStore.setState({ assets: newAssets });
  
  // 5. 强制垃圾回收（提示浏览器）
  if (window.gc) window.gc();
};
```

---

### 方案 3：添加虚拟化渲染（根本解决）

**目标：** 只渲染可见节点

**修改位置：**
1. `App.tsx` - 节点渲染逻辑

**修改方案：**
```typescript
// 1. 计算可见区域
const viewportBounds = {
  left: -pan.x / scale,
  top: -pan.y / scale,
  right: (-pan.x + window.innerWidth) / scale,
  bottom: (-pan.y + window.innerHeight) / scale
};

// 2. 只渲染可见节点
{Array.from(nodes.values())
  .filter(node => {
    // 检查节点是否在可见区域内
    return node.x + node.width > viewportBounds.left &&
           node.x < viewportBounds.right &&
           node.y + node.height > viewportBounds.top &&
           node.y < viewportBounds.bottom;
  })
  .map(node => (
    <Node key={node.id} node={node} ... />
  ))
}
```

---

### 方案 4：添加懒加载和异步解码（简单有效）

**目标：** 图片进入可见区域时才加载

**修改位置：**
1. `components/Node.tsx` - 图片渲染

**修改方案：**
```typescript
<img 
  ref={mediaRef as any} 
  src={node.data.image} 
  className="..." 
  draggable={false} 
  loading="lazy"        // ✅ 懒加载
  decoding="async"      // ✅ 异步解码
/>
```

---

## 🎯 实施优先级

| 优先级 | 方案 | 效果 | 难度 | 时间 |
|--------|------|------|------|------|
| 🔴 **P0** | 方案 1：修复拖入图片 | 立即见效 | 简单 | 5 分钟 |
| 🔴 **P0** | 方案 4：懒加载 + 异步解码 | 立即见效 | 简单 | 2 分钟 |
| 🟡 **P1** | 方案 2：修复清理逻辑 | 清理后流畅 | 中等 | 15 分钟 |
| 🟢 **P2** | 方案 3：虚拟化渲染 | 根本解决 | 复杂 | 30 分钟 |

---

## 📝 总结

**问题根源：**
1. 🔴 拖入图片使用 Base64（体积膨胀 33%，内存占用巨大）
2. 🔴 清理不彻底（Blob URL 没有 revoke，DOM 引用没有清理）
3. 🔴 没有虚拟化渲染（渲染所有节点）
4. 🔴 没有懒加载和异步解码（浏览器同步解码所有图片）

**建议：**
- 先做方案 1 + 方案 4（7 分钟，立即见效）
- 再做方案 2（15 分钟，彻底修复清理）
- 最后做方案 3（30 分钟，根本解决）

**预期效果：**
- 拖入图片不再卡顿
- 清理后立即流畅
- 拖入 100 张图片也不卡
