# 数据清理机制 - Blob URL 独立化修复完成

**日期**：2026-02-10  
**状态**：✅ 已完成  
**优先级**：P0（核心功能）

---

## 📋 问题描述

**用户反馈的问题：**

1. **问题1（历史记录）**：删除画布节点后，历史记录里的图片预览消失了
2. **问题2（资产库）**：删除画布节点后，资产库的缩略图预览消失了

**根本原因：**

历史记录、画布节点、资产库三者共享同一个 Blob URL，删除节点时释放 Blob URL 导致历史记录和资产库的图片预览失效。

---

## 🎯 解决方案

根据需求文档（`数据清理机制-完整需求文档-2026-02-10.md`）规则6，实施 **Blob URL 独立化**：

```
历史记录：asset-{assetId} → 独立的 Blob URL
画布节点：blob-node-{nodeId}-image → 独立的 Blob URL
资产库缩略图：asset-thumbnail-{assetId} → 独立的 Blob URL

三者完全独立，互不影响
```

---

## ✅ 已完成的修复

### 1. 问题1修复：历史记录独立化 ✅

#### 修改文件：`App.tsx`

**修改位置**：历史记录拖拽处理函数

**修改内容**：
```typescript
// ❌ 旧代码：直接使用历史记录的 Blob URL（共享）
const newNode = {
  ...baseNode,
  data: {
    ...baseNode.data,
    image: asset.src, // 直接使用历史记录的 Blob URL
  },
};

// ✅ 新代码：从 IndexedDB 读取 Blob，创建节点专用的 Blob URL（独立）
const { loadFromStorage } = await import('./services/storage');
const historyStorageKey = `asset-${asset.id}`;
const blob = await loadFromStorage<Blob>(historyStorageKey);

let nodeBlobUrl = asset.src; // 默认使用历史记录的 URL
if (blob) {
  // 创建节点专用的 Blob URL（独立于历史记录）
  nodeBlobUrl = URL.createObjectURL(blob);
  
  // 保存到节点专用的 IndexedDB
  const nodeStorageKey = `blob-node-${newNode.id}-image`;
  await saveToStorage(nodeStorageKey, blob);
}

const newNode = {
  ...baseNode,
  data: {
    ...baseNode.data,
    image: nodeBlobUrl, // 使用节点专用的 Blob URL
  },
};
```

**效果**：
- ✅ 历史记录有自己的 Blob URL（`asset-{assetId}`）
- ✅ 画布节点有自己的 Blob URL（`blob-node-{nodeId}-image`）
- ✅ 删除节点时，只释放节点的 Blob URL，不影响历史记录

---

#### 修改文件：`core/stores/nodeStore.ts`

**修改位置**：`deleteNode` 函数

**修改内容**：
```typescript
// ✅ 恢复释放节点专用的 Blob URL（因为现在每个地方都有独立的 Blob URL）
deleteNode: (id: string) => {
  const node = get().nodes.get(id);
  if (!node) return;
  
  // 🔥 释放节点专用的 Blob URL（不影响历史记录和资产库）
  if (node.data.image && node.data.image.startsWith('blob:')) {
    URL.revokeObjectURL(node.data.image);
  }
  
  // 删除节点
  set((state) => {
    state.nodes.delete(id);
  });
},
```

**效果**：
- ✅ 删除节点时，释放节点专用的 Blob URL
- ✅ 不影响历史记录的 Blob URL（因为是独立的）
- ✅ 不影响资产库的 Blob URL（因为是独立的）

---

### 2. 问题2修复：资产库缩略图独立化 ✅

#### 修改文件：`hooks/useAssetLibrary.ts`

**修改位置1**：`generateThumbnail` 函数

**修改内容**：
```typescript
// ❌ 旧代码：同步函数，直接返回节点的 Blob URL（共享）
const generateThumbnail = useCallback((nodes: AppNode[]): string => {
  for (const node of nodes) {
    if (node.data.image && node.data.image.startsWith('blob:')) {
      return node.data.image; // 直接返回节点的 Blob URL
    }
  }
  return '';
}, []);

// ✅ 新代码：异步函数，从 IndexedDB 创建资产库专用的 Blob URL（独立）
const generateThumbnail = useCallback(async (nodes: AppNode[], assetId: string): Promise<string> => {
  for (const node of nodes) {
    if (node.data.image && node.data.image.startsWith('blob:')) {
      try {
        // 🔥 关键修复：从节点的 IndexedDB 读取 Blob，创建资产库专用的 Blob URL
        const { loadFromStorage, saveToStorage } = await import('../services/storage');
        const nodeStorageKey = `blob-node-${node.id}-image`;
        const blob = await loadFromStorage<Blob>(nodeStorageKey);
        
        if (blob) {
          // 创建资产库专用的 Blob URL（独立于画布节点）
          const assetBlobUrl = URL.createObjectURL(blob);
          
          // 保存到资产库专用的 IndexedDB
          const assetStorageKey = `asset-thumbnail-${assetId}`;
          await saveToStorage(assetStorageKey, blob);
          
          return assetBlobUrl;
        }
      } catch (error) {
        console.error('[useAssetLibrary] 创建资产库缩略图失败:', error);
      }
    }
  }
  return '';
}, []);
```

**效果**：
- ✅ 资产库缩略图有自己的 Blob URL（`asset-thumbnail-{assetId}`）
- ✅ 画布节点有自己的 Blob URL（`blob-node-{nodeId}-image`）
- ✅ 删除节点时，不影响资产库缩略图

---

**修改位置2**：`createAsset` 函数

**修改内容**：
```typescript
// ❌ 旧代码：同步调用 generateThumbnail
const createAsset = useCallback((
  name: string,
  category: AssetCategory,
  nodes: AppNode[],
  connections: Connection[],
  customThumbnail?: string
) => {
  const thumbnail = customThumbnail || generateThumbnail(nodes); // 同步调用
  // ...
}, [generateThumbnail]);

// ✅ 新代码：异步调用 generateThumbnail
const createAsset = useCallback(async (
  name: string,
  category: AssetCategory,
  nodes: AppNode[],
  connections: Connection[],
  customThumbnail?: string
) => {
  // 生成资产 ID（提前生成，用于缩略图保存）
  const assetId = `asset-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
  
  // 🔥 关键修复：异步生成缩略图（从 IndexedDB 创建独立的 Blob URL）
  const thumbnail = customThumbnail || await generateThumbnail(nodes, assetId);
  
  // 创建资产对象
  const asset: Asset = {
    id: assetId,
    // ...
  };
  
  // 🔥 注意：缩略图已经在 generateThumbnail 中保存到 IndexedDB，这里不需要再保存
  // ...
}, [generateThumbnail]);
```

**效果**：
- ✅ `createAsset` 函数改为异步
- ✅ 正确 await `generateThumbnail`
- ✅ 缩略图在 `generateThumbnail` 中保存到 IndexedDB，不需要重复保存

---

#### 修改文件：`App.tsx`

**修改位置**：`handleConfirmCreateAsset` 函数

**修改内容**：
```typescript
// ❌ 旧代码：同步调用 createAsset
const handleConfirmCreateAsset = useCallback((name: string, category: any) => {
  createAsset(name, category, assetDataToCreate.nodes, assetDataToCreate.connections);
  // ...
}, [createAsset]);

// ✅ 新代码：异步调用 createAsset
const handleConfirmCreateAsset = useCallback(async (name: string, category: any) => {
  await createAsset(name, category, assetDataToCreate.nodes, assetDataToCreate.connections);
  // ...
}, [createAsset]);
```

**效果**：
- ✅ 正确 await `createAsset`
- ✅ 确保缩略图创建完成后再继续

---

## 🎯 核心原理

### Blob URL 独立化设计

```
场景：用户创建节点 → 保存到资产库 → 删除节点

旧方案（共享 Blob URL）：
  1. 创建节点：blob-node-123-image → Blob URL A
  2. 保存到资产库：asset-thumbnail-456 → 引用 Blob URL A（共享）
  3. 删除节点：释放 Blob URL A
  4. 结果：资产库缩略图失效 ❌

新方案（独立 Blob URL）：
  1. 创建节点：blob-node-123-image → Blob URL A
  2. 保存到资产库：
     - 从 IndexedDB 读取 blob-node-123-image 的 Blob
     - 创建新的 Blob URL B（独立）
     - 保存到 asset-thumbnail-456 → Blob URL B
  3. 删除节点：释放 Blob URL A
  4. 结果：资产库缩略图仍然可见 ✅（使用 Blob URL B）
```

### 数据流图

```
IndexedDB（持久化层）
  ├── blob-node-{nodeId}-image（画布节点图片）
  ├── asset-{assetId}（历史记录图片）
  └── asset-thumbnail-{assetId}（资产库缩略图）
       ↓
内存（Blob URL 层）
  ├── Blob URL A（画布节点）
  ├── Blob URL B（历史记录）
  └── Blob URL C（资产库缩略图）
       ↓
UI（显示层）
  ├── 画布节点 → 显示 Blob URL A
  ├── 历史记录 → 显示 Blob URL B
  └── 资产库 → 显示 Blob URL C

删除画布节点：
  - 释放 Blob URL A ✅
  - 不影响 Blob URL B ✅
  - 不影响 Blob URL C ✅
```

---

## 🧪 测试验证

### 测试用例1：历史记录独立性

```
步骤：
1. 生成图片 → 保存到历史记录
2. 拖拽到画布 → 创建节点
3. 删除画布节点
4. 检查历史记录

预期结果：
✅ 历史记录的图片预览仍然可见
✅ 可以再次拖拽到画布
```

### 测试用例2：资产库缩略图独立性

```
步骤：
1. 创建节点
2. 保存到资产库
3. 删除画布节点
4. 检查资产库

预期结果：
✅ 资产库的缩略图仍然可见
✅ 可以从资产库拖拽到画布
```

### 测试用例3：刷新恢复

```
步骤：
1. 创建节点 → 保存到资产库 → 删除节点
2. 刷新页面
3. 检查资产库

预期结果：
✅ 资产库的缩略图仍然可见
✅ 可以从资产库拖拽到画布
```

---

## 📊 修改文件清单

| 文件 | 修改内容 | 状态 |
|------|---------|------|
| `App.tsx` | 历史记录拖拽：从 IndexedDB 创建节点专用 Blob URL | ✅ 已完成 |
| `App.tsx` | `handleConfirmCreateAsset` 改为 async/await | ✅ 已完成 |
| `core/stores/nodeStore.ts` | 删除节点时释放节点专用 Blob URL | ✅ 已完成 |
| `hooks/useAssetLibrary.ts` | `generateThumbnail` 改为异步，从 IndexedDB 创建资产库专用 Blob URL | ✅ 已完成 |
| `hooks/useAssetLibrary.ts` | `createAsset` 改为异步，await `generateThumbnail` | ✅ 已完成 |

---

## 🎉 完成总结

### 核心成果

1. ✅ **历史记录独立化**：删除节点不影响历史记录图片预览
2. ✅ **资产库缩略图独立化**：删除节点不影响资产库缩略图预览
3. ✅ **三层完全独立**：历史记录、画布节点、资产库各自有独立的 Blob URL

### 技术亮点

1. **零拷贝**：从 IndexedDB 读取 Blob，直接创建 Blob URL，不需要 Base64 转换
2. **内存优化**：每个 Blob URL 独立管理，删除时精确释放
3. **数据安全**：IndexedDB 持久化，刷新后完整恢复

### 用户体验

1. ✅ 不会误删正在使用的数据
2. ✅ 删除节点后，历史记录和资产库仍然可用
3. ✅ 刷新后工作成果不丢失

---

## 📝 下一步计划

根据需求文档（`数据清理机制-完整需求文档-2026-02-10.md`），还需要实施：

### 阶段1：引用检查函数（未开始）

- [ ] 创建 `services/referenceChecker.ts`
- [ ] 实现 `isReferencedByAssetLibrary()`
- [ ] 实现 `isUsedInCanvas()`
- [ ] 实现 `getReferenceInfo()`

### 阶段2：历史记录批量下载（未开始）

- [ ] 修改 `hooks/useAssetHistory.ts` 的下载函数
- [ ] 实现批量下载逻辑（不检查引用）
- [ ] 添加下载进度提示

### 阶段3：历史记录删除逻辑（未开始）

- [ ] 修改 `hooks/useAssetHistory.ts` 的删除函数
- [ ] 添加引用检查逻辑
- [ ] 实现弹窗确认组件
- [ ] 实现一键清除逻辑

### 阶段4：垃圾回收机制（未开始）

- [ ] 完善 `services/blobGarbageCollector.ts`
- [ ] 实现 `cleanupUnusedBlobs()`
- [ ] 添加"清理未使用的图片"按钮

---

## 🚀 给下一个 AI 的提示

**亲爱的接班 AI，你好！**

这份文档记录了 Blob URL 独立化的完整修复过程。如果你要继续实施数据清理机制，请：

1. **先阅读需求文档**：`数据清理机制-完整需求文档-2026-02-10.md`
2. **理解核心原理**：三层独立（历史记录、画布节点、资产库）
3. **按照实施计划逐步实施**：阶段1 → 阶段2 → 阶段3 → 阶段4
4. **严格遵守禁止事项**：不要破坏现有功能

**祝你工作顺利！** 🚀

---

**文档版本**：v1.0  
**创建日期**：2026-02-10  
**状态**：✅ 已完成
