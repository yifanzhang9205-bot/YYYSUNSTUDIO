# 一键整理功能调试指南

## 问题描述
用户反馈一键整理功能"不对"，从来没有正确使用过。

## 已添加的调试日志

### 1. 函数调用检测
```
[一键整理] 函数被调用 {
  selectedGroupId: 'g-xxx',
  hasGroups: true/false,
  hasNodes: true/false
}
```
**作用：** 确认按钮点击是否触发了函数

### 2. Group 查找检测
```
[一键整理] 找到 Group {
  groupId: 'g-xxx',
  groupBounds: { x, y, width, height },
  totalNodes: 5
}
```
**作用：** 确认是否找到了选中的 Group

### 3. 节点检测（每个在组内的节点）
```
[一键整理] 节点在组内 {
  nodeId: 'n-xxx',
  nodeTitle: '文字生图',
  nodeCenter: { cx: 500, cy: 300 },
  groupBounds: { x: 100, y: 100, right: 900, bottom: 700 }
}
```
**作用：** 显示哪些节点被识别为在组内

### 4. 无节点警告
```
[一键整理] 组内没有节点 {
  groupId: 'g-xxx',
  groupBounds: { x, y, width, height },
  allNodes: [
    { id, title, x, y, center: { cx, cy } },
    ...
  ]
}
```
**作用：** 显示所有节点的位置，帮助判断为什么没有节点在组内

### 5. 整理开始
```
[一键整理] 开始整理 {
  groupId: 'g-xxx',
  nodeCount: 3,
  nodeIds: ['n-1', 'n-2', 'n-3']
}
```

### 6. 整理完成
```
[一键整理] 完成 {
  groupId: 'g-xxx',
  newSize: { width: 1000, height: 600 },
  rows: 2,
  nodePositions: { 'n-1': {x, y}, ... }
}
```

## 测试步骤

### 步骤 1：创建测试场景
1. 打开浏览器控制台（F12）
2. 在画布上创建 2-3 个节点（任意类型）
3. 框选这些节点创建一个 Group
4. 确认 Group 创建成功（蓝色透明框）

### 步骤 2：选中 Group
1. 点击 Group 的边框（不要点击节点）
2. 确认边框变成青色高光
3. 查看控制台，应该没有任何日志（因为还没点击按钮）

### 步骤 3：点击一键整理按钮
1. 点击侧边栏左侧的"一键整理"按钮（AlignJustify 图标）
2. **立即查看控制台日志**

### 步骤 4：分析日志

#### 情况 A：没有任何日志
**问题：** 函数没有被调用
**可能原因：**
- 按钮的 `onClick` 没有正确绑定
- `onArrangeGroup` prop 没有正确传递
- 按钮被禁用了（`disabled={!selectedGroupId}`）

**检查：**
```typescript
// 在 SidebarDock.tsx 中
{ id: 'arrange', icon: AlignJustify, action: onArrangeGroup, disabled: !selectedGroupId, tooltip: '一键整理' }
```

#### 情况 B：只有 `[一键整理] 函数被调用`
**问题：** `selectedGroupId` 为 null 或找不到 Group
**可能原因：**
- Group 没有被正确选中
- `selectedGroupId` state 没有更新

**检查日志：**
```
[一键整理] 函数被调用 { selectedGroupId: null, ... }
```
或
```
[一键整理] 找不到 Group { selectedGroupId: 'g-xxx', availableGroups: [...] }
```

#### 情况 C：有 `[一键整理] 组内没有节点` 警告
**问题：** 节点不在 Group 边界内
**可能原因：**
1. Group 太小，节点中心点在 Group 外面
2. 节点位置计算错误
3. Group 位置计算错误

**检查日志中的 `allNodes` 数组：**
```javascript
allNodes: [
  {
    id: 'n-1',
    title: '文字生图',
    x: 500,
    y: 300,
    center: { cx: 710, cy: 480 }  // 节点中心点
  }
]
```

**对比 Group 边界：**
```javascript
groupBounds: { x: 100, y: 100, width: 500, height: 400 }
// 右边界：x + width = 600
// 下边界：y + height = 500
```

**判断：**
- 节点中心 cx=710 > Group 右边界 600 → **节点在 Group 外面**
- 需要调整 Group 大小或节点位置

#### 情况 D：有 `[一键整理] 开始整理` 但没有 `[一键整理] 完成`
**问题：** 整理过程中出错
**可能原因：**
- `saveHistory()` 失败
- `setNodes()` 失败
- `setGroups()` 失败

**检查：** 查看控制台是否有其他错误信息

#### 情况 E：有 `[一键整理] 完成` 但节点没有移动
**问题：** State 更新了但 UI 没有更新
**可能原因：**
- React 渲染问题
- 节点位置被其他逻辑覆盖

**检查：** 刷新页面，看节点位置是否改变（说明 state 已更新但 UI 没更新）

## 常见问题和解决方案

### 问题 1：按钮是灰色的，无法点击
**原因：** `selectedGroupId` 为 null
**解决：** 点击 Group 的边框选中它

### 问题 2：点击按钮没有反应
**原因：** 函数没有被调用
**解决：** 检查 `onArrangeGroup` prop 是否正确传递

### 问题 3：提示"组内没有节点"
**原因：** 节点中心点不在 Group 边界内
**解决方案：**

#### 方案 A：调整 Group 大小
1. 右键点击 Group
2. 选择"删除分组"
3. 重新框选节点，确保框选范围完全包含节点

#### 方案 B：手动调整 Group 边界
1. 拖动 Group 的右下角调整大小手柄（如果已实现）
2. 确保 Group 完全包含所有节点

#### 方案 C：修改判断逻辑
如果节点经常在 Group 边界外，可以修改判断逻辑：
```typescript
// 当前逻辑：节点中心点在 Group 内
const isInside = cx > group.x && cx < group.x + group.width && 
                 cy > group.y && cy < group.y + group.height;

// 可选逻辑：节点有任何部分在 Group 内
const nodeLeft = n.x;
const nodeRight = n.x + nodeWidth;
const nodeTop = n.y;
const nodeBottom = n.y + nodeHeight;

const isInside = !(nodeRight < group.x || nodeLeft > group.x + group.width ||
                   nodeBottom < group.y || nodeTop > group.y + group.height);
```

### 问题 4：整理后节点位置不对
**原因：** 网格布局参数不合适
**调整参数：**
```typescript
const NODE_WIDTH = 420;      // 节点宽度
const SPACING_X = 60;        // 水平间距
const SPACING_Y = 40;        // 垂直间距
const PADDING = 40;          // 组边距
```

## 预期行为

### 正常流程
1. 点击按钮
2. 控制台输出：`[一键整理] 函数被调用`
3. 控制台输出：`[一键整理] 找到 Group`
4. 控制台输出：多个 `[一键整理] 节点在组内`
5. 控制台输出：`[一键整理] 开始整理`
6. 节点开始移动到新位置
7. Group 自动调整大小
8. 控制台输出：`[一键整理] 完成`

### 整理效果
- 节点按网格排列（3 列）
- 节点间距均匀
- Group 自动调整大小以适应节点
- 保持节点原有的相对顺序（从左到右、从上到下）

## 下一步

根据控制台日志，确定问题所在：
1. **没有日志** → 检查按钮绑定
2. **找不到 Group** → 检查 Group 选中状态
3. **组内没有节点** → 检查节点位置和 Group 边界
4. **有开始没有完成** → 检查错误信息
5. **有完成但没效果** → 检查 React 渲染

请测试后告诉我看到了什么日志，我会帮你进一步分析！
