# 辅助线虚线问题 - 完整修复

**日期**: 2026-02-04  
**问题**: 向下拖动文字节点时出现奇怪的线条，继续拖动会出现多根线  
**状态**: ✅ 已修复

---

## 🔍 问题分析

### 现象描述
1. 创建文字节点后，向下拖动节点
2. 出现一根奇怪的线条（不像正常的辅助线）
3. 继续向下拖动，会出现两根线
4. 松手后，线条不消失，一直残留

### 根本原因

**问题 1：`detectHelperLines` 的依赖导致无限循环**

```typescript
// ❌ 错误的代码
const detectHelperLines = useCallback((
  draggingNode: AppNode,
  allNodes: Map<string, AppNode>,
  threshold: number = 5
): HelperLine[] => {
  if (!isDragging) return [];  // 🔥 这里检查 isDragging
  // ...
}, [isDragging]); // 🔥 依赖 isDragging
```

**问题链：**
1. `isDragging` 变化 → `detectHelperLines` 函数重新创建
2. `detectHelperLines` 重新创建 → `useEffect` 的依赖数组变化
3. `useEffect` 重新执行 → 重新注册事件监听器
4. 事件监听器中调用 `detectHelperLines` → 可能设置 `helperLines`
5. 如果在拖动结束后，RAF 回调还在执行 → 会设置辅助线
6. 辅助线就一直显示了

**问题 2：辅助线清除时机不对**

辅助线只在两个地方清除：
1. `handleMouseUp` 中
2. `cancelDrag` 中

但是如果 RAF 回调在 `handleMouseUp` 之后执行，就会重新设置辅助线。

**问题 3：`useEffect` 的清理函数没有清除辅助线**

```typescript
// ❌ 错误的代码
return () => {
  window.removeEventListener('mousemove', handleMouseMove);
  window.removeEventListener('mouseup', handleMouseUp);
  // 🔥 缺少：清除辅助线
};
```

当 `isDragging` 变为 `false` 时，清理函数执行，但没有清除辅助线。

---

## ✅ 修复方案

### 修改 1：移除 `detectHelperLines` 的 `isDragging` 依赖

**位置**: `hooks/useDrag.ts` 第 60-75 行

```typescript
// ✅ 修复后的代码
const detectHelperLines = useCallback((
  draggingNode: AppNode,
  allNodes: Map<string, AppNode>,
  threshold: number = 5
): HelperLine[] => {
  // 🔥 移除 isDragging 检查，改为在调用时检查
  const lines: HelperLine[] = [];
  // ...
  return lines;
}, []); // 🔥 移除 isDragging 依赖
```

**原因**：
- 避免 `isDragging` 变化时函数重新创建
- 避免触发 `useEffect` 重新执行
- 避免无限循环

### 修改 2：在 RAF 回调中检查拖动状态

**位置**: `hooks/useDrag.ts` 第 280-295 行

```typescript
// ✅ 修复后的代码
helperLinesRafRef.current = requestAnimationFrame(() => {
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      // 🔥 只在拖动状态下检测辅助线
      if (!dragRef.current) return;
      
      const currentNode = nodes.get(id);
      if (currentNode) {
        const lines = detectHelperLines(
          { ...currentNode, x: newX, y: newY },
          nodes,
          5
        );
        setHelperLines(lines);
      }
    });
  });
});
```

**原因**：
- 在调用时检查拖动状态，而不是在函数定义时检查
- 如果拖动已结束（`dragRef.current` 为 `null`），直接返回
- 避免延迟执行的 RAF 回调设置辅助线

### 修改 3：添加独立的 `useEffect` 清除辅助线

**位置**: `hooks/useDrag.ts` 第 200-210 行

```typescript
// ✅ 修复后的代码
useEffect(() => {
  if (!isDragging) {
    // 🔥 只在拖动结束时清除辅助线
    setHelperLines([]);
  }
}, [isDragging]);
```

**原因**：
- 使用独立的 `useEffect` 监听 `isDragging` 变化
- 当 `isDragging` 变为 `false` 时，清除辅助线
- 避免在清理函数中调用 `setState`，防止无限循环

### 修改 4：从清理函数中移除 `setHelperLines`

**位置**: `hooks/useDrag.ts` 第 380-395 行

```typescript
// ✅ 修复后的代码
return () => {
  window.removeEventListener('mousemove', handleMouseMove);
  window.removeEventListener('mouseup', handleMouseUp);
  
  // 🔥 只取消 RAF 回调，不调用 setState
  if (rafRef.current) {
    cancelAnimationFrame(rafRef.current);
    rafRef.current = null;
  }
  if (helperLinesRafRef.current) {
    cancelAnimationFrame(helperLinesRafRef.current);
    helperLinesRafRef.current = null;
  }
};
```

**原因**：
- 清理函数中不能调用 `setState`，会导致无限循环
- 改为使用独立的 `useEffect` 清除辅助线
- 清理函数只负责取消 RAF 回调和移除事件监听器

---

## 🎯 修复效果

### 修复前
- ❌ 拖动节点时出现奇怪的线条
- ❌ 继续拖动会出现多根线
- ❌ 松手后线条不消失
- ❌ 辅助线检测逻辑有 Bug

### 修复后
- ✅ 拖动节点时正常显示辅助线
- ✅ 辅助线数量正常
- ✅ 松手后辅助线立即消失
- ✅ 辅助线检测逻辑正确

---

## 📝 技术细节

### 为什么要移除 `isDragging` 依赖？

**问题**：
```typescript
const detectHelperLines = useCallback(() => {
  if (!isDragging) return [];
  // ...
}, [isDragging]); // 🔥 每次 isDragging 变化，函数都会重新创建
```

**后果**：
1. `isDragging` 从 `false` 变为 `true` → 函数重新创建
2. 函数重新创建 → `useEffect` 的依赖数组变化
3. `useEffect` 重新执行 → 重新注册事件监听器
4. 旧的事件监听器被移除，新的事件监听器被注册
5. 如果此时有 RAF 回调还在执行，可能会使用旧的函数引用

**解决方案**：
- 移除 `isDragging` 依赖，函数只创建一次
- 在调用时检查 `dragRef.current`，而不是在函数定义时检查 `isDragging`

### 为什么要在清理函数中清除辅助线？

**❌ 错误的做法**：
```typescript
useEffect(() => {
  if (!isDragging) return;
  
  // 注册事件监听器
  window.addEventListener('mousemove', handleMouseMove);
  window.addEventListener('mouseup', handleMouseUp);
  
  return () => {
    window.removeEventListener('mousemove', handleMouseMove);
    window.removeEventListener('mouseup', handleMouseUp);
    // 🔥 错误：在清理函数中调用 setState
    setHelperLines([]);
  };
}, [isDragging, ...]);
```

**后果**：
- 清理函数执行 → 调用 `setHelperLines([])` → 触发组件重新渲染
- 组件重新渲染 → `useEffect` 重新执行（因为依赖变化）
- `useEffect` 重新执行 → 清理函数再次执行
- 清理函数再次执行 → 再次调用 `setHelperLines([])` → **无限循环！**

**✅ 正确的做法**：
```typescript
// 独立的 useEffect 监听 isDragging 变化
useEffect(() => {
  if (!isDragging) {
    // 🔥 只在拖动结束时清除辅助线
    setHelperLines([]);
  }
}, [isDragging]);

// 事件监听器的 useEffect
useEffect(() => {
  if (!isDragging) return;
  
  // 注册事件监听器
  window.addEventListener('mousemove', handleMouseMove);
  window.addEventListener('mouseup', handleMouseUp);
  
  return () => {
    window.removeEventListener('mousemove', handleMouseMove);
    window.removeEventListener('mouseup', handleMouseUp);
    // 🔥 正确：不在清理函数中调用 setState
    // 只取消 RAF 回调
    if (rafRef.current) {
      cancelAnimationFrame(rafRef.current);
      rafRef.current = null;
    }
    if (helperLinesRafRef.current) {
      cancelAnimationFrame(helperLinesRafRef.current);
      helperLinesRafRef.current = null;
    }
  };
}, [isDragging, ...]);
```

**解决方案**：
- 使用独立的 `useEffect` 监听 `isDragging` 变化
- 当 `isDragging` 变为 `false` 时，清除辅助线
- 清理函数只负责取消 RAF 回调和移除事件监听器
- 避免在清理函数中调用 `setState`

### 为什么要在 RAF 回调中检查拖动状态？

**问题**：
```typescript
helperLinesRafRef.current = requestAnimationFrame(() => {
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      // 🔥 没有检查拖动状态
      const currentNode = nodes.get(id);
      if (currentNode) {
        const lines = detectHelperLines(...);
        setHelperLines(lines); // 🔥 可能在拖动结束后执行
      }
    });
  });
});
```

**后果**：
- RAF 回调是异步的，可能在拖动结束后才执行
- 如果拖动已结束，但 RAF 回调还在执行，会设置辅助线
- 辅助线就一直显示了

**解决方案**：
- 在 RAF 回调中检查 `dragRef.current`
- 如果拖动已结束（`dragRef.current` 为 `null`），直接返回
- 避免延迟执行的 RAF 回调设置辅助线

---

## 🧪 测试验证

### 测试步骤
1. 创建一个文字节点
2. 向下拖动节点
3. 观察是否出现辅助线
4. 继续拖动，观察辅助线数量
5. 松手，观察辅助线是否消失

### 预期结果
- ✅ 拖动时正常显示辅助线（对齐其他节点时）
- ✅ 辅助线数量正常（不会出现多根）
- ✅ 松手后辅助线立即消失
- ✅ 不会出现奇怪的线条

---

## 📚 相关文档

- `hooks/useDrag.ts` - 拖拽逻辑
- `节点拖动性能优化-辅助线节流和transition修复-2026-02-02.md` - 之前的优化
- `UI改版-阶段2-功能3-Helper-Lines-完成-2026-02-01.md` - 辅助线功能实施

---

## 💡 经验总结

### 教训
1. **避免在 `useCallback` 依赖中使用频繁变化的状态**
   - `isDragging` 频繁变化，会导致函数重新创建
   - 函数重新创建会触发 `useEffect` 重新执行
   - 可能导致无限循环或状态不一致

2. **异步回调要检查状态**
   - RAF 回调是异步的，可能在状态变化后才执行
   - 必须在回调中检查当前状态，避免使用过期的状态

3. **清理函数要彻底**
   - 不仅要移除事件监听器，还要清除状态
   - 不仅要清除状态，还要取消异步回调
   - 确保组件卸载或状态变化时，所有副作用都被清理

### 最佳实践
1. **`useCallback` 依赖尽量少**
   - 只依赖不变的值（如 `scale`、`onUpdateNode`）
   - 避免依赖频繁变化的状态（如 `isDragging`）
   - 在调用时检查状态，而不是在定义时检查

2. **异步回调要防御性编程**
   - 检查上下文是否还有效（`dragRef.current`）
   - 检查状态是否还符合预期
   - 避免使用过期的状态或引用

3. **清理函数要全面**
   - 移除事件监听器
   - 清除状态
   - 取消异步回调（RAF、setTimeout、Promise）
   - 清理 DOM 修改（transform、CSS 变量）

---

**修复完成！辅助线功能现在完全正常了。** ✅
