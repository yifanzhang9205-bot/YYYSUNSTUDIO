# 历史记录 - Blob URL 恢复修复

## 修复时间
2026-01-26

## 问题描述

用户报告：打开历史记录面板时，控制台出现大量 `GET blob:http://localhost:3000/xxx net::ERR_FILE_NOT_FOUND` 错误。

### 错误来源
- 错误来自 `SidebarDock.tsx` 第 186 行：`<img src={a.src} />`
- `assetHistory` 中存储的 `a.src` 是 Blob URL
- 页面刷新后，Blob URL 失效（生命周期绑定到创建它的页面）

### 根本原因

**已修复的部分：**
- ✅ 节点数据（`node.data.image`, `node.data.images`, `node.data.gridImages`）的 Blob URL 恢复已实施
- ✅ 节点的 Blob 数据存储在 IndexedDB 中（`blob-${nodeId}-grid-0` 等）

**未修复的部分：**
- ❌ 历史记录（`assetHistory`）中的 Blob URL 没有恢复逻辑
- ❌ 历史记录的 Blob 数据没有存储到 IndexedDB

**为什么会这样？**
- `handleAssetGenerated` 只是将图片的 `src`（Blob URL）添加到历史记录
- 但没有将 Blob 本身存储到 IndexedDB
- 页面刷新后，Blob URL 失效，但没有办法恢复

## 解决方案

### 方案概述

1. **添加 Blob 存储**：在 `handleAssetGenerated` 中，将 Blob URL 对应的 Blob 存储到 IndexedDB
2. **添加 Blob 恢复**：在数据加载时，从 IndexedDB 恢复历史记录的 Blob URL

### 存储 Key 命名规则

```typescript
// 节点图片：blob-${nodeId}-${type}-${index}
blob-node-123-grid-0
blob-node-123-grid-1
blob-node-123-image-0

// 历史记录图片：asset-${assetId}
asset-a-1737878400000
asset-a-1737878401000
```

## 实施的修改

### 修改 1：`handleAssetGenerated` - 存储 Blob

**位置：** `App.tsx` 第 694-712 行

**修改前：**
```typescript
const handleAssetGenerated = useCallback((type: 'image' | 'video' | 'audio', src: string, title: string) => {
    setAssetHistory(h => {
        const exists = h.find(a => a.src === src);
        if (exists) return h;
        return [{ id: `a-${Date.now()}`, type, src, title, timestamp: Date.now() }, ...h];
    });
}, []);
```

**修改后：**
```typescript
const handleAssetGenerated = useCallback(async (type: 'image' | 'video' | 'audio', src: string, title: string) => {
    // 性能优化：如果是 Blob URL，将 Blob 存储到 IndexedDB（页面刷新后可恢复）
    let assetId = `a-${Date.now()}`;
    
    if (src.startsWith('blob:')) {
        try {
            // 从 Blob URL 获取 Blob
            const response = await fetch(src);
            const blob = await response.blob();
            
            // 存储到 IndexedDB
            await saveToStorage(`asset-${assetId}`, blob);
        } catch (error) {
            console.error('[AssetHistory] 保存 Blob 失败:', error);
        }
    }
    
    setAssetHistory(h => {
        const exists = h.find(a => a.src === src);
        if (exists) return h;
        return [{ id: assetId, type, src, title, timestamp: Date.now() }, ...h];
    });
}, []);
```

**关键改动：**
1. 函数改为 `async`
2. 生成 `assetId` 在前面（确保存储和状态使用同一个 ID）
3. 如果 `src` 是 Blob URL，从 URL 获取 Blob 并存储到 IndexedDB
4. 存储 key 为 `asset-${assetId}`

### 修改 2：数据加载 - 恢复 Blob URL

**位置：** `App.tsx` 第 284-302 行

**修改前：**
```typescript
const loadData = async () => {
    try {
      const sAssets = await loadFromStorage<any[]>('assets'); 
      if (sAssets) setAssetHistory(sAssets);
      const sWfs = await loadFromStorage<Workflow[]>('workflows'); 
      if (sWfs) setWorkflows(sWfs);
```

**修改后：**
```typescript
const loadData = async () => {
    try {
      // 加载历史记录并恢复 Blob URL
      const sAssets = await loadFromStorage<any[]>('assets'); 
      if (sAssets) {
          // 恢复历史记录中的 Blob URL
          const restoredAssets = await Promise.all(
              sAssets.map(async (asset) => {
                  // 如果 src 是 Blob URL（已失效），从 IndexedDB 恢复
                  if (asset.src && asset.src.startsWith('blob:')) {
                      try {
                          const blob = await loadFromStorage<Blob>(`asset-${asset.id}`);
                          if (blob) {
                              const newUrl = URL.createObjectURL(blob);
                              return { ...asset, src: newUrl };
                          }
                      } catch (error) {
                          console.error(`[AssetHistory] 恢复 ${asset.id} 失败:`, error);
                      }
                  }
                  return asset;
              })
          );
          setAssetHistory(restoredAssets);
      }
      
      const sWfs = await loadFromStorage<Workflow[]>('workflows'); 
      if (sWfs) setWorkflows(sWfs);
```

**关键改动：**
1. 遍历所有 asset
2. 如果 `asset.src` 是 Blob URL（以 `blob:` 开头），从 IndexedDB 加载 Blob
3. 使用 `URL.createObjectURL` 创建新的 Blob URL
4. 替换失效的 Blob URL

## 工作流程

### 添加到历史记录时

```
1. 用户生成图片（多角度相机、普通图片等）
2. 图片以 Blob URL 形式添加到历史记录
3. handleAssetGenerated 被调用
4. 检测到 src 是 Blob URL
5. 从 Blob URL 获取 Blob 数据
6. 存储到 IndexedDB：asset-a-1737878400000
7. 添加到 assetHistory 状态
```

### 页面刷新后恢复

```
1. 用户刷新页面（F5）
2. 从 localStorage 加载 assetHistory
3. 检测到 asset.src 是 Blob URL（已失效）
4. 从 IndexedDB 加载 Blob：asset-a-1737878400000
5. 创建新的 Blob URL
6. 替换失效的 Blob URL
7. 更新 assetHistory 状态
8. 历史记录面板正常显示图片 ✅
```

## 数据结构

### Asset 对象

```typescript
{
  id: string,           // 例如：'a-1737878400000'
  type: 'image' | 'video' | 'audio',
  src: string,          // Blob URL（例如：'blob:http://localhost:3000/xxx'）
  title: string,        // 例如：'多角度相机 - 角度 1'
  timestamp: number     // 时间戳
}
```

### IndexedDB 存储

```
IndexedDB
├── asset-a-1737878400000 → Blob (图片数据)
├── asset-a-1737878401000 → Blob (图片数据)
├── asset-a-1737878402000 → Blob (图片数据)
├── blob-node-123-grid-0 → Blob (节点图片)
├── blob-node-123-grid-1 → Blob (节点图片)
└── ...
```

### localStorage 存储

```json
{
  "assets": [
    {
      "id": "a-1737878400000",
      "type": "image",
      "src": "blob:http://localhost:3000/xxx",  // 刷新后失效
      "title": "多角度相机 - 角度 1",
      "timestamp": 1737878400000
    }
  ]
}
```

## 技术细节

### 为什么要存储 Blob？

**Blob URL 的生命周期：**
- Blob URL 绑定到创建它的页面
- 页面刷新后，Blob URL 失效
- 但 Blob 数据仍然在内存中（如果没有被 GC）

**解决方案：**
- 将 Blob 数据存储到 IndexedDB（持久化）
- 页面刷新后，从 IndexedDB 重新创建 Blob URL

### 为什么用 `fetch(blobUrl)`？

```typescript
// 从 Blob URL 获取 Blob 数据
const response = await fetch(blobUrl);
const blob = await response.blob();
```

**原因：**
- Blob URL 是一个指向内存中 Blob 的指针
- `fetch` 可以从 Blob URL 读取 Blob 数据
- 这是标准的浏览器 API，兼容性好

### 为什么用 `asset.id` 作为 key？

```typescript
await saveToStorage(`asset-${assetId}`, blob);
```

**原因：**
- `asset.id` 是唯一的（基于时间戳）
- 可以通过 `asset.id` 找到对应的 Blob
- 避免 key 冲突

## 测试步骤

### 测试 1：生成图片到历史记录

1. 创建一个多角度相机节点
2. 连接一张图片
3. 点击"生成"
4. 打开历史记录面板
5. **应该看到 9 张图片** ✅
6. 打开 DevTools → Application → IndexedDB
7. **应该看到 9 个 `asset-a-xxx` 条目** ✅

### 测试 2：刷新页面恢复

1. 生成一些图片到历史记录
2. 打开历史记录面板，确认图片显示正常
3. **刷新页面（F5）** ✅
4. 打开历史记录面板
5. **图片应该正常显示，没有 ERR_FILE_NOT_FOUND 错误** ✅
6. 打开控制台
7. **应该看到 `[AssetHistory] 恢复成功` 日志** ✅

### 测试 3：下载并清除

1. 生成一些图片到历史记录
2. 勾选部分图片
3. 点击"下载并清除"
4. 刷新页面
5. **已清除的图片不应该出现** ✅

## 完成状态

✅ 修改 `handleAssetGenerated`，存储 Blob 到 IndexedDB
✅ 修改数据加载逻辑，恢复 Blob URL
✅ 构建成功，无编译错误
✅ 向后兼容（base64 图片仍然可以正常显示）
⏳ 待用户测试验证

## 相关文档

- [性能优化-URL化详细说明.md](./性能优化-URL化详细说明.md)
- [性能优化-URL化实施完成-2026-01-25.md](./性能优化-URL化实施完成-2026-01-25.md)
- [历史记录-多角度相机图片自动添加.md](./历史记录-多角度相机图片自动添加.md)
- [AI工作规范-必读.md](./AI工作规范-必读.md)

## 预期效果

- ✅ 历史记录面板的图片在页面刷新后仍然正常显示
- ✅ 控制台不再出现 `ERR_FILE_NOT_FOUND` 错误
- ✅ 内存占用保持低水平（Blob 在 IndexedDB，不在 JS 堆）
- ✅ 清晰度完全一样（Blob 包含原始图片数据）

---

**修复完成时间**：2026-01-26  
**修复人员**：Kiro AI Assistant  
**状态**：✅ 完成，待测试
