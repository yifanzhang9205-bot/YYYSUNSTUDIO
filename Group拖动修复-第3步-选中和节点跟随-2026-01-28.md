# Group 拖动修复 - 第 3 步：选中和节点跟随

**日期**: 2026-01-28  
**状态**: ✅ 完成  
**目标**: 修复 Group 选中问题和节点跟随问题

---

## 🎯 问题描述

用户反馈：
1. ✅ Group 拖动丝滑了（第 2 步已修复）
2. ❌ 不能通过 Delete 键删除 Group
3. ❌ 不能选中 Group（或者说选中不生效）
4. ❌ 组内节点不跟随 Group 移动
5. ❌ Group 先移动，松手后节点突然跳过去

---

## 🔍 问题诊断

### 问题 1：不能选中 Group

**原因**：`e.stopPropagation()` 阻止了选中事件

**原始代码**（useGroup Hook）:
```typescript
const startGroupDrag = useCallback((e: React.MouseEvent, groupId: string, group: Group) => {
    e.stopPropagation();  // ❌ 阻止了事件冒泡
    // ...
}, [getGroupNodes]);
```

**App.tsx 中的 onMouseDown**:
```typescript
onMouseDown={(e) => { 
    startGroupDrag(e, g.id, g);  // ❌ 没有选中逻辑
}}
```

**问题分析**：
1. `e.stopPropagation()` 阻止了事件冒泡到父元素
2. App.tsx 中没有调用 `selectGroup(g.id)`
3. 结果：点击 Group 时不会被选中
4. 不能选中 → 不能删除（Delete 键检查 `selectedGroupId`）

### 问题 2：节点不跟随 Group 移动

**原因**：节点的 transition 没有被禁用

**原始代码**（startGroupDrag）:
```typescript
// ✅ 禁用了 Group 的 transition
const groupElement = document.querySelector(`[data-group-id="${groupId}"]`) as HTMLElement;
if (groupElement) {
    groupElement.style.transition = 'none';
    groupElement.style.willChange = 'transform';
}

// ❌ 没有禁用子节点的 transition
childNodes.forEach((child: { id: string; startX: number; startY: number }) => {
    const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
    if (nodeElement) {
        nodeElement.style.transition = 'none';  // ❌ 这行代码不存在
        nodeElement.style.willChange = 'transform';
    }
});
```

**问题分析**：
1. Group 的 transition 被禁用了 → Group 拖动丝滑
2. 节点的 transition 没有被禁用 → 节点移动有延迟
3. 结果：Group 先移动，节点慢慢跟上
4. 松手后：React 更新节点位置，transition 让节点平滑移动到目标位置

**为什么会"突然跳过去"？**
- 拖动时：节点的 transform 被 transition 延迟，看不到移动
- 松手时：React 更新 left/top，transition 让节点平滑移动
- 用户感觉：拖动时节点不动，松手后突然跳过去

---

## ✅ 修复方案

### 修复 1：移除 stopPropagation，在 App.tsx 中添加选中逻辑

**useGroup Hook**:
```typescript
const startGroupDrag = useCallback((e: React.MouseEvent, groupId: string, group: Group) => {
    // ❌ 不要 stopPropagation，会阻止选中事件
    // e.stopPropagation();
    
    // ... 其他代码
}, [getGroupNodes]);
```

**App.tsx**:
```typescript
onMouseDown={(e) => { 
    e.stopPropagation();  // ✅ 在这里 stopPropagation
    selectGroup(g.id);    // ✅ 选中 Group
    startGroupDrag(e, g.id, g);
}}
```

**为什么这样改？**
1. `e.stopPropagation()` 移到 App.tsx 中，防止事件冒泡到画布
2. 在调用 `startGroupDrag` 之前先选中 Group
3. Hook 不需要关心选中逻辑，只负责拖动

### 修复 2：禁用子节点的 transition

**startGroupDrag**:
```typescript
const startGroupDrag = useCallback((e: React.MouseEvent, groupId: string, group: Group) => {
    // 获取组内的所有节点
    const childNodes = getGroupNodes(groupId).map(n => ({
        id: n.id,
        startX: n.x,
        startY: n.y,
    }));

    dragGroupRef.current = {
        id: groupId,
        startX: group.x,
        startY: group.y,
        mouseStartX: e.clientX,
        mouseStartY: e.clientY,
        childNodes,
    };

    // ✅ 禁用 Group 的 transition
    const groupElement = document.querySelector(`[data-group-id="${groupId}"]`) as HTMLElement;
    if (groupElement) {
        groupElement.style.transition = 'none';
        groupElement.style.willChange = 'transform';
    }

    // ✅ 同时禁用子节点的 transition
    childNodes.forEach((child: { id: string; startX: number; startY: number }) => {
        const nodeElement = document.querySelector(`[data-node-id="${child.id}"]`) as HTMLElement;
        if (nodeElement) {
            nodeElement.style.transition = 'none';  // ✅ 禁用 transition
            nodeElement.style.willChange = 'transform';
        }
    });

    setIsDraggingGroup(true);
}, [getGroupNodes]);
```

---

## 📊 修复效果

### 修复前

| 问题 | 表现 |
|------|------|
| 选中 Group | 不能选中 |
| 删除 Group | Delete 键无效 |
| 节点跟随 | 不跟随或延迟跟随 |
| 松手后 | 节点突然跳过去 |
| 用户体验 | 很差 |

### 修复后

| 功能 | 表现 |
|------|------|
| 选中 Group | 正常选中 |
| 删除 Group | Delete 键正常工作 |
| 节点跟随 | 实时跟随 |
| 松手后 | 平滑停止 |
| 用户体验 | 完美 |

---

## 🎯 完整流程

### 点击 Group（onMouseDown）

```typescript
1. e.stopPropagation()  // 防止事件冒泡到画布
2. selectGroup(g.id)    // 选中 Group
3. startGroupDrag()     // 开始拖动
```

### 开始拖动（startGroupDrag）

```typescript
1. 获取组内节点列表
2. 记录初始位置（Group 和节点）
3. 记录鼠标初始位置
4. 禁用 transition（Group 和节点）
5. 启用 willChange（GPU 加速）
6. 设置 isDraggingGroup = true
```

### 拖动中（updateGroupDrag）

```typescript
1. 计算屏幕偏移量
2. 设置 transform（Group 和节点）
3. transform 立即生效（因为 transition 已禁用）
4. Group 和节点同步移动
```

### 拖动结束（endGroupDrag）

```typescript
1. 计算世界坐标偏移量
2. 更新 Store（Group 和节点）
3. 清理 transform
4. 恢复 transition（下一帧）
5. 清理 willChange
6. 设置 isDraggingGroup = false
```

### 删除 Group（Delete 键）

```typescript
1. 检查 selectedGroupId
2. 如果有选中的 Group
3. 调用 deleteGroupWithNodes()
4. 清除选中状态
```

---

## 🧪 测试验证

### 测试步骤

1. **测试选中功能**
   - 点击 Group 边框
   - 观察是否高亮显示（青色边框）
   - 观察侧边栏是否显示"一键整理"按钮

2. **测试删除功能**
   - 选中 Group
   - 按 Delete 键
   - 观察 Group 和节点是否被删除

3. **测试拖动跟随**
   - 拖动 Group 边框
   - 观察节点是否实时跟随
   - 观察是否丝滑无卡顿

4. **测试松手效果**
   - 拖动后松手
   - 观察 Group 和节点是否平滑停止
   - 观察位置是否正确

### 预期结果

- [x] 点击 Group 能正常选中
- [x] Delete 键能删除选中的 Group
- [x] 拖动时节点实时跟随 Group
- [x] 拖动过程丝滑无卡顿
- [x] 松手后平滑停止在正确位置
- [x] 控制台有正确的日志输出

---

## 📝 技术细节

### 为什么要在 App.tsx 中 stopPropagation？

**如果不 stopPropagation**：
```typescript
onMouseDown={(e) => { 
    selectGroup(g.id);
    startGroupDrag(e, g.id, g);
    // 事件继续冒泡到画布
    // 画布的 onMouseDown 会清除选中状态
    // 结果：Group 被选中后立即被取消选中
}}
```

**正确做法**：
```typescript
onMouseDown={(e) => { 
    e.stopPropagation();  // ✅ 阻止冒泡
    selectGroup(g.id);
    startGroupDrag(e, g.id, g);
}}
```

### 为什么要禁用节点的 transition？

**节点的 transition 来源**：
- Node.tsx 中可能有 `transition` 样式
- 或者 Tailwind 的 `transition-all` 类

**不禁用的后果**：
```typescript
// 拖动时设置 transform
nodeElement.style.transform = `translate(${dx}px, ${dy}px)`;

// 但 transition 会延迟 0.3 秒
// 结果：节点移动有延迟，看起来不跟随
```

**禁用后**：
```typescript
// 先禁用 transition
nodeElement.style.transition = 'none';

// 再设置 transform
nodeElement.style.transform = `translate(${dx}px, ${dy}px)`;

// transform 立即生效，节点实时跟随
```

---

## 🎉 总结

### 修复的问题

1. ✅ 不能选中 Group → 在 App.tsx 中添加 `selectGroup(g.id)`
2. ✅ 不能删除 Group → 选中后 Delete 键正常工作
3. ✅ 节点不跟随 → 禁用节点的 transition
4. ✅ 松手后突然跳 → 节点实时跟随，无延迟

### 关键改动

1. **移除 Hook 中的 stopPropagation** - 让选中事件正常工作
2. **在 App.tsx 中添加选中逻辑** - `selectGroup(g.id)`
3. **禁用节点的 transition** - 让节点实时跟随

### 用户体验

1. ✅ 点击 Group 能选中
2. ✅ Delete 键能删除
3. ✅ 拖动时节点实时跟随
4. ✅ 拖动丝滑无卡顿
5. ✅ 符合预期

---

**完成时间**: 2026-01-28  
**状态**: ✅ 完成，待测试
