# 数据持久化架构升级 - 需求文档

**创建日期**：2026-02-09  
**状态**：待实施  
**优先级**：P0（核心功能）  
**预计工时**：2-3 小时

---

## 📋 需求概述

### 核心目标

1. **画布节点持久化**：图片不随页面刷新丢失
2. **内存优化**：不超高占用内存（懒加载 + 手动释放）
3. **资产库独立存储**：与历史记录、画布节点完全隔离
4. **架构扩展性**：预留未来迁移到服务器的能力
5. **独立清除逻辑**：历史记录和资产库分别清除

### 用户价值

- ✅ 页面刷新后工作成果不丢失
- ✅ 内存占用可控，不会卡顿
- ✅ 资产库永久保存，不受其他操作影响
- ✅ 清除逻辑清晰，不会误删

---

## 🏗️ 架构设计

### 三层存储架构

```
┌─────────────────────────────────────────────────────────┐
│                  第 1 层：内存层（Blob URL）               │
│  - 用途：UI 显示                                          │
│  - 生命周期：页面刷新后失效                                │
│  - 优势：零拷贝，性能最高                                  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│              第 2 层：持久化层（IndexedDB）                │
│  - 用途：页面刷新后恢复                                    │
│  - 生命周期：手动删除前永久保存                             │
│  - 优势：不占内存，容量大（几百 MB）                        │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│              第 3 层：元数据层（localStorage）             │
│  - 用途：保存节点结构（位置、类型、连接关系）                │
│  - 生命周期：永久保存                                      │
│  - 优势：快速加载，体积小                                  │
└─────────────────────────────────────────────────────────┘
```

### 数据隔离设计

```
画布节点：
  - IndexedDB: "blob-node-{nodeId}-image"
  - 生命周期：删除节点时清除

资产库：
  - IndexedDB: "asset-thumbnail-{assetId}"
  - 生命周期：删除资产时清除

历史记录：
  - IndexedDB: "asset-{assetId}"
  - 生命周期：清除历史记录时检查引用
```

---

## 📊 技术方案

### 方案对比

| 方案 | 内存占用 | 磁盘占用 | 页面刷新 | 性能 | 清晰度 | 推荐度 |
|------|---------|---------|---------|------|--------|--------|
| **当前（纯 Blob URL）** | 高 | 0 | ❌ 丢失 | ⚡ 最快 | ✅ 100% | ❌ |
| **纯 Base64** | 极高 | 高 | ✅ 保留 | 🐌 慢 | ✅ 100% | ❌ |
| **Blob URL + IndexedDB** | 低 | 中 | ✅ 保留 | ⚡ 快 | ✅ 100% | ✅ **推荐** |
| **HTTP URL（服务器）** | 极低 | 0 | ✅ 保留 | 🌐 网络 | ✅ 100% | ✅ 未来 |

### 选择理由

**Blob URL + IndexedDB** 是当前最优方案：
- ✅ 性能高（零拷贝）
- ✅ 内存可控（懒加载）
- ✅ 持久化（刷新不丢失）
- ✅ 清晰度 100%（无损存储）
- ✅ 架构扩展性（可迁移到服务器）

---

## 🎯 实施计划

### 阶段 1：画布节点持久化（1 小时）

**目标**：画布图片不随刷新丢失

**任务清单**：
- [ ] 1.1 添加 persist 中间件到 nodeStore
- [ ] 1.2 实现 Map 序列化/反序列化
- [ ] 1.3 实现图片 Blob 保存到 IndexedDB
- [ ] 1.4 实现页面加载时恢复 Blob URL
- [ ] 1.5 测试刷新恢复功能

**修改文件**：
- `core/stores/nodeStore.ts`（添加 persist）
- `services/blobStorage.ts`（新增保存/恢复函数）
- `App.tsx`（添加恢复逻辑）

**风险评估**：
- ⚠️ 风险：persist 中间件可能与 Map 不兼容
- ✅ 缓解：使用自定义序列化函数

---

### 阶段 2：资产库独立存储（1 小时）

**目标**：资产库永久保存，不受其他操作影响

**任务清单**：
- [ ] 2.1 创建资产时保存缩略图到 IndexedDB
- [ ] 2.2 页面加载时恢复资产库缩略图
- [ ] 2.3 删除资产时清理 IndexedDB
- [ ] 2.4 测试资产库刷新恢复

**修改文件**：
- `hooks/useAssetLibrary.ts`（添加 IndexedDB 保存）
- `core/stores/assetLibraryStore.ts`（添加恢复逻辑）
- `services/blobStorage.ts`（新增资产库专用函数）

**风险评估**：
- ⚠️ 风险：资产库已有 persist，可能冲突
- ✅ 缓解：检查现有实现，兼容修改

---

### 阶段 3：引用计数保护（30 分钟）

**目标**：防止误删被资产库引用的图片

**任务清单**：
- [ ] 3.1 历史记录添加 `usedByAssetLibrary` 字段
- [ ] 3.2 创建资产时标记历史记录
- [ ] 3.3 清除历史记录时检查引用
- [ ] 3.4 添加删除提示弹窗
- [ ] 3.5 测试引用保护逻辑

**修改文件**：
- `hooks/useAssetHistory.ts`（添加引用检查）
- `core/stores/assetHistoryStore.ts`（添加字段）
- `components/AssetHistoryPanel.tsx`（添加提示）

**风险评估**：
- ⚠️ 风险：历史记录数据结构变更
- ✅ 缓解：向后兼容，默认值为 false

---

### 阶段 4：测试和优化（30 分钟）

**目标**：确保功能稳定，性能优化

**任务清单**：
- [ ] 4.1 测试画布节点刷新恢复
- [ ] 4.2 测试资产库刷新恢复
- [ ] 4.3 测试内存占用（10 个节点）
- [ ] 4.4 测试清除逻辑（历史记录 vs 资产库）
- [ ] 4.5 测试边界情况（空数据、大量数据）
- [ ] 4.6 性能优化（懒加载、批量操作）

**测试用例**：
- ✅ 创建 10 个图片节点 → 刷新页面 → 图片完整显示
- ✅ 创建 5 个资产 → 刷新页面 → 资产库完整显示
- ✅ 清除历史记录 → 资产库不受影响
- ✅ 删除资产 → 历史记录不受影响
- ✅ 内存占用 < 50MB（10 个节点）

**风险评估**：
- ⚠️ 风险：性能问题（大量数据）
- ✅ 缓解：懒加载、批量操作、RAF 节流

---

## 🚫 禁止事项（架构保护）

### 不允许的操作

1. ❌ **禁止修改 App.tsx 的业务逻辑**
   - 只能添加恢复逻辑（useEffect）
   - 不能修改现有功能

2. ❌ **禁止破坏现有功能**
   - 图片生成、视频上传、历史记录等
   - 必须保持 100% 兼容

3. ❌ **禁止跨层混用**
   - UI 组件 → Hook → Store（单向数据流）
   - 不能跳过 Hook 直接操作 Store

4. ❌ **禁止使用 Base64 存储**
   - 内存占用高（133% 体积）
   - localStorage 限制 5MB

5. ❌ **禁止阻塞 UI**
   - IndexedDB 操作必须异步
   - 使用 RAF 或 setTimeout

### 必须遵守的规则

1. ✅ **遵守三层架构**
   - UI Layer：只做展示
   - Hooks Layer：处理交互
   - Core Layer：数据管理

2. ✅ **保持向后兼容**
   - 旧数据能正常加载
   - 新字段有默认值

3. ✅ **完整的错误处理**
   - try-catch 包裹所有异步操作
   - 失败时不影响现有功能

4. ✅ **清晰的日志**
   - 每个关键操作都有 console.log
   - 便于调试和追踪

5. ✅ **性能优化**
   - 懒加载（只加载可见的）
   - 批量操作（减少 IndexedDB 调用）
   - RAF 节流（避免频繁更新）

---

## 📝 验收标准

### 功能验收

- [ ] 画布节点刷新后完整恢复（图片、位置、连接）
- [ ] 资产库刷新后完整恢复（缩略图、名称、分类）
- [ ] 历史记录清除时检查引用，提示用户
- [ ] 资产库删除时不影响历史记录
- [ ] 内存占用可控（< 50MB，10 个节点）

### 性能验收

- [ ] 页面加载时间 < 2 秒（10 个节点）
- [ ] 图片恢复时间 < 100ms（单个）
- [ ] 内存占用 < 50MB（10 个节点）
- [ ] 磁盘占用合理（< 100MB，50 个节点）

### 兼容性验收

- [ ] 旧数据能正常加载
- [ ] 现有功能不受影响
- [ ] 错误处理完整
- [ ] 日志清晰可追踪

---

## 🔄 回滚方案

### 如果出现问题

1. **立即停止实施**
2. **记录错误信息**
3. **回滚到上一个稳定版本**
4. **分析问题原因**
5. **修复后重新实施**

### 回滚步骤

```bash
# 1. 查看 Git 状态
git status

# 2. 回滚所有修改
git checkout .

# 3. 清理未跟踪文件
git clean -fd

# 4. 验证功能正常
# 测试图片生成、视频上传、历史记录等
```

---

## 📚 参考文档

- **架构文档**：`ARCHITECTURE.md`
- **项目入职规则**：`.kiro/steering/project-onboarding.md`
- **Blob 存储服务**：`services/blobStorage.ts`
- **节点 Store**：`core/stores/nodeStore.ts`
- **资产库 Store**：`core/stores/assetLibraryStore.ts`
- **历史记录 Hook**：`hooks/useAssetHistory.ts`

---

## 🎯 成功指标

### 用户体验

- ✅ 页面刷新后，画布完整恢复
- ✅ 资产库永久保存，不丢失
- ✅ 清除逻辑清晰，不误删
- ✅ 性能流畅，不卡顿

### 技术指标

- ✅ 代码清晰，易维护
- ✅ 架构合理，易扩展
- ✅ 错误处理完整
- ✅ 日志清晰可追踪

### 未来扩展

- ✅ 可迁移到服务器存储
- ✅ 可添加懒加载优化
- ✅ 可添加缩略图压缩
- ✅ 可添加定期清理

---

**文档版本**：v1.0  
**创建日期**：2026-02-09  
**状态**：✅ 已确认，待实施
