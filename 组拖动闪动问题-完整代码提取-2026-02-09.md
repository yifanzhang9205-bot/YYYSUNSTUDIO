# 组拖动闪动问题 - 完整代码提取

**问题描述**: 松开鼠标后组还在闪动

---

## 1. useGroup.ts - 拖动逻辑

### 状态定义（第65行）

```typescript
const [isDraggingGroup, setIsDraggingGroup] = useState<boolean>(false);
const dragGroupRef = useRef<DragGroupContext | null>(null);

// 🔥 拖动偏移量状态（用于实时更新标题和 Toolbar 位置）
const [draggingGroupOffset, setDraggingGroupOffset] = useState<{ id: string; dx: number; dy: number } | null>(null);
```

### startGroupDrag（第130-190行）

```typescript
const startGroupDrag = useCallback((e: React.MouseEvent, groupId: string, group: Group) => {
  // ❌ 不要 stopPropagation，会阻止选中事件
  // e.stopPropagation();
  
  // 🔥 先初始化拖动上下文（不包含子节点）
  dragGroupRef.current = {
    id: groupId,
    startX: group.x,
    startY: group.y,
    mouseStartX: e.clientX,
    mouseStartY: e.clientY,
    childNodes: [], // 先设置为空数组
  };

  // ✅ 设置拖动状态（先设置状态，让 React 禁用 transition）
  setIsDraggingGroup(true);

  // 🔥 关键修复：使用双重 requestAnimationFrame 延迟查询 DOM
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      if (!dragGroupRef.current) return;

      // 查询子节点的 DOM 元素
      const childNodes = getGroupNodes(groupId).map(n => {
        let element = document.querySelector(`[data-node-id="${n.id}"]`) as HTMLElement;
        
        if (!element) {
          element = document.getElementById(`node-${n.id}`) as HTMLElement;
        }
        
        if (!element) {
          console.warn(`[useGroup] 找不到节点 DOM 元素: ${n.id}`);
        }
        
        return {
          id: n.id,
          startX: n.x,
          startY: n.y,
          element,
        };
      });

      // 更新拖动上下文，添加子节点
      if (dragGroupRef.current) {
        dragGroupRef.current.childNodes = childNodes;
      }
    });
  });
}, [getGroupNodes]);
```

### updateGroupDrag（第192-230行）

```typescript
const updateGroupDrag = useCallback((e: MouseEvent, currentScale: number): boolean => {
  if (!dragGroupRef.current) {
    return false;
  }

  const { id, mouseStartX, mouseStartY, childNodes } = dragGroupRef.current;

  // 计算偏移量（屏幕坐标）
  const screenDx = e.clientX - mouseStartX;
  const screenDy = e.clientY - mouseStartY;

  // 🔥 关键修复：转换为世界坐标，因为 Canvas 已经有 scale
  const worldDx = screenDx / currentScale;
  const worldDy = screenDy / currentScale;

  // 使用 CSS transform 优化性能（直接操作 DOM）
  const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
  
  if (groupElement) {
    groupElement.style.transform = `translate(${worldDx}px, ${worldDy}px)`;
    groupElement.style.willChange = 'transform';
  }

  // 🔥 关键修复：使用缓存的 DOM 元素引用，不再每次查询
  childNodes.forEach((child: { id: string; startX: number; startY: number; element: HTMLElement | null }) => {
    if (child.element) {
      child.element.style.transform = `translate(${worldDx}px, ${worldDy}px)`;
      child.element.style.willChange = 'transform';
    }
  });
  
  // 🔥 新增：更新拖动偏移量状态（用于实时更新标题和 Toolbar 位置）
  setDraggingGroupOffset({ id, dx: worldDx, dy: worldDy });
  
  return true;
}, []);
```

### endGroupDrag（第232-310行）- 关键函数

```typescript
const endGroupDrag = useCallback((e: MouseEvent, currentScale: number) => {
  if (!dragGroupRef.current) return;

  const { id, startX, startY, mouseStartX, mouseStartY, childNodes } = dragGroupRef.current;

  // 计算最终位置（世界坐标）
  const screenDx = e.clientX - mouseStartX;
  const screenDy = e.clientY - mouseStartY;
  const worldDx = screenDx / currentScale;
  const worldDy = screenDy / currentScale;
  const finalX = startX + worldDx;
  const finalY = startY + worldDy;

  // 保存历史
  onSaveHistory();

  // 🔥 第一步：先更新 Store（让 React 知道新位置）
  // 注意：此时 transform 还在，所以视觉上不会动
  onUpdateGroup(id, { x: finalX, y: finalY });

  // 更新子节点位置（使用 Store）
  childNodes.forEach((child: { id: string; startX: number; startY: number }) => {
    onUpdateNode(child.id, {
      x: child.startX + worldDx,
      y: child.startY + worldDy,
    });
  });

  // 清除拖动上下文
  dragGroupRef.current = null;

  // 🔥 第二步：延迟清除 transform 和状态
  // 等待 React 完成渲染（left/top 已更新），再清除 transform
  // 这样视觉上完全无缝：transform 位置 = left/top 位置
  requestAnimationFrame(() => {
    requestAnimationFrame(() => {
      // 清除拖动状态（恢复 transition）
      setIsDraggingGroup(false);
      
      // 清除拖动偏移量状态
      setDraggingGroupOffset(null);

      // 清除所有 transform 和 willChange
      const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
      if (groupElement) {
        groupElement.style.transform = '';
        groupElement.style.willChange = 'auto';
      }

      childNodes.forEach((child: { id: string; startX: number; startY: number; element: HTMLElement | null }) => {
        if (child.element) {
          child.element.style.transform = '';
          child.element.style.willChange = 'auto';
        }
      });
    });
  });
}, [onSaveHistory, onUpdateGroup, onUpdateNode]);
```

### 全局事件监听（第350-380行）

```typescript
useEffect(() => {
  const handleGlobalMouseMove = (e: MouseEvent) => {
    if (!dragGroupRef.current) return;
    updateGroupDrag(e, scaleRef.current);
  };

  const handleGlobalMouseUp = (e: MouseEvent) => {
    if (!dragGroupRef.current) return;
    endGroupDrag(e, scaleRef.current);
  };

  window.addEventListener('mousemove', handleGlobalMouseMove);
  window.addEventListener('mouseup', handleGlobalMouseUp);

  return () => {
    window.removeEventListener('mousemove', handleGlobalMouseMove);
    window.removeEventListener('mouseup', handleGlobalMouseUp);
  };
}, [updateGroupDrag, endGroupDrag]);
```

---

## 2. App.tsx - 组渲染

### 组渲染代码（第1705-1750行）

```typescript
{groups.map(g => {
  // 🔥 判断当前组是否正在被拖动
  const isThisGroupDragging = isDraggingGroup && selectedGroupId === g.id;
  
  // 🔥 判断是否是临时组
  const isTemporary = g.title === '临时分组';
  
  // 🔥 组颜色选择：获取组的颜色样式
  const colorStyle = getGroupColorStyle(g.color, selectedGroupId === g.id, isTemporary);
  
  // 🔥 圆点大小：根据缩放比例动态调整
  const dotSize = Math.max(8, 10 / scale);
  const dotOffset = dotSize / 2;
  
  return (
    <div 
      key={g.id} 
      id={`group-${g.id}`}
      data-group-id={g.id}
      className="absolute border group/group"
      style={{ 
        left: g.x, 
        top: g.y, 
        width: g.width, 
        height: g.height,
        borderColor: colorStyle.borderColor,
        borderWidth: colorStyle.borderWidth || '2px',
        background: colorStyle.background,
        boxShadow: colorStyle.boxShadow || 'none',
        borderRadius: 0,
        transition: isThisGroupDragging ? 'none' : 'all 0.2s ease', // 🔥 拖动时禁用 transition
      }} 
      onMouseDown={(e) => { 
        e.stopPropagation();
        selectGroup(g.id);
        startGroupDrag(e, g.id, g);
      }} 
      onDoubleClick={(e) => {
        e.stopPropagation();
      }}
      onContextMenu={e => { 
        e.stopPropagation(); 
        openContextMenu({visible:true, x:e.clientX, y:e.clientY, id:g.id}, {type:'group', id:g.id}); 
      }}
    >
      {/* 四个角的调整大小圆点 */}
      {/* ... */}
    </div>
  );
})}
```

---

## 3. 问题分析

### 当前时序

1. **拖动开始**：
   - `setIsDraggingGroup(true)` → React 重渲染 → `transition: 'none'`
   - 双重 RAF 延迟查询子节点 DOM

2. **拖动中**：
   - 直接操作 DOM：`groupElement.style.transform = ...`
   - 不触发 React 重渲染

3. **拖动结束**：
   - 更新 Store：`onUpdateGroup(id, { x: finalX, y: finalY })`
   - 此时 `transform` 还在，`transition` 还是 `'none'`
   - 清除 `dragGroupRef.current = null`
   - **双重 RAF 延迟**：
     - `setIsDraggingGroup(false)` → React 重渲染 → `transition: 'all 0.2s ease'`
     - 清除 `transform` 和 `willChange`

### 可能的问题

1. **React 重渲染时机**：
   - `setIsDraggingGroup(false)` 触发重渲染
   - 重渲染时 `transition` 从 `'none'` 变为 `'all 0.2s ease'`
   - 如果此时 `left/top` 和 `transform` 位置不一致，会触发过渡动画

2. **Store 更新时机**：
   - `onUpdateGroup` 可能是异步的（Zustand）
   - React 可能在 Store 更新前就重渲染了
   - 导致 `left/top` 还是旧值，清除 `transform` 后元素跳回旧位置

3. **双重 RAF 不够**：
   - 可能需要更多帧等待 React 完成渲染
   - 或者需要等待 Zustand Store 更新完成

---

## 4. 可能的解决方案

### 方案 A：完全禁用 transition

```typescript
style={{ 
  left: g.x, 
  top: g.y, 
  width: g.width, 
  height: g.height,
  borderColor: colorStyle.borderColor,
  borderWidth: colorStyle.borderWidth || '2px',
  background: colorStyle.background,
  boxShadow: colorStyle.boxShadow || 'none',
  borderRadius: 0,
  transition: 'none', // 🔥 完全禁用 transition
}}
```

### 方案 B：延迟更长时间

```typescript
// 在 endGroupDrag 中
requestAnimationFrame(() => {
  requestAnimationFrame(() => {
    requestAnimationFrame(() => { // 🔥 三重 RAF
      setIsDraggingGroup(false);
      setDraggingGroupOffset(null);
      // 清除 transform...
    });
  });
});
```

### 方案 C：等待 Store 更新完成

```typescript
// 在 endGroupDrag 中
onUpdateGroup(id, { x: finalX, y: finalY });

// 等待子节点更新
childNodes.forEach((child) => {
  onUpdateNode(child.id, {
    x: child.startX + worldDx,
    y: child.startY + worldDy,
  });
});

// 🔥 使用 setTimeout 等待 Store 更新
setTimeout(() => {
  setIsDraggingGroup(false);
  setDraggingGroupOffset(null);
  
  // 清除 transform
  const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
  if (groupElement) {
    groupElement.style.transform = '';
    groupElement.style.willChange = 'auto';
  }
  
  childNodes.forEach((child) => {
    if (child.element) {
      child.element.style.transform = '';
      child.element.style.willChange = 'auto';
    }
  });
}, 0);
```

### 方案 D：不清除 transform，让 React 接管

```typescript
// 在 endGroupDrag 中
onUpdateGroup(id, { x: finalX, y: finalY });

childNodes.forEach((child) => {
  onUpdateNode(child.id, {
    x: child.startX + worldDx,
    y: child.startY + worldDy,
  });
});

// 🔥 只清除状态，不清除 transform
// React 重渲染时会自动更新 left/top，transform 会被覆盖
dragGroupRef.current = null;
setIsDraggingGroup(false);
setDraggingGroupOffset(null);

// ❌ 不清除 transform，让 React 接管
// const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
// if (groupElement) {
//   groupElement.style.transform = '';
//   groupElement.style.willChange = 'auto';
// }
```

---

## 5. 调试建议

### 添加 console.log

```typescript
const endGroupDrag = useCallback((e: MouseEvent, currentScale: number) => {
  if (!dragGroupRef.current) return;

  const { id, startX, startY, mouseStartX, mouseStartY, childNodes } = dragGroupRef.current;

  const screenDx = e.clientX - mouseStartX;
  const screenDy = e.clientY - mouseStartY;
  const worldDx = screenDx / currentScale;
  const worldDy = screenDy / currentScale;
  const finalX = startX + worldDx;
  const finalY = startY + worldDy;

  console.log('[endGroupDrag] 开始', {
    id,
    startX,
    startY,
    finalX,
    finalY,
    worldDx,
    worldDy,
  });

  onSaveHistory();
  onUpdateGroup(id, { x: finalX, y: finalY });

  childNodes.forEach((child) => {
    onUpdateNode(child.id, {
      x: child.startX + worldDx,
      y: child.startY + worldDy,
    });
  });

  dragGroupRef.current = null;

  console.log('[endGroupDrag] Store 更新完成，准备清除 transform');

  requestAnimationFrame(() => {
    console.log('[endGroupDrag] RAF 1');
    requestAnimationFrame(() => {
      console.log('[endGroupDrag] RAF 2 - 清除状态和 transform');
      
      setIsDraggingGroup(false);
      setDraggingGroupOffset(null);

      const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
      if (groupElement) {
        console.log('[endGroupDrag] 清除组 transform', {
          currentTransform: groupElement.style.transform,
          currentLeft: groupElement.style.left,
          currentTop: groupElement.style.top,
        });
        groupElement.style.transform = '';
        groupElement.style.willChange = 'auto';
      }

      childNodes.forEach((child) => {
        if (child.element) {
          child.element.style.transform = '';
          child.element.style.willChange = 'auto';
        }
      });
      
      console.log('[endGroupDrag] 完成');
    });
  });
}, [onSaveHistory, onUpdateGroup, onUpdateNode]);
```

---

## 6. 完整文件路径

- `hooks/useGroup.ts` - 拖动逻辑
- `App.tsx` - 组渲染（第1705-1750行）
- `core/stores/groupStore.ts` - 组数据管理（如果有）

---

希望这些信息能帮助 Gemini 找到问题！
