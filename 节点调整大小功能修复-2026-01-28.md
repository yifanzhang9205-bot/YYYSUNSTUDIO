# 节点调整大小功能修复

**日期**：2026-01-28  
**状态**：✅ 已完成  
**问题**：节点右下角的调整大小功能失效，无法通过拖拽调整节点尺寸

---

## 问题分析

### 现象
用户反馈：之前节点可以通过右下角的交互点来调整大小，现在这个功能丧失了。

### 根本原因
在架构重构过程中，`handleGlobalMouseMove` 函数被简化，**删除了 resize 的实时更新逻辑**。

虽然 `handleGlobalMouseUp` 中有 resize 结束的处理（保存最终尺寸），但是在拖动过程中（`mousemove`）没有更新节点的大小，导致：
1. 用户拖动时看不到实时的调整效果
2. 节点尺寸不会随鼠标移动而变化
3. 功能看起来"失效"了

### 代码对比

**之前（有 resize 逻辑，但被删除了）**：
```typescript
const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
  // ... 其他逻辑
  
  // ❌ 这段代码被删除了
  if (resizingNodeId && resizeContextRef.current) {
    const { startX, startY, initialWidth, initialHeight, element } = resizeContextRef.current;
    
    if (element) {
      const dx = (clientX - startX) / scale;
      const dy = (clientY - startY) / scale;
      const newWidth = Math.max(200, initialWidth + dx);
      const newHeight = Math.max(100, initialHeight + dy);
      
      element.style.width = `${newWidth}px`;
      element.style.height = `${newHeight}px`;
      element.style.transition = 'none';
    }
    return;
  }
}, [/* ... */]);
```

**现在（没有 resize 逻辑）**：
```typescript
const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
  // ... 其他逻辑
  
  // === 节点拖拽：useDrag Hook 内部自动处理 ===
  // === Group 拖动：useGroup Hook 内部自动处理 ===
  
  // ❌ 没有 resize 逻辑！
}, [/* ... */]);
```

---

## 解决方案

### 修复思路
在 `handleGlobalMouseMove` 中**恢复 resize 的实时更新逻辑**。

### 修复代码

**文件**：`App.tsx`

**修改**：`handleGlobalMouseMove` 函数

```typescript
const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
    const { clientX, clientY } = e;
    
    // 性能优化：使用 ref 存储鼠标位置，避免每次都触发 React 重渲染
    mousePosRef.current = { x: clientX, y: clientY };
    
    // 只在绘制连接线时更新 state（触发重渲染）
    if (connectionStartRef.current) {
        if (mousePosRafRef.current) {
            cancelAnimationFrame(mousePosRafRef.current);
        }
        
        mousePosRafRef.current = requestAnimationFrame(() => {
            setMousePos({ x: clientX, y: clientY });
        });
    }
    
    if (selectionRect) { 
        updateBoxSelection(clientX, clientY);
        return; 
    }
    
    // 🔥 关键修复：恢复节点调整大小的实时更新逻辑
    if (resizingNodeId && resizeContextRef.current) {
        const { startX, startY, initialWidth, initialHeight, element } = resizeContextRef.current;
        
        if (element) {
            // 计算新的尺寸（考虑 scale）
            const dx = (clientX - startX) / scale;
            const dy = (clientY - startY) / scale;
            const newWidth = Math.max(200, initialWidth + dx); // 最小宽度 200px
            const newHeight = Math.max(100, initialHeight + dy); // 最小高度 100px
            
            // 使用 CSS 直接操作 DOM（性能优化）
            element.style.width = `${newWidth}px`;
            element.style.height = `${newHeight}px`;
            element.style.transition = 'none'; // 禁用 transition
        }
        return;
    }
    
    // === 节点拖拽：useDrag Hook 内部自动处理 ===
    // === Group 拖动：useGroup Hook 内部自动处理 ===
}, [selectionRect, resizingNodeId, scale, updateBoxSelection]);
```

### 关键改动
1. **添加 resize 检测**：检查 `resizingNodeId` 和 `resizeContextRef.current`
2. **计算新尺寸**：根据鼠标移动距离和 scale 计算新的宽度和高度
3. **最小尺寸限制**：宽度最小 200px，高度最小 100px
4. **直接操作 DOM**：使用 `element.style` 直接修改尺寸（性能优化）
5. **禁用 transition**：避免调整过程中的动画效果
6. **更新依赖数组**：添加 `resizingNodeId` 和 `scale`

---

## 技术细节

### 为什么直接操作 DOM？
1. **性能优化**：避免每次 mousemove 触发 React 重渲染
2. **实时反馈**：用户可以看到节点尺寸实时变化
3. **流畅体验**：60fps 的调整效果

### 为什么需要考虑 scale？
Canvas 有缩放功能，鼠标移动的屏幕距离需要除以 scale 才能得到世界坐标的距离。

```typescript
const dx = (clientX - startX) / scale; // 屏幕距离 → 世界距离
const dy = (clientY - startY) / scale;
```

### 为什么需要最小尺寸限制？
防止节点被调整得太小，影响可用性。

```typescript
const newWidth = Math.max(200, initialWidth + dx); // 最小 200px
const newHeight = Math.max(100, initialHeight + dy); // 最小 100px
```

### resize 的完整流程
1. **开始调整**（`onResizeMouseDown`）：
   - 保存初始尺寸和鼠标位置
   - 缓存 DOM 元素引用
   - 设置 `resizingNodeId` 状态

2. **调整过程**（`handleGlobalMouseMove`）：
   - 计算新的尺寸
   - 直接修改 DOM 元素的 width/height
   - 禁用 transition

3. **结束调整**（`handleGlobalMouseUp`）：
   - 读取最终尺寸
   - 更新 Store（React state）
   - 清除内联样式
   - 恢复 transition

---

## 测试验证

### 测试步骤
1. 创建一个节点
2. 将鼠标移到节点右下角
3. 看到调整大小的交互点（小圆点）
4. 按住鼠标左键拖拽
5. 观察节点尺寸是否实时变化
6. 松开鼠标
7. 观察节点尺寸是否保持

### 预期结果
- ✅ 右下角有调整大小的交互点
- ✅ 拖拽时节点尺寸实时变化
- ✅ 松开鼠标后尺寸保持
- ✅ 调整过程流畅无卡顿
- ✅ 节点不会被调整得太小（最小 200x100）

---

## 相关文件

- `App.tsx`：修复 `handleGlobalMouseMove` 函数
- `components/Node.tsx`：右下角调整大小的交互点（无需修改）

---

## 总结

### 问题根源
架构重构时删除了 `handleGlobalMouseMove` 中的 resize 逻辑，导致调整大小功能失效。

### 解决方案
恢复 resize 的实时更新逻辑，在 `handleGlobalMouseMove` 中处理节点尺寸的实时变化。

### 关键技术
- 直接操作 DOM（性能优化）
- 考虑 Canvas scale（坐标转换）
- 最小尺寸限制（可用性保护）
- 禁用 transition（流畅体验）

### 最终效果
- ✅ 调整大小功能恢复正常
- ✅ 实时反馈流畅
- ✅ 性能优化到位
- ✅ 用户体验良好

---

**修复完成！节点调整大小功能已恢复正常。** 🎉
