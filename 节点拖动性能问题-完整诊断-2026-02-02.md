# 节点拖动性能问题 - 完整诊断

## 实施日期
2026-02-02

## 问题描述
用户反馈：节点拖动不够丝滑，有滞涩感。

---

## 🔍 问题诊断

### 已实施的修改

1. **✅ 方案 1：辅助线检测节流**（`hooks/useDrag.ts`）
   - 每 3 帧检测一次辅助线（降低到 20 FPS）
   - 避免每次 mousemove 都触发 `setHelperLines`

2. **❌ 方案 3：从 useMemo 依赖项中移除 isDraggingNode 和 isDraggingGroup**（`App.tsx`）
   - **这个修改可能引入了新的 Bug！**
   - 原因：Node 组件需要知道是否在拖动，才能禁用 transition
   - 如果 useMemo 不依赖这些状态，Node 组件可能不会重新渲染

### 真正的问题

查看历史文档 `性能优化-完美丝滑拖动-2026-01-26-终极版.md`，发现：

**之前的丝滑方案：**
- 拖动过程中：使用 `transform` 直接操作 DOM（不触发 React 重渲染）
- 拖动结束时：更新 state（触发一次重渲染）
- **关键**：在 mouseup 时，临时禁用 transition，避免闪动

**当前代码的问题：**
1. `useDrag` Hook 已经实现了 transform 方案 ✅
2. 但是 Node.tsx 没有根据 `isDragging` 状态禁用 transition ❌
3. 导致拖动时仍然有 transition 效果，产生滞涩感 ❌

---

## 💡 正确的修复方案

### 方案 A：恢复 useMemo 依赖项（推荐）

**原理**：
- 让 Node 组件能够根据 `isDragging` 状态重新渲染
- 在拖动时禁用 transition，拖动结束后恢复

**修改位置**：`App.tsx` - useMemo 依赖项

```typescript
// 恢复依赖项
}, [nodes, selectedNodeIds, resizingNodeId, connectionStart, isDraggingNode, isDraggingGroup, handleNodeUpdate, ...]);
```

**优点**：
- 简单，只需恢复一行代码
- 符合 React 的设计理念

**缺点**：
- 拖动时会触发一次重渲染（但只有一次，不是每帧）

---

### 方案 B：在 useDrag Hook 中直接禁用 transition（更优）

**原理**：
- 在 `handleMouseDown` 时，直接设置 `element.style.transition = 'none'`
- 在 `handleMouseUp` 时，恢复 `element.style.transition = ''`
- 完全避免 React 重渲染

**修改位置**：`hooks/useDrag.ts`

```typescript
// handleMouseDown 中添加
element.style.transition = 'none';

// handleMouseUp 中添加
requestAnimationFrame(() => {
    if (element) {
        element.style.transition = '';
    }
});
```

**优点**：
- 性能最优，不触发 React 重渲染
- 符合之前的 transform 方案

**缺点**：
- 需要确保 Node 组件的最外层 div 有 transition 样式

---

## 🎯 推荐方案：方案 B

**理由**：
1. 性能最优，不触发 React 重渲染
2. 符合之前的 transform 方案
3. 代码更简洁，不需要修改 useMemo 依赖项

**实施步骤**：
1. 检查 `useDrag.ts` 中是否已经有 transition 禁用逻辑
2. 如果没有，添加 transition 禁用逻辑
3. 测试拖动性能

---

## 📊 性能对比

| 方案 | 拖动过程 | 拖动结束 | React 重渲染 | 性能 |
|------|---------|---------|-------------|------|
| 当前（有问题） | transform + transition | 更新 state | 1 次 | ❌ 滞涩 |
| 方案 A | transform + 重渲染禁用 transition | 更新 state | 2 次 | ✅ 较好 |
| 方案 B | transform + 直接禁用 transition | 更新 state | 1 次 | ✅✅ 最优 |

---

## 🔧 下一步

1. 检查 `useDrag.ts` 中的 transition 禁用逻辑
2. 如果缺失，实施方案 B
3. 测试拖动性能
4. 如果仍然有问题，考虑其他因素（如辅助线绘制、连接线更新等）

---

**实施时间**：2026-02-02  
**实施人员**：Kiro AI Assistant  
**状态**：🔍 诊断完成，等待实施
