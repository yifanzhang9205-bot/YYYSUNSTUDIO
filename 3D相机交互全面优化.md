# 3D 相机交互全面优化

## 修复的问题

### 1. 点击就跳视角 ❌ → ✅
**问题：** 轻轻点击就触发拖动，导致视角跳动

**原因：** 没有最小移动距离判断，任何鼠标移动都会触发拖动

**修复：**
```typescript
// 添加最小移动距离判断
const minDistance = 3; // 3 像素
if (!dragStateRef.current.hasMoved && Math.abs(dx) < minDistance && Math.abs(dy) < minDistance) {
  return; // 移动距离太小，不触发拖动
}

// 标记为已移动
dragStateRef.current.hasMoved = true;
```

**效果：** 只有真正拖动（移动超过 3 像素）才会改变视角

### 2. 滚轮还是控制画布 ❌ → ✅
**问题：** 选中节点后滚轮还是控制画布缩放

**原因：** `isExpanded` 硬编码为 `true`，没有传递真正的选中状态

**修复：**
```typescript
// Node.tsx 中
<MultiAngleCameraNode
  isExpanded={isSelected || false}  // ✅ 传递真正的选中状态
  // ...
/>
```

**效果：** 
- 选中时：滚轮控制相机推进/拉远
- 未选中时：滚轮控制画布缩放

### 3. 拖拽视野乱跳 ❌ → ✅
**问题：** 拖拽时视野会突然跳到其他位置

**原因：** 
1. 点击触发了微小的拖动
2. 拖动结束时总是更新 state，即使没有真正移动

**修复：**
```typescript
const onUp = (e: MouseEvent) => {
  // 只有真正移动过才更新 React state
  if (dragStateRef.current.hasMoved) {
    // 更新 state...
  }
  
  dragStateRef.current.hasMoved = false;
};
```

**效果：** 只有真正拖动过才会保存新的角度，点击不会改变视角

### 4. 距离档位不够细致 ❌ → ✅
**问题：** 只有 3 个档位（特写、中景、远景），不够用

**修复：** 扩展到 7 个档位
```typescript
const DISTANCE = [
  { v: 0, label: '特写' },      // 0
  { v: 2, label: '近景' },      // 2
  { v: 4, label: '中近' },      // 4
  { v: 6, label: '中景' },      // 6
  { v: 8, label: '中远' },      // 8
  { v: 10, label: '远景' },     // 10
  { v: 12, label: '大远景' },   // 12
];
```

**范围：** 0-14（连续可调）

**效果：** 更细致的距离控制，适合不同的拍摄需求

## 优化后的交互逻辑

### 拖动逻辑
```
用户按下鼠标
  → 记录起始位置和角度
  → 标记 isDragging = true, hasMoved = false

用户移动鼠标
  → 计算移动距离 (dx, dy)
  → 如果移动距离 < 3px 且未标记为已移动
      → 忽略（防止点击触发拖动）
  → 如果移动距离 >= 3px
      → 标记 hasMoved = true
      → 直接更新 Three.js 场景
      → 立即渲染

用户松开鼠标
  → 如果 hasMoved = true
      → 更新 React state（保存最终值）
  → 如果 hasMoved = false
      → 不更新 state（只是点击，不是拖动）
  → 重置状态
```

### 滚轮逻辑
```
用户滚动滚轮
  → 检查节点是否被选中 (isExpanded)
  → 如果未选中
      → 不阻止事件冒泡
      → 画布处理滚轮（缩放画布）
  → 如果已选中
      → 阻止事件冒泡
      → 直接更新 Three.js 场景
      → 相机推进/拉远
      → 立即渲染
```

## 距离映射关系

### 距离值 → 实际距离
```typescript
const dist = 2.5 + cameraZoom * 0.5;
```

| 档位 | cameraZoom | 实际距离 | 说明 |
|------|-----------|---------|------|
| 特写 | 0 | 2.5 | 极近，适合面部特写 |
| 近景 | 2 | 3.5 | 近距离，适合半身 |
| 中近 | 4 | 4.5 | 中近距离，适合全身 |
| 中景 | 6 | 5.5 | 中等距离，适合人物+环境 |
| 中远 | 8 | 6.5 | 中远距离，环境为主 |
| 远景 | 10 | 7.5 | 远距离，大场景 |
| 大远景 | 12 | 8.5 | 极远，全景 |
| 最远 | 14 | 9.5 | 超远，鸟瞰 |

## 操作说明

### 1. 选中节点
- **方法：** 点击节点的任意位置
- **视觉反馈：**
  - 节点边框变为青色高光
  - 操作提示变为 "拖拽旋转 · 滚轮推进"

### 2. 旋转相机（拖拽）
- **操作：** 在 3D 视口中拖拽（移动超过 3 像素）
- **效果：**
  - 水平拖动 → 改变方位角（绕目标旋转）
  - 垂直拖动 → 改变仰角（上下移动）
- **注意：** 轻轻点击不会触发拖动

### 3. 推进/拉远（滚轮）
- **前提：** 节点已选中
- **操作：** 滚动鼠标滚轮
- **效果：**
  - 向上滚 → 拉远（远景）
  - 向下滚 → 推进（特写）
  - 范围：0-14（特写到超远）
  - 画布不受影响

### 4. 推进/拉远（Shift + 拖拽）
- **操作：** 按住 Shift + 拖拽
- **效果：**
  - 向上拖 → 拉远
  - 向下拖 → 推进
- **优势：** 不需要选中节点，始终可用

### 5. 快速预设
- **操作：** 点击设置按钮，选择预设档位
- **预设：**
  - 方位角：8 个方向（正面、右前、右侧...）
  - 仰角：4 个角度（俯拍、高角度、平视、仰拍）
  - 距离：7 个档位（特写到大远景）

## 测试步骤

### 测试 1：点击不触发拖动
1. 打开多角度相机节点
2. 在 3D 视口中轻轻点击（不移动鼠标）
3. **预期：** 视角不变，没有跳动

### 测试 2：拖动流畅
1. 在 3D 视口中拖拽（移动超过 3 像素）
2. **预期：** 视角跟随鼠标移动，非常流畅

### 测试 3：滚轮控制相机（选中时）
1. 点击节点选中（边框变青色）
2. 滚动鼠标滚轮
3. **预期：** 相机推进/拉远，画布不动

### 测试 4：滚轮控制画布（未选中时）
1. 点击画布空白处（取消选中）
2. 滚动鼠标滚轮
3. **预期：** 画布缩放，相机不动

### 测试 5：Shift + 拖拽推进
1. 按住 Shift + 在 3D 视口中拖拽
2. **预期：** 相机推进/拉远，非常流畅

### 测试 6：距离档位
1. 打开设置面板
2. 点击不同的距离档位
3. **预期：** 相机移动到对应距离

## 技术细节

### 最小移动距离判断
```typescript
const minDistance = 3; // 3 像素

// 计算移动距离
const dx = e.clientX - dragStateRef.current.startX;
const dy = e.clientY - dragStateRef.current.startY;

// 判断是否超过最小距离
if (!hasMoved && Math.abs(dx) < minDistance && Math.abs(dy) < minDistance) {
  return; // 忽略微小移动
}
```

**为什么是 3 像素？**
- 太小（1-2px）：手抖会触发拖动
- 太大（5+px）：拖动不够灵敏
- 3px：平衡点，既防止误触又保持灵敏

### 事件冒泡控制
```typescript
const handleWheel = (e: React.WheelEvent) => {
  if (!isExpanded) {
    return; // 不阻止冒泡，让画布处理
  }
  
  e.stopPropagation(); // 阻止冒泡到画布
  e.preventDefault();  // 阻止默认滚动
  
  // 控制相机...
};
```

### 性能优化
- ✅ 拖动时直接操作 Three.js，不经过 React
- ✅ 只在拖动结束时更新 React state
- ✅ 复用 BufferAttribute，不重新分配内存
- ✅ 立即渲染，60 FPS 流畅体验

## 总结

优化后的 3D 相机：
- ✅ 点击不会跳视角（最小移动距离判断）
- ✅ 滚轮正确控制相机（选中状态判断）
- ✅ 拖拽流畅不乱跳（只在真正移动时更新）
- ✅ 距离档位更细致（7 个档位，0-14 范围）
- ✅ 60 FPS 丝滑体验（性能优化）

现在的交互更加稳定、流畅、符合直觉！
