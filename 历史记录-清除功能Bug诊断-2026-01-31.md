# 历史记录清除功能 Bug 诊断报告

**日期**: 2026-01-31  
**问题**: 清除缓存和下载并清除功能没有真正实现

---

## 🔴 问题 1：清除缓存功能 - 完全是花架子

### 问题描述

在 `components/SidebarDock.tsx` 第 160-192 行，"清除缓存"按钮的实现：

```typescript
<button 
    onClick={() => {
        const unselectedCount = filteredAssets.length - selectedImageIds.size;
        if (unselectedCount === 0) {
            alert('没有未选中的图片');
            return;
        }
        
        const confirmed = window.confirm(
            `⚠️ 确认删除？\n\n将永久删除 ${unselectedCount} 张未选中的图片\n\n此操作不可恢复！`
        );
        
        if (confirmed) {
            const unselectedIds = filteredAssets
                .filter(img => !selectedImageIds.has(img.id))
                .map(img => img.id);
            
            unselectedIds.forEach(id => onDeleteAsset(id));  // ❌ 问题在这里
            setSelectedImageIds(new Set());
            
            alert(`✅ 已删除 ${unselectedCount} 张图片\n\n内存已释放！`);
        }
    }}
>
```

### 根本原因

**`onDeleteAsset` 的实现有严重问题！**

在 `App.tsx` 第 2174-2176 行：

```typescript
onDeleteAsset={(id) => {
    const newHistory = assetHistory.filter(a => a.id !== id);
    setAssetHistory(newHistory);  // ❌ 只更新了 Store，没有保存到 IndexedDB
}}
```

**问题分析：**

1. ✅ **正确**：调用 `setAssetHistory(newHistory)` 更新了 Zustand Store
2. ❌ **错误**：**没有立即保存到 IndexedDB**
3. ❌ **错误**：**没有清理 Blob URL**（内存泄漏）
4. ⚠️ **依赖 useEffect**：虽然 App.tsx 第 770-777 行有 useEffect 监听 `assetHistory` 变化并保存，但这是**异步的、延迟的**

```typescript
// App.tsx 第 770-777 行
useEffect(() => {
    if (!isLoaded) return; 
    
    saveToStorage('assets', assetHistory);  // ⚠️ 异步保存，有延迟
    saveToStorage('workflows', workflows);
    saveToStorage('nodes', Array.from(nodes.values()));
    saveToStorage('connections', connections);
    saveToStorage('groups', groups);
}, [assetHistory, workflows, nodes, connections, groups, isLoaded]);
```

**为什么是花架子？**

- 用户点击"清除缓存"后，UI 上图片消失了（因为 Store 更新了）
- 但是：
  1. **Blob URL 没有被释放**（内存泄漏）
  2. **IndexedDB 保存是异步的**（如果用户立即刷新页面，数据可能还在）
  3. **没有错误处理**（如果保存失败，用户不知道）

---

## 🔴 问题 2：下载并清除功能 - 可能没有真正清除

### 问题描述

在 `hooks/useAssetHistory.ts` 第 60-125 行，`downloadSelectedImagesAndClear` 函数：

```typescript
const downloadSelectedImagesAndClear = useCallback(async (selectedIds: Set<string>) => {
    // ... 下载逻辑 ...
    
    // 下载完成后，清除选中的图片
    const newAssetHistory = assetHistory.filter(a => !selectedIds.has(a.id));
    setAssetHistory(newAssetHistory);  // ✅ 更新 Store
    
    // 立即保存到 localStorage（不等待 useEffect）
    await saveToStorage('assets', newAssetHistory);  // ✅ 立即保存
    
    // 清理 Blob URL（避免内存泄漏）
    assetsToDownload.forEach(asset => {
        if (asset.src && asset.src.startsWith('blob:')) {
            URL.revokeObjectURL(asset.src);  // ✅ 清理 Blob URL
        }
    });
}, [assetHistory, setAssetHistory]);
```

### 分析

**这个函数的实现是正确的！**

1. ✅ 更新 Store：`setAssetHistory(newAssetHistory)`
2. ✅ 立即保存：`await saveToStorage('assets', newAssetHistory)`
3. ✅ 清理内存：`URL.revokeObjectURL(asset.src)`

**但是有一个潜在问题：**

- 如果下载过程中出错（网络问题、ZIP 生成失败），会抛出异常
- 异常被 `catch` 捕获后，只打印日志，**不会清除数据**
- 用户可能以为下载成功了，但实际上失败了，数据也没清除

---

## 🔴 问题 3：IndexedDB 中的 Blob 数据没有清除

### 问题描述

在 `handleAssetGenerated` 中（`hooks/useAssetHistory.ts` 第 25-56 行）：

```typescript
const handleAssetGenerated = useCallback(async (
    type: 'image' | 'video' | 'audio', 
    src: string, 
    title: string
) => {
    let assetId = `a-${Date.now()}`;
    
    if (src.startsWith('blob:')) {
        try {
            // 从 Blob URL 获取 Blob
            const response = await fetch(src);
            const blob = await response.blob();
            
            // 存储到 IndexedDB
            await saveToStorage(`asset-${assetId}`, blob);  // ❌ 存储了 Blob
        } catch (error) {
            console.error('[AssetHistory] 保存 Blob 失败:', error);
        }
    }
    
    // 使用 Store 的 addHistory 方法
    const exists = assetHistory.find(a => a.src === src);
    if (!exists) {
        addHistory({ 
            id: assetId, 
            type, 
            src, 
            title, 
            timestamp: Date.now() 
        });
    }
}, [assetHistory, addHistory]);
```

**问题：**

- 当用户删除历史记录时，只删除了 `assetHistory` 数组中的记录
- **但是 IndexedDB 中的 `asset-${assetId}` Blob 数据没有删除！**
- 这会导致：
  1. **IndexedDB 占用空间越来越大**（Blob 数据永远不会被清理）
  2. **内存泄漏**（虽然 Blob URL 被释放了，但 IndexedDB 中的 Blob 还在）

---

## 📊 问题总结

| 功能 | 问题 | 严重程度 | 影响 |
|------|------|---------|------|
| **清除缓存** | 只更新 Store，没有立即保存，没有清理 Blob URL，没有清理 IndexedDB | 🔴 严重 | 内存泄漏 + 数据可能没删除 |
| **下载并清除** | 实现正确，但错误处理不完善 | 🟡 中等 | 下载失败时数据没清除 |
| **IndexedDB Blob** | 删除历史记录时，IndexedDB 中的 Blob 没有删除 | 🔴 严重 | IndexedDB 占用空间越来越大 |

---

## 🔧 修复方案

### 修复 1：重构 `onDeleteAsset`

在 `App.tsx` 中，将 `onDeleteAsset` 改为：

```typescript
onDeleteAsset={async (id) => {
    // 1. 找到要删除的资源
    const asset = assetHistory.find(a => a.id === id);
    
    // 2. 清理 Blob URL（避免内存泄漏）
    if (asset && asset.src && asset.src.startsWith('blob:')) {
        URL.revokeObjectURL(asset.src);
    }
    
    // 3. 删除 IndexedDB 中的 Blob 数据
    if (asset && asset.id) {
        try {
            await deleteFromStorage(`asset-${asset.id}`);
        } catch (error) {
            console.error('[AssetHistory] 删除 IndexedDB Blob 失败:', error);
        }
    }
    
    // 4. 更新 Store
    const newHistory = assetHistory.filter(a => a.id !== id);
    setAssetHistory(newHistory);
    
    // 5. 立即保存到 IndexedDB
    await saveToStorage('assets', newHistory);
}}
```

### 修复 2：增强 `downloadSelectedImagesAndClear`

在 `hooks/useAssetHistory.ts` 中，增强错误处理：

```typescript
const downloadSelectedImagesAndClear = useCallback(async (selectedIds: Set<string>) => {
    if (selectedIds.size === 0) return;
    
    const assetsToDownload = assetHistory.filter(a => selectedIds.has(a.id));
    if (assetsToDownload.length === 0) return;
    
    try {
        // ... 下载逻辑 ...
        
        // ✅ 下载成功后才清除
        const newAssetHistory = assetHistory.filter(a => !selectedIds.has(a.id));
        setAssetHistory(newAssetHistory);
        await saveToStorage('assets', newAssetHistory);
        
        // 清理 Blob URL
        assetsToDownload.forEach(asset => {
            if (asset.src && asset.src.startsWith('blob:')) {
                URL.revokeObjectURL(asset.src);
            }
        });
        
        // 🔥 新增：清理 IndexedDB 中的 Blob 数据
        for (const asset of assetsToDownload) {
            if (asset.id) {
                try {
                    await deleteFromStorage(`asset-${asset.id}`);
                } catch (error) {
                    console.error(`[AssetHistory] 删除 IndexedDB Blob ${asset.id} 失败:`, error);
                }
            }
        }
        
        // ✅ 成功提示
        alert(`✅ 已下载并清除 ${assetsToDownload.length} 张图片`);
        
    } catch (error) {
        console.error('[AssetHistory] 批量下载失败:', error);
        // ❌ 失败提示
        alert(`❌ 下载失败：${error.message}\n\n数据未清除，请重试`);
    }
}, [assetHistory, setAssetHistory]);
```

### 修复 3：添加 `deleteFromStorage` 函数

在 `services/storage.ts` 中，添加删除函数：

```typescript
/**
 * 从 IndexedDB 删除数据
 */
export const deleteFromStorage = async (key: string): Promise<void> => {
    const db = await openDB();
    const tx = db.transaction('app-data', 'readwrite');
    await tx.objectStore('app-data').delete(key);
    await tx.done;
};
```

---

## 🎯 验收标准

修复后，需要验证：

1. **清除缓存**：
   - [ ] 点击"清除缓存"后，UI 上图片立即消失
   - [ ] 刷新页面后，图片不会重新出现
   - [ ] 浏览器开发者工具 → Application → IndexedDB → 确认 Blob 数据被删除
   - [ ] 浏览器任务管理器 → 确认内存占用下降

2. **下载并清除**：
   - [ ] 点击"下载并清除"后，图片成功下载
   - [ ] 下载完成后，UI 上图片立即消失
   - [ ] 刷新页面后，图片不会重新出现
   - [ ] IndexedDB 中的 Blob 数据被删除
   - [ ] 如果下载失败，显示错误提示，数据不清除

3. **内存管理**：
   - [ ] 删除 100 张图片后，内存占用明显下降
   - [ ] IndexedDB 占用空间明显减少
   - [ ] 没有 Blob URL 泄漏（开发者工具 → Memory → 检查 Blob URL 数量）

---

## 📝 总结

**清除缓存功能确实是花架子！**

- ❌ 只更新了 UI，没有真正清理数据
- ❌ Blob URL 没有释放（内存泄漏）
- ❌ IndexedDB 中的 Blob 没有删除（磁盘空间泄漏）
- ❌ 依赖异步 useEffect，不可靠

**下载并清除功能基本正确，但：**

- ⚠️ 错误处理不完善
- ❌ IndexedDB 中的 Blob 没有删除

**需要立即修复！**
