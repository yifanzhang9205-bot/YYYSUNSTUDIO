# 双击画布菜单 - 清理和重构方案

**日期**：2026-02-03  
**状态**：待实施  
**优先级**：中

---

## 📋 问题分析

### 当前状态

**双击画布菜单的位置：**
- 文件：`App.tsx`
- 行号：第 2085 行
- 代码：
```typescript
{[NodeType.STORY_STUDIO, NodeType.CHARACTER_REFERENCE, NodeType.SCENE_REFERENCE, NodeType.STORYBOARD_SHOT].map(t => { ... })}
```

**问题：**
1. ❌ 菜单项是硬编码的数组，没有使用 NodeRegistry
2. ❌ 菜单逻辑在 App.tsx 中，违反三层架构
3. ❌ 已删除的 4 个节点类型仍然在菜单中：
   - `NodeType.STORY_STUDIO`（创意工作室）
   - `NodeType.CHARACTER_REFERENCE`（角色参考）
   - `NodeType.SCENE_REFERENCE`（场景参考）
   - `NodeType.STORYBOARD_SHOT`（分镜生成）

### 架构分析

**当前架构（错误）：**
```
App.tsx (UI Layer)
  ├── 硬编码菜单项数组
  ├── 渲染菜单
  └── 处理点击事件
```

**正确架构（应该是）：**
```
App.tsx (UI Layer)
  ↓ 调用
useContextMenu.ts (Hooks Layer)
  ├── 获取菜单项
  ├── 处理菜单逻辑
  ↓ 获取数据
NodeRegistry.ts (Core Layer)
  └── 提供节点定义
```

---

## 🎯 修复方案

### 方案 A：快速修复（推荐）

**目标**：删除废弃节点，保持现有架构

**步骤：**

1. **修改 App.tsx 第 2085 行**
   ```typescript
   // ❌ 删除前（包含 4 个废弃节点）
   {[NodeType.STORY_STUDIO, NodeType.CHARACTER_REFERENCE, NodeType.SCENE_REFERENCE, NodeType.STORYBOARD_SHOT].map(t => { ... })}
   
   // ✅ 删除后（只保留剧本节点）
   {[NodeType.SCRIPT_NODE].map(t => { ... })}
   ```

2. **验证修改**
   - 双击画布，检查菜单是否正确显示
   - 确认只显示"剧本节点"，不显示废弃节点
   - 测试创建剧本节点功能

**优点：**
- 快速简单，5 分钟完成
- 风险低，只改一行代码
- 立即解决问题

**缺点：**
- 没有解决架构问题
- 菜单项仍然是硬编码的
- 未来添加节点仍需修改 App.tsx

**适用场景：**
- 用户急需修复
- 暂时没有时间重构
- 先解决燃眉之急

---

### 方案 B：架构重构（推荐，但需要更多时间）

**目标**：彻底解决架构问题，符合三层架构

**步骤：**

#### 第 1 步：修改 NodeRegistry，添加分类信息

```typescript
// core/registry/NodeRegistry.ts

export function initializeNodeRegistry(): void {
  nodeRegistry.registerAll([
    // ========== 基础节点 ==========
    {
      type: NodeType.PROMPT_INPUT,
      name: '创意描述',
      iconName: 'Type',
      category: 'basic', // ✅ 添加分类
      // ...
    },
    {
      type: NodeType.IMAGE_GENERATOR,
      name: '文字生图',
      iconName: 'Image',
      category: 'basic',
      // ...
    },
    {
      type: NodeType.VIDEO_GENERATOR,
      name: '文生视频',
      iconName: 'Video',
      category: 'basic',
      // ...
    },

    // ========== 故事创作节点 ==========
    {
      type: NodeType.SCRIPT_NODE,
      name: '剧本节点',
      iconName: 'Film',
      category: 'story', // ✅ 添加分类
      // ...
    },

    // ========== 高级工具节点 ==========
    {
      type: NodeType.MULTI_ANGLE_CAMERA,
      name: '多角度相机',
      iconName: 'LayoutTemplate',
      category: 'advanced', // ✅ 添加分类
      // ...
    },
    {
      type: NodeType.GRID_SPLITTER,
      name: '九宫格处理',
      iconName: 'Grid3X3',
      category: 'advanced',
      // ...
    },
  ]);
}

// ✅ 添加工具函数：按分类获取节点
export function getNodesByCategory(category: 'basic' | 'story' | 'advanced'): NodeDefinition[] {
  return nodeRegistry.getByCategory(category);
}
```

#### 第 2 步：创建 useContextMenu Hook

```typescript
// hooks/useContextMenu.ts

import { useState, useCallback } from 'react';
import { NodeType } from '../types';
import { getNodesByCategory } from '../core/registry/NodeRegistry';

export interface ContextMenuState {
  visible: boolean;
  x: number;
  y: number;
  id: string;
}

export interface ContextMenuTarget {
  type: 'node' | 'create' | 'group' | 'connection' | 'smart-connect';
  id?: string;
  from?: string;
  to?: string;
  portType?: 'input' | 'output';
  compatibleTypes?: NodeType[];
}

export function useContextMenu() {
  const [contextMenu, setContextMenu] = useState<ContextMenuState>({
    visible: false,
    x: 0,
    y: 0,
    id: '',
  });
  
  const [contextMenuTarget, setContextMenuTarget] = useState<ContextMenuTarget | null>(null);

  const openContextMenu = useCallback((menu: ContextMenuState, target: ContextMenuTarget) => {
    setContextMenu(menu);
    setContextMenuTarget(target);
  }, []);

  const closeContextMenu = useCallback(() => {
    setContextMenu({ visible: false, x: 0, y: 0, id: '' });
    setContextMenuTarget(null);
  }, []);

  // ✅ 获取菜单项（从 NodeRegistry）
  const getMenuItems = useCallback(() => {
    return {
      basic: getNodesByCategory('basic'),
      story: getNodesByCategory('story'),
      advanced: getNodesByCategory('advanced'),
    };
  }, []);

  return {
    contextMenu,
    contextMenuTarget,
    openContextMenu,
    closeContextMenu,
    getMenuItems,
  };
}
```

#### 第 3 步：修改 App.tsx，使用 Hook

```typescript
// App.tsx

// ✅ 使用 useContextMenu Hook
const {
  contextMenu,
  contextMenuTarget,
  openContextMenu,
  closeContextMenu,
  getMenuItems,
} = useContextMenu();

// ✅ 获取菜单项
const menuItems = getMenuItems();

// ... 在渲染部分 ...

{contextMenuTarget?.type === 'create' && (
  <>
    {/* 基础节点 */}
    <div className="px-3 py-1.5 text-[9px] font-bold uppercase tracking-wider text-white/30">
      基础节点
    </div>
    {menuItems.basic.map(def => {
      const ItemIcon = getNodeIcon(def.type);
      return (
        <button
          key={def.type}
          className="w-full text-left px-3 py-1.5 text-[11px] font-medium text-white/90 hover:bg-white/10 rounded-lg flex items-center gap-2 transition-colors"
          onClick={() => {
            addNode(def.type, (contextMenu.x - pan.x) / scale, (contextMenu.y - pan.y) / scale);
            closeContextMenu();
          }}
        >
          <ItemIcon size={13} className="text-cyan-400" />
          {def.name}
        </button>
      );
    })}

    {/* 故事创作 */}
    {menuItems.story.length > 0 && (
      <>
        <div className="px-3 py-1.5 text-[9px] font-bold uppercase tracking-wider text-white/30 mt-1 border-t border-white/5 pt-1.5">
          故事创作
        </div>
        {menuItems.story.map(def => {
          const ItemIcon = getNodeIcon(def.type);
          return (
            <button
              key={def.type}
              className="w-full text-left px-3 py-1.5 text-[11px] font-medium text-white/90 hover:bg-white/10 rounded-lg flex items-center gap-2 transition-colors"
              onClick={() => {
                addNode(def.type, (contextMenu.x - pan.x) / scale, (contextMenu.y - pan.y) / scale);
                closeContextMenu();
              }}
            >
              <ItemIcon size={13} className="text-purple-400" />
              {def.name}
            </button>
          );
        })}
      </>
    )}

    {/* 高级工具 */}
    {menuItems.advanced.length > 0 && (
      <>
        <div className="px-3 py-1.5 text-[9px] font-bold uppercase tracking-wider text-white/30 mt-1 border-t border-white/5 pt-1.5">
          高级工具
        </div>
        {menuItems.advanced.map(def => {
          const ItemIcon = getNodeIcon(def.type);
          return (
            <button
              key={def.type}
              className="w-full text-left px-3 py-1.5 text-[11px] font-medium text-white/90 hover:bg-white/10 rounded-lg flex items-center gap-2 transition-colors"
              onClick={() => {
                addNode(def.type, (contextMenu.x - pan.x) / scale, (contextMenu.y - pan.y) / scale);
                closeContextMenu();
              }}
            >
              <ItemIcon size={13} className="text-pink-400" />
              {def.name}
            </button>
          );
        })}
      </>
    )}
  </>
)}
```

**优点：**
- ✅ 符合三层架构
- ✅ 菜单项从 NodeRegistry 获取，自动同步
- ✅ 未来添加节点，只需修改 NodeRegistry
- ✅ 代码更清晰，职责分离

**缺点：**
- 需要更多时间（约 30-60 分钟）
- 需要创建新文件（useContextMenu.ts）
- 需要修改 App.tsx 的多处代码

**适用场景：**
- 有时间进行重构
- 想彻底解决架构问题
- 为未来扩展做准备

---

## 🤔 用户决策

**问题 1：选择哪个方案？**

- **方案 A（快速修复）**：5 分钟，只改一行代码，立即解决问题
- **方案 B（架构重构）**：30-60 分钟，创建新 Hook，彻底解决架构问题

**问题 2：是否需要修改 App.tsx？**

- **方案 A**：需要修改 App.tsx（1 行代码）
- **方案 B**：需要修改 App.tsx（约 50 行代码，但是符合架构规范）

**问题 3：修改 App.tsx 的理由？**

1. **为什么必须改 App.tsx？**
   - 双击画布菜单的代码在 App.tsx 中
   - 需要删除废弃节点的引用
   - 方案 A：只改菜单项数组（1 行）
   - 方案 B：抽离菜单逻辑到 Hook（50 行）

2. **不改会有什么问题？**
   - 用户双击画布时，仍然会看到 4 个废弃节点
   - 点击废弃节点会报错（因为组件已删除）
   - 用户体验差

3. **有没有其他方案？**
   - ❌ 没有其他方案，菜单代码在 App.tsx 中，必须修改

4. **改了会有什么影响？**
   - 方案 A：影响极小，只改 1 行代码
   - 方案 B：影响较大，但符合架构规范，长期收益高

5. **这属于允许的情况吗？**
   - ✅ 是的，属于"重构过程中"和"修复 Bug"
   - ✅ 方案 A：快速修复，风险低
   - ✅ 方案 B：架构重构，符合项目目标

---

## 📊 方案对比

| 维度 | 方案 A（快速修复） | 方案 B（架构重构） |
|------|-------------------|-------------------|
| **时间** | 5 分钟 | 30-60 分钟 |
| **代码量** | 1 行 | 约 150 行（新增 Hook + 修改 App.tsx） |
| **风险** | 极低 | 中等 |
| **架构** | 不符合三层架构 | 符合三层架构 |
| **可维护性** | 低（硬编码） | 高（从 Registry 获取） |
| **可扩展性** | 低（需修改 App.tsx） | 高（只需修改 Registry） |
| **长期收益** | 低 | 高 |

---

## 🎯 推荐方案

**推荐：方案 A（快速修复）**

**理由：**
1. 用户急需修复，快速解决问题
2. 风险极低，只改 1 行代码
3. 不影响其他功能
4. 可以后续再进行架构重构

**后续计划：**
- 先用方案 A 快速修复
- 等有时间后，再实施方案 B 进行架构重构
- 将菜单逻辑抽离到 useContextMenu Hook
- 从 NodeRegistry 获取菜单项

---

## 📝 实施步骤（方案 A）

### 第 1 步：修改 App.tsx

**位置**：第 2085 行

**修改前：**
```typescript
{[NodeType.STORY_STUDIO, NodeType.CHARACTER_REFERENCE, NodeType.SCENE_REFERENCE, NodeType.STORYBOARD_SHOT].map(t => { ... })}
```

**修改后：**
```typescript
{[NodeType.SCRIPT_NODE].map(t => { ... })}
```

### 第 2 步：验证修改

1. 启动应用
2. 双击画布空白处
3. 检查菜单是否正确显示：
   - ✅ 基础节点：创意描述、文字生图、文生视频
   - ✅ 故事创作：剧本节点（只有 1 个）
   - ✅ 高级工具：多角度相机、九宫格处理
4. 点击"剧本节点"，检查是否能正常创建
5. 确认没有显示废弃节点

### 第 3 步：创建 Git 提交

```bash
git add App.tsx
git commit -m "清理双击画布菜单中的废弃节点

- 删除 4 个废弃节点：STORY_STUDIO, CHARACTER_REFERENCE, SCENE_REFERENCE, STORYBOARD_SHOT
- 只保留剧本节点（SCRIPT_NODE）
- 修复双击画布时显示废弃节点的问题

相关文档：
- 节点删除完成-2026-02-03.md
- .kiro/specs/node-consolidation/requirements.md"
```

---

## ✅ 验收标准

- [ ] 双击画布，菜单正确显示
- [ ] 基础节点：3 个（创意描述、文字生图、文生视频）
- [ ] 故事创作：1 个（剧本节点）
- [ ] 高级工具：2 个（多角度相机、九宫格处理）
- [ ] 不显示废弃节点
- [ ] 点击"剧本节点"能正常创建
- [ ] 其他菜单功能正常（节点右键菜单、连接线菜单等）
- [ ] 编译通过，无 TypeScript 错误
- [ ] 功能测试通过

---

## 📚 相关文档

- **节点删除文档**：`节点删除完成-2026-02-03.md`
- **需求文档**：`.kiro/specs/node-consolidation/requirements.md`
- **架构文档**：`ARCHITECTURE.md`
- **入职规则**：`.kiro/steering/project-onboarding.md`

---

**文档版本**：v1.0  
**创建日期**：2026-02-03  
**状态**：待用户决策
