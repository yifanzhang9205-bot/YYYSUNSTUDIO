# 小地图功能说明

## 功能概述
在画布右下角添加了小地图（Minimap）功能，用于快速导航和定位节点，特别是在边角生成的节点。

## 主要功能

### 1. 节点可视化
- **白色方块**：普通节点
- **红色方块**：孤立节点（没有连接线的节点）
- **灰色线条**：节点之间的连接关系
- **青色框**：当前视口位置

### 2. 交互功能
- **点击跳转**：点击小地图任意位置，视口立即跳转到该位置
- **拖动导航**：按住鼠标拖动，实时移动视口
- **折叠/展开**：点击地图图标可以折叠或展开小地图面板

### 3. UI 集成
- 小地图按钮和缩放按钮集成在同一个黑色圆角控制栏上
- 小地图面板绝对定位，向上弹出，不占用控制栏空间
- 控制栏已优化为原来的约 1/3 大小，更加紧凑

## 控制栏优化

### 尺寸调整
- **整体容器**：`gap-3 px-4 py-2` → `gap-2 px-3 py-1.5`（更紧凑）
- **缩放滑块**：`w-12` (48px) → `w-8` (32px)（约为原来的 1/3）
- **百分比显示**：`w-8` → `w-7`（更窄）
- **元素间距**：`gap-2` → `gap-1.5`（更紧密）
- **分隔线高度**：`h-6` → `h-5`（更矮）
- **适配按钮间距**：`ml-2 pl-3` → `ml-1 pl-2.5`（更紧凑）

### 布局结构
```
[小地图按钮] | [缩小] [滑块] [百分比] [放大] | [适配视图]
```

## 使用场景

### 1. 查找边角节点
当在画布边角生成节点后，可以通过小地图快速定位：
1. 点击右下角的地图图标展开小地图
2. 红色方块表示孤立节点（可能是刚生成的）
3. 点击红色方块位置，视口立即跳转

### 2. 全局导航
- 查看所有节点的分布情况
- 快速跳转到任意区域
- 了解节点之间的连接关系

### 3. 视口定位
- 青色框显示当前视口位置
- 拖动青色框可以移动视口
- 适合在大型画布中快速导航

## 技术实现

### 组件位置
- `components/Minimap.tsx`：小地图组件
- `App.tsx`：集成到底部控制栏（约 2920 行）

### 核心功能
1. **Canvas 渲染**：使用 Canvas API 绘制节点、连接线、视口
2. **坐标转换**：画布坐标 ↔ 小地图坐标的双向转换
3. **孤立节点检测**：自动识别没有连接线的节点
4. **交互处理**：点击、拖动、折叠/展开

### 性能优化
- 使用 `useRef` 避免不必要的重渲染
- Canvas 高 DPI 支持（适配高分辨率屏幕）
- 只在展开时渲染 Canvas

## 后续优化建议

1. **节点分组显示**：不同类型的节点用不同颜色区分
2. **搜索功能**：在小地图中搜索特定节点
3. **缩略图预览**：鼠标悬停显示节点缩略图
4. **自定义尺寸**：允许用户调整小地图大小

## 更新日志

### 2025-01-24
- ✅ 实现小地图基础功能
- ✅ 集成到底部控制栏
- ✅ 优化控制栏尺寸（缩小到原来的 1/3）
- ✅ 添加孤立节点高亮
- ✅ 支持点击和拖动导航
