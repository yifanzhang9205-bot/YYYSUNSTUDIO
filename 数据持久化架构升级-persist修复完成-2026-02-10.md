# 数据持久化架构升级 - persist 修复完成

**问题**：`nodes.values is not a function`  
**原因**：persist 中间件序列化/反序列化逻辑错误  
**解决方案**：使用 `partialize` 和 `merge` 正确处理 Map 数据结构  
**时间**：2026-02-10

---

## 🔧 修复内容

### 1. 导入 persist 中间件

```typescript
import { persist, createJSONStorage } from 'zustand/middleware';
```

### 2. 修复序列化逻辑

**错误的方式**（导致崩溃）：
```typescript
serialize: (state) => {
  return JSON.stringify({
    nodes: Array.from(state.state.nodes.entries()), // ❌ 路径错误
  });
},
deserialize: (str) => {
  const data = JSON.parse(str);
  return {
    state: {  // ❌ 结构不匹配
      nodes: new Map(data.nodes || []),
    },
    version: 0,
  };
},
```

**正确的方式**（使用 partialize 和 merge）：
```typescript
{
  name: 'canvas-nodes-storage',
  storage: createJSONStorage(() => localStorage),
  partialize: (state) => ({
    // 只持久化 nodes，序列化为数组
    nodes: Array.from(state.nodes.entries()),
  }),
  merge: (persistedState: any, currentState) => {
    // 反序列化：将数组转回 Map
    const nodes = Array.isArray(persistedState?.nodes)
      ? new Map(persistedState.nodes)
      : new Map();
    
    return {
      ...currentState,
      nodes,
    };
  },
}
```

### 3. 关键改进

1. **使用 `partialize`**：
   - 只序列化需要持久化的字段
   - 自动处理 JSON.stringify
   - 路径正确：`state.nodes` 而不是 `state.state.nodes`

2. **使用 `merge`**：
   - 正确的反序列化时机
   - 返回完整的 state 对象
   - 保留其他字段（函数等）

3. **类型安全**：
   - 检查 `persistedState?.nodes` 是否为数组
   - 默认值为空 Map
   - 避免运行时错误

---

## ✅ 验证步骤

### 步骤 1：清除旧数据

在浏览器控制台执行：
```javascript
localStorage.removeItem('canvas-nodes-storage');
location.reload();
```

### 步骤 2：测试持久化

1. 创建 3 个节点
2. 刷新页面
3. 验证节点完整恢复（位置、类型、数据）

### 步骤 3：测试图片持久化

1. 创建图片节点
2. 刷新页面
3. 验证图片显示正常（通过 IndexedDB 恢复）

---

## 📊 完整架构

### 三层存储架构

```
┌─────────────────────────────────────────────────────────────┐
│                     第 1 层：内存层                          │
│                                                              │
│  Blob URL (blob:http://...)                                 │
│  - UI 显示                                                   │
│  - 零拷贝                                                    │
│  - 页面刷新后失效 ❌                                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  第 2 层：持久化层（图片）                    │
│                                                              │
│  IndexedDB (Blob 对象)                                       │
│  - 页面刷新后恢复 ✅                                         │
│  - 不占用内存                                                │
│  - 懒加载                                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                第 3 层：元数据层（节点结构）                  │
│                                                              │
│  localStorage + persist 中间件                               │
│  - 节点位置、类型、连接关系 ✅                               │
│  - Map 数据结构持久化 ✅                                     │
│  - 自动序列化/反序列化 ✅                                    │
└─────────────────────────────────────────────────────────────┘
```

### 数据流

**保存流程**：
```
1. 用户创建节点
   ↓
2. 生成图片 → Blob URL（内存层）
   ↓
3. 保存到 IndexedDB（持久化层）
   ↓
4. persist 自动保存节点元数据到 localStorage（元数据层）
```

**恢复流程**：
```
1. 页面加载
   ↓
2. persist 自动恢复节点元数据（位置、类型）
   ↓
3. useEffect 遍历节点，从 IndexedDB 恢复图片
   ↓
4. 生成新的 Blob URL
   ↓
5. 更新节点数据
   ↓
6. UI 显示完整恢复 ✅
```

---

## 🎯 阶段 1 完成度

- [x] **任务 1.1**：添加 persist 中间件到 nodeStore ✅
- [x] **任务 1.2**：实现图片 Blob 保存到 IndexedDB ✅
- [x] **任务 1.3**：实现页面加载时恢复 Blob URL ✅
- [x] **任务 1.4**：修改节点创建逻辑（保存 Blob）✅
- [x] **任务 1.5**：修改节点删除逻辑（清理 Blob）✅

**阶段 1 进度**：100% ✅

---

## 🚀 下一步

### 阶段 2：资产库独立存储（1 小时）

- [ ] 创建资产时保存缩略图到 IndexedDB
- [ ] 页面加载时恢复资产库缩略图
- [ ] 删除资产时清理 IndexedDB

### 阶段 3：引用计数保护（30 分钟）

- [ ] 历史记录添加 `usedByAssetLibrary` 字段
- [ ] 创建资产时标记历史记录
- [ ] 清除历史记录时检查引用

---

## 📝 技术要点

### 为什么使用 partialize 而不是 serialize？

1. **partialize**：
   - Zustand 推荐的方式
   - 自动处理 JSON.stringify
   - 类型安全
   - 只序列化需要的字段

2. **serialize**（不推荐）：
   - 需要手动处理 JSON.stringify
   - 容易出错（路径、结构）
   - 需要配合 deserialize 使用

### 为什么使用 merge 而不是 deserialize？

1. **merge**：
   - 在正确的时机调用
   - 返回完整的 state 对象
   - 保留函数和其他字段
   - 类型安全

2. **deserialize**（不推荐）：
   - 需要返回特定结构
   - 容易出错（结构不匹配）
   - 不保留函数

---

## ✅ 验收标准

- [x] 页面不再崩溃 ✅
- [x] 可以创建节点 ✅
- [x] 节点元数据持久化（位置、类型）✅
- [x] 图片持久化（IndexedDB）✅
- [x] 刷新页面后完整恢复 ✅
- [x] 删除节点时清理 IndexedDB ✅

---

**文档版本**：v1.0  
**创建日期**：2026-02-10  
**状态**：✅ 已完成
