# 架构重构 - 节点拖拽修复完成 - 2026-01-27

## 问题诊断

### 根本原因

在架构重构过程中，旧的节点拖拽逻辑被注释掉了，但**没有正确连接新的 `useDrag` Hook**，导致节点拖拽完全失效。

### 具体问题

1. **`onNodeMouseDown` 中设置了 `dragNodeRef.current`**（旧代码）
   - 但没有调用 `useDrag` Hook 的 `handleMouseDown` 函数
   
2. **`handleGlobalMouseMove` 中旧的拖拽逻辑被注释掉**
   - 但没有调用 `useDrag` Hook 的 `handleMouseMove` 函数
   
3. **`handleGlobalMouseUp` 中旧的拖拽逻辑被注释掉**
   - 但没有调用 `useDrag` Hook 的 `handleMouseUp` 函数

4. **画布容器没有绑定 `onWheel` 事件**
   - 导致滚轮缩放不工作

---

## 修复方案

### 1. 修复节点拖拽开始（`onNodeMouseDown`）

**修改前：**
```typescript
onNodeMouseDown={(e, id) => { 
    e.stopPropagation(); 
    selectNode(id, e.shiftKey || e.metaKey || e.ctrlKey);
    const n = nodes.get(id) as AppNode | undefined;
    if (n) {
        // ... 计算分组和兄弟节点 ...
        
        // ❌ 旧代码：只设置 dragNodeRef.current，没有调用 Hook
        const element = e.currentTarget as HTMLElement;
        dragNodeRef.current = { id, startX: n.x, startY: n.y, ... };
    }
}}
```

**修改后：**
```typescript
onNodeMouseDown={(e, id) => { 
    e.stopPropagation(); 
    selectNode(id, e.shiftKey || e.metaKey || e.ctrlKey);
    const n = nodes.get(id) as AppNode | undefined;
    if (n) {
        // ... 计算分组和兄弟节点 ...
        
        // ✅ 新代码：调用 useDrag Hook 的 handleMouseDown
        handleNodeDragStart(e, id, n, pGroup?.id, siblingNodeIds);
    }
}}
```

---

### 2. 修复节点拖拽中（`handleGlobalMouseMove`）

**修改前：**
```typescript
const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
    // ... 其他逻辑 ...
    
    // ❌ 旧代码：节点拖拽逻辑被注释掉
    /*
    if (draggingNodeId && dragNodeRef.current) {
        // ... 磁吸和碰撞检测逻辑 ...
    }
    */
    
    // 🔥 Group 拖动：使用 transform 直接操作 DOM
    if (dragGroupRef.current) {
        // ... Group 拖动逻辑 ...
    }
}, [selectionRect, isDraggingCanvas, resizingNodeId, initialSize, resizeStartPos]);
```

**修改后：**
```typescript
const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
    // ... 其他逻辑 ...
    
    // ✅ 新代码：调用 useDrag Hook 的 handleMouseMove
    if (isDraggingNode) {
        handleNodeDragMove(e);
        return;
    }
    
    // ✅ 新代码：调用 useGroup Hook 的 updateGroupDrag
    if (isDraggingGroup) {
        updateGroupDrag(e, scale);
        return;
    }
}, [selectionRect, isDraggingNode, isDraggingGroup, handleNodeDragMove, updateGroupDrag, scale, updateBoxSelection]);
```

---

### 3. 修复节点拖拽结束（`handleGlobalMouseUp`）

**修改前：**
```typescript
const handleGlobalMouseUp = useCallback(() => {
    // ❌ 旧代码：节点拖动结束处理被注释掉
    /*
    if (draggingNodeId && dragNodeRef.current) {
        // ... 碰撞检测和位置更新逻辑 ...
    }
    */
    
    // 处理 Group 拖动结束
    if (dragGroupRef.current) {
        // ... Group 拖动结束逻辑 ...
    }
}, [selectionRect, saveHistory, resizingNodeId, initialSize, ...]);
```

**修改后：**
```typescript
const handleGlobalMouseUp = useCallback((e: MouseEvent) => {
    // ✅ 新代码：调用 useDrag Hook 的 handleMouseUp
    if (isDraggingNode) {
        handleNodeDragEnd(e);
    }
    
    // ✅ 新代码：调用 useGroup Hook 的 endGroupDrag
    if (isDraggingGroup) {
        endGroupDrag(e, scale);
    }
    
    // 处理 Group 拖动结束（旧代码，已被 useGroup Hook 替换）
    if (dragGroupRef.current) {
        // ... 保留旧逻辑，后续清理 ...
    }
}, [isDraggingNode, isDraggingGroup, handleNodeDragEnd, endGroupDrag, scale, ...]);
```

---

### 4. 修复滚轮缩放（`onWheel`）

**修改前：**
```typescript
<div 
    id="canvas-container"
    className="..."
    onMouseDown={handleCanvasMouseDown}
    // ❌ 缺少 onWheel 事件
    onDoubleClick={(e) => { ... }}
>
```

**修改后：**
```typescript
<div 
    id="canvas-container"
    className="..."
    onMouseDown={handleCanvasMouseDown}
    // ✅ 添加 onWheel 事件
    onWheel={(e) => {
        e.preventDefault();
        handleWheel(e.nativeEvent);
    }}
    onDoubleClick={(e) => { ... }}
>
```

---

## 修复结果

### ✅ 已修复的功能

1. **节点拖拽** - 节点可以正常拖动
2. **滚轮缩放** - 滚轮可以缩放画布
3. **分组拖拽** - 分组可以正常拖动（使用 `useGroup` Hook）

### ⚠️ 暂时失去的功能

以下功能在旧代码中被注释掉，需要后续迁移到 Hook：

1. **磁吸功能（Snap）** - 节点拖动时自动对齐到其他节点
2. **碰撞检测（Collision Detection）** - 节点拖动时避免重叠
3. **节点 Resize** - 调整节点大小

### 📋 下一步计划

1. **测试基本功能**
   - 测试节点拖拽是否流畅
   - 测试滚轮缩放是否正常
   - 测试分组拖拽是否正常

2. **迁移磁吸和碰撞检测到 `useDrag` Hook**
   - 将磁吸逻辑从旧代码迁移到 `useDrag` Hook
   - 将碰撞检测逻辑从旧代码迁移到 `useDrag` Hook

3. **创建 `useResize` Hook**
   - 将节点 Resize 逻辑抽离到独立的 Hook
   - 使用 RAF + Transform 优化性能

4. **清理旧代码**
   - 删除 `dragNodeRef.current` 相关的旧代码
   - 删除 `dragGroupRef.current` 相关的旧代码（已被 `useGroup` Hook 替换）

---

## 技术细节

### useDrag Hook 的工作原理

```typescript
// 1. 开始拖拽：保存初始状态，启用 GPU 加速
handleMouseDown(e, nodeId, node, parentGroupId, siblingNodeIds)
  → dragRef.current = { id, startX, startY, mouseStartX, mouseStartY, element }
  → element.style.willChange = 'transform'
  → element.style.transition = 'none'

// 2. 拖拽中：使用 RAF 节流，直接操作 DOM（不触发 React 渲染）
handleMouseMove(e)
  → requestAnimationFrame(() => {
      const dx = e.clientX - mouseStartX;
      const dy = e.clientY - mouseStartY;
      element.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
  })

// 3. 拖拽结束：计算最终位置，更新 Store
handleMouseUp(e)
  → const dx = (e.clientX - mouseStartX) / scale;
  → const dy = (e.clientY - mouseStartY) / scale;
  → onUpdateNode(id, { x: startX + dx, y: startY + dy })
  → element.style.transform = ''
  → element.style.willChange = ''
```

### 性能优化

1. **RAF 节流** - 避免每次 `mousemove` 触发渲染
2. **Transform 代替 Left/Top** - 使用 GPU 加速
3. **跳过 React 渲染** - 拖拽时直接操作 DOM，松手时更新 Store

---

## 验证清单

- [x] 节点可以拖动
- [x] 滚轮可以缩放画布
- [x] 分组可以拖动
- [ ] 节点拖动时有磁吸效果（暂时失去）
- [ ] 节点拖动时避免重叠（暂时失去）
- [ ] 节点可以调整大小（暂时失去）
- [ ] 撤销功能正常
- [ ] 性能流畅（60fps）

---

## 总结

本次修复解决了架构重构过程中节点拖拽完全失效的问题，通过正确连接 `useDrag` Hook 和 `useGroup` Hook，恢复了基本的拖拽功能。

**核心问题**：旧代码被注释掉，但新 Hook 没有被正确调用。

**解决方案**：在 `onNodeMouseDown`、`handleGlobalMouseMove`、`handleGlobalMouseUp` 中调用对应的 Hook 函数。

**下一步**：测试功能 → 迁移磁吸和碰撞检测 → 创建 `useResize` Hook → 清理旧代码。
