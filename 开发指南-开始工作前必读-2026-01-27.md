# 开发指南 - 开始工作前必读 - 2026-01-27

## 🎉 架构重构已完成！

恭喜！架构重构已经 100% 完成，你现在可以开始修 Bug 和添加新功能了。

---

## ✅ 当前状态

### 架构完成度：100%

| 项目 | 状态 | 说明 |
|------|------|------|
| 三层架构 | ✅ 完成 | UI → Hooks → Core |
| 数据管理 | ✅ 完成 | 7 个 Zustand Stores |
| 交互逻辑 | ✅ 完成 | 6 个 Hooks |
| 节点注册 | ✅ 完成 | NodeRegistry |
| 代码清理 | ✅ 完成 | 旧代码注释已删除 |
| 编译状态 | ✅ 成功 | 0 错误 |

### 核心目标达成：100%

| 目标 | 状态 | 说明 |
|------|------|------|
| 不再碰 App.tsx | ✅ 达成 | 有明确的扩展点 |
| 结构清晰 | ✅ 达成 | 三层架构清晰 |
| 易于扩展 | ✅ 达成 | 模块化设计 |
| 不会塌方 | ✅ 达成 | 模块独立 |

---

## 📚 必读文档

### 1. 架构文档（最重要）

**文件**：`ARCHITECTURE.md`

**内容**：
- 三层架构说明
- App.tsx 修改规则
- 开发检查清单
- 正面案例 + 反面案例

**为什么重要**：
- 这是项目的"宪法"
- 所有开发都要遵守
- 违反规则会导致架构崩溃

**必须记住**：
1. 不是控制代码行数，而是控制代码职责
2. App.tsx 是高优先级保护区，轻易不能动
3. 添加新功能有明确的唯一入口

---

### 2. 入职规则

**文件**：`.kiro/steering/project-onboarding.md`

**内容**：
- 重构需求文档（第一原则）
- 三层架构说明
- 核心规则
- 禁止事项

**为什么重要**：
- 新接手项目必读
- 理解重构的目标和原则
- 避免犯常见错误

---

### 3. 重构需求文档

**文件**：`.kiro/specs/canvas-architecture-refactor/requirements.md`

**内容**：
- 核心目标
- 三层架构设计
- 实施计划
- 验收标准

**为什么重要**：
- 理解重构的背景和目标
- 理解架构设计的原因
- 理解验收标准

---

## 🎯 开始工作前的检查清单

### ✅ 我已经理解了：

- [ ] 三层架构（UI → Hooks → Core）
- [ ] App.tsx 是高优先级保护区
- [ ] 添加新功能的唯一入口
- [ ] 禁止事项（不能在 UI 里写业务逻辑）
- [ ] 单向数据流（UI → Hooks → Store）

### ✅ 我知道如何：

- [ ] 添加新节点类型（修改 NodeRegistry）
- [ ] 添加新交互逻辑（创建新 Hook）
- [ ] 添加新数据管理（创建新 Store）
- [ ] 添加新计算逻辑（修改 geometry.ts）
- [ ] 修改 App.tsx（遵守规则）

### ✅ 我知道在哪里找：

- [ ] 架构文档（ARCHITECTURE.md）
- [ ] 入职规则（project-onboarding.md）
- [ ] 重构需求（requirements.md）
- [ ] 快速参考（QUICK_REFERENCE.md）

---

## 🚀 常见开发场景

### 场景 1：添加新节点类型

**需求**：添加一个"音频编辑"节点

**步骤**：
1. 在 `core/registry/NodeRegistry.ts` 中添加节点定义
2. 创建节点组件 `components/AudioEditorNode.tsx`
3. 完成

**不需要**：
- ❌ 修改 App.tsx
- ❌ 修改其他文件

**示例**：
```typescript
// core/registry/NodeRegistry.ts
export const nodeDefinitions = new Map([
  // ... 其他节点
  [NodeType.AUDIO_EDITOR, {
    type: NodeType.AUDIO_EDITOR,
    name: '音频编辑',
    icon: 'Music',
    defaultSize: { width: 420, height: 300 },
    defaultData: { audioUri: '', duration: 0 },
    component: AudioEditorNode,
  }],
]);
```

---

### 场景 2：添加新交互逻辑

**需求**：添加"双击节点放大"功能

**步骤**：
1. 创建 `hooks/useNodeZoom.ts`
2. 在 Hook 中实现逻辑
3. 在 App.tsx 中调用 Hook（一行代码）

**不需要**：
- ❌ 在 App.tsx 中写逻辑
- ❌ 在 Node.tsx 中写逻辑

**示例**：
```typescript
// hooks/useNodeZoom.ts
export const useNodeZoom = () => {
  const updateNode = useNodeStore(state => state.updateNode);
  
  const handleDoubleClick = useCallback((nodeId: string) => {
    updateNode(nodeId, { scale: 2 });
  }, [updateNode]);
  
  return { handleDoubleClick };
};

// App.tsx
function App() {
  const { handleDoubleClick } = useNodeZoom();
  // 传递给 Node 组件
}
```

---

### 场景 3：修复 Bug

**需求**：修复节点拖动时的闪烁问题

**步骤**：
1. 定位 Bug 所在的层（UI / Hooks / Core）
2. 修改对应的文件
3. 测试验证

**可能的位置**：
- 拖动逻辑：`hooks/useDrag.ts`
- 渲染逻辑：`App.tsx` 或 `Node.tsx`
- 数据管理：`core/stores/nodeStore.ts`

**不要**：
- ❌ 在多个文件中乱改
- ❌ 跨层修改
- ❌ 不测试就提交

---

### 场景 4：性能优化

**需求**：优化 1000 个节点的渲染性能

**步骤**：
1. 定位性能瓶颈（使用 React DevTools）
2. 根据瓶颈位置优化：
   - 渲染慢：添加 useMemo, useCallback
   - 计算慢：优化 `core/utils/geometry.ts`
   - 状态更新慢：优化 Store

**不要**：
- ❌ 盲目优化
- ❌ 过早优化
- ❌ 不测试就优化

---

## ⚠️ 常见错误（避免）

### 错误 1：在 App.tsx 中写业务逻辑

```typescript
// ❌ 错误
function App() {
  const handleAddNode = (type: string) => {
    const newNode = {
      id: Date.now().toString(),
      type,
      x: Math.random() * 800,
      y: Math.random() * 600,
      // ... 50 行业务逻辑
    };
    setNodes([...nodes, newNode]);
  };
}

// ✅ 正确
function App() {
  const { addNode } = useNodeStore();
}
```

---

### 错误 2：在 UI 组件中写计算

```typescript
// ❌ 错误
function Node({ node }) {
  const distance = Math.sqrt(
    Math.pow(node.x - targetX, 2) + Math.pow(node.y - targetY, 2)
  );
}

// ✅ 正确
function Node({ node }) {
  const distance = calculateDistance(node, target);
}
```

---

### 错误 3：跨层混用

```typescript
// ❌ 错误：UI 组件直接操作 Store
function Node({ node }) {
  const { setNodes } = useNodeStore();
  
  const handleDrag = (e) => {
    setNodes((nodes) => /* ... */);
  };
}

// ✅ 正确：通过 Hook 操作 Store
function Node({ node }) {
  const { handleDrag } = useDrag(node.id);
}
```

---

### 错误 4：Map/Array 混用

```typescript
// ❌ 错误
const nodes = new Map();
setNodes([...nodes, newNode]); // 崩溃

// ✅ 正确
const nodes = new Map();
setNodes(new Map([...nodes, [newNode.id, newNode]]));
```

---

## 🔧 开发工具

### 编译

```bash
npm run build
```

### 开发服务器

```bash
npm run dev
```

### 类型检查

```bash
npx tsc --noEmit
```

### 查看诊断信息

使用 Kiro 的 `getDiagnostics` 工具查看编译错误。

---

## 📝 提交代码前的检查清单

### ✅ 代码质量

- [ ] 代码放在正确的层了吗？
- [ ] 有没有跨层混用？
- [ ] 有没有在 UI 组件里写业务逻辑？
- [ ] 有没有在 UI 组件里写计算？
- [ ] 有没有违反单向数据流？

### ✅ 编译和测试

- [ ] 编译通过了吗？（npm run build）
- [ ] 类型检查通过了吗？（npx tsc --noEmit）
- [ ] 功能测试通过了吗？
- [ ] 有没有引入新的 Bug？

### ✅ 文档

- [ ] 如果修改了架构，更新了 ARCHITECTURE.md 吗？
- [ ] 如果添加了新功能，更新了 README.md 吗？
- [ ] 如果修复了 Bug，记录了修复过程吗？

---

## 🎯 核心原则（永远记住）

### 1. 不是控制代码行数，而是控制代码职责

**错误理解**：
- ❌ "App.tsx 必须 < 500 行"
- ❌ "每个函数必须 < 20 行"

**正确理解**：
- ✅ "App.tsx 只做组合和配置，不写业务逻辑"
- ✅ "每个函数只做一件事"

---

### 2. App.tsx 是高优先级保护区

**修改前必须问**：
1. 为什么必须改 App.tsx？
2. 不改会有什么问题？
3. 有没有其他方案？
4. 用户同意了吗？

---

### 3. 添加新功能有明确的唯一入口

| 需求 | 唯一入口 |
|------|---------|
| 添加新节点 | `core/registry/NodeRegistry.ts` |
| 添加新交互 | `hooks/` 对应的 Hook |
| 添加新数据 | `core/stores/` 对应的 Store |
| 添加新计算 | `core/utils/geometry.ts` |

---

### 4. 单向数据流

```
用户操作 → UI → Hooks → Store → UI
```

**禁止**：
- ❌ UI 直接操作 Store
- ❌ Store 直接调用 UI
- ❌ Hook 直接操作 DOM

---

## 🚀 开始工作吧！

你现在已经准备好了：

1. ✅ 架构重构 100% 完成
2. ✅ 文档完善
3. ✅ 编译成功
4. ✅ 核心目标达成

**接下来你可以**：
- 修复 Bug
- 添加新功能
- 性能优化
- 代码重构

**记住**：
- 遵守架构规则
- 不要破坏三层架构
- 不要随意修改 App.tsx
- 有问题查文档

**祝你开发顺利！🎉**

---

## 📚 快速参考

### 文档位置

| 文档 | 路径 |
|------|------|
| 架构文档 | `ARCHITECTURE.md` |
| 入职规则 | `.kiro/steering/project-onboarding.md` |
| 重构需求 | `.kiro/specs/canvas-architecture-refactor/requirements.md` |
| 快速参考 | `.kiro/QUICK_REFERENCE.md` |
| 开发指南 | `开发指南-开始工作前必读-2026-01-27.md`（本文档） |

### 目录结构

```
src/
├── App.tsx                        # 主入口（只做组合）
├── core/                          # 底层
│   ├── stores/                    # 数据管理（7 个 Store）
│   ├── utils/                     # 纯计算
│   └── registry/                  # 节点注册
├── hooks/                         # 交互逻辑（6 个 Hook）
└── components/                    # UI 组件
```

### 常用命令

```bash
# 编译
npm run build

# 开发服务器
npm run dev

# 类型检查
npx tsc --noEmit
```

---

**文档版本**：v1.0  
**创建日期**：2026-01-27  
**状态**：✅ 生效中
