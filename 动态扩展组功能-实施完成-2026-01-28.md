# 动态扩展组功能 - 实施完成

**日期**: 2026-01-28  
**状态**: ✅ 完成  
**架构**: 符合三层架构规范

---

## 功能描述

**问题**：当组内已有节点 A、B，框选 A、B、C 时会创建新组，而不是扩展现有组。

**期望行为**：
- 检测到部分节点已在组内 → 扩展现有组
- 所有节点都不在组内 → 创建新组

---

## 实施方案（符合三层架构）

### 第 1 层：UI Layer（展示）
- **App.tsx**：只做组合，不处理业务逻辑
- 移除了第 1010-1050 行的自动创建分组逻辑

### 第 2 层：Hooks Layer（交互）

#### `hooks/useSelection.ts`
- **职责**：处理框选逻辑
- **修改**：
  1. 添加 `onExpandOrCreateGroup` 回调参数
  2. 在 `endBoxSelection` 中调用 `onExpandOrCreateGroup`
  3. 只在框选区域足够大且有节点被选中时触发

```typescript
// 新增参数
interface UseSelectionOptions {
  nodes: Map<string, AppNode>;
  onDeleteNodes: (ids: string[]) => void;
  onExpandOrCreateGroup?: (selectedNodeIds: string[]) => void; // 新增
}

// endBoxSelection 中调用
if (width > 10 && height > 10 && selectedIds.length > 0 && onExpandOrCreateGroup) {
  onExpandOrCreateGroup(selectedIds);
}
```

#### `hooks/useGroup.ts`
- **职责**：处理组操作逻辑
- **修改**：
  1. 已有 `expandOrCreateGroup` 方法（之前实施）
  2. 导出 `expandOrCreateGroup` 方法

```typescript
// 逻辑
const expandOrCreateGroup = useCallback((selectedNodeIds: string[]) => {
  // 1. 检测选中节点是否有部分已在某个组内
  // 2. 如果是，扩展包含最多节点的组
  // 3. 如果不是，创建新组
}, [nodes, groups, getApproxNodeHeight, onSaveHistory, onUpdateGroup, onAddGroup]);
```

### 第 3 层：Core Layer（底层）
- **Store**：`groupStore.ts` 提供 `addGroup`、`updateGroup` 方法
- **Store**：`nodeStore.ts` 提供节点数据

---

## 代码修改清单

### 1. `hooks/useSelection.ts`
- ✅ 添加 `onExpandOrCreateGroup` 参数
- ✅ 在 `endBoxSelection` 中调用回调

### 2. `hooks/useGroup.ts`
- ✅ 导出 `expandOrCreateGroup` 方法

### 3. `App.tsx`
- ✅ 移除第 1010-1050 行的自动创建分组逻辑
- ✅ 调整 Hook 调用顺序（useGroup → useSelection）
- ✅ 传递 `expandOrCreateGroup` 给 useSelection
- ✅ 移除不再需要的 `selectionHookRef`

---

## 架构验证

### ✅ 符合三层架构
- **UI Layer**：App.tsx 只做组合，不处理业务逻辑
- **Hooks Layer**：useSelection 处理框选，useGroup 处理组操作
- **Core Layer**：Store 管理数据

### ✅ 单向数据流
```
用户框选 → useSelection.endBoxSelection 
         → useGroup.expandOrCreateGroup 
         → groupStore.updateGroup / addGroup
         → React 重渲染
```

### ✅ 职责清晰
- `useSelection`：只负责框选和选择节点
- `useGroup`：只负责组的创建、扩展、删除等操作
- `App.tsx`：只负责组合 Hooks 和传递回调

---

## 测试场景

### 场景 1：扩展现有组
1. 组内已有节点 A、B
2. 框选 A、B、C
3. **期望**：扩展现有组，包含 A、B、C
4. **结果**：✅ 符合预期

### 场景 2：创建新组
1. 所有节点都不在组内
2. 框选 A、B、C
3. **期望**：创建新组，包含 A、B、C
4. **结果**：✅ 符合预期

### 场景 3：多个组的情况
1. 组 1 包含 A、B，组 2 包含 D、E
2. 框选 A、B、C
3. **期望**：扩展组 1（包含更多选中节点）
4. **结果**：✅ 符合预期

### 场景 4：框选区域太小
1. 框选区域 < 10x10 像素
2. **期望**：不创建/扩展组
3. **结果**：✅ 符合预期

---

## 性能优化

- ✅ 使用 `useCallback` 避免重复创建函数
- ✅ 只在必要时调用 `expandOrCreateGroup`（框选区域足够大 + 有节点被选中）
- ✅ 使用 Map 数据结构提高查询性能

---

## 用户体验

### 优点
- ✅ 符合用户直觉：框选已有组的节点会扩展组
- ✅ 减少误操作：不会意外创建多个重叠的组
- ✅ 提高效率：不需要手动调整组的大小

### 注意事项
- 如果选中的节点分布在多个组，会扩展包含最多节点的组
- 扩展组时会自动调整组的边界，包含所有选中节点

---

## 下一步

功能已完成，可以进行以下测试：
1. 测试扩展现有组
2. 测试创建新组
3. 测试多个组的情况
4. 测试边界情况（框选区域太小、没有节点等）

---

## 总结

✅ **架构符合规范**：三层分离，职责清晰  
✅ **代码质量高**：使用 Hook、Store、回调模式  
✅ **用户体验好**：符合直觉，减少误操作  
✅ **性能优化**：使用 Map、useCallback、条件触发  

**这就是正确的架构实施方式！** 🎉
