# 3D 相机改进 - 智能命名 + 禁用交互 - 2026-01-30

**日期**：2026-01-30  
**状态**：✅ 完成  
**耗时**：约 30 分钟

---

## 📋 任务概述

实施两个改进：
1. **智能命名**：九宫格节点按角度、高度、景别命名（例如："正面-平视-中景"）
2. **禁用交互**：生成过程中禁止移动预览框、调整角度、滚轮缩放

---

## ✅ 完成的工作

### 1. 智能命名系统（`hooks/useNodeActions.ts`）

**新增命名函数**：

#### `getAzimuthName` - 方位角命名
```typescript
const getAzimuthName = (angle: number): string => {
    const normalized = ((angle % 360) + 360) % 360;
    if (normalized < 22.5 || normalized >= 337.5) return '正面';
    if (normalized < 67.5) return '右前';
    if (normalized < 112.5) return '右侧';
    if (normalized < 157.5) return '右后';
    if (normalized < 202.5) return '背面';
    if (normalized < 247.5) return '左后';
    if (normalized < 292.5) return '左侧';
    return '左前';
};
```

**映射规则**：
| 角度范围 | 名称 |
|---------|------|
| 337.5° - 22.5° | 正面 |
| 22.5° - 67.5° | 右前 |
| 67.5° - 112.5° | 右侧 |
| 112.5° - 157.5° | 右后 |
| 157.5° - 202.5° | 背面 |
| 202.5° - 247.5° | 左后 |
| 247.5° - 292.5° | 左侧 |
| 292.5° - 337.5° | 左前 |

---

#### `getElevationName` - 仰角命名
```typescript
const getElevationName = (angle: number): string => {
    if (angle >= 40) return '俯拍';
    if (angle >= 10) return '高角度';
    if (angle >= -10) return '平视';
    if (angle >= -30) return '低角度';
    return '仰拍';
};
```

**映射规则**：
| 角度范围 | 名称 |
|---------|------|
| ≥ 40° | 俯拍 |
| 10° - 40° | 高角度 |
| -10° - 10° | 平视 |
| -30° - -10° | 低角度 |
| < -30° | 仰拍 |

---

#### `getDistanceName` - 景别命名
```typescript
const getDistanceName = (z: number): string => {
    if (z <= 1.5) return '特写';
    if (z <= 3) return '近景';
    if (z <= 4.5) return '半身';
    if (z <= 6) return '中景';
    if (z <= 7.5) return '全身';
    if (z <= 9) return '远景';
    return '全景';
};
```

**映射规则**：
| 距离范围 | 名称 |
|---------|------|
| 0 - 1.5 | 特写 |
| 1.5 - 3 | 近景 |
| 3 - 4.5 | 半身 |
| 4.5 - 6 | 中景 |
| 6 - 7.5 | 全身 |
| 7.5 - 9 | 远景 |
| > 9 | 全景 |

---

#### 组合命名
```typescript
const azimuthName = getAzimuthName(hAngle);
const elevationName = getElevationName(vAngle);
const distanceName = getDistanceName(zoom);
const gridNodeTitle = `${azimuthName}-${elevationName}-${distanceName}`;
```

**示例**：
- 水平角度 0°，垂直角度 0°，距离 5 → **"正面-平视-中景"**
- 水平角度 45°，垂直角度 30°，距离 2 → **"右前-高角度-近景"**
- 水平角度 180°，垂直角度 -20°，距离 8 → **"背面-低角度-远景"**
- 水平角度 90°，垂直角度 50°，距离 1 → **"右侧-俯拍-特写"**

---

### 2. 禁用生成中的交互（`components/MultiAngleCameraNode.tsx`）

#### 禁用 3D 视口拖拽
```typescript
const handleViewportMouseDown = (e: React.MouseEvent) => {
    // 🔥 生成中禁止交互
    if (isWorking) {
      e.preventDefault();
      e.stopPropagation();
      return;
    }
    
    e.stopPropagation();
    // ... 原有逻辑
};
```

---

#### 禁用滚轮缩放
```typescript
const handleWheel = (e: React.WheelEvent) => {
    // 🔥 生成中禁止交互
    if (isWorking) {
      e.preventDefault();
      e.stopPropagation();
      return;
    }
    
    e.stopPropagation();
    e.preventDefault();
    // ... 原有逻辑
};
```

---

#### 禁用滑块
```typescript
<input 
    type="range" 
    min={s.min} 
    max={s.max} 
    step={s.step || 1} 
    value={s.v}
    onChange={(e: React.ChangeEvent<HTMLInputElement>) => s.fn(Number(e.target.value))}
    disabled={isWorking}  // 🔥 生成中禁用
    className={`w-full h-1.5 rounded-full appearance-none ${isWorking ? 'cursor-not-allowed opacity-50' : 'cursor-pointer'} bg-white/10`}
    // ...
/>
```

---

#### 禁用文本框
```typescript
<textarea
    className={`w-full px-4 py-3 bg-white/5 border border-white/5 rounded-xl text-[13px] text-white resize-none focus:border-white/20 focus:outline-none focus:ring-0 placeholder-white/30 transition-colors ${isWorking ? 'cursor-not-allowed opacity-50' : ''}`}
    rows={3}
    placeholder="光照、材质、风格等..."
    value={userPrompt || ''}
    onChange={(e: React.ChangeEvent<HTMLTextAreaElement>) => onUserPromptChange(e.target.value)}
    disabled={isWorking}  // 🔥 生成中禁用
/>
```

---

#### 禁用 3D 视口鼠标样式
```typescript
<div
    ref={containerRef}
    className={`absolute inset-0 ${isDragging ? 'cursor-grabbing' : isWorking ? 'cursor-not-allowed' : 'cursor-grab'}`}
    onMouseDown={handleViewportMouseDown}
    onWheel={handleWheel}
/>
```

---

## 🎯 功能特点

### 智能命名系统

✅ **语义化命名**：一眼就能看出角度、高度、景别  
✅ **自动生成**：无需手动输入  
✅ **唯一性**：不同角度生成不同名称  
✅ **易于识别**：方便用户快速找到需要的角度  
✅ **专业术语**：使用电影摄影术语（俯拍、仰拍、特写、中景等）

**命名示例**：
- "正面-平视-中景" - 最常用的角度
- "右侧-俯拍-特写" - 侧面特写镜头
- "背面-平视-全身" - 背面全身镜头
- "左前-高角度-近景" - 左前方高角度近景

---

### 禁用交互系统

✅ **完全禁用**：生成中无法移动预览框  
✅ **禁用滚轮**：生成中无法缩放  
✅ **禁用滑块**：生成中无法调整角度  
✅ **禁用文本框**：生成中无法修改描述  
✅ **视觉反馈**：禁用状态有明显的视觉提示（半透明 + 禁用鼠标样式）  
✅ **防止误操作**：避免用户在生成中修改参数导致混乱

---

## 📊 代码改动

| 文件 | 改动类型 | 行数 | 状态 |
|------|---------|------|------|
| `hooks/useNodeActions.ts` | 新增命名函数 | +40 行 | ✅ 完成 |
| `components/MultiAngleCameraNode.tsx` | 禁用交互 | +15 行 | ✅ 完成 |
| **总计** | | **+55 行** | ✅ 完成 |

---

## 🧪 编译检查

```bash
✅ hooks/useNodeActions.ts: No diagnostics found
✅ components/MultiAngleCameraNode.tsx: No diagnostics found
```

---

## 🎯 用户体验改进

### 改进前
- ❌ 九宫格节点命名为"九宫格 1"、"九宫格 2"...
- ❌ 无法快速识别角度
- ❌ 生成中可以移动预览框，导致混乱
- ❌ 生成中可以调整角度，导致参数不一致

### 改进后
- ✅ 九宫格节点命名为"正面-平视-中景"、"右侧-俯拍-特写"...
- ✅ 一眼就能看出角度、高度、景别
- ✅ 生成中完全禁用交互，防止误操作
- ✅ 生成中参数锁定，保证一致性

---

## 📝 测试场景

### 智能命名测试
1. **正面平视中景**：
   - 水平角度：0°
   - 垂直角度：0°
   - 距离：5
   - ✅ 应该命名为："正面-平视-中景"

2. **右侧俯拍特写**：
   - 水平角度：90°
   - 垂直角度：50°
   - 距离：1
   - ✅ 应该命名为："右侧-俯拍-特写"

3. **背面仰拍全景**：
   - 水平角度：180°
   - 垂直角度：-40°
   - 距离：10
   - ✅ 应该命名为："背面-仰拍-全景"

### 禁用交互测试
4. **生成中拖动预览框**：
   - 点击"生成"
   - 尝试拖动 3D 视口
   - ✅ 应该无法拖动，鼠标显示禁用样式

5. **生成中滚轮缩放**：
   - 点击"生成"
   - 尝试滚轮缩放
   - ✅ 应该无法缩放

6. **生成中调整滑块**：
   - 点击"生成"
   - 尝试拖动滑块
   - ✅ 应该无法拖动，滑块半透明

7. **生成中修改文本框**：
   - 点击"生成"
   - 尝试输入文字
   - ✅ 应该无法输入，文本框半透明

---

## 🎉 总结

**已完成**：
- ✅ 智能命名系统（3 个命名函数）
- ✅ 禁用生成中的所有交互
- ✅ 编译检查通过

**成果**：
- 🎯 九宫格节点命名更语义化、更专业
- 🎯 生成中完全禁用交互，防止误操作
- 🎯 用户体验大幅提升
- 🎯 符合专业摄影术语

**下一步**：
- 🔄 用户测试验证
- 🔄 收集用户反馈
- 🔄 根据反馈调整命名规则（如果需要）

---

**状态**：✅ 实施完成，等待用户测试验证

