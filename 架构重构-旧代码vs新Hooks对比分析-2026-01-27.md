# 架构重构 - 旧代码 vs 新 Hooks 对比分析 - 2026-01-27

**日期**: 2026-01-27  
**目的**: 逐步验证 Hooks 是否完全替换了旧逻辑  
**结论**: 见文档末尾

---

## 📊 功能对比总览

| 功能 | 旧代码位置 | 新 Hook | 是否完全替换 | 备注 |
|------|-----------|---------|-------------|------|
| 节点拖拽 | App.tsx 第 1019-1072 行 | useDrag | ⚠️ 部分替换 | 缺少磁吸和碰撞检测 |
| 拖拽状态管理 | App.tsx 第 426-434 行 | useDrag | ✅ 完全替换 | `isDragging` 已实现 |
| 拖拽开始 | App.tsx 第 2477 行 | useDrag | ✅ 完全替换 | `handleMouseDown` 已实现 |
| 拖拽结束 | App.tsx 第 1078-1160 行 | useDrag | ⚠️ 部分替换 | 缺少碰撞检测 |
| Group 拖拽 | App.tsx 第 2285-2320 行 | useGroup | ✅ 完全替换 | `isDraggingGroup` 已实现 |
| 节点 Resize | App.tsx 第 1062-1072 行 | ❌ 无 Hook | ❌ 未替换 | 需要保留 |

---

## 🔍 详细功能分析

### 1. 节点拖拽 - 基础功能

#### 旧代码实现（App.tsx 第 1019-1072 行）

```typescript
if (draggingNodeId && dragNodeRef.current) {
   const { startX, startY, mouseStartX, mouseStartY, nodeWidth, nodeHeight, element } = dragNodeRef.current;
   const currentScale = scale;
   let dx = (clientX - mouseStartX) / currentScale;
   let dy = (clientY - mouseStartY) / currentScale;
   let proposedX = startX + dx;
   let proposedY = startY + dy;
   
   // 使用 transform 直接操作 DOM
   if (element) {
       element.style.transform = `translate(${proposedX - startX}px, ${proposedY - startY}px)`;
   }
}
```

**功能**:
- ✅ 计算拖拽偏移量
- ✅ 考虑缩放比例
- ✅ 使用 transform 优化性能

#### 新 Hook 实现（useDrag.ts）

```typescript
const handleMouseMove = useCallback((e: MouseEvent) => {
  if (!dragRef.current) return;

  rafRef.current = requestAnimationFrame(() => {
    if (!dragRef.current) return;

    const { element, mouseStartX, mouseStartY } = dragRef.current;
    const dx = e.clientX - mouseStartX;
    const dy = e.clientY - mouseStartY;

    if (element) {
      element.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
    }
  });
}, []);
```

**功能**:
- ✅ 计算拖拽偏移量
- ✅ 使用 transform 优化性能
- ✅ 使用 RAF 节流
- ✅ 使用 translate3d 启用 GPU 加速

**对比结果**: ✅ **新 Hook 功能更强**
- 新增了 RAF 节流
- 新增了 GPU 加速（translate3d）
- 性能更好

---

### 2. 磁吸功能（Snap Logic）

#### 旧代码实现（App.tsx 第 1027-1051 行）

```typescript
// Snap Logic
const SNAP = SNAP_THRESHOLD / currentScale; 
const myL = proposedX; const myC = proposedX + nodeWidth / 2; const myR = proposedX + nodeWidth;
const myT = proposedY; const myM = proposedY + nodeHeight / 2; const myB = proposedY + nodeHeight;
let snappedX = false; let snappedY = false;

Array.from(nodesRef.current.values()).forEach(other => {
    if (other.id === draggingNodeId) return;
    const otherBounds = getNodeBounds(other);
    if (!snappedX) {
        if (Math.abs(myL - otherBounds.x) < SNAP) { proposedX = otherBounds.x; snappedX = true; }
        else if (Math.abs(myL - otherBounds.r) < SNAP) { proposedX = otherBounds.r; snappedX = true; }
        // ... 更多磁吸逻辑
    }
    if (!snappedY) {
        if (Math.abs(myT - otherBounds.y) < SNAP) { proposedY = otherBounds.y; snappedY = true; }
        // ... 更多磁吸逻辑
    }
});
```

**功能**:
- ✅ 左边缘对齐
- ✅ 右边缘对齐
- ✅ 中心对齐
- ✅ 上边缘对齐
- ✅ 下边缘对齐
- ✅ 垂直中心对齐

#### 新 Hook 实现

❌ **未实现**

**对比结果**: ❌ **新 Hook 缺少磁吸功能**

---

### 3. 碰撞检测（Collision Detection）

#### 旧代码实现（App.tsx 第 1093-1150 行）

```typescript
// 碰撞检测
let collisionX = finalX;
let collisionY = finalY;
const nodeWidth = node.width || 420;
const nodeHeight = node.height || getApproxNodeHeight(node);
const myBounds = { x: collisionX, y: collisionY, width: nodeWidth, height: nodeHeight, r: collisionX + nodeWidth, b: collisionY + nodeHeight };
const otherNodes = Array.from(nodesRef.current.values()).filter(n => n.id !== draggingNodeId);

for (const other of otherNodes) {
    const otherBounds = getNodeBounds(other);
    
    // 检测碰撞
    const overlapsX = myBounds.x < otherBounds.r && myBounds.r > otherBounds.x;
    const overlapsY = myBounds.y < otherBounds.b && myBounds.b > otherBounds.y;
    
    if (overlapsX && overlapsY) {
        // 计算推开距离
        // ... 复杂的碰撞处理逻辑
    }
}
```

**功能**:
- ✅ 检测节点重叠
- ✅ 计算推开距离
- ✅ 避免节点重叠

#### 新 Hook 实现

❌ **未实现**

**对比结果**: ❌ **新 Hook 缺少碰撞检测功能**

---

### 4. 拖拽结束处理

#### 旧代码实现（App.tsx 第 1078-1160 行）

```typescript
if (draggingNodeId && dragNodeRef.current) {
    const { element, startX, startY } = dragNodeRef.current;
    const node = nodesRef.current.get(draggingNodeId);
    
    if (element && node) {
        // 从 transform 中读取最终位置
        const transform = element.style.transform;
        const match = transform.match(/translate\(([^,]+)px,\s*([^)]+)px\)/);
        
        if (match) {
            const dx = parseFloat(match[1]);
            const dy = parseFloat(match[2]);
            const finalX = startX + dx;
            const finalY = startY + dy;
            
            // 碰撞检测（见上）
            // ...
            
            // 更新 state
            setNodes(prev => {
                const newMap = new Map(prev);
                const node = newMap.get(draggingNodeId);
                if (node) {
                    newMap.set(draggingNodeId, { ...node, x: collisionX, y: collisionY });
                }
                return newMap;
            });
            
            // 清理 DOM
            element.style.transform = '';
        }
    }
}
```

**功能**:
- ✅ 读取最终位置
- ✅ 碰撞检测
- ✅ 更新 state
- ✅ 清理 DOM

#### 新 Hook 实现（useDrag.ts）

```typescript
const handleMouseUp = useCallback((e: MouseEvent) => {
  if (!dragRef.current) return;

  const { id, startX, startY, mouseStartX, mouseStartY, element } = dragRef.current;

  // 计算最终位置
  const dx = (e.clientX - mouseStartX) / scale;
  const dy = (e.clientY - mouseStartY) / scale;
  const finalX = startX + dx;
  const finalY = startY + dy;

  // 更新 Store
  onUpdateNode(id, { x: finalX, y: finalY });

  // 清理 DOM
  if (element) {
    element.style.transform = '';
    element.style.willChange = '';
    element.style.transition = '';
  }

  // 清理上下文
  dragRef.current = null;
}, [scale, onUpdateNode]);
```

**功能**:
- ✅ 计算最终位置
- ❌ 无碰撞检测
- ✅ 更新 state
- ✅ 清理 DOM

**对比结果**: ⚠️ **新 Hook 缺少碰撞检测**

---

### 5. Group 拖拽

#### 旧代码实现（App.tsx 第 2285-2320 行）

```typescript
// 拖动时禁用 transition
transition: (draggingGroup?.id === g.id || draggingNodeParentGroupId === g.id) ? 'none' : 'all 0.3s cubic-bezier(0.32, 0.72, 0, 1)'

// 拖拽开始
setActiveGroupNodeIds(childNodes.map(c => c.id)); 
setDraggingGroup({ id: g.id });
```

**功能**:
- ✅ 拖动时禁用 transition
- ✅ 标记拖动中的 Group
- ✅ 标记 Group 内的节点

#### 新 Hook 实现（useGroup.ts）

```typescript
const {
  isDraggingGroup,
  startGroupDrag,
  updateGroupDrag,
  endGroupDrag,
} = useGroup({ ... });
```

**功能**:
- ✅ `isDraggingGroup` 状态
- ✅ 拖拽开始/更新/结束

**对比结果**: ✅ **新 Hook 完全替换**

---

### 6. 节点 Resize

#### 旧代码实现（App.tsx 第 1062-1072 行）

```typescript
if (resizingNodeId && initialSize && resizeStartPos && resizeContextRef.current) {
    const currentScale = scale;
    const dx = (clientX - resizeStartPos.x) / currentScale; 
    const dy = (clientY - resizeStartPos.y) / currentScale;
    
    const { element } = resizeContextRef.current;
    if (element) {
        const newWidth = Math.max(360, initialSize.width + dx);
        const newHeight = Math.max(240, initialSize.height + dy);
        element.style.width = `${newWidth}px`;
        element.style.height = `${newHeight}px`;
    }
}
```

**功能**:
- ✅ 节点大小调整
- ✅ 最小尺寸限制
- ✅ 考虑缩放比例

#### 新 Hook 实现

❌ **无对应 Hook**

**对比结果**: ❌ **需要保留旧代码**

---

## 📋 功能缺失清单

### 🔴 高优先级（影响核心功能）

1. **磁吸功能（Snap Logic）**
   - 位置：App.tsx 第 1027-1051 行
   - 影响：节点对齐体验
   - 建议：保留旧代码或迁移到 useDrag Hook

2. **碰撞检测（Collision Detection）**
   - 位置：App.tsx 第 1093-1150 行
   - 影响：节点不会重叠
   - 建议：保留旧代码或迁移到 useDrag Hook

3. **节点 Resize**
   - 位置：App.tsx 第 1062-1072 行
   - 影响：无法调整节点大小
   - 建议：保留旧代码或创建 useResize Hook

### 🟡 中优先级（影响用户体验）

4. **拖拽状态清理**
   - 位置：App.tsx 第 1313-1322 行
   - 影响：状态清理逻辑
   - 建议：检查是否已被 Hooks 处理

---

## 🎯 验证结论

### ✅ 已完全替换的功能

1. **节点拖拽基础功能** - useDrag Hook
2. **拖拽状态管理** - useDrag Hook
3. **Group 拖拽** - useGroup Hook
4. **拖拽性能优化** - useDrag Hook（RAF + GPU 加速）

### ⚠️ 部分替换的功能

1. **拖拽结束处理** - useDrag Hook（缺少碰撞检测）

### ❌ 未替换的功能

1. **磁吸功能** - 需要保留或迁移
2. **碰撞检测** - 需要保留或迁移
3. **节点 Resize** - 需要保留或创建新 Hook

---

## 💡 建议方案

### 方案 A：保留旧代码（推荐）⭐⭐⭐

**优点**:
- 功能完整，不影响用户体验
- 风险最低
- 可以逐步迁移

**缺点**:
- 代码冗余
- 需要维护两套逻辑

**实施步骤**:
1. 注释掉旧代码中使用 `draggingNodeId` 等变量的部分
2. 保留磁吸、碰撞检测、Resize 的逻辑
3. 修改这些逻辑使用 `isDraggingNode` 等新状态
4. 测试功能是否正常

### 方案 B：迁移到 Hooks（理想）⭐⭐

**优点**:
- 架构清晰
- 代码统一

**缺点**:
- 工作量大
- 需要重新测试

**实施步骤**:
1. 在 useDrag Hook 中添加磁吸功能
2. 在 useDrag Hook 中添加碰撞检测
3. 创建 useResize Hook
4. 删除旧代码
5. 全面测试

### 方案 C：混合方案（折中）⭐⭐⭐

**优点**:
- 平衡功能和架构
- 风险可控

**缺点**:
- 需要仔细设计接口

**实施步骤**:
1. 保留磁吸和碰撞检测在 App.tsx 中
2. 将这些逻辑封装成独立函数
3. 在 useDrag Hook 的回调中调用这些函数
4. 逐步迁移到 Hook 中

---

## 🚀 推荐执行方案

**我推荐采用方案 C（混合方案）**，具体步骤：

### 第 1 步：保留关键逻辑（立即执行）

保留以下旧代码：
- 磁吸功能（第 1027-1051 行）
- 碰撞检测（第 1093-1150 行）
- 节点 Resize（第 1062-1072 行）

### 第 2 步：修改变量引用（立即执行）

将旧代码中的变量引用修改为新 Hook 的状态：
- `draggingNodeId` → 从 `dragNodeRef.current` 获取
- `isDraggingNode` → 使用 useDrag 的 `isDragging`
- `draggingGroup` → 使用 useGroup 的 `isDraggingGroup`

### 第 3 步：封装独立函数（后续优化）

将磁吸和碰撞检测封装成独立函数：
```typescript
// core/utils/geometry.ts
export function applySnapLogic(proposedX, proposedY, nodeWidth, nodeHeight, otherNodes, scale) { ... }
export function detectCollision(finalX, finalY, nodeWidth, nodeHeight, otherNodes) { ... }
```

### 第 4 步：集成到 Hook（未来重构）

在 useDrag Hook 中调用这些函数：
```typescript
const handleMouseUp = useCallback((e: MouseEvent) => {
  // ... 计算最终位置
  
  // 应用磁吸
  const snapped = applySnapLogic(finalX, finalY, ...);
  
  // 碰撞检测
  const collision = detectCollision(snapped.x, snapped.y, ...);
  
  // 更新 Store
  onUpdateNode(id, { x: collision.x, y: collision.y });
}, []);
```

---

## 📝 下一步行动

1. **立即执行**：修改旧代码的变量引用，让代码可以编译
2. **测试验证**：测试拖拽、磁吸、碰撞检测是否正常工作
3. **后续优化**：逐步将逻辑迁移到 Hooks 中

---

**创建时间**: 2026-01-27  
**创建人**: AI Assistant  
**状态**: ✅ 分析完成，等待用户决策
