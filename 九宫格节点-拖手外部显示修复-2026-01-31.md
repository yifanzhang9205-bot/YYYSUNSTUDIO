# 九宫格节点 - 拖手外部显示修复

**日期**: 2026-01-31  
**状态**: ✅ 已完成

---

## 问题描述

拖手虽然在 GridSplitterNode.tsx 的最外层 `div`，但仍然被裁剪，无法显示在节点外部。

### 根本原因

GridSplitterNode.tsx 的最外层 `div` 本身就是 Node.tsx 渲染的节点容器的一部分，Node.tsx 的容器有 `overflow: hidden`（通过 `rounded-[24px]` 和内部的 `overflow-hidden`），所以拖手仍然被裁剪。

```tsx
// Node.tsx 的结构
<div className="... rounded-[24px] overflow-hidden">  {/* 这里有 overflow: hidden */}
  <GridSplitterNode>
    <div className="relative">  {/* 最外层 div */}
      {/* 拖手在这里，但还是被 Node.tsx 的 overflow 裁剪 */}
      <div className="absolute -bottom-5 -right-5 ...">拖手</div>
    </div>
  </GridSplitterNode>
</div>
```

---

## 解决方案

**把拖手从 GridSplitterNode.tsx 移到 Node.tsx**，放在节点容器的外面。

### 修改 1：Node.tsx - 添加拖手

```tsx
// components/Node.tsx (第 881-887 行)

const isInteracting = isDragging || isResizing || isGroupDragging;

// 🔥 九宫格节点拖手逻辑
const isGridSplitter = node.type === NodeType.GRID_SPLITTER;
const hasGridSelection = isGridSplitter && node.data.selectedIndex !== undefined && node.data.selectedIndex >= 0;
const showGridDragHandle = hasGridSelection && isSelected;

return (
  <div className="... rounded-[24px]">
    {/* 🔥 九宫格节点拖手 - 在节点外部，不被裁剪 */}
    {showGridDragHandle && (
      <div 
        className="absolute -bottom-5 -right-5 h-8 px-3 rounded-full bg-gray-100/95 backdrop-blur-md border border-gray-300 flex items-center gap-1.5 cursor-grab active:cursor-grabbing hover:bg-gray-200 hover:border-gray-400 active:scale-95 transition-all shadow-md z-[60]"
        draggable={true}
        onDragStart={(e) => {
          if (!node.data.inputImage || node.data.selectedIndex === undefined) {
            e.preventDefault();
            return;
          }
          e.stopPropagation();
          const dragData = {
            type: 'grid-splitter-image',
            originalImage: node.data.inputImage,
            selectedIndex: node.data.selectedIndex,
            croppedImage: node.data.croppedImages?.[node.data.selectedIndex]
          };
          e.dataTransfer.setData('application/json', JSON.stringify(dragData));
          e.dataTransfer.effectAllowed = 'copy';
          if (e.currentTarget instanceof HTMLElement) {
            e.currentTarget.style.opacity = '0.5';
          }
        }}
        onDragEnd={(e) => {
          e.stopPropagation();
          if (e.currentTarget instanceof HTMLElement) {
            e.currentTarget.style.opacity = '1';
          }
        }}
        onMouseDown={(e) => e.stopPropagation()}
        title="拖动创建新图片节点"
      >
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className="text-gray-600">
          <circle cx="9" cy="12" r="1"/><circle cx="9" cy="5" r="1"/><circle cx="9" cy="19" r="1"/><circle cx="15" cy="12" r="1"/><circle cx="15" cy="5" r="1"/><circle cx="15" cy="19" r="1"/>
        </svg>
        <span className="text-[11px] font-medium text-gray-700">拖出</span>
      </div>
    )}
    
    {/* 节点内容 */}
    ...
  </div>
);
```

### 修改 2：GridSplitterNode.tsx - 删除拖手

```tsx
// components/GridSplitterNode.tsx (第 391-393 行)

// 展开状态 - 显示完整的九宫格处理界面
return (
  <div className="w-full h-full relative">
    {/* 🔥 拖手已移到 Node.tsx，这里删除 */}
    <div className="w-full h-full flex flex-col bg-[#0a0a0a] rounded-2xl overflow-hidden">
      ...
    </div>
  </div>
);
```

---

## 显示逻辑

```tsx
// 显示条件
const isGridSplitter = node.type === NodeType.GRID_SPLITTER;
const hasGridSelection = isGridSplitter && node.data.selectedIndex !== undefined && node.data.selectedIndex >= 0;
const showGridDragHandle = hasGridSelection && isSelected;

// 翻译：
// 1. 节点类型是九宫格
// 2. 有选中的图片（单图模式）
// 3. 节点被选中
```

---

## 拖动数据

```tsx
const dragData = {
  type: 'grid-splitter-image',
  originalImage: node.data.inputImage,      // 原始 21:9 图片（高质量）
  selectedIndex: node.data.selectedIndex,   // 选中的索引
  croppedImage: node.data.croppedImages?.[node.data.selectedIndex]  // 切割后的图片（预览）
};
```

---

## 样式规格

| 属性 | 值 | 说明 |
|------|-----|------|
| 位置 | `absolute -bottom-5 -right-5` | 右下角外侧，距离 20px |
| 高度 | `h-8` | 32px |
| 内边距 | `px-3` | 左右 12px |
| 圆角 | `rounded-full` | 完全圆角 |
| 背景 | `bg-gray-100/95` | 浅灰色，95% 不透明度 |
| 边框 | `border border-gray-300` | 灰色边框 |
| 阴影 | `shadow-md` | 中等阴影 |
| z-index | `z-[60]` | 确保在最上层 |
| 光标 | `cursor-grab` / `active:cursor-grabbing` | 抓手光标 |
| 悬停 | `hover:bg-gray-200 hover:border-gray-400` | 悬停时变深 |
| 点击 | `active:scale-95` | 点击时缩小 |

---

## 验证清单

- ✅ 拖手在节点外部显示（不被裁剪）
- ✅ 显示条件：单图模式 + 节点选中
- ✅ 位置：右下角外侧，距离 20px
- ✅ 配色：浅灰色（`bg-gray-100/95`）
- ✅ z-index：`z-[60]`（确保在最上层）
- ✅ 拖动功能：使用原始高质量图片
- ✅ 阻止节点拖动：`onMouseDown={(e) => e.stopPropagation()}`
- ✅ 拖动时视觉反馈：透明度 50%
- ✅ 拖动结束恢复：透明度 100%

---

## 架构说明

### 为什么要在 Node.tsx 中添加拖手？

1. **Node.tsx 是节点容器的最外层**：它控制节点的位置、大小、边框、阴影等
2. **GridSplitterNode.tsx 是节点内容**：它被 Node.tsx 包裹，受 `overflow: hidden` 限制
3. **拖手需要在节点外部**：所以必须在 Node.tsx 中添加，而不是 GridSplitterNode.tsx

### 数据流

```
Node.tsx (节点容器)
  ├── 读取 node.data.selectedIndex（判断是否显示拖手）
  ├── 读取 node.data.inputImage（拖动时传递原始图片）
  ├── 读取 node.data.croppedImages（拖动时传递切割图片）
  └── 渲染拖手（在节点外部）

GridSplitterNode.tsx (节点内容)
  ├── 管理图片切割逻辑
  ├── 管理选中状态
  └── 通过 onUpdate 更新 node.data
```

---

## 注意事项

1. **不要在 GridSplitterNode.tsx 中添加拖手**：会被 Node.tsx 的 `overflow: hidden` 裁剪
2. **不要在 App.tsx 中添加拖手**：违反架构规则（UI Layer 不应该有业务逻辑）
3. **拖手只在 Node.tsx 中添加**：这是唯一正确的位置

---

## 相关文件

- `components/Node.tsx` - 节点容器，拖手在这里
- `components/GridSplitterNode.tsx` - 节点内容，拖手已删除
- `App.tsx` - 处理拖放事件（`handleCanvasDrop`）

---

## 测试步骤

1. 创建九宫格节点
2. 上传 21:9 图片
3. 双击选择一张图片（进入单图模式）
4. 点击节点（选中节点）
5. 查看右下角是否显示拖手（浅灰色，距离 20px）
6. 拖动拖手到画布
7. 验证是否创建新的高质量图片节点

---

## 完成时间

2026-01-31 23:45
