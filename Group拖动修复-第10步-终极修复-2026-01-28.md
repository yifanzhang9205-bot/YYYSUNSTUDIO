# Group 拖动修复 - 第 10 步：终极修复（2026-01-28）

## 问题描述

用户报告：拖动组时像果冻，拖动走一半，松开鼠标后瞬间到达指定位置

**具体症状**：
- 拖动 10px，组只移动 5px（走一半）
- 松开鼠标后，组瞬间跳到 10px（瞬间到达）

## 问题分析

### 真正的根本原因

**Canvas 有 `scale` 缩放，导致坐标系不一致！**

在 `App.tsx` 中，Canvas 有这样的 transform：

```typescript
<div style={{ transform: `translate(${pan.x}px, ${pan.y}px) scale(${scale})` }}>
  {/* 组和节点都在这里面 */}
</div>
```

**问题**：
1. 组的 `left/top` 是**世界坐标**（未缩放）
2. Canvas 有 `scale(${scale})`，所以组在屏幕上的实际位置是 `left * scale`
3. 拖动时，`updateGroupDrag` 使用**屏幕坐标** `screenDx`
4. 但组已经被 Canvas 缩放了，所以 `transform: translate(screenDx)` 的实际效果是 `screenDx * scale`（相对于世界坐标）

**举例**（假设 `scale = 0.5`）：
```
1. 拖动 10px（屏幕坐标）
2. transform = translate(10px, 10px)
3. 但组已经被 scale(0.5) 缩放了
4. 所以实际移动 = 10px * 0.5 = 5px（世界坐标）
5. 松开时更新 state = 10px（世界坐标）
6. 所以跳了 5px
```

### 详细流程

```
1. 拖动中（updateGroupDrag）：
   - screenDx = 10px（屏幕坐标）
   - transform = translate(10px, 10px)
   - 实际移动 = 10px * scale = 5px（世界坐标）
   - 视觉上：组在 5px

2. 松开时（endGroupDrag）：
   - worldDx = 10px / scale = 10px（世界坐标）
   - 更新 state (x = 10px)
   - 清除 transform
   - React 渲染到 10px
   - 视觉上：从 5px 跳到 10px（跳了 5px）
```

## 解决方案

### 核心思路

**`transform` 需要使用世界坐标，而不是屏幕坐标。**

因为 Canvas 已经有 `scale`，所以 `transform` 的值会被 Canvas 的 `scale` 再次缩放。

### 修改位置

`hooks/useGroup.ts` 的 `updateGroupDrag` 方法（第 193-228 行）

### 修改内容

```typescript
// ❌ 旧代码：使用屏幕坐标
const screenDx = e.clientX - mouseStartX;
const screenDy = e.clientY - mouseStartY;

groupElement.style.transform = `translate(${screenDx}px, ${screenDy}px)`;
// 实际效果：translate(10px) * scale(0.5) = 5px（世界坐标）

// ✅ 新代码：转换为世界坐标
const screenDx = e.clientX - mouseStartX;
const screenDy = e.clientY - mouseStartY;

// 🔥 关键修复：转换为世界坐标
const worldDx = screenDx / currentScale;
const worldDy = screenDy / currentScale;

groupElement.style.transform = `translate(${worldDx}px, ${worldDy}px)`;
// 实际效果：translate(20px) * scale(0.5) = 10px（世界坐标）
```

### 为什么要除以 scale？

因为 Canvas 有 `scale(${scale})`，所以：
- `transform: translate(20px)` 在 Canvas 中的实际效果是 `20px * scale`
- 要让实际效果是 `10px`，需要 `transform: translate(10px / scale)`
- 即 `translate(20px) * scale(0.5) = 10px`

## 修复效果

### 修复前（scale = 0.5）
```
拖动 10px（屏幕）→ transform(10px) → 实际 5px（世界）→ 松开 → 跳到 10px
```

### 修复后（scale = 0.5）
```
拖动 10px（屏幕）→ transform(20px) → 实际 10px（世界）→ 松开 → 停在 10px（无跳跃）
```

## 技术细节

### 坐标系说明

1. **屏幕坐标**：鼠标的 `clientX/clientY`，未缩放
2. **世界坐标**：Canvas 内部的坐标，组的 `left/top`
3. **转换公式**：`世界坐标 = 屏幕坐标 / scale`

### Canvas 的 transform 层级

```html
<div style="transform: scale(0.5)">  <!-- Canvas -->
  <div style="left: 100px; transform: translate(20px)">  <!-- Group -->
    <!-- 实际位置 = (100 + 20) * 0.5 = 60px（屏幕坐标） -->
  </div>
</div>
```

### 为什么之前没发现？

因为之前测试时 `scale` 可能是 1.0，所以没有缩放，问题不明显。

## 相关文件

- `hooks/useGroup.ts`（第 193-228 行）

## 测试方法

1. 打开浏览器
2. **缩放 Canvas**（滚轮缩放，让 scale ≠ 1.0）
3. 创建一个组，包含多个节点
4. 拖动组
5. 观察拖动时是否跟手，松开时是否有跳跃

**预期结果**：
- 拖动时完全跟手，无延迟
- 松开时平滑停止，无跳跃
- 任何 scale 下都正常

## 总结

跳跃问题的根本原因是 **Canvas 的 `scale` 导致坐标系不一致**。

`transform` 使用屏幕坐标，但组已经被 Canvas 缩放了，所以实际移动距离 = `screenDx * scale`，与最终的世界坐标不一致。

通过将 `transform` 转换为世界坐标（`worldDx = screenDx / scale`），彻底解决了跳跃问题。

**核心原则**：
- 拖动时用 `transform`（性能）+ 世界坐标（避免跳跃）
- 松开时用 state（数据）+ 世界坐标（一致性）
- 始终注意 Canvas 的 `scale` 对坐标的影响

---

**修复完成时间**：2026-01-28
**修复人**：AI Assistant
**状态**：✅ 已完成（终极版）
