# 3D 相机提示词分析和改进方案

## 当前提示词结构

### 1. 提示词组成
当前提示词包含以下部分：
1. **角色定义**：定义 AI 为 3D CGI 艺术家
2. **特征锁定**：强调保持角色一致性（面部、发型、服装等）
3. **输出格式**：要求生成 ONE SINGLE 21:9 图片，包含 3×3 网格
4. **相机角度**：9 个面板的详细视觉描述
5. **布局说明**：ASCII 图示九宫格布局
6. **规则列表**：✅ 和 ❌ 的约束条件
7. **用户自定义**：额外风格要求（可选）

### 2. 角度生成逻辑
```typescript
// 九宫格偏移量
const hOffsets = [-30, 0, 30];  // 水平：左中右
const vOffsets = [20, 0, -20];  // 垂直：上中下（俯视、平视、仰视）
const zOffsets = [-1.5, 0, 1.5]; // 距离：近中远
```

**问题：偏移量较大**
- 水平 ±30° 跨度太大（从左前到右后）
- 可能导致角色变形或不一致

### 3. 视觉描述方式
使用**详细的文字描述**而不是精确度数：
```
"Three-quarter view from front-right. Both eyes visible, far eye slightly foreshortened."
"Strict side profile from the right. Only one eye and one ear visible."
```

## 历史优化记录

### 优化 1：精确角度描述（2026-01-23）
- **改进**：从文字描述改为精确度数（如 "front-right 45°"）
- **效果**：理论上更精确，但实际效果未知

### 优化 2：减小偏移量
- **改进前**：±20° 水平，±15° 垂直
- **改进后**：±15° 水平，±10° 垂直
- **目的**：更聚焦在目标角度周围

### 优化 3：修正距离逻辑
- **问题**：距离偏移用了列索引（col）而不是行索引（row）
- **修正**：改为用行索引，上近下远

## 当前问题分析

### 问题 1：提示词过于复杂
**现状：**
- 约 50 行提示词
- 大量重复的约束条件
- 多层嵌套的规则说明

**影响：**
- AI 可能抓不住重点
- 过多的"不要做"反而引起注意
- 处理时间更长

### 问题 2：视觉描述不够精确
**现状：**
```
"Three-quarter view from front-right. Both eyes visible, far eye slightly foreshortened. Slight depth to the face."
```

**问题：**
- "slightly foreshortened" 是主观描述
- "slight depth" 没有量化标准
- AI 对这些描述的理解可能不一致

### 问题 3：九宫格变化范围
**现状：**
- 水平 ±30°（总跨度 60°）
- 垂直 ±20°（总跨度 40°）
- 距离 ±1.5（总跨度 3）

**问题：**
- 跨度太大，可能导致角色变形
- 边缘面板与中心面板差异过大
- 不符合"细微变化"的预期

### 问题 4：特征锁定效果不佳
**现状：**
```
STRICTLY MAINTAIN the character's identity across all 9 panels:
- Facial Features: EXACT same eye shape, nose shape, mouth, and expression
- Hair: EXACT same hairstyle, length, color, and texture
...
```

**问题：**
- 过于强调"EXACT same"可能适得其反
- AI 可能理解为"完全相同的图片"
- 缺少"允许的变化"说明

## 改进方案

### 方案 A：精简提示词（推荐）
**核心思路：**
- 去掉冗余的规则说明
- 使用更简洁的指令
- 强调"自然的 3D 旋转"而不是"完全相同"

**示例提示词：**
```
Generate ONE 21:9 image containing a 3×3 grid (9 panels).

Re-render the reference character from 9 camera angles:
- Panel 1-3 (top row): slightly closer, rotated left/center/right
- Panel 4-6 (middle row): target distance, rotated left/center/right
- Panel 7-9 (bottom row): slightly farther, rotated left/center/right

MAINTAIN: Same character, same style, same lighting, same background
VARY: Only camera position (rotation ±25°, elevation ±15°, distance ±1.2)

Target angle: {azimuth}°, {elevation}°, distance {zoom}
```

**优势：**
- 提示词减少 70%
- 重点突出
- 更符合 AI 的理解方式

### 方案 B：参数化描述
**核心思路：**
- 使用精确的度数和距离值
- 类似 3D 软件的相机参数
- 减少文字描述的歧义

**示例提示词：**
```
Camera parameters for 9 panels (3×3 grid in ONE 21:9 image):

Panel 1: azimuth {h-25}°, elevation {v+15}°, distance {z-1.2}
Panel 2: azimuth {h}°, elevation {v+15}°, distance {z-1.2}
Panel 3: azimuth {h+25}°, elevation {v+15}°, distance {z-1.2}
Panel 4: azimuth {h-25}°, elevation {v}°, distance {z}
Panel 5: azimuth {h}°, elevation {v}°, distance {z} ← TARGET
Panel 6: azimuth {h+25}°, elevation {v}°, distance {z}
Panel 7: azimuth {h-25}°, elevation {v-15}°, distance {z+1.2}
Panel 8: azimuth {h}°, elevation {v-15}°, distance {z+1.2}
Panel 9: azimuth {h+25}°, elevation {v-15}°, distance {z+1.2}

Render the same character from reference image at these angles.
Maintain consistent appearance, only change camera position.
```

**优势：**
- 最精确的控制
- 类似专业 3D 软件
- 减少理解偏差

### 方案 C：减小变化范围
**核心思路：**
- 保持当前提示词结构
- 只调整偏移量参数
- 更聚焦的角度范围

**调整参数：**
```typescript
// 改进前
const hOffsets = [-30, 0, 30];
const vOffsets = [20, 0, -20];
const zOffsets = [-1.5, 0, 1.5];

// 改进后
const hOffsets = [-20, 0, 20];  // 减小到 ±20°
const vOffsets = [12, 0, -12];  // 减小到 ±12°
const zOffsets = [-1.0, 0, 1.0]; // 减小到 ±1.0
```

**优势：**
- 改动最小
- 风险最低
- 可以快速测试

## 推荐实施步骤

### 第一步：减小偏移量（方案 C）
- 快速测试，看看是否改善
- 如果效果好，继续优化
- 如果效果差，说明问题不在偏移量

### 第二步：精简提示词（方案 A）
- 去掉冗余的规则
- 使用更自然的语言
- 强调"3D 旋转"而不是"完全相同"

### 第三步：参数化描述（方案 B）
- 如果前两步效果不佳
- 尝试最精确的参数化方式
- 可能需要多次调试

## 需要你确认的问题

1. **当前最不满意的是什么？**
   - 角色不一致（面部、发型、服装变化）
   - 角度不准确（没有按照设定的角度生成）
   - 九宫格布局混乱（不是 3×3 网格）
   - 其他问题？

2. **你期望的效果是什么？**
   - 完全相同的角色，只是角度不同
   - 允许轻微的变化，但保持整体一致
   - 像 3D 模型一样精确的旋转

3. **你觉得哪个方案更合适？**
   - 方案 A：精简提示词
   - 方案 B：参数化描述
   - 方案 C：减小偏移量
   - 或者组合使用？

请告诉我你的想法，我们一起调整！
