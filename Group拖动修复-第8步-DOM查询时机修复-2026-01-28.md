# Group 拖动修复 - 第 8 步：DOM 查询时机修复

**日期**：2026-01-28  
**问题**：组先过去，节点突然闪过去  
**状态**：✅ 已修复

---

## 🔍 问题现象

1. **拖动组时**：组立即移动，但节点不跟随
2. **松手后**：节点突然"闪现"到新位置
3. **控制台报错**：`[useGroup] 找不到节点 DOM 元素: n-xxx`

---

## 🎯 问题根源

### React 渲染时机问题

当用户点击组时，事件处理流程：

```
1. 用户点击组
   ↓
2. startGroupDrag 立即执行
   ↓
3. 尝试查询子节点的 DOM 元素
   ↓
4. ❌ 失败！节点的 DOM 还在 React 渲染队列中
   ↓
5. 组使用 transform 移动，但节点没有跟随
   ↓
6. endGroupDrag 时，节点通过 Store 更新位置
   ↓
7. 节点"闪现"到新位置
```

### 为什么会这样？

**React 的渲染是异步的**：
- 当你点击组时，React 可能正在重新渲染节点
- `startGroupDrag` 在 React 渲染完成**之前**执行
- 此时 DOM 元素还没有挂载，`querySelector` 返回 `null`

---

## ✅ 解决方案

### 使用 `requestAnimationFrame` 延迟查询 DOM

```typescript
const startGroupDrag = useCallback((e: React.MouseEvent, groupId: string, group: Group) => {
  // 1. 先初始化拖动上下文（不包含子节点）
  dragGroupRef.current = {
    id: groupId,
    startX: group.x,
    startY: group.y,
    mouseStartX: e.clientX,
    mouseStartY: e.clientY,
    childNodes: [], // 先设置为空数组
  };

  // 2. 立即禁用组的 transition
  const groupElement = document.querySelector(`[data-group-id="${groupId}"]`) as HTMLElement;
  if (groupElement) {
    groupElement.style.transition = 'none';
    groupElement.style.willChange = 'transform';
  }

  // 3. 设置拖动状态
  setIsDraggingGroup(true);

  // 🔥 4. 使用 requestAnimationFrame 延迟查询 DOM
  // 确保 React 已经完成渲染，DOM 元素已经挂载
  requestAnimationFrame(() => {
    if (!dragGroupRef.current) return; // 如果已经取消拖动，直接返回

    // 查询子节点的 DOM 元素
    const childNodes = getGroupNodes(groupId).map(n => {
      let element = document.querySelector(`[data-node-id="${n.id}"]`) as HTMLElement;
      
      if (!element) {
        element = document.getElementById(`node-${n.id}`) as HTMLElement;
      }
      
      if (!element) {
        console.warn(`[useGroup] 找不到节点 DOM 元素: ${n.id}`);
      }
      
      return {
        id: n.id,
        startX: n.x,
        startY: n.y,
        element,
      };
    });

    // 更新拖动上下文，添加子节点
    if (dragGroupRef.current) {
      dragGroupRef.current.childNodes = childNodes;

      // 禁用子节点的 transition
      childNodes.forEach(child => {
        if (child.element) {
          child.element.style.transition = 'none';
          child.element.style.willChange = 'transform';
        }
      });
    }
  });
}, [getGroupNodes]);
```

---

## 🎉 修复效果

### 修复前
- ❌ 组先移动，节点不跟随
- ❌ 松手后节点"闪现"
- ❌ 控制台报错：找不到 DOM 元素

### 修复后
- ✅ 组和节点**同步移动**
- ✅ 拖动过程**丝滑流畅**
- ✅ 无控制台报错

---

## 🔧 技术细节

### 为什么 `requestAnimationFrame` 有效？

`requestAnimationFrame` 会在**浏览器下一次重绘之前**执行回调：

```
用户点击
  ↓
startGroupDrag 执行
  ↓
React 调度渲染
  ↓
React 完成渲染
  ↓
浏览器准备重绘
  ↓
requestAnimationFrame 回调执行 ← 此时 DOM 已经更新
  ↓
查询子节点的 DOM 元素 ← 成功！
```

### 为什么不用 `setTimeout(fn, 0)`？

`setTimeout(fn, 0)` 会在**宏任务队列**中执行，可能比 React 渲染更早：

```
用户点击
  ↓
startGroupDrag 执行
  ↓
setTimeout(fn, 0) 添加到宏任务队列
  ↓
宏任务队列执行 ← 可能在 React 渲染之前
  ↓
查询子节点的 DOM 元素 ← 可能失败
```

---

## 📋 修改文件

- ✅ `hooks/useGroup.ts` - 修改 `startGroupDrag` 函数

---

## 🎯 架构合规性

- ✅ **没有修改 App.tsx**（遵守架构规则）
- ✅ **只修改了 hooks/useGroup.ts**（交互逻辑层）
- ✅ **没有修改 Node.tsx**（UI 层已经正确）

---

## 🚀 下一步

如果还有问题，可能需要：
1. 检查节点是否被正确渲染（`console.log` 节点数量）
2. 检查 `data-node-id` 属性是否正确设置
3. 检查是否有其他代码干扰了 DOM 结构

---

**修复完成！** 🎉
