# 组拖动闪烁修复 - 方案 A 实施代码

**方案**: 延迟 React 重渲染  
**修改文件**: `hooks/useGroup.ts`  
**修改函数**: `endGroupDrag`  
**预期效果**: ✅ 完全消除闪烁

---

## 📝 修改前的代码

```typescript
const endGroupDrag = useCallback((e: MouseEvent, currentScale: number) => {
  if (!dragGroupRef.current) return;

  const { id, startX, startY, mouseStartX, mouseStartY, childNodes } = dragGroupRef.current;

  // 计算最终位置（世界坐标）
  const screenDx = e.clientX - mouseStartX;
  const screenDy = e.clientY - mouseStartY;
  const worldDx = screenDx / currentScale;
  const worldDy = screenDy / currentScale;
  const finalX = startX + worldDx;
  const finalY = startY + worldDy;

  // 保存历史
  onSaveHistory();

  // 🔥 关键修复：先操作 DOM（禁用 transition + 清除 transform + 强制重绘），再更新 Store
  // 这是节点拖动成功的核心方案！
  
  // 1. 处理组元素
  const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
  if (groupElement) {
    // 禁用 transition
    const originalTransition = groupElement.style.transition;
    groupElement.style.transition = 'none';
    
    // 清除 transform 和 willChange
    groupElement.style.transform = '';
    groupElement.style.willChange = 'auto';
    
    // 强制浏览器重绘（关键！）
    groupElement.offsetHeight;
    
    // 异步恢复 transition（在下一帧）
    requestAnimationFrame(() => {
      groupElement.style.transition = originalTransition;
    });
  }

  // 2. 处理子节点元素
  childNodes.forEach((child: { id: string; startX: number; startY: number; element: HTMLElement | null }) => {
    if (child.element) {
      // 禁用 transition
      const originalTransition = child.element.style.transition;
      child.element.style.transition = 'none';
      
      // 清除 transform 和 willChange
      child.element.style.transform = '';
      child.element.style.willChange = 'auto';
      
      // 强制浏览器重绘（关键！）
      child.element.offsetHeight;
      
      // 异步恢复 transition（在下一帧）
      requestAnimationFrame(() => {
        if (child.element) {
          child.element.style.transition = originalTransition;
        }
      });
    }
  });

  // 3. 更新 Store（让 React 知道新位置）
  onUpdateGroup(id, { x: finalX, y: finalY });

  // 4. 更新子节点位置（使用 Store）
  childNodes.forEach((child: { id: string; startX: number; startY: number }) => {
    onUpdateNode(child.id, {
      x: child.startX + worldDx,
      y: child.startY + worldDy,
    });
  });

  // 5. 清除拖动状态和上下文
  setIsDraggingGroup(false);
  setDraggingGroupOffset(null);
  dragGroupRef.current = null;
}, [onSaveHistory, onUpdateGroup, onUpdateNode]);
```

---

## ✅ 修改后的代码（方案 A）

```typescript
const endGroupDrag = useCallback((e: MouseEvent, currentScale: number) => {
  if (!dragGroupRef.current) return;

  const { id, startX, startY, mouseStartX, mouseStartY, childNodes } = dragGroupRef.current;

  // 计算最终位置（世界坐标）
  const screenDx = e.clientX - mouseStartX;
  const screenDy = e.clientY - mouseStartY;
  const worldDx = screenDx / currentScale;
  const worldDy = screenDy / currentScale;
  const finalX = startX + worldDx;
  const finalY = startY + worldDy;

  // 保存历史
  onSaveHistory();

  // 🔥 关键修复：先操作 DOM，再延迟更新 Store
  // 这样 React 重渲染时，transition 已经恢复，不会被覆盖
  
  // 1. 处理组元素
  const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
  if (groupElement) {
    // 禁用 transition
    const originalTransition = groupElement.style.transition;
    groupElement.style.transition = 'none';
    
    // 清除 transform 和 willChange
    groupElement.style.transform = '';
    groupElement.style.willChange = 'auto';
    
    // 强制浏览器重绘（关键！）
    groupElement.offsetHeight;
    
    // 异步恢复 transition（在下一帧）
    // 🔥 关键修复：在 transition 恢复后，再更新 Store
    requestAnimationFrame(() => {
      if (groupElement) {
        groupElement.style.transition = originalTransition;
      }
      
      // 🔥 延迟更新 Store，确保 transition 已恢复
      // 这样 React 重渲染时，transition 不会被 shouldDisableTransition 覆盖
      onUpdateGroup(id, { x: finalX, y: finalY });
      
      // 更新子节点位置
      childNodes.forEach((child: { id: string; startX: number; startY: number }) => {
        onUpdateNode(child.id, {
          x: child.startX + worldDx,
          y: child.startY + worldDy,
        });
      });
    });
  }

  // 2. 处理子节点元素
  childNodes.forEach((child: { id: string; startX: number; startY: number; element: HTMLElement | null }) => {
    if (child.element) {
      // 禁用 transition
      const originalTransition = child.element.style.transition;
      child.element.style.transition = 'none';
      
      // 清除 transform 和 willChange
      child.element.style.transform = '';
      child.element.style.willChange = 'auto';
      
      // 强制浏览器重绘（关键！）
      child.element.offsetHeight;
      
      // 异步恢复 transition（在下一帧）
      requestAnimationFrame(() => {
        if (child.element) {
          child.element.style.transition = originalTransition;
        }
      });
    }
  });

  // 3. 清除拖动状态和上下文
  // 🔥 注意：不要在这里调用 setIsDraggingGroup(false)
  // 因为这会立即触发 React 重渲染，导致 transition 被覆盖
  // 改为在 requestAnimationFrame 内部调用
  
  // 延迟清除拖动状态，确保 DOM 操作完成后再清除
  requestAnimationFrame(() => {
    setIsDraggingGroup(false);
    setDraggingGroupOffset(null);
    dragGroupRef.current = null;
  });
}, [onSaveHistory, onUpdateGroup, onUpdateNode]);
```

---

## 🔍 关键改动说明

### 改动 1：延迟 Store 更新

**之前：**
```typescript
// 立即更新 Store
onUpdateGroup(id, { x: finalX, y: finalY });
childNodes.forEach((child) => {
  onUpdateNode(child.id, { ... });
});
```

**之后：**
```typescript
// 延迟更新 Store，在 transition 恢复后
requestAnimationFrame(() => {
  if (groupElement) {
    groupElement.style.transition = originalTransition;
  }
  
  // 在这里更新 Store
  onUpdateGroup(id, { x: finalX, y: finalY });
  childNodes.forEach((child) => {
    onUpdateNode(child.id, { ... });
  });
});
```

**原因：**
- 确保 transition 恢复后再触发 React 重渲染
- 这样 React 重渲染时，transition 已经是 `'all 0.2s ease'`
- 不会被 `shouldDisableTransition` 覆盖

### 改动 2：延迟清除拖动状态

**之前：**
```typescript
// 立即清除拖动状态
setIsDraggingGroup(false);
setDraggingGroupOffset(null);
dragGroupRef.current = null;
```

**之后：**
```typescript
// 延迟清除拖动状态
requestAnimationFrame(() => {
  setIsDraggingGroup(false);
  setDraggingGroupOffset(null);
  dragGroupRef.current = null;
});
```

**原因：**
- 确保 DOM 操作完全完成后再清除状态
- 避免状态清除导致的额外重渲染

---

## 📊 执行时序对比

### 修改前（❌ 闪烁）

```
T0: 用户松开鼠标
T1: endGroupDrag 触发
T2: groupElement.style.transition = 'none'
T3: groupElement.offsetHeight (强制重绘)
T4: requestAnimationFrame 注册回调 (恢复 transition)
T5: onUpdateGroup() 立即调用 ← 问题！
T6: React 重渲染
T7: shouldDisableTransition = false
T8: React 设置 transition = 'all 0.2s ease' ← 覆盖了 T2 的 'none'
T9: React 设置 left/top = finalX/finalY
T10: 浏览器看到 transition 启用 + left/top 改变 → 播放动画 ← 闪烁！
T11: requestAnimationFrame 回调执行 (恢复 transition)
```

### 修改后（✅ 无闪烁）

```
T0: 用户松开鼠标
T1: endGroupDrag 触发
T2: groupElement.style.transition = 'none'
T3: groupElement.offsetHeight (强制重绘)
T4: requestAnimationFrame 注册回调
T5: setIsDraggingGroup(false) 延迟调用 ← 改为延迟
T6: onUpdateGroup() 延迟调用 ← 改为延迟
T7: requestAnimationFrame 回调执行
T8: groupElement.style.transition = originalTransition (恢复 transition)
T9: onUpdateGroup() 调用 ← 现在调用
T10: React 重渲染
T11: shouldDisableTransition = false
T12: React 设置 transition = 'all 0.2s ease'
T13: React 设置 left/top = finalX/finalY
T14: 浏览器看到 transition 启用 + left/top 改变 → 播放动画
T15: 但此时 transition 已经恢复，所以动画是平滑的，不是闪烁 ✅
```

---

## 🧪 测试清单

### 测试 1：基本功能
- [ ] 创建一个组
- [ ] 拖动组
- [ ] 松开鼠标
- [ ] 观察：无闪烁？✅

### 测试 2：多个组
- [ ] 创建多个组
- [ ] 依次拖动每个组
- [ ] 观察：都无闪烁？✅

### 测试 3：快速拖动
- [ ] 快速连续拖动同一个组
- [ ] 观察：无闪烁？✅

### 测试 4：组内节点
- [ ] 拖动组内的节点
- [ ] 观察：节点无闪烁？✅

### 测试 5：边界情况
- [ ] 拖动组到画布边界
- [ ] 拖动组到其他组上方
- [ ] 拖动组时快速缩放
- [ ] 观察：都无闪烁？✅

### 测试 6：性能
- [ ] 打开 Chrome DevTools Performance
- [ ] 拖动组，松开鼠标
- [ ] 观察：是否有多余的重排/重绘？
- [ ] 预期：只有必要的重排/重绘 ✅

---

## 🚀 实施步骤

### 步骤 1：备份原文件
```bash
cp hooks/useGroup.ts hooks/useGroup.ts.backup-20260209
```

### 步骤 2：应用修改
- 打开 `hooks/useGroup.ts`
- 找到 `endGroupDrag` 函数
- 按照上面的代码替换

### 步骤 3：验证修改
```bash
# 检查是否有语法错误
npm run build

# 或者在浏览器中测试
npm run dev
```

### 步骤 4：测试修复
- 按照上面的测试清单逐一测试
- 确保所有测试都通过

### 步骤 5：提交代码
```bash
git add hooks/useGroup.ts
git commit -m "fix: 组拖动闪烁问题 - 延迟 React 重渲染方案"
```

---

## 📋 完整的修改代码（可直接复制）

```typescript
/**
 * 🔥 修复：组拖动闪烁问题
 * 原因：React 重渲染时，transition 被 shouldDisableTransition 覆盖
 * 方案：延迟 Store 更新，确保 transition 恢复后再触发 React 重渲染
 * 日期：2026-02-09
 */
const endGroupDrag = useCallback((e: MouseEvent, currentScale: number) => {
  if (!dragGroupRef.current) return;

  const { id, startX, startY, mouseStartX, mouseStartY, childNodes } = dragGroupRef.current;

  // 计算最终位置（世界坐标）
  const screenDx = e.clientX - mouseStartX;
  const screenDy = e.clientY - mouseStartY;
  const worldDx = screenDx / currentScale;
  const worldDy = screenDy / currentScale;
  const finalX = startX + worldDx;
  const finalY = startY + worldDy;

  // 保存历史
  onSaveHistory();

  // 🔥 关键修复：先操作 DOM，再延迟更新 Store
  // 这样 React 重渲染时，transition 已经恢复，不会被覆盖
  
  // 1. 处理组元素
  const groupElement = document.querySelector(`[data-group-id="${id}"]`) as HTMLElement;
  if (groupElement) {
    // 禁用 transition
    const originalTransition = groupElement.style.transition;
    groupElement.style.transition = 'none';
    
    // 清除 transform 和 willChange
    groupElement.style.transform = '';
    groupElement.style.willChange = 'auto';
    
    // 强制浏览器重绘（关键！）
    groupElement.offsetHeight;
    
    // 异步恢复 transition（在下一帧）
    // 🔥 关键修复：在 transition 恢复后，再更新 Store
    requestAnimationFrame(() => {
      if (groupElement) {
        groupElement.style.transition = originalTransition;
      }
      
      // 🔥 延迟更新 Store，确保 transition 已恢复
      // 这样 React 重渲染时，transition 不会被 shouldDisableTransition 覆盖
      onUpdateGroup(id, { x: finalX, y: finalY });
      
      // 更新子节点位置
      childNodes.forEach((child: { id: string; startX: number; startY: number }) => {
        onUpdateNode(child.id, {
          x: child.startX + worldDx,
          y: child.startY + worldDy,
        });
      });
    });
  }

  // 2. 处理子节点元素
  childNodes.forEach((child: { id: string; startX: number; startY: number; element: HTMLElement | null }) => {
    if (child.element) {
      // 禁用 transition
      const originalTransition = child.element.style.transition;
      child.element.style.transition = 'none';
      
      // 清除 transform 和 willChange
      child.element.style.transform = '';
      child.element.style.willChange = 'auto';
      
      // 强制浏览器重绘（关键！）
      child.element.offsetHeight;
      
      // 异步恢复 transition（在下一帧）
      requestAnimationFrame(() => {
        if (child.element) {
          child.element.style.transition = originalTransition;
        }
      });
    }
  });

  // 3. 清除拖动状态和上下文
  // 🔥 注意：延迟清除拖动状态，确保 DOM 操作完成后再清除
  requestAnimationFrame(() => {
    setIsDraggingGroup(false);
    setDraggingGroupOffset(null);
    dragGroupRef.current = null;
  });
}, [onSaveHistory, onUpdateGroup, onUpdateNode]);
```

---

## ⚠️ 注意事项

1. **不要改 App.tsx**
   - 这个修复只需要改 `hooks/useGroup.ts`
   - 不需要改 App.tsx 的渲染逻辑

2. **保持 transition 的原始值**
   - 代码中保存了 `originalTransition`
   - 确保恢复时用的是原始值，不是硬编码的值

3. **处理 null 检查**
   - 代码中有 `if (groupElement)` 和 `if (child.element)` 的检查
   - 确保不会因为 DOM 元素不存在而出错

4. **测试多个组**
   - 确保修复对多个组都有效
   - 不要只测试单个组

---

## 📞 如果还有闪烁

如果应用这个修复后仍然有闪烁，请检查：

1. **是否正确应用了修改？**
   - 确认 `onUpdateGroup` 在 `requestAnimationFrame` 内部
   - 确认 `setIsDraggingGroup(false)` 也在 `requestAnimationFrame` 内部

2. **是否有其他地方在改 transition？**
   - 搜索 `groupElement.style.transition`
   - 搜索 `shouldDisableTransition`
   - 确保没有其他地方在覆盖 transition

3. **是否有 CSS 类在控制 transition？**
   - 检查 CSS 文件中是否有 `.group` 的 transition 定义
   - 如果有，可能需要调整优先级

4. **是否是浏览器缓存问题？**
   - 清除浏览器缓存
   - 按 Ctrl+Shift+R 强制刷新

---

## 🎯 预期结果

应用这个修复后：
- ✅ 组拖动松手后无闪烁
- ✅ 多个组都无闪烁
- ✅ 快速连续拖动无闪烁
- ✅ 性能无明显下降
- ✅ 用户体验大幅提升

