# UI 改版 - 阶段 2 - 功能 2：动画连接线

> 📅 实施时间：2026-02-01  
> ⏱️ 预计耗时：1 小时  
> 🎯 目标：连接线添加流动动画，表示数据流向

---

## 🎯 功能目标

### 动画效果

1. **连接线流动动画**
   - 虚线流动效果
   - 表示数据从输出端流向输入端
   - 动画速度：1 秒完成一个循环
   - 流畅、连续

2. **临时连接线动画**
   - 拖拽创建连接时的临时线
   - 虚线 + 脉冲效果
   - 更明显的视觉反馈

---

## 📋 实施方案

### 方案 A：SVG stroke-dasharray 动画（推荐）

**优点**：
- 原生 SVG 动画
- 性能好
- 代码简洁

**实施步骤**：

1. 修改 `App.tsx` 的连接线渲染
   - 添加 `stroke-dasharray` 属性
   - 添加 `stroke-dashoffset` 动画

2. 修改 `index.html`（可选）
   - 添加 CSS 动画关键帧

---

## 🔧 详细实施

### 步骤 1：找到连接线渲染位置

**位置**：`App.tsx`

**搜索关键词**：
- `<line` 或 `<path`
- `connections.map`
- `stroke=`

---

### 步骤 2：添加流动动画

**修改内容**：

```tsx
// 普通连接线
<line
  x1={startX}
  y1={startY}
  x2={endX}
  y2={endY}
  stroke="#b1b1b7"
  strokeWidth="2"
  // 🆕 添加虚线和动画
  strokeDasharray="8 4"
  strokeDashoffset="0"
  className="animate-flow"
/>

// 临时连接线
<line
  x1={startX}
  y1={startY}
  x2={mouseX}
  y2={mouseY}
  stroke="#3b82f6"
  strokeWidth="2"
  // 🆕 添加虚线和动画
  strokeDasharray="8 4"
  strokeDashoffset="0"
  className="animate-flow"
  opacity="0.6"
/>
```

---

### 步骤 3：添加 CSS 动画（index.html）

**位置**：`index.html` 的 `<style>` 标签

**添加内容**：

```css
/* 连接线流动动画 */
@keyframes dash-flow {
  to {
    stroke-dashoffset: -24;
  }
}

.animate-flow {
  animation: dash-flow 1s linear infinite;
}
```

**说明**：
- `stroke-dashoffset: -24`：虚线向后移动 24px
- `1s`：1 秒完成一个循环
- `linear`：匀速运动
- `infinite`：无限循环

---

## 🎨 视觉效果

### 连接线流动

```
虚线模式：8px 实线 + 4px 空白
↓ 1 秒动画
虚线向后移动 24px（2 个完整周期）
```

**效果描述**：
- 虚线从输出端流向输入端
- 流动速度适中
- 表示数据流向

---

### 临时连接线

```
虚线模式：8px 实线 + 4px 空白
颜色：蓝色（#3b82f6）
透明度：60%
动画：流动 + 脉冲
```

**效果描述**：
- 拖拽时更明显的视觉反馈
- 蓝色表示"正在连接"
- 流动动画表示方向

---

## 📊 改动统计

| 文件 | 改动类型 | 预计改动量 |
|------|---------|-----------|
| `index.html` | 新增 CSS | +10 行 |
| `App.tsx` | 修改连接线渲染 | +4 行 |
| **总计** | | **+14 行** |

---

## ✅ 验收标准

### 功能验收

- [ ] 连接线有流动动画
- [ ] 流动方向正确（输出 → 输入）
- [ ] 动画速度适中（1 秒/循环）
- [ ] 临时连接线有动画
- [ ] 动画流畅，无卡顿

### 性能验收

- [ ] 动画不影响拖动性能
- [ ] 大量连接线时动画正常
- [ ] CPU 占用无明显增加

### 兼容性验收

- [ ] Chrome 正常
- [ ] Firefox 正常
- [ ] Safari 正常

---

## 🎯 技术细节

### 为什么使用 stroke-dasharray？

1. **原生 SVG 属性**
   - 浏览器原生支持
   - 性能最优
   - 无需额外库

2. **灵活控制**
   - 可以调整虚线长度
   - 可以调整间隔
   - 可以调整动画速度

3. **兼容性好**
   - 所有现代浏览器支持
   - 无需 polyfill

---

### 为什么使用 CSS 动画？

1. **性能优化**
   - GPU 加速
   - 不阻塞主线程
   - 比 JS 动画更流畅

2. **代码简洁**
   - 只需添加 CSS 类
   - 不需要 requestAnimationFrame
   - 易于维护

---

## 🚀 下一步

完成后，继续实施**功能 3：Node Resizer 改进**

---

**📅 创建时间**：2026-02-01  
**⏱️ 预计耗时**：1 小时  
**🎯 优先级**：2  
**⚠️ 风险**：无

