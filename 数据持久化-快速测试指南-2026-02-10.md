# 数据持久化 - 快速测试指南

**测试日期**：2026-02-10  
**预计时间**：10分钟  
**更新日期**：2026-02-10（添加键名迁移步骤）

---

## ⚠️ 重要：先执行数据迁移

如果你之前已经使用过系统并保存了数据，**必须先执行数据迁移**，否则旧数据无法恢复。

### 迁移步骤

1. 打开迁移工具：在浏览器中打开 `migrate-blob-keys.html`
2. 点击"扫描旧键名"，查看有多少旧数据
3. 如果有旧数据，点击"开始迁移"
4. 等待迁移完成，确认"已迁移"数量正确
5. 关闭迁移工具，继续测试

**如果没有旧数据**：跳过迁移步骤，直接开始测试。

---

## 🎯 测试目标

验证以下功能：
1. ✅ 画布节点图片刷新后不丢失
2. ✅ 资产库缩略图刷新后不丢失
3. ✅ 历史记录拖拽图片刷新后不丢失
4. ✅ 删除操作正确清理 IndexedDB

---

## 📋 测试步骤

### 测试1：画布节点持久化（3分钟）

```
1. 生成 3 张图片节点
   - 双击画布 → 选择"图片生成" → 输入提示词 → 生成
   - 重复 3 次

2. 刷新页面（F5）
   ✅ 检查：3 张图片是否完整显示

3. 强制刷新（Ctrl+F5）
   ✅ 检查：3 张图片是否完整显示

4. 删除 1 个节点
   ✅ 检查：节点已删除

5. 打开开发者工具（F12）
   - Application → IndexedDB → sunstudio_db → app_data
   ✅ 检查：对应的 "blob-node-xxx-image" 键是否已删除
```

**预期结果**：
- ✅ 刷新后图片完整显示
- ✅ 删除后 IndexedDB 清理

---

### 测试2：资产库持久化（3分钟）

```
1. 创建资产
   - 选中 2-3 个节点（包含图片）
   - 点击"添加到资产库"
   - 输入名称："测试资产1"
   - 选择分类："角色"
   - 确认

2. 刷新页面（F5）
   ✅ 检查：资产库是否显示"测试资产1"
   ✅ 检查：缩略图是否正常显示

3. 强制刷新（Ctrl+F5）
   ✅ 检查：资产库是否显示"测试资产1"
   ✅ 检查：缩略图是否正常显示

4. 删除资产
   - 点击资产的"更多"按钮（三个点）
   - 选择"删除"
   ✅ 检查：资产已删除

5. 打开开发者工具（F12）
   - Application → IndexedDB → sunstudio_db → app_data
   ✅ 检查：对应的 "asset-thumbnail-xxx" 键是否已删除
```

**预期结果**：
- ✅ 刷新后资产库完整显示
- ✅ 删除后 IndexedDB 清理

---

### 测试3：历史记录拖拽（2分钟）

```
1. 从历史记录拖拽图片
   - 打开历史记录面板
   - 拖拽 2-3 张图片到画布

2. 强制刷新（Ctrl+F5）
   ✅ 检查：拖拽的图片是否完整显示

3. 删除节点
   ✅ 检查：节点已删除
```

**预期结果**：
- ✅ 拖拽的图片刷新后完整显示
- ✅ 删除后 IndexedDB 清理

---

### 测试4：数据隔离（2分钟）

```
1. 创建测试数据
   - 画布：3 个图片节点
   - 资产库：2 个资产
   - 历史记录：保留几张图片

2. 删除画布节点
   ✅ 检查：资产库和历史记录不受影响

3. 删除资产库
   ✅ 检查：画布节点和历史记录不受影响

4. 清除历史记录
   ✅ 检查：画布节点和资产库不受影响
```

**预期结果**：
- ✅ 删除操作完全隔离，互不影响

---

## 🐛 如果发现问题

### 问题1：刷新后图片丢失

**检查步骤**：
1. 打开开发者工具（F12）
2. Console 标签页
3. 查找错误信息（红色文字）
4. 截图发给我

**可能原因**：
- IndexedDB 保存失败
- Blob URL 恢复失败
- persist 中间件配置问题

---

### 问题2：删除后 IndexedDB 未清理

**检查步骤**：
1. 打开开发者工具（F12）
2. Application → IndexedDB → sunstudio_db → app_data
3. 查看是否有残留的键
4. 截图发给我

**可能原因**：
- deleteFromStorage 函数未调用
- 异步删除失败

---

### 问题3：控制台有错误信息

**处理方式**：
1. 截图完整的错误信息
2. 记录操作步骤
3. 发给我分析

---

## 📊 测试结果记录

### 测试1：画布节点持久化
- [ ] F5 刷新：图片完整显示
- [ ] Ctrl+F5 刷新：图片完整显示
- [ ] 删除清理：IndexedDB 已清理

### 测试2：资产库持久化
- [ ] F5 刷新：资产库完整显示
- [ ] Ctrl+F5 刷新：资产库完整显示
- [ ] 删除清理：IndexedDB 已清理

### 测试3：历史记录拖拽
- [ ] Ctrl+F5 刷新：图片完整显示
- [ ] 删除清理：IndexedDB 已清理

### 测试4：数据隔离
- [ ] 删除画布节点：其他模块不受影响
- [ ] 删除资产库：其他模块不受影响
- [ ] 清除历史记录：其他模块不受影响

---

## ✅ 测试通过标准

**全部通过**：
- ✅ 所有刷新测试通过
- ✅ 所有删除清理测试通过
- ✅ 所有数据隔离测试通过
- ✅ 控制台无错误信息

**部分通过**：
- ⚠️ 某些场景失败
- ⚠️ 控制台有警告信息
- 需要进一步调试

**未通过**：
- ❌ 多个场景失败
- ❌ 控制台有错误信息
- 需要回滚代码

---

## 🎉 测试完成后

如果测试全部通过，恭喜！数据持久化架构升级成功完成。

如果有问题，请提供：
1. 失败的测试步骤
2. 控制台错误信息截图
3. IndexedDB 状态截图

我会立即帮你分析和修复。

---

**文档版本**：v1.0  
**创建日期**：2026-02-10  
**预计测试时间**：10分钟
