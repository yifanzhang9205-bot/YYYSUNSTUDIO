# 架构重构 - 缩放拖拽修复测试 - 2026-01-27

## 🎯 测试目标

验证 useDrag Hook 的 scale 补偿修复是否解决了"不同缩放级别下拖拽行为不一致"的问题。

---

## 🐛 Bug 描述

**症状：**
- ✅ 100% 缩放：拖拽非常丝滑
- ❌ 90% 缩放：节点几乎不动，松手后瞬间跳到目标位置
- ❌ 其他缩放：行为异常

**根本原因：**
- Node 组件使用 `left/top` 定位（世界坐标）
- Canvas 容器应用 `transform: scale()` 影响 `left/top`
- 拖拽时 `transform: translate()` 使用屏幕像素，未补偿 scale
- 松手时最终位置计算正确除以 scale，导致跳跃

---

## 🔧 修复方案

**修复位置：** `hooks/useDrag.ts` 第 73-74 行

**修复前：**
```typescript
const dx = e.clientX - mouseStartX;  // 屏幕像素
const dy = e.clientY - mouseStartY;  // 屏幕像素
```

**修复后：**
```typescript
const dx = (e.clientX - mouseStartX) / scale;  // 世界坐标
const dy = (e.clientY - mouseStartY) / scale;  // 世界坐标
```

**数学原理：**
```
屏幕位置 = (left + transform.x) × scale
要移动 screenDx 像素：transform.x = screenDx / scale
```

---

## 📋 测试计划

### 测试环境
- 浏览器：Chrome/Edge
- 开发服务器：`npm run dev` (已启动)
- 测试节点：任意节点类型

### 测试步骤

#### 第 1 步：100% 缩放测试（基准）
1. 打开应用，确认画布缩放为 100%
2. 创建一个测试节点
3. 拖拽节点，观察：
   - [ ] 节点跟随鼠标移动
   - [ ] 移动轨迹流畅
   - [ ] 松手后节点停在正确位置
   - [ ] 无跳跃现象
4. 记录：✅ 通过 / ❌ 失败

#### 第 2 步：90% 缩放测试（Bug 复现场景）
1. 将画布缩放调整到 90%
2. 拖拽同一个节点，观察：
   - [ ] 节点跟随鼠标移动（不是几乎不动）
   - [ ] 移动轨迹流畅
   - [ ] 松手后节点停在正确位置
   - [ ] 无跳跃现象
3. 记录：✅ 通过 / ❌ 失败

#### 第 3 步：75% 缩放测试
1. 将画布缩放调整到 75%
2. 拖拽节点，观察：
   - [ ] 节点跟随鼠标移动
   - [ ] 移动轨迹流畅
   - [ ] 松手后节点停在正确位置
   - [ ] 无跳跃现象
3. 记录：✅ 通过 / ❌ 失败

#### 第 4 步：50% 缩放测试（极端缩小）
1. 将画布缩放调整到 50%
2. 拖拽节点，观察：
   - [ ] 节点跟随鼠标移动
   - [ ] 移动轨迹流畅
   - [ ] 松手后节点停在正确位置
   - [ ] 无跳跃现象
3. 记录：✅ 通过 / ❌ 失败

#### 第 5 步：125% 缩放测试
1. 将画布缩放调整到 125%
2. 拖拽节点，观察：
   - [ ] 节点跟随鼠标移动
   - [ ] 移动轨迹流畅
   - [ ] 松手后节点停在正确位置
   - [ ] 无跳跃现象
3. 记录：✅ 通过 / ❌ 失败

#### 第 6 步：150% 缩放测试（极端放大）
1. 将画布缩放调整到 150%
2. 拖拽节点，观察：
   - [ ] 节点跟随鼠标移动
   - [ ] 移动轨迹流畅
   - [ ] 松手后节点停在正确位置
   - [ ] 无跳跃现象
3. 记录：✅ 通过 / ❌ 失败

#### 第 7 步：动态缩放测试
1. 在拖拽过程中，使用滚轮改变缩放
2. 观察：
   - [ ] 拖拽不会中断
   - [ ] 节点位置不会错乱
   - [ ] 松手后位置正确
3. 记录：✅ 通过 / ❌ 失败

#### 第 8 步：性能测试
1. 在各个缩放级别下拖拽节点
2. 打开浏览器控制台
3. 观察：
   - [ ] 无 React 渲染日志（拖拽中）
   - [ ] 无错误或警告
   - [ ] 拖拽流畅，无卡顿
4. 记录：✅ 通过 / ❌ 失败

#### 第 9 步：数据持久化测试
1. 在 90% 缩放下拖拽节点到新位置
2. 刷新页面
3. 观察：
   - [ ] 节点在新位置（不是旧位置）
   - [ ] 位置坐标正确
4. 记录：✅ 通过 / ❌ 失败

---

## 📊 测试结果记录表

| 缩放级别 | 跟随鼠标 | 轨迹流畅 | 位置正确 | 无跳跃 | 总体结果 |
|---------|---------|---------|---------|--------|---------|
| 100%    | ⬜      | ⬜      | ⬜      | ⬜     | ⬜      |
| 90%     | ⬜      | ⬜      | ⬜      | ⬜     | ⬜      |
| 75%     | ⬜      | ⬜      | ⬜      | ⬜     | ⬜      |
| 50%     | ⬜      | ⬜      | ⬜      | ⬜     | ⬜      |
| 125%    | ⬜      | ⬜      | ⬜      | ⬜     | ⬜      |
| 150%    | ⬜      | ⬜      | ⬜      | ⬜     | ⬜      |
| 动态    | ⬜      | ⬜      | ⬜      | ⬜     | ⬜      |
| 性能    | -       | -       | -       | -      | ⬜      |
| 持久化  | -       | -       | ⬜      | -      | ⬜      |

**图例：**
- ⬜ 待测试
- ✅ 通过
- ❌ 失败
- ⚠️ 部分通过

---

## 🎯 验收标准

**必须全部通过：**
1. ✅ 所有缩放级别下，节点都能跟随鼠标移动
2. ✅ 所有缩放级别下，拖拽轨迹流畅
3. ✅ 所有缩放级别下，松手后位置正确
4. ✅ 所有缩放级别下，无跳跃现象
5. ✅ 拖拽中无 React 渲染
6. ✅ 数据持久化正确

**如果任何一项失败：**
- 记录失败现象
- 分析失败原因
- 调整修复方案
- 重新测试

---

## 🔍 调试技巧

### 如果节点不跟随鼠标：
1. 检查 `useDrag.ts` 第 73-74 行是否正确除以 scale
2. 检查 scale 值是否正确传递到 Hook
3. 在 `handleMouseMove` 中添加 console.log：
   ```typescript
   console.log('scale:', scale, 'dx:', dx, 'dy:', dy);
   ```

### 如果节点跳跃：
1. 检查 `handleMouseUp` 中的最终位置计算（第 95-99 行）
2. 确认是否也除以了 scale
3. 检查 transform 是否正确清除（第 109 行）

### 如果性能不佳：
1. 检查是否使用了 RAF（第 67 行）
2. 检查是否直接操作 DOM（第 79 行）
3. 检查是否禁用了 transition（第 187 行）

---

## 📝 测试注意事项

1. **每次测试前清空画布**：避免节点重叠影响判断
2. **使用相同的节点**：确保测试一致性
3. **记录详细现象**：如果失败，描述具体表现
4. **截图或录屏**：方便后续分析
5. **测试多种节点类型**：确保修复对所有节点有效

---

## 🚀 下一步

测试完成后：
1. 填写测试结果记录表
2. 如果全部通过 → 继续验收第 9 步
3. 如果有失败 → 分析原因，调整修复方案

---

**测试人员：** [待填写]  
**测试时间：** [待填写]  
**测试结果：** [待填写]
