# 文字节点升级 - 阶段 8：AI 输入框组件创建完成

**日期**：2026-02-04  
**状态**：✅ 完成  
**负责人**：AI Assistant

---

## 📋 任务概述

创建通用的 AI 输入框组件，浮动在节点底部外部，用于所有节点的 AI 生成功能。

---

## 🎯 核心目标

### 1. 架构重新设计

**旧架构**：
- 红色部分（节点内部）：用户输入区域
- 用户在节点内部输入提示词

**新架构**：
- 红色部分（节点内部）：只读显示区域，显示 AI 生成的内容
- 黄色部分（AI 输入框）：浮动在节点底部外部，用户输入自然语言

### 2. AI 输入框功能

- ✅ 用户输入区域（textarea，支持 Enter 发送，Shift+Enter 换行）
- ✅ AI 模型选择下拉框（Gemini 2.0 Flash、Claude Sonnet、GPT-4 等）
- ✅ 发送按钮（↑ 图标）
- ✅ 选中节点时显示，不选中时隐藏
- ✅ 阻止事件冒泡（防止触发节点拖动）
- ✅ 自动聚焦（选中节点时）

### 3. 通用性

- ✅ 可用于所有节点类型（文字、图片、视频等）
- ✅ 通过 Props 配置（nodeId, onSend, placeholder, models 等）
- ✅ 独立组件，易于复用

---

## 📁 文件变更

### 新增文件

#### 1. `components/AIInputBar.tsx`

**职责**：通用的 AI 输入框组件

**功能**：
- 用户输入自然语言
- 选择 AI 模型
- 发送按钮
- Enter 发送，Shift+Enter 换行
- 选中节点时显示，不选中时隐藏

**Props 接口**：
```typescript
export interface AIInputBarProps {
  nodeId: string;                    // 节点 ID
  isSelected: boolean;               // 是否选中（控制显示/隐藏）
  onSend: (nodeId: string, prompt: string, model: string) => void; // 发送回调
  placeholder?: string;              // 占位符文字
  models?: Array<{ id: string; name: string }>; // AI 模型列表
  defaultModel?: string;             // 默认选中的模型
  isGenerating?: boolean;            // 是否正在生成（禁用输入）
}
```

**默认 AI 模型列表**：
- Gemini 2.0 Flash
- Gemini 1.5 Pro
- Claude Sonnet 4
- GPT-4o
- GPT-4o Mini

**UI 风格**：
- 暗色主题（bg-gray-800, border-gray-600）
- 与双击菜单风格一致
- 圆角、阴影、边框

**交互逻辑**：
- Enter 发送，Shift+Enter 换行
- 发送后清空输入框
- 发送后重新聚焦
- 阻止事件冒泡（防止触发节点拖动）
- 自动聚焦（选中节点时）

### 修改文件

#### 1. `components/Node.tsx`

**变更**：
- 导入 `AIInputBar` 组件
- 在文字节点底部渲染 `AIInputBar`
- 传递 Props：nodeId, isSelected, onSend, isGenerating

**代码**：
```tsx
{/* 🔥 AI 输入框 - 通用组件，浮动在节点底部外部 */}
{node.type === NodeType.PROMPT_INPUT && (
  <AIInputBar
    nodeId={node.id}
    isSelected={isSelected || false}
    onSend={(nodeId, prompt, model) => {
      // 更新节点的 prompt 和 model
      onUpdate(nodeId, { prompt, model });
      // 触发生成（如果需要）
      // onAction(nodeId, prompt);
    }}
    placeholder="输入你的想法，AI 会帮你实现..."
    isGenerating={isWorking}
  />
)}
```

---

## 🎨 UI 设计

### 布局

```
┌─────────────────────────────────────┐
│         节点内容（只读显示）          │
│                                     │
│   显示 AI 生成的文字/图片/视频等     │
│                                     │
└─────────────────────────────────────┘
         ↓ (margin-top: 8px)
┌─────────────────────────────────────┐
│ 💡 输入你的想法，AI 会帮你实现       │ ← 提示文字
├─────────────────────────────────────┤
│ [模型选择] [输入框............] [↑] │ ← 控制栏
└─────────────────────────────────────┘
```

### 样式

- **容器**：`bg-gray-800 border border-gray-600 rounded-lg shadow-lg`
- **提示文字**：`text-xs text-gray-400`
- **模型选择**：`bg-gray-700 border border-gray-600 text-xs text-gray-200`
- **输入框**：`bg-gray-700 border border-gray-600 text-sm text-gray-100`
- **发送按钮**：`bg-blue-600 hover:bg-blue-700 text-white`

### 位置

- `position: absolute`
- `top: 100%`（节点底部）
- `left: 0; right: 0`（与节点同宽）
- `margin-top: 8px`（与节点间距）
- `z-index: 50`（在节点上方）

---

## 🔧 技术实现

### 1. 状态管理

```typescript
const [prompt, setPrompt] = useState('');
const [selectedModel, setSelectedModel] = useState(defaultModel);
const textareaRef = useRef<HTMLTextAreaElement>(null);
```

### 2. 自动聚焦

```typescript
useEffect(() => {
  if (isSelected && textareaRef.current) {
    setTimeout(() => {
      textareaRef.current?.focus();
    }, 100);
  }
}, [isSelected]);
```

### 3. 发送处理

```typescript
const handleSend = useCallback(() => {
  const trimmedPrompt = prompt.trim();
  if (!trimmedPrompt || isGenerating) {
    return;
  }
  
  onSend(nodeId, trimmedPrompt, selectedModel);
  setPrompt('');
  textareaRef.current?.focus();
}, [nodeId, prompt, selectedModel, isGenerating, onSend]);
```

### 4. 键盘事件

```typescript
const handleKeyDown = useCallback((e: React.KeyboardEvent<HTMLTextAreaElement>) => {
  if (e.key === 'Enter' && !e.shiftKey) {
    e.preventDefault();
    handleSend();
  }
}, [handleSend]);
```

### 5. 阻止事件冒泡

```typescript
const handleMouseDown = useCallback((e: React.MouseEvent) => {
  e.stopPropagation();
}, []);
```

---

## ✅ 完成的功能

### 核心功能
- [x] 创建 `AIInputBar` 组件
- [x] 定义 Props 接口
- [x] 实现 UI 布局（提示文字 + 控制栏）
- [x] 实现交互逻辑（Enter 发送，Shift+Enter 换行）
- [x] 实现模型选择
- [x] 实现发送回调
- [x] 实现显示/隐藏逻辑（基于节点选中状态）
- [x] 实现自动聚焦
- [x] 实现阻止事件冒泡
- [x] 集成到 Node.tsx
- [x] TypeScript 编译通过

### UI/UX
- [x] 暗色主题
- [x] 圆角、阴影、边框
- [x] 悬停效果
- [x] 禁用状态
- [x] 占位符文字
- [x] 响应式布局

### 通用性
- [x] 可用于所有节点类型
- [x] 通过 Props 配置
- [x] 独立组件，易于复用

---

## 📊 代码统计

- **新增文件**：1 个（`AIInputBar.tsx`）
- **修改文件**：1 个（`Node.tsx`）
- **新增代码**：约 200 行
- **TypeScript 编译**：✅ 通过

---

## 🚀 下一步计划

### 阶段 9：修改文字节点内部为只读显示区域

**目标**：
- 移除文字节点内部的输入区域
- 改为只读显示区域
- 显示 AI 生成的内容（文字、图片、视频等）

**文件**：
- `components/TextNode.tsx`
- `components/TextNode/TextNodeInitial.tsx`
- `components/TextNode/TextNodeManual.tsx`
- `components/TextNode/TextNodeReverse.tsx`
- `components/TextNode/TextNodeTextToImage.tsx`
- `components/TextNode/TextNodePromptGenerator.tsx`

**任务**：
1. 移除所有输入框（textarea、input）
2. 改为只读显示区域（div、p、pre 等）
3. 显示 AI 生成的内容
4. 保留模式切换按钮（初始状态）
5. 保留错误提示
6. 保留加载状态

### 阶段 10：测试和优化

**目标**：
- 测试 AI 输入框功能
- 测试文字节点显示
- 测试模型选择
- 测试发送回调
- 优化 UI/UX
- 修复 Bug

---

## 📝 备注

### 设计决策

1. **为什么浮动在节点底部外部？**
   - 不占用节点内部空间
   - 不影响节点内容显示
   - 易于扩展到其他节点类型

2. **为什么选中时显示，不选中时隐藏？**
   - 避免画布混乱
   - 聚焦当前操作的节点
   - 符合用户直觉

3. **为什么使用暗色主题？**
   - 与双击菜单风格一致
   - 突出显示，易于识别
   - 现代化设计

4. **为什么不需要生成倍数和质量参数？**
   - 用户需求：只需要选择模型和发送
   - 简化 UI，降低学习成本
   - 参数可以在节点内部设置

### 技术亮点

1. **React.memo 优化**：避免不必要的重新渲染
2. **useCallback 优化**：避免函数重新创建
3. **useRef 优化**：避免状态更新导致的重新渲染
4. **事件冒泡控制**：防止触发节点拖动
5. **自动聚焦**：提升用户体验
6. **键盘快捷键**：Enter 发送，Shift+Enter 换行

### 可扩展性

1. **通用组件**：可用于所有节点类型
2. **Props 配置**：易于定制
3. **模型列表**：可动态配置
4. **回调函数**：可自定义发送逻辑
5. **样式定制**：可通过 className 和 style 定制

---

## 🎉 总结

AI 输入框组件已经创建完成，并成功集成到文字节点中。这是一个通用组件，未来可以扩展到所有节点类型（图片、视频等）。

**核心变化**：
- 从"节点内输入"改为"外部 AI 输入框 + 节点内显示结果"
- 节点内部只显示生成结果，不再是输入区域
- AI 输入框浮动在节点底部外部，选中时显示

**下一步**：
- 修改文字节点内部为只读显示区域
- 测试和优化

---

**文档创建时间**：2026-02-04  
**最后更新时间**：2026-02-04
