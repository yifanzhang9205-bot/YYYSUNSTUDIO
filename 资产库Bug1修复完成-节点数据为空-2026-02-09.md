# 资产库 Bug 1 修复完成 - 节点数据为空

> 日期：2026-02-09  
> 状态：✅ 已修复

---

## 🎯 问题描述

**现象：**
- 用户框选节点 → 点击"编组" → 点击"添加到资产库" → 填写信息 → 确认
- 资产库中显示了资产卡片
- 但是拖拽/点击资产时，画布上没有任何节点出现
- 日志显示：`nodesCount: 0, connectionsCount: 0`

**根本原因：**
- `handleConfirmCreateAsset` 依赖 `selectedNodeIds` 获取节点数据
- 但在点击"编组"时，`clearSelection()` 已经清空了 `selectedNodeIds`
- 当用户点击"确认"时，`selectedNodeIds` 是空数组
- 所以创建的资产没有任何节点数据

---

## ✅ 修复方案

### 核心思路

**在打开对话框时保存节点数据，而不是在确认时获取。**

```
✅ 正确流程：
1. 用户框选节点 → selectedNodeIds = ['node-1', 'node-2']
2. 用户点击"编组" → clearSelection() → selectedNodeIds = []
3. 用户点击"添加到资产库"
   → 立即获取组内节点数据
   → 保存到 assetDataToCreate state
   → 打开对话框
4. 用户填写信息，点击"确认"
   → 使用 assetDataToCreate（而不是 selectedNodeIds）
   → 创建资产成功！
```

---

## 📝 修改内容

### 修改 1：`handleAddGroupToAssetLibrary`（第 718 行）

**修改前：**
```typescript
const handleAddGroupToAssetLibrary = useCallback(() => {
  if (!selectedGroupId) return;
  
  console.log('[App] 添加到资产库', { selectedGroupId });
  
  // 获取组内的所有节点
  const groupNodes = getGroupNodes(selectedGroupId);
  
  // 获取组内节点之间的连接
  const groupConnections = connections.filter(conn => 
    groupNodes.some(n => n.id === conn.from) && groupNodes.some(n => n.id === conn.to)
  );
  
  // 显示创建资产对话框
  setShowCreateAssetDialog(true);  // ❌ 只打开对话框，没有保存数据
  
  console.log('[App] 准备创建资产', { nodesCount: groupNodes.length, connectionsCount: groupConnections.length });
}, [selectedGroupId, getGroupNodes, connections]);
```

**修改后：**
```typescript
const handleAddGroupToAssetLibrary = useCallback(() => {
  if (!selectedGroupId) return;
  
  console.log('[App] 添加到资产库', { selectedGroupId });
  
  // ✅ 在打开对话框前，保存节点数据（修复 Bug：节点数据为空）
  const groupNodes = getGroupNodes(selectedGroupId);
  const groupConnections = connections.filter(conn => 
    groupNodes.some(n => n.id === conn.from) && groupNodes.some(n => n.id === conn.to)
  );
  
  // ✅ 保存到 state，供确认时使用
  setAssetDataToCreate({ nodes: groupNodes, connections: groupConnections });
  
  // 显示创建资产对话框
  setShowCreateAssetDialog(true);
  
  console.log('[App] 准备创建资产', { nodesCount: groupNodes.length, connectionsCount: groupConnections.length });
}, [selectedGroupId, getGroupNodes, connections]);
```

**关键改动：**
- ✅ 添加 `setAssetDataToCreate({ nodes: groupNodes, connections: groupConnections })`
- ✅ 在打开对话框前保存节点数据

---

### 修改 2：`handleConfirmCreateAsset`（第 610 行）

**修改前：**
```typescript
const handleConfirmCreateAsset = useCallback((name: string, category: any) => {
  // ❌ 使用 selectedNodeIds，但它已经被清空了
  const selectedNodes = Array.from(nodes.values()).filter(n => selectedNodeIds.includes(n.id));
  
  // ❌ 结果：selectedNodes = []
  const selectedConnections = connections.filter(conn => 
    selectedNodeIds.includes(conn.from) && selectedNodeIds.includes(conn.to)
  );
  
  // ❌ 创建的资产没有任何节点数据
  createAsset(name, category, selectedNodes, selectedConnections);
  
  // 关闭对话框
  setShowCreateAssetDialog(false);
  
  // 取消选中
  clearSelection();
  
  console.log('[资产库] 资产创建成功', { name, category, nodesCount: selectedNodes.length });
  // 输出：nodesCount: 0  ❌
}, [selectedNodeIds, nodes, connections, createAsset, clearSelection]);
```

**修改后：**
```typescript
const handleConfirmCreateAsset = useCallback((name: string, category: any) => {
  // ✅ 使用保存的数据，而不是 selectedNodeIds（修复 Bug：节点数据为空）
  if (!assetDataToCreate) {
    console.error('[资产库] 没有要创建的资产数据');
    return;
  }
  
  // ✅ 使用保存的节点数据
  createAsset(name, category, assetDataToCreate.nodes, assetDataToCreate.connections);
  
  // 清理
  setAssetDataToCreate(null);
  setShowCreateAssetDialog(false);
  clearSelection();
  
  console.log('[资产库] 资产创建成功', { 
    name, 
    category, 
    nodesCount: assetDataToCreate.nodes.length,
    connectionsCount: assetDataToCreate.connections.length,
  });
  // 输出：nodesCount: 3  ✅
}, [assetDataToCreate, createAsset, clearSelection]);
```

**关键改动：**
- ✅ 使用 `assetDataToCreate` 而不是 `selectedNodeIds`
- ✅ 添加空值检查（`if (!assetDataToCreate)`）
- ✅ 使用后清理 `setAssetDataToCreate(null)`
- ✅ 日志显示正确的节点数量

---

## 🧪 验证步骤

### 测试场景 1：正常流程

1. **框选节点**
   - 在画布上框选 3 个节点
   - 验证：节点被选中（青色边框）

2. **点击"编组"**
   - 点击工具栏的"编组"按钮
   - 验证：创建临时组（青色边框）
   - 验证：节点选中状态被清除

3. **点击"添加到资产库"**
   - 点击工具栏的"添加到资产库"按钮
   - 验证：打开创建资产对话框
   - **检查日志**：`[App] 准备创建资产 { nodesCount: 3, connectionsCount: 2 }`

4. **填写资产信息**
   - 输入资产名称："测试资产"
   - 选择分类："场景"
   - 点击"确认"

5. **验证创建成功**
   - **检查日志**：`[资产库] 资产创建成功 { name: '测试资产', category: 'scene', nodesCount: 3, connectionsCount: 2 }`
   - 验证：资产库中显示新资产
   - 验证：缩略图显示正常

6. **验证使用资产**
   - 拖拽资产到画布
   - **验证**：画布上出现 3 个节点 ✅
   - **验证**：节点之间的连接正常 ✅

---

### 测试场景 2：边界情况

#### 场景 2.1：空组
1. 创建一个空组（没有节点）
2. 点击"添加到资产库"
3. **预期**：日志显示 `nodesCount: 0`
4. **预期**：仍然可以创建资产（空资产）

#### 场景 2.2：取消对话框
1. 框选节点 → 编组 → 添加到资产库
2. 打开对话框后，点击"取消"
3. **预期**：对话框关闭
4. **预期**：`assetDataToCreate` 仍然保留（下次打开对话框时会被覆盖）

#### 场景 2.3：多次打开对话框
1. 框选节点 A → 编组 → 添加到资产库 → 取消
2. 框选节点 B → 编组 → 添加到资产库 → 确认
3. **预期**：创建的资产包含节点 B（不是节点 A）

---

## 📊 修复前后对比

### 修复前（❌ 错误）

```
用户操作：框选 3 个节点 → 编组 → 添加到资产库 → 确认

日志输出：
[App] 准备创建资产 { nodesCount: 3, connectionsCount: 2 }
[资产库] 资产创建成功 { name: '测试', category: 'scene', nodesCount: 0 }  ❌
[useAssetLibrary] 创建资产 { id: 'asset-xxx', name: '测试', nodesCount: 0 }  ❌

结果：
- 资产库中显示资产卡片 ✅
- 拖拽资产到画布 → 没有任何节点出现 ❌
- 日志显示 nodesCount: 0 ❌
```

### 修复后（✅ 正确）

```
用户操作：框选 3 个节点 → 编组 → 添加到资产库 → 确认

日志输出：
[App] 准备创建资产 { nodesCount: 3, connectionsCount: 2 }
[资产库] 资产创建成功 { name: '测试', category: 'scene', nodesCount: 3 }  ✅
[useAssetLibrary] 创建资产 { id: 'asset-xxx', name: '测试', nodesCount: 3 }  ✅

结果：
- 资产库中显示资产卡片 ✅
- 拖拽资产到画布 → 出现 3 个节点 ✅
- 日志显示 nodesCount: 3 ✅
```

---

## 🎯 修复总结

### 问题本质
- **时序逻辑错误**：在错误的时机获取数据
- **不是** Blob URL 的问题
- **不是**性能问题
- **不是**内存泄漏

### 解决方案
- **在打开对话框时保存数据**（而不是在确认时获取）
- 使用 `assetDataToCreate` state 保存节点数据
- 确认时使用保存的数据（而不是 `selectedNodeIds`）

### 修改范围
- 文件：`App.tsx`
- 修改行数：2 个函数，共约 20 行
- 影响范围：只影响资产库创建功能，不影响其他功能

### 风险评估
- **风险等级**：低
- **原因**：
  1. 只修改了资产库相关的两个函数
  2. 没有修改数据结构
  3. 没有修改其他功能
  4. 添加了空值检查（更安全）

---

## 🚀 下一步

**问题 1 已修复！✅**

现在可以继续修复**问题 2（刷新后图片变成图标）**。

问题 2 的修复需要：
1. 添加 Blob URL → Base64 转换函数
2. 修改 `deepCopyNodes` 为异步函数
3. 修改 `generateThumbnail` 为异步函数
4. 修改 `createAsset` 为异步函数
5. 更新调用方式（处理异步）

---

**修复完成！现在资产库可以正确保存节点数据了。** 🎉
