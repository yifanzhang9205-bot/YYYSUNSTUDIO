# 3D相机自动输出节点 - 需求文档

**日期**: 2026-01-30  
**目标**: 3D 相机节点自动创建九宫格节点，支持持续生成

---

## 📋 核心需求

### **功能概述**
3D 相机节点作为纯粹的"生成器"，每次生成完成后自动创建九宫格节点（或输出到已连接的节点），不保存历史记录。

---

## 🎯 详细规格

### **问题 1：节点位置（A+B 混合方案）**

#### **规则**
1. **优先位置**：3D 相机节点右侧 + 300px
2. **智能检测**：检测右侧是否有足够空间（200×200 区域）
3. **自动寻找**：如果右侧没有空间，自动寻找空白区域
4. **连线可见**：无论放在哪里，都有连线指示关系

#### **实施细节**
```typescript
// 位置计算逻辑
const calculateNewNodePosition = (cameraNodePos, existingNodes) => {
  // 1. 优先尝试右侧
  const preferredPos = {
    x: cameraNodePos.x + 300,
    y: cameraNodePos.y + (autoCreatedCount * 150),  // 每次向下偏移 150px
  };
  
  // 2. 检测是否有空间（200×200 区域）
  const hasSpace = !existingNodes.some(node => 
    isOverlapping(node.position, preferredPos, 200, 200)
  );
  
  if (hasSpace) {
    return preferredPos;
  }
  
  // 3. 自动寻找空白区域
  return findEmptySpace(cameraNodePos, existingNodes, 200, 200);
};
```

---

### **问题 2：自动创建规则**

#### **规则 1：已连接九宫格节点**
```
条件：3D 相机的输出端口已连接到九宫格节点
行为：不创建新节点，直接更新已连接的九宫格节点
结果：只有 1 个九宫格节点（被更新）
```

#### **规则 2：已连接其他节点**
```
条件：3D 相机的输出端口已连接到非九宫格节点（如自定义图片节点）
行为：
  1. 输出到已连接的节点
  2. 同时创建新的九宫格节点
  3. 新的九宫格节点也自动连接到 3D 相机
结果：2 个节点（已连接的节点 + 新的九宫格节点）
```

#### **规则 3：未连接任何节点**
```
条件：3D 相机的输出端口没有连接
行为：自动创建新的九宫格节点 + 自动连接
结果：1 个新的九宫格节点
```

#### **实施细节**
```typescript
const handleGenerateComplete = (gridImages: string[]) => {
  // 获取输出端口的连接
  const outputConnections = getConnectionsFromNode(cameraNodeId, 'output');
  
  if (outputConnections.length === 0) {
    // 规则 3：未连接 → 创建新九宫格节点
    createAndConnectGridNode(gridImages);
  } else {
    // 检查连接的节点类型
    const connectedNodes = outputConnections.map(conn => 
      getNode(conn.target)
    );
    
    const hasGridNode = connectedNodes.some(node => 
      node.type === 'gridSplitter'
    );
    
    if (hasGridNode) {
      // 规则 1：已连接九宫格 → 只更新，不创建
      connectedNodes.forEach(node => {
        if (node.type === 'gridSplitter') {
          updateNode(node.id, { inputImage: gridImages });
        } else {
          updateNode(node.id, { gridImages });
        }
      });
    } else {
      // 规则 2：已连接其他节点 → 输出 + 创建新九宫格
      // 1. 输出到已连接的节点
      connectedNodes.forEach(node => {
        updateNode(node.id, { gridImages });
      });
      
      // 2. 创建新的九宫格节点
      createAndConnectGridNode(gridImages);
    }
  }
};
```

---

### **问题 3：持续生成**

#### **场景分析**
```
场景 1：第一次生成
- 用户点击"生成" → 创建九宫格节点 1 → 自动连接

场景 2：第二次生成（已有九宫格节点）
- 用户调整角度 → 点击"生成"
- 检测到已连接九宫格节点 1
- 选项 A：更新九宫格节点 1（覆盖）
- 选项 B：创建九宫格节点 2（新增）

场景 3：第三次生成
- 用户再次调整角度 → 点击"生成"
- 检测到已连接九宫格节点 1 和 2
- 选项 A：更新最新的节点（节点 2）
- 选项 B：创建九宫格节点 3（新增）
```

#### **推荐方案：始终创建新节点**
```
理由：
1. ✅ 用户可以保留多个版本
2. ✅ 用户可以对比不同角度
3. ✅ 用户可以删除不需要的节点
4. ✅ 符合"生成器"的定位（每次生成都是新的输出）

实施：
- 每次生成都创建新的九宫格节点
- 自动连接到 3D 相机的输出端口
- 按序号命名："九宫格 1"、"九宫格 2"、"九宫格 3"...
```

#### **实施细节**
```typescript
// 全局计数器（记录已创建的九宫格节点数量）
let gridNodeCounter = 0;

const createAndConnectGridNode = (gridImages: string[]) => {
  gridNodeCounter++;
  
  const newNodeId = createNode({
    type: 'gridSplitter',
    position: calculateNewNodePosition(cameraNodePos, existingNodes),
    data: {
      inputImage: gridImages,  // 21:9 九宫格图片
      croppedImages: [],
      selectedIndex: -1,
    },
    label: `九宫格 ${gridNodeCounter}`,  // 按序号命名
  });
  
  // 自动连接
  createConnection({
    source: cameraNodeId,
    sourceHandle: 'output',
    target: newNodeId,
    targetHandle: 'input',
  });
  
  return newNodeId;
};
```

---

### **问题 4：防止重复生成**

#### **需求**
生成中（`isWorking = true`）时：
1. ❌ 禁用"生成"按钮
2. ❌ 禁止拖动 3D 相机视角
3. ❌ 禁止调整角度滑块
4. ❌ 禁止滚轮缩放

#### **UI 状态**
```
生成中：
- 按钮文字："生成中..."
- 按钮图标：加载动画（旋转圆圈）
- 按钮状态：disabled
- 3D 视口：添加遮罩层（半透明黑色）
- 鼠标样式：not-allowed
- 滑块：disabled
```

#### **实施细节**
```typescript
// 在 MultiAngleCameraNode 组件中
const MultiAngleCameraNode = ({ isWorking, ... }) => {
  return (
    <div className="relative">
      {/* 3D 视口 */}
      <div
        ref={containerRef}
        className={`
          ${isDragging ? 'cursor-grabbing' : 'cursor-grab'}
          ${isWorking ? 'pointer-events-none' : ''}  // ✅ 禁止交互
        `}
        onMouseDown={isWorking ? undefined : handleViewportMouseDown}  // ✅ 禁止拖动
        onWheel={isWorking ? undefined : handleWheel}  // ✅ 禁止滚轮
      />
      
      {/* 生成中遮罩 */}
      {isWorking && (
        <div className="absolute inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-10">
          <div className="flex flex-col items-center gap-3">
            <div className="w-12 h-12 border-4 border-white/20 border-t-white rounded-full animate-spin" />
            <span className="text-[13px] text-white/80 font-medium">生成中...</span>
          </div>
        </div>
      )}
      
      {/* 控制面板 */}
      <div className="p-5 space-y-5">
        {/* 滑块 */}
        <input
          type="range"
          disabled={isWorking}  // ✅ 禁用滑块
          className={isWorking ? 'opacity-50 cursor-not-allowed' : ''}
        />
        
        {/* 生成按钮 */}
        <button
          onClick={onGenerate}
          disabled={isWorking}  // ✅ 禁用按钮
          className="..."
        >
          {isWorking ? (
            <>
              <div className="w-4 h-4 border-2 border-black/20 border-t-black rounded-full animate-spin" />
              <span>生成中...</span>
            </>
          ) : (
            <>
              <Play size={14} fill="black" />
              <span>生成</span>
            </>
          )}
        </button>
      </div>
    </div>
  );
};
```

---

## 🎯 完整工作流程

### **流程 1：第一次生成**
```
1. 用户创建 3D 相机节点
2. 连接输入图片（角色 A）
3. 调整角度（水平 45°，垂直 0°，距离 5）
4. 点击"生成"
   ↓
5. isWorking = true
   - 按钮禁用
   - 3D 视口禁止交互
   - 显示"生成中..."遮罩
   ↓
6. 调用 API 生成九宫格图片
   ↓
7. 生成完成
   - isWorking = false
   - 移除遮罩
   - 按钮恢复
   ↓
8. 检测输出端口：未连接
   ↓
9. 自动创建九宫格节点 1
   - 位置：3D 相机右侧 + 300px
   - 名称："九宫格 1"
   - 数据：九宫格图片
   ↓
10. 自动连接：3D 相机 → 九宫格节点 1
```

### **流程 2：第二次生成（持续生成）**
```
1. 用户调整角度（水平 90°，垂直 0°，距离 5）
2. 点击"生成"
   ↓
3. isWorking = true（禁止交互）
   ↓
4. 生成完成
   ↓
5. 检测输出端口：已连接九宫格节点 1
   ↓
6. 创建九宫格节点 2
   - 位置：3D 相机右侧 + 300px，向下偏移 150px
   - 名称："九宫格 2"
   - 数据：新的九宫格图片
   ↓
7. 自动连接：3D 相机 → 九宫格节点 2
   ↓
8. 现在有 2 个九宫格节点（节点 1 和节点 2）
```

### **流程 3：连接到自定义节点**
```
1. 用户创建自定义图片节点
2. 手动连接：3D 相机 → 自定义节点
3. 点击"生成"
   ↓
4. 生成完成
   ↓
5. 检测输出端口：已连接自定义节点（非九宫格）
   ↓
6. 输出到自定义节点（更新数据）
   ↓
7. 同时创建九宫格节点 3
   - 位置：自动寻找空白区域
   - 名称："九宫格 3"
   ↓
8. 自动连接：3D 相机 → 九宫格节点 3
   ↓
9. 现在有 3 个输出：节点 1、节点 2、自定义节点、节点 3
```

---

## ✅ 验收标准

### **功能测试**
- [ ] 第一次生成：自动创建九宫格节点 1 + 自动连接
- [ ] 第二次生成：自动创建九宫格节点 2 + 自动连接
- [ ] 第三次生成：自动创建九宫格节点 3 + 自动连接
- [ ] 连接自定义节点：输出到自定义节点 + 创建新九宫格节点
- [ ] 连接九宫格节点：只更新，不创建新节点
- [ ] 生成中：按钮禁用 + 3D 视口禁止交互
- [ ] 节点位置：优先右侧，智能避让
- [ ] 节点命名：按序号命名（九宫格 1, 2, 3...）

### **边界测试**
- [ ] 右侧没有空间：自动寻找空白区域
- [ ] 画布已满：仍然能创建节点（可能重叠，但有连线）
- [ ] 快速连续点击生成：只执行一次（防止重复）
- [ ] 生成中切换输入图片：等待当前生成完成
- [ ] 生成中删除节点：不影响生成流程

### **性能测试**
- [ ] 创建 10 个九宫格节点：不卡顿
- [ ] 生成中拖动画布：流畅
- [ ] 生成中缩放画布：流畅

---

## 🚀 实施计划

### **阶段 1：核心功能（1.5 小时）**
1. 修改生成完成的回调逻辑
2. 实现自动创建九宫格节点
3. 实现自动连接
4. 实现按序号命名

### **阶段 2：智能位置（1 小时）**
1. 实现优先右侧逻辑
2. 实现空间检测
3. 实现自动寻找空白区域
4. 实现向下偏移

### **阶段 3：防止重复生成（30 分钟）**
1. 添加生成中遮罩
2. 禁用按钮和交互
3. 添加加载动画

### **阶段 4：测试和优化（30 分钟）**
1. 测试各种场景
2. 优化节点位置算法
3. 优化动画效果

**总计**：约 3.5 小时

---

## 📝 补充说明

### **你提到的所有要点**
✅ **问题 1**：A+B 混合（优先右侧 + 智能检测）  
✅ **问题 2 - 规则 1**：连接非九宫格 → 输出 + 创建新九宫格  
✅ **问题 2 - 规则 2**：未连接 → 自动创建 + 连接  
✅ **问题 3**：持续生成 → 始终创建新节点  
✅ **防止重复生成**：生成中禁用按钮 + 禁止交互  
✅ **问题 3 - 命名**：按序号命名（九宫格 1, 2, 3...）

### **没有遗漏的地方！**
你的需求非常完整，考虑到了：
- ✅ 节点位置（智能排列）
- ✅ 自动创建规则（3 种情况）
- ✅ 持续生成（始终创建新节点）
- ✅ 防止重复生成（禁用交互）
- ✅ 节点命名（按序号）
- ✅ 连线可见性（无论位置）

---

## 🎯 下一步

需求文档已完成！现在可以开始实施了。

要不要我现在就开始编码？预计 3.5 小时完成。

或者你还有其他想法要补充？🚀
