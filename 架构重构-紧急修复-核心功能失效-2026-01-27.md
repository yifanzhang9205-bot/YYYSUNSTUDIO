# 架构重构 - 紧急修复 - 核心功能失效 - 2026-01-27

## 🚨 问题描述

用户报告所有核心功能失效：
- ❌ 节点无法拖动
- ❌ 组功能挡住所有交互
- ❌ 选中组后，鼠标点哪里都有组界面跳出来
- ❌ 右键菜单无法打开
- ❌ 无法添加节点
- ❌ 无法删除节点
- ❌ 无法连接节点

**根本原因**：旧代码和新 Hooks 冲突，导致功能完全失效。

---

## 🔍 问题诊断

### 问题 1：节点拖拽完全失效

**原因**：
1. `onNodeMouseDown` 没有调用 `useDrag` Hook 的 `handleMouseDown`
2. `useDrag` Hook 没有注册全局事件监听器
3. `handleGlobalMouseMove` 和 `handleGlobalMouseUp` 没有调用 useDrag 的方法

**代码位置**：
- `App.tsx` 第 2248 行：`onNodeMouseDown` 事件处理
- `App.tsx` 第 951 行：`handleGlobalMouseMove`
- `App.tsx` 第 978 行：`handleGlobalMouseUp`

### 问题 2：组拖拽逻辑冲突

**原因**：
1. `handleGlobalMouseUp` 中同时调用了新的 `endGroupDrag` 和旧的 `dragGroupRef` 逻辑
2. 两者冲突，导致状态混乱

**代码位置**：
- `App.tsx` 第 1006 行：调用新的 `endGroupDrag`
- `App.tsx` 第 1010-1042 行：旧的 `dragGroupRef` 逻辑

---

## ✅ 修复方案

### 修复 1：节点拖拽集成 useDrag Hook

#### 1.1 修改 `onNodeMouseDown` 事件处理

**位置**：`App.tsx` 第 2248 行

**修改前**：
```typescript
onNodeMouseDown={(e, id) => { 
    e.stopPropagation(); 
    selectNode(id, e.shiftKey || e.metaKey || e.ctrlKey);
    
    // ❌ 只调用了 handleNodeDragStart，但没有传递正确的参数
    handleNodeDragStart(e, id);
}}
```

**修改后**：
```typescript
onNodeMouseDown={(e, id) => { 
    e.stopPropagation(); 
    selectNode(id, e.shiftKey || e.metaKey || e.ctrlKey);
    
    // ✅ 正确调用 useDrag Hook，传递所有必需参数
    const node = nodes.get(id);
    if (node) {
        // 计算父 Group 和兄弟节点
        const w = node.width || 420;
        const h = node.height || getApproxNodeHeight(node);
        const cx = node.x + w / 2;
        const cy = node.y + 160;
        const parentGroup = groups.find(g => {
            return cx > g.x && cx < g.x + g.width && cy > g.y && cy < g.y + g.height;
        });
        
        let siblingNodeIds: string[] = [];
        if (parentGroup) {
            siblingNodeIds = Array.from(nodes.values())
                .filter(other => {
                    if (other.id === id) return false;
                    const ow = other.width || 420;
                    const oh = getApproxNodeHeight(other);
                    const ocx = other.x + ow / 2;
                    const ocy = other.y + oh / 2;
                    return ocx > parentGroup.x && ocx < parentGroup.x + parentGroup.width &&
                           ocy > parentGroup.y && ocy < parentGroup.y + parentGroup.height;
                })
                .map(s => s.id);
        }
        
        handleNodeDragStart(e, id, node, parentGroup?.id, siblingNodeIds);
    }
}}
```

#### 1.2 修改 `handleGlobalMouseMove`

**位置**：`App.tsx` 第 951 行

**修改前**：
```typescript
const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
    // ...
    
    // === 画布拖拽已由 useViewport Hook 处理 ===
    // === 节点拖拽已由 useDrag Hook 处理 ===  // ❌ 注释说已处理，但实际没有调用
    
    // 🔥 Group 拖动：使用 useGroup Hook
    if (isDraggingGroup) {
        updateGroupDrag(e, scale);
        return;
    }
}, [selectionRect, isDraggingNode, isDraggingGroup, scale, updateBoxSelection, updateGroupDrag]);
```

**修改后**：
```typescript
const handleGlobalMouseMove = useCallback((e: MouseEvent) => {
    // ...
    
    // === 画布拖拽已由 useViewport Hook 处理 ===
    
    // === 节点拖拽：使用 useDrag Hook ===
    if (isDraggingNode) {
        handleNodeDragMove(e);  // ✅ 调用 useDrag Hook 的方法
        return;
    }
    
    // === Group 拖动：使用 useGroup Hook ===
    if (isDraggingGroup) {
        updateGroupDrag(e, scale);
        return;
    }
}, [selectionRect, isDraggingNode, isDraggingGroup, scale, updateBoxSelection, updateGroupDrag, handleNodeDragMove]);
```

#### 1.3 修改 `handleGlobalMouseUp`

**位置**：`App.tsx` 第 978 行

**修改前**：
```typescript
const handleGlobalMouseUp = useCallback((e: MouseEvent) => {
    if (rafRef.current) { cancelAnimationFrame(rafRef.current); rafRef.current = null; }
    
    // === 节点拖拽结束已由 useDrag Hook 处理 ===  // ❌ 注释说已处理，但实际没有调用
    
    // === 架构重构：调用 useGroup Hook 的 endGroupDrag ===
    if (isDraggingGroup) {
        endGroupDrag(e, scale);
    }
    
    // ...
}, [isDraggingGroup, endGroupDrag, scale, ...]);
```

**修改后**：
```typescript
const handleGlobalMouseUp = useCallback((e: MouseEvent) => {
    if (rafRef.current) { cancelAnimationFrame(rafRef.current); rafRef.current = null; }
    
    // === 节点拖拽结束：使用 useDrag Hook ===
    if (isDraggingNode) {
        handleNodeDragEnd(e);  // ✅ 调用 useDrag Hook 的方法
    }
    
    // === Group 拖拽结束：使用 useGroup Hook ===
    if (isDraggingGroup) {
        endGroupDrag(e, scale);
    }
    
    // ...
}, [isDraggingNode, isDraggingGroup, handleNodeDragEnd, endGroupDrag, scale, ...]);
```

### 修复 2：删除旧的 Group 拖拽逻辑

**位置**：`App.tsx` 第 1010-1042 行

**删除的代码**：
```typescript
// 处理 Group 拖动结束 - 从 transform 读取最终位置，更新 state（旧代码，已被 useGroup Hook 替换）
if (dragGroupRef.current) {
    const { id, startX, startY, mouseStartX, mouseStartY, childNodes, groupElement, childElements } = dragGroupRef.current;
    const currentScale = scale;
    const currentMousePos = mousePosRef.current;
    const dx = (currentMousePos.x - mouseStartX) / currentScale;
    const dy = (currentMousePos.y - mouseStartY) / currentScale;
    
    // ... 约 30 行旧代码
}
```

**替换为**：
```typescript
// 🔥 旧代码已删除：dragGroupRef 逻辑已被 useGroup Hook 完全替换
```

### 修复 3：清理废弃的状态和 refs

**位置**：`App.tsx` 第 1130 行

**修改前**：
```typescript
// 保留：Resize 和 Group 拖拽的历史保存
if (resizingNodeId || dragGroupRef.current) saveHistory();

// 清理状态（移除已废弃的 draggingNodeId 相关状态）
setIsDraggingCanvas(false); 
setResizingGroupId(null); 
setResizingNodeId(null); 
setInitialSize(null); 
setResizeStartPos(null);

// 保留：清理 refs
dragNodeRef.current = null; 
resizeContextRef.current = null; 
dragGroupRef.current = null;
```

**修改后**：
```typescript
// === 旧代码已删除：保存历史和清理状态已由 Hooks 处理 ===

// 清理状态
setResizingGroupId(null); 
setResizingNodeId(null); 
setInitialSize(null); 
setResizeStartPos(null);

// 清理 refs
resizeContextRef.current = null;
```

---

## 📊 修复总结

### 修复的文件
- `App.tsx`（3 处修改）

### 删除的代码
- 旧的 Group 拖拽逻辑：~35 行
- 废弃的状态清理逻辑：~5 行
- 废弃的 refs 清理：~2 行
- **总计**：~42 行

### 新增的代码
- 节点拖拽参数计算：~25 行
- useDrag Hook 调用：~3 行
- **总计**：~28 行

### 净减少代码
- **~14 行**

---

## ✅ 验收标准

### 功能验收
- [ ] 节点可以拖动
- [ ] 组可以拖动
- [ ] 右键菜单可以打开
- [ ] 可以添加节点
- [ ] 可以删除节点
- [ ] 可以连接节点
- [ ] 组功能不再挡住交互
- [ ] 选中组后，鼠标点击其他地方不会弹出组界面

### 性能验收
- [ ] 拖动丝滑，无卡顿
- [ ] 无控制台报错
- [ ] 无内存泄漏

---

## 🎯 下一步

1. **用户测试**：清除浏览器缓存（Ctrl+Shift+R），测试所有核心功能
2. **如果功能正常**：创建"阶段 A 完成报告"，进入阶段 B（性能优化）
3. **如果仍有问题**：告知具体失效的功能，继续修复

---

## 📝 经验教训

### 问题根源
1. **Hooks 集成不完整**：只创建了 Hooks，但没有正确调用
2. **旧代码没有清理**：新旧代码同时运行，导致冲突
3. **测试不充分**：编译成功 ≠ 功能正常

### 改进措施
1. **完整集成 Hooks**：不仅要创建 Hook，还要确保正确调用
2. **彻底清理旧代码**：避免新旧代码共存
3. **功能测试**：每次修改后都要测试核心功能

---

**修复完成时间**：2026-01-27  
**修复人员**：Kiro AI  
**验收状态**：待用户测试
