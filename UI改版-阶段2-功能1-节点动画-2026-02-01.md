# UI 改版 - 阶段 2 - 功能 1：节点动画

> 📅 实施时间：2026-02-01  
> ⏱️ 预计耗时：1 小时  
> 🎯 目标：添加节点淡入淡出和位置过渡动画

---

## 🎯 功能目标

### 动画效果

1. **节点添加动画**
   - 新节点淡入（opacity: 0 → 1）
   - 新节点缩放（scale: 0.95 → 1）
   - 动画时长：200ms
   - 缓动函数：ease-out

2. **节点删除动画**
   - 节点淡出（opacity: 1 → 0）
   - 节点缩放（scale: 1 → 0.95）
   - 动画时长：150ms
   - 缓动函数：ease-in

3. **节点位置过渡**
   - 拖动时：无动画（实时跟随）
   - 自动布局时：平滑过渡
   - 动画时长：300ms
   - 缓动函数：ease-in-out

---

## 📋 实施方案

### 方案 A：CSS Transition（推荐）

**优点**：
- 简单、性能好
- 浏览器硬件加速
- 代码量少

**缺点**：
- 控制粒度较粗
- 难以实现复杂动画

**实施步骤**：

1. 修改 `components/Node.tsx`
   - 添加 CSS transition 类
   - 添加淡入淡出动画

2. 修改 `App.tsx`（可选）
   - 添加节点创建时的初始状态
   - 添加节点删除时的动画

---

### 方案 B：Framer Motion（备选）

**优点**：
- 功能强大
- 动画控制精细
- 支持复杂动画序列

**缺点**：
- 需要安装依赖
- 增加包体积
- 学习成本

**暂不采用**（除非用户要求）

---

## 🔧 详细实施

### 步骤 1：修改 Node.tsx - 添加动画样式

**位置**：`components/Node.tsx`

**修改内容**：

```tsx
// 在节点主容器添加动画类
<div
  ref={nodeRef}
  data-node-id={node.id}
  className={`
    absolute cursor-move select-none
    bg-white border border-gray-200 rounded-lg shadow-md
    hover:shadow-lg
    // 🆕 添加动画
    transition-all duration-200 ease-out
    animate-in fade-in zoom-in-95
  `}
  style={{
    left: node.x,
    top: node.y,
    width: node.width,
    height: node.height,
    zIndex: node.zIndex,
    // 🆕 添加位置过渡（仅在非拖动时）
    transition: isDragging ? 'none' : 'left 300ms ease-in-out, top 300ms ease-in-out',
  }}
>
```

**说明**：
- `transition-all duration-200 ease-out`：所有属性过渡
- `animate-in fade-in zoom-in-95`：Tailwind 动画类（淡入 + 缩放）
- `transition: isDragging ? 'none' : '...'`：拖动时禁用动画

---

### 步骤 2：添加自定义动画类（如果 Tailwind 不支持）

**位置**：`index.html` 或 `App.tsx` 的 `<style>` 标签

**添加内容**：

```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes fadeOut {
  from {
    opacity: 1;
    transform: scale(1);
  }
  to {
    opacity: 0;
    transform: scale(0.95);
  }
}

.node-enter {
  animation: fadeIn 200ms ease-out;
}

.node-exit {
  animation: fadeOut 150ms ease-in;
}

.node-position-transition {
  transition: left 300ms ease-in-out, top 300ms ease-in-out;
}
```

---

### 步骤 3：修改 Node.tsx - 应用动画类

**修改内容**：

```tsx
// 添加状态跟踪
const [isEntering, setIsEntering] = useState(true);

useEffect(() => {
  // 节点创建后，标记为已进入
  const timer = setTimeout(() => setIsEntering(false), 200);
  return () => clearTimeout(timer);
}, []);

// 在节点主容器添加动画类
<div
  ref={nodeRef}
  data-node-id={node.id}
  className={`
    absolute cursor-move select-none
    bg-white border border-gray-200 rounded-lg shadow-md
    hover:shadow-lg
    ${isEntering ? 'node-enter' : ''}
    ${!isDragging ? 'node-position-transition' : ''}
  `}
  style={{
    left: node.x,
    top: node.y,
    width: node.width,
    height: node.height,
    zIndex: node.zIndex,
  }}
>
```

---

### 步骤 4：修改删除逻辑（可选）

**位置**：`hooks/useNodeActions.ts` 或 `App.tsx`

**目标**：删除节点前先播放动画

**修改内容**：

```tsx
const deleteNode = (nodeId: string) => {
  // 1. 添加删除动画类
  const nodeElement = document.querySelector(`[data-node-id="${nodeId}"]`);
  if (nodeElement) {
    nodeElement.classList.add('node-exit');
    
    // 2. 等待动画完成后再删除
    setTimeout(() => {
      nodeStore.getState().deleteNode(nodeId);
    }, 150); // 动画时长
  } else {
    // 如果找不到元素，直接删除
    nodeStore.getState().deleteNode(nodeId);
  }
};
```

**注意**：这一步可能需要修改多个地方的删除逻辑，建议先跳过，只实现添加动画。

---

## 🎨 视觉效果

### 添加节点

```
初始状态：opacity: 0, scale: 0.95
↓ 200ms ease-out
最终状态：opacity: 1, scale: 1
```

### 删除节点

```
初始状态：opacity: 1, scale: 1
↓ 150ms ease-in
最终状态：opacity: 0, scale: 0.95
```

### 位置过渡

```
拖动时：无动画（实时跟随）
自动布局：300ms ease-in-out 平滑过渡
```

---

## ✅ 验收标准

### 功能验收

- [ ] 新建节点时有淡入动画
- [ ] 新建节点时有缩放动画
- [ ] 拖动节点时无动画（实时跟随）
- [ ] 自动布局时节点平滑移动
- [ ] 动画流畅，无卡顿

### 性能验收

- [ ] 动画不影响拖动性能
- [ ] 大量节点时动画正常
- [ ] 内存占用无明显增加

### 兼容性验收

- [ ] Chrome 正常
- [ ] Firefox 正常
- [ ] Safari 正常

---

## 🚀 实施步骤总结

1. ✅ 创建实施文档（本文档）
2. ⏳ 修改 `components/Node.tsx` - 添加动画样式
3. ⏳ 添加自定义动画类（如果需要）
4. ⏳ 测试动画效果
5. ⏳ 修复问题（如果有）
6. ⏳ 创建完成文档

---

## 📝 注意事项

### 性能优化

1. **使用 CSS Transform**
   - 使用 `transform: scale()` 而不是 `width/height`
   - 使用 `opacity` 而不是 `visibility`
   - 原因：这两个属性可以触发 GPU 加速

2. **避免重排**
   - 不要在动画中修改 `width/height`
   - 不要在动画中修改 `left/top`（除非必要）

3. **拖动时禁用动画**
   - 拖动时设置 `transition: none`
   - 避免动画延迟影响拖动手感

### 架构原则

1. **只改 UI Layer**
   - 只修改 `components/Node.tsx`
   - 不修改 Hooks 和 Core

2. **保持功能不变**
   - 动画是视觉增强
   - 不影响现有功能

3. **可选功能**
   - 删除动画可以后续添加
   - 先实现添加动画

---

## 🎯 下一步

完成后，继续实施**功能 2：动画连接线**

---

**📅 创建时间**：2026-02-01  
**⏱️ 预计耗时**：1 小时  
**🎯 优先级**：1（最高）  
**⚠️ 风险**：无

