# 3D 相机 - 输出后自动清空 - 2026-01-31

## 问题描述

用户反馈：**每次九宫格和 3D 相机连接，都会自动导入 3D 相机上次生成的图片。**

### 期望行为

3D 相机生成完图片并输出到九宫格后，应该：
1. ✅ 自动创建九宫格节点
2. ✅ 自动连接到九宫格节点
3. ❌ **清空自己的图片数据，为下一次生成做准备**

### 实际行为

3D 相机生成完图片后：
1. ✅ 自动创建九宫格节点
2. ✅ 自动连接到九宫格节点
3. ❌ **保留了图片数据（`gridImages` 和 `image`）**
4. ❌ **下次连接新的九宫格时，旧图片会自动传递过去**

## 问题根源

### 当前流程

```typescript
// 1. 生成完成，保存图片到 3D 相机节点
handleNodeUpdate(node.id, { 
    gridImages: blobUrls,  // ❌ 保存了图片
    image: blobUrls[0]     // ❌ 保存了图片
});

// 2. 自动创建九宫格节点
const newGridNode: AppNode = {
    data: {
        inputImage: blobUrls[0],  // ✅ 使用 3D 相机的图片
    }
};

// 3. 自动连接
useConnectionStore.getState().addConnection({
    from: node.id,
    to: gridNodeId,
});

// ❌ 问题：3D 相机的图片数据没有清空
// 下次连接新的九宫格时，旧图片还在，会自动传递过去
```

### 为什么会自动传递？

在 `components/Node.tsx` 中，九宫格节点会从连接的节点获取输入图片：

```typescript
// 九宫格处理节点
if (node.type === NodeType.GRID_SPLITTER) {
    // 获取输入图片
    let inputImageSrc: string | undefined;
    
    if (node.data.inputImage) {
        // 有自己保存的图片
        inputImageSrc = node.data.inputImage;
    } else if (inputAssets && inputAssets.length > 0 && inputAssets[0].type === 'image') {
        // ❌ 从连接的节点获取（3D 相机的旧图片）
        inputImageSrc = inputAssets[0].src;
        shouldSaveInput = true;
    }
}
```

所以，如果 3D 相机的 `image` 字段还有值，新连接的九宫格节点会自动获取这个旧图片。

## 解决方案

### 修改位置

**文件：`hooks/useNodeActions.ts`**

**位置：第 828 行之后**

### 修改内容

在自动创建九宫格节点后，立即清空 3D 相机的输出数据：

```typescript
// 🔥 清空 3D 相机的输出数据，为下一次生成做准备
// 这样下次连接九宫格时，不会自动导入旧图片
handleNodeUpdate(node.id, { 
    gridImages: undefined,
    image: undefined
});

console.log('[MultiAngleCamera] 已清空输出数据，准备下一次生成');
```

### 新流程

```typescript
// 1. 生成完成，保存图片到 3D 相机节点
handleNodeUpdate(node.id, { 
    gridImages: blobUrls,
    image: blobUrls[0]
});

// 2. 自动创建九宫格节点
const newGridNode: AppNode = {
    data: {
        inputImage: blobUrls[0],  // ✅ 使用 3D 相机的图片
    }
};

// 3. 自动连接
useConnectionStore.getState().addConnection({
    from: node.id,
    to: gridNodeId,
});

// 4. ✅ 清空 3D 相机的输出数据
handleNodeUpdate(node.id, { 
    gridImages: undefined,
    image: undefined
});

// ✅ 现在 3D 相机的图片数据已清空
// 下次连接新的九宫格时，不会自动传递旧图片
```

## 测试验证

### 测试步骤

1. **第一次生成**
   - 3D 相机生成图片
   - 自动创建九宫格节点 A
   - 九宫格节点 A 接收图片并切割
   - ✅ 3D 相机的图片数据被清空

2. **第二次生成**
   - 3D 相机再次生成图片
   - 自动创建九宫格节点 B
   - 九宫格节点 B 接收新图片并切割
   - ✅ 3D 相机的图片数据被清空

3. **手动连接**
   - 创建一个新的九宫格节点 C
   - 手动连接 3D 相机 → 九宫格节点 C
   - ✅ 九宫格节点 C 不会自动导入旧图片（因为 3D 相机已清空）
   - ✅ 九宫格节点 C 显示空状态，等待用户上传或生成

### 预期结果

- ✅ 3D 相机生成完图片后，自动清空输出数据
- ✅ 下次连接九宫格时，不会自动导入旧图片
- ✅ 九宫格节点显示空状态，等待新的输入
- ✅ 3D 相机可以重复生成，每次都是新的图片

## 用户体验改进

### 修复前

```
用户：生成 3D 相机图片 → 自动创建九宫格 A
用户：再次生成 3D 相机图片 → 自动创建九宫格 B
用户：手动连接九宫格 C 到 3D 相机
结果：❌ 九宫格 C 自动导入了旧图片（第二次生成的）
```

### 修复后

```
用户：生成 3D 相机图片 → 自动创建九宫格 A → 3D 相机清空
用户：再次生成 3D 相机图片 → 自动创建九宫格 B → 3D 相机清空
用户：手动连接九宫格 C 到 3D 相机
结果：✅ 九宫格 C 显示空状态，等待新的输入
```

## 设计理念

### 为什么要清空？

1. **避免混淆**：用户不会误以为旧图片是新生成的
2. **节省内存**：不保留不需要的图片数据
3. **清晰的状态**：3D 相机的状态清晰（有图片 = 刚生成，无图片 = 已输出）
4. **符合直觉**：生成 → 输出 → 清空 → 准备下一次

### 为什么不保留？

1. **已经输出了**：图片已经传递给九宫格节点，不需要再保留
2. **可以重新生成**：如果需要，用户可以再次点击"生成"按钮
3. **避免重复使用**：防止用户误用旧图片

## 编译检查

```bash
✅ hooks/useNodeActions.ts: No diagnostics found
```

## 总结

### 修改内容

- **文件**：`hooks/useNodeActions.ts`
- **位置**：第 828 行之后
- **修改**：添加清空逻辑

### 效果

- ✅ 3D 相机生成完图片后，自动清空输出数据
- ✅ 下次连接九宫格时，不会自动导入旧图片
- ✅ 用户体验更清晰、更符合直觉
- ✅ 节省内存，避免混淆

### 下一步

- ✅ 功能完成，可以测试
- ✅ 用户可以正常使用 3D 相机和九宫格节点
- ✅ 每次生成都是新的图片，不会混淆

---

**完成时间：** 2026-01-31
