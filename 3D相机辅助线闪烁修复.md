# 3D 相机辅助线闪烁修复

## 问题描述

**现象：** 画面底部有四条线，拖动相机路过正中那条线时，画面会"切一下"（闪烁）

**用户反馈：**
> "画面底部有四条线，正中那条线，每次拖动相机路过那里的时候，画面都会切一下，为什么呢"

## 问题分析

### 四条辅助线是什么？

1. **高度参考圆环**（heightRing）- 水平圆环，显示相机高度
2. **垂直线**（verticalLine）- 从相机到地面的垂直线
3. **地面连线**（groundLine）- 从地面投影点到目标中心的线 ⚠️ **这条线会闪烁**
4. **地面投影点**（groundMarker）- 小圆点标记

### 为什么会"切一下"？

这些辅助线使用的是 **虚线材质**（`LineDashedMaterial`）：

```typescript
const groundLine = new THREE.Line(
  groundLineGeo, 
  new THREE.LineDashedMaterial({ 
    color: 0x555555, 
    dashSize: 0.2,  // 虚线段长度
    gapSize: 0.1    // 间隙长度
  })
);
groundLine.computeLineDistances(); // 计算虚线距离
```

**问题代码：**
```typescript
// ❌ 每次更新辅助线位置时都重新计算虚线距离
const groundAttr = helpers.groundLine.geometry.attributes.position;
if (groundAttr) {
  const positions = groundAttr.array as Float32Array;
  positions[0] = groundX; positions[1] = 0.02; positions[2] = groundZ;
  positions[3] = 0; positions[4] = 0.02; positions[5] = 0;
  groundAttr.needsUpdate = true;
  helpers.groundLine.computeLineDistances(); // ❌ 导致闪烁
}
```

### 为什么 `computeLineDistances()` 会导致闪烁？

`computeLineDistances()` 的作用：
- 计算线段上每个顶点的累积距离
- 用于虚线材质的渲染（决定虚线段和间隙的位置）

**问题：**
1. 每次更新顶点位置后调用 `computeLineDistances()`
2. 虚线模式会重新计算，虚线段的位置会"跳动"
3. 当相机移动时，地面连线的长度和方向都在变化
4. 虚线段的起始位置会不断重新计算，导致视觉上的"闪烁"或"切换"

### 特别是"正中那条线"

**地面连线**（groundLine）是从地面投影点到目标中心的线：
- 起点：相机在地面的投影点（会随相机移动）
- 终点：目标中心（固定在 0, 0.02, 0）

当相机旋转时：
- 地面投影点会绕着目标中心移动
- 地面连线的长度和方向都在变化
- 每次调用 `computeLineDistances()` 都会重新计算虚线模式
- 导致虚线段的位置"跳动"，产生"切一下"的效果

## 解决方案

### 核心思路

**`computeLineDistances()` 只需要在初始化时调用一次，后续更新顶点位置时不需要重新计算**

### 修复代码

#### 初始化时（保留）
```typescript
// ✅ 初始化时调用一次
const groundLine = new THREE.Line(groundLineGeo, new THREE.LineDashedMaterial({ 
  color: 0x555555, 
  dashSize: 0.2, 
  gapSize: 0.1 
}));
groundLine.computeLineDistances(); // ✅ 只在初始化时调用
helpersGroup.add(groundLine);
```

#### 更新时（移除）
```typescript
// ✅ 更新顶点位置时不重新计算
const groundAttr = helpers.groundLine.geometry.attributes.position;
if (groundAttr) {
  const positions = groundAttr.array as Float32Array;
  positions[0] = groundX; positions[1] = 0.02; positions[2] = groundZ;
  positions[3] = 0; positions[4] = 0.02; positions[5] = 0;
  groundAttr.needsUpdate = true;
  // ❌ 移除：每次更新都重新计算会导致闪烁
  // helpers.groundLine.computeLineDistances();
}
```

### 同样的修复应用到所有辅助线

1. **高度参考圆环**（heightRing）
2. **垂直线**（verticalLine）
3. **地面连线**（groundLine）

所有这些虚线都只在初始化时调用 `computeLineDistances()`，更新时不再调用。

## 技术细节

### `computeLineDistances()` 的工作原理

```typescript
// 虚线材质需要知道每个顶点的累积距离
const line = new THREE.Line(geometry, new THREE.LineDashedMaterial({
  dashSize: 0.2,  // 虚线段长度
  gapSize: 0.1    // 间隙长度
}));

// 计算累积距离
line.computeLineDistances();

// 内部会创建一个 lineDistances 属性
// lineDistances[0] = 0
// lineDistances[1] = distance(vertex[0], vertex[1])
// lineDistances[2] = distance(vertex[0], vertex[1]) + distance(vertex[1], vertex[2])
// ...
```

### 为什么初始化后不需要重新计算？

**虚线模式是相对的：**
- 虚线段和间隙的长度是固定的（dashSize, gapSize）
- 只要线段的拓扑结构不变（顶点数量和连接关系），就不需要重新计算
- 即使顶点位置变化，虚线模式也会自动适应

**只有在以下情况才需要重新计算：**
- 添加或删除顶点
- 改变顶点的连接关系
- 改变虚线段或间隙的长度

**我们的情况：**
- 只是更新顶点位置（相机移动）
- 顶点数量和连接关系不变
- 不需要重新计算

### 为什么会"切一下"？

```
初始状态：
  地面连线长度 = 5
  虚线模式：[实线 0.2] [间隙 0.1] [实线 0.2] [间隙 0.1] ...

相机移动后：
  地面连线长度 = 3
  重新计算虚线距离 → 虚线模式重新分布
  虚线段的起始位置改变 → 视觉上"跳动"

如果不重新计算：
  地面连线长度 = 3
  虚线模式保持不变（相对位置）
  虚线段平滑移动 → 没有"跳动"
```

## 修复前后对比

### 修复前 ❌

```
拖动相机
  ↓
更新辅助线顶点位置
  ↓
调用 computeLineDistances() ❌
  ↓
虚线模式重新计算
  ↓
虚线段位置"跳动"
  ↓
画面"切一下"（闪烁）
```

### 修复后 ✅

```
拖动相机
  ↓
更新辅助线顶点位置
  ↓
不调用 computeLineDistances() ✅
  ↓
虚线模式保持不变
  ↓
虚线段平滑移动
  ↓
画面流畅，没有闪烁
```

## 测试步骤

### 测试 1：拖动相机不闪烁
1. 打开多角度相机节点
2. 观察画面底部的四条辅助线
3. 拖动相机旋转一圈
4. **预期：** 辅助线平滑移动，没有"切一下"的效果

### 测试 2：路过正中线不闪烁
1. 拖动相机，让地面连线（正中那条线）从短变长
2. 观察虚线段的移动
3. **预期：** 虚线段平滑移动，没有"跳动"或"闪烁"

### 测试 3：Shift + 拖拽不闪烁
1. 按住 Shift + 拖拽推进/拉远
2. 观察辅助线的变化
3. **预期：** 辅助线平滑缩放，没有闪烁

### 测试 4：快速拖动不闪烁
1. 快速拖动相机旋转
2. 观察辅助线的变化
3. **预期：** 即使快速移动，辅助线也不会闪烁

## 其他可能的闪烁原因

如果修复后还有闪烁，可能是以下原因：

### 1. 渲染频率问题
```typescript
// 确保每次更新后都渲染
sceneRef.current.renderer.render(sceneRef.current.scene, sceneRef.current.camera);
```

### 2. BufferAttribute 更新问题
```typescript
// 确保标记为需要更新
heightRingAttr.needsUpdate = true;
```

### 3. 材质透明度问题
```typescript
// 如果材质有透明度，可能导致渲染顺序问题
new THREE.LineDashedMaterial({ 
  color: 0x666666, 
  transparent: true,  // 可能导致闪烁
  opacity: 0.5
});
```

## 总结

这次修复解决了辅助线闪烁的问题：

1. ✅ **移除不必要的 `computeLineDistances()` 调用**
   - 只在初始化时调用一次
   - 更新顶点位置时不再调用

2. ✅ **虚线模式保持稳定**
   - 虚线段的相对位置不变
   - 平滑移动，没有"跳动"

3. ✅ **画面流畅**
   - 拖动相机时没有"切一下"的效果
   - 辅助线平滑移动

**核心原则：** `computeLineDistances()` 只在初始化时调用，后续更新顶点位置时不需要重新计算！
