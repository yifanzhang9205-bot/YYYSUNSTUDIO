# 多角度相机 - 提示词优化说明

## 优化时间
2026-01-23

## 优化内容

### 1. 提示词优化
### 2. 切换到 NanoBanana API

## 问题分析

### 原有问题
1. **角度描述不精确**
   - 使用模糊的文字描述（如 "front three-quarter view"）
   - AI 对文字描述的理解可能不准确
   - 缺少具体的度数参考

2. **九宫格变化逻辑错误**
   - 距离偏移使用了列（col）索引，应该用行（row）
   - 导致距离变化方向错误（应该是上近下远，而不是左近右远）

3. **提示词过于冗长**
   - 大量重复的规则和说明
   - 可能让 AI 混淆重点
   - 特殊角度的额外说明反而增加理解难度

4. **偏移量过大**
   - 水平偏移 ±20°，垂直偏移 ±15°
   - 导致九宫格中的角度差异过大
   - 不够聚焦在目标角度周围

## 优化方案

### 1. 精确的角度描述
**改进前：**
```typescript
'front three-quarter view'
'side profile view'
'direct back view'
```

**改进后：**
```typescript
'front-right 45°'
'right side 90°'
'back 180°'
```

**优势：**
- 直接给出度数，更精确
- AI 更容易理解和执行
- 减少歧义

### 2. 修正九宫格变化逻辑
**改进前：**
```typescript
const z = Math.max(0, Math.min(10, centerZ + zOffsets[col])); // 错误：用列索引
```

**改进后：**
```typescript
const z = Math.max(0, Math.min(10, centerZ + zOffsets[row])); // 正确：用行索引
```

**九宫格布局逻辑：**
```
┌─────┬─────┬─────┐
│  1  │  2  │  3  │  ← 上排（近距离，row=0, z-0.8）
├─────┼─────┼─────┤
│  4  │  5  │  6  │  ← 中排（目标距离，row=1, z+0）
├─────┼─────┼─────┤
│  7  │  8  │  9  │  ← 下排（远距离，row=2, z+0.8）
└─────┴─────┴─────┘
   ↑     ↑     ↑
  左    中    右
 (h-15°)(h+0°)(h+15°)
```

### 3. 减小偏移量
**改进前：**
- 水平偏移：±20°
- 垂直偏移：±15°
- 距离偏移：±1

**改进后：**
- 水平偏移：±15°（减小 25%）
- 垂直偏移：±10°（减小 33%）
- 距离偏移：±0.8（减小 20%）

**优势：**
- 九宫格更聚焦在目标角度周围
- 减少角度跨度，提高一致性
- 更符合"细微变化"的设计目标

### 4. 精简提示词
**改进前：**
- 约 80 行提示词
- 大量重复的规则说明
- 特殊角度的额外指令

**改进后：**
- 约 40 行提示词
- 精简到核心要求
- 直接给出精确的度数参数

**核心改进：**
```
**TARGET CAMERA ANGLES:**
Center panel (Panel 5) is the main target:
- Azimuth: 90° (0°=front, 90°=right side, 180°=back, 270°=left side)
- Elevation: 0° (positive=looking down, 0°=eye level, negative=looking up)
- Distance: 5.0 (0=extreme close-up, 5=medium, 10=wide shot)

**9 PANEL SPECIFICATIONS:**
Panel 1: azimuth 75°, elevation 10°, distance 4.2
Panel 2: azimuth 90°, elevation 10°, distance 4.2
Panel 3: azimuth 105°, elevation 10°, distance 4.2
Panel 4: azimuth 75°, elevation 0°, distance 5.0
Panel 5: azimuth 90°, elevation 0°, distance 5.0  ← 目标
Panel 6: azimuth 105°, elevation 0°, distance 5.0
Panel 7: azimuth 75°, elevation -10°, distance 5.8
Panel 8: azimuth 90°, elevation -10°, distance 5.8
Panel 9: azimuth 105°, elevation -10°, distance 5.8
```

### 5. 简化距离描述
**改进前：**
```typescript
'extreme close-up (face only, very tight framing)'
'close-up (head and shoulders)'
'medium close-up (chest up)'
```

**改进后：**
```typescript
'extreme close-up'
'close-up'
'medium close-up'
```

**优势：**
- 去掉括号内的详细说明
- 减少提示词长度
- AI 对标准术语理解更准确

## 预期效果

### API 切换
- ✅ 使用 NanoBanana API（与其他节点一致）
- ✅ 支持图生图（通过 referenceUrls）
- ✅ 需要 ImgBB 上传参考图片

### 角度控制
- ✅ 更精确的角度控制
- ✅ 减少 AI 理解偏差
- ✅ 提高生成结果的准确性

### 九宫格布局
- ✅ 正确的距离变化方向（上近下远）
- ✅ 更聚焦的角度范围（±15° 而不是 ±20°）
- ✅ 更自然的细微变化

### 提示词质量
- ✅ 精简到核心要求
- ✅ 减少 AI 混淆的可能性
- ✅ 提高生成速度（更短的提示词）

## 技术细节

### API 调用流程
```
用户设置角度参数
    ↓
构建精确的提示词
    ↓
上传参考图片到 ImgBB（获取 URL）
    ↓
调用 NanoBanana API（传入 referenceUrls）
    ↓
下载生成的图片
    ↓
转换为 base64
    ↓
显示在节点中
```

### 角度映射表
| 度数范围 | 描述 | 说明 |
|---------|------|------|
| 0° - 22.5° | front | 正面 |
| 22.5° - 67.5° | front-right 45° | 右前 45° |
| 67.5° - 112.5° | right side 90° | 右侧 90° |
| 112.5° - 157.5° | back-right 135° | 右后 135° |
| 157.5° - 202.5° | back 180° | 背面 180° |
| 202.5° - 247.5° | back-left 225° | 左后 225° |
| 247.5° - 292.5° | left side 270° | 左侧 270° |
| 292.5° - 337.5° | front-left 315° | 左前 315° |

### 仰角映射表
| 度数范围 | 描述 | 说明 |
|---------|------|------|
| 80° ~ 90° | top-down view | 俯视 |
| 40° ~ 80° | high angle | 高角度 |
| 10° ~ 40° | slightly high angle | 微高角度 |
| -10° ~ 10° | eye level | 平视 |
| -30° ~ -10° | slightly low angle | 微低角度 |
| -60° ~ -30° | low angle | 低角度 |
| -90° ~ -60° | bottom-up view | 仰视 |

### 距离映射表
| 数值范围 | 描述 | 说明 |
|---------|------|------|
| 0 - 0.5 | extreme close-up | 极近特写 |
| 0.5 - 1.5 | close-up | 特写 |
| 1.5 - 3 | medium close-up | 近景 |
| 3 - 4.5 | medium shot | 半身 |
| 4.5 - 6 | medium full shot | 中景 |
| 6 - 7.5 | full shot | 全身 |
| 7.5 - 9 | wide shot | 远景 |
| 9 - 10 | extreme wide shot | 全景 |

## 使用建议

### 配置 ImgBB（必需）
多角度相机现在使用 NanoBanana API，需要通过 ImgBB 上传参考图片：

1. 访问 https://api.imgbb.com/ 获取 API Key
2. 在 `.env.local` 中添加：
   ```
   IMGBB_API_KEY=你的_api_key
   ```
3. 重启开发服务器

详细配置步骤请参考 [ImgBB配置指南.md](./ImgBB配置指南.md)

### 最佳实践
1. **选择标准角度**
   - 使用预设角度（0°, 45°, 90° 等）
   - 避免使用过于极端的角度

2. **合理的距离设置**
   - 特写：0-2
   - 中景：4-6
   - 远景：7-10

3. **补充描述**
   - 在"补充描述"中添加光照、材质等细节
   - 避免重复角度信息（已自动生成）

### 测试建议
1. **测试标准角度**
   - 正面（0°）
   - 侧面（90°）
   - 背面（180°）

2. **测试不同距离**
   - 特写（2）
   - 中景（5）
   - 远景（8）

3. **对比优化前后**
   - 角度准确性
   - 九宫格一致性
   - 生成速度

## 后续优化方向

### 可能的改进
1. **自适应偏移量**
   - 根据距离自动调整偏移量
   - 近景用更小的偏移，远景用更大的偏移

2. **角度预览**
   - 在 3D 视图中显示九宫格的 9 个角度
   - 让用户直观看到变化范围

3. **批量生成**
   - 一次生成多个不同角度的九宫格
   - 提供更多选择

4. **提示词模板**
   - 允许用户自定义提示词模板
   - 保存常用的提示词配置

---

**优化完成！** 🎉

现在的提示词更精确、更简洁、更易于 AI 理解。
