# 组功能增强方案

## 当前问题
1. 组的大小固定，无法动态扩展
2. 无法向已有组添加新节点
3. 缺少对齐和排列工具
4. 无法批量调整节点大小

## 改进方案

### 1. 动态扩展组
**行为**：
- 框选时，如果包含已有组的节点 → 扩大该组，包含所有新选中的节点
- 不创建新组，而是更新现有组的边界

**实现**：
```typescript
// 在 handleGlobalMouseUp 中
if (enclosed.length > 0) {
    // 检查是否有节点已经在组中
    const existingGroup = groups.find(g => 
        enclosed.some(n => isNodeInGroup(n, g))
    );
    
    if (existingGroup) {
        // 扩展现有组
        updateGroupBounds(existingGroup.id, [...existingGroup.nodeIds, ...newNodeIds]);
    } else {
        // 创建新组
        createNewGroup(enclosed);
    }
}
```

### 2. 组工具栏 UI

#### 位置
- 浮动在组的顶部中央
- 跟随组移动
- 选中组时显示，取消选中时隐藏

#### 布局
```
┌─────────────────────────────────────────────────┐
│  [≡] [+] [≣]  |  [⊤] [⊥] [⊢]  |  [|||] [≡≡] [:::] │
└─────────────────────────────────────────────────┘
   对齐          分布           排列
```

#### 功能列表

**对齐工具**：
1. 左对齐 (Alt+A) - 所有节点左边缘对齐
2. 水平居中 (Alt+H) - 所有节点水平居中对齐
3. 右对齐 (Alt+D) - 所有节点右边缘对齐
4. 顶部对齐 (Alt+W) - 所有节点顶部对齐
5. 垂直居中 (Alt+V) - 所有节点垂直居中对齐
6. 底部对齐 (Alt+S) - 所有节点底部对齐

**分布工具**：
1. 水平间距分布 (Shift+H) - 节点之间水平间距相等
2. 垂直间距分布 (Shift+V) - 节点之间垂直间距相等
3. 自动排列 (Shift+A) - 网格布局，自动计算最佳行列数

**大小调整**：
- 滑块：统一缩放所有节点（50% - 200%）
- 保持宽高比
- 实时预览

### 3. 实现细节

#### 对齐算法
```typescript
// 左对齐
const minX = Math.min(...nodes.map(n => n.x));
nodes.forEach(n => n.x = minX);

// 水平居中
const centerX = (Math.min(...nodes.map(n => n.x)) + Math.max(...nodes.map(n => n.x + n.width))) / 2;
nodes.forEach(n => n.x = centerX - n.width / 2);

// 右对齐
const maxX = Math.max(...nodes.map(n => n.x + n.width));
nodes.forEach(n => n.x = maxX - n.width);
```

#### 分布算法
```typescript
// 水平间距分布
const sorted = nodes.sort((a, b) => a.x - b.x);
const totalWidth = sorted.reduce((sum, n) => sum + n.width, 0);
const availableSpace = (maxX - minX) - totalWidth;
const gap = availableSpace / (nodes.length - 1);

let currentX = minX;
sorted.forEach(n => {
    n.x = currentX;
    currentX += n.width + gap;
});
```

#### 自动排列算法
```typescript
// 计算最佳行列数
const nodeCount = nodes.length;
const cols = Math.ceil(Math.sqrt(nodeCount));
const rows = Math.ceil(nodeCount / cols);

// 网格布局
const gap = 20; // 间距
nodes.forEach((n, i) => {
    const col = i % cols;
    const row = Math.floor(i / cols);
    n.x = groupX + col * (nodeWidth + gap);
    n.y = groupY + row * (nodeHeight + gap);
});
```

#### 大小调整
```typescript
// 统一缩放
const scale = sliderValue / 100; // 0.5 - 2.0
nodes.forEach(n => {
    n.width = originalWidth * scale;
    n.height = originalHeight * scale;
});
```

### 4. UI 组件设计

```tsx
<GroupToolbar 
    groupId={selectedGroupId}
    position={{ x: group.x + group.width / 2, y: group.y - 60 }}
    onAlign={(type) => handleAlign(type)}
    onDistribute={(type) => handleDistribute(type)}
    onArrange={() => handleAutoArrange()}
    onScaleChange={(scale) => handleScaleNodes(scale)}
/>
```

### 5. 快捷键绑定

```typescript
useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
        if (!selectedGroupId) return;
        
        if (e.altKey) {
            switch(e.key) {
                case 'a': handleAlign('left'); break;
                case 'h': handleAlign('center-h'); break;
                case 'd': handleAlign('right'); break;
                case 'w': handleAlign('top'); break;
                case 'v': handleAlign('center-v'); break;
                case 's': handleAlign('bottom'); break;
            }
        }
        
        if (e.shiftKey) {
            switch(e.key) {
                case 'h': handleDistribute('horizontal'); break;
                case 'v': handleDistribute('vertical'); break;
                case 'a': handleAutoArrange(); break;
            }
        }
    };
    
    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
}, [selectedGroupId]);
```

## 实施步骤

1. ✅ 修改框选逻辑，支持动态扩展组
2. ✅ 创建 GroupToolbar 组件
3. ✅ 实现对齐功能
4. ✅ 实现分布功能
5. ✅ 实现自动排列功能
6. ✅ 实现大小调整功能
7. ✅ 添加快捷键支持
8. ✅ 测试和优化

## 注意事项

- 所有操作都要支持撤销（saveHistory）
- 操作时要考虑节点的实际高度（getApproxNodeHeight）
- 自动排列要考虑连接线，尽量减少交叉
- 大小调整要保持节点的宽高比
- 工具栏要跟随组移动和缩放

## 预期效果

用户体验：
1. 框选节点 → 自动创建/扩展组
2. 选中组 → 顶部出现工具栏
3. 点击对齐按钮 → 节点立即对齐
4. 拖动滑块 → 所有节点同步缩放
5. 按快捷键 → 快速执行操作

性能：
- 所有操作都是 O(n)，n 为组内节点数
- 使用 transform 优化动画
- 批量更新，减少重渲染
