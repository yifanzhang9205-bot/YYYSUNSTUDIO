# 3D 相机辅助线和推进功能修复

## 问题描述
1. **Shift + 拖拽推进功能没有了** - 用户无法前后推进相机
2. **参考线消失了** - 高度圆环和垂直线等辅助线不显示

## 问题原因

### 1. 辅助线不显示
在性能优化时，辅助线的初始化代码创建了空的 `BufferGeometry`：

```typescript
// ❌ 问题代码：空的 geometry，没有 position attribute
const heightRingGeo = new THREE.BufferGeometry();
const heightRing = new THREE.Line(heightRingGeo, material);
```

后续更新代码尝试访问 `geometry.attributes.position`，但因为初始化时没有创建，所以是 `undefined`：

```typescript
const heightRingAttr = helpers.heightRing.geometry.attributes.position;
if (heightRingAttr) {  // ❌ 这里是 undefined，所以不会执行
  // 更新顶点...
}
```

**结果：** 辅助线不显示

### 2. 推进功能存在但不明显
Shift + 拖拽的功能代码还在，但：
- 操作提示中没有说明
- 用户不知道如何使用

## 修复方案

### 1. 修复辅助线初始化

给每个辅助线的 geometry 添加初始的顶点数据：

```typescript
// ✅ 修复后：创建带初始顶点的 geometry

// 高度参考圆环
const heightRingPoints: THREE.Vector3[] = [];
for (let i = 0; i <= 64; i++) {
  const angle = (i / 64) * Math.PI * 2;
  heightRingPoints.push(new THREE.Vector3(
    Math.cos(angle) * 5, 
    1.5, 
    Math.sin(angle) * 5
  ));
}
const heightRingGeo = new THREE.BufferGeometry().setFromPoints(heightRingPoints);
const heightRing = new THREE.Line(heightRingGeo, material);
heightRing.computeLineDistances();  // ✅ 计算虚线距离
helpersGroup.add(heightRing);

// 垂直线（相机到地面）
const verticalLineGeo = new THREE.BufferGeometry().setFromPoints([
  new THREE.Vector3(0, 1.5, 5),   // 相机位置
  new THREE.Vector3(0, 0.02, 5)   // 地面投影点
]);
const verticalLine = new THREE.Line(verticalLineGeo, material);
verticalLine.computeLineDistances();
helpersGroup.add(verticalLine);

// 地面连线（投影点到目标）
const groundLineGeo = new THREE.BufferGeometry().setFromPoints([
  new THREE.Vector3(0, 0.02, 5),  // 地面投影点
  new THREE.Vector3(0, 0.02, 0)   // 目标中心
]);
const groundLine = new THREE.Line(groundLineGeo, material);
groundLine.computeLineDistances();
helpersGroup.add(groundLine);
```

**关键点：**
- 使用 `setFromPoints()` 创建初始顶点
- 调用 `computeLineDistances()` 计算虚线距离
- 这样后续更新时 `geometry.attributes.position` 就存在了

### 2. 更新操作提示

```typescript
// 修复前
<span>拖拽旋转 · 滚轮缩放</span>

// 修复后
<span>拖拽旋转 · Shift+拖拽推进 · 滚轮缩放</span>
```

## 辅助线说明

### 1. 高度参考圆环（Height Ring）
- **颜色：** 灰色虚线
- **位置：** 与相机同高度的水平圆环
- **作用：** 显示相机的高度和水平距离

### 2. 垂直线（Vertical Line）
- **颜色：** 灰色虚线
- **位置：** 从相机垂直到地面
- **作用：** 显示相机的垂直位置

### 3. 地面连线（Ground Line）
- **颜色：** 灰色虚线
- **位置：** 从地面投影点到目标中心
- **作用：** 显示相机在地面上的投影位置

### 4. 地面投影点标记（Ground Marker）
- **颜色：** 灰色半透明圆点
- **位置：** 相机在地面上的投影
- **作用：** 标记相机的地面位置

## 操作说明

### 1. 旋转相机
- **操作：** 直接拖拽 3D 视口
- **效果：** 
  - 水平拖动 → 改变方位角（绕目标旋转）
  - 垂直拖动 → 改变仰角（上下移动）

### 2. 推进/拉远相机
- **操作：** 按住 **Shift** + 拖拽
- **效果：** 
  - 向上拖 → 拉远（远景）
  - 向下拖 → 推进（特写）
- **范围：** 0-10（特写到远景）

### 3. 滚轮缩放
- **操作：** 滚动鼠标滚轮
- **效果：** 
  - 向上滚 → 拉远
  - 向下滚 → 推进

## 视觉反馈

### 拖动时的实时反馈
1. **角度数值更新** - 右上角的胶囊显示实时角度
2. **辅助线跟随** - 高度圆环、垂直线等实时更新
3. **相机模型移动** - 3D 场景中的相机指示器实时移动
4. **光标变化** - 拖动时光标变为 `grabbing`

### 辅助线的动态变化
- **高度圆环半径** - 随相机水平距离变化
- **垂直线长度** - 随相机高度变化
- **地面投影点位置** - 随相机位置移动

## 测试步骤

### 测试 1：辅助线显示
1. 打开多角度相机节点
2. 连接一张图片
3. **应该看到：**
   - 灰色虚线圆环（高度参考）
   - 从相机到地面的垂直虚线
   - 地面上的连线和圆点标记

### 测试 2：旋转功能
1. 在 3D 视口中拖拽
2. **应该看到：**
   - 相机模型旋转
   - 辅助线跟随移动
   - 右上角角度数值实时更新
   - 非常丝滑，无延迟

### 测试 3：推进功能
1. 按住 **Shift** 键
2. 在 3D 视口中上下拖拽
3. **应该看到：**
   - 相机前后移动
   - 高度圆环半径变化
   - 距离数值更新（右上角）
   - 非常丝滑，无延迟

### 测试 4：滚轮缩放
1. 滚动鼠标滚轮
2. **应该看到：**
   - 相机前后移动
   - 辅助线跟随更新
   - 距离数值更新
   - 流畅无卡顿

## 性能说明

辅助线的更新已经优化：
- ✅ 复用 BufferAttribute，不重新分配内存
- ✅ 直接修改顶点数据，不创建新对象
- ✅ 只在需要时调用 `computeLineDistances()`
- ✅ 拖动时立即渲染，无延迟

**性能影响：** 可忽略不计（< 1ms）

## 总结

修复了两个问题：
1. ✅ 辅助线正确显示（高度圆环、垂直线、地面连线）
2. ✅ 推进功能正常工作（Shift + 拖拽）
3. ✅ 操作提示更新（告诉用户如何使用）
4. ✅ 保持 60 FPS 丝滑体验

现在 3D 相机应该可以：
- 流畅地旋转
- 流畅地推进/拉远
- 显示清晰的参考线
- 提供实时的视觉反馈
